"""Instalador autocontenido y reversible de DIAGPROG5 V107."""
import ast
import os
from pathlib import Path
import runpy
import shutil
import tempfile
import time

TARGET_VERSION = 107
OUTPUT_NAME = 'bot_wallapop_profesional_v107_ADMIN_INSTALACION_SEGURA.py'
MAIN_SOURCE = 'import customtkinter as ctk\nfrom PIL import Image, ImageDraw, ImageFont, ImageEnhance, ImageFilter\nimport os\nimport time\nimport random\nimport math\nimport threading\nimport glob\nimport json\nimport hashlib\nfrom pathlib import Path\nimport webbrowser\nimport mimetypes\nimport subprocess\nimport urllib.request\nimport re\nimport sys\nimport gc\nimport csv\nimport base64\nimport uuid\nimport platform\nimport importlib.util\nimport shutil\nimport zipfile\nimport socket\nfrom http.server import BaseHTTPRequestHandler, ThreadingHTTPServer\nfrom tkinter import messagebox\n\ntry:\n    from playwright.sync_api import sync_playwright, TimeoutError as PlaywrightTimeoutError\nexcept Exception:\n    sync_playwright = None\n    PlaywrightTimeoutError = Exception\n\n\ntry:\n    from cryptography.hazmat.primitives.asymmetric.ed25519 import Ed25519PublicKey\nexcept Exception:\n    Ed25519PublicKey = None\n\n\ntry:\n    from cryptography.hazmat.primitives.serialization import load_pem_private_key\nexcept Exception:\n    load_pem_private_key = None\n\n\n\ndef resource_path(nombre):\n    """\n    Devuelve la ruta correcta tanto ejecutando el .py como dentro de PyInstaller.\n    """\n    try:\n        base = sys._MEIPASS\n    except Exception:\n        base = os.path.abspath(".")\n    return os.path.join(base, nombre)\n\n\ndef asset_path(nombre):\n    """\n    Busca recursos visuales de forma robusta.\n\n    Orden de búsqueda:\n    1. Carpeta del script .py\n    2. Carpeta de recursos de PyInstaller (_MEIPASS)\n    3. Carpeta de trabajo actual\n    4. Carpeta padre del script\n    5. Carpeta Downloads/Descargas del usuario como último recurso\n    """\n    candidatos = []\n\n    # 1) Junto al script\n    try:\n        candidatos.append(\n            Path(__file__).resolve().parent / nombre\n        )\n    except Exception:\n        pass\n\n    # 2) PyInstaller\n    try:\n        candidatos.append(\n            Path(sys._MEIPASS) / nombre\n        )\n    except Exception:\n        pass\n\n    # 3) CWD\n    try:\n        candidatos.append(\n            Path.cwd() / nombre\n        )\n    except Exception:\n        pass\n\n    # 4) Padre del script\n    try:\n        candidatos.append(\n            Path(__file__).resolve().parent.parent / nombre\n        )\n    except Exception:\n        pass\n\n    # 5) Cache persistente de recursos de DIAGPROG5\n    try:\n        _raiz_assets = (\n            Path(\n                os.environ.get("LOCALAPPDATA")\n                or os.environ.get("APPDATA")\n                or Path.home()\n            )\n            / "DIAGPROG5"\n            / "assets"\n        )\n        candidatos.append(\n            _raiz_assets / nombre\n        )\n    except Exception:\n        pass\n\n    # 6) Descargas/Downloads\n    try:\n        home = Path.home()\n        for carpeta in ("Downloads", "Descargas"):\n            candidatos.append(\n                home / carpeta / nombre\n            )\n    except Exception:\n        pass\n\n    # 6) resource_path antiguo\n    try:\n        candidatos.append(\n            Path(resource_path(nombre))\n        )\n    except Exception:\n        pass\n\n    vistos = set()\n\n    for ruta in candidatos:\n        try:\n            ruta = ruta.resolve()\n        except Exception:\n            ruta = Path(ruta)\n\n        clave = str(ruta).lower()\n\n        if clave in vistos:\n            continue\n\n        vistos.add(clave)\n\n        try:\n            if ruta.exists() and ruta.is_file():\n                return str(ruta)\n        except Exception:\n            pass\n\n    # Devuelve ruta junto al script como referencia principal\n    try:\n        return str(\n            Path(__file__).resolve().parent / nombre\n        )\n    except Exception:\n        return nombre\n\n\n# =========================================================\n# DIRECTORIOS DE DATOS SEGUROS\n# =========================================================\n\ndef _carpeta_datos_usuario():\n    """\n    Datos de trabajo en una ruta escribible del usuario.\n    Evita WinError 5 cuando el bot está en Escritorio, Program Files,\n    carpetas sincronizadas o ubicaciones protegidas.\n    """\n    raiz = (\n        os.environ.get("LOCALAPPDATA")\n        or os.environ.get("APPDATA")\n        or os.path.expanduser("~")\n    )\n\n    ruta = Path(raiz) / "DIAGPROG5"\n\n    try:\n        ruta.mkdir(\n            parents=True,\n            exist_ok=True\n        )\n\n        prueba = ruta / ".__write_test.tmp"\n        prueba.write_text(\n            "ok",\n            encoding="utf-8"\n        )\n        prueba.unlink(\n            missing_ok=True\n        )\n\n        return str(\n            ruta.resolve()\n        )\n\n    except Exception:\n        # Último recurso: carpeta del usuario.\n        ruta = Path.home() / "DIAGPROG5"\n        ruta.mkdir(\n            parents=True,\n            exist_ok=True\n        )\n        return str(\n            ruta.resolve()\n        )\n\n\nCARPETA_DATOS_USUARIO = _carpeta_datos_usuario()\nCARPETA_ANUNCIOS = os.path.join(\n    CARPETA_DATOS_USUARIO,\n    "anuncios_generados"\n)\nCARPETA_BACKUPS_SEGURA = os.path.join(\n    CARPETA_DATOS_USUARIO,\n    "backups"\n)\n\nos.makedirs(\n    CARPETA_ANUNCIOS,\n    exist_ok=True\n)\nos.makedirs(\n    CARPETA_BACKUPS_SEGURA,\n    exist_ok=True\n)\n\n\n# =========================================================\n# SISTEMA DE LICENCIAS · DIAGPROG5\n# =========================================================\n\nLICENCIA_PUBLIC_KEY_B64 = "bsQYrQjavR4KYljXhlrz49WCm3hMstEoiRdnDjAIngA="\n\nARCHIVO_SERVIDOR_LICENCIAS = "license_server.json"\nARCHIVO_ESTADO_ONLINE = "license_online_state.json"\nTOLERANCIA_OFFLINE_HORAS = 72\n\nDIAGPROG5_EDITION = "ADMIN"\nADMIN_PRIVATE_KEY_FILE = "diagprog5_private_key.pem"\n\ndef es_edicion_admin():\n    return str(DIAGPROG5_EDITION).upper() == "ADMIN"\n\n\ndef _ruta_clave_privada_admin():\n    return Path(__file__).resolve().parent / ADMIN_PRIVATE_KEY_FILE\n\n\ndef clave_privada_admin_disponible():\n    return (\n        es_edicion_admin()\n        and load_pem_private_key is not None\n        and _ruta_clave_privada_admin().exists()\n    )\n\n\n\n\ndef _config_servidor_licencias():\n    """\n    Configuración del backend online.\n    Se lee desde license_server.json junto al bot.\n    """\n    ruta = Path(ARCHIVO_SERVIDOR_LICENCIAS)\n\n    if not ruta.exists():\n        return {\n            "enabled": False,\n            "api_url": "",\n            "timeout": 10,\n        }\n\n    try:\n        datos = json.loads(\n            ruta.read_text(\n                encoding="utf-8"\n            )\n        )\n\n        return {\n            "enabled": bool(\n                datos.get(\n                    "enabled",\n                    False\n                )\n            ),\n            "api_url": str(\n                datos.get(\n                    "api_url",\n                    ""\n                )\n            ).rstrip("/"),\n            "timeout": int(\n                datos.get(\n                    "timeout",\n                    10\n                )\n            ),\n        }\n\n    except Exception:\n        return {\n            "enabled": False,\n            "api_url": "",\n            "timeout": 10,\n        }\n\n\ndef _ruta_estado_online_local():\n    return _carpeta_licencia_local() / ARCHIVO_ESTADO_ONLINE\n\n\ndef _cargar_estado_online():\n    ruta = _ruta_estado_online_local()\n\n    if not ruta.exists():\n        return {}\n\n    try:\n        return json.loads(\n            ruta.read_text(\n                encoding="utf-8"\n            )\n        )\n    except Exception:\n        return {}\n\n\ndef _guardar_estado_online(datos):\n    ruta = _ruta_estado_online_local()\n\n    temporal = ruta.with_suffix(\n        ".tmp"\n    )\n\n    temporal.write_text(\n        json.dumps(\n            datos,\n            ensure_ascii=False,\n            indent=2\n        ),\n        encoding="utf-8"\n    )\n\n    os.replace(\n        temporal,\n        ruta\n    )\n\n\ndef _post_json_licencias(endpoint, payload):\n    cfg = _config_servidor_licencias()\n\n    if not cfg["enabled"] or not cfg["api_url"]:\n        raise RuntimeError(\n            "Servidor de licencias online no configurado."\n        )\n\n    cuerpo = json.dumps(\n        payload,\n        ensure_ascii=False\n    ).encode("utf-8")\n\n    req = urllib.request.Request(\n        cfg["api_url"] + endpoint,\n        data=cuerpo,\n        headers={\n            "Content-Type": "application/json",\n            "User-Agent": "DIAGPROG5-LicenseClient/1.0",\n        },\n        method="POST"\n    )\n\n    with urllib.request.urlopen(\n        req,\n        timeout=cfg["timeout"]\n    ) as resp:\n        raw = resp.read().decode(\n            "utf-8"\n        )\n\n    datos = json.loads(\n        raw\n    )\n\n    if not isinstance(\n        datos,\n        dict\n    ):\n        raise RuntimeError(\n            "Respuesta inválida del servidor de licencias."\n        )\n\n    return datos\n\n\ndef licencia_requiere_online(datos):\n    """\n    Las licencias V86 creadas como online usan online=True.\n    Las licencias antiguas V67/V86 siguen funcionando offline.\n    """\n    return bool(\n        (datos or {}).get(\n            "online",\n            False\n        )\n    )\n\n\ndef _horas_desde_timestamp_iso(valor):\n    try:\n        ultima = time.strptime(\n            valor,\n            "%Y-%m-%dT%H:%M:%S"\n        )\n        ultima_ts = time.mktime(\n            ultima\n        )\n        return max(\n            0.0,\n            (\n                time.time()\n                - ultima_ts\n            )\n            / 3600.0\n        )\n    except Exception:\n        return 999999.0\n\n\ndef activar_o_comprobar_online(datos_licencia):\n    """\n    Activa/comprueba la licencia en el servidor central.\n    El backend aplica max_pcs y revocación.\n    """\n    token = cargar_token_licencia()\n\n    respuesta = _post_json_licencias(\n        "/activate",\n        {\n            "license_id": datos_licencia.get(\n                "license_id",\n                ""\n            ),\n            "machine": obtener_codigo_equipo(),\n            "plan": datos_licencia.get(\n                "plan",\n                ""\n            ),\n            "token": token,\n        }\n    )\n\n    if not bool(\n        respuesta.get(\n            "ok",\n            False\n        )\n    ):\n        return False, str(\n            respuesta.get(\n                "message",\n                "Licencia rechazada por el servidor."\n            )\n        )\n\n    estado = {\n        "license_id": datos_licencia.get(\n            "license_id",\n            ""\n        ),\n        "machine": obtener_codigo_equipo(),\n        "last_ok": time.strftime(\n            "%Y-%m-%dT%H:%M:%S"\n        ),\n        "plan": respuesta.get(\n            "plan",\n            datos_licencia.get(\n                "plan",\n                ""\n            )\n        ),\n        "max_pcs": respuesta.get(\n            "max_pcs",\n            datos_licencia.get(\n                "max_pcs",\n                1\n            )\n        ),\n        "active_pcs": respuesta.get(\n            "active_pcs",\n            1\n        ),\n    }\n\n    _guardar_estado_online(\n        estado\n    )\n\n    return True, estado\n\n\ndef comprobar_licencia_online_o_gracia(datos_licencia):\n    """\n    Intenta validar online.\n    Si Internet falla, permite trabajar hasta 72h desde la última\n    comprobación online correcta.\n    """\n    try:\n        ok, resultado = activar_o_comprobar_online(\n            datos_licencia\n        )\n\n        if ok:\n            return True, resultado\n\n        # El servidor respondió y la rechazó: NO usamos gracia offline.\n        return False, resultado\n\n    except Exception as e:\n        estado = _cargar_estado_online()\n\n        if (\n            estado.get(\n                "license_id"\n            )\n            == datos_licencia.get(\n                "license_id"\n            )\n            and estado.get(\n                "machine"\n            )\n            == obtener_codigo_equipo()\n        ):\n            horas = _horas_desde_timestamp_iso(\n                estado.get(\n                    "last_ok",\n                    ""\n                )\n            )\n\n            if horas <= TOLERANCIA_OFFLINE_HORAS:\n                return True, {\n                    "offline_grace": True,\n                    "hours_since_check": horas,\n                    "message": (\n                        "Servidor no disponible. "\n                        "Usando tolerancia offline temporal."\n                    ),\n                }\n\n        return (\n            False,\n            "No pude contactar con el servidor de licencias y "\n            "no existe una comprobación online válida dentro de las "\n            f"últimas {TOLERANCIA_OFFLINE_HORAS} horas.\\n\\n"\n            f"Detalle: {e}"\n        )\n\n\ndef admin_generar_license_id():\n    alfabeto = "ABCDEFGHJKLMNPQRSTUVWXYZ23456789"\n    return "DP5-" + "-".join(\n        "".join(secrets.choice(alfabeto) for _ in range(4))\n        for _ in range(3)\n    )\n\n\ndef admin_b64url(data):\n    return base64.urlsafe_b64encode(\n        data\n    ).decode("ascii").rstrip("=")\n\n\ndef admin_generar_token_licencia(\n    machine,\n    customer,\n    plan,\n    dias,\n    permanente,\n    online\n):\n    if not es_edicion_admin():\n        raise RuntimeError("Solo disponible en ADMIN.")\n\n    if load_pem_private_key is None:\n        raise RuntimeError("Falta cryptography.")\n\n    key_path = _ruta_clave_privada_admin()\n\n    if not key_path.exists():\n        raise RuntimeError(\n            "No encuentro diagprog5_private_key.pem junto al bot ADMIN."\n        )\n\n    plan = str(plan or "PRO").upper().strip()\n\n    if plan not in ("BASICA", "PRO", "TALLER", "VITALICIA"):\n        raise RuntimeError("Plan no válido.")\n\n    customer = str(customer or "").strip() or "Cliente"\n    machine = str(machine or "").strip()\n\n    if not online and not machine.startswith("DP5PC-"):\n        raise RuntimeError(\n            "Para OFFLINE pega el código DP5PC del cliente."\n        )\n\n    try:\n        dias = max(1, int(dias))\n    except Exception:\n        dias = 365\n\n    payload = {\n        "v": 2,\n        "license_id": admin_generar_license_id(),\n        "customer": customer,\n        "plan": plan,\n        "machine": "" if online else machine,\n        "issued": time.strftime("%Y-%m-%d"),\n        "expires": (\n            ""\n            if permanente\n            else time.strftime(\n                "%Y-%m-%d",\n                time.localtime(\n                    time.time() + dias * 86400\n                )\n            )\n        ),\n        "permanent": bool(permanente),\n        "online": bool(online),\n        "max_pcs": 3 if plan == "TALLER" else 1,\n    }\n\n    private_key = load_pem_private_key(\n        key_path.read_bytes(),\n        password=None\n    )\n\n    payload_raw = json.dumps(\n        payload,\n        separators=(",", ":"),\n        ensure_ascii=False\n    ).encode("utf-8")\n\n    signature = private_key.sign(\n        payload_raw\n    )\n\n    token = (\n        LICENCIA_PREFIJO\n        + "."\n        + admin_b64url(payload_raw)\n        + "."\n        + admin_b64url(signature)\n    )\n\n    return token, payload\n\n\ndef _admin_server_config():\n    ruta = Path("license_admin_server.json")\n\n    if not ruta.exists():\n        return {}\n\n    try:\n        return json.loads(\n            ruta.read_text(encoding="utf-8")\n        )\n    except Exception:\n        return {}\n\n\ndef admin_post_online(endpoint, payload):\n    cfg = _admin_server_config()\n\n    api_url = str(\n        cfg.get("api_url", "")\n    ).rstrip("/")\n\n    secret = str(\n        cfg.get("admin_secret", "")\n    ).strip()\n\n    if (\n        not api_url\n        or not secret\n        or "TU-WORKER" in api_url\n        or "CAMBIA-ESTO" in secret\n    ):\n        return False, (\n            "Servidor online todavía no configurado. "\n            "Cuando despleguemos Cloudflare funcionará este botón."\n        )\n\n    req = urllib.request.Request(\n        api_url + endpoint,\n        data=json.dumps(payload).encode("utf-8"),\n        headers={\n            "Content-Type": "application/json",\n            "X-Admin-Secret": secret,\n            "User-Agent": "DIAGPROG5-Admin/1.0",\n        },\n        method="POST"\n    )\n\n    try:\n        with urllib.request.urlopen(req, timeout=15) as resp:\n            datos = json.loads(\n                resp.read().decode("utf-8")\n            )\n\n        return bool(datos.get("ok")), str(\n            datos.get(\n                "message",\n                "Operación completada."\n            )\n        )\n\n    except Exception as e:\n        return False, str(e)\n\n\ndef admin_registrar_licencia_online(payload):\n    if not payload.get("online"):\n        return True, "Licencia offline: no necesita servidor."\n\n    return admin_post_online(\n        "/admin/upsert-license",\n        {\n            "license_id": payload.get("license_id", ""),\n            "customer": payload.get("customer", ""),\n            "plan": payload.get("plan", ""),\n            "max_pcs": payload.get("max_pcs", 1),\n            "expires": payload.get("expires", ""),\n            "permanent": payload.get("permanent", False),\n            "revoked": False,\n        }\n    )\n\n\ndef admin_accion_online(accion, license_id):\n    endpoint = {\n        "revocar": "/admin/revoke-license",\n        "desrevocar": "/admin/unrevoke-license",\n        "reset": "/admin/reset-machines",\n    }.get(accion)\n\n    if not endpoint:\n        return False, "Acción no reconocida."\n\n    if not str(license_id).strip():\n        return False, "Introduce un ID de licencia."\n\n    return admin_post_online(\n        endpoint,\n        {"license_id": str(license_id).strip()}\n    )\n\n\ndef comprobar_estado_online_manual():\n    token = cargar_token_licencia()\n    ok, datos = verificar_token_licencia(\n        token\n    )\n\n    if not ok:\n        messagebox.showerror(\n            "Licencia",\n            str(datos)\n        )\n        return\n\n    if not licencia_requiere_online(\n        datos\n    ):\n        messagebox.showinfo(\n            "Licencia",\n            "Esta licencia es del sistema offline V67/V86."\n        )\n        return\n\n    ok_online, detalle = comprobar_licencia_online_o_gracia(\n        datos\n    )\n\n    if ok_online:\n        if isinstance(\n            detalle,\n            dict\n        ) and detalle.get(\n            "offline_grace"\n        ):\n            mensaje = (\n                "Licencia válida mediante tolerancia offline.\\n"\n                f"Horas desde la última comprobación: "\n                f"{detalle.get(\'hours_since_check\', 0):.1f}"\n            )\n        else:\n            mensaje = (\n                "Licencia online válida.\\n"\n                f"PCs activos: {detalle.get(\'active_pcs\', \'—\')} / "\n                f"{detalle.get(\'max_pcs\', \'—\')}"\n            )\n\n        messagebox.showinfo(\n            "Licencia online",\n            mensaje\n        )\n    else:\n        messagebox.showerror(\n            "Licencia online",\n            str(detalle)\n        )\n\nLICENCIA_PREFIJO = "DP5L1"\nlicencia_actual_info = {}\n\n\ndef _carpeta_licencia_local():\n    base_app = os.environ.get(\n        "APPDATA",\n        os.path.expanduser("~")\n    )\n\n    ruta = Path(base_app) / "DIAGPROG5"\n    ruta.mkdir(\n        parents=True,\n        exist_ok=True\n    )\n\n    return ruta\n\n\ndef _ruta_licencia_local():\n    return _carpeta_licencia_local() / "license_token.txt"\n\n\ndef obtener_codigo_equipo():\n    """\n    Genera un identificador estable del PC sin exponer el MachineGuid real.\n    """\n    partes = []\n\n    try:\n        import winreg\n\n        clave = winreg.OpenKey(\n            winreg.HKEY_LOCAL_MACHINE,\n            r"SOFTWARE\\Microsoft\\Cryptography"\n        )\n\n        machine_guid, _ = winreg.QueryValueEx(\n            clave,\n            "MachineGuid"\n        )\n\n        partes.append(\n            str(machine_guid)\n        )\n    except Exception:\n        pass\n\n    try:\n        partes.append(\n            platform.node()\n        )\n    except Exception:\n        pass\n\n    try:\n        partes.append(\n            str(uuid.getnode())\n        )\n    except Exception:\n        pass\n\n    if not partes:\n        partes.append(\n            os.environ.get(\n                "COMPUTERNAME",\n                "UNKNOWN-PC"\n            )\n        )\n\n    bruto = "|".join(partes).encode(\n        "utf-8",\n        errors="ignore"\n    )\n\n    digest = hashlib.sha256(\n        bruto\n    ).hexdigest().upper()\n\n    corto = digest[:24]\n\n    return (\n        "DP5PC-"\n        + "-".join(\n            corto[i:i + 4]\n            for i in range(\n                0,\n                len(corto),\n                4\n            )\n        )\n    )\n\n\ndef _b64url_decode(data):\n    data = str(data)\n    relleno = "=" * (\n        (4 - len(data) % 4) % 4\n    )\n\n    return base64.urlsafe_b64decode(\n        data + relleno\n    )\n\n\ndef _b64url_encode(data):\n    return (\n        base64.urlsafe_b64encode(\n            data\n        )\n        .decode("ascii")\n        .rstrip("=")\n    )\n\n\ndef verificar_token_licencia(token):\n    """\n    Verifica firma, equipo y caducidad.\n    Devuelve (True/False, datos_o_motivo).\n    """\n    token = str(token or "").strip()\n\n    if not token:\n        return False, "No has introducido ninguna licencia."\n\n    if Ed25519PublicKey is None:\n        return (\n            False,\n            "Falta el componente \'cryptography\'. "\n            "Ejecuta instalar_licencias.bat y vuelve a abrir el bot."\n        )\n\n    try:\n        partes = token.split(".")\n\n        if len(partes) != 3:\n            return False, "Formato de licencia incorrecto."\n\n        prefijo, payload_b64, firma_b64 = partes\n\n        if prefijo != LICENCIA_PREFIJO:\n            return False, "Esta licencia no pertenece a DIAGPROG5."\n\n        payload_raw = _b64url_decode(\n            payload_b64\n        )\n        firma = _b64url_decode(\n            firma_b64\n        )\n\n        clave_publica = Ed25519PublicKey.from_public_bytes(\n            base64.b64decode(\n                LICENCIA_PUBLIC_KEY_B64\n            )\n        )\n\n        clave_publica.verify(\n            firma,\n            payload_raw\n        )\n\n        datos = json.loads(\n            payload_raw.decode(\n                "utf-8"\n            )\n        )\n\n    except Exception:\n        return False, "Firma de licencia no válida o licencia modificada."\n\n    codigo_equipo = obtener_codigo_equipo()\n    maquina_licencia = str(\n        datos.get(\n            "machine",\n            ""\n        )\n    ).strip()\n\n    es_online = bool(\n        datos.get(\n            "online",\n            False\n        )\n    )\n\n    # Compatibilidad V67/V86: las licencias antiguas siguen ligadas\n    # directamente a un único PC.\n    if not es_online:\n        if not maquina_licencia:\n            return False, "La licencia no contiene un equipo autorizado."\n\n        if maquina_licencia != codigo_equipo:\n            return (\n                False,\n                "Esta licencia pertenece a otro PC.\\n\\n"\n                f"Este PC: {codigo_equipo}"\n            )\n\n    permanente = bool(\n        datos.get(\n            "permanent",\n            False\n        )\n    )\n\n    if not permanente:\n        caduca = str(\n            datos.get(\n                "expires",\n                ""\n            )\n        ).strip()\n\n        if not caduca:\n            return False, "La licencia no contiene fecha de caducidad."\n\n        try:\n            hoy = time.strftime(\n                "%Y-%m-%d"\n            )\n\n            if hoy > caduca:\n                return (\n                    False,\n                    f"La licencia caducó el {caduca}."\n                )\n        except Exception:\n            return False, "No pude comprobar la caducidad."\n\n    return True, datos\n\n\ndef guardar_token_licencia(token):\n    ruta = _ruta_licencia_local()\n\n    temporal = ruta.with_suffix(\n        ".tmp"\n    )\n\n    temporal.write_text(\n        str(token).strip(),\n        encoding="utf-8"\n    )\n\n    os.replace(\n        temporal,\n        ruta\n    )\n\n\ndef cargar_token_licencia():\n    ruta = _ruta_licencia_local()\n\n    if not ruta.exists():\n        return ""\n\n    try:\n        return ruta.read_text(\n            encoding="utf-8"\n        ).strip()\n    except Exception:\n        return ""\n\n\ndef licencia_local_valida():\n    global licencia_actual_info\n\n    token = cargar_token_licencia()\n\n    ok, datos = verificar_token_licencia(\n        token\n    )\n\n    if ok:\n        if licencia_requiere_online(\n            datos\n        ):\n            ok_online, detalle = comprobar_licencia_online_o_gracia(\n                datos\n            )\n\n            if not ok_online:\n                licencia_actual_info = {}\n                return False\n\n        licencia_actual_info = datos\n        return True\n\n    licencia_actual_info = {}\n    return False\n\n\ndef dias_restantes_licencia():\n    datos = licencia_actual_info or {}\n\n    if not datos:\n        return None\n\n    if bool(\n        datos.get(\n            "permanent",\n            False\n        )\n    ):\n        return -1\n\n    caduca = str(\n        datos.get(\n            "expires",\n            ""\n        )\n    ).strip()\n\n    if not caduca:\n        return None\n\n    try:\n        fecha_caduca = time.strptime(\n            caduca,\n            "%Y-%m-%d"\n        )\n\n        fin_ts = time.mktime(\n            fecha_caduca\n        )\n\n        hoy = time.localtime()\n\n        inicio_hoy = time.mktime(\n            (\n                hoy.tm_year,\n                hoy.tm_mon,\n                hoy.tm_mday,\n                0,\n                0,\n                0,\n                hoy.tm_wday,\n                hoy.tm_yday,\n                hoy.tm_isdst,\n            )\n        )\n\n        dias = int(\n            (fin_ts - inicio_hoy)\n            // 86400\n        )\n\n        return max(\n            0,\n            dias\n        )\n\n    except Exception:\n        return None\n\n\ndef texto_dias_restantes_licencia():\n    dias = dias_restantes_licencia()\n\n    if dias == -1:\n        return "SIN CADUCIDAD"\n\n    if dias is None:\n        return "—"\n\n    if dias == 0:\n        return "CADUCA HOY"\n\n    if dias == 1:\n        return "1 DÍA"\n\n    return f"{dias} DÍAS"\n\n\ndef fecha_caducidad_licencia():\n    datos = licencia_actual_info or {}\n\n    if bool(\n        datos.get(\n            "permanent",\n            False\n        )\n    ):\n        return "PERMANENTE"\n\n    return str(\n        datos.get(\n            "expires",\n            "—"\n        )\n    )\n\n\ndef nivel_visual_plan():\n    nombre = plan_actual_nombre()\n\n    mapa = {\n        "BASICA": "BÁSICA",\n        "PRO": "PRO",\n        "TALLER": "TALLER",\n        "VITALICIA": "VITALICIA",\n    }\n\n    return mapa.get(\n        nombre,\n        nombre\n    )\n\n\ndef actualizar_panel_licencia_cliente():\n    try:\n        if es_edicion_admin():\n            return\n\n        plan = nivel_visual_plan()\n        dias_txt = texto_dias_restantes_licencia()\n        caduca = fecha_caducidad_licencia()\n\n        etiqueta_cliente_plan.configure(\n            text=plan\n        )\n\n        etiqueta_cliente_dias.configure(\n            text=dias_txt\n        )\n\n        etiqueta_cliente_caduca.configure(\n            text=f"Caducidad: {caduca}"\n        )\n\n        # Barra orientativa. Si es permanente, llena.\n        dias = dias_restantes_licencia()\n\n        if dias == -1:\n            progreso = 1.0\n        elif dias is None:\n            progreso = 0.0\n        else:\n            # Referencia visual de 365 días.\n            progreso = max(\n                0.0,\n                min(\n                    1.0,\n                    dias / 365.0\n                )\n            )\n\n        barra_cliente_licencia.set(\n            progreso\n        )\n\n        if dias == -1:\n            estado_txt = "ACTIVA · PERMANENTE"\n        elif dias == 0:\n            estado_txt = "ACTIVA · CADUCA HOY"\n        elif isinstance(dias, int):\n            estado_txt = "ACTIVA"\n        else:\n            estado_txt = "ACTIVA"\n\n        etiqueta_cliente_estado.configure(\n            text=estado_txt,\n            text_color=(\n                "#46d47a"\n                if dias != 0\n                else "#f7c948"\n            )\n        )\n\n    except Exception:\n        pass\n\n\ndef resumen_licencia():\n    datos = licencia_actual_info or {}\n\n    if not datos:\n        return "SIN ACTIVAR"\n\n    plan = datos.get(\n        "plan",\n        "—"\n    )\n\n    cliente = datos.get(\n        "customer",\n        "—"\n    )\n\n    caduca = (\n        "PERMANENTE"\n        if datos.get(\n            "permanent",\n            False\n        )\n        else datos.get(\n            "expires",\n            "—"\n        )\n    )\n\n    return (\n        f"ACTIVA · {plan} · {cliente} · {caduca}"\n    )\n\n\nPLANES_DIAGPROG5 = {\n    "BASICA": {\n        "nivel": 1,\n        "nombre": "BÁSICA",\n        "max_cola": 5,\n        "max_lote": 2,\n        "max_pcs": 1,\n        "funciones": {\n            "lotes": True,\n            "lotes_avanzados": False,\n            "auditoria": False,\n            "autorreparacion": False,\n            "supervisor": False,\n            "exportaciones": False,\n            "analitica": False,\n            "duplicar_variacion": False,\n            "cuarentena": False,\n            "snapshots": False,\n            "estrategias": False,\n            "anti_duplicado_visual": False,\n        },\n    },\n    "PRO": {\n        "nivel": 2,\n        "nombre": "PRO",\n        "max_cola": 30,\n        "max_lote": 30,\n        "max_pcs": 1,\n        "funciones": {\n            "lotes": True,\n            "lotes_avanzados": True,\n            "auditoria": True,\n            "autorreparacion": True,\n            "supervisor": True,\n            "exportaciones": True,\n            "analitica": True,\n            "duplicar_variacion": True,\n            "cuarentena": True,\n            "snapshots": True,\n            "estrategias": True,\n            "anti_duplicado_visual": True,\n        },\n    },\n    "TALLER": {\n        "nivel": 3,\n        "nombre": "TALLER",\n        "max_cola": 100,\n        "max_lote": 100,\n        "max_pcs": 3,\n        "funciones": {\n            "lotes": True,\n            "lotes_avanzados": True,\n            "auditoria": True,\n            "autorreparacion": True,\n            "supervisor": True,\n            "exportaciones": True,\n            "analitica": True,\n            "duplicar_variacion": True,\n            "cuarentena": True,\n            "snapshots": True,\n            "estrategias": True,\n            "anti_duplicado_visual": True,\n        },\n    },\n    "VITALICIA": {\n        "nivel": 2,\n        "nombre": "VITALICIA",\n        "max_cola": 30,\n        "max_lote": 30,\n        "max_pcs": 1,\n        "funciones": {\n            "lotes": True,\n            "lotes_avanzados": True,\n            "auditoria": True,\n            "autorreparacion": True,\n            "supervisor": True,\n            "exportaciones": True,\n            "analitica": True,\n            "duplicar_variacion": True,\n            "cuarentena": True,\n            "snapshots": True,\n            "estrategias": True,\n            "anti_duplicado_visual": True,\n        },\n    },\n}\n\n\ndef plan_actual_nombre():\n    plan = str(\n        (licencia_actual_info or {}).get(\n            "plan",\n            "BASICA"\n        )\n    ).upper().strip()\n\n    if plan not in PLANES_DIAGPROG5:\n        plan = "BASICA"\n\n    return plan\n\n\ndef plan_actual():\n    return PLANES_DIAGPROG5[\n        plan_actual_nombre()\n    ]\n\n\ndef tiene_funcion_plan(nombre_funcion):\n    # La edición del creador/ADMIN siempre tiene acceso total.\n    if es_edicion_admin():\n        return True\n\n    return bool(\n        plan_actual().get(\n            "funciones",\n            {}\n        ).get(\n            nombre_funcion,\n            False\n        )\n    )\n\n\ndef limite_cola_plan():\n    if es_edicion_admin():\n        try:\n            return int(\n                capacidad_cola_actual()\n            )\n        except Exception:\n            return 500\n\n    return int(\n        plan_actual().get(\n            "max_cola",\n            5\n        )\n    )\n\n\ndef limite_lote_plan():\n    if es_edicion_admin():\n        return 500\n\n    return int(\n        plan_actual().get(\n            "max_lote",\n            5\n        )\n    )\n\n\ndef limite_cola_efectivo():\n    """Única fuente de verdad para el máximo real de la cola."""\n    try:\n        configurada = int(\n            capacidad_cola_actual()\n        )\n    except Exception:\n        try:\n            configurada = int(\n                MAX_COLA\n            )\n        except Exception:\n            configurada = (\n                500\n                if es_edicion_admin()\n                else limite_cola_plan()\n            )\n\n    configurada = max(\n        1,\n        min(\n            5000,\n            configurada\n        )\n    )\n\n    if es_edicion_admin():\n        return configurada\n\n    return min(\n        configurada,\n        int(\n            limite_cola_plan()\n        )\n    )\n\n\ndef espacio_cola_disponible():\n    try:\n        usados = len(\n            cola_anuncios\n        )\n    except Exception:\n        usados = 0\n\n    return max(\n        0,\n        limite_cola_efectivo()\n        - usados\n    )\n\n\ndef requerir_funcion_plan(nombre_funcion, nombre_visible):\n    if tiene_funcion_plan(nombre_funcion):\n        return True\n\n    plan = plan_actual().get(\n        "nombre",\n        plan_actual_nombre()\n    )\n\n    try:\n        messagebox.showwarning(\n            "Función no disponible",\n            (\n                f"{nombre_visible} no está incluido en tu plan {plan}.\\n\\n"\n                "Necesitas un plan superior para usar esta función."\n            )\n        )\n    except Exception:\n        pass\n\n    try:\n        estado.configure(\n            text=(\n                f"🔒 {nombre_visible} no está disponible "\n                f"en el plan {plan}."\n            )\n        )\n    except Exception:\n        pass\n\n    return False\n\n\ndef aplicar_limites_plan():\n    """\n    Aplica límites dinámicos tras validar la licencia.\n    """\n    global MAX_COLA\n\n    MAX_COLA = limite_cola_efectivo()\n\n    try:\n        if len(cola_anuncios) > MAX_COLA:\n            estado.configure(\n                text=(\n                    f"⚠ Límite actual: {MAX_COLA} anuncios en cola. "\n                    "Los anuncios existentes no se borrarán, pero no podrás añadir más."\n                )\n            )\n    except Exception:\n        pass\n\n    try:\n        var_max_cola.set(\n            str(MAX_COLA)\n        )\n    except Exception:\n        pass\n\n\ndef resumen_capacidades_plan():\n    plan = plan_actual()\n    funcs = plan.get(\n        "funciones",\n        {}\n    )\n\n    lineas = [\n        f"Plan: {plan.get(\'nombre\', plan_actual_nombre())}",\n        f"Máximo en cola: {plan.get(\'max_cola\', 5)}",\n        f"Máximo por lote: {plan.get(\'max_lote\', 5)}",\n        "",\n        "Funciones:",\n        f"• Lotes: {\'Sí\' if funcs.get(\'lotes\') else \'No\'}",\n        f"• Gestión avanzada de lotes: {\'Sí\' if funcs.get(\'lotes_avanzados\') else \'No\'}",\n        f"• Auditoría: {\'Sí\' if funcs.get(\'auditoria\') else \'No\'}",\n        f"• Autorreparación: {\'Sí\' if funcs.get(\'autorreparacion\') else \'No\'}",\n        f"• Supervisor: {\'Sí\' if funcs.get(\'supervisor\') else \'No\'}",\n        f"• Exportaciones: {\'Sí\' if funcs.get(\'exportaciones\') else \'No\'}",\n        f"• Analítica: {\'Sí\' if funcs.get(\'analitica\') else \'No\'}",\n        f"• Duplicar con variación: {\'Sí\' if funcs.get(\'duplicar_variacion\') else \'No\'}",\n        f"• Cuarentena: {\'Sí\' if funcs.get(\'cuarentena\') else \'No\'}",\n        f"• Snapshots: {\'Sí\' if funcs.get(\'snapshots\') else \'No\'}",\n        f"• Estrategias: {\'Sí\' if funcs.get(\'estrategias\') else \'No\'}",\n        f"• Anti-duplicado visual: {\'Sí\' if funcs.get(\'anti_duplicado_visual\') else \'No\'}",\n    ]\n\n    return "\\n".join(\n        lineas\n    )\n\n\ndef mostrar_capacidades_plan():\n    try:\n        messagebox.showinfo(\n            "Funciones de tu plan",\n            resumen_capacidades_plan()\n        )\n    except Exception:\n        pass\n\n\ndef mostrar_info_licencia():\n    token = cargar_token_licencia()\n    ok, datos = verificar_token_licencia(\n        token\n    )\n\n    if not ok:\n        messagebox.showerror(\n            "Licencia",\n            str(datos)\n        )\n        return\n\n    caduca = (\n        "Sin caducidad"\n        if datos.get(\n            "permanent",\n            False\n        )\n        else datos.get(\n            "expires",\n            "—"\n        )\n    )\n\n    messagebox.showinfo(\n        "Licencia DIAGPROG5",\n        (\n            "Estado: ACTIVA\\n"\n            f"Plan: {datos.get(\'plan\', \'—\')}\\n"\n            f"Cliente: {datos.get(\'customer\', \'—\')}\\n"\n            f"ID: {datos.get(\'license_id\', \'—\')}\\n"\n            f"Caduca: {caduca}\\n\\n"\n            f"Equipo:\\n{obtener_codigo_equipo()}"\n        )\n    )\n\n\ndef copiar_codigo_equipo():\n    codigo = obtener_codigo_equipo()\n\n    try:\n        ventana.clipboard_clear()\n        ventana.clipboard_append(\n            codigo\n        )\n        ventana.update()\n    except Exception:\n        pass\n\n    try:\n        estado.configure(\n            text="✅ Código del PC copiado."\n        )\n    except Exception:\n        pass\n\n\ndef mostrar_activacion_si_necesaria():\n    """\n    Bloquea el acceso al bot hasta introducir una licencia firmada válida.\n    """\n    global licencia_actual_info\n\n    if licencia_local_valida():\n        return True\n\n    try:\n        ventana.withdraw()\n    except Exception:\n        pass\n\n    resultado = {\n        "activado": False\n    }\n\n    dialogo = ctk.CTkToplevel(\n        ventana\n    )\n\n    dialogo.title(\n        "DIAGPROG5 · Activación"\n    )\n\n    dialogo.geometry(\n        "680x640"\n    )\n\n    dialogo.resizable(\n        False,\n        False\n    )\n\n    dialogo.configure(\n        fg_color="#050608"\n    )\n\n    try:\n        dialogo.grab_set()\n    except Exception:\n        pass\n\n    ctk.CTkLabel(\n        dialogo,\n        text="◢ DIAGPROG5 // LICENSE TERMINAL ◣",\n        font=("Segoe UI", 26, "bold"),\n        text_color="#ff2438"\n    ).pack(\n        pady=(28, 4)\n    )\n\n    ctk.CTkLabel(\n        dialogo,\n        text="ACTIVACIÓN DE LICENCIA",\n        font=("Segoe UI", 18, "bold")\n    ).pack(\n        pady=(0, 20)\n    )\n\n    ctk.CTkLabel(\n        dialogo,\n        text=(\n            "1. Copia el código de este PC.\\n"\n            "2. El administrador genera una licencia para ese código.\\n"\n            "3. Pega la licencia y pulsa ACTIVAR."\n        ),\n        font=("Segoe UI", 12),\n        justify="left",\n        text_color="#b7c2cc"\n    ).pack(\n        padx=30,\n        pady=(0, 18),\n        anchor="w"\n    )\n\n    ctk.CTkLabel(\n        dialogo,\n        text="Código de este PC",\n        font=("Segoe UI", 13, "bold")\n    ).pack(\n        padx=30,\n        anchor="w"\n    )\n\n    entrada_pc = ctk.CTkEntry(\n        dialogo,\n        height=40\n    )\n\n    entrada_pc.pack(\n        fill="x",\n        padx=30,\n        pady=(6, 8)\n    )\n\n    entrada_pc.insert(\n        0,\n        obtener_codigo_equipo()\n    )\n\n    entrada_pc.configure(\n        state="readonly"\n    )\n\n    def copiar_pc_dialogo():\n        try:\n            dialogo.clipboard_clear()\n            dialogo.clipboard_append(\n                obtener_codigo_equipo()\n            )\n            dialogo.update()\n            etiqueta_resultado.configure(\n                text="✅ Código del PC copiado.",\n                text_color="#46d47a"\n            )\n        except Exception:\n            pass\n\n    ctk.CTkButton(\n        dialogo,\n        text="COPIAR CÓDIGO DEL PC",\n        command=copiar_pc_dialogo,\n        height=36,\n        fg_color="#1b2733",\n        hover_color="#243545"\n    ).pack(\n        padx=30,\n        pady=(0, 18)\n    )\n\n    ctk.CTkLabel(\n        dialogo,\n        text="Licencia",\n        font=("Segoe UI", 13, "bold")\n    ).pack(\n        padx=30,\n        anchor="w"\n    )\n\n    caja_licencia = ctk.CTkTextbox(\n        dialogo,\n        height=145,\n        wrap="word"\n    )\n\n    caja_licencia.pack(\n        fill="x",\n        padx=30,\n        pady=(6, 10)\n    )\n\n    etiqueta_resultado = ctk.CTkLabel(\n        dialogo,\n        text="Sin activar",\n        font=("Segoe UI", 12),\n        text_color="#f7c948",\n        wraplength=560,\n        justify="left"\n    )\n\n    etiqueta_resultado.pack(\n        padx=30,\n        pady=(0, 10),\n        anchor="w"\n    )\n\n    def activar():\n        global licencia_actual_info\n\n        token = caja_licencia.get(\n            "1.0",\n            "end"\n        ).strip()\n\n        ok, datos = verificar_token_licencia(\n            token\n        )\n\n        if not ok:\n            etiqueta_resultado.configure(\n                text="❌ " + str(datos),\n                text_color="#ff6b6b"\n            )\n            return\n\n        try:\n            guardar_token_licencia(\n                token\n            )\n        except Exception as e:\n            etiqueta_resultado.configure(\n                text=(\n                    "❌ La licencia es válida, pero no pude guardarla: "\n                    + str(e)\n                ),\n                text_color="#ff6b6b"\n            )\n            return\n\n        if licencia_requiere_online(\n            datos\n        ):\n            ok_online, detalle_online = comprobar_licencia_online_o_gracia(\n                datos\n            )\n\n            if not ok_online:\n                try:\n                    _ruta_licencia_local().unlink(\n                        missing_ok=True\n                    )\n                except Exception:\n                    pass\n\n                etiqueta_resultado.configure(\n                    text="❌ " + str(detalle_online),\n                    text_color="#ff6b6b"\n                )\n                return\n\n        licencia_actual_info = datos\n        resultado["activado"] = True\n\n        etiqueta_resultado.configure(\n            text="✅ LICENCIA ACTIVADA CORRECTAMENTE",\n            text_color="#46d47a"\n        )\n\n        dialogo.after(\n            700,\n            dialogo.destroy\n        )\n\n    ctk.CTkButton(\n        dialogo,\n        text="ACTIVAR LICENCIA",\n        command=activar,\n        height=46,\n        font=("Segoe UI", 15, "bold"),\n        fg_color="#d91624",\n        hover_color="#f02636"\n    ).pack(\n        fill="x",\n        padx=30,\n        pady=(0, 10)\n    )\n\n    ctk.CTkLabel(\n        dialogo,\n        text=(\n            "La licencia queda vinculada a este equipo. "\n            "No necesita Internet para funcionar."\n        ),\n        font=("Segoe UI", 10),\n        text_color="#7f8a95",\n        wraplength=560\n    ).pack(\n        padx=30,\n        pady=(0, 20)\n    )\n\n    def cerrar_sin_activar():\n        resultado["activado"] = False\n        dialogo.destroy()\n\n    dialogo.protocol(\n        "WM_DELETE_WINDOW",\n        cerrar_sin_activar\n    )\n\n    ventana.wait_window(\n        dialogo\n    )\n\n    if resultado["activado"]:\n        try:\n            ventana.deiconify()\n        except Exception:\n            pass\n\n        return True\n\n    try:\n        ventana.destroy()\n    except Exception:\n        pass\n\n    return False\n\n\n# =========================================================\n# CONFIGURACIÓN\n# =========================================================\n\nANCHO = 1080\nALTO = 1080\n\nLIMITE_DESCRIPCION = 500\n\nTEXTO_VEHICULOS = "PARA CUALQUIER TIPO DE VEHÍCULO"\n\nultima_carpeta_anuncio = None\n\n# Datos que la extensión de Chromium leerá desde http://127.0.0.1:8765/anuncio\n# En esta primera prueba solo enviaremos el título.\nanuncio_extension = {\n    "titulo": "",\n    "descripcion": "",\n    "precio": "",\n    "imagenes": []\n}\n\n\n# =========================================================\n# FUENTES\n# =========================================================\n\ndef obtener_fuente(tamano, negrita=False):\n\n    if negrita:\n        ruta = r"C:\\Windows\\Fonts\\arialbd.ttf"\n    else:\n        ruta = r"C:\\Windows\\Fonts\\arial.ttf"\n\n    try:\n        return ImageFont.truetype(ruta, tamano)\n    except:\n        return ImageFont.load_default()\n\n\n# =========================================================\n# DETECTAR SERVICIO\n# =========================================================\n\ndef detectar_servicio(texto):\n\n    texto = texto.lower()\n\n    if (\n        "dpf" in texto\n        or "filtro de partículas" in texto\n        or "filtro de particulas" in texto\n    ):\n        return "DPF"\n\n    if "egr" in texto:\n        return "EGR"\n\n    if "adblue" in texto or "scr" in texto:\n        return "ADBLUE"\n\n    if (\n        "diagnosis" in texto\n        or "diagnóstico" in texto\n        or "diagnostico" in texto\n    ):\n        return "DIAGNOSIS"\n\n    return "GENERAL"\n\n\n# =========================================================\n# LIMITAR DESCRIPCIÓN\n# =========================================================\n\ndef limitar_descripcion(texto):\n\n    if len(texto) <= LIMITE_DESCRIPCION:\n        return texto\n\n    return texto[:LIMITE_DESCRIPCION]\n\n\n# =========================================================\n# CREAR DESCRIPCIÓN\n# =========================================================\n\ndef crear_descripcion(\n    servicio,\n    contacto,\n    mostrar_contacto_descripcion\n):\n\n    tipo = detectar_servicio(servicio)\n\n    if tipo == "DPF":\n\n        descripcion = (\n            "Servicio especializado en diagnosis, limpieza y regeneración "\n            "del sistema DPF / filtro de partículas.\\n\\n"\n            "✔ Diagnosis previa del sistema\\n"\n            "✔ Limpieza y regeneración según estado\\n"\n            "✔ Revisión del funcionamiento del DPF\\n"\n            "✔ Para cualquier tipo de vehículo\\n\\n"\n            "Consulta indicando marca, modelo, año y motorización."\n        )\n\n    elif tipo == "EGR":\n\n        descripcion = (\n            "Servicio especializado en diagnosis, limpieza y revisión "\n            "del sistema EGR.\\n\\n"\n            "✔ Diagnosis electrónica\\n"\n            "✔ Limpieza de la válvula EGR\\n"\n            "✔ Comprobación del sistema\\n"\n            "✔ Para cualquier tipo de vehículo\\n\\n"\n            "Consulta indicando marca, modelo, año y motorización."\n        )\n\n    elif tipo == "ADBLUE":\n\n        descripcion = (\n            "Servicio de diagnosis y reparación de averías relacionadas "\n            "con el sistema AdBlue / SCR.\\n\\n"\n            "✔ Diagnosis electrónica\\n"\n            "✔ Localización de averías\\n"\n            "✔ Comprobación del sistema\\n"\n            "✔ Para cualquier tipo de vehículo\\n\\n"\n            "Consulta indicando marca, modelo, año, motor y síntomas."\n        )\n\n    elif tipo == "DIAGNOSIS":\n\n        descripcion = (\n            "Servicio profesional de diagnosis electrónica.\\n\\n"\n            "✔ Lectura de códigos de avería\\n"\n            "✔ Comprobación de sistemas electrónicos\\n"\n            "✔ Localización de posibles fallos\\n"\n            "✔ Para cualquier tipo de vehículo\\n\\n"\n            "Consulta indicando los datos del vehículo."\n        )\n\n    else:\n\n        descripcion = (\n            f"{servicio}\\n\\n"\n            "Servicio profesional de automoción.\\n\\n"\n            "✔ Atención especializada\\n"\n            "✔ Revisión según vehículo\\n"\n            "✔ Presupuesto según trabajo\\n"\n            "✔ Para cualquier tipo de vehículo\\n\\n"\n            "Consulta indicando marca, modelo, año y motorización."\n        )\n\n    if (\n        mostrar_contacto_descripcion\n        and contacto\n    ):\n        descripcion += (\n            f"\\n\\nCONTACTO: {contacto}"\n        )\n\n    return limitar_descripcion(\n        descripcion\n    )\n\n\n# =========================================================\n# BENEFICIOS\n# =========================================================\n\ndef obtener_beneficios(servicio):\n\n    tipo = detectar_servicio(servicio)\n\n    if tipo == "DPF":\n\n        return [\n            "Diagnosis previa del sistema",\n            "Limpieza y regeneración del filtro",\n            "Revisión del funcionamiento del DPF",\n            "Servicio especializado",\n            "Para cualquier tipo de vehículo"\n        ]\n\n    if tipo == "EGR":\n\n        return [\n            "Diagnosis electrónica",\n            "Limpieza de la válvula EGR",\n            "Revisión de funcionamiento",\n            "Comprobación del sistema",\n            "Para cualquier tipo de vehículo"\n        ]\n\n    if tipo == "ADBLUE":\n\n        return [\n            "Diagnosis electrónica",\n            "Comprobación del sistema SCR",\n            "Localización de averías",\n            "Revisión de sensores",\n            "Para cualquier tipo de vehículo"\n        ]\n\n    if tipo == "DIAGNOSIS":\n\n        return [\n            "Lectura de códigos de avería",\n            "Diagnosis electrónica",\n            "Comprobación de sistemas",\n            "Localización de posibles fallos",\n            "Para cualquier tipo de vehículo"\n        ]\n\n    return [\n        "Diagnosis previa",\n        "Atención especializada",\n        "Servicio profesional",\n        "Revisión del sistema",\n        "Para cualquier tipo de vehículo"\n    ]\n\n\n# =========================================================\n# PALETAS\n# =========================================================\n\nPALETAS = [\n\n    {\n        "fondo": (17, 22, 29),\n        "panel": (30, 38, 48),\n        "principal": (36, 142, 230),\n        "texto": (245, 248, 250),\n        "secundario": (190, 202, 215)\n    },\n\n    {\n        "fondo": (238, 241, 245),\n        "panel": (255, 255, 255),\n        "principal": (27, 93, 155),\n        "texto": (22, 27, 32),\n        "secundario": (80, 90, 102)\n    },\n\n    {\n        "fondo": (25, 25, 25),\n        "panel": (43, 43, 43),\n        "principal": (210, 65, 45),\n        "texto": (250, 250, 250),\n        "secundario": (200, 200, 200)\n    },\n\n    {\n        "fondo": (12, 34, 38),\n        "panel": (18, 51, 57),\n        "principal": (42, 187, 174),\n        "texto": (245, 250, 250),\n        "secundario": (185, 215, 215)\n    }\n]\n\n\n# =========================================================\n# TEXTO CENTRADO\n# =========================================================\n\ndef escribir_centrado(\n    draw,\n    texto,\n    y,\n    fuente,\n    color,\n    ancho_max=900\n):\n\n    palabras = texto.split()\n\n    lineas = []\n    linea = ""\n\n    for palabra in palabras:\n\n        prueba = (linea + " " + palabra).strip()\n\n        caja = draw.textbbox(\n            (0, 0),\n            prueba,\n            font=fuente\n        )\n\n        ancho = caja[2] - caja[0]\n\n        if ancho <= ancho_max:\n            linea = prueba\n\n        else:\n\n            if linea:\n                lineas.append(linea)\n\n            linea = palabra\n\n    if linea:\n        lineas.append(linea)\n\n    espacio = fuente.size + 10\n\n    for numero, linea in enumerate(lineas):\n\n        caja = draw.textbbox(\n            (0, 0),\n            linea,\n            font=fuente\n        )\n\n        ancho = caja[2] - caja[0]\n\n        x = (ANCHO - ancho) // 2\n\n        draw.text(\n            (x, y + numero * espacio),\n            linea,\n            font=fuente,\n            fill=color\n        )\n\n    return y + len(lineas) * espacio\n\n\n# =========================================================\n# CREAR CARTEL\n# =========================================================\n\nARCHIVO_HISTORIAL_IMAGENES = "historial_imagenes.json"\n\n\ndef _cargar_historial_imagenes():\n    try:\n        if os.path.isfile(ARCHIVO_HISTORIAL_IMAGENES):\n            with open(\n                ARCHIVO_HISTORIAL_IMAGENES,\n                "r",\n                encoding="utf-8"\n            ) as f:\n                datos = json.load(f)\n\n            return datos if isinstance(datos, list) else []\n    except Exception:\n        pass\n\n    return []\n\n\ndef _guardar_firma_imagen(firma):\n    historial = _cargar_historial_imagenes()\n    historial.append(firma)\n\n    # Conservamos bastantes combinaciones para evitar repeticiones.\n    historial = historial[-5000:]\n\n    try:\n        with open(\n            ARCHIVO_HISTORIAL_IMAGENES,\n            "w",\n            encoding="utf-8"\n        ) as f:\n            json.dump(\n                historial,\n                f,\n                ensure_ascii=False,\n                indent=2\n            )\n    except Exception:\n        pass\n\n\ndef _tematica_por_titulo(titulo):\n    t = str(titulo).lower()\n\n    if any(x in t for x in [\n        "diagnosis", "diagnóstico", "obd", "centralita",\n        "electrónica", "electronica", "avería", "averia"\n    ]):\n        return "diagnosis"\n\n    if any(x in t for x in [\n        "turbo", "tuning", "potencia", "escape", "admisión",\n        "admision", "intercooler"\n    ]):\n        return "performance"\n\n    if any(x in t for x in [\n        "camión", "camion", "trailer", "remolque", "industrial",\n        "furgoneta"\n    ]):\n        return "industrial"\n\n    if any(x in t for x in [\n        "tractor", "agrícola", "agricola", "maquinaria"\n    ]):\n        return "agricola"\n\n    if any(x in t for x in [\n        "motor", "culata", "inyector", "inyección",\n        "inyeccion", "distribución", "distribucion"\n    ]):\n        return "motor"\n\n    return "taller"\n\n\ndef _elegir_combinacion_taller(titulo):\n    """\n    Genera una combinación visual nueva y persistente.\n    """\n    historial = set(_cargar_historial_imagenes())\n    tematica = _tematica_por_titulo(titulo)\n\n    escenas = {\n        "diagnosis": [\n            "puesto de diagnosis con portátil",\n            "vehículo conectado a equipo OBD",\n            "zona electrónica de taller",\n            "banco de diagnosis profesional",\n        ],\n        "performance": [\n            "box de preparación deportiva",\n            "zona de turbo y escape",\n            "banco de potencia",\n            "taller de alto rendimiento",\n        ],\n        "industrial": [\n            "nave de vehículo industrial",\n            "box de furgonetas y camiones",\n            "zona de mantenimiento pesado",\n            "taller industrial amplio",\n        ],\n        "agricola": [\n            "nave de maquinaria agrícola",\n            "zona de tractor y aperos",\n            "taller de maquinaria pesada",\n            "banco de reparación agrícola",\n        ],\n        "motor": [\n            "banco de motor",\n            "zona de reparación mecánica",\n            "motor sobre soporte",\n            "box de reconstrucción mecánica",\n        ],\n        "taller": [\n            "elevador hidráulico",\n            "box de reparación",\n            "zona de herramientas",\n            "zona de inspección",\n            "banco de trabajo",\n        ],\n    }\n\n    camaras = [\n        "frontal bajo",\n        "tres cuartos izquierdo",\n        "tres cuartos derecho",\n        "perspectiva amplia",\n        "ángulo diagonal",\n        "plano mecánico",\n        "vista central simétrica",\n        "perspectiva industrial",\n    ]\n\n    luces = [\n        "blanca industrial con acento rojo",\n        "cenital fría",\n        "lateral dramática",\n        "roja ambiental muy sutil",\n        "cinematográfica suave",\n        "alto contraste industrial",\n    ]\n\n    fondos = [\n        "pared de paneles metálicos",\n        "pared oscura de herramientas",\n        "nave profunda con elevadores",\n        "zona técnica minimalista",\n        "garaje profesional ordenado",\n        "zona de boxes",\n    ]\n\n    for _ in range(500):\n        datos = {\n            "tematica": tematica,\n            "escena": random.choice(escenas[tematica]),\n            "camara": random.choice(camaras),\n            "luz": random.choice(luces),\n            "fondo": random.choice(fondos),\n            "vehiculo_pos": random.choice(["izquierda", "centro", "derecha"]),\n            "detalle": random.randint(100000, 999999999),\n        }\n\n        firma = hashlib.sha256(\n            json.dumps(\n                datos,\n                sort_keys=True,\n                ensure_ascii=False\n            ).encode("utf-8")\n        ).hexdigest()\n\n        if firma not in historial:\n            _guardar_firma_imagen(firma)\n            return datos, firma\n\n    # Fallback prácticamente imposible de repetir\n    datos = {\n        "tematica": tematica,\n        "escena": random.choice(escenas[tematica]),\n        "camara": random.choice(camaras),\n        "luz": random.choice(luces),\n        "fondo": random.choice(fondos),\n        "vehiculo_pos": random.choice(["izquierda", "centro", "derecha"]),\n        "detalle": time.time_ns(),\n    }\n\n    firma = hashlib.sha256(\n        repr(datos).encode("utf-8")\n    ).hexdigest()\n\n    _guardar_firma_imagen(firma)\n    return datos, firma\n\n\ndef _dibujar_gradiente_vertical(draw, arriba, abajo):\n    for y in range(ALTO):\n        p = y / max(1, ALTO - 1)\n\n        color = tuple(\n            int(arriba[i] * (1 - p) + abajo[i] * p)\n            for i in range(3)\n        )\n\n        draw.line(\n            (0, y, ANCHO, y),\n            fill=color\n        )\n\n\ndef _dibujar_vineta(imagen):\n    """\n    Oscurece ligeramente bordes y esquinas.\n    """\n    overlay = Image.new(\n        "RGBA",\n        imagen.size,\n        (0, 0, 0, 0)\n    )\n\n    od = ImageDraw.Draw(overlay)\n\n    for i in range(16):\n        alpha = int(7 + i * 3)\n        margen = i * 18\n\n        od.rounded_rectangle(\n            (\n                margen,\n                margen,\n                ANCHO - margen,\n                ALTO - margen\n            ),\n            radius=70,\n            outline=(0, 0, 0, alpha),\n            width=24\n        )\n\n    base = imagen.convert("RGBA")\n    base.alpha_composite(overlay)\n\n    return base.convert("RGB")\n\n\ndef _dibujar_taller_fondo(draw, rng, rojo):\n    """\n    Construye un taller más profundo y ordenado.\n    """\n    horizonte = rng.randint(545, 600)\n\n    # Pared trasera\n    draw.rectangle(\n        (0, 0, ANCHO, horizonte),\n        fill=(18, 20, 23)\n    )\n\n    # Paneles verticales\n    for x in range(0, ANCHO, 120):\n        draw.line(\n            (x, 0, x, horizonte),\n            fill=(31, 34, 38),\n            width=2\n        )\n\n    # Líneas horizontales\n    for y in range(100, horizonte, 95):\n        draw.line(\n            (0, y, ANCHO, y),\n            fill=(25, 28, 31),\n            width=1\n        )\n\n    # Suelo con perspectiva\n    draw.rectangle(\n        (0, horizonte, ANCHO, ALTO),\n        fill=(12, 14, 16)\n    )\n\n    centro = rng.randint(490, 590)\n\n    for x in range(-400, 1600, 125):\n        draw.line(\n            (centro, horizonte, x, ALTO),\n            fill=(29, 31, 34),\n            width=2\n        )\n\n    for y in range(horizonte, ALTO, 70):\n        draw.line(\n            (0, y, ANCHO, y),\n            fill=(26, 28, 31),\n            width=2\n        )\n\n    # Luces superiores\n    for x in [120, 360, 600, 840, 1030]:\n        ancho = rng.randint(100, 145)\n\n        draw.rounded_rectangle(\n            (\n                x - ancho // 2,\n                42,\n                x + ancho // 2,\n                58\n            ),\n            radius=8,\n            fill=(205, 210, 214)\n        )\n\n    # Línea roja arquitectónica\n    draw.rectangle(\n        (0, horizonte - 10, ANCHO, horizonte - 5),\n        fill=rojo\n    )\n\n    return horizonte\n\n\ndef _dibujar_elevador(draw, rng, lado):\n    if lado == "izquierda":\n        x1, x2 = 70, 118\n        brazo = 320\n    else:\n        x1, x2 = 960, 1008\n        brazo = 760\n\n    draw.rounded_rectangle(\n        (x1, 250, x2, 820),\n        radius=9,\n        fill=(40, 44, 49)\n    )\n\n    draw.rectangle(\n        (x1 + 10, 275, x2 - 10, 770),\n        fill=(72, 77, 82)\n    )\n\n    draw.rectangle(\n        (\n            min(x2, brazo),\n            650,\n            max(x2, brazo),\n            674\n        ),\n        fill=(78, 83, 88)\n    )\n\n\ndef _dibujar_coche(draw, rng, tematica, rojo):\n    """\n    Silueta de vehículo más limpia y con detalles.\n    """\n    if tematica == "industrial":\n        x = rng.randint(200, 250)\n        y = rng.randint(555, 595)\n        w = rng.randint(650, 700)\n        h = 245\n\n        draw.rounded_rectangle(\n            (x, y - 110, x + w, y + h),\n            radius=35,\n            fill=(34, 37, 41),\n            outline=(75, 80, 86),\n            width=3\n        )\n\n        # Cabina\n        draw.polygon(\n            [\n                (x + 420, y - 105),\n                (x + 560, y - 105),\n                (x + 640, y - 10),\n                (x + 420, y - 10)\n            ],\n            fill=(24, 31, 36)\n        )\n\n        ruedas = [\n            x + 150,\n            x + 470,\n            x + w - 105\n        ]\n\n    elif tematica == "agricola":\n        x = rng.randint(220, 280)\n        y = rng.randint(570, 610)\n        w = 610\n\n        draw.rounded_rectangle(\n            (x, y, x + w, y + 180),\n            radius=45,\n            fill=(35, 39, 43),\n            outline=(76, 82, 88),\n            width=3\n        )\n\n        draw.rectangle(\n            (x + 300, y - 150, x + 470, y + 20),\n            fill=(31, 35, 39)\n        )\n\n        draw.rectangle(\n            (x + 320, y - 130, x + 450, y - 20),\n            fill=(20, 28, 33)\n        )\n\n        ruedas = [\n            x + 120,\n            x + 460\n        ]\n\n    else:\n        x = rng.randint(205, 255)\n        y = rng.randint(565, 610)\n        w = rng.randint(610, 690)\n\n        draw.rounded_rectangle(\n            (x, y, x + w, y + 175),\n            radius=60,\n            fill=(35, 38, 42),\n            outline=(78, 83, 88),\n            width=3\n        )\n\n        draw.polygon(\n            [\n                (x + 125, y + 5),\n                (x + 245, y - 105),\n                (x + w - 190, y - 105),\n                (x + w - 65, y + 5)\n            ],\n            fill=(36, 39, 43)\n        )\n\n        # Cristales\n        draw.polygon(\n            [\n                (x + 250, y - 90),\n                (x + 345, y - 90),\n                (x + 345, y - 8),\n                (x + 165, y - 8)\n            ],\n            fill=(18, 26, 31)\n        )\n\n        draw.polygon(\n            [\n                (x + 365, y - 90),\n                (x + w - 195, y - 90),\n                (x + w - 90, y - 8),\n                (x + 365, y - 8)\n            ],\n            fill=(18, 26, 31)\n        )\n\n        ruedas = [\n            x + 145,\n            x + w - 145\n        ]\n\n    for wx in ruedas:\n        draw.ellipse(\n            (wx - 60, y + 110, wx + 60, y + 230),\n            fill=(4, 5, 6)\n        )\n        draw.ellipse(\n            (wx - 33, y + 137, wx + 33, y + 203),\n            fill=(86, 92, 98)\n        )\n        draw.ellipse(\n            (wx - 13, y + 157, wx + 13, y + 183),\n            fill=(30, 33, 36)\n        )\n\n    # Reflejo rojo muy sutil\n    draw.line(\n        (x + 80, y + 45, x + w - 80, y + 45),\n        fill=rojo,\n        width=3\n    )\n\n\ndef _dibujar_elemento_tematico(draw, rng, tematica, rojo):\n    """\n    Añade un elemento secundario que ayuda a distinguir anuncios.\n    """\n    if tematica == "diagnosis":\n        # Monitor / portátil\n        draw.rounded_rectangle(\n            (760, 385, 980, 520),\n            radius=14,\n            fill=(27, 31, 35),\n            outline=(80, 88, 95),\n            width=3\n        )\n        draw.rectangle(\n            (785, 410, 955, 490),\n            fill=(12, 24, 30)\n        )\n\n        for i in range(5):\n            y = 425 + i * 12\n            draw.line(\n                (800, y, 930 - i * 10, y),\n                fill=(\n                    rojo\n                    if i == 1\n                    else (60, 150, 180)\n                ),\n                width=3\n            )\n\n    elif tematica == "performance":\n        # Turbo estilizado\n        cx, cy = 850, 440\n\n        draw.ellipse(\n            (cx - 85, cy - 85, cx + 85, cy + 85),\n            outline=(110, 118, 125),\n            width=15\n        )\n\n        draw.ellipse(\n            (cx - 38, cy - 38, cx + 38, cy + 38),\n            fill=(38, 43, 48),\n            outline=rojo,\n            width=5\n        )\n\n        for ang in range(0, 360, 45):\n            rad = math.radians(ang)\n            x2 = cx + int(math.cos(rad) * 70)\n            y2 = cy + int(math.sin(rad) * 70)\n\n            draw.line(\n                (cx, cy, x2, y2),\n                fill=(100, 108, 115),\n                width=5\n            )\n\n    elif tematica == "motor":\n        # Motor sobre soporte\n        draw.rounded_rectangle(\n            (745, 390, 950, 545),\n            radius=18,\n            fill=(42, 46, 50),\n            outline=(95, 102, 108),\n            width=3\n        )\n\n        for x in range(775, 920, 45):\n            draw.ellipse(\n                (x, 420, x + 32, 452),\n                fill=(24, 27, 30),\n                outline=(105, 112, 118),\n                width=2\n            )\n\n    elif tematica == "agricola":\n        draw.rectangle(\n            (760, 405, 955, 545),\n            fill=(39, 43, 47),\n            outline=(90, 97, 102),\n            width=3\n        )\n\n        for y in range(425, 525, 25):\n            draw.line(\n                (785, y, 930, y),\n                fill=(75, 82, 87),\n                width=3\n            )\n\n    elif tematica == "industrial":\n        draw.rounded_rectangle(\n            (760, 390, 960, 535),\n            radius=14,\n            fill=(38, 42, 46)\n        )\n\n        draw.text(\n            (800, 430),\n            "HEAVY\\nSERVICE",\n            font=obtener_fuente(25, True),\n            fill=(190, 196, 201)\n        )\n\n    else:\n        # Carro de herramientas\n        draw.rounded_rectangle(\n            (795, 400, 955, 560),\n            radius=14,\n            fill=(45, 49, 53)\n        )\n\n        for y in [430, 468, 506]:\n            draw.line(\n                (815, y, 935, y),\n                fill=(95, 102, 108),\n                width=3\n            )\n\n        draw.rectangle(\n            (810, 530, 940, 545),\n            fill=rojo\n        )\n\n\ndef _crear_cartel_base(\n    titulo,\n    beneficios,\n    precio,\n    numero,\n    contacto,\n    mostrar_contacto_imagen\n):\n    """\n    Imagen principal premium para Wallapop.\n    """\n    datos, firma = _elegir_combinacion_taller(\n        titulo\n    )\n\n    rng = random.Random(\n        int(firma[:16], 16)\n    )\n\n    rojo = (226, 24, 38)\n    rojo_oscuro = (120, 10, 18)\n    blanco = (246, 247, 248)\n    gris = (169, 176, 184)\n    gris_claro = (205, 210, 214)\n    panel = (16, 19, 22)\n    panel_2 = (24, 28, 32)\n\n    imagen = Image.new(\n        "RGB",\n        (ANCHO, ALTO),\n        (8, 10, 12)\n    )\n\n    draw = ImageDraw.Draw(\n        imagen\n    )\n\n    _dibujar_gradiente_vertical(\n        draw,\n        (14, 17, 20),\n        (5, 6, 8)\n    )\n\n    horizonte = _dibujar_taller_fondo(\n        draw,\n        rng,\n        rojo_oscuro\n    )\n\n    _dibujar_elevador(\n        draw,\n        rng,\n        rng.choice(["izquierda", "derecha"])\n    )\n\n    _dibujar_coche(\n        draw,\n        rng,\n        datos["tematica"],\n        rojo\n    )\n\n    _dibujar_elemento_tematico(\n        draw,\n        rng,\n        datos["tematica"],\n        rojo\n    )\n\n    # Banda vertical de marca\n    draw.rectangle(\n        (0, 0, 18, ALTO),\n        fill=rojo\n    )\n\n    # Header compacto\n    draw.rounded_rectangle(\n        (55, 55, 1025, 355),\n        radius=28,\n        fill=panel\n    )\n\n    # Línea interior roja\n    draw.rounded_rectangle(\n        (75, 78, 92, 332),\n        radius=8,\n        fill=rojo\n    )\n\n    fuente_marca = obtener_fuente(25, True)\n    fuente_titulo = obtener_fuente(56, True)\n    fuente_sub = obtener_fuente(23, True)\n    fuente_precio = obtener_fuente(38, True)\n    fuente_tag = obtener_fuente(20, True)\n    fuente_mini = obtener_fuente(17, False)\n\n    draw.text(\n        (115, 92),\n        "DIAGPROG5",\n        font=fuente_marca,\n        fill=rojo\n    )\n\n    draw.text(\n        (275, 94),\n        "AUTOMOCIÓN · SERVICIO PROFESIONAL",\n        font=obtener_fuente(18, True),\n        fill=gris\n    )\n\n    titulo_limpio = str(titulo).upper().strip()\n\n    if len(titulo_limpio) > 52:\n        titulo_limpio = (\n            titulo_limpio[:49] + "..."\n        )\n\n    escribir_centrado(\n        draw,\n        titulo_limpio,\n        155,\n        fuente_titulo,\n        blanco,\n        820\n    )\n\n    subtitulos = {\n        "diagnosis": "DIAGNOSIS · ELECTRÓNICA · AUTOMOCIÓN",\n        "performance": "PERFORMANCE · MOTOR · AUTOMOCIÓN",\n        "industrial": "INDUSTRIAL · TALLER · AUTOMOCIÓN",\n        "agricola": "MAQUINARIA · MECÁNICA · SERVICIO",\n        "motor": "MOTOR · MECÁNICA · REPARACIÓN",\n        "taller": "TALLER · MECÁNICA · AUTOMOCIÓN",\n    }\n\n    escribir_centrado(\n        draw,\n        subtitulos.get(\n            datos["tematica"],\n            subtitulos["taller"]\n        ),\n        292,\n        fuente_sub,\n        gris,\n        800\n    )\n\n    # Precio\n    texto_precio = (\n        f"DESDE {precio} €"\n        if precio\n        else "CONSULTAR"\n    )\n\n    bbox = draw.textbbox(\n        (0, 0),\n        texto_precio,\n        font=fuente_precio\n    )\n\n    pw = bbox[2] - bbox[0]\n\n    draw.rounded_rectangle(\n        (\n            ANCHO - pw - 125,\n            380,\n            ANCHO - 55,\n            460\n        ),\n        radius=22,\n        fill=rojo\n    )\n\n    draw.text(\n        (\n            ANCHO - pw - 95,\n            398\n        ),\n        texto_precio,\n        font=fuente_precio,\n        fill=(255, 255, 255)\n    )\n\n    # Etiquetas visuales pequeñas\n    tag1 = datos["tematica"].upper()\n    tag2 = datos["escena"].upper()\n\n    draw.rounded_rectangle(\n        (65, 840, 290, 890),\n        radius=16,\n        fill=panel_2\n    )\n\n    draw.text(\n        (88, 854),\n        tag1,\n        font=fuente_tag,\n        fill=rojo\n    )\n\n    draw.rounded_rectangle(\n        (305, 840, 1015, 890),\n        radius=16,\n        fill=panel_2\n    )\n\n    if len(tag2) > 44:\n        tag2 = tag2[:41] + "..."\n\n    draw.text(\n        (330, 854),\n        tag2,\n        font=fuente_tag,\n        fill=gris_claro\n    )\n\n    # Pie profesional\n    draw.line(\n        (65, 930, 1015, 930),\n        fill=rojo,\n        width=3\n    )\n\n    draw.text(\n        (70, 950),\n        "SERVICIO PROFESIONAL · CONSULTA DISPONIBILIDAD",\n        font=fuente_mini,\n        fill=gris\n    )\n\n    draw.text(\n        (70, 985),\n        datos["luz"].upper(),\n        font=obtener_fuente(15, False),\n        fill=(110, 118, 125)\n    )\n\n    if mostrar_contacto_imagen and contacto:\n        texto_contacto = f"CONTACTO: {contacto}"\n\n        bbox = draw.textbbox(\n            (0, 0),\n            texto_contacto,\n            font=obtener_fuente(20, True)\n        )\n\n        cw = bbox[2] - bbox[0]\n\n        draw.rounded_rectangle(\n            (\n                ANCHO - cw - 100,\n                945,\n                ANCHO - 55,\n                1002\n            ),\n            radius=14,\n            fill=(26, 29, 33)\n        )\n\n        draw.text(\n            (\n                ANCHO - cw - 78,\n                960\n            ),\n            texto_contacto,\n            font=obtener_fuente(20, True),\n            fill=blanco\n        )\n\n    imagen = _dibujar_vineta(\n        imagen\n    )\n\n    # Acabado premium: mejora de contraste, nitidez y color.\n    try:\n        imagen = ImageEnhance.Contrast(imagen).enhance(1.08)\n        imagen = ImageEnhance.Sharpness(imagen).enhance(1.25)\n        imagen = ImageEnhance.Color(imagen).enhance(0.96)\n        imagen = imagen.filter(\n            ImageFilter.UnsharpMask(\n                radius=1.2,\n                percent=115,\n                threshold=3\n            )\n        )\n    except Exception:\n        pass\n\n    return imagen\n\n\n# =========================================================\n# ANTIDUPLICADO VISUAL\n# =========================================================\n\ndef _cargar_hashes_visuales():\n    if not os.path.isfile(ARCHIVO_HASHES_VISUALES):\n        return []\n\n    try:\n        with open(\n            ARCHIVO_HASHES_VISUALES,\n            "r",\n            encoding="utf-8"\n        ) as f:\n            datos = json.load(f)\n\n        return datos if isinstance(datos, list) else []\n    except Exception:\n        return []\n\n\ndef _guardar_hash_visual(hash_hex, titulo=""):\n    datos = _cargar_hashes_visuales()\n\n    datos.append({\n        "hash": hash_hex,\n        "titulo": str(titulo),\n        "fecha": time.strftime("%d/%m/%Y %H:%M:%S")\n    })\n\n    datos = datos[-5000:]\n\n    try:\n        with open(\n            ARCHIVO_HASHES_VISUALES,\n            "w",\n            encoding="utf-8"\n        ) as f:\n            json.dump(\n                datos,\n                f,\n                ensure_ascii=False,\n                indent=2\n            )\n    except Exception:\n        pass\n\n\ndef _hash_perceptual_imagen(imagen):\n    """\n    Average hash 8x8 -> 64 bits.\n    """\n    mini = imagen.convert("L").resize(\n        (8, 8),\n        Image.Resampling.LANCZOS\n    )\n\n    pixeles = list(mini.getdata())\n    media = sum(pixeles) / max(1, len(pixeles))\n\n    bits = 0\n\n    for valor in pixeles:\n        bits = (bits << 1) | int(valor >= media)\n\n    return f"{bits:016x}"\n\n\ndef _distancia_hash_visual(hash_a, hash_b):\n    try:\n        return (\n            int(hash_a, 16)\n            ^ int(hash_b, 16)\n        ).bit_count()\n    except Exception:\n        return 64\n\n\ndef _config_visual():\n    datos = cargar_configuracion_general()\n\n    return {\n        "activo": bool(\n            datos.get(\n                "anti_duplicado_visual",\n                True\n            )\n        ),\n        "umbral": max(\n            0,\n            int(\n                datos.get(\n                    "umbral_similitud_visual",\n                    10\n                )\n            )\n        ),\n        "max_regeneraciones": max(\n            0,\n            int(\n                datos.get(\n                    "max_regeneraciones_visuales",\n                    4\n                )\n            )\n        ),\n    }\n\n\ndef _originalidad_visual(hash_actual):\n    historial = _cargar_hashes_visuales()\n\n    if not historial:\n        return 100, 64\n\n    distancias = []\n\n    for item in historial:\n        h = (\n            item.get("hash", "")\n            if isinstance(item, dict)\n            else str(item)\n        )\n\n        if h:\n            distancias.append(\n                _distancia_hash_visual(\n                    hash_actual,\n                    h\n                )\n            )\n\n    if not distancias:\n        return 100, 64\n\n    minima = min(distancias)\n\n    # 0 bits distintos = 0% originalidad\n    # 32+ bits distintos = 100%\n    score = int(\n        min(\n            100,\n            max(\n                0,\n                (minima / 32) * 100\n            )\n        )\n    )\n\n    return score, minima\n\n\ndef crear_cartel(\n    titulo,\n    beneficios,\n    precio,\n    numero,\n    contacto,\n    mostrar_contacto_imagen\n):\n    """\n    Wrapper premium:\n    genera imagen y comprueba que no sea demasiado parecida\n    a las anteriores. Si lo es, regenera automáticamente.\n    """\n    global visual_regeneraciones_sesion\n    global visual_ultima_originalidad\n    global visual_ultimo_estado\n\n    cfg = _config_visual()\n\n    if not tiene_funcion_plan(\n        "anti_duplicado_visual"\n    ):\n        cfg["activo"] = False\n\n    max_intentos = (\n        cfg["max_regeneraciones"] + 1\n        if cfg["activo"]\n        else 1\n    )\n\n    mejor_imagen = None\n    mejor_hash = ""\n    mejor_score = -1\n    mejor_distancia = -1\n\n    for intento in range(1, max_intentos + 1):\n        imagen = _crear_cartel_base(\n            titulo,\n            beneficios,\n            precio,\n            numero + intento - 1,\n            contacto,\n            mostrar_contacto_imagen\n        )\n\n        hash_actual = _hash_perceptual_imagen(\n            imagen\n        )\n\n        score, distancia = _originalidad_visual(\n            hash_actual\n        )\n\n        if score > mejor_score:\n            mejor_imagen = imagen\n            mejor_hash = hash_actual\n            mejor_score = score\n            mejor_distancia = distancia\n\n        if (\n            not cfg["activo"]\n            or distancia > cfg["umbral"]\n        ):\n            if intento > 1:\n                visual_regeneraciones_sesion += (\n                    intento - 1\n                )\n\n            visual_ultima_originalidad = score\n            visual_ultimo_estado = (\n                f"ÚNICA · distancia {distancia}"\n            )\n\n            _guardar_hash_visual(\n                hash_actual,\n                titulo\n            )\n\n            return imagen\n\n    # Si todas se parecían, usamos la mejor encontrada.\n    visual_regeneraciones_sesion += max(\n        0,\n        max_intentos - 1\n    )\n    visual_ultima_originalidad = max(\n        0,\n        mejor_score\n    )\n    visual_ultimo_estado = (\n        f"MEJOR DISPONIBLE · distancia {mejor_distancia}"\n    )\n\n    if mejor_hash:\n        _guardar_hash_visual(\n            mejor_hash,\n            titulo\n        )\n\n    return mejor_imagen\n\n\ndef contar_hashes_visuales():\n    return len(\n        _cargar_hashes_visuales()\n    )\n\n\n\n# =========================================================\n# MOSTRAR IMAGEN\n# =========================================================\n\nimagen_preview_ctk = None\n\n\ndef mostrar_imagen(\n    imagen,\n    etiqueta\n):\n    global imagen_preview_ctk\n\n    copia = imagen.copy()\n    copia.thumbnail((520, 520))\n\n    imagen_preview_ctk = ctk.CTkImage(\n        light_image=copia,\n        dark_image=copia,\n        size=copia.size\n    )\n\n    etiqueta.configure(\n        image=imagen_preview_ctk,\n        text=""\n    )\n\n    etiqueta._wallapopbot_image = imagen_preview_ctk\n\n\n# =========================================================\n# CONTADOR EN TIEMPO REAL\n# =========================================================\n\ndef controlar_descripcion(event=None):\n\n    texto = caja_descripcion.get(\n        "1.0",\n        "end-1c"\n    )\n\n    if len(texto) > LIMITE_DESCRIPCION:\n\n        texto = texto[\n            :LIMITE_DESCRIPCION\n        ]\n\n        posicion = caja_descripcion.index(\n            "insert"\n        )\n\n        caja_descripcion.delete(\n            "1.0",\n            "end"\n        )\n\n        caja_descripcion.insert(\n            "1.0",\n            texto\n        )\n\n    cantidad = len(\n        caja_descripcion.get(\n            "1.0",\n            "end-1c"\n        )\n    )\n\n    contador_descripcion.configure(\n        text=(\n            f"{cantidad} / "\n            f"{LIMITE_DESCRIPCION} caracteres"\n        )\n    )\n\n\n# =========================================================\n# GUARDAR LA DESCRIPCIÓN EDITADA\n# =========================================================\n\ndef obtener_descripcion_actual():\n\n    descripcion = caja_descripcion.get(\n        "1.0",\n        "end-1c"\n    )\n\n    return limitar_descripcion(\n        descripcion\n    )\n\n\n# =========================================================\n# GENERAR ANUNCIO\n# =========================================================\n\ndef generar_anuncio():\n    # La generación manual está disponible en TODOS los planes,\n    # incluido BÁSICA. Las restricciones se aplican solo a funciones premium.\n\n    global ultima_carpeta_anuncio\n\n    servicio = (\n        entrada_servicio.get().strip()\n    )\n\n    precio = (\n        entrada_precio.get().strip()\n    )\n\n    contacto = (\n        entrada_contacto.get().strip()\n    )\n\n    mostrar_contacto_imagen = (\n        check_contacto_imagen.get()\n    )\n\n    mostrar_contacto_descripcion = (\n        check_contacto_descripcion.get()\n    )\n\n    if servicio == "":\n\n        estado.configure(\n            text="❌ Escribe el servicio."\n        )\n\n        return\n\n    boton.configure(\n        state="disabled"\n    )\n\n    try:\n\n        titulo_anuncio = servicio\n\n        descripcion = crear_descripcion(\n            servicio,\n            contacto,\n            mostrar_contacto_descripcion\n        )\n\n        beneficios = obtener_beneficios(\n            servicio\n        )\n\n        caja_titulo.delete(\n            "1.0",\n            "end"\n        )\n\n        caja_titulo.insert(\n            "1.0",\n            titulo_anuncio\n        )\n\n        caja_descripcion.delete(\n            "1.0",\n            "end"\n        )\n\n        caja_descripcion.insert(\n            "1.0",\n            descripcion\n        )\n\n        controlar_descripcion()\n\n        identificador = (\n            f"{int(time.time())}_"\n            f"{random.randint(1000, 9999)}"\n        )\n\n        carpeta = os.path.join(\n            CARPETA_ANUNCIOS,\n            f"anuncio_{identificador}"\n        )\n\n        os.makedirs(\n            carpeta,\n            exist_ok=True\n        )\n\n        ultima_carpeta_anuncio = os.path.abspath(carpeta)\n\n        numero = 0\n\n        estado.configure(\n            text="Creando imagen profesional de taller..."\n        )\n        ventana.update()\n\n        imagen = crear_cartel(\n            titulo_anuncio,\n            beneficios,\n            precio,\n            numero,\n            contacto,\n            mostrar_contacto_imagen\n        )\n\n        ruta_imagen = os.path.join(\n            carpeta,\n            "imagen_1.png"\n        )\n        imagen.save(ruta_imagen, quality=95)\n\n        mostrar_imagen(\n            imagen,\n            vistas[0]\n        )\n\n        # Importante:\n        # usamos el texto que realmente aparece\n        # en la caja de descripción.\n\n        descripcion_final = (\n            obtener_descripcion_actual()\n        )\n\n\n        # Datos que leerá la extensión de Chromium\n        anuncio_extension["titulo"] = titulo_anuncio[:50]\n        anuncio_extension["descripcion"] = descripcion_final[:640]\n        anuncio_extension["precio"] = precio\n        anuncio_extension["imagenes"] = [\n            "http://127.0.0.1:8765/imagen/1"\n        ]\n\n        with open(\n            os.path.join(\n                carpeta,\n                "anuncio.txt"\n            ),\n            "w",\n            encoding="utf-8"\n        ) as archivo:\n\n            archivo.write(\n                "TÍTULO:\\n"\n            )\n\n            archivo.write(\n                titulo_anuncio\n                + "\\n\\n"\n            )\n\n            archivo.write(\n                "PRECIO:\\n"\n            )\n\n            if precio:\n                archivo.write(\n                    precio + " €\\n\\n"\n                )\n            else:\n                archivo.write(\n                    "Consultar\\n\\n"\n                )\n\n            archivo.write(\n                "TIPO DE VEHÍCULO:\\n"\n            )\n\n            archivo.write(\n                "Cualquier tipo de vehículo\\n\\n"\n            )\n\n            archivo.write(\n                "CONTACTO:\\n"\n            )\n\n            if contacto:\n                archivo.write(\n                    contacto + "\\n\\n"\n                )\n            else:\n                archivo.write(\n                    "No indicado\\n\\n"\n                )\n\n            archivo.write(\n                "DESCRIPCIÓN:\\n"\n            )\n\n            archivo.write(\n                descripcion_final\n            )\n\n        guardar_estado_anuncio(\n            ultima_carpeta_anuncio,\n            "GENERADO",\n            titulo_anuncio,\n            precio\n        )\n\n        incrementar_estadistica("generados")\n\n        estado.configure(\n            text=(\n                "✅ Anuncio completo "\n                "generado y guardado"\n            )\n        )\n\n        try:\n            refrescar_historial()\n        except Exception:\n            pass\n\n        guardar_borrador(silencioso=True)\n\n    except Exception as error:\n\n        estado.configure(\n            text="❌ Error: " + str(error)\n        )\n\n    finally:\n\n        boton.configure(\n            state="normal"\n        )\n\n\n\n\n# =========================================================\n# PUENTE LOCAL PARA LA EXTENSIÓN\n# =========================================================\n\nclass WallapopBridgeHandler(BaseHTTPRequestHandler):\n\n    def _cors(self):\n        self.send_header("Access-Control-Allow-Origin", "*")\n        self.send_header("Access-Control-Allow-Methods", "GET, OPTIONS")\n        self.send_header("Access-Control-Allow-Headers", "Content-Type")\n\n    def do_OPTIONS(self):\n        self.send_response(204)\n        self._cors()\n        self.end_headers()\n\n    def do_GET(self):\n\n        # Datos del anuncio\n        if self.path.startswith("/anuncio"):\n            contenido = json.dumps(\n                anuncio_extension,\n                ensure_ascii=False\n            ).encode("utf-8")\n\n            self.send_response(200)\n            self.send_header("Content-Type", "application/json; charset=utf-8")\n            self.send_header("Content-Length", str(len(contenido)))\n            self._cors()\n            self.end_headers()\n            self.wfile.write(contenido)\n            return\n\n        # Imágenes del último anuncio:\n        # /imagen/1\n        if self.path.startswith("/imagen/"):\n\n            try:\n                numero = int(\n                    self.path.split("/imagen/", 1)[1].split("?", 1)[0]\n                )\n            except:\n                numero = 0\n\n            if numero != 1:\n                self.send_response(404)\n                self._cors()\n                self.end_headers()\n                return\n\n            if (\n                not ultima_carpeta_anuncio\n                or not os.path.isdir(ultima_carpeta_anuncio)\n            ):\n                self.send_response(404)\n                self._cors()\n                self.end_headers()\n                return\n\n            ruta = os.path.join(\n                ultima_carpeta_anuncio,\n                f"imagen_{numero}.png"\n            )\n\n            if not os.path.isfile(ruta):\n                self.send_response(404)\n                self._cors()\n                self.end_headers()\n                return\n\n            with open(ruta, "rb") as archivo:\n                contenido = archivo.read()\n\n            self.send_response(200)\n            self.send_header("Content-Type", "image/png")\n            self.send_header("Content-Length", str(len(contenido)))\n            self._cors()\n            self.end_headers()\n            self.wfile.write(contenido)\n            return\n\n        contenido = json.dumps(\n            {"error": "Ruta no encontrada"},\n            ensure_ascii=False\n        ).encode("utf-8")\n\n        self.send_response(404)\n        self.send_header("Content-Type", "application/json; charset=utf-8")\n        self.send_header("Content-Length", str(len(contenido)))\n        self._cors()\n        self.end_headers()\n        self.wfile.write(contenido)\n\n    def log_message(self, format, *args):\n        pass\n\n\ndef iniciar_servidor_extension():\n\n    try:\n        servidor = ThreadingHTTPServer(\n            ("127.0.0.1", 8765),\n            WallapopBridgeHandler\n        )\n\n        hilo = threading.Thread(\n            target=servidor.serve_forever,\n            daemon=True\n        )\n\n        hilo.start()\n\n        print(\n            "Puente Wallapop activo en "\n            "http://127.0.0.1:8765/anuncio"\n        )\n\n        return servidor\n\n    except OSError as error:\n\n        print(\n            "No se pudo abrir el puerto 8765:",\n            error\n        )\n\n        return None\n\n\n# =========================================================\n# PREPARAR EN WALLAPOP\n# =========================================================\n\n\ndef validar_anuncio_antes_de_wallapop(mostrar_ok=False):\n    """\n    Comprueba todo antes de abrir/usar Wallapop.\n    Devuelve (ok, errores).\n    """\n    errores = []\n\n    titulo = caja_titulo.get("1.0", "end-1c").strip()\n    descripcion = obtener_descripcion_actual().strip()\n    precio = entrada_precio.get().strip()\n    categoria = categoria_publicacion.get().strip()\n    estado_elegido = estado_producto_publicacion.get().strip()\n\n    # Título\n    if not titulo:\n        errores.append("Falta el título.")\n    elif len(titulo) > 50:\n        errores.append(\n            f"El título tiene {len(titulo)} caracteres; Wallapop admite 50."\n        )\n\n    # Descripción\n    if not descripcion:\n        errores.append("Falta la descripción.")\n    elif len(descripcion) > 640:\n        errores.append(\n            f"La descripción tiene {len(descripcion)} caracteres; "\n            "el bot enviará como máximo 640."\n        )\n\n    # Precio\n    if not precio:\n        errores.append("Falta el precio.")\n    else:\n        precio_limpio = (\n            precio.replace("€", "")\n            .replace(",", ".")\n            .strip()\n        )\n        try:\n            valor_precio = float(precio_limpio)\n            if valor_precio <= 0:\n                errores.append("El precio debe ser mayor que 0.")\n        except Exception:\n            errores.append("El precio no es válido.")\n\n    # Categoría\n    if not categoria:\n        errores.append("Falta la categoría.")\n    elif ">" not in categoria:\n        errores.append(\n            "La categoría debe escribirse como una ruta separada por >."\n        )\n\n    # Estado\n    if not estado_elegido:\n        errores.append("Falta el estado del producto.")\n\n    # Imágenes\n    if not ultima_carpeta_anuncio:\n        errores.append("No hay un anuncio generado todavía.")\n    elif not os.path.isdir(ultima_carpeta_anuncio):\n        errores.append("No encuentro la carpeta del último anuncio.")\n    else:\n        imagenes = sorted(\n            glob.glob(\n                os.path.join(\n                    ultima_carpeta_anuncio,\n                    "imagen_*.png"\n                )\n            )\n        )[:1]\n\n        if len(imagenes) != 1:\n            errores.append(\n                f"Se necesita 1 imagen y he encontrado {len(imagenes)}."\n            )\n        else:\n            for i, ruta in enumerate(imagenes, start=1):\n                try:\n                    if os.path.getsize(ruta) <= 0:\n                        errores.append(\n                            f"La imagen {i} está vacía o dañada."\n                        )\n                except Exception:\n                    errores.append(\n                        f"No pude comprobar la imagen {i}."\n                    )\n\n    # Playwright\n    if sync_playwright is None:\n        errores.append(\n            "Playwright no está disponible. "\n            "Instálalo con: python -m pip install playwright"\n        )\n\n    # Chromium controlable / ejecutable local\n    if not _puerto_chrome_activo():\n        if not _encontrar_chromium():\n            errores.append(\n                "Chromium no está conectado y no encuentro "\n                "ningún ejecutable compatible automáticamente."\n            )\n\n    if errores:\n        mensaje = (\n            "No se puede continuar todavía:\\n\\n• "\n            + "\\n• ".join(errores)\n        )\n\n        estado.configure(\n            text=f"❌ Validación: {len(errores)} problema(s)."\n        )\n\n        try:\n            messagebox.showerror(\n                "Revisar anuncio",\n                mensaje\n            )\n        except Exception:\n            pass\n\n        print("VALIDACIÓN FALLIDA")\n        for error in errores:\n            print(" -", error)\n\n        return False, errores\n\n    estado.configure(\n        text="✅ Validación correcta. El anuncio está listo."\n    )\n\n    print("VALIDACIÓN OK: anuncio listo para Wallapop.")\n\n    if mostrar_ok:\n        try:\n            messagebox.showinfo(\n                "Anuncio listo",\n                "Todo está correcto.\\n\\n"\n                "El anuncio está listo para preparar o publicar."\n            )\n        except Exception:\n            pass\n\n    return True, []\n\n\ndef comprobar_anuncio_manual():\n    validar_anuncio_antes_de_wallapop(\n        mostrar_ok=True\n    )\n\n\ndef preparar_wallapop():\n\n    servicio = caja_titulo.get("1.0", "end-1c").strip()\n    descripcion = obtener_descripcion_actual()\n    precio = entrada_precio.get().strip()\n\n    if not servicio:\n        estado.configure(text="❌ Primero genera un anuncio.")\n        return\n\n    if not ultima_carpeta_anuncio or not os.path.isdir(ultima_carpeta_anuncio):\n        estado.configure(text="❌ No encuentro las imágenes del último anuncio.")\n        return\n\n    imagenes = sorted(\n        glob.glob(os.path.join(ultima_carpeta_anuncio, "imagen_*.png"))\n    )\n\n    if len(imagenes) < 1:\n        estado.configure(text="❌ No encuentro imágenes para subir.")\n        return\n\n    # Wallapop muestra un máximo de 50 caracteres para el título.\n    titulo_wallapop = servicio[:50]\n\n    estado.configure(text="⏳ Preparando formulario de Wallapop...")\n    boton_wallapop.configure(state="disabled")\n    try:\n        boton_preparar.configure(state="disabled")\n    except Exception:\n        pass\n\n    hilo = threading.Thread(\n        target=_preparar_wallapop_hilo,\n        args=(titulo_wallapop, descripcion, precio, imagenes),\n        daemon=True\n    )\n    hilo.start()\n\n\ndef _preparar_wallapop_hilo(titulo_wallapop, descripcion, precio, imagenes):\n\n    try:\n        with sync_playwright() as p:\n\n            navegador = p.chromium.connect_over_cdp(\n                "http://127.0.0.1:9222"\n            )\n\n            if not navegador.contexts:\n                raise RuntimeError("No se encontró el navegador controlable.")\n\n            contexto = navegador.contexts[0]\n\n            if contexto.pages:\n                pagina = contexto.pages[0]\n            else:\n                pagina = contexto.new_page()\n\n            pagina.goto(\n                "https://es.wallapop.com/app/catalog/upload/services",\n                wait_until="domcontentloaded",\n                timeout=60000\n            )\n\n            pagina.wait_for_timeout(2500)\n\n            # Si Wallapop redirige al login, no intentamos iniciar sesión\n            # ni resolver reCAPTCHA automáticamente.\n            if "accounts.wallapop.com" in pagina.url:\n                raise RuntimeError(\n                    "Wallapop pide iniciar sesión. Inicia sesión manualmente "\n                    "en este navegador y vuelve a pulsar PREPARAR EN WALLAPOP."\n                )\n\n            # Resumen inicial, si aparece.\n            resumen = pagina.locator(\n                \'textarea[placeholder*="Resumen"], input[placeholder*="Resumen"]\'\n            )\n            if resumen.count():\n                resumen.first.fill(titulo_wallapop)\n                continuar = pagina.get_by_role("button", name="Continuar")\n                if continuar.count():\n                    continuar.first.click()\n                    pagina.wait_for_timeout(1200)\n\n            # Título\n            titulo = pagina.locator(\n                \'input[placeholder*="Título"], textarea[placeholder*="Título"]\'\n            )\n            if not titulo.count():\n                titulo = pagina.locator(\'input\').filter(has=pagina.locator("xpath=.."))\n            if pagina.locator(\'input[placeholder*="Título"]\').count():\n                pagina.locator(\'input[placeholder*="Título"]\').first.fill(titulo_wallapop)\n            elif pagina.locator(\'textarea[placeholder*="Título"]\').count():\n                pagina.locator(\'textarea[placeholder*="Título"]\').first.fill(titulo_wallapop)\n\n            # Descripción\n            if pagina.locator(\'textarea[placeholder*="Descripción"]\').count():\n                pagina.locator(\'textarea[placeholder*="Descripción"]\').first.fill(\n                    descripcion\n                )\n\n            # Precio\n            campo_precio = pagina.locator(\n                \'input[placeholder*="Precio"], input[inputmode="decimal"], input[inputmode="numeric"]\'\n            )\n            if precio and campo_precio.count():\n                campo_precio.first.fill(precio)\n\n            # Fotos\n            selector_archivo = pagina.locator(\'input[type="file"]\')\n            if selector_archivo.count():\n                selector_archivo.first.set_input_files(imagenes[:1])\n\n            # Nunca pulsamos Publicar.\n            ventana.after(\n                0,\n                lambda: estado.configure(\n                    text="✅ Formulario preparado. Revísalo y pulsa Publicar manualmente."\n                )\n            )\n\n    except Exception as error:\n        mensaje = str(error)\n        ventana.after(\n            0,\n            lambda m=mensaje: estado.configure(text="❌ " + m)\n        )\n\n    finally:\n        ventana.after(\n            0,\n            lambda: (boton_wallapop.configure(state="normal"), boton_preparar.configure(state="normal"))\n        )\n\n\n\n\n# =========================================================\n# ESTADO E HISTORIAL DE ANUNCIOS\n# =========================================================\n\nARCHIVO_HISTORIAL_OCULTO = "historial_oculto.json"\n\n\ndef guardar_estado_anuncio(carpeta, estado_anuncio, titulo="", precio=""):\n    """\n    Guarda el estado del anuncio dentro de su propia carpeta.\n    Estados usados: GENERADO, PREPARADO, PUBLICADO.\n    """\n    if not carpeta:\n        return\n\n    try:\n        os.makedirs(carpeta, exist_ok=True)\n\n        ruta = os.path.join(carpeta, "estado.json")\n\n        datos = {\n            "estado": estado_anuncio,\n            "titulo": str(titulo).strip(),\n            "precio": str(precio).strip(),\n            "fecha_estado": time.strftime("%d/%m/%Y %H:%M:%S"),\n            "timestamp_estado": time.time()\n        }\n\n        with open(ruta, "w", encoding="utf-8") as archivo:\n            json.dump(datos, archivo, ensure_ascii=False, indent=4)\n\n    except Exception as e:\n        print("Aviso: no pude guardar estado del anuncio:", e)\n\n\ndef leer_estado_anuncio(carpeta):\n    ruta = os.path.join(str(carpeta), "estado.json")\n\n    if not os.path.isfile(ruta):\n        return {\n            "estado": "GENERADO",\n            "titulo": "",\n            "precio": "",\n            "fecha_estado": "",\n            "timestamp_estado": 0\n        }\n\n    try:\n        with open(ruta, "r", encoding="utf-8") as archivo:\n            datos = json.load(archivo)\n\n        if isinstance(datos, dict):\n            return datos\n    except Exception:\n        pass\n\n    return {\n        "estado": "GENERADO",\n        "titulo": "",\n        "precio": "",\n        "fecha_estado": "",\n        "timestamp_estado": 0\n    }\n\n\ndef leer_datos_anuncio_txt(carpeta):\n    """\n    Recupera título y precio de anuncio.txt si estado.json es antiguo\n    o no contiene esos datos.\n    """\n    resultado = {\n        "titulo": "",\n        "precio": ""\n    }\n\n    ruta = os.path.join(str(carpeta), "anuncio.txt")\n\n    if not os.path.isfile(ruta):\n        return resultado\n\n    try:\n        contenido = Path(ruta).read_text(encoding="utf-8")\n\n        m_titulo = re.search(\n            r"TÍTULO:\\s*\\n([^\\n]+)",\n            contenido,\n            re.IGNORECASE\n        )\n        if m_titulo:\n            resultado["titulo"] = m_titulo.group(1).strip()\n\n        m_precio = re.search(\n            r"PRECIO:\\s*\\n([^\\n]+)",\n            contenido,\n            re.IGNORECASE\n        )\n        if m_precio:\n            resultado["precio"] = m_precio.group(1).replace("€", "").strip()\n\n    except Exception:\n        pass\n\n    return resultado\n\n\ndef _cargar_corte_historial():\n    if not os.path.isfile(ARCHIVO_HISTORIAL_OCULTO):\n        return 0.0\n\n    try:\n        with open(ARCHIVO_HISTORIAL_OCULTO, "r", encoding="utf-8") as archivo:\n            datos = json.load(archivo)\n        return float(datos.get("ocultar_antes_de", 0))\n    except Exception:\n        return 0.0\n\n\ndef limpiar_historial_visual():\n    """\n    Limpia la lista visible sin borrar carpetas, imágenes ni publicados.json.\n    Los anuncios nuevos volverán a aparecer normalmente.\n    """\n    try:\n        with open(ARCHIVO_HISTORIAL_OCULTO, "w", encoding="utf-8") as archivo:\n            json.dump(\n                {"ocultar_antes_de": time.time()},\n                archivo,\n                ensure_ascii=False,\n                indent=4\n            )\n\n        estado.configure(\n            text="Historial visual limpiado. No se han borrado anuncios ni imágenes."\n        )\n        refrescar_historial()\n\n    except Exception as e:\n        estado.configure(text="❌ No pude limpiar el historial: " + str(e))\n\n\nfiltro_historial = "TODOS"\n\n\ndef ver_historial_todos():\n    global filtro_historial\n    filtro_historial = "TODOS"\n    refrescar_historial()\n\n\ndef ver_historial_publicados():\n    global filtro_historial\n    filtro_historial = "PUBLICADO"\n    refrescar_historial()\n\n\n# =========================================================\n# PROTECCIÓN CONTRA PUBLICACIONES DUPLICADAS\n# =========================================================\n\nARCHIVO_PUBLICADOS = os.path.join(\n    CARPETA_DATOS_USUARIO,\n    "publicados.json"\n)\n\n_PUBLICADOS_LEGACY_MIGRADO = False\n\n\ndef _migrar_publicados_legacy():\n    global _PUBLICADOS_LEGACY_MIGRADO\n\n    if _PUBLICADOS_LEGACY_MIGRADO:\n        return\n\n    _PUBLICADOS_LEGACY_MIGRADO = True\n\n    destino = Path(\n        ARCHIVO_PUBLICADOS\n    )\n\n    destino.parent.mkdir(\n        parents=True,\n        exist_ok=True\n    )\n\n    combinados = {}\n\n    # Conservar primero lo que ya exista en la ubicación persistente.\n    try:\n        if destino.exists():\n            datos = json.loads(\n                destino.read_text(\n                    encoding="utf-8"\n                )\n            )\n            if isinstance(datos, dict):\n                combinados.update(\n                    datos\n                )\n    except Exception:\n        pass\n\n    home = Path.home()\n\n    candidatos = [\n        Path.cwd() / "publicados.json",\n        Path(__file__).resolve().parent / "publicados.json",\n        home / "Downloads" / "publicados.json",\n        home / "Descargas" / "publicados.json",\n        home / "Desktop" / "publicados.json",\n        home / "Escritorio" / "publicados.json",\n        home / "Documents" / "publicados.json",\n        home / "Documentos" / "publicados.json",\n    ]\n\n    # Buscar copias antiguas dentro de las carpetas de DIAGPROG5 y ubicaciones\n    # habituales. Solo importamos diccionarios con estructura de publicados.\n    bases = [\n        Path(\n            CARPETA_DATOS_USUARIO\n        ),\n        home / "Downloads",\n        home / "Descargas",\n        home / "Desktop",\n        home / "Escritorio",\n        home / "Documents",\n        home / "Documentos",\n    ]\n\n    for base in bases:\n        try:\n            if base.exists():\n                candidatos.extend(\n                    base.rglob(\n                        "publicados.json"\n                    )\n                )\n        except Exception:\n            pass\n\n    vistos = set()\n\n    for ruta in candidatos:\n        try:\n            ruta = Path(\n                ruta\n            )\n\n            clave = str(\n                ruta.resolve()\n            ).lower()\n\n            if clave in vistos:\n                continue\n\n            vistos.add(\n                clave\n            )\n\n            if not ruta.is_file():\n                continue\n\n            datos = json.loads(\n                ruta.read_text(\n                    encoding="utf-8"\n                )\n            )\n\n            if not isinstance(\n                datos,\n                dict\n            ):\n                continue\n\n            for identificador, item in datos.items():\n                if not isinstance(\n                    item,\n                    dict\n                ):\n                    continue\n\n                # Solo migramos entradas que parecen registros DIAGPROG5.\n                if not str(\n                    item.get(\n                        "titulo",\n                        ""\n                    )\n                ).strip():\n                    continue\n\n                if not str(\n                    item.get(\n                        "fecha",\n                        ""\n                    )\n                ).strip():\n                    continue\n\n                existente = combinados.get(\n                    identificador\n                )\n\n                if not isinstance(\n                    existente,\n                    dict\n                ):\n                    combinados[\n                        identificador\n                    ] = dict(\n                        item\n                    )\n                else:\n                    # Mantener protecciones/estado ya guardados y completar\n                    # campos que falten con la copia antigua.\n                    for k, v in item.items():\n                        existente.setdefault(\n                            k,\n                            v\n                        )\n\n        except Exception:\n            pass\n\n    try:\n        contenido_publicados = json.dumps(\n            combinados,\n            ensure_ascii=False,\n            indent=4\n        )\n\n        ultimo_error = None\n\n        for _intento in range(8):\n            try:\n                destino.parent.mkdir(\n                    parents=True,\n                    exist_ok=True\n                )\n\n                temporal = destino.with_name(\n                    destino.name\n                    + ".tmp."\n                    + str(os.getpid())\n                )\n\n                temporal.write_text(\n                    contenido_publicados,\n                    encoding="utf-8"\n                )\n\n                os.replace(\n                    str(temporal),\n                    str(destino)\n                )\n\n                ultimo_error = None\n                break\n\n            except OSError as e:\n                ultimo_error = e\n\n                if (\n                    isinstance(e, PermissionError)\n                    or getattr(\n                        e,\n                        "winerror",\n                        None\n                    ) == 32\n                ):\n                    time.sleep(\n                        0.15 * (_intento + 1)\n                    )\n                    continue\n\n                break\n\n        if ultimo_error is not None:\n            print(\n                "[ROTACION] No pude migrar publicados.json ahora:",\n                ultimo_error\n            )\n    except Exception:\n        pass\n\n\n\ndef crear_id_anuncio(titulo, descripcion, precio):\n    contenido = "|".join([\n        str(titulo).strip().lower(),\n        str(descripcion).strip().lower(),\n        str(precio).strip().lower()\n    ])\n    return hashlib.sha256(contenido.encode("utf-8")).hexdigest()\n\n\ndef cargar_publicados():\n    try:\n        _migrar_publicados_legacy()\n    except Exception:\n        pass\n\n    if not os.path.isfile(ARCHIVO_PUBLICADOS):\n        return {}\n\n    try:\n        with open(ARCHIVO_PUBLICADOS, "r", encoding="utf-8") as archivo:\n            datos = json.load(archivo)\n        return datos if isinstance(datos, dict) else {}\n    except Exception:\n        return {}\n\n\ndef anuncio_ya_publicado(titulo, descripcion, precio):\n    return crear_id_anuncio(titulo, descripcion, precio) in cargar_publicados()\n\n\ndef guardar_anuncio_publicado(titulo, descripcion, precio, url_anuncio=""):\n    publicados = cargar_publicados()\n    identificador = crear_id_anuncio(titulo, descripcion, precio)\n\n    registro = {\n        "titulo": str(titulo).strip(),\n        "precio": str(precio).strip(),\n        "fecha": time.strftime("%d/%m/%Y %H:%M:%S")\n    }\n\n    url_limpia = str(\n        url_anuncio\n        or ""\n    ).strip()\n\n    if (\n        "wallapop.com" in url_limpia.lower()\n        and "/item/" in url_limpia.lower()\n    ):\n        registro["url"] = url_limpia\n        registro["url_anuncio"] = url_limpia\n\n    publicados[identificador] = registro\n\n    ruta_publicados = Path(\n        ARCHIVO_PUBLICADOS\n    )\n\n    ruta_publicados.parent.mkdir(\n        parents=True,\n        exist_ok=True\n    )\n\n    temporal = ruta_publicados.with_suffix(\n        ".json.tmp"\n    )\n\n    temporal.write_text(\n        json.dumps(\n            publicados,\n            ensure_ascii=False,\n            indent=4\n        ),\n        encoding="utf-8"\n    )\n\n    _ultimo_error_publicados = None\n\n    for _intento_publicados in range(8):\n        try:\n            os.replace(\n                str(temporal),\n                str(ruta_publicados)\n            )\n\n            _ultimo_error_publicados = None\n            break\n\n        except OSError as e:\n            _ultimo_error_publicados = e\n\n            if (\n                isinstance(e, PermissionError)\n                or getattr(\n                    e,\n                    "winerror",\n                    None\n                ) == 32\n            ):\n                time.sleep(\n                    0.15 * (_intento_publicados + 1)\n                )\n                continue\n\n            break\n\n    if _ultimo_error_publicados is not None:\n        # No bloquear el bot por un lock temporal de Windows.\n        print(\n            "[PUBLICADOS] Archivo ocupado temporalmente:",\n            _ultimo_error_publicados\n        )\n\n\n# =========================================================\n# SEGUIMIENTO DE FASES Y ERRORES\n# =========================================================\n\nfase_actual = "En espera"\n\nultimo_error_cola = {\n    "fase": "",\n    "mensaje": "",\n    "fecha": "",\n    "diagnostico": ""\n}\n\n\ndef actualizar_fase(nombre, detalle=""):\n    global fase_actual\n\n    fase_actual = nombre\n\n    texto = f"Fase actual: {nombre}"\n    if detalle:\n        texto += f" · {detalle}"\n\n    print(texto)\n\n    try:\n        ventana.after(\n            0,\n            lambda t=texto: etiqueta_fase.configure(text=t)\n        )\n    except Exception:\n        pass\n\n\ndef mostrar_error_fase(error):\n    mensaje = str(error)\n    texto = f"❌ Error en {fase_actual}: {mensaje}"\n\n    print(texto)\n\n    try:\n        ventana.after(\n            0,\n            lambda t=texto: estado.configure(text=t)\n        )\n    except Exception:\n        pass\n\n    try:\n        ventana.after(\n            0,\n            lambda: etiqueta_fase.configure(\n                text=f"Fase actual: ERROR · {fase_actual}"\n            )\n        )\n    except Exception:\n        pass\n\n    try:\n        messagebox.showerror(\n            "Error durante la automatización",\n            f"El bot se detuvo en:\\n\\n{fase_actual}\\n\\n"\n            f"Motivo:\\n{mensaje}"\n        )\n    except Exception:\n        pass\n\n\n# =========================================================\n# DIAGNÓSTICO AUTOMÁTICO DE FALLOS DE PUBLICACIÓN\n# =========================================================\n\nCARPETA_ERRORES_PUBLICACION = "errores_publicacion"\n\n\ndef _nombre_seguro_archivo(texto):\n    texto = re.sub(r\'[^a-zA-Z0-9áéíóúÁÉÍÓÚñÑ_-]+\', \'_\', str(texto).strip())\n    texto = texto.strip("_")\n    return texto[:60] or "anuncio"\n\n\ndef guardar_diagnostico_fallo_publicacion(\n    page,\n    titulo,\n    fase,\n    mensaje,\n    carpeta_anuncio=None\n):\n    """\n    Guarda captura, HTML y un TXT con URL/fase/error.\n    No contiene cookies ni contraseñas.\n    """\n    try:\n        os.makedirs(CARPETA_ERRORES_PUBLICACION, exist_ok=True)\n\n        sello = time.strftime("%Y%m%d_%H%M%S")\n        nombre = f"{sello}_{_nombre_seguro_archivo(titulo)}"\n        carpeta = os.path.abspath(\n            os.path.join(CARPETA_ERRORES_PUBLICACION, nombre)\n        )\n        os.makedirs(carpeta, exist_ok=True)\n\n        url = "No disponible"\n\n        if page is not None:\n            try:\n                url = page.url or "No disponible"\n            except Exception:\n                pass\n\n            try:\n                page.screenshot(\n                    path=os.path.join(carpeta, "captura_error.png"),\n                    full_page=True\n                )\n            except Exception as e:\n                print("No pude guardar captura del error:", e)\n\n            try:\n                html = page.content()\n                Path(\n                    os.path.join(carpeta, "pagina_error.html")\n                ).write_text(\n                    html,\n                    encoding="utf-8",\n                    errors="ignore"\n                )\n            except Exception as e:\n                print("No pude guardar HTML del error:", e)\n\n        informe = (\n            "ERROR DE PUBLICACIÓN · WallapopBot\\n"\n            "====================================\\n\\n"\n            f"Fecha: {time.strftime(\'%d/%m/%Y %H:%M:%S\')}\\n"\n            f"Anuncio: {titulo}\\n"\n            f"Fase: {fase}\\n"\n            f"URL: {url}\\n\\n"\n            f"Motivo:\\n{mensaje}\\n\\n"\n        )\n\n        if carpeta_anuncio:\n            informe += f"Carpeta del anuncio: {carpeta_anuncio}\\n"\n\n        Path(\n            os.path.join(carpeta, "error.txt")\n        ).write_text(\n            informe,\n            encoding="utf-8"\n        )\n\n        print("Diagnóstico de error guardado en:", carpeta)\n        return carpeta\n\n    except Exception as e:\n        print("No pude crear diagnóstico de error:", e)\n        return ""\n\n\ndef fase_segura_para_reintento(fase):\n    """\n    Solo reintentamos automáticamente antes de haber pulsado Publicar.\n    Evita duplicar anuncios si no sabemos con certeza qué ocurrió después del clic.\n    """\n    return str(fase) in {\n        "Navegador",\n        "Inicio de venta",\n        "Resumen",\n        "Fotos",\n        "Categoría",\n        "Datos del anuncio",\n        "Estado",\n        "Precio",\n        "Envío",\n        "Final",\n    }\n\n\n# =========================================================\n# PUBLICAR EN WALLAPOP - TODO DESDE EL BOT\n# =========================================================\n\ndef visible(locator):\n    try:\n        return locator.count() > 0 and locator.first.is_visible()\n    except Exception:\n        return False\n\n\ndef esperar_producto(page, timeout=6000):\n    try:\n        page.get_by_text("Información del producto", exact=True).wait_for(\n            state="visible",\n            timeout=timeout\n        )\n        return True\n    except Exception:\n        return False\n\n\ndef click_continuar_visible(page):\n    # Los botones reales están dentro de Shadow DOM.\n    # Playwright atraviesa Shadow DOM abierto con get_by_role.\n    botones = page.get_by_role("button", name="Continuar")\n    total = botones.count()\n\n    for i in range(total - 1, -1, -1):\n        b = botones.nth(i)\n        try:\n            if b.is_visible() and b.is_enabled():\n                b.scroll_into_view_if_needed()\n                b.click(timeout=5000)\n                return True\n        except Exception:\n            pass\n\n    try:\n        ok = page.evaluate("""\n            () => {\n                const candidatos = [];\n                const walk = root => {\n                    const nodes = root.querySelectorAll(\'*\');\n                    for (const el of nodes) {\n                        if (el.shadowRoot) walk(el.shadowRoot);\n                        const txt = (el.innerText || el.textContent || \'\').trim().toLowerCase();\n                        if (\n                            (el.tagName === \'BUTTON\' || el.getAttribute(\'role\') === \'button\')\n                            && txt.includes(\'continuar\')\n                        ) {\n                            candidatos.push(el);\n                        }\n                    }\n                };\n                walk(document);\n\n                const visibles = candidatos.filter(el => {\n                    const r = el.getBoundingClientRect();\n                    return r.width > 0 && r.height > 0 && !el.disabled;\n                });\n\n                const el = visibles[visibles.length - 1];\n                if (!el) return false;\n                el.scrollIntoView({block: \'center\'});\n                el.click();\n                return true;\n            }\n        """)\n\n        if ok:\n            return True\n    except Exception:\n        pass\n\n    try:\n        ok = page.evaluate("""\n            () => {\n                const candidatos = [];\n                const walk = root => {\n                    const nodes = root.querySelectorAll(\'*\');\n                    for (const el of nodes) {\n                        if (el.shadowRoot) walk(el.shadowRoot);\n                        const txt = (el.innerText || el.textContent || \'\').trim().toLowerCase();\n                        if (\n                            (el.tagName === \'BUTTON\' || el.getAttribute(\'role\') === \'button\')\n                            && txt.includes(\'continuar\')\n                        ) {\n                            candidatos.push(el);\n                        }\n                    }\n                };\n                walk(document);\n\n                const visibles = candidatos.filter(el => {\n                    const r = el.getBoundingClientRect();\n                    return r.width > 0 && r.height > 0 && !el.disabled;\n                });\n\n                const el = visibles[visibles.length - 1];\n                if (!el) return false;\n                el.scrollIntoView({block: \'center\'});\n                el.click();\n                return true;\n            }\n        """)\n\n        if ok:\n            return True\n    except Exception:\n        pass\n\n    try:\n        ok = page.evaluate("""\n            () => {\n                const candidatos = [];\n                const walk = root => {\n                    const nodes = root.querySelectorAll(\'*\');\n                    for (const el of nodes) {\n                        if (el.shadowRoot) walk(el.shadowRoot);\n                        const txt = (el.innerText || el.textContent || \'\').trim().toLowerCase();\n                        if (\n                            (el.tagName === \'BUTTON\' || el.getAttribute(\'role\') === \'button\')\n                            && txt.includes(\'continuar\')\n                        ) {\n                            candidatos.push(el);\n                        }\n                    }\n                };\n                walk(document);\n\n                const visibles = candidatos.filter(el => {\n                    const r = el.getBoundingClientRect();\n                    return r.width > 0 && r.height > 0 && !el.disabled;\n                });\n\n                const el = visibles[visibles.length - 1];\n                if (!el) return false;\n                el.scrollIntoView({block: \'center\'});\n                el.click();\n                return true;\n            }\n        """)\n\n        if ok:\n            return True\n    except Exception:\n        pass\n\n    try:\n        ok = page.evaluate("""\n            () => {\n                const candidatos = [];\n                const walk = root => {\n                    const nodes = root.querySelectorAll(\'*\');\n                    for (const el of nodes) {\n                        if (el.shadowRoot) walk(el.shadowRoot);\n                        const txt = (el.innerText || el.textContent || \'\').trim().toLowerCase();\n                        if (\n                            (el.tagName === \'BUTTON\' || el.getAttribute(\'role\') === \'button\')\n                            && txt.includes(\'continuar\')\n                        ) {\n                            candidatos.push(el);\n                        }\n                    }\n                };\n                walk(document);\n\n                const visibles = candidatos.filter(el => {\n                    const r = el.getBoundingClientRect();\n                    return r.width > 0 && r.height > 0 && !el.disabled;\n                });\n\n                const el = visibles[visibles.length - 1];\n                if (!el) return false;\n                el.scrollIntoView({block: \'center\'});\n                el.click();\n                return true;\n            }\n        """)\n\n        if ok:\n            return True\n    except Exception:\n        pass\n\n    return False\n\n\n\n\ndef esperar_y_rellenar_resumen(page, titulo):\n    """\n    Espera a que aparezca el primer formulario después de pulsar \'Mis servicios\',\n    rellena Resumen y pulsa el primer Continuar.\n    """\n    print("Esperando el campo Resumen...")\n\n    resumen = page.locator("#summary")\n\n    try:\n        resumen.first.wait_for(state="visible", timeout=20000)\n    except Exception:\n        # Fallback por nombre/placeholder\n        resumen = page.locator(\'input[name="summary"], textarea[name="summary"]\')\n        try:\n            resumen.first.wait_for(state="visible", timeout=5000)\n        except Exception:\n            raise RuntimeError(\n                "Mis servicios está seleccionado, pero no encuentro el campo Resumen."\n            )\n\n    resumen.first.fill(str(titulo)[:50])\n    print("Resumen rellenado.")\n\n    page.wait_for_timeout(500)\n\n    # En esta primera pantalla Wallapop usa <walla-button> con el botón real\n    # dentro de su shadowRoot. Probamos primero el método normal y, si falla,\n    # hacemos clic directamente sobre ese botón interno.\n    pulsado = click_continuar_visible(page)\n\n    if not pulsado:\n        try:\n            pulsado = page.evaluate("""\n                () => {\n                    const hosts = [...document.querySelectorAll(\'walla-button\')];\n\n                    // Buscamos desde el final porque normalmente el Continuar\n                    // de este formulario es el último walla-button visible.\n                    for (let i = hosts.length - 1; i >= 0; i--) {\n                        const host = hosts[i];\n                        const texto = (host.textContent || \'\').trim().toLowerCase();\n                        const boton = host.shadowRoot?.querySelector(\'button\');\n\n                        if (!boton) continue;\n\n                        const rect = host.getBoundingClientRect();\n                        const visible =\n                            rect.width > 0 &&\n                            rect.height > 0 &&\n                            getComputedStyle(host).visibility !== \'hidden\';\n\n                        if (!visible) continue;\n\n                        const esContinuar =\n                            texto.includes(\'continuar\') ||\n                            host.getAttribute(\'data-testid\') === \'continue-button\' ||\n                            (boton.textContent || \'\').trim().toLowerCase().includes(\'continuar\');\n\n                        if (esContinuar && !boton.disabled) {\n                            boton.scrollIntoView({block: \'center\'});\n                            boton.click();\n                            return true;\n                        }\n                    }\n                    return false;\n                }\n            """)\n        except Exception:\n            pulsado = False\n\n    if not pulsado:\n        raise RuntimeError(\n            "Resumen rellenado, pero no pude pulsar el primer Continuar ni por "\n            "Playwright ni entrando directamente en el Shadow DOM."\n        )\n\n    print("Primer Continuar pulsado.")\n\n    # Esperar a que aparezca la zona de fotos.\n    try:\n        page.locator("#dropAreaPreviewInput").first.wait_for(\n            state="attached", timeout=15000\n        )\n    except Exception:\n        pass\n\n    page.wait_for_timeout(500)\n    return True\n\n\n\ndef subir_fotos_robusto(page, archivos):\n    """\n    Sube las fotos aunque Wallapop cambie el id del input.\n    Busca primero #dropAreaPreviewInput y después cualquier input[type=file].\n    """\n    print("Buscando el selector de fotos...")\n\n    candidatos = page.locator("#dropAreaPreviewInput")\n    if not candidatos.count():\n        candidatos = page.locator(\'input[type="file"]\')\n\n    if not candidatos.count():\n        raise RuntimeError("No encuentro ningún input para subir fotos.")\n\n    elegido = None\n    for i in range(candidatos.count()):\n        c = candidatos.nth(i)\n        try:\n            acepta = (c.get_attribute("accept") or "").lower()\n            if "image" in acepta or not acepta:\n                elegido = c\n                break\n        except Exception:\n            pass\n\n    if elegido is None:\n        elegido = candidatos.first\n\n    print("Subiendo", len(archivos), "fotos...")\n    elegido.set_input_files(archivos)\n\n    # Esperar a que aparezcan previsualizaciones o cambie el estado del input.\n    page.wait_for_timeout(2500)\n\n    # Comprobación visual aproximada: al menos una imagen/previsualización visible.\n    try:\n        previews = page.locator("img:visible")\n        print("Imágenes visibles tras subir:", previews.count())\n    except Exception:\n        pass\n\n    return True\n\n\ndef entrar_en_vender(page):\n    """\n    Flujo inicial:\n    Wallapop -> Vender -> Algo que ya no necesito -> Resumen.\n\n    Si ya estamos en una pantalla posterior, no vuelve atrás.\n    """\n\n    # Si ya estamos dentro del flujo, no repetir el inicio.\n    try:\n        if page.locator("#summary").count():\n            print("Ya estamos en la pantalla de Resumen.")\n            return True\n        if page.locator("#dropAreaPreviewInput").count():\n            print("Ya estamos en la pantalla de Fotos.")\n            return True\n        if page.locator(\'input[name="category_leaf_id"]\').count():\n            print("Ya estamos en la pantalla de Categoría.")\n            return True\n        if formulario_producto_visible(page):\n            print("Ya estamos en el formulario final.")\n            return True\n    except Exception:\n        pass\n\n    opcion_texto = "Algo que ya no necesito"\n\n    # Si ya estamos en "¿Qué subirás?", pulsar directamente la opción correcta.\n    try:\n        opcion = page.get_by_text(opcion_texto, exact=True)\n        if opcion.count() and opcion.first.is_visible():\n            print("Pantalla \'¿Qué subirás?\' encontrada.")\n            opcion.first.click(timeout=7000)\n            print("\'Algo que ya no necesito\' pulsado. Esperando Resumen...")\n\n            try:\n                page.locator("#summary").first.wait_for(\n                    state="visible",\n                    timeout=15000\n                )\n                print("Campo Resumen visible.")\n                return True\n            except Exception:\n                raise RuntimeError(\n                    "Pulsé \'Algo que ya no necesito\', pero no apareció Resumen."\n                )\n    except RuntimeError:\n        raise\n    except Exception:\n        pass\n\n    print("Buscando el botón Vender...")\n\n    vender = page.get_by_role("button", name=re.compile(r"^Vender$", re.I))\n    if not vender.count():\n        vender = page.get_by_text("Vender", exact=True)\n\n    if not vender.count():\n        raise RuntimeError(\n            "No encuentro el botón Vender. "\n            "Deja abierta una página de Wallapop con tu sesión iniciada."\n        )\n\n    vender.first.click(timeout=7000)\n    print("Vender pulsado.")\n\n    opcion = page.get_by_text(opcion_texto, exact=True)\n\n    try:\n        opcion.first.wait_for(state="visible", timeout=15000)\n    except Exception:\n        raise RuntimeError(\n            "Pulsé Vender, pero no apareció \'Algo que ya no necesito\'."\n        )\n\n    opcion.first.click(timeout=7000)\n    print("\'Algo que ya no necesito\' pulsado. Esperando Resumen...")\n\n    try:\n        page.locator("#summary").first.wait_for(\n            state="visible",\n            timeout=15000\n        )\n    except Exception:\n        raise RuntimeError(\n            "Pulsé \'Algo que ya no necesito\', pero no apareció Resumen."\n        )\n\n    print("Campo Resumen visible.")\n    return True\n\n\ndef formulario_producto_visible(page):\n    """\n    Detecta el formulario final de \'Algo que ya no necesito\'.\n\n    Lo consideramos visible cuando aparecen los campos propios del producto:\n    Título/Descripción y Estado/Precio. No dependemos de un número concreto\n    de inputs porque Wallapop cambia la estructura del DOM.\n    """\n    try:\n        cuerpo = (page.locator("body").inner_text() or "").lower()\n\n        tiene_titulo = "título" in cuerpo or "titulo" in cuerpo\n        tiene_descripcion = "descripción" in cuerpo or "descripcion" in cuerpo\n        tiene_estado = "estado" in cuerpo\n        tiene_precio = "precio" in cuerpo\n\n        if tiene_titulo and tiene_descripcion and tiene_estado and tiene_precio:\n            return True\n    except Exception:\n        pass\n\n    # Fallback estructural.\n    try:\n        producto = page.locator("tsl-upload-product")\n        if producto.count():\n            txt = (producto.first.inner_text() or "").lower()\n            if (\n                ("título" in txt or "titulo" in txt)\n                and ("descripción" in txt or "descripcion" in txt)\n                and "precio" in txt\n            ):\n                return True\n    except Exception:\n        pass\n\n    return False\n\n\ndef _texto_control_categoria(page):\n    """\n    Devuelve el texto visible del componente de categoría, si puede leerlo.\n    """\n    try:\n        inp = page.locator(\'input[name="category_leaf_id"]\')\n        if inp.count():\n            txt = inp.first.evaluate("""\n                el => {\n                    const host = el.closest(\'walla-text-input\');\n                    return (host?.innerText || host?.textContent || \'\').trim();\n                }\n            """)\n            if txt:\n                return txt\n    except Exception:\n        pass\n\n    try:\n        bloques = page.locator("section, div").filter(\n            has_text="Categoría y subcategoría"\n        )\n        for i in range(min(bloques.count(), 12)):\n            b = bloques.nth(i)\n            if b.is_visible():\n                txt = (b.inner_text() or "").strip()\n                if txt:\n                    return txt\n    except Exception:\n        pass\n\n    return ""\n\n\ndef categoria_confirmada(page):\n    """\n    Confirma la ruta:\n    Motor y accesorios > Recambios de coches y furgonetas > Tuning\n\n    La comprobación principal es que el selector/breadcrumb muestre Tuning.\n    """\n    txt = _texto_control_categoria(page).lower()\n\n    if "tuning" in txt:\n        return True\n\n    try:\n        body = (page.locator("body").inner_text() or "").lower()\n        if (\n            "tuning" in body\n            and "motor y accesorios" in body\n            and (\n                "recambios de coches y furgonetas" in body\n                or "recambios de coches" in body\n            )\n        ):\n            return True\n    except Exception:\n        pass\n\n    return False\n\n\ndef _abrir_categoria(page):\n    input_categoria = page.locator(\'input[name="category_leaf_id"]\')\n\n    try:\n        input_categoria.first.wait_for(state="attached", timeout=15000)\n    except Exception:\n        pass\n\n    if input_categoria.count() == 0:\n        raise RuntimeError(\n            "No encuentro el control de Categoría y subcategoría."\n        )\n\n    try:\n        input_categoria.first.evaluate("""\n            el => {\n                const host = el.closest(\'walla-text-input\');\n                if (host) {\n                    host.scrollIntoView({block:\'center\'});\n                    host.click();\n                } else {\n                    el.click();\n                }\n            }\n        """)\n    except Exception:\n        input_categoria.first.click(force=True, timeout=3000)\n\n    page.wait_for_timeout(650)\n    return True\n\n\ndef _click_opcion_categoria(page, texto, timeout=10000):\n    """\n    Pulsa una opción exacta del desplegable de categorías.\n    Funciona con walla-dropdown-item y con texto/rol como fallback.\n    """\n    print(f"Buscando categoría: {texto}")\n\n    # 1) aria-label exacto\n    opcion = page.locator(\n        f\'walla-dropdown-item[role="option"][aria-label="{texto}"]\'\n    )\n\n    try:\n        opcion.first.wait_for(state="attached", timeout=2500)\n    except Exception:\n        pass\n\n    candidatos = []\n\n    if opcion.count():\n        candidatos = [opcion.nth(i) for i in range(opcion.count())]\n    else:\n        # 2) rol option\n        try:\n            por_rol = page.get_by_role("option", name=texto, exact=True)\n            if por_rol.count():\n                candidatos = [\n                    por_rol.nth(i) for i in range(por_rol.count())\n                ]\n        except Exception:\n            pass\n\n    if not candidatos:\n        # 3) texto exacto visible\n        try:\n            por_texto = page.get_by_text(texto, exact=True)\n            if por_texto.count():\n                candidatos = [\n                    por_texto.nth(i) for i in range(por_texto.count())\n                ]\n        except Exception:\n            pass\n\n    # Preferir candidatos visibles y, si hay duplicados, el último.\n    visibles = []\n    for c in candidatos:\n        try:\n            if c.is_visible():\n                visibles.append(c)\n        except Exception:\n            pass\n\n    if not visibles:\n        raise RuntimeError(\n            f"No encuentro la opción de categoría \'{texto}\'."\n        )\n\n    objetivo = visibles[-1]\n\n    try:\n        objetivo.scroll_into_view_if_needed()\n    except Exception:\n        pass\n\n    try:\n        objetivo.click(timeout=5000)\n    except Exception:\n        objetivo.click(force=True, timeout=3000)\n\n    print(f"Categoría pulsada: {texto}")\n    page.wait_for_timeout(650)\n    return True\n\n\ndef seleccionar_categoria_configurada(page, ruta_categoria):\n    """\n    Selecciona una ruta de categoría configurable desde la interfaz.\n    Ejemplo:\n    Motor y accesorios > Recambios de coches y furgonetas > Tuning\n    """\n    partes = [\n        p.strip()\n        for p in str(ruta_categoria).replace("›", ">").split(">")\n        if p.strip()\n    ]\n\n    if not partes:\n        raise RuntimeError("No has indicado una categoría.")\n\n    print("Seleccionando categoría: " + " > ".join(partes))\n    _abrir_categoria(page)\n\n    for indice, parte in enumerate(partes):\n        try:\n            _click_opcion_categoria(page, parte)\n        except Exception:\n            # Compatibilidad con el nombre que Wallapop ha mostrado\n            # en algunas versiones.\n            if parte == "Recambios de coches y furgonetas":\n                _click_opcion_categoria(page, "Recambios de coches")\n            else:\n                raise\n\n        page.wait_for_timeout(500)\n\n    print("Categoría seleccionada. Esperando formulario...")\n    page.wait_for_timeout(1200)\n\n    for _ in range(20):\n        if formulario_producto_visible(page):\n            return True\n        page.wait_for_timeout(250)\n\n    # Algunas categorías pueden mostrar el formulario con un pequeño retraso\n    # o con una estructura diferente; si el último nombre aparece en el\n    # selector, consideramos que la selección se hizo.\n    try:\n        texto_categoria = _texto_control_categoria(page).lower()\n        if partes[-1].lower() in texto_categoria:\n            return True\n    except Exception:\n        pass\n\n    raise RuntimeError(\n        "La categoría se seleccionó, pero Wallapop no mostró "\n        "el formulario final."\n    )\n\n\ndef seleccionar_categoria_tuning(page):\n    # Compatibilidad con versiones anteriores.\n    return seleccionar_categoria_configurada(\n        page,\n        "Motor y accesorios > Recambios de coches y furgonetas > Tuning"\n    )\n\n\ndef rellenar_campo_por_label(page, etiqueta, valor, limite=None):\n    """\n    Fallback genérico.\n    """\n    if valor is None:\n        return False\n\n    valor = str(valor).strip()\n    if not valor:\n        return False\n\n    if limite:\n        valor = valor[:limite]\n\n    # Intento por texto accesible.\n    try:\n        campo = page.get_by_label(\n            re.compile(rf"{re.escape(etiqueta)}", re.I)\n        )\n        for i in range(campo.count()):\n            c = campo.nth(i)\n            if c.is_visible():\n                c.fill(valor)\n                return True\n    except Exception:\n        pass\n\n    return False\n\n\ndef rellenar_formulario_producto(page, titulo, descripcion, precio):\n    """\n    Rellena Título, Descripción y Precio identificando cada campo por su propio\n    componente/label. No usamos posiciones globales de inputs porque Wallapop\n    mantiene inputs internos de categoría y búsqueda.\n    """\n    print("Localizando campos reales de Información del producto...")\n\n    producto = page.locator("tsl-upload-product")\n    try:\n        producto.first.wait_for(state="attached", timeout=8000)\n    except Exception:\n        pass\n\n    if not producto.count():\n        print("No encuentro tsl-upload-product")\n        return False, False, False\n\n    ok_titulo = False\n    ok_desc = False\n    ok_precio = False\n\n    # TÍTULO: buscar el walla-text-input cuyo texto contenga "Título".\n    componentes = producto.locator("walla-text-input")\n    for i in range(componentes.count()):\n        comp = componentes.nth(i)\n        try:\n            txt = (comp.inner_text() or comp.text_content() or "").strip().lower()\n        except Exception:\n            txt = ""\n\n        if "título" in txt or "titulo" in txt:\n            campo = comp.locator("input")\n            if campo.count():\n                try:\n                    campo.first.fill(str(titulo)[:50])\n                    ok_titulo = True\n                    print("Título: OK")\n                    break\n                except Exception as e:\n                    print("Título: error", e)\n\n    # DESCRIPCIÓN: buscar textarea dentro del componente cuyo texto diga Descripción.\n    componentes_desc = producto.locator("walla-text-input, walla-text-area, div")\n    for i in range(componentes_desc.count()):\n        comp = componentes_desc.nth(i)\n        try:\n            txt = (comp.inner_text() or comp.text_content() or "").strip().lower()\n        except Exception:\n            txt = ""\n\n        if "descripción" in txt or "descripcion" in txt:\n            campo = comp.locator("textarea")\n            if campo.count():\n                try:\n                    campo.first.fill(str(descripcion)[:640])\n                    ok_desc = True\n                    print("Descripción: OK")\n                    break\n                except Exception as e:\n                    print("Descripción: error", e)\n\n    # Fallback para descripción: único textarea visible dentro de tsl-upload-product.\n    if not ok_desc:\n        textareas = producto.locator("textarea:visible")\n        if textareas.count() >= 1:\n            try:\n                textareas.first.fill(str(descripcion)[:640])\n                ok_desc = True\n                print("Descripción: OK por textarea visible")\n            except Exception:\n                pass\n\n    # PRECIO: identificar el componente que contiene el texto "Precio".\n    for i in range(componentes.count()):\n        comp = componentes.nth(i)\n        try:\n            txt = (comp.inner_text() or comp.text_content() or "").strip().lower()\n        except Exception:\n            txt = ""\n\n        if "precio" in txt:\n            campo = comp.locator("input")\n            if campo.count():\n                try:\n                    campo.first.fill(str(precio).replace("€", "").strip())\n                    ok_precio = True\n                    print("Precio: OK")\n                    break\n                except Exception as e:\n                    print("Precio: error", e)\n\n    print(\n        "Resultado campos:",\n        "Título=", ok_titulo,\n        "Descripción=", ok_desc,\n        "Precio=", ok_precio\n    )\n    return ok_titulo, ok_desc, ok_precio\n\n\n\n\n\n\n\ndef seleccionar_estado_configurado(page, estado_elegido):\n    """\n    Selecciona el estado elegido desde la interfaz.\n    """\n    estado_elegido = str(estado_elegido).strip() or "Nuevo"\n    print(f"Seleccionando Estado: {estado_elegido}...")\n\n    etiquetas = [\n        page.get_by_text("Estado", exact=True),\n        page.get_by_text("Estado*", exact=True),\n        page.locator(\'label:has-text("Estado")\'),\n        page.locator(\'[aria-label*="Estado" i]\'),\n    ]\n\n    etiqueta = None\n    for loc in etiquetas:\n        try:\n            for i in range(min(loc.count(), 10)):\n                el = loc.nth(i)\n                if el.is_visible():\n                    etiqueta = el\n                    break\n            if etiqueta is not None:\n                break\n        except Exception:\n            pass\n\n    if etiqueta is None:\n        raise RuntimeError("No encuentro el campo Estado.")\n\n    etiqueta.scroll_into_view_if_needed()\n    page.wait_for_timeout(250)\n\n    abierto = False\n    candidatos = [etiqueta]\n\n    for nivel in range(1, 5):\n        try:\n            candidatos.append(etiqueta.locator("xpath=" + "/.." * nivel))\n        except Exception:\n            pass\n\n    for c in candidatos:\n        try:\n            if c.is_visible():\n                c.click(timeout=1200)\n                abierto = True\n                break\n        except Exception:\n            pass\n\n    if not abierto:\n        raise RuntimeError("No pude abrir el desplegable Estado.")\n\n    page.wait_for_timeout(350)\n\n    opciones = [\n        page.get_by_role("option", name=estado_elegido, exact=True),\n        page.locator(\n            \'[role="option"]\',\n            has_text=estado_elegido\n        ),\n        page.locator(\n            \'walla-dropdown-item\',\n            has_text=estado_elegido\n        ),\n        page.get_by_text(estado_elegido, exact=True),\n    ]\n\n    for loc in opciones:\n        try:\n            limite = min(loc.count(), 20)\n            for i in range(limite):\n                opcion = loc.nth(i)\n                if opcion.is_visible():\n                    opcion.click(timeout=1500)\n                    page.wait_for_timeout(350)\n                    print(f"Estado: {estado_elegido} seleccionado.")\n                    return True\n        except Exception:\n            pass\n\n    raise RuntimeError(\n        f"Abrí Estado, pero no encontré la opción \'{estado_elegido}\'."\n    )\n\n\ndef seleccionar_estado_nuevo(page):\n    return seleccionar_estado_configurado(page, "Nuevo")\n\n\ndef rellenar_precio_robusto(page, precio):\n    """\n    Rellena Precio DESPUÉS de Estado y verifica que no se vacíe.\n    """\n    valor = str(precio).replace("€", "").strip()\n    if not valor:\n        raise RuntimeError("El precio está vacío.")\n\n    print("Rellenando Precio de forma robusta...")\n\n    producto = page.locator("tsl-upload-product")\n    controles = producto.locator("walla-text-input")\n    campo_precio = None\n\n    for i in range(controles.count()):\n        comp = controles.nth(i)\n        try:\n            txt = (comp.inner_text() or comp.text_content() or "").strip().lower()\n        except Exception:\n            txt = ""\n        if "precio" in txt:\n            inp = comp.locator("input")\n            if inp.count():\n                campo_precio = inp.first\n                break\n\n    if campo_precio is None:\n        raise RuntimeError("No encuentro el campo Precio.")\n\n    # Setter normal\n    try:\n        campo_precio.fill(valor)\n    except Exception:\n        pass\n\n    page.wait_for_timeout(300)\n\n    # Setter nativo para que Angular reciba input/change correctamente.\n    actual = ""\n    try:\n        actual = campo_precio.input_value().strip()\n    except Exception:\n        pass\n\n    if not actual:\n        campo_precio.evaluate(\n            """(el, value) => {\n                const setter = Object.getOwnPropertyDescriptor(\n                    HTMLInputElement.prototype, \'value\'\n                ).set;\n                el.focus();\n                setter.call(el, value);\n                el.dispatchEvent(new InputEvent(\'input\', {\n                    bubbles: true,\n                    composed: true,\n                    inputType: \'insertText\',\n                    data: value\n                }));\n                el.dispatchEvent(new Event(\'change\', {\n                    bubbles: true,\n                    composed: true\n                }));\n                el.blur();\n            }""",\n            valor\n        )\n\n    page.wait_for_timeout(500)\n\n    try:\n        actual = campo_precio.input_value().strip()\n    except Exception:\n        actual = ""\n\n    if not actual:\n        raise RuntimeError("Wallapop dejó el Precio vacío.")\n\n    print("Precio confirmado:", actual)\n    return True\n\n\ndef desactivar_envio(page):\n    """\n    Desactiva específicamente el interruptor de la fila \'Activar envío\'.\n\n    Wallapop no siempre expone este switch como checkbox/role=switch,\n    así que lo localizamos por la posición respecto al texto \'Activar envío\'.\n    """\n    print("Comprobando Activar envío...")\n\n    etiqueta = page.get_by_text("Activar envío", exact=True)\n\n    try:\n        etiqueta.first.wait_for(state="visible", timeout=8000)\n    except Exception:\n        print("No aparece \'Activar envío\'; continúo.")\n        return True\n\n    etiqueta.first.scroll_into_view_if_needed()\n    page.wait_for_timeout(400)\n\n    # 1) Intentar encontrar un switch/checkbox dentro de los ancestros cercanos.\n    switch = None\n\n    for niveles in range(1, 7):\n        try:\n            cont = etiqueta.first.locator("xpath=" + "/.." * niveles)\n            candidatos = cont.locator(\n                \'[role="switch"], input[type="checkbox"], walla-switch, walla-toggle, button\'\n            )\n\n            for j in range(candidatos.count()):\n                c = candidatos.nth(j)\n                try:\n                    if not c.is_visible():\n                        continue\n\n                    box = c.bounding_box()\n                    if not box:\n                        continue\n\n                    # Queremos el control a la derecha de "Activar envío",\n                    # no cualquier botón del bloque.\n                    box_label = etiqueta.first.bounding_box()\n                    if not box_label:\n                        continue\n\n                    misma_fila = abs(\n                        (box["y"] + box["height"]/2)\n                        - (box_label["y"] + box_label["height"]/2)\n                    ) < 45\n\n                    a_la_derecha = box["x"] > box_label["x"]\n\n                    if misma_fila and a_la_derecha:\n                        switch = c\n                        break\n                except Exception:\n                    pass\n\n            if switch is not None:\n                break\n        except Exception:\n            pass\n\n    # 2) Fallback: buscar cualquier elemento clicable visible a la derecha\n    #    y en la misma fila que el texto "Activar envío".\n    if switch is None:\n        box_label = etiqueta.first.bounding_box()\n\n        if box_label:\n            candidatos = page.locator(\n                \'button:visible, [role="switch"]:visible, \'\n                \'input[type="checkbox"]:visible, div:visible\'\n            )\n\n            mejor = None\n            mejor_dist = None\n\n            for i in range(candidatos.count()):\n                c = candidatos.nth(i)\n                try:\n                    box = c.bounding_box()\n                    if not box:\n                        continue\n\n                    centro_y = box["y"] + box["height"]/2\n                    centro_label_y = box_label["y"] + box_label["height"]/2\n\n                    if abs(centro_y - centro_label_y) > 35:\n                        continue\n\n                    if box["x"] <= box_label["x"] + box_label["width"]:\n                        continue\n\n                    # El switch de la captura es pequeño; descartamos bloques enormes.\n                    if box["width"] > 120 or box["height"] > 80:\n                        continue\n\n                    dist = box["x"] - (box_label["x"] + box_label["width"])\n\n                    if mejor_dist is None or dist < mejor_dist:\n                        mejor = c\n                        mejor_dist = dist\n                except Exception:\n                    pass\n\n            switch = mejor\n\n    if switch is None:\n        raise RuntimeError("No encuentro el interruptor de \'Activar envío\'.")\n\n    # Leer estado si Wallapop lo expone.\n    estado_antes = None\n\n    try:\n        aria = switch.get_attribute("aria-checked")\n        if aria == "true":\n            estado_antes = True\n        elif aria == "false":\n            estado_antes = False\n    except Exception:\n        pass\n\n    if estado_antes is None:\n        try:\n            if switch.get_attribute("type") == "checkbox":\n                estado_antes = switch.is_checked()\n        except Exception:\n            pass\n\n    # Si sabemos que ya está apagado, no tocarlo.\n    if estado_antes is False:\n        print("Envío ya estaba desactivado.")\n        return True\n\n    print("Interruptor de envío encontrado. Haciendo clic...")\n\n    try:\n        switch.click(timeout=3500)\n    except Exception:\n        try:\n            switch.click(force=True, timeout=3500)\n        except Exception:\n            # Último fallback: clic por coordenadas en el centro.\n            box = switch.bounding_box()\n            if not box:\n                raise RuntimeError("No pude hacer clic en Activar envío.")\n\n            page.mouse.click(\n                box["x"] + box["width"]/2,\n                box["y"] + box["height"]/2\n            )\n\n    page.wait_for_timeout(700)\n\n    # Verificación si existe aria-checked/checkbox.\n    try:\n        aria = switch.get_attribute("aria-checked")\n        if aria == "true":\n            raise RuntimeError("El envío sigue activado después del clic.")\n        if aria == "false":\n            print("Envío desactivado.")\n            return True\n    except RuntimeError:\n        raise\n    except Exception:\n        pass\n\n    try:\n        if switch.get_attribute("type") == "checkbox":\n            if switch.is_checked():\n                raise RuntimeError("El envío sigue activado después del clic.")\n            print("Envío desactivado.")\n            return True\n    except RuntimeError:\n        raise\n    except Exception:\n        pass\n\n    # Si Wallapop no expone el estado, asumimos éxito tras el clic único.\n    # No repetimos el clic para evitar volver a activarlo.\n    print("Envío: clic de desactivación realizado.")\n    return True\n\n\ndef _pagina_responde_wallapop(page, timeout_segundos=12):\n    """\n    Comprueba que la página sigue viva y que el DOM responde.\n    """\n    try:\n        page.wait_for_load_state(\n            "domcontentloaded",\n            timeout=int(timeout_segundos * 1000)\n        )\n    except Exception:\n        # Puede estar ya cargada; comprobamos DOM directamente.\n        pass\n\n    try:\n        return bool(\n            page.evaluate(\n                "() => document && document.body && document.readyState !== \'loading\'"\n            )\n        )\n    except Exception:\n        return False\n\n\ndef _limpiar_pestanas_wallapop(contexto, conservar=None):\n    """\n    Cierra pestañas antiguas/sobrantes de upload/published para evitar\n    consumo de memoria y que el bot seleccione una pestaña incorrecta.\n    """\n    cerradas = 0\n\n    try:\n        paginas = list(contexto.pages)\n    except Exception:\n        return 0\n\n    wallapop = []\n\n    for p in paginas:\n        try:\n            if p.is_closed():\n                continue\n\n            url = (p.url or "").lower()\n\n            if "wallapop.com" in url:\n                wallapop.append(p)\n        except Exception:\n            pass\n\n    # Conservar explícitamente la página indicada y, si no se indicó,\n    # la primera pestaña normal de Wallapop.\n    if conservar is None:\n        for p in wallapop:\n            try:\n                url = (p.url or "").lower()\n                if (\n                    "/app/catalog/upload" not in url\n                    and "/app/catalog/published" not in url\n                ):\n                    conservar = p\n                    break\n            except Exception:\n                pass\n\n    for p in wallapop:\n        if p == conservar:\n            continue\n\n        try:\n            url = (p.url or "").lower()\n\n            # Pestañas de formularios o confirmaciones antiguas son seguras\n            # de limpiar entre anuncios.\n            if (\n                "/app/catalog/upload" in url\n                or "/app/catalog/published" in url\n            ):\n                p.close()\n                cerradas += 1\n        except Exception:\n            pass\n\n    if cerradas:\n        print(f"Optimización: {cerradas} pestaña(s) antigua(s) cerradas.")\n\n    return cerradas\n\n\ndef optimizar_navegador_ahora():\n    """\n    Optimización manual: limpia pestañas obsoletas, fuerza GC y comprueba CDP.\n    """\n    try:\n        estado.configure(\n            text="⚙ Optimizando navegador y memoria..."\n        )\n\n        _asegurar_chromium_controlable()\n\n        if sync_playwright is None:\n            raise RuntimeError("Playwright no está disponible.")\n\n        with sync_playwright() as p:\n            navegador = _conectar_playwright_cdp(p)\n\n            if not navegador.contexts:\n                raise RuntimeError(\n                    "Chromium está conectado pero no devuelve contexto."\n                )\n\n            contexto = navegador.contexts[0]\n            pagina = _buscar_pagina_wallapop(contexto)\n\n            _limpiar_pestanas_wallapop(\n                contexto,\n                conservar=pagina\n            )\n\n        liberados = gc.collect()\n\n        estado.configure(\n            text=(\n                f"✅ Optimización terminada · "\n                f"memoria Python revisada ({liberados} objetos liberados)."\n            )\n        )\n\n        try:\n            refrescar_dashboard()\n        except Exception:\n            pass\n\n        return True\n\n    except Exception as e:\n        estado.configure(\n            text="❌ No pude optimizar: " + str(e)\n        )\n        return False\n\n\ndef precalentar_wallapop_para_cola():\n    """\n    Deja Chromium y Wallapop listos ANTES del primer anuncio.\n    Reduce fallos y tiempo de arranque de la cola.\n    """\n    config = obtener_config_rendimiento()\n\n    if not config["activo"]:\n        return True\n\n    actualizar_fase(\n        "Precalentamiento",\n        "preparando navegador para la cola"\n    )\n\n    _asegurar_chromium_controlable()\n\n    if sync_playwright is None:\n        raise RuntimeError("Playwright no está disponible.")\n\n    with sync_playwright() as p:\n        navegador = _conectar_playwright_cdp(p)\n\n        if not navegador.contexts:\n            raise RuntimeError(\n                "No hay contexto de Chromium durante el precalentamiento."\n            )\n\n        contexto = navegador.contexts[0]\n        pagina = _buscar_pagina_wallapop(contexto)\n\n        _comprobar_sesion_wallapop(pagina)\n\n        if not _pagina_responde_wallapop(\n            pagina,\n            config["timeout_wallapop"]\n        ):\n            print("Wallapop no respondía; recargando portada...")\n            pagina.goto(\n                "https://es.wallapop.com",\n                wait_until="domcontentloaded",\n                timeout=config["timeout_wallapop"] * 1000\n            )\n\n        _limpiar_pestanas_wallapop(\n            contexto,\n            conservar=pagina\n        )\n\n    print("Precalentamiento completado.")\n    return True\n\n\ndef mantenimiento_rendimiento_cola(numero_procesado):\n    """\n    Mantenimiento ligero entre anuncios.\n    """\n    config = obtener_config_rendimiento()\n\n    if not config["activo"]:\n        return\n\n    # Liberación periódica de objetos Python.\n    if numero_procesado % config["limpiar_memoria_cada"] == 0:\n        liberados = gc.collect()\n        print(\n            f"Mantenimiento: GC ejecutado ({liberados} objetos liberados)."\n        )\n\n    # Limpieza de pestañas obsoletas.\n    if numero_procesado % config["limpiar_pestanas_cada"] == 0:\n        try:\n            if sync_playwright is None:\n                return\n\n            with sync_playwright() as p:\n                navegador = _conectar_playwright_cdp(p)\n\n                if not navegador.contexts:\n                    return\n\n                contexto = navegador.contexts[0]\n\n                # En V86 solo limpiamos restos de formularios/confirmaciones.\n                # Las pestañas normales se conservan como base de sesión.\n                _limpiar_pestanas_residuales_antes_de_anuncio(\n                    contexto\n                )\n        except Exception as e:\n            print(\n                "Aviso mantenimiento navegador:",\n                e\n            )\n\n\ndef _parece_error_navegador(texto):\n    t = str(texto or "").lower()\n    patrones = [\n        "target page, context or browser has been closed",\n        "page has been closed",\n        "browser has been closed",\n        "context has been closed",\n        "connection closed",\n        "websocket",\n        "cdp",\n        "chromium",\n        "timeout",\n        "timed out",\n        "no responde",\n        "no respondió",\n        "no respondio",\n        "net::err",\n        "navigation",\n    ]\n    return any(p in t for p in patrones)\n\n\ndef recuperar_navegador_para_cola():\n    global recuperaciones_navegador_sesion\n    global ultima_recuperacion_navegador\n\n    print("Iniciando recuperación automática del navegador...")\n\n    _asegurar_chromium_controlable()\n\n    if sync_playwright is None:\n        raise RuntimeError("Playwright no está disponible.")\n\n    with sync_playwright() as p:\n        navegador = _conectar_playwright_cdp(p)\n\n        if not navegador.contexts:\n            raise RuntimeError(\n                "Chromium respondió pero no devolvió contexto."\n            )\n\n        contexto = navegador.contexts[0]\n\n        _cerrar_todas_pestanas_wallapop(\n            contexto\n        )\n\n        pagina = contexto.new_page()\n\n        try:\n            pagina.goto(\n                "https://es.wallapop.com",\n                wait_until="domcontentloaded",\n                timeout=30000\n            )\n            pagina.wait_for_timeout(800)\n            _comprobar_sesion_wallapop(pagina)\n        finally:\n            try:\n                if not pagina.is_closed():\n                    pagina.close()\n            except Exception:\n                pass\n\n    recuperaciones_navegador_sesion += 1\n    ultima_recuperacion_navegador = time.strftime(\n        "%d/%m/%Y %H:%M:%S"\n    )\n\n    try:\n        ventana.after(\n            0,\n            refrescar_dashboard\n        )\n    except Exception:\n        pass\n\n    return True\n\n\ndef _datos_cdp():\n    """\n    Devuelve la información del puerto de depuración de Chromium.\n    """\n    try:\n        with urllib.request.urlopen(\n            f"http://127.0.0.1:{obtener_puerto_chromium()}/json/version",\n            timeout=2.0\n        ) as r:\n            if r.status != 200:\n                return None\n\n            return json.loads(\n                r.read().decode("utf-8", errors="ignore")\n            )\n    except Exception:\n        return None\n\n\ndef _puerto_chrome_activo():\n    return _datos_cdp() is not None\n\n\ndef _encontrar_chromium():\n    """\n    Busca Chromium/Chrome en varias ubicaciones habituales.\n    Priorizamos el Chromium portable que ya usa este bot.\n    """\n    usuario = os.path.expanduser("~")\n\n    candidatos = [\n        # Chromium portable del proyecto/usuario\n        os.path.join(usuario, "Desktop", "chrome-win", "chrome.exe"),\n        os.path.join(usuario, "Escritorio", "chrome-win", "chrome.exe"),\n\n        # Chromium instalado\n        os.path.join(\n            usuario,\n            "AppData",\n            "Local",\n            "Chromium",\n            "Application",\n            "chrome.exe"\n        ),\n\n        # Google Chrome instalado para el usuario\n        os.path.join(\n            usuario,\n            "AppData",\n            "Local",\n            "Google",\n            "Chrome",\n            "Application",\n            "chrome.exe"\n        ),\n\n        # Instalaciones globales\n        r"C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe",\n        r"C:\\Program Files (x86)\\Google\\Chrome\\Application\\chrome.exe",\n    ]\n\n    for ruta in candidatos:\n        if os.path.isfile(ruta):\n            return ruta\n\n    return None\n\n\ndef _perfil_chromium_bot():\n    """\n    Perfil real que ya venías usando con tu sesión de Wallapop.\n    """\n    usuario = os.path.expanduser("~")\n\n    return os.path.join(\n        usuario,\n        "AppData",\n        "Local",\n        "Chromium",\n        "User Data"\n    )\n\n\ndef _esperar_puerto_cdp(segundos=18):\n    limite = time.time() + segundos\n\n    while time.time() < limite:\n        if _puerto_chrome_activo():\n            return True\n        time.sleep(0.35)\n\n    return False\n\n\ndef _asegurar_chromium_controlable():\n    """\n    Garantiza que exista un Chromium controlable por Playwright.\n\n    - Si el puerto 9222 ya funciona, lo reutiliza.\n    - Si no, localiza Chromium automáticamente.\n    - Usa el perfil de Wallapop que ya tienes configurado.\n    - No cierra ni mata navegadores del usuario.\n    """\n    datos = _datos_cdp()\n\n    if datos:\n        print(\n            "Chromium controlable ya está activo:",\n            datos.get("Browser", "puerto 9222")\n        )\n        return True\n\n    chrome = _encontrar_chromium()\n\n    if not chrome:\n        raise RuntimeError(\n            "No encuentro Chromium/Chrome automáticamente. "\n            "Instala Chromium o conserva la carpeta Desktop\\\\chrome-win."\n        )\n\n    user_data = _perfil_chromium_bot()\n    os.makedirs(user_data, exist_ok=True)\n\n    args = [\n        chrome,\n        f"--remote-debugging-port={obtener_puerto_chromium()}",\n        "--remote-allow-origins=*",\n        f"--user-data-dir={user_data}",\n        "--profile-directory=Profile 1",\n        "--no-first-run",\n        "--no-default-browser-check",\n        "https://es.wallapop.com"\n    ]\n\n    print("Abriendo Chromium controlable...")\n    print("Ejecutable:", chrome)\n    print("Perfil:", user_data)\n\n    kwargs = {\n        "stdout": subprocess.DEVNULL,\n        "stderr": subprocess.DEVNULL,\n    }\n\n    # Evita que aparezca una consola negra adicional al crear el .exe.\n    if os.name == "nt":\n        kwargs["creationflags"] = getattr(\n            subprocess,\n            "CREATE_NO_WINDOW",\n            0\n        )\n\n    try:\n        subprocess.Popen(args, **kwargs)\n    except Exception as e:\n        raise RuntimeError(\n            "No pude iniciar Chromium automáticamente: " + str(e)\n        )\n\n    if not _esperar_puerto_cdp(18):\n        raise RuntimeError(\n            "Chromium se abrió, pero el puerto 9222 no respondió. "\n            "Cierra únicamente el Chromium del bot y vuelve a intentarlo."\n        )\n\n    print("Chromium controlable conectado correctamente.")\n    return True\n\n\ndef _conectar_playwright_cdp(playwright):\n    """\n    Conecta Playwright al navegador con varios reintentos.\n    """\n    ultimo_error = None\n\n    for intento in range(1, 6):\n        try:\n            print(\n                f"Conectando Playwright a Chromium "\n                f"(intento {intento}/5)..."\n            )\n\n            navegador = playwright.chromium.connect_over_cdp(\n                f"http://127.0.0.1:{obtener_puerto_chromium()}",\n                timeout=10000\n            )\n\n            if navegador.contexts:\n                print("Playwright conectado correctamente.")\n                return navegador\n\n            ultimo_error = RuntimeError(\n                "Chromium respondió pero no devolvió ningún contexto."\n            )\n\n        except Exception as e:\n            ultimo_error = e\n\n        time.sleep(0.8)\n\n    raise RuntimeError(\n        "No pude conectar Playwright con Chromium después de 5 intentos. "\n        f"Último error: {ultimo_error}"\n    )\n\n\ndef _cerrar_todas_pestanas_wallapop(contexto, conservar=None):\n    """\n    Cierra todas las pestañas de Wallapop del contexto, excepto `conservar`.\n\n    No toca pestañas de otras webs ni ventanas ajenas al bot.\n    """\n    cerradas = 0\n\n    try:\n        paginas = list(contexto.pages)\n    except Exception:\n        return 0\n\n    for p in paginas:\n        try:\n            if p == conservar:\n                continue\n\n            if p.is_closed():\n                continue\n\n            url = (p.url or "").lower()\n\n            if "wallapop.com" in url:\n                try:\n                    p.close(run_before_unload=False)\n                except TypeError:\n                    p.close()\n\n                cerradas += 1\n\n        except Exception:\n            pass\n\n    if cerradas:\n        print(\n            f"Limpieza total: {cerradas} pestaña(s) de Wallapop cerradas."\n        )\n\n    return cerradas\n\n\ndef _crear_pagina_limpia_wallapop(contexto):\n    """\n    Crea una pestaña NUEVA para un único anuncio de la cola.\n\n    Antes de abrirla, elimina TODAS las pestañas Wallapop anteriores.\n    La sesión/login se conserva porque pertenece al contexto/perfil Chromium.\n    """\n    actualizar_fase(\n        "Pestaña limpia",\n        "cerrando pestañas anteriores y abriendo una nueva"\n    )\n\n    # Evita acumulación: antes de cada anuncio no debe quedar ninguna\n    # pestaña vieja de Wallapop.\n    _cerrar_todas_pestanas_wallapop(\n        contexto\n    )\n\n    try:\n        pagina = contexto.new_page()\n    except Exception as e:\n        raise RuntimeError(\n            "No pude crear una pestaña nueva de Wallapop: " + str(e)\n        )\n\n    try:\n        pagina.goto(\n            "https://es.wallapop.com",\n            wait_until="domcontentloaded",\n            timeout=30000\n        )\n\n        pagina.bring_to_front()\n\n        # Pequeña espera para dejar que el frontend termine de iniciar.\n        pagina.wait_for_timeout(900)\n\n        _comprobar_sesion_wallapop(pagina)\n\n        if not _pagina_responde_wallapop(\n            pagina,\n            obtener_config_rendimiento().get(\n                "timeout_wallapop",\n                12\n            )\n        ):\n            raise RuntimeError(\n                "La pestaña nueva de Wallapop no responde correctamente."\n            )\n\n        print(\n            "Pestaña limpia preparada para el anuncio:",\n            pagina.url\n        )\n\n        return pagina\n\n    except Exception:\n        try:\n            if not pagina.is_closed():\n                pagina.close()\n        except Exception:\n            pass\n\n        raise\n\n\ndef _cerrar_pagina_anuncio_cola(pagina, contexto=None):\n    """\n    Cierra SIEMPRE la pestaña utilizada por el anuncio.\n\n    Después hace una segunda pasada y elimina cualquier pestaña Wallapop\n    residual que haya quedado abierta por redirecciones o confirmaciones.\n    """\n    if pagina is not None:\n        try:\n            if not pagina.is_closed():\n                print(\n                    "Cerrando pestaña usada por el anuncio..."\n                )\n\n                try:\n                    pagina.close(\n                        run_before_unload=False\n                    )\n                except TypeError:\n                    pagina.close()\n\n        except Exception as e:\n            print(\n                "Aviso al cerrar pestaña principal del anuncio:",\n                e\n            )\n\n    if contexto is not None:\n        try:\n            # Damos un instante a Chromium para actualizar context.pages.\n            time.sleep(0.25)\n\n            restantes = _cerrar_todas_pestanas_wallapop(\n                contexto\n            )\n\n            if restantes:\n                print(\n                    "Se cerraron pestañas residuales de Wallapop tras el anuncio."\n                )\n        except Exception as e:\n            print(\n                "Aviso en limpieza final de pestañas:",\n                e\n            )\n\n\ndef _limpiar_pestanas_residuales_antes_de_anuncio(contexto):\n    """\n    Elimina pestañas antiguas de upload/published que hayan quedado vivas\n    por algún fallo anterior, pero conserva las pestañas normales del usuario.\n    """\n    cerradas = 0\n\n    try:\n        paginas = list(contexto.pages)\n    except Exception:\n        return 0\n\n    for p in paginas:\n        try:\n            if p.is_closed():\n                continue\n\n            url = (p.url or "").lower()\n\n            if (\n                "wallapop.com" in url\n                and (\n                    "/app/catalog/upload" in url\n                    or "/app/catalog/published" in url\n                )\n            ):\n                p.close()\n                cerradas += 1\n\n        except Exception:\n            pass\n\n    if cerradas:\n        print(\n            f"Limpieza previa: {cerradas} pestaña(s) residual(es) cerradas."\n        )\n\n    return cerradas\n\n\ndef _buscar_pagina_wallapop(contexto):\n    """\n    Reutiliza la mejor pestaña de Wallapop disponible.\n    Si no existe, crea una nueva dentro del MISMO navegador controlado.\n    """\n    paginas_wallapop = []\n\n    for pagina in contexto.pages:\n        try:\n            if "wallapop.com" in pagina.url:\n                paginas_wallapop.append(pagina)\n        except Exception:\n            pass\n\n    # Prioridad 1: formulario de subida ya abierto.\n    for pagina in paginas_wallapop:\n        try:\n            if "/app/catalog/upload" in pagina.url:\n                return pagina\n        except Exception:\n            pass\n\n    # Prioridad 2: cualquier página normal de Wallapop.\n    if paginas_wallapop:\n        return paginas_wallapop[0]\n\n    print("No hay pestaña de Wallapop. Creando una nueva...")\n\n    try:\n        pagina = contexto.new_page()\n        pagina.goto(\n            "https://es.wallapop.com",\n            wait_until="domcontentloaded",\n            timeout=30000\n        )\n        return pagina\n    except Exception as e:\n        raise RuntimeError(\n            "Chromium está conectado, pero no pude abrir Wallapop: "\n            + str(e)\n        )\n\n\ndef _comprobar_sesion_wallapop(page):\n    """\n    Comprueba de forma simple si Wallapop ha enviado al login.\n    """\n    try:\n        url = page.url.lower()\n    except Exception:\n        url = ""\n\n    if "accounts.wallapop.com" in url or "/login" in url:\n        raise RuntimeError(\n            "Wallapop necesita iniciar sesión. "\n            "Inicia sesión una vez en el Chromium del bot y vuelve a intentarlo."\n        )\n\n    return True\n\n\ndef _cerrar_pestana_post_publicacion(page, contexto, paginas_antes):\n    """\n    Después de publicar:\n    1) cierra el modal "¡Yuhu! Producto subido" pulsando "Ahora no, gracias"\n       o la X;\n    2) cierra pestañas nuevas de confirmación si aparecieron;\n    3) deja una página normal de Wallapop lista para el siguiente anuncio.\n    """\n    try:\n        page.wait_for_timeout(1200)\n    except Exception:\n        pass\n\n    # -----------------------------------------------------\n    # 1) CERRAR MODAL "PRODUCTO SUBIDO"\n    # -----------------------------------------------------\n    modal_cerrado = False\n\n    # Opción preferida: botón "Ahora no, gracias"\n    candidatos_no_gracias = [\n        page.get_by_role("button", name="Ahora no, gracias"),\n        page.get_by_text("Ahora no, gracias", exact=True),\n        page.locator(\'button:has-text("Ahora no, gracias")\'),\n        page.locator(\'walla-button:has-text("Ahora no, gracias")\'),\n    ]\n\n    for loc in candidatos_no_gracias:\n        try:\n            for i in range(min(loc.count(), 10)):\n                el = loc.nth(i)\n                if el.is_visible():\n                    el.scroll_into_view_if_needed()\n                    el.click(timeout=2500)\n                    modal_cerrado = True\n                    print("Modal de producto subido cerrado con \'Ahora no, gracias\'.")\n                    break\n            if modal_cerrado:\n                break\n        except Exception:\n            pass\n\n    # Fallback: pulsar la X del modal.\n    if not modal_cerrado:\n        candidatos_x = [\n            page.get_by_role("button", name="Cerrar"),\n            page.locator(\'button[aria-label*="cerrar" i]\'),\n            page.locator(\'button[aria-label*="close" i]\'),\n            page.locator(\'[role="dialog"] button\').first,\n        ]\n\n        for loc in candidatos_x:\n            try:\n                if loc.count() and loc.first.is_visible():\n                    loc.first.click(timeout=2500)\n                    modal_cerrado = True\n                    print("Modal de producto subido cerrado con la X.")\n                    break\n            except Exception:\n                pass\n\n    # Fallback Shadow DOM / búsqueda por texto visible.\n    if not modal_cerrado:\n        try:\n            modal_cerrado = bool(page.evaluate("""\n                () => {\n                    const textos = [\n                        \'ahora no, gracias\',\n                        \'producto subido\',\n                        \'yuhu\'\n                    ];\n\n                    const visibles = [...document.querySelectorAll(\'body *\')].filter(el => {\n                        const r = el.getBoundingClientRect();\n                        return r.width > 0 && r.height > 0;\n                    });\n\n                    // Primero intentamos encontrar y clicar "Ahora no, gracias".\n                    for (const el of visibles) {\n                        const t = (el.innerText || el.textContent || \'\').trim().toLowerCase();\n                        if (t === \'ahora no, gracias\') {\n                            if (el.click) {\n                                el.click();\n                                return true;\n                            }\n                        }\n                    }\n\n                    // Después buscamos botones dentro de un diálogo que contenga\n                    // "Producto subido" o "Yuhu".\n                    const dialogs = [\n                        ...document.querySelectorAll(\'[role="dialog"], walla-modal, walla-dialog\')\n                    ];\n\n                    for (const dlg of dialogs) {\n                        const dt = (dlg.innerText || dlg.textContent || \'\').toLowerCase();\n                        if (!dt.includes(\'producto subido\') && !dt.includes(\'yuhu\')) continue;\n\n                        const botones = [...dlg.querySelectorAll(\'button\')];\n                        for (const b of botones) {\n                            const bt = (b.innerText || b.textContent || \'\').trim().toLowerCase();\n                            if (bt.includes(\'ahora no\') || bt.includes(\'cerrar\') || bt === \'×\') {\n                                b.click();\n                                return true;\n                            }\n                        }\n\n                        // Shadow roots cercanos\n                        for (const host of dlg.querySelectorAll(\'*\')) {\n                            const btn = host.shadowRoot?.querySelector(\'button\');\n                            if (!btn) continue;\n\n                            const bt = (btn.innerText || btn.textContent || \'\').trim().toLowerCase();\n                            if (bt.includes(\'ahora no\') || bt.includes(\'cerrar\') || bt === \'×\') {\n                                btn.click();\n                                return true;\n                            }\n                        }\n                    }\n\n                    return false;\n                }\n            """))\n            if modal_cerrado:\n                print("Modal de producto subido cerrado mediante fallback DOM.")\n        except Exception:\n            pass\n\n    # Esperar a que desaparezca el overlay/modal.\n    if modal_cerrado:\n        try:\n            page.wait_for_timeout(700)\n        except Exception:\n            pass\n    else:\n        print("Aviso: no detecté el modal de producto subido.")\n\n    # -----------------------------------------------------\n    # 2) CERRAR PESTAÑAS NUEVAS DE CONFIRMACIÓN\n    # -----------------------------------------------------\n    try:\n        paginas_actuales = list(contexto.pages)\n    except Exception:\n        paginas_actuales = []\n\n    nuevas = [p for p in paginas_actuales if p not in paginas_antes]\n\n    for p in nuevas:\n        try:\n            if not p.is_closed():\n                print("Cerrando pestaña nueva de confirmación...")\n                p.close()\n        except Exception:\n            pass\n\n    # -----------------------------------------------------\n    # 3) DEJAR WALLAPOP LISTO PARA EL SIGUIENTE ANUNCIO\n    # -----------------------------------------------------\n    try:\n        if not page.is_closed():\n            url = (page.url or "").lower()\n\n            # La pantalla /app/catalog/published es la que se ve después\n            # de publicar. Volvemos a la portada para que el siguiente ciclo\n            # empiece limpio.\n            if "/app/catalog/published" in url:\n                print("Saliendo de la pantalla de producto publicado...")\n                page.goto(\n                    "https://es.wallapop.com",\n                    wait_until="domcontentloaded",\n                    timeout=25000\n                )\n                page.wait_for_timeout(700)\n\n            page.bring_to_front()\n            return True\n\n    except Exception as e:\n        print("Aviso al limpiar post-publicación:", e)\n\n    # Si la pestaña principal se cerró, traer otra de Wallapop al frente.\n    try:\n        for p in contexto.pages:\n            if not p.is_closed() and "wallapop.com" in (p.url or "").lower():\n                p.bring_to_front()\n                return True\n    except Exception:\n        pass\n\n    return True\n\n\ndef _es_error_limite_catalogo(texto):\n    """\n    Detecta mensajes que indican que Wallapop no admite más anuncios\n    por haber alcanzado el límite actual del catálogo/cuenta.\n    """\n    t = str(texto or "").lower()\n\n    patrones = [\n        "límite de anuncios",\n        "limite de anuncios",\n        "límite de productos",\n        "limite de productos",\n        "has alcanzado el límite",\n        "has alcanzado el limite",\n        "alcanzado el máximo",\n        "alcanzado el maximo",\n        "máximo de anuncios",\n        "maximo de anuncios",\n        "no puedes publicar más",\n        "no puedes publicar mas",\n        "demasiados anuncios",\n        "too many listings",\n        "listing limit",\n        "limit reached",\n        "catalog limit",\n    ]\n\n    return any(\n        patron in t\n        for patron in patrones\n    )\n\n\ndef _publicar_anuncio(page):\n    """\n    Pulsa Publicar UNA sola vez y SOLO devuelve éxito cuando Wallapop\n    muestra una señal clara de que el anuncio fue publicado.\n\n    No repite el clic bajo ninguna circunstancia.\n    """\n    print("Buscando botón Publicar...")\n\n    publicar = page.get_by_role(\n        "button",\n        name=re.compile(r"^Publicar$", re.I)\n    )\n\n    if not publicar.count():\n        publicar = page.get_by_text("Publicar", exact=True)\n\n    if not publicar.count():\n        raise RuntimeError("No encuentro el botón Publicar.")\n\n    boton = publicar.first\n\n    try:\n        boton.scroll_into_view_if_needed()\n    except Exception:\n        pass\n\n    try:\n        if not boton.is_enabled():\n            raise RuntimeError(\n                "El botón Publicar está deshabilitado. "\n                "Falta algún dato obligatorio en Wallapop."\n            )\n    except RuntimeError:\n        raise\n    except Exception:\n        pass\n\n    print("Publicando anuncio...")\n    boton.click(timeout=7000)\n\n    # Desde aquí NO se debe reintentar automáticamente el clic.\n    # Esperamos una señal inequívoca de éxito.\n    limite = time.time() + 25\n\n    while time.time() < limite:\n        try:\n            url = (page.url or "").lower()\n\n            # Primero inspeccionamos el contenido. Es importante hacerlo\n            # ANTES de aceptar /app/catalog/published como éxito, porque\n            # Wallapop puede mostrar avisos o modales sobre esa misma URL.\n            body = ""\n\n            try:\n                body = (\n                    page.locator("body").inner_text(\n                        timeout=1400\n                    ) or ""\n                ).lower()\n            except Exception:\n                body = ""\n\n            if _es_error_limite_catalogo(body):\n                raise RuntimeError(\n                    "LIMITE_CATALOGO: Wallapop indica que se ha "\n                    "alcanzado el límite de anuncios."\n                )\n\n            # Señales visibles del modal real de Wallapop.\n            señales = [\n                "Producto subido",\n                "¡Yuhu! Producto subido",\n                "Ahora no, gracias",\n            ]\n\n            for texto_exito in señales:\n                try:\n                    loc = page.get_by_text(\n                        texto_exito,\n                        exact=False\n                    )\n\n                    if (\n                        loc.count()\n                        and loc.first.is_visible()\n                    ):\n                        print(\n                            "Publicación verificada por mensaje:",\n                            texto_exito\n                        )\n                        return True\n                except Exception:\n                    pass\n\n            if (\n                "producto subido" in body\n                or "yuhu" in body\n            ):\n                print(\n                    "Publicación verificada por contenido de la página."\n                )\n                return True\n\n            # URL como señal secundaria, solo si no existe aviso de límite.\n            if "/app/catalog/published" in url:\n                print(\n                    "Publicación verificada por URL /app/catalog/published."\n                )\n                return True\n\n        except RuntimeError as e:\n            if "LIMITE_CATALOGO:" in str(e):\n                raise\n        except Exception:\n            pass\n\n        time.sleep(0.45)\n\n    # El clic sí se ejecutó, por lo que NO es seguro reintentarlo.\n    raise RuntimeError(\n        "Se pulsó Publicar, pero no pude verificar que Wallapop "\n        "confirmara la publicación. No se reintentará automáticamente "\n        "para evitar crear un anuncio duplicado."\n    )\n\n\ndef preparar_wallapop():\n    """\n    Un solo botón:\n    - toma los datos generados por el bot\n    - abre/conecta Chromium\n    - entra en Vender > Algo que ya no necesito\n    - rellena Resumen\n    - sube 1 foto\n    - selecciona Motor y accesorios > Recambios de coches y furgonetas > Tuning\n    - rellena Título, Descripción y Precio\n    - selecciona Estado = Nuevo\n    - desactiva los envíos\n    - usa la localización guardada en el perfil\n    - pulsa Publicar\n    """\n\n    validacion_ok, _errores = validar_anuncio_antes_de_wallapop(\n        mostrar_ok=False\n    )\n    if not validacion_ok:\n        return\n\n    titulo_actual = caja_titulo.get("1.0", "end-1c").strip()\n    descripcion = obtener_descripcion_actual().strip()\n    precio = entrada_precio.get().strip()\n\n    if not titulo_actual:\n        estado.configure(text="❌ Primero genera un anuncio.")\n        return\n\n    if not descripcion:\n        estado.configure(text="❌ Falta la descripción.")\n        return\n\n    if not precio:\n        estado.configure(text="❌ Escribe un precio antes de publicar.")\n        return\n\n    if not ultima_carpeta_anuncio or not os.path.isdir(ultima_carpeta_anuncio):\n        estado.configure(text="❌ No encuentro las imágenes del último anuncio.")\n        return\n\n    imagenes = sorted(\n        glob.glob(os.path.join(ultima_carpeta_anuncio, "imagen_*.png"))\n    )[:1]\n\n    if len(imagenes) < 1:\n        estado.configure(text="❌ Necesito 1 imagen del anuncio.")\n        return\n\n    if sync_playwright is None:\n        estado.configure(\n            text="❌ Falta Playwright. Ejecuta: pip install playwright"\n        )\n        return\n\n    boton_wallapop.configure(state="disabled")\n    try:\n        boton_preparar.configure(state="disabled")\n    except Exception:\n        pass\n    estado.configure(text="⏳ Publicando anuncio en Wallapop...")\n\n    hilo = threading.Thread(\n        target=_publicar_wallapop_hilo,\n        args=(\n            titulo_actual[:50],\n            descripcion[:640],\n            precio.replace("€", "").strip(),\n            imagenes,\n            ultima_carpeta_anuncio,\n            categoria_publicacion.get().strip(),\n            estado_producto_publicacion.get().strip(),\n            bool(switch_desactivar_envio.get()),\n            bool(switch_publicar_automaticamente.get()),\n            False\n        ),\n        daemon=True\n    )\n    hilo.start()\n\n\ndef _publicar_wallapop_hilo(\n    titulo,\n    descripcion,\n    precio,\n    imagenes,\n    carpeta_anuncio=None,\n    ruta_categoria=None,\n    estado_elegido=None,\n    quitar_envio=None,\n    publicar_auto=None,\n    modo_cola=False\n):\n    if carpeta_anuncio is None:\n        carpeta_anuncio = ultima_carpeta_anuncio\n\n    if ruta_categoria is None:\n        ruta_categoria = categoria_publicacion.get().strip()\n\n    if estado_elegido is None:\n        estado_elegido = estado_producto_publicacion.get().strip()\n\n    if quitar_envio is None:\n        try:\n            quitar_envio = bool(switch_desactivar_envio.get())\n        except Exception:\n            quitar_envio = True\n\n    if publicar_auto is None:\n        try:\n            publicar_auto = bool(switch_publicar_automaticamente.get())\n        except Exception:\n            publicar_auto = True\n\n    resultado_ok = False\n    pagina = None\n\n    if modo_cola:\n        ultimo_error_cola["fase"] = ""\n        ultimo_error_cola["mensaje"] = ""\n        ultimo_error_cola["fecha"] = ""\n        ultimo_error_cola["diagnostico"] = ""\n\n    try:\n        actualizar_fase("Navegador", "conectando con Chromium")\n        _asegurar_chromium_controlable()\n\n        with sync_playwright() as p:\n            navegador = _conectar_playwright_cdp(p)\n\n            if not navegador.contexts:\n                raise RuntimeError(\n                    "Chromium está conectado, pero no encuentro un contexto de navegador."\n                )\n\n            contexto = navegador.contexts[0]\n\n            # Siempre se parte de una pestaña nueva. Reutilizar un formulario\n            # anterior dejaba estados internos de Wallapop y podía bloquear la\n            # publicación o conservar campos de otro anuncio.\n            pagina = _crear_pagina_limpia_wallapop(\n                contexto\n            )\n\n            # PASO 0: Vender > Algo que ya no necesito\n            actualizar_fase("Inicio de venta", "Vender > Algo que ya no necesito")\n            entrar_en_vender(pagina)\n            pagina.wait_for_timeout(600)\n\n            # PASO 1: Resumen\n            actualizar_fase("Resumen", "rellenando resumen inicial")\n            resumen = pagina.locator("#summary")\n            resumen_visible = False\n            try:\n                resumen_visible = (\n                    resumen.count() > 0\n                    and resumen.first.is_visible()\n                )\n            except Exception:\n                pass\n\n            if resumen_visible:\n                esperar_y_rellenar_resumen(pagina, titulo)\n\n            # PASO 2: Fotos\n            actualizar_fase("Fotos", "subiendo 1 imagen")\n            pagina.wait_for_timeout(500)\n\n            input_categoria = pagina.locator(\n                \'input[name="category_leaf_id"]\'\n            )\n\n            categoria_presente = input_categoria.count() > 0\n            producto_presente = formulario_producto_visible(pagina)\n\n            if not categoria_presente and not producto_presente:\n                input_fotos = pagina.locator("#dropAreaPreviewInput")\n\n                try:\n                    input_fotos.first.wait_for(\n                        state="attached",\n                        timeout=12000\n                    )\n                except Exception:\n                    pass\n\n                if input_fotos.count() == 0:\n                    input_fotos = pagina.locator(\'input[type="file"]\')\n\n                if input_fotos.count() == 0:\n                    raise RuntimeError(\n                        "No encuentro el selector para subir las fotos."\n                    )\n\n                subir_fotos_robusto(pagina, imagenes)\n                pagina.wait_for_timeout(2200)\n\n                if not click_continuar_visible(pagina):\n                    pulsado = pagina.evaluate("""\n                        () => {\n                            const hosts = [...document.querySelectorAll(\'walla-button\')];\n                            for (let i = hosts.length - 1; i >= 0; i--) {\n                                const host = hosts[i];\n                                const txt = (host.textContent || \'\').toLowerCase();\n                                const btn = host.shadowRoot?.querySelector(\'button\');\n                                if (!btn) continue;\n                                const r = host.getBoundingClientRect();\n                                if (\n                                    r.width > 0 &&\n                                    r.height > 0 &&\n                                    txt.includes(\'continuar\') &&\n                                    !btn.disabled\n                                ) {\n                                    btn.click();\n                                    return true;\n                                }\n                            }\n                            return false;\n                        }\n                    """)\n\n                    if not pulsado:\n                        raise RuntimeError(\n                            "Las fotos se cargaron, pero no pude pulsar Continuar."\n                        )\n\n                pagina.wait_for_timeout(1000)\n\n            # PASO 3: Categoría elegida en la interfaz\n            actualizar_fase("Categoría", "seleccionando ruta elegida")\n            if not formulario_producto_visible(pagina):\n                seleccionar_categoria_configurada(\n                    pagina,\n                    ruta_categoria\n                )\n\n            print("Categoría lista. Esperando formulario de producto...")\n\n            for _ in range(24):\n                if formulario_producto_visible(pagina):\n                    break\n                pagina.wait_for_timeout(250)\n\n            if not formulario_producto_visible(pagina):\n                raise RuntimeError(\n                    "Después de seleccionar la categoría no aparecieron "\n                    "Título/Descripción/Estado/Precio."\n                )\n\n            print("Formulario final detectado. Voy a rellenar los campos.")\n            actualizar_fase("Datos del anuncio", "Título · Descripción · Precio")\n\n            # PASO 4: Título, descripción y precio\n            ok_titulo, ok_desc, ok_precio = rellenar_formulario_producto(\n                pagina,\n                titulo,\n                descripcion,\n                precio\n            )\n\n            faltan = []\n            if not ok_titulo:\n                faltan.append("Título")\n            if not ok_desc:\n                faltan.append("Descripción")\n            if not ok_precio:\n                faltan.append("Precio")\n\n            if faltan:\n                raise RuntimeError(\n                    "No pude rellenar: " + ", ".join(faltan)\n                )\n\n            pagina.wait_for_timeout(700)\n\n            # PASO 5: Estado elegido en la interfaz\n            actualizar_fase("Estado", estado_elegido)\n            seleccionar_estado_configurado(\n                pagina,\n                estado_elegido\n            )\n\n            # PASO 6: volver a confirmar Precio DESPUÉS de Estado\n            # (Wallapop puede re-renderizar este bloque y vaciarlo).\n            actualizar_fase("Precio", "confirmando valor final")\n            rellenar_precio_robusto(pagina, precio)\n\n            # PASO 7: Envío (gestionable desde la interfaz)\n            actualizar_fase("Envío", "aplicando configuración")\n            if quitar_envio:\n                desactivar_envio(pagina)\n            else:\n                print("Envío: se mantiene según la configuración de Wallapop.")\n\n            # La localización ya queda guardada en el perfil de Wallapop.\n            # No la tocamos.\n            pagina.wait_for_timeout(700)\n\n            # PASO 8: PUBLICAR o dejar preparado\n            actualizar_fase("Final", "preparando acción final")\n            if publicar_auto:\n                if anuncio_ya_publicado(titulo, descripcion, precio):\n                    incrementar_estadistica("duplicados_bloqueados")\n                    raise RuntimeError(\n                        "Este anuncio ya fue publicado anteriormente. "\n                        "Se ha detenido para evitar un duplicado."\n                    )\n\n                actualizar_fase("Publicar", "enviando anuncio a Wallapop")\n\n                if modo_cola and modo_prueba_activado():\n                    print(\n                        "MODO PRUEBA: formulario completado. "\n                        "No se pulsará Publicar."\n                    )\n\n                    try:\n                        ventana.after(\n                            0,\n                            lambda: estado.configure(\n                                text=(\n                                    "🧪 MODO PRUEBA · anuncio rellenado "\n                                    "sin pulsar Publicar"\n                                )\n                            )\n                        )\n                    except Exception:\n                        pass\n\n                    # Consideramos la prueba correcta, pero NO se registra\n                    # como publicado.\n                    resultado_ok = True\n                    return True\n\n                paginas_antes_publicar = list(contexto.pages)\n\n                _publicar_anuncio(pagina)\n\n                # Muy importante para la cola: cerramos/limpiamos la pantalla\n                # de "anuncio publicado" antes de pasar al siguiente.\n                if modo_cola:\n                    actualizar_fase(\n                        "Post-publicación",\n                        "cerrando confirmación antes de cerrar la pestaña"\n                    )\n\n                    # Intentamos limpiar el modal de éxito. Aunque no se consiga,\n                    # el finally cerrará esta pestaña completa.\n                    _cerrar_pestana_post_publicacion(\n                        pagina,\n                        contexto,\n                        paginas_antes_publicar\n                    )\n\n                _url_publicada = ""\n\n                try:\n                    _paginas_candidatas = list(\n                        contexto.pages\n                    )\n\n                    for _pag in reversed(\n                        _paginas_candidatas\n                    ):\n                        try:\n                            _u = str(\n                                _pag.url\n                                or ""\n                            ).strip()\n\n                            if (\n                                "wallapop.com" in _u.lower()\n                                and "/item/" in _u.lower()\n                            ):\n                                _url_publicada = _u\n                                break\n                        except Exception:\n                            pass\n                except Exception:\n                    pass\n\n                guardar_anuncio_publicado(\n                    titulo,\n                    descripcion,\n                    precio,\n                    _url_publicada\n                )\n\n                incrementar_estadistica("publicados")\n                if modo_cola:\n                    incrementar_estadistica("publicados_desde_cola")\n\n                guardar_estado_anuncio(\n                    carpeta_anuncio,\n                    "PUBLICADO",\n                    titulo,\n                    precio\n                )\n\n                mensaje_final = "✅ Anuncio enviado a Wallapop."\n            else:\n                actualizar_fase("Preparado", "detenido antes de Publicar")\n                pagina.bring_to_front()\n\n                guardar_estado_anuncio(\n                    carpeta_anuncio,\n                    "PREPARADO",\n                    titulo,\n                    precio\n                )\n\n                incrementar_estadistica("preparados")\n\n                mensaje_final = "✅ Anuncio preparado. Revisa y pulsa Publicar cuando quieras."\n                print("Modo revisión: no se ha pulsado Publicar.")\n\n            try:\n                ventana.after(0, refrescar_historial)\n            except Exception:\n                pass\n\n            actualizar_fase("Completado", mensaje_final)\n\n            ventana.after(\n                0,\n                lambda m=mensaje_final: estado.configure(text=m)\n            )\n\n            resultado_ok = True\n\n    except Exception as error:\n        incrementar_estadistica("fallidos")\n        mensaje = str(error)\n\n        ruta_diagnostico = guardar_diagnostico_fallo_publicacion(\n            pagina,\n            titulo,\n            fase_actual,\n            mensaje,\n            carpeta_anuncio\n        )\n\n        if modo_cola:\n            incrementar_estadistica("errores_cola")\n\n            ultimo_error_cola["fase"] = str(fase_actual)\n            ultimo_error_cola["mensaje"] = mensaje\n            ultimo_error_cola["fecha"] = time.strftime("%d/%m/%Y %H:%M:%S")\n            ultimo_error_cola["diagnostico"] = ruta_diagnostico\n\n            print(f"❌ Error en cola · {fase_actual}: {mensaje}")\n            try:\n                ventana.after(\n                    0,\n                    lambda m=mensaje: estado.configure(\n                        text=f"❌ Cola detenida en {fase_actual}: {m}"\n                    )\n                )\n            except Exception:\n                pass\n        else:\n            mostrar_error_fase(error)\n\n            if ruta_diagnostico:\n                try:\n                    ventana.after(\n                        0,\n                        lambda r=ruta_diagnostico: estado.configure(\n                            text=f"❌ Error guardado en: {r}"\n                        )\n                    )\n                except Exception:\n                    pass\n\n    finally:\n        # La clave de V86:\n        # cada anuncio de cola destruye por completo su pestaña, incluso si falló.\n        # El siguiente anuncio abrirá una nueva con la misma sesión de Chromium.\n        if modo_cola:\n            try:\n                _cerrar_pagina_anuncio_cola(\n                    pagina,\n                    contexto\n                )\n            except Exception:\n                pass\n\n        if not modo_cola:\n            try:\n                ventana.after(\n                    0,\n                    lambda: (\n                        boton_wallapop.configure(state="normal"),\n                        boton_preparar.configure(state="normal")\n                    )\n                )\n            except Exception:\n                pass\n\n    return resultado_ok\n\n\n\n\n\n# =========================================================\n# COLA DE PUBLICACIÓN\n# =========================================================\n\ncola_anuncios = []\ncola_en_ejecucion = False\ncola_cancelar = False\ncola_pausada = False\ncola_inicio_sesion = None\ncola_publicados_sesion = 0\ncola_errores_sesion = 0\n\n# Métricas de rendimiento de la sesión\nrendimiento_tiempos_publicacion = []\nrendimiento_ultimo_tiempo = 0.0\nrendimiento_anuncios_procesados = 0\n\nrecuperaciones_navegador_sesion = 0\nultima_recuperacion_navegador = ""\n\nARCHIVO_CHECKPOINT_COLA = "checkpoint_cola.json"\nsupervisor_ultimo_estado = "SIN COMPROBAR"\nsupervisor_ultimo_detalle = ""\nsupervisor_reparaciones_sesion = 0\n\nproteccion_ultimo_evento = ""\nproteccion_ultimo_detalle = ""\nsesion_interrumpida_detectada = False\n\n# Control operativo\nARCHIVO_CONTROL_DIARIO = "control_diario.json"\ncola_inicio_programado = ""\nmodo_prueba_activo_sesion = False\n\n# Cola inteligente adaptativa\nritmo_adaptativo_exitos = 0\nritmo_adaptativo_errores = 0\nritmo_adaptativo_ajuste = 0\nultima_pausa_adaptativa = 0\n\n# Diagnóstico preventivo\nultimo_diagnostico_prevuelo = "SIN COMPROBAR"\nultimo_diagnostico_prevuelo_detalle = ""\nultima_hora_prevuelo = ""\n\nCARPETA_SNAPSHOTS = "snapshots"\nARCHIVO_CONFIG_BUENA = "configuracion_ultima_buena.json"\nARCHIVO_PUBLICADOS_ARCHIVO = "publicados_archivo.json"\nultimo_snapshot_creado = ""\n\n# Anti-duplicado visual\nARCHIVO_HASHES_VISUALES = "historial_hashes_visuales.json"\nvisual_regeneraciones_sesion = 0\nvisual_ultima_originalidad = 100\nvisual_ultimo_estado = "SIN COMPROBAR"\n\n# Auditor de cola\nARCHIVO_ULTIMA_AUDITORIA = "ultima_auditoria_cola.json"\nauditoria_ultimo_estado = "SIN AUDITAR"\nauditoria_preparacion = 0\nauditoria_criticos = 0\nauditoria_avisos = 0\n\n# Cuarentena inteligente\ncuarentena_sesion = 0\nultimo_motivo_cuarentena = ""\n\n# Mega actualización V86\nobjetivo_sesion_publicaciones = 0\nbusqueda_cola_actual = ""\n\n# Megapack 2\nfavoritos_cola_sesion = 0\nultimo_export_json = ""\n\n# Gestión avanzada de lotes\nultimo_lote_creado = ""\nsimilitudes_textuales_detectadas = 0\nultimo_autoguardado_borrador = ""\n\n# Megapack 4\nARCHIVO_ACTIVIDAD = "actividad_bot.json"\nultimo_estado_deshacer = None\nultima_accion_registrada = ""\nnombre_sesion_actual = ""\n\n# Megapack 5\nultimo_eta_segundos = 0\nultima_velocidad_hora = 0.0\n\nMAX_COLA = 500\nARCHIVO_COLA = "cola_publicacion.json"\ncola_seleccion_actual = ""\n\n\ndef _etiquetas_selector_cola():\n    valores = []\n\n    for i, item in enumerate(cola_anuncios, start=1):\n        titulo = str(item.get("titulo", "")).strip() or "Sin título"\n        estado_item = str(item.get("estado_cola", "PENDIENTE"))\n        valores.append(\n            f"{i:02d} · {estado_item} · {titulo[:45]}"\n        )\n\n    return valores or ["Cola vacía"]\n\n\ndef actualizar_selector_cola():\n    """\n    Refresca el selector visual conservando la selección cuando sea posible.\n    """\n    global cola_seleccion_actual\n\n    try:\n        valores = _etiquetas_selector_cola()\n        selector_cola.configure(values=valores)\n\n        if not cola_anuncios:\n            cola_seleccion_actual = "Cola vacía"\n            selector_cola.set("Cola vacía")\n            return\n\n        # Conservar selección si sigue existiendo.\n        if cola_seleccion_actual in valores:\n            selector_cola.set(cola_seleccion_actual)\n        else:\n            cola_seleccion_actual = valores[0]\n            selector_cola.set(valores[0])\n\n    except Exception:\n        pass\n\n\ndef al_cambiar_selector_cola(valor):\n    global cola_seleccion_actual\n    cola_seleccion_actual = valor\n\n\ndef _indice_seleccionado_cola():\n    """\n    Devuelve índice Python (0-based) del anuncio seleccionado.\n    """\n    valor = ""\n\n    try:\n        valor = selector_cola.get().strip()\n    except Exception:\n        valor = cola_seleccion_actual\n\n    if not valor or valor == "Cola vacía":\n        return None\n\n    try:\n        numero = int(valor.split("·", 1)[0].strip())\n        indice = numero - 1\n\n        if 0 <= indice < len(cola_anuncios):\n            return indice\n    except Exception:\n        pass\n\n    return None\n\n\ndef _cola_editable():\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes modificar la cola mientras se está publicando."\n        )\n        return False\n    return True\n\n\ndef ver_error_seleccionado_cola():\n    indice = _indice_seleccionado_cola()\n\n    if indice is None:\n        estado.configure(\n            text="Selecciona primero un anuncio de la cola."\n        )\n        return\n\n    item = cola_anuncios[indice]\n\n    if item.get("estado_cola") != "ERROR":\n        detalle_error_texto.configure(state="normal")\n        detalle_error_texto.delete("1.0", "end")\n        detalle_error_texto.insert(\n            "1.0",\n            "El anuncio seleccionado no tiene ningún error registrado."\n        )\n        detalle_error_texto.configure(state="disabled")\n\n        estado.configure(\n            text="El anuncio seleccionado no tiene error."\n        )\n        return\n\n    fase = item.get("error_fase") or "No indicada"\n    motivo = item.get("error_motivo") or "No hay detalle guardado."\n    fecha = item.get("error_fecha") or "—"\n    intentos = item.get("intentos", 1)\n    diagnostico = item.get("error_diagnostico") or "No disponible"\n\n    detalle = (\n        f"ANUNCIO\\n"\n        f"{item.get(\'titulo\', \'Sin título\')}\\n\\n"\n        f"FASE DEL ERROR\\n"\n        f"{fase}\\n\\n"\n        f"MOTIVO\\n"\n        f"{motivo}\\n\\n"\n        f"INTENTOS\\n"\n        f"{intentos}\\n\\n"\n        f"FECHA\\n"\n        f"{fecha}\\n\\n"\n        f"DIAGNÓSTICO GUARDADO EN\\n"\n        f"{diagnostico}"\n    )\n\n    detalle_error_texto.configure(state="normal")\n    detalle_error_texto.delete("1.0", "end")\n    detalle_error_texto.insert("1.0", detalle)\n    detalle_error_texto.configure(state="disabled")\n\n    estado.configure(\n        text=f"❌ Error en {fase}: {motivo}"\n    )\n\n\ndef abrir_diagnostico_error_seleccionado():\n    indice = _indice_seleccionado_cola()\n\n    if indice is None:\n        estado.configure(text="Selecciona primero un anuncio.")\n        return\n\n    item = cola_anuncios[indice]\n    ruta = item.get("error_diagnostico", "")\n\n    if not ruta or not os.path.isdir(ruta):\n        estado.configure(\n            text="Este error no tiene carpeta de diagnóstico disponible."\n        )\n        return\n\n    try:\n        if os.name == "nt":\n            os.startfile(ruta)\n        else:\n            webbrowser.open(Path(ruta).as_uri())\n\n        estado.configure(\n            text="Carpeta de diagnóstico abierta."\n        )\n    except Exception as e:\n        estado.configure(\n            text="❌ No pude abrir el diagnóstico: " + str(e)\n        )\n\n\ndef eliminar_seleccionado_cola():\n    if not _cola_editable():\n        return\n\n    indice = _indice_seleccionado_cola()\n\n    if indice is None:\n        estado.configure(text="Selecciona primero un anuncio de la cola.")\n        return\n\n    item = cola_anuncios.pop(indice)\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n\n    estado.configure(\n        text=f"Anuncio eliminado de la cola: {item.get(\'titulo\', \'Sin título\')}"\n    )\n\n\ndef mover_seleccionado_arriba():\n    if not _cola_editable():\n        return\n\n    indice = _indice_seleccionado_cola()\n\n    if indice is None:\n        estado.configure(text="Selecciona primero un anuncio.")\n        return\n\n    if indice == 0:\n        estado.configure(text="Ese anuncio ya está el primero.")\n        return\n\n    cola_anuncios[indice - 1], cola_anuncios[indice] = (\n        cola_anuncios[indice],\n        cola_anuncios[indice - 1],\n    )\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n\n    # Mantener seleccionado el elemento movido.\n    try:\n        valores = _etiquetas_selector_cola()\n        selector_cola.set(valores[indice - 1])\n        al_cambiar_selector_cola(valores[indice - 1])\n    except Exception:\n        pass\n\n    estado.configure(text="Prioridad aumentada.")\n\n\ndef mover_seleccionado_abajo():\n    if not _cola_editable():\n        return\n\n    indice = _indice_seleccionado_cola()\n\n    if indice is None:\n        estado.configure(text="Selecciona primero un anuncio.")\n        return\n\n    if indice >= len(cola_anuncios) - 1:\n        estado.configure(text="Ese anuncio ya está el último.")\n        return\n\n    cola_anuncios[indice + 1], cola_anuncios[indice] = (\n        cola_anuncios[indice],\n        cola_anuncios[indice + 1],\n    )\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n\n    try:\n        valores = _etiquetas_selector_cola()\n        selector_cola.set(valores[indice + 1])\n        al_cambiar_selector_cola(valores[indice + 1])\n    except Exception:\n        pass\n\n    estado.configure(text="Prioridad reducida.")\n\n\ndef cargar_seleccionado_en_editor():\n    """\n    Carga el anuncio elegido de la cola en los campos principales para editarlo.\n    """\n    global ultima_carpeta_anuncio\n\n    if not _cola_editable():\n        return\n\n    indice = _indice_seleccionado_cola()\n\n    if indice is None:\n        estado.configure(text="Selecciona primero un anuncio.")\n        return\n\n    item = cola_anuncios[indice]\n\n    caja_titulo.delete("1.0", "end")\n    caja_titulo.insert("1.0", item.get("titulo", ""))\n\n    caja_descripcion.delete("1.0", "end")\n    caja_descripcion.insert("1.0", item.get("descripcion", ""))\n\n    entrada_precio.delete(0, "end")\n    entrada_precio.insert(0, item.get("precio", ""))\n\n    categoria_publicacion.set(\n        item.get(\n            "categoria",\n            "Motor y accesorios > Recambios de coches y furgonetas > Tuning"\n        )\n    )\n\n    estado_producto_publicacion.set(\n        item.get("estado", "Nuevo")\n    )\n\n    try:\n        switch_desactivar_envio.set(\n            bool(item.get("desactivar_envio", True))\n        )\n    except Exception:\n        pass\n\n    ultima_carpeta_anuncio = item.get("carpeta")\n\n    actualizar_contadores_profesionales()\n    refrescar_resumen_publicacion()\n\n    estado.configure(\n        text="✅ Anuncio de la cola cargado en el editor."\n    )\n\n    try:\n        tabs.set("Crear anuncio")\n    except Exception:\n        pass\n\n\ndef actualizar_seleccionado_desde_editor():\n    """\n    Guarda los cambios hechos en el editor dentro del elemento seleccionado.\n    """\n    if not _cola_editable():\n        return\n\n    indice = _indice_seleccionado_cola()\n\n    if indice is None:\n        estado.configure(text="Selecciona primero un anuncio.")\n        return\n\n    try:\n        nuevo = _snapshot_anuncio_actual()\n    except Exception as e:\n        estado.configure(text="❌ " + str(e))\n        return\n\n    estado_anterior = cola_anuncios[indice].get(\n        "estado_cola",\n        "PENDIENTE"\n    )\n\n    # Si ya estaba publicado, no permitir editar para republicar accidentalmente.\n    if estado_anterior == "PUBLICADO":\n        estado.configure(\n            text="⚠️ Un anuncio marcado PUBLICADO no se modifica en la cola."\n        )\n        return\n\n    nuevo["estado_cola"] = (\n        "PENDIENTE"\n        if estado_anterior in ("ERROR", "PENDIENTE")\n        else estado_anterior\n    )\n\n    cola_anuncios[indice] = nuevo\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n\n    estado.configure(\n        text="✅ Cambios guardados en el anuncio seleccionado."\n    )\n\n\ndef guardar_cola_en_disco():\n    """\n    Guarda la cola de forma atómica para reducir riesgo de corrupción.\n    """\n    temporal = ARCHIVO_COLA + ".tmp"\n\n    try:\n        with open(\n            temporal,\n            "w",\n            encoding="utf-8"\n        ) as archivo:\n            json.dump(\n                cola_anuncios,\n                archivo,\n                ensure_ascii=False,\n                indent=2\n            )\n\n        os.replace(\n            temporal,\n            ARCHIVO_COLA\n        )\n\n    except Exception as e:\n        print(\n            "Aviso: no pude guardar la cola de forma atómica:",\n            e\n        )\n\n        try:\n            if os.path.isfile(temporal):\n                os.remove(temporal)\n        except Exception:\n            pass\n\n\ndef cargar_cola_desde_disco():\n    """\n    Recupera automáticamente la cola guardada al abrir el programa.\n    """\n    cola_anuncios.clear()\n\n    if not os.path.isfile(ARCHIVO_COLA):\n        return\n\n    try:\n        with open(\n            ARCHIVO_COLA,\n            "r",\n            encoding="utf-8"\n        ) as archivo:\n            datos = json.load(archivo)\n\n        if not isinstance(datos, list):\n            return\n\n        for item in datos[:limite_cola_efectivo()]:\n            if not isinstance(item, dict):\n                continue\n\n            carpeta = item.get("carpeta", "")\n            imagenes = item.get("imagenes", [])\n\n            # Solo recuperar anuncios cuyo contenido todavía exista.\n            if not carpeta or not os.path.isdir(carpeta):\n                continue\n\n            imagenes_validas = [\n                ruta\n                for ruta in imagenes\n                if os.path.isfile(ruta)\n            ]\n\n            if len(imagenes_validas) != 4:\n                continue\n\n            item["imagenes"] = imagenes_validas\n\n            if item.get("estado_cola") == "PUBLICANDO":\n                item["estado_cola"] = "PENDIENTE"\n\n            cola_anuncios.append(item)\n\n        print(\n            f"Cola recuperada: {len(cola_anuncios)} anuncio(s)."\n        )\n\n    except Exception as e:\n        print("Aviso: no pude recuperar la cola:", e)\n\n\n\n\ndef _snapshot_anuncio_actual():\n    titulo = caja_titulo.get("1.0", "end-1c").strip()\n    descripcion = obtener_descripcion_actual().strip()\n    precio = entrada_precio.get().strip().replace("€", "").strip()\n\n    if not titulo:\n        raise RuntimeError("Primero genera un anuncio.")\n\n    if not descripcion:\n        raise RuntimeError("Falta la descripción.")\n\n    if not precio:\n        raise RuntimeError("Falta el precio.")\n\n    if not ultima_carpeta_anuncio or not os.path.isdir(ultima_carpeta_anuncio):\n        raise RuntimeError("No encuentro la carpeta del anuncio actual.")\n\n    imagenes = sorted(\n        glob.glob(\n            os.path.join(\n                ultima_carpeta_anuncio,\n                "imagen_*.png"\n            )\n        )\n    )[:1]\n\n    if len(imagenes) != 1:\n        raise RuntimeError(\n            f"Se necesita 1 imagen y he encontrado {len(imagenes)}."\n        )\n\n    return {\n        "titulo": titulo[:50],\n        "descripcion": descripcion[:640],\n        "precio": precio,\n        "imagenes": imagenes,\n        "carpeta": ultima_carpeta_anuncio,\n        "categoria": categoria_publicacion.get().strip(),\n        "estado": estado_producto_publicacion.get().strip(),\n        "desactivar_envio": bool(switch_desactivar_envio.get()),\n        "estado_cola": "PENDIENTE"\n    }\n\n\ndef agregar_actual_a_cola():\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes añadir anuncios mientras la cola está publicando."\n        )\n        return\n\n    if len(cola_anuncios) >= limite_cola_efectivo():\n        estado.configure(\n            text=f"❌ La cola admite como máximo {limite_cola_efectivo()} anuncios."\n        )\n        return\n\n    try:\n        item = _snapshot_anuncio_actual()\n    except Exception as e:\n        estado.configure(text="❌ " + str(e))\n        try:\n            messagebox.showerror("Cola de publicación", str(e))\n        except Exception:\n            pass\n        return\n\n    # No meter dos veces la misma carpeta.\n    if any(\n        x.get("carpeta") == item["carpeta"]\n        for x in cola_anuncios\n    ):\n        estado.configure(\n            text="⚠️ Este anuncio ya está en la cola."\n        )\n        return\n\n    if not item.get("lote_id"):\n        asignar_lote_item(\n            item,\n            "MANUAL",\n            "MANUAL"\n        )\n\n    cola_anuncios.append(item)\n    guardar_cola_en_disco()\n    refrescar_cola()\n    estado.configure(\n        text=f"✅ Anuncio añadido a la cola · {len(cola_anuncios)} pendiente(s)."\n    )\n\n\ndef quitar_ultimo_de_cola():\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes modificar la cola mientras se está publicando."\n        )\n        return\n\n    if not cola_anuncios:\n        estado.configure(text="La cola ya está vacía.")\n        return\n\n    cola_anuncios.pop()\n    guardar_cola_en_disco()\n    refrescar_cola()\n    estado.configure(text="Último anuncio eliminado de la cola.")\n\n\ndef validar_item_cola(item):\n    """\n    Revisa un anuncio ANTES de abrir Wallapop.\n    Devuelve una lista de problemas encontrados.\n    """\n    errores = []\n\n    titulo = str(item.get("titulo", "")).strip()\n    descripcion = str(item.get("descripcion", "")).strip()\n    precio = str(item.get("precio", "")).strip()\n    categoria = str(item.get("categoria", "")).strip()\n    estado_item = str(item.get("estado", "")).strip()\n    carpeta = str(item.get("carpeta", "")).strip()\n    imagenes = item.get("imagenes", [])\n\n    if not titulo:\n        errores.append("Falta el título.")\n    elif len(titulo) > 50:\n        errores.append("El título supera 50 caracteres.")\n\n    if not descripcion:\n        errores.append("Falta la descripción.")\n    elif len(descripcion) > 640:\n        errores.append("La descripción supera 640 caracteres.")\n\n    if not precio:\n        errores.append("Falta el precio.")\n    else:\n        try:\n            valor = float(\n                precio.replace("€", "")\n                .replace(",", ".")\n                .strip()\n            )\n            if valor <= 0:\n                errores.append("El precio debe ser mayor que 0.")\n        except Exception:\n            errores.append("El precio no es válido.")\n\n    if not categoria:\n        errores.append("Falta la categoría.")\n\n    if not estado_item:\n        errores.append("Falta el estado del producto.")\n\n    if not carpeta or not os.path.isdir(carpeta):\n        errores.append("No encuentro la carpeta del anuncio.")\n\n    if not isinstance(imagenes, list) or len(imagenes) != 1:\n        errores.append("El anuncio debe tener exactamente 1 imagen.")\n    else:\n        ruta = imagenes[0]\n        if not os.path.isfile(ruta):\n            errores.append("No encuentro la imagen del anuncio.")\n        else:\n            try:\n                if os.path.getsize(ruta) <= 0:\n                    errores.append("La imagen está vacía o dañada.")\n            except Exception:\n                errores.append("No pude comprobar la imagen.")\n\n    score, problemas_calidad = calcular_calidad_item(item)\n    item["calidad"] = score\n    item["calidad_problemas"] = problemas_calidad\n\n    return errores\n\n\ndef marcar_error_prevalidacion(item, errores):\n    item["estado_cola"] = "ERROR"\n    item["error_fase"] = "Prevalidación de cola"\n    item["error_motivo"] = " · ".join(errores)\n    item["error_fecha"] = time.strftime("%d/%m/%Y %H:%M:%S")\n    item["error_diagnostico"] = ""\n    item["intentos"] = int(item.get("intentos", 0))\n\n\ndef validar_toda_la_cola():\n    """\n    Revisa todos los pendientes sin publicar nada.\n    """\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes validar la cola mientras está publicando."\n        )\n        return\n\n    revisados = 0\n    con_error = 0\n\n    for item in cola_anuncios:\n        if item.get("estado_cola") != "PENDIENTE":\n            continue\n\n        revisados += 1\n        errores = validar_item_cola(item)\n\n        if errores:\n            marcar_error_prevalidacion(item, errores)\n            con_error += 1\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n\n    if revisados == 0:\n        estado.configure(text="No hay anuncios pendientes para validar.")\n    elif con_error:\n        estado.configure(\n            text=(\n                f"⚠️ Cola revisada: {con_error} de {revisados} "\n                "anuncio(s) necesitan corrección."\n            )\n        )\n    else:\n        estado.configure(\n            text=f"✅ Cola revisada: {revisados} anuncio(s) listos."\n        )\n\n\ndef eliminar_publicados_de_cola():\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes limpiar publicados mientras la cola está activa."\n        )\n        return\n\n    antes = len(cola_anuncios)\n    cola_anuncios[:] = [\n        item\n        for item in cola_anuncios\n        if item.get("estado_cola") != "PUBLICADO"\n    ]\n\n    eliminados = antes - len(cola_anuncios)\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n\n    estado.configure(\n        text=f"✅ Eliminados {eliminados} publicado(s) de la cola."\n    )\n\n\ndef confirmar_reintento_revisar():\n    indice = _indice_seleccionado_cola()\n\n    if indice is None:\n        estado.configure(\n            text="Selecciona primero un anuncio."\n        )\n        return\n\n    item = cola_anuncios[indice]\n\n    if item.get("estado_cola") != "REVISAR":\n        estado.configure(\n            text="El anuncio seleccionado no está en estado REVISAR."\n        )\n        return\n\n    respuesta = messagebox.askyesno(\n        "Confirmar reintento",\n        (\n            "Este anuncio quedó en un estado ambiguo tras una interrupción.\\n\\n"\n            "Antes de reintentarlo, comprueba que NO esté ya publicado en Wallapop.\\n\\n"\n            "¿Quieres devolverlo a PENDIENTE?"\n        )\n    )\n\n    if not respuesta:\n        return\n\n    item["estado_cola"] = "PENDIENTE"\n    item["error_fase"] = ""\n    item["error_motivo"] = ""\n    item["error_fecha"] = ""\n    item["intentos"] = 0\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n\n    estado.configure(\n        text="✅ Anuncio devuelto a PENDIENTE."\n    )\n\n\ndef reintentar_seleccionado_cola():\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes reintentar mientras la cola está publicando."\n        )\n        return\n\n    indice = _indice_seleccionado_cola()\n\n    if indice is None:\n        estado.configure(\n            text="Selecciona primero un anuncio de la cola."\n        )\n        return\n\n    item = cola_anuncios[indice]\n\n    if item.get("estado_cola") != "ERROR":\n        estado.configure(\n            text="El anuncio seleccionado no está en estado ERROR."\n        )\n        return\n\n    item["estado_cola"] = "PENDIENTE"\n    item["error_fase"] = ""\n    item["error_motivo"] = ""\n    item["error_fecha"] = ""\n    item["error_diagnostico"] = ""\n    item["intentos"] = 0\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n\n    estado.configure(\n        text=f"✅ \'{item.get(\'titulo\', \'Anuncio\')}\' listo para reintentar."\n    )\n\n\ndef _recrear_imagen_item_cola(item):\n    """\n    Regenera la imagen del anuncio usando el generador actual.\n    """\n    titulo = str(item.get("titulo", "")).strip()\n    precio = str(item.get("precio", "")).strip()\n\n    if not titulo:\n        return False, "Falta el título; no puedo regenerar la imagen."\n\n    carpeta = str(item.get("carpeta", "")).strip()\n\n    if not carpeta or not os.path.isdir(carpeta):\n        identificador = (\n            f"{int(time.time() * 1000)}_"\n            f"{random.randint(10000, 99999)}"\n        )\n\n        carpeta = os.path.abspath(\n            os.path.join(\n                CARPETA_ANUNCIOS,\n                f"anuncio_{identificador}"\n            )\n        )\n        os.makedirs(carpeta, exist_ok=True)\n        item["carpeta"] = carpeta\n\n    try:\n        beneficios = obtener_beneficios(titulo)\n\n        imagen = crear_cartel(\n            titulo,\n            beneficios,\n            precio,\n            0,\n            "",\n            False\n        )\n\n        ruta_imagen = os.path.join(\n            carpeta,\n            "imagen_1.png"\n        )\n\n        imagen.save(\n            ruta_imagen,\n            quality=95\n        )\n\n        item["imagenes"] = [ruta_imagen]\n\n        return True, "Imagen regenerada correctamente."\n\n    except Exception as e:\n        return False, "No pude regenerar la imagen: " + str(e)\n\n\ndef _reparar_config_item_cola(item):\n    """\n    Completa valores de publicación seguros usando la configuración general.\n    Nunca inventa un precio.\n    """\n    datos = cargar_configuracion_general()\n    cambios = []\n\n    if not str(item.get("categoria", "")).strip():\n        item["categoria"] = datos.get(\n            "categoria_defecto",\n            "Motor y accesorios > Recambios de coches y furgonetas > Tuning"\n        )\n        cambios.append("categoría restaurada")\n\n    if not str(item.get("estado", "")).strip():\n        item["estado"] = datos.get(\n            "estado_defecto",\n            "Nuevo"\n        )\n        cambios.append("estado restaurado")\n\n    if "desactivar_envio" not in item:\n        item["desactivar_envio"] = bool(\n            datos.get(\n                "desactivar_envio_defecto",\n                True\n            )\n        )\n        cambios.append("envío restaurado")\n\n    return cambios\n\n\ndef _es_error_temporal_reintentable(fase, motivo):\n    """\n    Errores que normalmente se solucionan reintentando desde cero,\n    sin necesidad de cambiar los datos del anuncio.\n    """\n    texto = (\n        str(fase or "")\n        + " "\n        + str(motivo or "")\n    ).lower()\n\n    patrones = [\n        "navegador",\n        "chromium",\n        "playwright",\n        "timeout",\n        "tiempo de espera",\n        "no encuentro",\n        "no encontré",\n        "no pude abrir",\n        "no respondió",\n        "no respondio",\n        "selector",\n        "categoría",\n        "categoria",\n        "estado",\n        "envío",\n        "envio",\n        "resumen",\n        "fotos",\n        "datos del anuncio",\n        "precio",\n    ]\n\n    return any(p in texto for p in patrones)\n\n\ndef reparar_item_error_cola(item):\n    """\n    Intenta reparar UN anuncio con ERROR.\n\n    Devuelve:\n      (True, detalle)  -> reparado y listo como PENDIENTE\n      (False, detalle) -> requiere intervención manual\n    """\n    fase = str(item.get("error_fase", ""))\n    motivo = str(item.get("error_motivo", ""))\n\n    # -----------------------------------------------------\n    # 1) DUPLICADOS -> crear un anuncio sustituto totalmente nuevo.\n    # -----------------------------------------------------\n    if _es_error_duplicado(\n        fase + " " + motivo\n    ):\n        ok = reemplazar_duplicado_por_anuncio_nuevo(\n            item\n        )\n\n        if ok:\n            return (\n                True,\n                "Duplicado sustituido por un anuncio nuevo."\n            )\n\n        return (\n            False,\n            "Detecté el duplicado, pero no pude crear un sustituto válido."\n        )\n\n    # -----------------------------------------------------\n    # 2) Prevalidación / datos incompletos.\n    # -----------------------------------------------------\n    cambios = _reparar_config_item_cola(\n        item\n    )\n\n    problemas = validar_item_cola(\n        item\n    )\n\n    # Si el único problema restante es imagen/carpeta, regeneramos.\n    texto_problemas = " ".join(\n        problemas\n    ).lower()\n\n    if (\n        "imagen" in texto_problemas\n        or "carpeta" in texto_problemas\n    ):\n        ok_img, detalle_img = _recrear_imagen_item_cola(\n            item\n        )\n\n        if ok_img:\n            cambios.append(\n                "imagen regenerada"\n            )\n        else:\n            return (\n                False,\n                detalle_img\n            )\n\n        problemas = validar_item_cola(\n            item\n        )\n\n    # Precio inválido NO se inventa.\n    if any(\n        "precio" in problema.lower()\n        for problema in problemas\n    ):\n        return (\n            False,\n            "El precio es inválido o falta. Necesita corrección manual."\n        )\n\n    # Título/descripción vacíos: intentamos reconstruirlos si existe uno de los dos.\n    titulo = str(\n        item.get("titulo", "")\n    ).strip()\n\n    descripcion = str(\n        item.get("descripcion", "")\n    ).strip()\n\n    if not titulo and descripcion:\n        titulo = descripcion.splitlines()[0][:50].strip()\n        item["titulo"] = titulo\n        cambios.append("título reconstruido")\n\n    if titulo and not descripcion:\n        try:\n            item["descripcion"] = crear_descripcion(\n                titulo,\n                "",\n                False\n            )\n        except Exception:\n            item["descripcion"] = (\n                f"{titulo}\\n\\n"\n                "Servicio profesional de automoción. "\n                "Consulta disponibilidad indicando los datos del vehículo."\n            )\n\n        cambios.append("descripción reconstruida")\n\n    problemas = validar_item_cola(\n        item\n    )\n\n    if not problemas:\n        item["estado_cola"] = "PENDIENTE"\n        item["error_fase"] = ""\n        item["error_motivo"] = ""\n        item["error_fecha"] = ""\n        item["error_diagnostico"] = ""\n        item["intentos"] = 0\n\n        return (\n            True,\n            ", ".join(cambios)\n            if cambios\n            else "Validación reparada."\n        )\n\n    # -----------------------------------------------------\n    # 3) Error temporal de interfaz/navegador -> volver a PENDIENTE.\n    # -----------------------------------------------------\n    if _es_error_temporal_reintentable(\n        fase,\n        motivo\n    ):\n        item["estado_cola"] = "PENDIENTE"\n        item["error_fase"] = ""\n        item["error_motivo"] = ""\n        item["error_fecha"] = ""\n        item["error_diagnostico"] = ""\n        item["intentos"] = 0\n\n        return (\n            True,\n            "Error temporal limpiado; anuncio preparado para reintentar."\n        )\n\n    return (\n        False,\n        "No hay una reparación automática segura para este error."\n    )\n\n\ndef reparar_errores_cola_automaticamente():\n    """\n    Recorre todos los elementos con ERROR e intenta arreglarlos.\n    No publica nada; solo repara y devuelve a PENDIENTE los que quedan válidos.\n    """\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes reparar la cola mientras está publicando."\n        )\n        return\n\n    errores = [\n        item\n        for item in cola_anuncios\n        if item.get("estado_cola") == "ERROR"\n    ]\n\n    if not errores:\n        estado.configure(\n            text="✅ No hay errores que reparar."\n        )\n        return\n\n    reparados = 0\n    pendientes_manual = 0\n    detalles = []\n\n    for indice, item in enumerate(\n        errores,\n        start=1\n    ):\n        titulo = item.get(\n            "titulo",\n            "Sin título"\n        )\n\n        estado.configure(\n            text=(\n                f"🛠 Reparando error {indice}/{len(errores)} · "\n                f"{titulo[:45]}"\n            )\n        )\n\n        try:\n            ventana.update_idletasks()\n        except Exception:\n            pass\n\n        try:\n            ok, detalle = reparar_item_error_cola(\n                item\n            )\n        except Exception as e:\n            ok = False\n            detalle = str(e)\n\n        if ok:\n            reparados += 1\n            detalles.append(\n                f"✅ {titulo}: {detalle}"\n            )\n        else:\n            pendientes_manual += 1\n\n            # Conservamos el error original y añadimos motivo de no reparación.\n            anterior = str(\n                item.get(\n                    "error_motivo",\n                    ""\n                )\n            ).strip()\n\n            item["error_motivo"] = (\n                anterior\n                + (\n                    "\\n\\n"\n                    if anterior\n                    else ""\n                )\n                + "Autorreparación: "\n                + detalle\n            )\n\n            detalles.append(\n                f"⚠️ {titulo}: {detalle}"\n            )\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n    refrescar_dashboard()\n\n    # Mostrar resumen en el panel de detalle.\n    try:\n        detalle_error_texto.configure(\n            state="normal"\n        )\n        detalle_error_texto.delete(\n            "1.0",\n            "end"\n        )\n        detalle_error_texto.insert(\n            "1.0",\n            "AUTORREPARACIÓN DE COLA\\n"\n            "========================\\n\\n"\n            + "\\n\\n".join(detalles)\n        )\n        detalle_error_texto.configure(\n            state="disabled"\n        )\n    except Exception:\n        pass\n\n    estado.configure(\n        text=(\n            f"🛠 Autorreparación terminada · "\n            f"{reparados} reparado(s) · "\n            f"{pendientes_manual} requieren revisión manual."\n        )\n    )\n\n    try:\n        messagebox.showinfo(\n            "Autorreparación de cola",\n            (\n                f"Reparados automáticamente: {reparados}\\n"\n                f"Requieren revisión manual: {pendientes_manual}\\n\\n"\n                "Los anuncios reparados han vuelto a PENDIENTE."\n            )\n        )\n    except Exception:\n        pass\n\n\ndef reintentar_errores_cola():\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes modificar la cola mientras se está publicando."\n        )\n        return\n\n    encontrados = 0\n\n    for item in cola_anuncios:\n        if item.get("estado_cola") == "ERROR":\n            item["estado_cola"] = "PENDIENTE"\n            encontrados += 1\n\n    if encontrados == 0:\n        estado.configure(\n            text="No hay anuncios con error para reintentar."\n        )\n        return\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n\n    estado.configure(\n        text=f"✅ {encontrados} anuncio(s) preparados para reintentar."\n    )\n\n\ndef vaciar_cola():\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes vaciar la cola mientras se está publicando."\n        )\n        return\n\n    cola_anuncios.clear()\n    guardar_cola_en_disco()\n    refrescar_cola()\n    estado.configure(text="Cola vaciada.")\n\n\ndef pausar_cola():\n    global cola_pausada\n\n    if not cola_en_ejecucion:\n        estado.configure(text="No hay una cola activa.")\n        return\n\n    cola_pausada = True\n\n    try:\n        boton_pausar_cola.configure(state="disabled")\n        boton_reanudar_cola.configure(state="normal")\n    except Exception:\n        pass\n\n    estado.configure(\n        text="⏸ Cola pausada. El anuncio actual terminará antes de detenerse."\n    )\n\n\ndef reanudar_cola():\n    global cola_pausada\n\n    if not cola_en_ejecucion:\n        estado.configure(text="No hay una cola activa.")\n        return\n\n    cola_pausada = False\n\n    try:\n        boton_pausar_cola.configure(state="normal")\n        boton_reanudar_cola.configure(state="disabled")\n    except Exception:\n        pass\n\n    estado.configure(text="▶ Cola reanudada.")\n\n\ndef _esperar_si_pausada():\n    while cola_pausada and not cola_cancelar:\n        time.sleep(0.5)\n\n\ndef _resumen_sesion_texto():\n    if not cola_inicio_sesion:\n        return "Sesión · Publicados: 0 · Errores: 0 · Tiempo: 00:00"\n\n    duracion = max(0, int(time.time() - cola_inicio_sesion))\n    media = 0.0\n\n    if rendimiento_tiempos_publicacion:\n        media = (\n            sum(rendimiento_tiempos_publicacion)\n            / len(rendimiento_tiempos_publicacion)\n        )\n\n    return (\n        f"Sesión · Publicados: {cola_publicados_sesion} · "\n        f"Errores: {cola_errores_sesion} · "\n        f"Tiempo: {duracion // 60:02d}:{duracion % 60:02d} · "\n        f"Media/anuncio: {media:.1f}s"\n    )\n\n\ndef _eta_cola_texto():\n    pendientes = sum(\n        1 for x in cola_anuncios\n        if x.get("estado_cola") == "PENDIENTE"\n    )\n\n    if pendientes <= 0:\n        return "ETA aprox.: 00:00"\n\n    try:\n        datos = cargar_configuracion_general()\n        pausa_min = int(datos.get("pausa_min_publicaciones", 8))\n        pausa_max = int(datos.get("pausa_max_publicaciones", 15))\n        pausa_media = max(0, (pausa_min + pausa_max) // 2)\n    except Exception:\n        pausa_media = 10\n\n    segundos = pendientes * (35 + pausa_media)\n\n    return (\n        f"ETA aprox.: {segundos // 60:02d}:{segundos % 60:02d}"\n    )\n\n\ndef cancelar_cola():\n    global cola_cancelar\n\n    if not cola_en_ejecucion:\n        estado.configure(text="No hay una cola en ejecución.")\n        return\n\n    # No interrumpe un anuncio a mitad: se detiene antes del siguiente.\n    cola_cancelar = True\n    estado.configure(\n        text="⏹ La cola se detendrá al terminar el anuncio actual."\n    )\n\n\ndef refrescar_progreso_cola():\n    try:\n        total = len(cola_anuncios)\n\n        if total <= 0:\n            progreso_cola.set(0)\n            etiqueta_progreso_cola.configure(\n                text="Progreso: 0 / 0"\n            )\n            return\n\n        terminados = sum(\n            1\n            for item in cola_anuncios\n            if item.get("estado_cola") in ("PUBLICADO", "ERROR")\n        )\n\n        progreso = terminados / total\n        progreso_cola.set(progreso)\n\n        publicados = sum(\n            1\n            for item in cola_anuncios\n            if item.get("estado_cola") == "PUBLICADO"\n        )\n\n        errores = sum(\n            1\n            for item in cola_anuncios\n            if item.get("estado_cola") == "ERROR"\n        )\n\n        etiqueta_progreso_cola.configure(\n            text=(\n                f"Progreso: {terminados}/{total}  ·  "\n                f"Publicados: {publicados}  ·  Errores: {errores}"\n            )\n        )\n    except Exception:\n        pass\n\n\ndef refrescar_salud_cola():\n    try:\n        cdp_ok = _puerto_chrome_activo()\n    except Exception:\n        cdp_ok = False\n\n    try:\n        playwright_ok = sync_playwright is not None\n    except Exception:\n        playwright_ok = False\n\n    try:\n        total = len(cola_anuncios)\n        pendientes = sum(\n            1 for x in cola_anuncios\n            if x.get("estado_cola") == "PENDIENTE"\n        )\n        errores = sum(\n            1 for x in cola_anuncios\n            if x.get("estado_cola") == "ERROR"\n        )\n    except Exception:\n        total = pendientes = errores = 0\n\n    estado_navegador_txt = "✅ Chromium" if cdp_ok else "❌ Chromium"\n    estado_playwright_txt = "✅ Playwright" if playwright_ok else "❌ Playwright"\n\n    try:\n        etiqueta_salud_cola.configure(\n            text=(\n                f"{estado_navegador_txt}   ·   "\n                f"{estado_playwright_txt}   ·   "\n                f"Cola: {total}   ·   "\n                f"Pendientes: {pendientes}   ·   "\n                f"Errores: {errores}"\n            ),\n            text_color=(\n                "#46d47a"\n                if cdp_ok and playwright_ok and errores == 0\n                else "#f7c948"\n            )\n        )\n    except Exception:\n        pass\n\n\ndef refrescar_cola():\n    try:\n        cola_texto.configure(state="normal")\n        cola_texto.delete("1.0", "end")\n\n        if not cola_anuncios:\n            cola_texto.insert(\n                "1.0",\n                "La cola está vacía.\\n\\n"\n                "Genera un anuncio y pulsa “Añadir anuncio actual”."\n            )\n        else:\n            lineas = []\n\n            filtro_actual = var_filtro_cola.get().strip().upper()\n            busqueda_actual = var_busqueda_cola.get().strip().lower()\n            filtro_lote_actual = var_filtro_lote.get().strip()\n\n            for i, item in enumerate(cola_anuncios, start=1):\n                estado_item = item.get("estado_cola", "PENDIENTE")\n\n                if (\n                    filtro_actual != "TODOS"\n                    and str(estado_item).upper() != filtro_actual\n                ):\n                    continue\n\n                if (\n                    busqueda_actual\n                    and busqueda_actual not in _texto_busqueda_item(item)\n                ):\n                    continue\n\n                nombre_lote_item = _nombre_lote_item(\n                    item\n                )\n\n                if (\n                    filtro_lote_actual != "TODOS"\n                    and nombre_lote_item != filtro_lote_actual\n                ):\n                    continue\n\n                icono = {\n                    "PENDIENTE": "🕒",\n                    "PUBLICANDO": "⏳",\n                    "PUBLICADO": "✅",\n                    "ERROR": "❌"\n                }.get(estado_item, "•")\n\n                intentos = int(item.get("intentos", 0))\n                reemplazos = int(\n                    item.get(\n                        "reemplazos_duplicado",\n                        0\n                    )\n                )\n\n                extra_reemplazo = (\n                    f" · Sustituciones: {reemplazos}"\n                    if reemplazos\n                    else ""\n                )\n\n                prioridad = item.get("prioridad", "Normal")\n                nivel_auditoria = item.get(\n                    "auditoria_nivel",\n                    "—"\n                )\n\n                calidad = item.get("calidad")\n                if calidad is None:\n                    calidad, problemas_calidad = calcular_calidad_item(item)\n                    item["calidad"] = calidad\n                    item["calidad_problemas"] = problemas_calidad\n\n                nota = str(\n                    item.get(\n                        "nota",\n                        ""\n                    )\n                ).strip()\n\n                extra_nota = (\n                    " · 📝"\n                    if nota\n                    else ""\n                )\n\n                extra_favorito = (\n                    " · ⭐"\n                    if item.get("favorito")\n                    else ""\n                )\n\n                similitud = int(\n                    item.get(\n                        "similaridad_maxima",\n                        0\n                    )\n                )\n\n                extra_similitud = (\n                    f" · ≈{similitud}%"\n                    if similitud\n                    else ""\n                )\n\n                extra_lote = (\n                    f" · L:{nombre_lote_item[:10]}"\n                    if nombre_lote_item\n                    else ""\n                )\n\n                lineas.append(\n                    f"{i:02d}. {icono} {estado_item:<10}  "\n                    f"{item[\'titulo\'][:31]:<31}  "\n                    f"{item[\'precio\']} €  ·  {prioridad:<6}  ·  Q:{calidad:>3}/100  "\n                    f"· A:{nivel_auditoria:<8} · Intentos: {intentos}"\n                    f"{extra_reemplazo}{extra_nota}{extra_favorito}"\n                    f"{extra_lote}{extra_similitud}"\n                )\n\n                if estado_item == "ERROR":\n                    motivo = str(\n                        item.get("error_motivo")\n                        or "Motivo no registrado."\n                    ).replace("\\n", " ")\n\n                    fase_error = str(\n                        item.get("error_fase")\n                        or "Publicación"\n                    )\n\n                    if len(motivo) > 95:\n                        motivo = motivo[:92] + "..."\n\n                    lineas.append(\n                        f"      ↳ {fase_error}: {motivo}"\n                    )\n\n            cola_texto.insert("1.0", "\\n".join(lineas))\n\n        cola_texto.configure(state="disabled")\n\n        pendientes = sum(\n            1 for x in cola_anuncios\n            if x.get("estado_cola") == "PENDIENTE"\n        )\n\n        etiqueta_cola_resumen.configure(\n            text=(\n                f"{len(cola_anuncios)} anuncio(s) · "\n                f"{pendientes} pendiente(s)"\n            )\n        )\n\n    except Exception:\n        pass\n\n    actualizar_selector_cola()\n    refrescar_salud_cola()\n    refrescar_progreso_cola()\n\n    try:\n        etiqueta_resumen_sesion.configure(\n            text=_resumen_sesion_texto()\n        )\n        etiqueta_eta_cola.configure(\n            text=_eta_cola_texto()\n        )\n    except Exception:\n        pass\n\n    try:\n        refrescar_dashboard()\n    except Exception:\n        pass\n\n    try:\n        refrescar_estadisticas()\n    except Exception:\n        pass\n\n\n# =========================================================\n# ROTACIÓN AUTOMÁTICA DEL CATÁLOGO\n# =========================================================\n\nARCHIVO_ROTACION_CATALOGO = os.path.join(\n    CARPETA_DATOS_USUARIO,\n    "rotacion_catalogo.json"\n)\n\nARCHIVO_HISTORIAL_ROTACION = os.path.join(\n    CARPETA_DATOS_USUARIO,\n    "historial_rotacion_catalogo.json"\n)\n\nrotacion_borrados_sesion = 0\n\n\ndef _rotacion_config_defecto():\n    return {\n        "activo": True,\n        "solo_anuncios_bot": True,\n        "antiguedad_min_horas": 24,\n        "max_borrados_sesion": 5,\n        "backup_antes_borrar": True,\n        "reintentar_publicacion": True,\n        "espera_despues_borrar": 3,\n    }\n\n\ndef cargar_config_rotacion():\n    datos = _rotacion_config_defecto()\n\n    try:\n        ruta = Path(\n            ARCHIVO_ROTACION_CATALOGO\n        )\n\n        if ruta.exists():\n            guardado = json.loads(\n                ruta.read_text(\n                    encoding="utf-8"\n                )\n            )\n\n            if isinstance(\n                guardado,\n                dict\n            ):\n                # Migración automática V86 -> V86:\n                # si existe antigüedad en días, convertir a horas.\n                if (\n                    "antiguedad_min_horas" not in guardado\n                    and "antiguedad_min_dias" in guardado\n                ):\n                    try:\n                        guardado[\n                            "antiguedad_min_horas"\n                        ] = int(\n                            guardado.get(\n                                "antiguedad_min_dias",\n                                1\n                            )\n                        ) * 24\n                    except Exception:\n                        guardado[\n                            "antiguedad_min_horas"\n                        ] = 24\n\n                datos.update(\n                    guardado\n                )\n    except Exception:\n        pass\n\n    return datos\n\n\ndef guardar_config_rotacion(datos):\n    ruta = Path(\n        ARCHIVO_ROTACION_CATALOGO\n    )\n\n    ruta.parent.mkdir(\n        parents=True,\n        exist_ok=True\n    )\n\n    ruta.write_text(\n        json.dumps(\n            datos,\n            ensure_ascii=False,\n            indent=2\n        ),\n        encoding="utf-8"\n    )\n\n\ndef _cargar_historial_rotacion():\n    try:\n        ruta = Path(\n            ARCHIVO_HISTORIAL_ROTACION\n        )\n\n        if ruta.exists():\n            datos = json.loads(\n                ruta.read_text(\n                    encoding="utf-8"\n                )\n            )\n\n            if isinstance(\n                datos,\n                list\n            ):\n                return datos\n    except Exception:\n        pass\n\n    return []\n\n\ndef _registrar_evento_rotacion(\n    accion,\n    candidato=None,\n    detalle=""\n):\n    historial = _cargar_historial_rotacion()\n\n    historial.append({\n        "fecha": time.strftime(\n            "%d/%m/%Y %H:%M:%S"\n        ),\n        "accion": str(\n            accion\n        ),\n        "id": (\n            candidato.get(\n                "id",\n                ""\n            )\n            if isinstance(\n                candidato,\n                dict\n            )\n            else ""\n        ),\n        "titulo": (\n            candidato.get(\n                "titulo",\n                ""\n            )\n            if isinstance(\n                candidato,\n                dict\n            )\n            else ""\n        ),\n        "precio": (\n            candidato.get(\n                "precio",\n                ""\n            )\n            if isinstance(\n                candidato,\n                dict\n            )\n            else ""\n        ),\n        "detalle": str(\n            detalle\n        ),\n    })\n\n    historial = historial[\n        -1000:\n    ]\n\n    Path(\n        ARCHIVO_HISTORIAL_ROTACION\n    ).write_text(\n        json.dumps(\n            historial,\n            ensure_ascii=False,\n            indent=2\n        ),\n        encoding="utf-8"\n    )\n\n\ndef _fecha_publicado_timestamp(valor):\n    try:\n        return time.mktime(\n            time.strptime(\n                str(valor),\n                "%d/%m/%Y %H:%M:%S"\n            )\n        )\n    except Exception:\n        return 0.0\n\n\ndef _horas_desde_publicacion(valor):\n    ts = _fecha_publicado_timestamp(\n        valor\n    )\n\n    if not ts:\n        return 999999\n\n    return max(\n        0,\n        int(\n            (\n                time.time()\n                - ts\n            )\n            // 3600\n        )\n    )\n\n\ndef _candidatos_rotacion_catalogo():\n    """\n    Solo considera publicaciones que están registradas localmente\n    como creadas por DIAGPROG5.\n    """\n    cfg = cargar_config_rotacion()\n    publicados = cargar_publicados()\n    candidatos = []\n\n    for identificador, datos in publicados.items():\n        if not isinstance(\n            datos,\n            dict\n        ):\n            continue\n\n        if datos.get(\n            "protegido_rotacion",\n            False\n        ):\n            continue\n\n        if datos.get(\n            "eliminado_wallapop",\n            False\n        ):\n            continue\n\n        fecha = datos.get(\n            "fecha",\n            ""\n        )\n\n        horas = _horas_desde_publicacion(\n            fecha\n        )\n\n        if horas < int(\n            cfg.get(\n                "antiguedad_min_horas",\n                24\n            )\n        ):\n            continue\n\n        candidatos.append({\n            "id": identificador,\n            "titulo": str(\n                datos.get(\n                    "titulo",\n                    ""\n                )\n            ).strip(),\n            "precio": str(\n                datos.get(\n                    "precio",\n                    ""\n                )\n            ).strip(),\n            "fecha": fecha,\n            "horas": horas,\n        })\n\n    candidatos.sort(\n        key=lambda x: (\n            _fecha_publicado_timestamp(\n                x.get(\n                    "fecha",\n                    ""\n                )\n            ),\n            x.get(\n                "titulo",\n                ""\n            ).lower()\n        )\n    )\n\n    return candidatos\n\n\ndef _guardar_backup_candidato_rotacion(\n    candidato\n):\n    cfg = cargar_config_rotacion()\n\n    if not cfg.get(\n        "backup_antes_borrar",\n        True\n    ):\n        return ""\n\n    try:\n        carpeta = (\n            Path(\n                CARPETA_DATOS_USUARIO\n            )\n            / "backups_rotacion"\n        )\n\n        carpeta.mkdir(\n            parents=True,\n            exist_ok=True\n        )\n\n        sello = time.strftime(\n            "%Y%m%d_%H%M%S"\n        )\n\n        ruta = carpeta / (\n            f"{sello}_"\n            f"{_nombre_seguro_archivo(candidato.get(\'titulo\', \'anuncio\'))}"\n            ".json"\n        )\n\n        ruta.write_text(\n            json.dumps(\n                candidato,\n                ensure_ascii=False,\n                indent=2\n            ),\n            encoding="utf-8"\n        )\n\n        return str(\n            ruta\n        )\n\n    except Exception:\n        return ""\n\n\ndef _marcar_publicado_eliminado_rotacion(\n    candidato\n):\n    try:\n        publicados = cargar_publicados()\n        identificador = candidato.get(\n            "id",\n            ""\n        )\n\n        if identificador in publicados:\n            publicados[\n                identificador\n            ][\n                "eliminado_wallapop"\n            ] = True\n\n            publicados[\n                identificador\n            ][\n                "fecha_eliminacion_wallapop"\n            ] = time.strftime(\n                "%d/%m/%Y %H:%M:%S"\n            )\n\n            with open(\n                ARCHIVO_PUBLICADOS,\n                "w",\n                encoding="utf-8"\n            ) as archivo:\n                json.dump(\n                    publicados,\n                    archivo,\n                    ensure_ascii=False,\n                    indent=4\n                )\n\n    except Exception:\n        pass\n\n\ndef proteger_publicado_rotacion(\n    identificador,\n    protegido=True\n):\n    publicados = cargar_publicados()\n\n    if identificador not in publicados:\n        return False\n\n    publicados[\n        identificador\n    ][\n        "protegido_rotacion"\n    ] = bool(\n        protegido\n    )\n\n    with open(\n        ARCHIVO_PUBLICADOS,\n        "w",\n        encoding="utf-8"\n    ) as archivo:\n        json.dump(\n            publicados,\n            archivo,\n            ensure_ascii=False,\n            indent=4\n        )\n\n    return True\n\n\ndef abrir_gestor_protecciones_rotacion():\n    publicados = cargar_publicados()\n\n    elementos = [\n        (\n            identificador,\n            datos\n        )\n        for identificador, datos in publicados.items()\n        if isinstance(\n            datos,\n            dict\n        )\n        and not datos.get(\n            "eliminado_wallapop",\n            False\n        )\n    ]\n\n    if not elementos:\n        messagebox.showinfo(\n            "Protección de anuncios",\n            "Todavía no hay publicaciones registradas por DIAGPROG5."\n        )\n        return\n\n    win = ctk.CTkToplevel(\n        ventana\n    )\n\n    win.title(\n        "DIAGPROG5 · Proteger anuncios"\n    )\n\n    win.geometry(\n        "760x380"\n    )\n\n    win.configure(\n        fg_color=COLOR_FONDO\n    )\n\n    ctk.CTkLabel(\n        win,\n        text="Proteger anuncios frente a la rotación",\n        font=("Segoe UI", 20, "bold")\n    ).pack(\n        anchor="w",\n        padx=20,\n        pady=(18, 4)\n    )\n\n    ctk.CTkLabel(\n        win,\n        text=(\n            "Los anuncios protegidos nunca serán eliminados "\n            "por la rotación automática."\n        ),\n        font=("Segoe UI", 10),\n        text_color=COLOR_TEXTO_SEC\n    ).pack(\n        anchor="w",\n        padx=20,\n        pady=(0, 14)\n    )\n\n    etiquetas = []\n\n    mapa = {}\n\n    for identificador, datos in elementos:\n        marca = (\n            "🔒"\n            if datos.get(\n                "protegido_rotacion",\n                False\n            )\n            else "○"\n        )\n\n        etiqueta = (\n            f"{marca} "\n            f"{datos.get(\'titulo\', \'Sin título\')} "\n            f"· {datos.get(\'fecha\', \'—\')}"\n        )\n\n        etiquetas.append(\n            etiqueta\n        )\n\n        mapa[\n            etiqueta\n        ] = identificador\n\n    selector = ctk.CTkOptionMenu(\n        win,\n        values=etiquetas,\n        height=38\n    )\n\n    selector.pack(\n        fill="x",\n        padx=20,\n        pady=(0, 16)\n    )\n\n    resultado = ctk.CTkLabel(\n        win,\n        text="",\n        font=("Segoe UI", 10)\n    )\n\n    resultado.pack(\n        anchor="w",\n        padx=20,\n        pady=(0, 8)\n    )\n\n    def cambiar(\n        protegido\n    ):\n        etiqueta = selector.get()\n        identificador = mapa.get(\n            etiqueta\n        )\n\n        if not identificador:\n            return\n\n        proteger_publicado_rotacion(\n            identificador,\n            protegido\n        )\n\n        resultado.configure(\n            text=(\n                "✅ Anuncio protegido."\n                if protegido\n                else "✅ Protección eliminada."\n            ),\n            text_color="#46d47a"\n        )\n\n    fila = ctk.CTkFrame(\n        win,\n        fg_color="transparent"\n    )\n\n    fila.pack(\n        fill="x",\n        padx=20,\n        pady=(0, 18)\n    )\n\n    ctk.CTkButton(\n        fila,\n        text="🔒 Proteger",\n        command=lambda: cambiar(\n            True\n        ),\n        height=38,\n        fg_color="#235d3a",\n        hover_color="#2d7549"\n    ).pack(\n        side="left",\n        fill="x",\n        expand=True,\n        padx=(0, 5)\n    )\n\n    ctk.CTkButton(\n        fila,\n        text="Quitar protección",\n        command=lambda: cambiar(\n            False\n        ),\n        height=38,\n        fg_color=COLOR_PANEL_2,\n        hover_color="#252b32"\n    ).pack(\n        side="left",\n        fill="x",\n        expand=True,\n        padx=(5, 0)\n    )\n\n\ndef mostrar_candidatos_rotacion():\n    candidatos = _candidatos_rotacion_catalogo()\n\n    if not candidatos:\n        messagebox.showinfo(\n            "Rotación automática",\n            (\n                "No hay anuncios antiguos elegibles para eliminar.\\n\\n"\n                "Puede ser porque son demasiado recientes o están protegidos."\n            )\n        )\n        return\n\n    lineas = []\n\n    for candidato in candidatos[:20]:\n        lineas.append(\n            f"• {candidato[\'titulo\']} · "\n            f"{candidato[\'horas\']} horas · "\n            f"{candidato[\'precio\']} €"\n        )\n\n    messagebox.showinfo(\n        "Candidatos de rotación",\n        (\n            "Más antiguos primero:\\n\\n"\n            + "\\n".join(\n                lineas\n            )\n        )\n    )\n\n\ndef _normalizar_texto_catalogo(valor):\n    texto = str(\n        valor or ""\n    ).strip().lower()\n\n    reemplazos = {\n        "á": "a",\n        "é": "e",\n        "í": "i",\n        "ó": "o",\n        "ú": "u",\n        "ü": "u",\n    }\n\n    for a, b in reemplazos.items():\n        texto = texto.replace(\n            a,\n            b\n        )\n\n    texto = re.sub(\n        r"\\s+",\n        " ",\n        texto\n    )\n\n    return texto\n\n\ndef _catalogo_visible_texto(page):\n    try:\n        return (\n            page.locator(\n                "body"\n            ).inner_text(\n                timeout=2500\n            )\n            or ""\n        )\n    except Exception:\n        return ""\n\n\ndef _catalogo_contiene_titulo(\n    page,\n    titulo\n):\n    objetivo = _normalizar_texto_catalogo(\n        titulo\n    )\n\n    for _ in range(16):\n        contenido = _normalizar_texto_catalogo(\n            _catalogo_visible_texto(\n                page\n            )\n        )\n\n        if objetivo and objetivo in contenido:\n            return True\n\n        try:\n            page.mouse.wheel(\n                0,\n                1100\n            )\n\n            page.wait_for_timeout(\n                350\n            )\n        except Exception:\n            pass\n\n    return False\n\n\ndef diagnosticar_rotacion_catalogo(\n    mostrar=True\n):\n    """\n    Explica exactamente por qué la rotación puede o no funcionar.\n    """\n    cfg = cargar_config_rotacion()\n    candidatos = _candidatos_rotacion_catalogo()\n\n    try:\n        _ui_activa = bool(\n            var_rotacion_activa.get()\n        )\n    except Exception:\n        _ui_activa = bool(\n            cfg.get(\n                "activo",\n                True\n            )\n        )\n\n    try:\n        _ui_activa = bool(\n            var_rotacion_activa.get()\n        )\n    except Exception:\n        _ui_activa = bool(\n            cfg.get(\n                "activo",\n                True\n            )\n        )\n\n    try:\n        _ui_activa = bool(\n            var_rotacion_activa.get()\n        )\n    except Exception:\n        _ui_activa = bool(\n            cfg.get(\n                "activo",\n                True\n            )\n        )\n\n    # V92 · DIAGNÓSTICO TOTAL DE ROTACIÓN\n    # Una sola ventana debe contener todo lo necesario para depurar.\n    try:\n        _diag_publicados = cargar_publicados()\n    except Exception as _e:\n        _diag_publicados = {}\n        _diag_publicados_error = str(_e)\n    else:\n        _diag_publicados_error = ""\n\n    try:\n        _diag_cola = list(cola_anuncios)\n    except Exception:\n        _diag_cola = []\n\n    try:\n        _diag_candidatos = obtener_candidatos_rotacion()\n    except Exception:\n        try:\n            _diag_candidatos = candidatos\n        except Exception:\n            _diag_candidatos = []\n\n    try:\n        _diag_log_path = Path(CARPETA_DATOS_USUARIO) / "rotacion_v92.log"\n        if not _diag_log_path.exists():\n            _diag_log_path = Path(CARPETA_DATOS_USUARIO) / "rotacion_v92.log"\n\n        if _diag_log_path.exists():\n            _diag_log_lineas = _diag_log_path.read_text(\n                encoding="utf-8",\n                errors="replace"\n            ).splitlines()[-12:]\n        else:\n            _diag_log_lineas = []\n    except Exception:\n        _diag_log_lineas = []\n\n    try:\n        _diag_motor_en_curso = bool(\n            globals().get(\n                "_ROTACION_V92_EN_CURSO",\n                globals().get(\n                    "_ROTACION_V92_EN_CURSO",\n                    False\n                )\n            )\n        )\n        _diag_motor_ultimo = str(\n            globals().get(\n                "_ROTACION_V92_ULTIMO",\n                globals().get(\n                    "_ROTACION_V92_ULTIMO",\n                    ""\n                )\n            )\n            or ""\n        )\n    except Exception:\n        _diag_motor_en_curso = False\n        _diag_motor_ultimo = ""\n\n    try:\n        _ui_activa = bool(\n            var_rotacion_activa.get()\n        )\n    except Exception:\n        _ui_activa = bool(\n            cfg.get(\n                "activo",\n                True\n            )\n        )\n\n    try:\n        _ui_activa = bool(\n            var_rotacion_activa.get()\n        )\n    except Exception:\n        _ui_activa = bool(\n            cfg.get(\n                "activo",\n                True\n            )\n        )\n\n    try:\n        _ui_activa = bool(\n            var_rotacion_activa.get()\n        )\n    except Exception:\n        _ui_activa = bool(\n            cfg.get(\n                "activo",\n                True\n            )\n        )\n\n    try:\n        _ui_activa = bool(\n            var_rotacion_activa.get()\n        )\n    except Exception:\n        _ui_activa = bool(\n            cfg.get(\n                "activo",\n                True\n            )\n        )\n\n    try:\n        _ui_activa = bool(\n            var_rotacion_activa.get()\n        )\n    except Exception:\n        _ui_activa = bool(\n            cfg.get(\n                "activo",\n                True\n            )\n        )\n\n    try:\n        _ui_activa = bool(\n            var_rotacion_activa.get()\n        )\n    except Exception:\n        _ui_activa = bool(\n            cfg.get(\n                "activo",\n                True\n            )\n        )\n\n    try:\n        _ui_activa = bool(\n            var_rotacion_activa.get()\n        )\n    except Exception:\n        _ui_activa = bool(\n            cfg.get(\n                "activo",\n                True\n            )\n        )\n\n    try:\n        _ui_activa = bool(\n            var_rotacion_activa.get()\n        )\n    except Exception:\n        _ui_activa = bool(\n            cfg.get(\n                "activo",\n                True\n            )\n        )\n\n    try:\n        _ui_activa = bool(\n            var_rotacion_activa.get()\n        )\n    except Exception:\n        _ui_activa = bool(\n            cfg.get(\n                "activo",\n                True\n            )\n        )\n\n    try:\n        _ui_activa = bool(\n            var_rotacion_activa.get()\n        )\n    except Exception:\n        _ui_activa = bool(\n            cfg.get(\n                "activo",\n                True\n            )\n        )\n\n    try:\n        _ui_activa = bool(\n            var_rotacion_activa.get()\n        )\n    except Exception:\n        _ui_activa = bool(\n            cfg.get(\n                "activo",\n                True\n            )\n        )\n\n    try:\n        _ui_activa = bool(\n            var_rotacion_activa.get()\n        )\n    except Exception:\n        _ui_activa = bool(\n            cfg.get(\n                "activo",\n                True\n            )\n        )\n\n    try:\n        _ui_activa = bool(\n            var_rotacion_activa.get()\n        )\n    except Exception:\n        _ui_activa = bool(\n            cfg.get(\n                "activo",\n                True\n            )\n        )\n\n    lineas = [\n        "DIAGNÓSTICO DE ROTACIÓN",\n        "=" * 38,\n        "",\n        f"Activo: {\'Sí\' if cfg.get(\'activo\', True) else \'No\'}",\n        (\n            "Antigüedad mínima: "\n            f"{cfg.get(\'antiguedad_min_horas\', 24)} horas"\n        ),\n        (\n            "Máximo por sesión: "\n            f"{cfg.get(\'max_borrados_sesion\', 5)}"\n        ),\n        (\n            "Eliminados esta sesión: "\n            f"{rotacion_borrados_sesion}"\n        ),\n        (\n            "Candidatos locales elegibles: "\n            f"{len(candidatos)}"\n        ),\n        "",\n    ]\n\n    if not candidatos:\n        lineas.extend([\n            "⚠ No hay candidatos locales.",\n            "",\n            (\n                "La rotación solo puede borrar anuncios que el bot "\n                "tiene registrados como publicados, no protegidos "\n                "y con suficiente antigüedad."\n            ),\n        ])\n\n    else:\n        lineas.append(\n            "Primeros candidatos:"\n        )\n\n        for c in candidatos[:8]:\n            lineas.append(\n                (\n                    f"• {c.get(\'titulo\', \'\')} · "\n                    f"{c.get(\'horas\', 0)} horas"\n                )\n            )\n\n    # CDP\n    try:\n        ok_cdp, detalle_cdp = (\n            _diagnostico_chrome_cdp()\n        )\n\n        lineas.extend([\n            "",\n            (\n                "Chrome/CDP: "\n                + (\n                    "✅ "\n                    if ok_cdp\n                    else "❌ "\n                )\n                + detalle_cdp\n            ),\n        ])\n    except Exception as e:\n        lineas.extend([\n            "",\n            f"Chrome/CDP: ❌ {e}",\n        ])\n\n    informe = "\\n".join(\n        lineas\n    )\n\n    if mostrar:\n        messagebox.showinfo(\n            "Rotación automática",\n            informe\n        )\n\n    return informe\n\n\ndef probar_rotacion_sin_borrar():\n    """\n    Prueba real de navegación y localización, pero NO borra.\n    """\n    candidatos = _candidatos_rotacion_catalogo()\n\n    if not candidatos:\n        messagebox.showwarning(\n            "Prueba de rotación",\n            (\n                "No hay candidatos elegibles.\\n\\n"\n                "Revisa la antigüedad mínima, los anuncios protegidos "\n                "y que DIAGPROG5 tenga publicaciones registradas."\n            )\n        )\n        return\n\n    candidato = candidatos[\n        0\n    ]\n\n    if sync_playwright is None:\n        messagebox.showerror(\n            "Prueba de rotación",\n            "Playwright no está disponible."\n        )\n        return\n\n    try:\n        _asegurar_chromium_controlable()\n\n        with sync_playwright() as p:\n            navegador = _conectar_playwright_cdp(\n                p\n            )\n\n            if not navegador.contexts:\n                raise RuntimeError(\n                    "No encuentro contexto de Chromium."\n                )\n\n            contexto = navegador.contexts[\n                0\n            ]\n\n            pagina = contexto.new_page()\n\n            try:\n                pagina.goto(\n                    "https://es.wallapop.com/app/catalog/published",\n                    wait_until="domcontentloaded",\n                    timeout=45000\n                )\n\n                pagina.wait_for_timeout(\n                    1800\n                )\n\n                encontrado = _catalogo_contiene_titulo(\n                    pagina,\n                    candidato.get(\n                        "titulo",\n                        ""\n                    )\n                )\n\n                if encontrado:\n                    messagebox.showinfo(\n                        "Prueba de rotación",\n                        (\n                            "✅ La rotación puede abrir el catálogo y "\n                            "localizar el candidato más antiguo.\\n\\n"\n                            f"{candidato.get(\'titulo\', \'\')}\\n"\n                            f"{candidato.get(\'horas\', 0)} horas\\n\\n"\n                            "NO se ha borrado nada."\n                        )\n                    )\n                else:\n                    messagebox.showwarning(\n                        "Prueba de rotación",\n                        (\n                            "Pude abrir el catálogo, pero no encontré "\n                            "el título registrado localmente:\\n\\n"\n                            f"{candidato.get(\'titulo\', \'\')}\\n\\n"\n                            "Es probable que el anuncio ya no exista, "\n                            "el título haya cambiado o Wallapop no lo "\n                            "haya renderizado en la lista."\n                        )\n                    )\n\n            finally:\n                try:\n                    pagina.close()\n                except Exception:\n                    pass\n\n    except Exception as e:\n        messagebox.showerror(\n            "Prueba de rotación",\n            str(e)\n        )\n\n\ndef _buscar_titulo_catalogo(\n    page,\n    titulo\n):\n    """\n    Busca título exacto, parcial y normalizado, haciendo scroll.\n    """\n    objetivo = str(\n        titulo or ""\n    ).strip()\n\n    if not objetivo:\n        return None\n\n    fragmento = objetivo[:60]\n\n    for _ in range(18):\n        candidatos = [\n            page.get_by_text(\n                objetivo,\n                exact=True\n            ),\n            page.get_by_text(\n                objetivo,\n                exact=False\n            ),\n            page.get_by_text(\n                fragmento,\n                exact=False\n            ),\n        ]\n\n        for loc in candidatos:\n            try:\n                total = min(\n                    loc.count(),\n                    8\n                )\n\n                for i in range(\n                    total\n                ):\n                    actual = loc.nth(\n                        i\n                    )\n\n                    if actual.is_visible():\n                        texto_actual = (\n                            actual.inner_text(\n                                timeout=800\n                            )\n                            or ""\n                        )\n\n                        if (\n                            _normalizar_texto_catalogo(\n                                objetivo\n                            )\n                            in _normalizar_texto_catalogo(\n                                texto_actual\n                            )\n                            or _normalizar_texto_catalogo(\n                                texto_actual\n                            )\n                            in _normalizar_texto_catalogo(\n                                objetivo\n                            )\n                        ):\n                            return actual\n            except Exception:\n                pass\n\n        try:\n            page.mouse.wheel(\n                0,\n                1050\n            )\n            page.wait_for_timeout(\n                350\n            )\n        except Exception:\n            pass\n\n    return None\n\n\ndef _contenedor_seguro_catalogo(\n    titulo_locator,\n    titulo,\n    precio=""\n):\n    """\n    Sube por el DOM hasta encontrar una fila/tarjeta pequeña que contenga\n    el título. Si hay precio, intenta validar también el precio.\n    """\n    actual = titulo_locator\n\n    for _ in range(10):\n        try:\n            actual = actual.locator(\n                "xpath=.."\n            )\n\n            contenido = (\n                actual.inner_text(\n                    timeout=1000\n                )\n                or ""\n            )\n\n            if titulo.lower() not in contenido.lower():\n                continue\n\n            if len(\n                contenido\n            ) > 6000:\n                continue\n\n            precio_limpio = (\n                str(precio)\n                .replace("€", "")\n                .strip()\n            )\n\n            if (\n                precio_limpio\n                and precio_limpio not in contenido\n            ):\n                # No descartamos aún: Wallapop puede formatear decimales.\n                pass\n\n            botones = actual.locator(\n                "button"\n            ).count()\n\n            checks = actual.locator(\n                \'input[type="checkbox"]\'\n            ).count()\n\n            if botones or checks:\n                return actual\n\n        except Exception:\n            pass\n\n    return None\n\n\ndef _confirmar_borrado_wallapop(\n    page\n):\n    patrones = re.compile(\n        r"^(eliminar|borrar|sí, eliminar|si, eliminar|confirmar)$",\n        re.I\n    )\n\n    for _ in range(4):\n        try:\n            botones = page.get_by_role(\n                "button",\n                name=patrones\n            )\n\n            for i in range(\n                botones.count()\n            ):\n                b = botones.nth(\n                    i\n                )\n\n                if b.is_visible():\n                    b.click(\n                        timeout=3000\n                    )\n\n                    page.wait_for_timeout(\n                        800\n                    )\n\n                    return True\n        except Exception:\n            pass\n\n        try:\n            page.wait_for_timeout(\n                400\n            )\n        except Exception:\n            pass\n\n    return False\n\n\ndef _eliminar_candidato_en_catalogo(\n    page,\n    candidato\n):\n    titulo = candidato.get(\n        "titulo",\n        ""\n    )\n\n    precio = candidato.get(\n        "precio",\n        ""\n    )\n\n    if not titulo:\n        return False, "El candidato no tiene título."\n\n    titulo_loc = _buscar_titulo_catalogo(\n        page,\n        titulo\n    )\n\n    if titulo_loc is None:\n        return (\n            False,\n            f"No encontré en el catálogo: {titulo}"\n        )\n\n    contenedor = _contenedor_seguro_catalogo(\n        titulo_loc,\n        titulo,\n        precio\n    )\n\n    if contenedor is None:\n        return (\n            False,\n            (\n                "Encontré el título, pero no pude aislar de forma "\n                "segura su tarjeta/fila."\n            )\n        )\n\n    # 1) Botón directo Eliminar/Borrar dentro del anuncio.\n    try:\n        directos = contenedor.get_by_role(\n            "button",\n            name=re.compile(\n                r"(eliminar|borrar)",\n                re.I\n            )\n        )\n\n        for i in range(\n            directos.count()\n        ):\n            b = directos.nth(\n                i\n            )\n\n            if b.is_visible():\n                b.click(\n                    timeout=3000\n                )\n\n                page.wait_for_timeout(\n                    600\n                )\n\n                _confirmar_borrado_wallapop(\n                    page\n                )\n\n                page.wait_for_timeout(\n                    1300\n                )\n\n                return True, (\n                    f"Eliminado: {titulo}"\n                )\n    except Exception:\n        pass\n\n    # 2) Menú de acciones.\n    try:\n        menus = contenedor.get_by_role(\n            "button",\n            name=re.compile(\n                r"(más|mas|opciones|acciones|menú|menu)",\n                re.I\n            )\n        )\n\n        for i in range(\n            menus.count()\n        ):\n            b = menus.nth(\n                i\n            )\n\n            if not b.is_visible():\n                continue\n\n            b.click(\n                timeout=3000\n            )\n\n            page.wait_for_timeout(\n                500\n            )\n\n            acciones = [\n                page.get_by_role(\n                    "menuitem",\n                    name=re.compile(\n                        r"(eliminar|borrar)",\n                        re.I\n                    )\n                ),\n                page.get_by_text(\n                    re.compile(\n                        r"^(eliminar|borrar)( producto| anuncio)?$",\n                        re.I\n                    )\n                ),\n            ]\n\n            for accion in acciones:\n                try:\n                    if (\n                        accion.count()\n                        and accion.first.is_visible()\n                    ):\n                        accion.first.click(\n                            timeout=3000\n                        )\n\n                        page.wait_for_timeout(\n                            500\n                        )\n\n                        _confirmar_borrado_wallapop(\n                            page\n                        )\n\n                        page.wait_for_timeout(\n                            1300\n                        )\n\n                        return True, (\n                            f"Eliminado: {titulo}"\n                        )\n                except Exception:\n                    pass\n\n    except Exception:\n        pass\n\n    # 3) Fallback usado por el catálogo: seleccionar checkbox y Borrar.\n    try:\n        checks = contenedor.locator(\n            \'input[type="checkbox"]\'\n        )\n\n        if (\n            checks.count()\n            and checks.first.is_visible()\n        ):\n            checks.first.check(\n                timeout=3000\n            )\n\n            page.wait_for_timeout(\n                400\n            )\n\n            borrar_global = page.get_by_role(\n                "button",\n                name=re.compile(\n                    r"^(borrar|eliminar)$",\n                    re.I\n                )\n            )\n\n            if not borrar_global.count():\n                borrar_global = page.get_by_text(\n                    re.compile(\n                        r"^(borrar|eliminar)$",\n                        re.I\n                    )\n                )\n\n            if (\n                borrar_global.count()\n                and borrar_global.first.is_visible()\n            ):\n                borrar_global.first.click(\n                    timeout=3000\n                )\n\n                page.wait_for_timeout(\n                    500\n                )\n\n                _confirmar_borrado_wallapop(\n                    page\n                )\n\n                page.wait_for_timeout(\n                    1300\n                )\n\n                return True, (\n                    f"Eliminado: {titulo}"\n                )\n\n    except Exception:\n        pass\n\n    return (\n        False,\n        (\n            "Encontré el anuncio, pero Wallapop no mostró "\n            "una acción de borrado reconocible."\n        )\n    )\n\n\ndef liberar_un_hueco_catalogo_wallapop():\n    """\n    Elimina COMO MÁXIMO un anuncio antiguo de DIAGPROG5.\n    Nunca elimina anuncios protegidos.\n    """\n    global rotacion_borrados_sesion\n\n    cfg = cargar_config_rotacion()\n\n    if not cfg.get(\n        "activo",\n        True\n    ):\n        return (\n            False,\n            "La rotación automática está desactivada."\n        )\n\n    max_sesion = max(\n        1,\n        int(\n            cfg.get(\n                "max_borrados_sesion",\n                5\n            )\n        )\n    )\n\n    if rotacion_borrados_sesion >= max_sesion:\n        return (\n            False,\n            (\n                "Se alcanzó el máximo de eliminaciones automáticas "\n                f"de esta sesión ({max_sesion})."\n            )\n        )\n\n    candidatos = _candidatos_rotacion_catalogo()\n\n    if not candidatos:\n        return (\n            False,\n            (\n                "No hay candidatos seguros registrados por DIAGPROG5. "\n                "V90 ya migró automáticamente los publicados.json antiguos. "\n                "Si sigue en 0, esos anuncios fueron publicados antes de que "\n                "DIAGPROG5 los registrara y no se borrarán automáticamente."\n            )\n        )\n\n    if sync_playwright is None:\n        return (\n            False,\n            "Playwright no está disponible."\n        )\n\n    _asegurar_chromium_controlable()\n\n    pagina = None\n\n    with sync_playwright() as p:\n        navegador = _conectar_playwright_cdp(\n            p\n        )\n\n        if not navegador.contexts:\n            return (\n                False,\n                "No encuentro el contexto de Chromium."\n            )\n\n        contexto = navegador.contexts[\n            0\n        ]\n\n        pagina = contexto.new_page()\n\n        try:\n            pagina.goto(\n                "https://es.wallapop.com/app/catalog/published",\n                wait_until="domcontentloaded",\n                timeout=45000\n            )\n\n            pagina.wait_for_timeout(\n                1800\n            )\n\n            if "accounts.wallapop.com" in (\n                pagina.url\n                or ""\n            ):\n                return (\n                    False,\n                    "Wallapop pide iniciar sesión."\n                )\n\n            # Intentamos varios candidatos antiguos por si uno ya no\n            # está visible en el catálogo o cambió.\n            for candidato in candidatos[\n                :8\n            ]:\n                _registrar_evento_rotacion(\n                    "CANDIDATO",\n                    candidato,\n                    (\n                        f"{candidato.get(\'horas\', 0)} horas de antigüedad"\n                    )\n                )\n\n                backup = (\n                    _guardar_backup_candidato_rotacion(\n                        candidato\n                    )\n                )\n\n                ok, detalle = (\n                    _eliminar_candidato_en_catalogo(\n                        pagina,\n                        candidato\n                    )\n                )\n\n                if not ok:\n                    _registrar_evento_rotacion(\n                        "NO_ELIMINADO",\n                        candidato,\n                        detalle\n                    )\n\n                    continue\n\n                rotacion_borrados_sesion += 1\n\n                _marcar_publicado_eliminado_rotacion(\n                    candidato\n                )\n\n                _registrar_evento_rotacion(\n                    "ELIMINADO",\n                    candidato,\n                    (\n                        detalle\n                        + (\n                            f" · Backup: {backup}"\n                            if backup\n                            else ""\n                        )\n                    )\n                )\n\n                return (\n                    True,\n                    (\n                        f"{candidato.get(\'titulo\', \'Anuncio\')} "\n                        f"({candidato.get(\'horas\', 0)} horas)"\n                    )\n                )\n\n            return (\n                False,\n                (\n                    "Había candidatos, pero no pude eliminar ninguno "\n                    "de forma segura en la interfaz actual de Wallapop."\n                )\n            )\n\n        finally:\n            try:\n                if (\n                    pagina is not None\n                    and not pagina.is_closed()\n                ):\n                    pagina.close()\n            except Exception:\n                pass\n\n\ndef gestionar_rotacion_limite_wallapop(\n    item\n):\n    """\n    Flujo llamado automáticamente SOLO cuando Wallapop devuelve\n    un error de límite de catálogo.\n    """\n    cfg = cargar_config_rotacion()\n\n    if not cfg.get(\n        "activo",\n        True\n    ):\n        return (\n            False,\n            "Rotación automática desactivada."\n        )\n\n    try:\n        ventana.after(\n            0,\n            lambda: estado.configure(\n                text=(\n                    "♻️ Límite de catálogo detectado. "\n                    "Buscando un anuncio antiguo para liberar un hueco..."\n                )\n            )\n        )\n    except Exception:\n        pass\n\n    ok, detalle = (\n        liberar_un_hueco_catalogo_wallapop()\n    )\n\n    if not ok:\n        _registrar_evento_rotacion(\n            "FALLO_ROTACION",\n            None,\n            detalle\n        )\n\n        return (\n            False,\n            detalle\n        )\n\n    item[\n        "ultima_rotacion_catalogo"\n    ] = time.strftime(\n        "%d/%m/%Y %H:%M:%S"\n    )\n\n    item[\n        "detalle_rotacion_catalogo"\n    ] = detalle\n\n    guardar_cola_en_disco()\n\n    try:\n        ventana.after(\n            0,\n            lambda d=detalle: estado.configure(\n                text=(\n                    "✅ Hueco liberado automáticamente · "\n                    f"{d}. Reintentando el anuncio nuevo..."\n                )\n            )\n        )\n    except Exception:\n        pass\n\n    return (\n        True,\n        detalle\n    )\n\n\ndef iniciar_cola_publicacion():\n    global cola_en_ejecucion, cola_cancelar, cola_pausada\n    global cola_inicio_sesion, cola_publicados_sesion, cola_errores_sesion\n\n    print(\n        "[COLA] Inicio solicitado · "\n        f"Plan={plan_actual_nombre()} · "\n        f"Total={len(cola_anuncios)}"\n    )\n\n    if cola_en_ejecucion:\n        estado.configure(text="La cola ya está en ejecución.")\n        return\n\n    # Prevalidación automática antes de iniciar.\n    hubo_cambios = False\n    for item in cola_anuncios:\n        if item.get("estado_cola") != "PENDIENTE":\n            continue\n\n        errores_item = validar_item_cola(item)\n        if errores_item:\n            marcar_error_prevalidacion(item, errores_item)\n            hubo_cambios = True\n\n    if hubo_cambios:\n        guardar_cola_en_disco()\n        refrescar_cola()\n\n    pendientes = [\n        x for x in cola_anuncios\n        if x.get("estado_cola") == "PENDIENTE"\n    ]\n\n    if not pendientes:\n        estado.configure(\n            text="❌ No hay anuncios pendientes en la cola."\n        )\n        return\n\n    # Validación básica de navegador antes de iniciar.\n    if sync_playwright is None:\n        estado.configure(\n            text="❌ Playwright no está disponible."\n        )\n        return\n\n    # Auditoría completa antes de iniciar.\n    # IMPORTANTE: BÁSICA no incluye auditoría premium, pero eso NO debe\n    # impedir publicar. En ese plan simplemente se salta esta fase.\n    if (\n        _config_auditoria()["activa"]\n        and tiene_funcion_plan("auditoria")\n    ):\n        if not ejecutar_auditoria_cola(\n            reparar=None,\n            mostrar_mensaje=False\n        ):\n            estado.configure(\n                text=(\n                    "⛔ Auditoría: hay errores críticos. "\n                    "La cola no se iniciará hasta corregirlos."\n                )\n            )\n            return\n\n    # Snapshot + diagnóstico preventivo antes de iniciar.\n    if not preflight_automatico_antes_de_cola():\n        return\n\n    # Precalentamiento previo: detecta fallos antes de empezar el lote.\n    try:\n        precalentar_wallapop_para_cola()\n    except Exception as e:\n        estado.configure(\n            text="❌ No se pudo preparar la cola: " + str(e)\n        )\n\n        try:\n            messagebox.showerror(\n                "Precalentamiento",\n                str(e)\n            )\n        except Exception:\n            pass\n\n        return\n\n    cola_en_ejecucion = True\n    cola_cancelar = False\n    cola_pausada = False\n\n    try:\n        estado.configure(\n            text=(\n                f"▶ Cola iniciada · {len(pendientes)} pendiente(s) · "\n                f"Plan {plan_actual().get(\'nombre\', plan_actual_nombre())}"\n            )\n        )\n    except Exception:\n        pass\n\n    print(\n        "Modo cola V86 activo: cada anuncio utilizará una pestaña nueva "\n        "y la cerrará al finalizar."\n    )\n    cola_inicio_sesion = time.time()\n    cola_publicados_sesion = 0\n    cola_errores_sesion = 0\n\n    boton_iniciar_cola.configure(state="disabled")\n    boton_cancelar_cola.configure(state="normal")\n\n    try:\n        boton_pausar_cola.configure(state="normal")\n        boton_reanudar_cola.configure(state="disabled")\n    except Exception:\n        pass\n\n    hilo = threading.Thread(\n        target=_procesar_cola_hilo,\n        daemon=True\n    )\n    hilo.start()\n\n\ndef _es_error_duplicado(texto):\n    """\n    Detecta mensajes típicos de anuncio duplicado.\n    """\n    t = str(texto or "").lower()\n\n    patrones = [\n        "duplicado",\n        "duplicate",\n        "ya fue publicado",\n        "ya publicado",\n        "ya existe",\n        "anuncio similar",\n        "producto similar",\n        "se ha bloqueado para evitar publicarlo dos veces",\n    ]\n\n    return any(p in t for p in patrones)\n\n\ndef _titulo_base_sin_variante(titulo):\n    """\n    Quita sufijos que el propio bot haya añadido anteriormente.\n    """\n    titulo = str(titulo or "").strip()\n\n    if " · " in titulo:\n        titulo = titulo.split(" · ", 1)[0].strip()\n\n    return titulo or "Servicio de automoción"\n\n\ndef _crear_titulo_reemplazo_unico(base, numero):\n    variantes = [\n        "Servicio premium",\n        "Especialista auto",\n        "Taller profesional",\n        "Diagnosis profesional",\n        "Servicio técnico",\n        "Atención especializada",\n        "Revisión profesional",\n        "Automoción profesional",\n        "Servicio integral",\n        "Solución especializada",\n        "Diagnosis avanzada",\n        "Taller especializado",\n        "Servicio experto",\n        "Atención profesional",\n        "Mecánica profesional",\n        "Servicio automoción",\n        "Revisión especializada",\n        "Asistencia técnica",\n        "Servicio completo",\n        "Especialista taller",\n    ]\n\n    sufijo = variantes[\n        (numero - 1) % len(variantes)\n    ]\n\n    espacio = max(8, 47 - len(sufijo))\n    base_corta = str(base)[:espacio].rstrip(" ·-")\n    candidato = f"{base_corta} · {sufijo}"\n\n    return candidato[:50]\n\n\ndef _crear_descripcion_reemplazo(titulo, descripcion_anterior, numero):\n    """\n    Cambia ligeramente el texto manteniendo el mismo servicio.\n    """\n    cierres = [\n        "Atención personalizada según vehículo y avería.",\n        "Consulta disponibilidad indicando los datos del vehículo.",\n        "Trabajo adaptado al modelo y motorización.",\n        "Valoración previa según vehículo y síntomas.",\n        "Servicio sujeto a revisión previa del vehículo.",\n        "Consulta condiciones según marca, modelo y motorización.",\n    ]\n\n    base = str(descripcion_anterior or "").strip()\n\n    # Si no había descripción útil, la generamos desde el título.\n    if not base:\n        try:\n            base = crear_descripcion(\n                titulo,\n                "",\n                False\n            )\n        except Exception:\n            base = (\n                f"{titulo}\\n\\n"\n                "Servicio profesional de automoción.\\n"\n                "Consulta indicando marca, modelo, año y motorización."\n            )\n\n    cierre = cierres[\n        (numero - 1) % len(cierres)\n    ]\n\n    # Evitar que el texto crezca indefinidamente.\n    if cierre.lower() not in base.lower():\n        base = base.rstrip() + "\\n\\n" + cierre\n\n    return limitar_descripcion(base)\n\n\ndef reemplazar_duplicado_por_anuncio_nuevo(item, max_intentos=25):\n    """\n    Sustituye EN EL MISMO hueco de la cola un anuncio duplicado por otro\n    con título, descripción, carpeta e imagen nuevas.\n\n    Conserva precio, categoría, estado y configuración de envío.\n    """\n    base_titulo = _titulo_base_sin_variante(\n        item.get("titulo", "")\n    )\n    precio = str(item.get("precio", "")).strip()\n    descripcion_anterior = str(\n        item.get("descripcion", "")\n    ).strip()\n\n    for intento in range(1, max_intentos + 1):\n        titulo_nuevo = _crear_titulo_reemplazo_unico(\n            base_titulo,\n            intento + random.randint(0, 100)\n        )\n\n        descripcion_nueva = _crear_descripcion_reemplazo(\n            titulo_nuevo,\n            descripcion_anterior,\n            intento\n        )\n\n        # Debe ser diferente a todo lo ya publicado.\n        if anuncio_ya_publicado(\n            titulo_nuevo,\n            descripcion_nueva,\n            precio\n        ):\n            continue\n\n        # Y también diferente a los demás elementos de la cola.\n        repetido_en_cola = any(\n            otro is not item\n            and crear_id_anuncio(\n                otro.get("titulo", ""),\n                otro.get("descripcion", ""),\n                otro.get("precio", "")\n            )\n            == crear_id_anuncio(\n                titulo_nuevo,\n                descripcion_nueva,\n                precio\n            )\n            for otro in cola_anuncios\n        )\n\n        if repetido_en_cola:\n            continue\n\n        identificador = (\n            f"{int(time.time() * 1000)}_"\n            f"{random.randint(10000, 99999)}"\n        )\n\n        carpeta = os.path.abspath(\n            os.path.join(\n                CARPETA_ANUNCIOS,\n                f"anuncio_{identificador}"\n            )\n        )\n        os.makedirs(\n            carpeta,\n            exist_ok=True\n        )\n\n        try:\n            beneficios = obtener_beneficios(\n                titulo_nuevo\n            )\n\n            imagen = crear_cartel(\n                titulo_nuevo,\n                beneficios,\n                precio,\n                0,\n                "",\n                False\n            )\n\n            ruta_imagen = os.path.join(\n                carpeta,\n                "imagen_1.png"\n            )\n            imagen.save(\n                ruta_imagen,\n                quality=95\n            )\n\n            with open(\n                os.path.join(\n                    carpeta,\n                    "anuncio.txt"\n                ),\n                "w",\n                encoding="utf-8"\n            ) as archivo:\n                archivo.write(\n                    "TÍTULO:\\n"\n                    + titulo_nuevo\n                    + "\\n\\n"\n                )\n                archivo.write(\n                    "PRECIO:\\n"\n                    + (\n                        precio + " €"\n                        if precio\n                        else "Consultar"\n                    )\n                    + "\\n\\n"\n                )\n                archivo.write(\n                    "TIPO DE VEHÍCULO:\\n"\n                    "Cualquier tipo de vehículo\\n\\n"\n                )\n                archivo.write(\n                    "CONTACTO:\\n"\n                    "No indicado\\n\\n"\n                )\n                archivo.write(\n                    "DESCRIPCIÓN:\\n"\n                    + descripcion_nueva\n                )\n\n            guardar_estado_anuncio(\n                carpeta,\n                "GENERADO",\n                titulo_nuevo,\n                precio\n            )\n\n            incrementar_estadistica(\n                "generados"\n            )\n\n            titulo_anterior = item.get(\n                "titulo",\n                "Sin título"\n            )\n\n            item["titulo_original_duplicado"] = (\n                item.get(\n                    "titulo_original_duplicado",\n                    titulo_anterior\n                )\n            )\n            item["reemplazos_duplicado"] = int(\n                item.get(\n                    "reemplazos_duplicado",\n                    0\n                )\n            ) + 1\n\n            item["titulo"] = titulo_nuevo\n            item["descripcion"] = descripcion_nueva\n            item["imagenes"] = [ruta_imagen]\n            item["carpeta"] = carpeta\n            item["estado_cola"] = "PENDIENTE"\n            item["error_fase"] = ""\n            item["error_motivo"] = ""\n            item["error_fecha"] = ""\n            item["error_diagnostico"] = ""\n            item["intentos"] = 0\n\n            guardar_cola_en_disco()\n\n            print(\n                "Duplicado sustituido automáticamente:",\n                repr(titulo_anterior),\n                "->",\n                repr(titulo_nuevo)\n            )\n\n            try:\n                ventana.after(\n                    0,\n                    lambda a=titulo_anterior, n=titulo_nuevo: estado.configure(\n                        text=(\n                            "♻️ Duplicado sustituido automáticamente · "\n                            f"{a} → {n}"\n                        )\n                    )\n                )\n                ventana.after(\n                    0,\n                    refrescar_cola\n                )\n                ventana.after(\n                    0,\n                    refrescar_historial\n                )\n                ventana.after(\n                    0,\n                    refrescar_dashboard\n                )\n            except Exception:\n                pass\n\n            return True\n\n        except Exception as e:\n            print(\n                "Error creando sustituto de duplicado:",\n                e\n            )\n\n            try:\n                if os.path.isdir(carpeta):\n                    shutil.rmtree(\n                        carpeta,\n                        ignore_errors=True\n                    )\n            except Exception:\n                pass\n\n    return False\n\n\ndef publicar_item_con_recuperacion(item):\n    config = obtener_config_rendimiento()\n    max_rec = (\n        config["max_recuperaciones"]\n        if config["auto_recuperar"]\n        else 0\n    )\n\n    recuperacion = 0\n\n    while True:\n        ok = _publicar_wallapop_hilo(\n            item["titulo"],\n            item["descripcion"],\n            item["precio"],\n            item["imagenes"],\n            item["carpeta"],\n            item["categoria"],\n            item["estado"],\n            item["desactivar_envio"],\n            True,\n            True\n        )\n\n        if ok:\n            return True\n\n        fase = (\n            ultimo_error_cola.get("fase")\n            or "Publicación"\n        )\n        mensaje = (\n            ultimo_error_cola.get("mensaje")\n            or ""\n        )\n\n        if recuperacion >= max_rec:\n            return False\n\n        if not fase_segura_para_reintento(fase):\n            return False\n\n        if not _parece_error_navegador(\n            fase + " " + mensaje\n        ):\n            return False\n\n        recuperacion += 1\n\n        espera = config["espera_recuperacion"]\n\n        try:\n            ventana.after(\n                0,\n                lambda n=recuperacion, m=max_rec: estado.configure(\n                    text=f"🔧 Recuperando navegador · {n}/{m}"\n                )\n            )\n        except Exception:\n            pass\n\n        if espera:\n            time.sleep(espera)\n\n        try:\n            recuperar_navegador_para_cola()\n        except Exception as e:\n            print("La recuperación automática falló:", e)\n            return False\n\n\ndef detectar_bloqueo_o_verificacion_wallapop(page):\n    try:\n        url = (page.url or "").lower()\n    except Exception:\n        url = ""\n\n    try:\n        cuerpo = (\n            page.locator("body").inner_text(\n                timeout=2500\n            ) or ""\n        ).lower()\n    except Exception:\n        cuerpo = ""\n\n    señales = [\n        "captcha",\n        "verifica que eres humano",\n        "verificación de seguridad",\n        "verificacion de seguridad",\n        "security check",\n        "too many requests",\n        "demasiadas solicitudes",\n        "rate limit",\n        "inténtalo más tarde",\n        "intentalo mas tarde",\n        "actividad inusual",\n        "unusual activity",\n        "acceso temporalmente bloqueado",\n        "temporalmente bloqueado",\n    ]\n\n    encontrado = next(\n        (\n            s\n            for s in señales\n            if s in cuerpo or s in url\n        ),\n        None\n    )\n\n    if encontrado:\n        return True, encontrado\n\n    return False, ""\n\n\ndef marcar_proteccion_wallapop(detalle):\n    global proteccion_ultimo_evento\n    global proteccion_ultimo_detalle\n\n    proteccion_ultimo_evento = time.strftime(\n        "%d/%m/%Y %H:%M:%S"\n    )\n    proteccion_ultimo_detalle = str(detalle)\n\n    print(\n        "Protección Wallapop activada:",\n        detalle\n    )\n\n    try:\n        ventana.after(\n            0,\n            refrescar_dashboard\n        )\n    except Exception:\n        pass\n\n\ndef recuperar_sesion_interrumpida():\n    global sesion_interrumpida_detectada\n\n    if not os.path.isfile(ARCHIVO_CHECKPOINT_COLA):\n        return\n\n    try:\n        with open(\n            ARCHIVO_CHECKPOINT_COLA,\n            "r",\n            encoding="utf-8"\n        ) as f:\n            checkpoint = json.load(f)\n\n        if not isinstance(checkpoint, dict):\n            return\n\n        if not bool(checkpoint.get("cola_en_ejecucion", False)):\n            return\n\n        cambios = 0\n\n        for item in cola_anuncios:\n            if item.get("estado_cola") == "PUBLICANDO":\n                item["estado_cola"] = "REVISAR"\n                item["error_fase"] = "Recuperación de sesión"\n                item["error_motivo"] = (\n                    "El programa se cerró mientras este anuncio estaba en proceso. "\n                    "Se ha marcado REVISAR para evitar una publicación duplicada."\n                )\n                item["error_fecha"] = time.strftime(\n                    "%d/%m/%Y %H:%M:%S"\n                )\n                cambios += 1\n\n        if cambios:\n            sesion_interrumpida_detectada = True\n            guardar_cola_en_disco()\n\n            try:\n                messagebox.showwarning(\n                    "Sesión anterior interrumpida",\n                    (\n                        f"Se han marcado {cambios} anuncio(s) como REVISAR.\\n\\n"\n                        "No se reintentaron automáticamente para evitar duplicados."\n                    )\n                )\n            except Exception:\n                pass\n\n            refrescar_cola()\n\n    except Exception as e:\n        print(\n            "Aviso al recuperar sesión interrumpida:",\n            e\n        )\n\n\ndef limpiar_checkpoint_al_cerrar_correctamente():\n    try:\n        _guardar_checkpoint_cola(\n            None,\n            "Programa cerrado correctamente"\n        )\n\n        if os.path.isfile(ARCHIVO_CHECKPOINT_COLA):\n            with open(\n                ARCHIVO_CHECKPOINT_COLA,\n                "r",\n                encoding="utf-8"\n            ) as f:\n                datos = json.load(f)\n\n            datos["cola_en_ejecucion"] = False\n\n            with open(\n                ARCHIVO_CHECKPOINT_COLA,\n                "w",\n                encoding="utf-8"\n            ) as f:\n                json.dump(\n                    datos,\n                    f,\n                    ensure_ascii=False,\n                    indent=2\n                )\n    except Exception:\n        pass\n\n\nESTRATEGIAS_PUBLICACION = {\n    "Conservador": {\n        "pausa_min": 14,\n        "pausa_max": 22,\n        "max_recuperaciones": 3,\n        "max_errores_consecutivos": 2,\n        "espera_error": 30,\n        "supervisor": True,\n        "modo_rendimiento": True,\n        "descripcion": "Máxima estabilidad y pausas amplias."\n    },\n    "Normal": {\n        "pausa_min": 8,\n        "pausa_max": 15,\n        "max_recuperaciones": 2,\n        "max_errores_consecutivos": 3,\n        "espera_error": 20,\n        "supervisor": True,\n        "modo_rendimiento": True,\n        "descripcion": "Equilibrio entre velocidad y estabilidad."\n    },\n    "Rápido": {\n        "pausa_min": 5,\n        "pausa_max": 9,\n        "max_recuperaciones": 1,\n        "max_errores_consecutivos": 4,\n        "espera_error": 10,\n        "supervisor": True,\n        "modo_rendimiento": True,\n        "descripcion": "Menos pausa y más velocidad."\n    },\n    "Personalizado": {\n        "descripcion": "Usa exactamente los valores configurados manualmente."\n    },\n}\n\n\ndef estrategia_actual():\n    nombre = var_estrategia_publicacion.get().strip()\n\n    if nombre not in ESTRATEGIAS_PUBLICACION:\n        nombre = "Normal"\n\n    return nombre\n\n\ndef aplicar_estrategia_publicacion(nombre=None, guardar=True):\n    if not requerir_funcion_plan(\n        "estrategias",\n        "Estrategias de publicación"\n    ):\n        return\n\n    """\n    Aplica un perfil completo de funcionamiento.\n    Personalizado no sobrescribe los valores manuales.\n    """\n    if nombre is None:\n        nombre = estrategia_actual()\n\n    if nombre not in ESTRATEGIAS_PUBLICACION:\n        nombre = "Normal"\n\n    var_estrategia_publicacion.set(nombre)\n\n    if nombre == "Personalizado":\n        estado.configure(\n            text="⚙ Estrategia Personalizado activa."\n        )\n        if guardar:\n            try:\n                guardar_configuracion_general()\n            except Exception:\n                pass\n        return\n\n    cfg = ESTRATEGIAS_PUBLICACION[nombre]\n\n    var_pausa_min_publicaciones.set(\n        str(cfg["pausa_min"])\n    )\n    var_pausa_max_publicaciones.set(\n        str(cfg["pausa_max"])\n    )\n    var_max_recuperaciones_anuncio.set(\n        str(cfg["max_recuperaciones"])\n    )\n    var_max_errores_consecutivos.set(\n        str(cfg["max_errores_consecutivos"])\n    )\n    var_espera_despues_error.set(\n        str(cfg["espera_error"])\n    )\n    var_supervisor_automatico.set(\n        bool(cfg["supervisor"])\n    )\n    var_modo_rendimiento.set(\n        bool(cfg["modo_rendimiento"])\n    )\n\n    try:\n        etiqueta_estrategia_desc.configure(\n            text=cfg["descripcion"]\n        )\n    except Exception:\n        pass\n\n    if guardar:\n        try:\n            guardar_configuracion_general()\n        except Exception:\n            pass\n\n    try:\n        refrescar_dashboard()\n    except Exception:\n        pass\n\n    estado.configure(\n        text=f"✅ Estrategia aplicada: {nombre}"\n    )\n\n\ndef obtener_descripcion_estrategia():\n    nombre = estrategia_actual()\n    return ESTRATEGIAS_PUBLICACION.get(\n        nombre,\n        ESTRATEGIAS_PUBLICACION["Normal"]\n    ).get(\n        "descripcion",\n        ""\n    )\n\n\nPRIORIDAD_VALOR = {\n    "Alta": 0,\n    "Normal": 1,\n    "Baja": 2,\n}\n\n\ndef _config_ritmo_adaptativo():\n    datos = cargar_configuracion_general()\n\n    return {\n        "activo": bool(datos.get("ritmo_adaptativo", True)),\n        "exitos_para_acelerar": max(\n            1,\n            int(datos.get("exitos_para_acelerar", 3))\n        ),\n        "errores_para_frenar": max(\n            1,\n            int(datos.get("errores_para_frenar", 1))\n        ),\n        "max_ajuste": max(\n            0,\n            int(datos.get("max_ajuste_pausa", 6))\n        ),\n    }\n\n\ndef registrar_resultado_adaptativo(ok):\n    global ritmo_adaptativo_exitos\n    global ritmo_adaptativo_errores\n    global ritmo_adaptativo_ajuste\n\n    cfg = _config_ritmo_adaptativo()\n\n    if not cfg["activo"]:\n        ritmo_adaptativo_ajuste = 0\n        return\n\n    if ok:\n        ritmo_adaptativo_exitos += 1\n        ritmo_adaptativo_errores = 0\n\n        if ritmo_adaptativo_exitos >= cfg["exitos_para_acelerar"]:\n            ritmo_adaptativo_exitos = 0\n            ritmo_adaptativo_ajuste = max(\n                -cfg["max_ajuste"],\n                ritmo_adaptativo_ajuste - 1\n            )\n            print(\n                "Ritmo adaptativo: reduciendo pausa en 1s.",\n                "Ajuste:",\n                ritmo_adaptativo_ajuste\n            )\n    else:\n        ritmo_adaptativo_errores += 1\n        ritmo_adaptativo_exitos = 0\n\n        if ritmo_adaptativo_errores >= cfg["errores_para_frenar"]:\n            ritmo_adaptativo_errores = 0\n            ritmo_adaptativo_ajuste = min(\n                cfg["max_ajuste"],\n                ritmo_adaptativo_ajuste + 2\n            )\n            print(\n                "Ritmo adaptativo: aumentando pausa en 2s.",\n                "Ajuste:",\n                ritmo_adaptativo_ajuste\n            )\n\n    try:\n        ventana.after(0, refrescar_dashboard)\n    except Exception:\n        pass\n\n\ndef obtener_pausa_adaptativa_publicaciones():\n    global ultima_pausa_adaptativa\n\n    base = obtener_pausa_aleatoria_publicaciones()\n    cfg = _config_ritmo_adaptativo()\n\n    if cfg["activo"]:\n        pausa = max(\n            0,\n            base + ritmo_adaptativo_ajuste\n        )\n    else:\n        pausa = base\n\n    ultima_pausa_adaptativa = pausa\n    return pausa\n\n\ndef ordenar_pendientes_por_prioridad():\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes reordenar mientras la cola está publicando."\n        )\n        return\n\n    pendientes = [\n        x for x in cola_anuncios\n        if x.get("estado_cola") == "PENDIENTE"\n    ]\n\n    resto = [\n        x for x in cola_anuncios\n        if x.get("estado_cola") != "PENDIENTE"\n    ]\n\n    pendientes.sort(\n        key=lambda x: PRIORIDAD_VALOR.get(\n            x.get("prioridad", "Normal"),\n            1\n        )\n    )\n\n    cola_anuncios[:] = pendientes + resto\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n\n    estado.configure(\n        text="✅ Pendientes ordenados por prioridad."\n    )\n\n\ndef asignar_prioridad_seleccionado():\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes cambiar prioridad mientras la cola publica."\n        )\n        return\n\n    indice = _indice_seleccionado_cola()\n\n    if indice is None:\n        estado.configure(\n            text="Selecciona primero un anuncio."\n        )\n        return\n\n    prioridad = var_prioridad_seleccionada.get().strip()\n\n    if prioridad not in PRIORIDAD_VALOR:\n        prioridad = "Normal"\n\n    cola_anuncios[indice]["prioridad"] = prioridad\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n\n    estado.configure(\n        text=f"✅ Prioridad asignada: {prioridad}"\n    )\n\n\ndef siguiente_anuncio_pendiente():\n    pendientes = [\n        x for x in cola_anuncios\n        if x.get("estado_cola") == "PENDIENTE"\n    ]\n\n    if not pendientes:\n        return None\n\n    pendientes.sort(\n        key=lambda x: PRIORIDAD_VALOR.get(\n            x.get("prioridad", "Normal"),\n            1\n        )\n    )\n\n    return pendientes[0]\n\n\ndef ejecutar_diagnostico_prevuelo():\n    """\n    Revisa la instalación y el estado básico antes de iniciar un lote.\n    No publica nada.\n    """\n    global ultimo_diagnostico_prevuelo\n    global ultimo_diagnostico_prevuelo_detalle\n    global ultima_hora_prevuelo\n\n    pruebas = []\n    correcto = True\n\n    # Archivos/carpetas escribibles.\n    # Se prueba la carpeta segura de DIAGPROG5, no la carpeta desde la que\n    # se ejecutó el .py/.exe (que puede estar protegida por Windows).\n    try:\n        carpeta_test = Path(\n            globals().get(\n                "CARPETA_DATOS_USUARIO",\n                str(Path.home() / "DIAGPROG5")\n            )\n        )\n\n        carpeta_test.mkdir(\n            parents=True,\n            exist_ok=True\n        )\n\n        prueba = carpeta_test / "._wallapopbot_write_test.tmp"\n        prueba.write_text(\n            "ok",\n            encoding="utf-8"\n        )\n        prueba.unlink(\n            missing_ok=True\n        )\n\n        pruebas.append(\n            f"✓ Carpeta de trabajo escribible · {carpeta_test}"\n        )\n    except Exception as e:\n        correcto = False\n        pruebas.append(\n            f"✗ Escritura en carpeta de trabajo: {e}"\n        )\n\n    # Playwright\n    if sync_playwright is None:\n        correcto = False\n        pruebas.append("✗ Playwright no disponible")\n    else:\n        pruebas.append("✓ Playwright disponible")\n\n    # Chromium/CDP\n    try:\n        if _puerto_chrome_activo():\n            pruebas.append("✓ Chromium/CDP responde")\n        else:\n            pruebas.append("⚠ Chromium/CDP no está activo todavía")\n    except Exception as e:\n        pruebas.append(f"⚠ Chromium/CDP: {e}")\n\n    # Configuración\n    try:\n        cfg = cargar_configuracion_general()\n        pruebas.append(\n            "✓ Configuración cargada · "\n            f"estrategia {cfg.get(\'estrategia_publicacion\', \'Normal\')}"\n        )\n    except Exception as e:\n        correcto = False\n        pruebas.append(f"✗ Configuración: {e}")\n\n    # Cola\n    try:\n        pendientes = sum(\n            1 for x in cola_anuncios\n            if x.get("estado_cola") == "PENDIENTE"\n        )\n        revisar = sum(\n            1 for x in cola_anuncios\n            if x.get("estado_cola") == "REVISAR"\n        )\n        pruebas.append(\n            f"✓ Cola · {pendientes} pendiente(s), {revisar} por revisar"\n        )\n    except Exception as e:\n        correcto = False\n        pruebas.append(f"✗ Cola: {e}")\n\n    ultimo_diagnostico_prevuelo = "OK" if correcto else "REVISAR"\n    ultimo_diagnostico_prevuelo_detalle = "\\n".join(pruebas)\n    ultima_hora_prevuelo = time.strftime("%H:%M:%S")\n\n    try:\n        refrescar_dashboard()\n    except Exception:\n        pass\n\n    try:\n        messagebox.showinfo(\n            "Diagnóstico preventivo",\n            (\n                f"Resultado: {ultimo_diagnostico_prevuelo}\\n\\n"\n                + ultimo_diagnostico_prevuelo_detalle\n            )\n        )\n    except Exception:\n        pass\n\n    return correcto\n\n\ndef abrir_carpeta_informes():\n    ruta = Path("informes_sesion")\n    ruta.mkdir(parents=True, exist_ok=True)\n\n    try:\n        os.startfile(str(ruta.resolve()))\n    except Exception as e:\n        estado.configure(\n            text=f"No pude abrir informes_sesion: {e}"\n        )\n\n\ndef _asegurar_carpeta_snapshots():\n    Path(CARPETA_SNAPSHOTS).mkdir(\n        parents=True,\n        exist_ok=True\n    )\n\n\ndef crear_snapshot_operativo(motivo="manual"):\n    if not requerir_funcion_plan(\n        "snapshots",\n        "Snapshots"\n    ):\n        return ""\n    global ultimo_snapshot_creado\n\n    try:\n        _asegurar_carpeta_snapshots()\n\n        sello = time.strftime("%Y%m%d_%H%M%S")\n        ruta = Path(CARPETA_SNAPSHOTS) / f"snapshot_{sello}.json"\n\n        datos = {\n            "fecha": time.strftime("%d/%m/%Y %H:%M:%S"),\n            "motivo": motivo,\n            "cola": cola_anuncios,\n            "configuracion": cargar_configuracion_general(),\n        }\n\n        ruta.write_text(\n            json.dumps(\n                datos,\n                ensure_ascii=False,\n                indent=2\n            ),\n            encoding="utf-8"\n        )\n\n        ultimo_snapshot_creado = str(ruta.resolve())\n\n        snapshots = sorted(\n            Path(CARPETA_SNAPSHOTS).glob("snapshot_*.json"),\n            key=lambda p: p.stat().st_mtime,\n            reverse=True\n        )\n\n        for antiguo in snapshots[30:]:\n            try:\n                antiguo.unlink()\n            except Exception:\n                pass\n\n        return str(ruta.resolve())\n\n    except Exception as e:\n        print("No pude crear snapshot:", e)\n        return ""\n\n\ndef _ultimo_snapshot():\n    _asegurar_carpeta_snapshots()\n\n    snapshots = sorted(\n        Path(CARPETA_SNAPSHOTS).glob("snapshot_*.json"),\n        key=lambda p: p.stat().st_mtime,\n        reverse=True\n    )\n\n    return snapshots[0] if snapshots else None\n\n\ndef restaurar_ultimo_snapshot():\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes restaurar mientras la cola está activa."\n        )\n        return\n\n    ruta = _ultimo_snapshot()\n\n    if not ruta:\n        estado.configure(\n            text="No hay snapshots disponibles."\n        )\n        return\n\n    try:\n        datos = json.loads(\n            ruta.read_text(\n                encoding="utf-8"\n            )\n        )\n\n        cola = datos.get("cola", [])\n        config = datos.get("configuracion", {})\n\n        if not isinstance(cola, list):\n            raise RuntimeError(\n                "El snapshot no contiene una cola válida."\n            )\n\n        if not messagebox.askyesno(\n            "Restaurar snapshot",\n            (\n                f"Se restaurarán {len(cola)} anuncio(s) y la configuración.\\n\\n"\n                f"Snapshot: {ruta.name}\\n\\n"\n                "¿Continuar?"\n            )\n        ):\n            return\n\n        cola_anuncios[:] = cola\n        guardar_cola_en_disco()\n\n        if isinstance(config, dict):\n            with open(\n                ARCHIVO_CONFIG,\n                "w",\n                encoding="utf-8"\n            ) as f:\n                json.dump(\n                    config,\n                    f,\n                    ensure_ascii=False,\n                    indent=2\n                )\n\n            cargar_configuracion_en_interfaz()\n            aplicar_configuracion_general()\n\n        refrescar_cola()\n        refrescar_dashboard()\n\n        estado.configure(\n            text=f"✅ Snapshot restaurado: {ruta.name}"\n        )\n\n    except Exception as e:\n        estado.configure(\n            text="❌ No pude restaurar el snapshot: " + str(e)\n        )\n\n\ndef guardar_configuracion_como_buena():\n    try:\n        datos = cargar_configuracion_general()\n\n        with open(\n            ARCHIVO_CONFIG_BUENA,\n            "w",\n            encoding="utf-8"\n        ) as f:\n            json.dump(\n                datos,\n                f,\n                ensure_ascii=False,\n                indent=2\n            )\n\n        return True\n    except Exception:\n        return False\n\n\ndef restaurar_configuracion_buena():\n    if not os.path.isfile(\n        ARCHIVO_CONFIG_BUENA\n    ):\n        estado.configure(\n            text="No hay una configuración buena guardada todavía."\n        )\n        return\n\n    try:\n        shutil.copy2(\n            ARCHIVO_CONFIG_BUENA,\n            ARCHIVO_CONFIG\n        )\n\n        cargar_configuracion_en_interfaz()\n        aplicar_configuracion_general()\n        refrescar_dashboard()\n\n        estado.configure(\n            text="✅ Última configuración buena restaurada."\n        )\n\n    except Exception as e:\n        estado.configure(\n            text="❌ No pude restaurar configuración: " + str(e)\n        )\n\n\ndef _cargar_archivo_publicados():\n    if not os.path.isfile(\n        ARCHIVO_PUBLICADOS_ARCHIVO\n    ):\n        return []\n\n    try:\n        with open(\n            ARCHIVO_PUBLICADOS_ARCHIVO,\n            "r",\n            encoding="utf-8"\n        ) as f:\n            datos = json.load(f)\n\n        return datos if isinstance(datos, list) else []\n    except Exception:\n        return []\n\n\ndef archivar_publicado(item):\n    try:\n        archivo = _cargar_archivo_publicados()\n\n        archivo.append({\n            "fecha": time.strftime("%d/%m/%Y %H:%M:%S"),\n            "titulo": item.get("titulo", ""),\n            "precio": item.get("precio", ""),\n            "categoria": item.get("categoria", ""),\n            "carpeta": item.get("carpeta", ""),\n            "intentos": item.get("intentos", 0),\n            "reemplazos_duplicado": item.get(\n                "reemplazos_duplicado",\n                0\n            ),\n        })\n\n        archivo = archivo[-5000:]\n\n        with open(\n            ARCHIVO_PUBLICADOS_ARCHIVO,\n            "w",\n            encoding="utf-8"\n        ) as f:\n            json.dump(\n                archivo,\n                f,\n                ensure_ascii=False,\n                indent=2\n            )\n\n    except Exception as e:\n        print(\n            "No pude archivar publicado:",\n            e\n        )\n\n\ndef contar_publicados_archivados():\n    return len(\n        _cargar_archivo_publicados()\n    )\n\n\ndef contar_snapshots():\n    try:\n        _asegurar_carpeta_snapshots()\n        return len(\n            list(\n                Path(CARPETA_SNAPSHOTS).glob(\n                    "snapshot_*.json"\n                )\n            )\n        )\n    except Exception:\n        return 0\n\n\ndef preflight_automatico_antes_de_cola():\n    # Snapshot es una función premium. Si el plan no la incluye,\n    # el preflight sigue funcionando normalmente sin bloquear la cola.\n    ruta = ""\n\n    if tiene_funcion_plan("snapshots"):\n        try:\n            ruta = crear_snapshot_operativo(\n                "antes_de_iniciar_cola"\n            )\n        except Exception as e:\n            print(\n                "Aviso: no se pudo crear snapshot previo:",\n                e\n            )\n\n    correcto = ejecutar_diagnostico_prevuelo()\n\n    if correcto:\n        guardar_configuracion_como_buena()\n\n        print(\n            "Preflight OK. Snapshot:",\n            ruta\n        )\n        return True\n\n    estado.configure(\n        text=(\n            "⛔ Preflight: hay problemas que revisar. "\n            "La cola no se ha iniciado."\n        )\n    )\n\n    return False\n\n\ndef _config_auditoria():\n    datos = cargar_configuracion_general()\n\n    return {\n        "activa": bool(\n            datos.get(\n                "auditoria_antes_de_publicar",\n                True\n            )\n        ),\n        "autorreparar": bool(\n            datos.get(\n                "autorreparar_auditoria",\n                True\n            )\n        ),\n        "bloquear_criticos": bool(\n            datos.get(\n                "bloquear_si_hay_criticos",\n                True\n            )\n        ),\n        "calidad_minima": max(\n            0,\n            min(\n                100,\n                int(\n                    datos.get(\n                        "calidad_minima_publicar",\n                        70\n                    )\n                )\n            )\n        ),\n    }\n\n\ndef auditar_item_cola(item):\n    """\n    Devuelve (nivel, problemas, score)\n    nivel: OK / AVISO / CRITICO\n    """\n    problemas = []\n    criticos = []\n    avisos = []\n\n    # Validación estructural existente\n    try:\n        errores = validar_item_cola(item)\n    except Exception as e:\n        errores = [str(e)]\n\n    for err in errores:\n        t = str(err)\n        if any(\n            palabra in t.lower()\n            for palabra in (\n                "falta",\n                "no encuentro",\n                "inválido",\n                "invalido",\n                "dañada",\n                "vacía",\n                "vacia"\n            )\n        ):\n            criticos.append(t)\n        else:\n            avisos.append(t)\n\n    # Calidad\n    try:\n        score, problemas_calidad = calcular_calidad_item(\n            item\n        )\n    except Exception:\n        score = 0\n        problemas_calidad = [\n            "No pude calcular la calidad."\n        ]\n\n    config = _config_auditoria()\n\n    if score < config["calidad_minima"]:\n        avisos.append(\n            f"Calidad {score}/100 por debajo del mínimo "\n            f"{config[\'calidad_minima\']}/100."\n        )\n\n    # Duplicado\n    try:\n        if anuncio_ya_publicado(\n            item.get("titulo", ""),\n            item.get("descripcion", ""),\n            item.get("precio", "")\n        ):\n            criticos.append(\n                "El anuncio coincide con uno ya publicado."\n            )\n    except Exception:\n        pass\n\n    try:\n        similitud = int(\n            item.get(\n                "similaridad_maxima",\n                0\n            )\n        )\n\n        umbral_texto = int(\n            cargar_configuracion_general().get(\n                "umbral_similitud_textual",\n                88\n            )\n        )\n\n        if similitud >= umbral_texto:\n            avisos.append(\n                f"Anuncio muy parecido a otro de la cola ({similitud}%)."\n            )\n    except Exception:\n        pass\n\n    # Estado ambiguo\n    if item.get("estado_cola") == "REVISAR":\n        criticos.append(\n            "El anuncio está en estado REVISAR."\n        )\n\n    problemas.extend(criticos)\n    problemas.extend(avisos)\n\n    if criticos:\n        nivel = "CRITICO"\n    elif avisos:\n        nivel = "AVISO"\n    else:\n        nivel = "OK"\n\n    return nivel, problemas, score\n\n\ndef ejecutar_auditoria_cola(\n    reparar=None,\n    mostrar_mensaje=True\n):\n    if not tiene_funcion_plan(\n        "auditoria"\n    ):\n        # Llamada automática: saltar auditoría sin bloquear.\n        if not mostrar_mensaje:\n            return True\n\n        # Llamada manual desde el botón: informar de que no está incluida.\n        requerir_funcion_plan(\n            "auditoria",\n            "Auditoría de cola"\n        )\n        return False\n\n    global auditoria_ultimo_estado\n    global auditoria_preparacion\n    global auditoria_criticos\n    global auditoria_avisos\n\n    if cola_en_ejecucion:\n        if mostrar_mensaje:\n            estado.configure(\n                text="❌ No puedes auditar mientras la cola está publicando."\n            )\n        return False\n\n    pendientes = [\n        x for x in cola_anuncios\n        if x.get("estado_cola") in (\n            "PENDIENTE",\n            "ERROR",\n            "REVISAR"\n        )\n    ]\n\n    if not pendientes:\n        auditoria_ultimo_estado = "OK"\n        auditoria_preparacion = 100\n        auditoria_criticos = 0\n        auditoria_avisos = 0\n        return True\n\n    config = _config_auditoria()\n\n    try:\n        detectar_similitudes_cola(\n            marcar=True,\n            mostrar=False\n        )\n    except Exception:\n        pass\n\n    if reparar is None:\n        reparar = config["autorreparar"]\n\n    resultados = []\n\n    # Reparación segura previa\n    if reparar:\n        for item in pendientes:\n            if item.get("estado_cola") == "ERROR":\n                try:\n                    reparar_item_error_cola(\n                        item\n                    )\n                except Exception:\n                    pass\n\n            # Duplicados detectables se sustituyen\n            try:\n                if (\n                    item.get("estado_cola") == "PENDIENTE"\n                    and anuncio_ya_publicado(\n                        item.get("titulo", ""),\n                        item.get("descripcion", ""),\n                        item.get("precio", "")\n                    )\n                ):\n                    reemplazar_duplicado_por_anuncio_nuevo(\n                        item\n                    )\n            except Exception:\n                pass\n\n    total = 0\n    ok_count = 0\n    criticos = 0\n    avisos = 0\n\n    for item in pendientes:\n        nivel, problemas, score = auditar_item_cola(\n            item\n        )\n\n        item["auditoria_nivel"] = nivel\n        item["auditoria_problemas"] = problemas\n        item["calidad"] = score\n\n        total += 1\n\n        if nivel == "OK":\n            ok_count += 1\n        elif nivel == "AVISO":\n            avisos += 1\n        else:\n            criticos += 1\n\n        resultados.append({\n            "titulo": item.get("titulo", ""),\n            "estado": item.get("estado_cola", ""),\n            "nivel": nivel,\n            "calidad": score,\n            "problemas": problemas,\n        })\n\n    auditoria_criticos = criticos\n    auditoria_avisos = avisos\n    auditoria_preparacion = int(\n        (ok_count / max(1, total)) * 100\n    )\n\n    if criticos:\n        auditoria_ultimo_estado = "CRITICOS"\n    elif avisos:\n        auditoria_ultimo_estado = "AVISOS"\n    else:\n        auditoria_ultimo_estado = "OK"\n\n    try:\n        with open(\n            ARCHIVO_ULTIMA_AUDITORIA,\n            "w",\n            encoding="utf-8"\n        ) as f:\n            json.dump(\n                {\n                    "fecha": time.strftime(\n                        "%d/%m/%Y %H:%M:%S"\n                    ),\n                    "estado": auditoria_ultimo_estado,\n                    "preparacion": auditoria_preparacion,\n                    "criticos": criticos,\n                    "avisos": avisos,\n                    "resultados": resultados,\n                },\n                f,\n                ensure_ascii=False,\n                indent=2\n            )\n    except Exception:\n        pass\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n\n    try:\n        refrescar_dashboard()\n    except Exception:\n        pass\n\n    if mostrar_mensaje:\n        try:\n            messagebox.showinfo(\n                "Auditoría de cola",\n                (\n                    f"Estado: {auditoria_ultimo_estado}\\n"\n                    f"Preparación: {auditoria_preparacion}%\\n"\n                    f"Críticos: {criticos}\\n"\n                    f"Avisos: {avisos}\\n"\n                    f"OK: {ok_count}"\n                )\n            )\n        except Exception:\n            pass\n\n    if config["bloquear_criticos"] and criticos:\n        return False\n\n    return True\n\n\ndef exportar_informe_auditoria():\n    if not os.path.isfile(\n        ARCHIVO_ULTIMA_AUDITORIA\n    ):\n        estado.configure(\n            text="Primero ejecuta una auditoría."\n        )\n        return\n\n    try:\n        datos = json.loads(\n            Path(\n                ARCHIVO_ULTIMA_AUDITORIA\n            ).read_text(\n                encoding="utf-8"\n            )\n        )\n\n        Path("informes_sesion").mkdir(\n            parents=True,\n            exist_ok=True\n        )\n\n        sello = time.strftime("%Y%m%d_%H%M%S")\n\n        ruta = Path(\n            "informes_sesion"\n        ) / f"auditoria_{sello}.txt"\n\n        lineas = [\n            "AUDITORÍA DE COLA · WallapopBot",\n            "================================",\n            "",\n            f"Fecha: {datos.get(\'fecha\', \'\')}",\n            f"Estado: {datos.get(\'estado\', \'\')}",\n            f"Preparación: {datos.get(\'preparacion\', 0)}%",\n            f"Críticos: {datos.get(\'criticos\', 0)}",\n            f"Avisos: {datos.get(\'avisos\', 0)}",\n            "",\n        ]\n\n        for i, r in enumerate(\n            datos.get("resultados", []),\n            start=1\n        ):\n            lineas.append(\n                f"{i:02d}. [{r.get(\'nivel\', \'\')}] "\n                f"{r.get(\'titulo\', \'\')} · "\n                f"Q:{r.get(\'calidad\', 0)}/100"\n            )\n\n            for problema in r.get(\n                "problemas",\n                []\n            ):\n                lineas.append(\n                    f"    - {problema}"\n                )\n\n        ruta.write_text(\n            "\\n".join(lineas),\n            encoding="utf-8"\n        )\n\n        estado.configure(\n            text=f"✅ Informe de auditoría: {ruta}"\n        )\n\n    except Exception as e:\n        estado.configure(\n            text="❌ No pude exportar auditoría: " + str(e)\n        )\n\n\ndef _config_cuarentena():\n    datos = cargar_configuracion_general()\n\n    return {\n        "activa": bool(\n            datos.get(\n                "cuarentena_automatica",\n                True\n            )\n        ),\n        "umbral": max(\n            1,\n            int(\n                datos.get(\n                    "fallos_repetidos_para_cuarentena",\n                    2\n                )\n            )\n        ),\n        "continuar": bool(\n            datos.get(\n                "continuar_tras_cuarentena",\n                True\n            )\n        ),\n    }\n\n\ndef _normalizar_error_para_huella(fase, motivo):\n    texto = (\n        str(fase or "")\n        + " | "\n        + str(motivo or "")\n    ).lower()\n\n    texto = re.sub(r"\\d+", "#", texto)\n    texto = re.sub(r"\\s+", " ", texto).strip()\n\n    return texto[:500]\n\n\ndef registrar_fallo_item(item, fase, motivo):\n    huella = _normalizar_error_para_huella(\n        fase,\n        motivo\n    )\n\n    historial = item.get(\n        "historial_fallos",\n        []\n    )\n\n    if not isinstance(historial, list):\n        historial = []\n\n    historial.append({\n        "fecha": time.strftime("%d/%m/%Y %H:%M:%S"),\n        "fase": str(fase),\n        "motivo": str(motivo),\n        "huella": huella,\n    })\n\n    item["historial_fallos"] = historial[-10:]\n    item["ultima_huella_error"] = huella\n\n    repetidos = sum(\n        1\n        for x in item["historial_fallos"]\n        if x.get("huella") == huella\n    )\n\n    item["fallos_iguales"] = repetidos\n    return repetidos\n\n\ndef enviar_a_cuarentena(item, motivo):\n    global cuarentena_sesion\n    global ultimo_motivo_cuarentena\n\n    item["estado_cola"] = "CUARENTENA"\n    item["cuarentena_fecha"] = time.strftime(\n        "%d/%m/%Y %H:%M:%S"\n    )\n    item["cuarentena_motivo"] = str(motivo)\n\n    cuarentena_sesion += 1\n    ultimo_motivo_cuarentena = str(motivo)\n\n    guardar_cola_en_disco()\n\n    try:\n        ventana.after(0, refrescar_cola)\n        ventana.after(0, refrescar_dashboard)\n    except Exception:\n        pass\n\n\ndef evaluar_cuarentena_item(item):\n    if not tiene_funcion_plan("cuarentena"):\n        return False\n\n    cfg = _config_cuarentena()\n\n    if not cfg["activa"]:\n        return False\n\n    repetidos = int(\n        item.get(\n            "fallos_iguales",\n            0\n        )\n    )\n\n    if repetidos < cfg["umbral"]:\n        return False\n\n    enviar_a_cuarentena(\n        item,\n        (\n            f"El mismo error se repitió {repetidos} veces: "\n            f"{item.get(\'error_motivo\', \'sin detalle\')}"\n        )\n    )\n\n    return True\n\n\ndef restaurar_cuarentena_seleccionado():\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes restaurar cuarentena mientras la cola publica."\n        )\n        return\n\n    indice = _indice_seleccionado_cola()\n\n    if indice is None:\n        estado.configure(\n            text="Selecciona primero un anuncio."\n        )\n        return\n\n    item = cola_anuncios[indice]\n\n    if item.get("estado_cola") != "CUARENTENA":\n        estado.configure(\n            text="El anuncio seleccionado no está en CUARENTENA."\n        )\n        return\n\n    item["estado_cola"] = "PENDIENTE"\n    item["fallos_iguales"] = 0\n    item["historial_fallos"] = []\n    item["cuarentena_fecha"] = ""\n    item["cuarentena_motivo"] = ""\n    item["error_fase"] = ""\n    item["error_motivo"] = ""\n    item["error_fecha"] = ""\n    item["intentos"] = 0\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n\n    estado.configure(\n        text="✅ Anuncio restaurado desde cuarentena."\n    )\n\n\ndef restaurar_toda_cuarentena():\n    preparar_deshacer_cola()\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes restaurar cuarentena mientras la cola publica."\n        )\n        return\n\n    total = 0\n\n    for item in cola_anuncios:\n        if item.get("estado_cola") != "CUARENTENA":\n            continue\n\n        item["estado_cola"] = "PENDIENTE"\n        item["fallos_iguales"] = 0\n        item["historial_fallos"] = []\n        item["cuarentena_fecha"] = ""\n        item["cuarentena_motivo"] = ""\n        item["error_fase"] = ""\n        item["error_motivo"] = ""\n        item["error_fecha"] = ""\n        item["intentos"] = 0\n\n        total += 1\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n\n    estado.configure(\n        text=f"✅ Restaurados {total} anuncio(s) desde cuarentena."\n    )\n\n\ndef contar_cuarentena():\n    return sum(\n        1\n        for x in cola_anuncios\n        if x.get("estado_cola") == "CUARENTENA"\n    )\n\n\ndef _texto_busqueda_item(item):\n    return " ".join([\n        str(item.get("titulo", "")),\n        str(item.get("descripcion", "")),\n        str(item.get("estado_cola", "")),\n        str(item.get("error_fase", "")),\n        str(item.get("error_motivo", "")),\n        str(item.get("nota", "")),\n    ]).lower()\n\n\ndef aplicar_busqueda_cola(*_args):\n    global busqueda_cola_actual\n    busqueda_cola_actual = var_busqueda_cola.get().strip().lower()\n    refrescar_cola()\n\n\ndef ordenar_cola_segun_seleccion():\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes ordenar mientras la cola está publicando."\n        )\n        return\n\n    criterio = var_orden_cola.get().strip()\n\n    def clave(item):\n        if criterio == "Prioridad":\n            return PRIORIDAD_VALOR.get(\n                item.get("prioridad", "Normal"),\n                1\n            )\n        if criterio == "Calidad":\n            try:\n                return -int(item.get("calidad", 0))\n            except Exception:\n                return 0\n        if criterio == "Estado":\n            orden = {\n                "PENDIENTE": 0,\n                "ERROR": 1,\n                "CUARENTENA": 2,\n                "REVISAR": 3,\n                "PUBLICADO": 4,\n            }\n            return orden.get(\n                item.get("estado_cola", ""),\n                9\n            )\n        if criterio == "Título":\n            return str(\n                item.get("titulo", "")\n            ).lower()\n        return 0\n\n    if criterio != "Orden actual":\n        cola_anuncios.sort(\n            key=clave\n        )\n        guardar_cola_en_disco()\n        refrescar_cola()\n\n    estado.configure(\n        text=f"✅ Cola ordenada por: {criterio}"\n    )\n\n\ndef guardar_nota_seleccionado():\n    indice = _indice_seleccionado_cola()\n\n    if indice is None:\n        estado.configure(\n            text="Selecciona un anuncio para guardar una nota."\n        )\n        return\n\n    nota = var_nota_anuncio.get().strip()\n    cola_anuncios[indice]["nota"] = nota\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n\n    estado.configure(\n        text="✅ Nota interna guardada."\n    )\n\n\ndef cargar_nota_seleccionado():\n    indice = _indice_seleccionado_cola()\n\n    if indice is None:\n        var_nota_anuncio.set("")\n        return\n\n    var_nota_anuncio.set(\n        str(\n            cola_anuncios[indice].get(\n                "nota",\n                ""\n            )\n        )\n    )\n\n\ndef reintentar_errores_seguros():\n    """\n    Devuelve a PENDIENTE únicamente errores que el bot reconoce como temporales.\n    No toca CUARENTENA, REVISAR ni duplicados ambiguos.\n    """\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes reintentar mientras la cola está activa."\n        )\n        return\n\n    total = 0\n\n    for item in cola_anuncios:\n        if item.get("estado_cola") != "ERROR":\n            continue\n\n        fase = item.get("error_fase", "")\n        motivo = item.get("error_motivo", "")\n\n        if _es_error_temporal_reintentable(\n            fase,\n            motivo\n        ):\n            item["estado_cola"] = "PENDIENTE"\n            item["error_fase"] = ""\n            item["error_motivo"] = ""\n            item["error_fecha"] = ""\n            item["error_diagnostico"] = ""\n            item["intentos"] = 0\n            total += 1\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n\n    estado.configure(\n        text=f"✅ {total} error(es) seguro(s) devueltos a PENDIENTE."\n    )\n\n\ndef autoarchivar_publicados():\n    """\n    Quita de la cola los PUBLICADO pero conserva su registro permanente.\n    """\n    if cola_en_ejecucion:\n        return 0\n\n    publicados = [\n        x for x in cola_anuncios\n        if x.get("estado_cola") == "PUBLICADO"\n    ]\n\n    if not publicados:\n        return 0\n\n    for item in publicados:\n        try:\n            archivar_publicado(item)\n        except Exception:\n            pass\n\n    cola_anuncios[:] = [\n        x for x in cola_anuncios\n        if x.get("estado_cola") != "PUBLICADO"\n    ]\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n\n    return len(publicados)\n\n\ndef exportar_paquete_diagnostico():\n    """\n    Crea un ZIP con JSON/logs/configuración útiles para depurar.\n    No incluye imágenes del usuario.\n    """\n    try:\n        Path("exportaciones").mkdir(\n            parents=True,\n            exist_ok=True\n        )\n\n        sello = time.strftime("%Y%m%d_%H%M%S")\n        ruta_zip = Path(\n            "exportaciones"\n        ) / f"diagnostico_bot_{sello}.zip"\n\n        candidatos = [\n            ARCHIVO_COLA,\n            ARCHIVO_CONFIG,\n            ARCHIVO_CHECKPOINT_COLA,\n            ARCHIVO_ULTIMA_AUDITORIA,\n            ARCHIVO_CONTROL_DIARIO,\n            ARCHIVO_PUBLICADOS_ARCHIVO,\n            ARCHIVO_HASHES_VISUALES,\n            "estadisticas_bot.json",\n    "actividad_bot.json",\n            "perfiles_lote.json",\n            "plantillas_publicacion.json",\n        ]\n\n        with zipfile.ZipFile(\n            ruta_zip,\n            "w",\n            compression=zipfile.ZIP_DEFLATED\n        ) as z:\n            for nombre in candidatos:\n                ruta = Path(nombre)\n                if ruta.exists() and ruta.is_file():\n                    z.write(\n                        ruta,\n                        arcname=ruta.name\n                    )\n\n            for carpeta in (\n                "logs",\n                "errores_publicacion",\n                "informes_sesion",\n            ):\n                ruta_c = Path(carpeta)\n\n                if not ruta_c.exists():\n                    continue\n\n                archivos = sorted(\n                    [\n                        p\n                        for p in ruta_c.rglob("*")\n                        if p.is_file()\n                    ],\n                    key=lambda p: p.stat().st_mtime,\n                    reverse=True\n                )[:20]\n\n                for archivo in archivos:\n                    try:\n                        z.write(\n                            archivo,\n                            arcname=str(\n                                Path(carpeta)\n                                / archivo.name\n                            )\n                        )\n                    except Exception:\n                        pass\n\n        estado.configure(\n            text=f"✅ Paquete de diagnóstico creado: {ruta_zip}"\n        )\n\n        return str(\n            ruta_zip.resolve()\n        )\n\n    except Exception as e:\n        estado.configure(\n            text="❌ No pude crear el ZIP de diagnóstico: " + str(e)\n        )\n        return ""\n\n\ndef objetivo_sesion_actual():\n    try:\n        return max(\n            1,\n            int(\n                cargar_configuracion_general().get(\n                    "objetivo_publicaciones_sesion",\n                    10\n                )\n            )\n        )\n    except Exception:\n        return 10\n\n\ndef progreso_objetivo_sesion():\n    objetivo = objetivo_sesion_actual()\n\n    return min(\n        100,\n        int(\n            (\n                cola_publicados_sesion\n                / max(1, objetivo)\n            )\n            * 100\n        )\n    )\n\n\ndef generar_lote_desde_publicados():\n    """\n    Toma los últimos publicados archivados y crea variaciones nuevas en cola.\n    """\n    archivo = _cargar_archivo_publicados()\n\n    if not archivo:\n        estado.configure(\n            text="No hay publicados archivados todavía."\n        )\n        return\n\n    try:\n        cantidad = int(\n            var_cantidad_lote.get()\n        )\n    except Exception:\n        cantidad = 3\n\n    cantidad = max(\n        1,\n        min(\n            cantidad,\n            espacio_cola_disponible()\n        )\n    )\n\n    if cantidad <= 0:\n        estado.configure(\n            text="La cola está llena."\n        )\n        return\n\n    base = list(\n        reversed(\n            archivo[-max(\n                cantidad,\n                min(capacidad_cola_actual(), len(archivo))\n            ):]\n        )\n    )\n\n    generados = 0\n\n    for registro in base:\n        if generados >= cantidad:\n            break\n\n        titulo = str(\n            registro.get(\n                "titulo",\n                "Servicio de automoción"\n            )\n        )\n\n        precio = str(\n            registro.get(\n                "precio",\n                ""\n            )\n        )\n\n        entrada_servicio.delete(\n            0,\n            "end"\n        )\n        entrada_servicio.insert(\n            0,\n            _crear_titulo_reemplazo_unico(\n                _titulo_base_sin_variante(\n                    titulo\n                ),\n                random.randint(\n                    1,\n                    999\n                )\n            )\n        )\n\n        entrada_precio.delete(\n            0,\n            "end"\n        )\n        entrada_precio.insert(\n            0,\n            precio\n        )\n\n        antes = len(\n            cola_anuncios\n        )\n\n        generar_y_anadir_a_cola()\n\n        if len(\n            cola_anuncios\n        ) > antes:\n            generados += 1\n\n    refrescar_cola()\n\n    estado.configure(\n        text=(\n            f"✅ {generados} variación(es) creadas "\n            "desde el historial publicado."\n        )\n    )\n\n\ndef marcar_favorito_seleccionado():\n    global favoritos_cola_sesion\n\n    indice = _indice_seleccionado_cola()\n\n    if indice is None:\n        estado.configure(\n            text="Selecciona un anuncio primero."\n        )\n        return\n\n    item = cola_anuncios[indice]\n    nuevo = not bool(\n        item.get(\n            "favorito",\n            False\n        )\n    )\n\n    item["favorito"] = nuevo\n\n    if nuevo:\n        favoritos_cola_sesion += 1\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n    refrescar_dashboard()\n\n    estado.configure(\n        text=(\n            "⭐ Marcado como favorito."\n            if nuevo\n            else "Favorito eliminado."\n        )\n    )\n\n\ndef clonar_seleccionado_variacion():\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes clonar mientras la cola publica."\n        )\n        return\n\n    indice = _indice_seleccionado_cola()\n\n    if indice is None:\n        estado.configure(\n            text="Selecciona un anuncio."\n        )\n        return\n\n    limite_plan = limite_cola_plan()\n\n    if len(cola_anuncios) >= limite_plan:\n        estado.configure(\n            text=(\n                f"❌ Tu plan {plan_actual().get(\'nombre\')} permite "\n                f"máximo {limite_plan} anuncios en cola."\n            )\n        )\n        return\n\n    original = cola_anuncios[indice]\n\n    nuevo = dict(original)\n    nuevo["estado_cola"] = "PENDIENTE"\n    nuevo["intentos"] = 0\n    nuevo["error_fase"] = ""\n    nuevo["error_motivo"] = ""\n    nuevo["error_fecha"] = ""\n    nuevo["error_diagnostico"] = ""\n    nuevo["favorito"] = False\n\n    cola_anuncios.append(nuevo)\n\n    ok = reemplazar_duplicado_por_anuncio_nuevo(\n        nuevo\n    )\n\n    if not ok:\n        try:\n            cola_anuncios.remove(nuevo)\n        except Exception:\n            pass\n\n        estado.configure(\n            text="❌ No pude crear una variación segura."\n        )\n        return\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n\n    estado.configure(\n        text="✅ Variación clonada y añadida a cola."\n    )\n\n\ndef archivar_publicados_manual():\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ Espera a que termine la cola."\n        )\n        return\n\n    total = autoarchivar_publicados()\n\n    estado.configure(\n        text=f"✅ {total} publicado(s) archivado(s)."\n    )\n\n\ndef limpiar_estados_terminados():\n    preparar_deshacer_cola()\n    """\n    Elimina de la cola PUBLICADO y ERROR resueltos manualmente.\n    Mantiene PENDIENTE, REVISAR y CUARENTENA.\n    """\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes limpiar mientras la cola publica."\n        )\n        return\n\n    antes = len(cola_anuncios)\n\n    cola_anuncios[:] = [\n        x\n        for x in cola_anuncios\n        if x.get("estado_cola") not in (\n            "PUBLICADO",\n        )\n    ]\n\n    eliminados = antes - len(cola_anuncios)\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n\n    estado.configure(\n        text=f"✅ {eliminados} elemento(s) terminado(s) limpiados."\n    )\n\n\ndef exportar_cola_json():\n    if not requerir_funcion_plan(\n        "exportaciones",\n        "Exportar cola a JSON"\n    ):\n        return ""\n    global ultimo_export_json\n\n    try:\n        Path("exportaciones").mkdir(\n            parents=True,\n            exist_ok=True\n        )\n\n        sello = time.strftime(\n            "%Y%m%d_%H%M%S"\n        )\n\n        ruta = Path(\n            "exportaciones"\n        ) / f"cola_completa_{sello}.json"\n\n        ruta.write_text(\n            json.dumps(\n                cola_anuncios,\n                ensure_ascii=False,\n                indent=2\n            ),\n            encoding="utf-8"\n        )\n\n        ultimo_export_json = str(\n            ruta.resolve()\n        )\n\n        estado.configure(\n            text=f"✅ Cola exportada a JSON: {ruta}"\n        )\n\n        return ultimo_export_json\n\n    except Exception as e:\n        estado.configure(\n            text="❌ No pude exportar JSON: " + str(e)\n        )\n        return ""\n\n\ndef estadisticas_cola_resumen():\n    resumen = {\n        "total": len(cola_anuncios),\n        "pendiente": 0,\n        "publicado": 0,\n        "error": 0,\n        "revisar": 0,\n        "cuarentena": 0,\n        "favoritos": 0,\n        "calidad_media": 0,\n    }\n\n    calidades = []\n\n    for item in cola_anuncios:\n        estado_item = str(\n            item.get(\n                "estado_cola",\n                ""\n            )\n        ).lower()\n\n        if estado_item in resumen:\n            resumen[estado_item] += 1\n\n        if item.get("favorito"):\n            resumen["favoritos"] += 1\n\n        try:\n            calidades.append(\n                int(\n                    item.get(\n                        "calidad",\n                        0\n                    )\n                )\n            )\n        except Exception:\n            pass\n\n    if calidades:\n        resumen["calidad_media"] = int(\n            sum(calidades)\n            / len(calidades)\n        )\n\n    return resumen\n\n\ndef preparar_siguiente_lote():\n    """\n    Usa los últimos publicados archivados para generar hasta la cantidad\n    configurada en var_cantidad_lote.\n    """\n    generar_lote_desde_publicados()\n\n\ndef _crear_id_lote(nombre_base=""):\n    sello = time.strftime("%Y%m%d_%H%M%S")\n    base = re.sub(\n        r"[^a-zA-Z0-9áéíóúÁÉÍÓÚñÑ_-]+",\n        "_",\n        str(nombre_base).strip()\n    ).strip("_")\n    base = base[:24] or "LOTE"\n    return f"{base}_{sello}"\n\n\ndef _nombre_lote_item(item):\n    return str(\n        item.get(\n            "lote_nombre",\n            item.get(\n                "lote_id",\n                "SIN LOTE"\n            )\n        )\n    ).strip() or "SIN LOTE"\n\n\ndef actualizar_menu_lotes():\n    try:\n        lotes = sorted(\n            {\n                _nombre_lote_item(x)\n                for x in cola_anuncios\n                if _nombre_lote_item(x) != "SIN LOTE"\n            },\n            key=str.lower\n        )\n\n        valores = ["TODOS", "SIN LOTE"] + lotes\n\n        menu_filtro_lote.configure(\n            values=valores\n        )\n\n        if var_filtro_lote.get() not in valores:\n            var_filtro_lote.set("TODOS")\n\n    except Exception:\n        pass\n\n\ndef asignar_lote_item(item, lote_id, lote_nombre):\n    item["lote_id"] = str(lote_id)\n    item["lote_nombre"] = str(lote_nombre)\n\n\ndef asignar_lote_seleccionado():\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes cambiar lotes mientras la cola publica."\n        )\n        return\n\n    indice = _indice_seleccionado_cola()\n\n    if indice is None:\n        estado.configure(\n            text="Selecciona un anuncio."\n        )\n        return\n\n    nombre = var_nombre_lote_manual.get().strip()\n\n    if not nombre:\n        estado.configure(\n            text="Escribe un nombre de lote."\n        )\n        return\n\n    asignar_lote_item(\n        cola_anuncios[indice],\n        _crear_id_lote(nombre),\n        nombre\n    )\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n    actualizar_menu_lotes()\n\n    estado.configure(\n        text=f"✅ Anuncio asignado al lote: {nombre}"\n    )\n\n\ndef items_lote_actual():\n    filtro = var_filtro_lote.get().strip()\n\n    if filtro == "TODOS":\n        return list(cola_anuncios)\n\n    if filtro == "SIN LOTE":\n        return [\n            x\n            for x in cola_anuncios\n            if _nombre_lote_item(x) == "SIN LOTE"\n        ]\n\n    return [\n        x\n        for x in cola_anuncios\n        if _nombre_lote_item(x) == filtro\n    ]\n\n\ndef prioridad_lote_actual():\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes cambiar prioridades durante la publicación."\n        )\n        return\n\n    items = items_lote_actual()\n\n    if not items:\n        estado.configure(\n            text="No hay anuncios en el lote seleccionado."\n        )\n        return\n\n    prioridad = var_prioridad_lote.get().strip()\n\n    if prioridad not in PRIORIDAD_VALOR:\n        prioridad = "Normal"\n\n    cambios = 0\n\n    for item in items:\n        if item.get("estado_cola") == "PUBLICADO":\n            continue\n\n        item["prioridad"] = prioridad\n        cambios += 1\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n\n    estado.configure(\n        text=(\n            f"✅ Prioridad {prioridad} aplicada a "\n            f"{cambios} anuncio(s) del lote."\n        )\n    )\n\n\ndef exportar_manifiesto_lote():\n    items = items_lote_actual()\n\n    if not items:\n        estado.configure(\n            text="No hay anuncios en el lote seleccionado."\n        )\n        return\n\n    try:\n        Path("exportaciones").mkdir(\n            parents=True,\n            exist_ok=True\n        )\n\n        nombre_lote = var_filtro_lote.get().strip()\n        nombre_seguro = re.sub(\n            r"[^a-zA-Z0-9_-]+",\n            "_",\n            nombre_lote\n        )[:40] or "lote"\n\n        sello = time.strftime("%Y%m%d_%H%M%S")\n        ruta = Path("exportaciones") / (\n            f"manifiesto_{nombre_seguro}_{sello}.txt"\n        )\n\n        lineas = [\n            "MANIFIESTO DE LOTE · WallapopBot",\n            "================================",\n            "",\n            f"Lote/filtro: {nombre_lote}",\n            f"Fecha: {time.strftime(\'%d/%m/%Y %H:%M:%S\')}",\n            f"Total: {len(items)}",\n            "",\n        ]\n\n        for i, item in enumerate(items, start=1):\n            lineas.extend([\n                f"{i:02d}. {item.get(\'titulo\', \'\')}",\n                f"    Estado: {item.get(\'estado_cola\', \'\')}",\n                f"    Precio: {item.get(\'precio\', \'\')} €",\n                f"    Prioridad: {item.get(\'prioridad\', \'Normal\')}",\n                f"    Calidad: {item.get(\'calidad\', \'—\')}",\n                f"    Lote: {_nombre_lote_item(item)}",\n                "",\n            ])\n\n        ruta.write_text(\n            "\\n".join(lineas),\n            encoding="utf-8"\n        )\n\n        estado.configure(\n            text=f"✅ Manifiesto creado: {ruta}"\n        )\n\n        return str(ruta.resolve())\n\n    except Exception as e:\n        estado.configure(\n            text="❌ No pude exportar el manifiesto: " + str(e)\n        )\n        return ""\n\n\ndef _texto_normalizado_similitud(texto):\n    t = str(texto or "").lower()\n    t = re.sub(\n        r"[^a-z0-9áéíóúüñ ]+",\n        " ",\n        t\n    )\n    t = re.sub(\n        r"\\s+",\n        " ",\n        t\n    ).strip()\n    return t\n\n\ndef similitud_textual_items(item_a, item_b):\n    titulo_a = _texto_normalizado_similitud(\n        item_a.get("titulo", "")\n    )\n    titulo_b = _texto_normalizado_similitud(\n        item_b.get("titulo", "")\n    )\n    desc_a = _texto_normalizado_similitud(\n        item_a.get("descripcion", "")\n    )\n    desc_b = _texto_normalizado_similitud(\n        item_b.get("descripcion", "")\n    )\n\n    ratio_titulo = SequenceMatcher(\n        None,\n        titulo_a,\n        titulo_b\n    ).ratio()\n\n    ratio_desc = SequenceMatcher(\n        None,\n        desc_a[:500],\n        desc_b[:500]\n    ).ratio()\n\n    return int(\n        (\n            ratio_titulo * 0.7\n            + ratio_desc * 0.3\n        )\n        * 100\n    )\n\n\ndef detectar_similitudes_cola(\n    marcar=True,\n    mostrar=True\n):\n    global similitudes_textuales_detectadas\n\n    try:\n        umbral = int(\n            cargar_configuracion_general().get(\n                "umbral_similitud_textual",\n                88\n            )\n        )\n    except Exception:\n        umbral = 88\n\n    pares = []\n\n    for item in cola_anuncios:\n        if marcar:\n            item["similaridad_maxima"] = 0\n            item["similar_a"] = ""\n\n    for i in range(len(cola_anuncios)):\n        a = cola_anuncios[i]\n\n        if a.get("estado_cola") == "PUBLICADO":\n            continue\n\n        for j in range(i + 1, len(cola_anuncios)):\n            b = cola_anuncios[j]\n\n            if b.get("estado_cola") == "PUBLICADO":\n                continue\n\n            score = similitud_textual_items(\n                a,\n                b\n            )\n\n            if score >= umbral:\n                pares.append((i, j, score))\n\n                if marcar:\n                    if score > int(a.get("similaridad_maxima", 0)):\n                        a["similaridad_maxima"] = score\n                        a["similar_a"] = b.get("titulo", "")\n\n                    if score > int(b.get("similaridad_maxima", 0)):\n                        b["similaridad_maxima"] = score\n                        b["similar_a"] = a.get("titulo", "")\n\n    similitudes_textuales_detectadas = len(pares)\n\n    if marcar:\n        guardar_cola_en_disco()\n        refrescar_cola()\n\n    try:\n        refrescar_dashboard()\n    except Exception:\n        pass\n\n    if mostrar:\n        try:\n            if pares:\n                detalle = "\\n".join(\n                    (\n                        f"• {cola_anuncios[i].get(\'titulo\', \'\')[:30]} "\n                        f"↔ {cola_anuncios[j].get(\'titulo\', \'\')[:30]} "\n                        f"({score}%)"\n                    )\n                    for i, j, score in pares[:12]\n                )\n\n                messagebox.showwarning(\n                    "Similitud textual",\n                    (\n                        f"Detectadas {len(pares)} pareja(s) "\n                        f"con similitud ≥ {umbral}%.\\n\\n"\n                        + detalle\n                    )\n                )\n            else:\n                messagebox.showinfo(\n                    "Similitud textual",\n                    "No se detectaron anuncios demasiado parecidos."\n                )\n        except Exception:\n            pass\n\n    return pares\n\n\ndef _programar_autoguardado_borrador():\n    global ultimo_autoguardado_borrador\n\n    try:\n        segundos = int(\n            cargar_configuracion_general().get(\n                "autoguardado_borrador_segundos",\n                30\n            )\n        )\n    except Exception:\n        segundos = 30\n\n    segundos = max(\n        10,\n        min(\n            600,\n            segundos\n        )\n    )\n\n    try:\n        guardar_borrador(\n            silencioso=True\n        )\n        ultimo_autoguardado_borrador = time.strftime(\n            "%H:%M:%S"\n        )\n    except Exception:\n        pass\n\n    try:\n        refrescar_dashboard()\n    except Exception:\n        pass\n\n    try:\n        ventana.after(\n            segundos * 1000,\n            _programar_autoguardado_borrador\n        )\n    except Exception:\n        pass\n\n\ndef registrar_actividad(accion, detalle=""):\n    global ultima_accion_registrada\n\n    try:\n        if not bool(\n            cargar_configuracion_general().get(\n                "registro_actividad",\n                True\n            )\n        ):\n            return\n\n        registros = []\n\n        if os.path.isfile(ARCHIVO_ACTIVIDAD):\n            try:\n                with open(\n                    ARCHIVO_ACTIVIDAD,\n                    "r",\n                    encoding="utf-8"\n                ) as f:\n                    cargado = json.load(f)\n\n                if isinstance(cargado, list):\n                    registros = cargado\n            except Exception:\n                registros = []\n\n        sesion = (\n            var_nombre_sesion.get().strip()\n            if "var_nombre_sesion" in globals()\n            else ""\n        )\n\n        entrada = {\n            "fecha": time.strftime("%d/%m/%Y %H:%M:%S"),\n            "sesion": sesion,\n            "accion": str(accion),\n            "detalle": str(detalle),\n        }\n\n        registros.append(entrada)\n        registros = registros[-3000:]\n\n        with open(\n            ARCHIVO_ACTIVIDAD,\n            "w",\n            encoding="utf-8"\n        ) as f:\n            json.dump(\n                registros,\n                f,\n                ensure_ascii=False,\n                indent=2\n            )\n\n        ultima_accion_registrada = (\n            f"{accion}: {detalle}"\n            if detalle\n            else str(accion)\n        )\n\n    except Exception:\n        pass\n\n\ndef preparar_deshacer_cola():\n    global ultimo_estado_deshacer\n    try:\n        ultimo_estado_deshacer = json.loads(\n            json.dumps(\n                cola_anuncios,\n                ensure_ascii=False\n            )\n        )\n    except Exception:\n        ultimo_estado_deshacer = None\n\n\ndef deshacer_ultima_accion_cola():\n    global ultimo_estado_deshacer\n\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes deshacer mientras la cola está publicando."\n        )\n        return\n\n    if ultimo_estado_deshacer is None:\n        estado.configure(\n            text="No hay ninguna acción de cola para deshacer."\n        )\n        return\n\n    actual = json.loads(\n        json.dumps(\n            cola_anuncios,\n            ensure_ascii=False\n        )\n    )\n\n    cola_anuncios[:] = ultimo_estado_deshacer\n    ultimo_estado_deshacer = actual\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n    actualizar_menu_lotes()\n    refrescar_dashboard()\n\n    registrar_actividad(\n        "Deshacer cola",\n        f"Restaurados {len(cola_anuncios)} elementos."\n    )\n\n    estado.configure(\n        text="↩ Última modificación de cola deshecha."\n    )\n\n\ndef salud_global_cola():\n    if not cola_anuncios:\n        return 100\n\n    puntuaciones = []\n\n    for item in cola_anuncios:\n        estado_item = item.get(\n            "estado_cola",\n            "PENDIENTE"\n        )\n\n        if estado_item == "PUBLICADO":\n            puntuaciones.append(100)\n            continue\n\n        try:\n            calidad = int(\n                item.get(\n                    "calidad",\n                    0\n                )\n            )\n        except Exception:\n            calidad = 0\n\n        base = calidad\n\n        if estado_item == "ERROR":\n            base -= 30\n        elif estado_item == "CUARENTENA":\n            base -= 40\n        elif estado_item == "REVISAR":\n            base -= 35\n\n        if int(item.get("similaridad_maxima", 0)) >= 88:\n            base -= 10\n\n        puntuaciones.append(\n            max(\n                0,\n                min(\n                    100,\n                    base\n                )\n            )\n        )\n\n    return int(\n        sum(puntuaciones)\n        / max(\n            1,\n            len(puntuaciones)\n        )\n    )\n\n\ndef estadisticas_lote_actual():\n    items = items_lote_actual()\n\n    resultado = {\n        "total": len(items),\n        "pendientes": 0,\n        "publicados": 0,\n        "errores": 0,\n        "cuarentena": 0,\n        "revisar": 0,\n        "calidad_media": 0,\n    }\n\n    calidades = []\n\n    for item in items:\n        estado_item = str(\n            item.get(\n                "estado_cola",\n                ""\n            )\n        ).upper()\n\n        mapa = {\n            "PENDIENTE": "pendientes",\n            "PUBLICADO": "publicados",\n            "ERROR": "errores",\n            "CUARENTENA": "cuarentena",\n            "REVISAR": "revisar",\n        }\n\n        clave = mapa.get(\n            estado_item\n        )\n\n        if clave:\n            resultado[clave] += 1\n\n        try:\n            calidades.append(\n                int(\n                    item.get(\n                        "calidad",\n                        0\n                    )\n                )\n            )\n        except Exception:\n            pass\n\n    if calidades:\n        resultado["calidad_media"] = int(\n            sum(calidades)\n            / len(calidades)\n        )\n\n    return resultado\n\n\ndef mostrar_estadisticas_lote():\n    stats = estadisticas_lote_actual()\n\n    try:\n        messagebox.showinfo(\n            "Estadísticas del lote",\n            (\n                f"Filtro/lote: {var_filtro_lote.get()}\\n\\n"\n                f"Total: {stats[\'total\']}\\n"\n                f"Pendientes: {stats[\'pendientes\']}\\n"\n                f"Publicados: {stats[\'publicados\']}\\n"\n                f"Errores: {stats[\'errores\']}\\n"\n                f"Cuarentena: {stats[\'cuarentena\']}\\n"\n                f"Revisar: {stats[\'revisar\']}\\n"\n                f"Calidad media: {stats[\'calidad_media\']}/100"\n            )\n        )\n    except Exception:\n        pass\n\n\ndef duplicar_lote_con_variaciones():\n    if not requerir_funcion_plan(\n        "lotes_avanzados",\n        "Duplicar lote con variaciones"\n    ):\n        return\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes duplicar lotes mientras la cola publica."\n        )\n        return\n\n    origen = [\n        x for x in items_lote_actual()\n        if x.get("estado_cola") != "PUBLICADO"\n    ]\n\n    if not origen:\n        estado.configure(\n            text="No hay anuncios válidos en el lote seleccionado."\n        )\n        return\n\n    huecos = max(\n        0,\n        espacio_cola_disponible()\n    )\n\n    if huecos <= 0:\n        estado.configure(\n            text="La cola está llena."\n        )\n        return\n\n    preparar_deshacer_cola()\n\n    nombre_origen = var_filtro_lote.get().strip()\n    nombre_nuevo = (\n        f"{nombre_origen}_VAR"\n        if nombre_origen not in ("TODOS", "SIN LOTE")\n        else "LOTE_VARIADO"\n    )\n\n    lote_id = _crear_id_lote(\n        nombre_nuevo\n    )\n\n    generados = 0\n\n    for original in origen[:huecos]:\n        nuevo = json.loads(\n            json.dumps(\n                original,\n                ensure_ascii=False\n            )\n        )\n\n        nuevo["estado_cola"] = "PENDIENTE"\n        nuevo["intentos"] = 0\n        nuevo["error_fase"] = ""\n        nuevo["error_motivo"] = ""\n        nuevo["error_fecha"] = ""\n        nuevo["error_diagnostico"] = ""\n        nuevo["favorito"] = False\n        nuevo["nota"] = ""\n\n        asignar_lote_item(\n            nuevo,\n            lote_id,\n            nombre_nuevo\n        )\n\n        cola_anuncios.append(\n            nuevo\n        )\n\n        if reemplazar_duplicado_por_anuncio_nuevo(\n            nuevo\n        ):\n            generados += 1\n        else:\n            try:\n                cola_anuncios.remove(\n                    nuevo\n                )\n            except Exception:\n                pass\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n    actualizar_menu_lotes()\n\n    var_filtro_lote.set(\n        nombre_nuevo\n    )\n\n    registrar_actividad(\n        "Duplicar lote",\n        f"{nombre_origen} -> {nombre_nuevo} ({generados})"\n    )\n\n    estado.configure(\n        text=f"✅ Lote duplicado con {generados} variación(es)."\n    )\n\n\ndef archivar_lote_actual():\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes archivar mientras la cola publica."\n        )\n        return\n\n    items = items_lote_actual()\n\n    if not items:\n        estado.configure(\n            text="El lote seleccionado está vacío."\n        )\n        return\n\n    preparar_deshacer_cola()\n\n    publicados = [\n        x\n        for x in items\n        if x.get("estado_cola") == "PUBLICADO"\n    ]\n\n    for item in publicados:\n        try:\n            archivar_publicado(\n                item\n            )\n        except Exception:\n            pass\n\n    ids = {\n        id(x)\n        for x in publicados\n    }\n\n    cola_anuncios[:] = [\n        x\n        for x in cola_anuncios\n        if id(x) not in ids\n    ]\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n    actualizar_menu_lotes()\n\n    registrar_actividad(\n        "Archivar lote",\n        f"{var_filtro_lote.get()} · {len(publicados)} publicados."\n    )\n\n    estado.configure(\n        text=f"✅ {len(publicados)} publicado(s) del lote archivado(s)."\n    )\n\n\ndef exportar_csv_lote_actual():\n    if not requerir_funcion_plan(\n        "exportaciones",\n        "Exportar lote a CSV"\n    ):\n        return ""\n    items = items_lote_actual()\n\n    if not items:\n        estado.configure(\n            text="No hay elementos en el lote seleccionado."\n        )\n        return\n\n    try:\n        Path("exportaciones").mkdir(\n            parents=True,\n            exist_ok=True\n        )\n\n        nombre = re.sub(\n            r"[^a-zA-Z0-9_-]+",\n            "_",\n            var_filtro_lote.get()\n        )[:35] or "lote"\n\n        sello = time.strftime("%Y%m%d_%H%M%S")\n        ruta = Path(\n            "exportaciones"\n        ) / f"lote_{nombre}_{sello}.csv"\n\n        import csv\n\n        columnas = [\n            "titulo",\n            "precio",\n            "estado_cola",\n            "prioridad",\n            "calidad",\n            "auditoria_nivel",\n            "lote",\n            "nota",\n        ]\n\n        with open(\n            ruta,\n            "w",\n            encoding="utf-8-sig",\n            newline=""\n        ) as f:\n            writer = csv.DictWriter(\n                f,\n                fieldnames=columnas\n            )\n            writer.writeheader()\n\n            for item in items:\n                writer.writerow({\n                    "titulo": item.get("titulo", ""),\n                    "precio": item.get("precio", ""),\n                    "estado_cola": item.get("estado_cola", ""),\n                    "prioridad": item.get("prioridad", "Normal"),\n                    "calidad": item.get("calidad", ""),\n                    "auditoria_nivel": item.get("auditoria_nivel", ""),\n                    "lote": _nombre_lote_item(item),\n                    "nota": item.get("nota", ""),\n                })\n\n        registrar_actividad(\n            "Exportar lote CSV",\n            str(ruta)\n        )\n\n        estado.configure(\n            text=f"✅ CSV de lote exportado: {ruta}"\n        )\n\n        return str(\n            ruta.resolve()\n        )\n\n    except Exception as e:\n        estado.configure(\n            text="❌ No pude exportar lote CSV: " + str(e)\n        )\n        return ""\n\n\ndef pausar_reanudar_seleccionado():\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes pausar/reanudar manualmente mientras la cola publica."\n        )\n        return\n\n    indice = _indice_seleccionado_cola()\n\n    if indice is None:\n        estado.configure(\n            text="Selecciona primero un anuncio."\n        )\n        return\n\n    item = cola_anuncios[indice]\n    actual = item.get("estado_cola", "PENDIENTE")\n\n    preparar_deshacer_cola()\n\n    if actual == "PENDIENTE":\n        item["estado_cola"] = "PAUSADO"\n        mensaje = "⏸ Anuncio pausado."\n        accion = "Pausar anuncio"\n\n    elif actual == "PAUSADO":\n        item["estado_cola"] = "PENDIENTE"\n        mensaje = "▶ Anuncio reanudado."\n        accion = "Reanudar anuncio"\n\n    else:\n        estado.configure(\n            text="Solo puedes pausar anuncios PENDIENTE o reanudar PAUSADO."\n        )\n        return\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n    refrescar_dashboard()\n\n    registrar_actividad(\n        accion,\n        item.get("titulo", "")\n    )\n\n    estado.configure(\n        text=mensaje\n    )\n\n\ndef reanudar_todos_pausados():\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ Espera a que termine la cola."\n        )\n        return\n\n    preparar_deshacer_cola()\n\n    total = 0\n\n    for item in cola_anuncios:\n        if item.get("estado_cola") == "PAUSADO":\n            item["estado_cola"] = "PENDIENTE"\n            total += 1\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n    refrescar_dashboard()\n\n    registrar_actividad(\n        "Reanudar pausados",\n        f"{total} anuncio(s)"\n    )\n\n    estado.configure(\n        text=f"▶ {total} anuncio(s) reanudado(s)."\n    )\n\n\ndef ordenar_favoritos_primero():\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes ordenar mientras la cola está publicando."\n        )\n        return\n\n    preparar_deshacer_cola()\n\n    cola_anuncios.sort(\n        key=lambda x: (\n            0 if x.get("favorito") else 1,\n            PRIORIDAD_VALOR.get(\n                x.get("prioridad", "Normal"),\n                1\n            ),\n            str(\n                x.get(\n                    "titulo",\n                    ""\n                )\n            ).lower()\n        )\n    )\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n\n    registrar_actividad(\n        "Ordenar favoritos",\n        "Favoritos primero."\n    )\n\n    estado.configure(\n        text="⭐ Favoritos colocados primero."\n    )\n\n\ndef calcular_eta_cola():\n    global ultimo_eta_segundos\n    global ultima_velocidad_hora\n\n    pendientes = sum(\n        1\n        for x in cola_anuncios\n        if x.get("estado_cola") == "PENDIENTE"\n    )\n\n    if pendientes <= 0:\n        ultimo_eta_segundos = 0\n        ultima_velocidad_hora = 0.0\n        return 0, 0.0\n\n    if rendimiento_tiempos_publicacion:\n        media_publicacion = (\n            sum(rendimiento_tiempos_publicacion)\n            / len(rendimiento_tiempos_publicacion)\n        )\n    else:\n        media_publicacion = 30.0\n\n    try:\n        cfg = obtener_config_rendimiento()\n        pausa_media = (\n            cfg.get("pausa_min", 8)\n            + cfg.get("pausa_max", 15)\n        ) / 2\n    except Exception:\n        pausa_media = 11.5\n\n    segundos_por_item = max(\n        1.0,\n        media_publicacion + pausa_media\n    )\n\n    ultimo_eta_segundos = int(\n        pendientes * segundos_por_item\n    )\n\n    ultima_velocidad_hora = (\n        3600.0 / segundos_por_item\n    )\n\n    return (\n        ultimo_eta_segundos,\n        ultima_velocidad_hora\n    )\n\n\ndef _formatear_duracion(segundos):\n    segundos = max(\n        0,\n        int(segundos)\n    )\n\n    horas = segundos // 3600\n    minutos = (\n        segundos % 3600\n    ) // 60\n    segs = segundos % 60\n\n    if horas:\n        return f"{horas}h {minutos:02d}m"\n\n    if minutos:\n        return f"{minutos}m {segs:02d}s"\n\n    return f"{segs}s"\n\n\ndef analitica_errores():\n    if not requerir_funcion_plan(\n        "analitica",\n        "Analítica de errores"\n    ):\n        return\n    errores = [\n        x\n        for x in cola_anuncios\n        if x.get("estado_cola") in (\n            "ERROR",\n            "CUARENTENA",\n            "REVISAR"\n        )\n    ]\n\n    if not errores:\n        try:\n            messagebox.showinfo(\n                "Analítica de errores",\n                "No hay errores, cuarentenas ni anuncios por revisar."\n            )\n        except Exception:\n            pass\n        return\n\n    por_fase = {}\n    por_motivo = {}\n\n    for item in errores:\n        fase = str(\n            item.get(\n                "error_fase",\n                "Sin fase"\n            )\n        ).strip() or "Sin fase"\n\n        motivo = str(\n            item.get(\n                "error_motivo",\n                item.get(\n                    "cuarentena_motivo",\n                    "Sin detalle"\n                )\n            )\n        ).strip() or "Sin detalle"\n\n        motivo_corto = motivo[:90]\n\n        por_fase[fase] = (\n            por_fase.get(\n                fase,\n                0\n            )\n            + 1\n        )\n\n        por_motivo[motivo_corto] = (\n            por_motivo.get(\n                motivo_corto,\n                0\n            )\n            + 1\n        )\n\n    top_fases = sorted(\n        por_fase.items(),\n        key=lambda x: x[1],\n        reverse=True\n    )[:8]\n\n    top_motivos = sorted(\n        por_motivo.items(),\n        key=lambda x: x[1],\n        reverse=True\n    )[:8]\n\n    texto = [\n        f"Total problemáticos: {len(errores)}",\n        "",\n        "Por fase:"\n    ]\n\n    for fase, total in top_fases:\n        texto.append(\n            f"• {fase}: {total}"\n        )\n\n    texto.append("")\n    texto.append("Motivos más frecuentes:")\n\n    for motivo, total in top_motivos:\n        texto.append(\n            f"• {total}× {motivo}"\n        )\n\n    try:\n        messagebox.showinfo(\n            "Analítica de errores",\n            "\\n".join(texto)\n        )\n    except Exception:\n        pass\n\n\ndef aplicar_edicion_masiva_lote():\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes editar un lote mientras publica."\n        )\n        return\n\n    items = items_lote_actual()\n\n    if not items:\n        estado.configure(\n            text="El lote/filtro actual está vacío."\n        )\n        return\n\n    preparar_deshacer_cola()\n\n    precio = var_precio_masivo.get().strip()\n    categoria = var_categoria_masiva.get().strip()\n    estado_m = var_estado_masivo.get().strip()\n    envio_m = var_envio_masivo.get().strip()\n\n    cambios = 0\n\n    for item in items:\n        if item.get("estado_cola") == "PUBLICADO":\n            continue\n\n        if precio:\n            item["precio"] = precio.replace(\n                "€",\n                ""\n            ).strip()\n\n        if categoria:\n            item["categoria"] = categoria\n\n        if estado_m != "Sin cambios":\n            item["estado"] = estado_m\n\n        if envio_m == "Desactivar":\n            item["desactivar_envio"] = True\n        elif envio_m == "Mantener":\n            item["desactivar_envio"] = False\n\n        if item.get("estado_cola") == "ERROR":\n            item["estado_cola"] = "PENDIENTE"\n            item["error_fase"] = ""\n            item["error_motivo"] = ""\n            item["error_fecha"] = ""\n\n        cambios += 1\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n\n    registrar_actividad(\n        "Edición masiva lote",\n        f"{var_filtro_lote.get()} · {cambios} anuncio(s)"\n    )\n\n    estado.configure(\n        text=f"✅ Edición aplicada a {cambios} anuncio(s) del lote."\n    )\n\n\ndef exportar_actividad_csv():\n    if not requerir_funcion_plan(\n        "exportaciones",\n        "Exportar actividad a CSV"\n    ):\n        return ""\n    if not os.path.isfile(\n        ARCHIVO_ACTIVIDAD\n    ):\n        estado.configure(\n            text="No hay actividad registrada todavía."\n        )\n        return\n\n    try:\n        with open(\n            ARCHIVO_ACTIVIDAD,\n            "r",\n            encoding="utf-8"\n        ) as f:\n            datos = json.load(f)\n\n        if not isinstance(\n            datos,\n            list\n        ):\n            datos = []\n\n        Path(\n            "exportaciones"\n        ).mkdir(\n            parents=True,\n            exist_ok=True\n        )\n\n        sello = time.strftime(\n            "%Y%m%d_%H%M%S"\n        )\n\n        ruta = Path(\n            "exportaciones"\n        ) / f"actividad_{sello}.csv"\n\n        import csv\n\n        with open(\n            ruta,\n            "w",\n            encoding="utf-8-sig",\n            newline=""\n        ) as f:\n            writer = csv.DictWriter(\n                f,\n                fieldnames=[\n                    "fecha",\n                    "sesion",\n                    "accion",\n                    "detalle",\n                ]\n            )\n            writer.writeheader()\n\n            for item in datos:\n                if isinstance(\n                    item,\n                    dict\n                ):\n                    writer.writerow({\n                        "fecha": item.get("fecha", ""),\n                        "sesion": item.get("sesion", ""),\n                        "accion": item.get("accion", ""),\n                        "detalle": item.get("detalle", ""),\n                    })\n\n        estado.configure(\n            text=f"✅ Actividad exportada: {ruta}"\n        )\n\n        return str(\n            ruta.resolve()\n        )\n\n    except Exception as e:\n        estado.configure(\n            text="❌ No pude exportar actividad: " + str(e)\n        )\n        return ""\n\n\ndef recuperar_cola_tras_cierre_inesperado():\n    """\n    Si Windows, Chrome o el bot se cerraron mientras un anuncio estaba\n    PUBLICANDO, al siguiente arranque vuelve a PENDIENTE en lugar de\n    dejar la cola bloqueada para siempre.\n    """\n    if cola_en_ejecucion:\n        return 0\n\n    recuperados = 0\n\n    for item in cola_anuncios:\n        if item.get("estado_cola") == "PUBLICANDO":\n            item["estado_cola"] = "PENDIENTE"\n            item["error_fase"] = ""\n            item["error_motivo"] = ""\n            item["error_fecha"] = ""\n            recuperados += 1\n\n    if recuperados:\n        guardar_cola_en_disco()\n\n    return recuperados\n\n\ndef chequeo_integridad_admin():\n    """\n    Comprobación rápida de las piezas críticas del creador.\n    No toca Wallapop ni publica nada.\n    """\n    pruebas = []\n\n    funciones = [\n        ("Generador", generar_anuncio),\n        ("Generador de lotes", generar_lote_y_anadir_cola),\n        ("Cola", iniciar_cola_publicacion),\n        ("Publicación", globals().get("publicar_anuncio_directamente")),\n        ("Diagnóstico", ejecutar_diagnostico_prevuelo),\n        ("Guardar cola", guardar_cola_en_disco),\n    ]\n\n    for nombre, funcion in funciones:\n        pruebas.append(\n            (nombre, callable(funcion))\n        )\n\n    try:\n        Path(CARPETA_DATOS_USUARIO).mkdir(\n            parents=True,\n            exist_ok=True\n        )\n        prueba = Path(CARPETA_DATOS_USUARIO) / ".__admin_integrity.tmp"\n        prueba.write_text("ok", encoding="utf-8")\n        prueba.unlink(missing_ok=True)\n        pruebas.append(("Carpeta de datos", True))\n    except Exception:\n        pruebas.append(("Carpeta de datos", False))\n\n    fallos = [\n        nombre\n        for nombre, ok in pruebas\n        if not ok\n    ]\n\n    return {\n        "ok": not fallos,\n        "fallos": fallos,\n        "total": len(pruebas),\n    }\n\n\ndef mantenimiento_rapido_admin():\n    """\n    Un botón para las tareas de mantenimiento habituales.\n    """\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ Detén la cola antes de ejecutar mantenimiento."\n        )\n        return\n\n    recuperados = recuperar_cola_tras_cierre_inesperado()\n\n    try:\n        reparar_errores_cola_automaticamente()\n    except Exception:\n        pass\n\n    try:\n        validar_toda_la_cola()\n    except Exception:\n        pass\n\n    resultado = chequeo_integridad_admin()\n\n    refrescar_cola()\n    refrescar_dashboard()\n\n    if resultado["ok"]:\n        estado.configure(\n            text=(\n                f"✅ Mantenimiento completado. "\n                f"Recuperados: {recuperados}. Sistema íntegro."\n            )\n        )\n    else:\n        estado.configure(\n            text=(\n                "⚠ Mantenimiento terminado. Revisar: "\n                + ", ".join(resultado["fallos"])\n            )\n        )\n\n\ndef comprobar_banner_ui():\n    """\n    Comprueba si el banner está disponible y devuelve su ruta.\n    """\n    ruta = asset_path(\n        "banner_dp5_limpio_v15.png"\n    )\n\n    existe = os.path.isfile(\n        ruta\n    )\n\n    return existe, ruta\n\n\ndef _diag_resultado(\n    categoria,\n    nombre,\n    ok,\n    detalle="",\n    reparable=False,\n    severidad="media"\n):\n    return {\n        "categoria": str(categoria),\n        "nombre": str(nombre),\n        "ok": bool(ok),\n        "detalle": str(detalle),\n        "reparable": bool(reparable),\n        "reparado": False,\n        "reparacion": "",\n        "severidad": str(severidad),\n    }\n\n\ndef _diagnostico_funcion_existente(nombres):\n    """\n    Devuelve la primera función válida encontrada entre varios alias.\n    Así evitamos falsos positivos si una función cambió de nombre.\n    """\n    for nombre in nombres:\n        fn = globals().get(nombre)\n\n        if callable(fn):\n            return True, nombre\n\n    return False, ""\n\n\ndef _diagnostico_puerto_abierto(host, puerto, timeout=0.8):\n    try:\n        with socket.create_connection(\n            (host, int(puerto)),\n            timeout=timeout\n        ):\n            return True\n    except Exception:\n        return False\n\n\ndef _diagnostico_chrome_cdp():\n    """\n    Comprueba el puerto CDP sin abrir ni cerrar Chrome.\n    """\n    puerto = 9222\n\n    try:\n        fn = globals().get("obtener_puerto_chromium")\n\n        if callable(fn):\n            puerto = int(fn())\n        elif "var_puerto_chromium" in globals():\n            puerto = int(\n                var_puerto_chromium.get()\n            )\n    except Exception:\n        puerto = 9222\n\n    if not _diagnostico_puerto_abierto(\n        "127.0.0.1",\n        puerto\n    ):\n        return False, (\n            f"Puerto CDP {puerto} no responde."\n        )\n\n    try:\n        with urllib.request.urlopen(\n            f"http://127.0.0.1:{puerto}/json/version",\n            timeout=2\n        ) as resp:\n            datos = json.loads(\n                resp.read().decode(\n                    "utf-8"\n                )\n            )\n\n        navegador = datos.get(\n            "Browser",\n            "Chromium/Chrome"\n        )\n\n        return True, (\n            f"{navegador} · puerto {puerto}"\n        )\n\n    except Exception as e:\n        return True, (\n            f"Puerto {puerto} abierto, pero /json/version falló: {e}"\n        )\n\n\ndef _diagnostico_espacio_disco(ruta):\n    try:\n        uso = shutil.disk_usage(\n            str(ruta)\n        )\n\n        libres_gb = (\n            uso.free\n            / (1024 ** 3)\n        )\n\n        return libres_gb >= 0.5, (\n            f"{libres_gb:.1f} GB libres"\n        )\n    except Exception as e:\n        return False, str(e)\n\n\ndef _diagnostico_json_archivo(ruta, default):\n    ruta = Path(ruta)\n\n    if not ruta.exists():\n        return True, "Todavía no creado", True\n\n    try:\n        datos = json.loads(\n            ruta.read_text(\n                encoding="utf-8"\n            )\n        )\n\n        if not isinstance(\n            datos,\n            type(default)\n        ):\n            return (\n                False,\n                (\n                    f"Tipo inesperado: {type(datos).__name__}; "\n                    f"esperado {type(default).__name__}"\n                ),\n                True\n            )\n\n        return True, "JSON válido", True\n\n    except Exception as e:\n        return False, f"JSON dañado: {e}", True\n\n\ndef _diagnostico_item_cola(item, indice):\n    errores = []\n\n    if not isinstance(\n        item,\n        dict\n    ):\n        return [\n            f"Elemento {indice + 1}: no es un objeto válido"\n        ]\n\n    titulo = str(\n        item.get(\n            "titulo",\n            ""\n        )\n    ).strip()\n\n    if not titulo:\n        errores.append(\n            f"Elemento {indice + 1}: título vacío"\n        )\n\n    estado_item = str(\n        item.get(\n            "estado_cola",\n            "PENDIENTE"\n        )\n    )\n\n    estados_validos = {\n        "PENDIENTE",\n        "PUBLICANDO",\n        "PUBLICADO",\n        "ERROR",\n        "REVISAR",\n        "CUARENTENA",\n        "PAUSADO",\n    }\n\n    if estado_item not in estados_validos:\n        errores.append(\n            f"Elemento {indice + 1}: estado desconocido \'{estado_item}\'"\n        )\n\n    try:\n        precio = item.get(\n            "precio",\n            ""\n        )\n\n        if str(precio).strip():\n            float(\n                str(precio)\n                .replace("€", "")\n                .replace(",", ".")\n                .strip()\n            )\n    except Exception:\n        errores.append(\n            f"Elemento {indice + 1}: precio inválido"\n        )\n\n    carpeta = str(\n        item.get(\n            "carpeta",\n            ""\n        )\n    ).strip()\n\n    if carpeta and not os.path.isdir(\n        carpeta\n    ):\n        errores.append(\n            f"Elemento {indice + 1}: carpeta no existe"\n        )\n\n    imagenes = item.get(\n        "imagenes",\n        []\n    )\n\n    if isinstance(\n        imagenes,\n        list\n    ):\n        faltantes = [\n            p\n            for p in imagenes\n            if p and not os.path.isfile(\n                str(p)\n            )\n        ]\n\n        if faltantes:\n            errores.append(\n                (\n                    f"Elemento {indice + 1}: "\n                    f"{len(faltantes)} imagen(es) no existen"\n                )\n            )\n\n    return errores\n\n\ndef diagnostico_integral_bot():\n    """\n    Diagnóstico de alta cobertura.\n    No publica nada y evita modificar el sistema durante el análisis.\n    """\n    resultados = []\n\n    # =====================================================\n    # A. CÓDIGO Y FUNCIONES\n    # =====================================================\n    grupos_funciones = [\n        (\n            "Generación",\n            "Generar anuncio",\n            ["generar_anuncio"]\n        ),\n        (\n            "Generación",\n            "Generar lote",\n            ["generar_lote_y_anadir_cola"]\n        ),\n        (\n            "Cola",\n            "Iniciar cola",\n            ["iniciar_cola_publicacion"]\n        ),\n        (\n            "Publicación",\n            "Motor de publicación",\n            [\n                "_publicar_wallapop_hilo",\n                "publicar_anuncio_directamente",\n                "_publicar_anuncio",\n            ]\n        ),\n        (\n            "Cola",\n            "Guardar cola",\n            ["guardar_cola_en_disco"]\n        ),\n        (\n            "Cola",\n            "Cargar cola",\n            ["cargar_cola_desde_disco"]\n        ),\n        (\n            "Cola",\n            "Refrescar cola",\n            ["refrescar_cola"]\n        ),\n        (\n            "Diagnóstico",\n            "Preflight",\n            ["ejecutar_diagnostico_prevuelo"]\n        ),\n        (\n            "Errores",\n            "Reparador de cola",\n            [\n                "reparar_errores_cola_automaticamente",\n                "autorreparar_errores_cola",\n            ]\n        ),\n    ]\n\n    for categoria, nombre, alias in grupos_funciones:\n        ok, alias_real = _diagnostico_funcion_existente(\n            alias\n        )\n\n        resultados.append(\n            _diag_resultado(\n                categoria,\n                nombre,\n                ok,\n                (\n                    f"Disponible como {alias_real}"\n                    if ok\n                    else "No se encontró ninguna implementación válida"\n                ),\n                reparable=False,\n                severidad="alta" if not ok else "baja"\n            )\n        )\n\n    # Sintaxis del propio archivo actual\n    try:\n        ruta_codigo = Path(__file__).resolve()\n\n        compile(\n            ruta_codigo.read_text(\n                encoding="utf-8"\n            ),\n            str(ruta_codigo),\n            "exec"\n        )\n\n        resultados.append(\n            _diag_resultado(\n                "Código",\n                "Sintaxis del programa",\n                True,\n                "Sintaxis Python válida",\n                False,\n                "alta"\n            )\n        )\n    except Exception as e:\n        resultados.append(\n            _diag_resultado(\n                "Código",\n                "Sintaxis del programa",\n                False,\n                str(e),\n                False,\n                "critica"\n            )\n        )\n\n    # =====================================================\n    # B. DEPENDENCIAS\n    # =====================================================\n    dependencias = [\n        ("customtkinter", "customtkinter"),\n        ("Pillow", "PIL"),\n        ("Playwright", "playwright"),\n        ("cryptography", "cryptography"),\n    ]\n\n    for visible, modulo in dependencias:\n        disponible = (\n            importlib.util.find_spec(\n                modulo\n            )\n            is not None\n        )\n\n        resultados.append(\n            _diag_resultado(\n                "Dependencias",\n                visible,\n                disponible,\n                "Disponible" if disponible else "No instalado",\n                reparable=False,\n                severidad="alta"\n            )\n        )\n\n    resultados.append(\n        _diag_resultado(\n            "Sistema",\n            "Python",\n            sys.version_info >= (3, 10),\n            platform.python_version(),\n            False,\n            "alta"\n        )\n    )\n\n    # =====================================================\n    # C. CARPETAS / DISCO / PERMISOS\n    # =====================================================\n    carpetas = []\n\n    for nombre_var, visible in [\n        ("CARPETA_DATOS_USUARIO", "Datos"),\n        ("CARPETA_ANUNCIOS", "Anuncios generados"),\n        ("CARPETA_BACKUPS_SEGURA", "Backups"),\n    ]:\n        if nombre_var in globals():\n            carpetas.append(\n                (\n                    visible,\n                    Path(\n                        globals()[\n                            nombre_var\n                        ]\n                    )\n                )\n            )\n\n    for nombre, ruta in carpetas:\n        try:\n            ruta.mkdir(\n                parents=True,\n                exist_ok=True\n            )\n\n            prueba = ruta / (\n                ".__diagprog5_write_test.tmp"\n            )\n\n            prueba.write_text(\n                "ok",\n                encoding="utf-8"\n            )\n\n            prueba.unlink(\n                missing_ok=True\n            )\n\n            resultados.append(\n                _diag_resultado(\n                    "Almacenamiento",\n                    f"Carpeta {nombre}",\n                    True,\n                    str(ruta),\n                    True,\n                    "alta"\n                )\n            )\n\n        except Exception as e:\n            resultados.append(\n                _diag_resultado(\n                    "Almacenamiento",\n                    f"Carpeta {nombre}",\n                    False,\n                    str(e),\n                    True,\n                    "alta"\n                )\n            )\n\n    ruta_disco = Path(\n        globals().get(\n            "CARPETA_DATOS_USUARIO",\n            Path.home()\n        )\n    )\n\n    ok_disco, detalle_disco = (\n        _diagnostico_espacio_disco(\n            ruta_disco\n        )\n    )\n\n    resultados.append(\n        _diag_resultado(\n            "Almacenamiento",\n            "Espacio libre",\n            ok_disco,\n            detalle_disco,\n            False,\n            "alta"\n        )\n    )\n\n    # =====================================================\n    # D. ARCHIVOS JSON\n    # =====================================================\n    archivos_json = {\n        "configuracion_bot.json": {},\n        "plantillas_publicacion.json": [],\n        "cola_publicacion.json": [],\n        "estadisticas_bot.json": {},\n        "actividad_bot.json": [],\n    }\n\n    for archivo, default in archivos_json.items():\n        ok, detalle, reparable = (\n            _diagnostico_json_archivo(\n                archivo,\n                default\n            )\n        )\n\n        resultados.append(\n            _diag_resultado(\n                "Archivos",\n                archivo,\n                ok,\n                detalle,\n                reparable,\n                "alta"\n            )\n        )\n\n    # =====================================================\n    # E. COLA\n    # =====================================================\n    try:\n        resultados.append(\n            _diag_resultado(\n                "Cola",\n                "Estructura de cola",\n                isinstance(\n                    cola_anuncios,\n                    list\n                ),\n                (\n                    f"{len(cola_anuncios)} elemento(s)"\n                    if isinstance(\n                        cola_anuncios,\n                        list\n                    )\n                    else "La cola no es una lista"\n                ),\n                False,\n                "critica"\n            )\n        )\n\n        errores_items = []\n\n        if isinstance(\n            cola_anuncios,\n            list\n        ):\n            for i, item in enumerate(\n                cola_anuncios\n            ):\n                errores_items.extend(\n                    _diagnostico_item_cola(\n                        item,\n                        i\n                    )\n                )\n\n        resultados.append(\n            _diag_resultado(\n                "Cola",\n                "Integridad de anuncios",\n                len(errores_items) == 0,\n                (\n                    "Todos los elementos son coherentes"\n                    if not errores_items\n                    else " · ".join(\n                        errores_items[:12]\n                    )\n                ),\n                True,\n                "alta"\n            )\n        )\n\n        atascados = [\n            x\n            for x in cola_anuncios\n            if x.get(\n                "estado_cola"\n            ) == "PUBLICANDO"\n        ]\n\n        resultados.append(\n            _diag_resultado(\n                "Cola",\n                "Anuncios atascados",\n                len(atascados) == 0,\n                (\n                    "Ninguno"\n                    if not atascados\n                    else f"{len(atascados)} en PUBLICANDO"\n                ),\n                True,\n                "alta"\n            )\n        )\n\n        # duplicados exactos\n        vistos = set()\n        duplicados = 0\n\n        for item in cola_anuncios:\n            titulo = str(\n                item.get(\n                    "titulo",\n                    ""\n                )\n            ).strip().lower()\n\n            descripcion = str(\n                item.get(\n                    "descripcion",\n                    ""\n                )\n            ).strip().lower()\n\n            clave = (\n                titulo,\n                descripcion\n            )\n\n            if titulo and clave in vistos:\n                duplicados += 1\n\n            vistos.add(\n                clave\n            )\n\n        resultados.append(\n            _diag_resultado(\n                "Cola",\n                "Duplicados exactos",\n                duplicados == 0,\n                (\n                    "No detectados"\n                    if duplicados == 0\n                    else f"{duplicados} duplicado(s)"\n                ),\n                True,\n                "media"\n            )\n        )\n\n        limite = limite_cola_plan()\n\n        resultados.append(\n            _diag_resultado(\n                "Cola",\n                "Capacidad",\n                len(cola_anuncios) <= limite,\n                f"{len(cola_anuncios)}/{limite}",\n                False,\n                "media"\n            )\n        )\n\n    except Exception as e:\n        resultados.append(\n            _diag_resultado(\n                "Cola",\n                "Diagnóstico interno",\n                False,\n                str(e),\n                False,\n                "critica"\n            )\n        )\n\n    # =====================================================\n    # F. CHROME / PLAYWRIGHT / CDP\n    # =====================================================\n    ok_cdp, detalle_cdp = (\n        _diagnostico_chrome_cdp()\n    )\n\n    resultados.append(\n        _diag_resultado(\n            "Navegador",\n            "Chrome / CDP",\n            True,\n            detalle_cdp if ok_cdp else "Inactivo ahora; se iniciará automáticamente al publicar.",\n            False,\n            "baja"\n        )\n    )\n\n    resultados.append(\n        _diag_resultado(\n            "Navegador",\n            "Playwright runtime",\n            sync_playwright is not None,\n            (\n                "Disponible"\n                if sync_playwright is not None\n                else "No disponible"\n            ),\n            False,\n            "alta"\n        )\n    )\n\n    # =====================================================\n    # G. INTERFAZ / ASSETS\n    # =====================================================\n    try:\n        ok_banner, ruta_banner = (\n            comprobar_banner_ui()\n        )\n\n        resultados.append(\n            _diag_resultado(\n                "Interfaz",\n                "Banner",\n                ok_banner,\n                ruta_banner,\n                False,\n                "baja"\n            )\n        )\n\n    except Exception as e:\n        resultados.append(\n            _diag_resultado(\n                "Interfaz",\n                "Banner",\n                False,\n                str(e),\n                False,\n                "baja"\n            )\n        )\n\n    # =====================================================\n    # H. LICENCIAS ADMIN\n    # =====================================================\n    if es_edicion_admin():\n        resultados.append(\n            _diag_resultado(\n                "Licencias",\n                "Ejecución ADMIN",\n                True,\n                "Autorizada. La clave privada no es necesaria para ejecutar ni publicar.",\n                False,\n                "baja"\n            )\n        )\n\n    # =====================================================\n    # I. ROTACIÓN DE CATÁLOGO\n    # =====================================================\n    try:\n        cfg_rot = cargar_config_rotacion()\n        candidatos_rot = _candidatos_rotacion_catalogo()\n\n        resultados.append(\n            _diag_resultado(\n                "Rotación de catálogo",\n                "Configuración",\n                True,\n                (\n                    f"{\'Activa\' if cfg_rot.get(\'activo\', True) else \'Desactivada\'} · "\n                    f"mínimo {cfg_rot.get(\'antiguedad_min_horas\', 24)} horas · "\n                    f"máximo {cfg_rot.get(\'max_borrados_sesion\', 5)} por sesión"\n                ),\n                False,\n                "baja"\n            )\n        )\n\n        resultados.append(\n            _diag_resultado(\n                "Rotación de catálogo",\n                "Candidatos disponibles",\n                True,\n                f"{len(candidatos_rot)} anuncio(s) elegible(s)",\n                False,\n                "baja"\n            )\n        )\n\n    except Exception as e:\n        resultados.append(\n            _diag_resultado(\n                "Rotación de catálogo",\n                "Motor de rotación",\n                False,\n                str(e),\n                False,\n                "media"\n            )\n        )\n\n    # =====================================================\n    # I. LOGS RECIENTES\n    # =====================================================\n    log_ruta = None\n\n    for candidato in [\n        "registro_bot.log",\n        "wallapopbot.log",\n        "bot.log",\n    ]:\n        if Path(\n            candidato\n        ).exists():\n            log_ruta = Path(\n                candidato\n            )\n            break\n\n    if log_ruta:\n        try:\n            lineas = log_ruta.read_text(\n                encoding="utf-8",\n                errors="ignore"\n            ).splitlines()[-250:]\n\n            palabras = (\n                "traceback",\n                "exception",\n                "error:",\n                "failed",\n            )\n\n            incidencias = [\n                l\n                for l in lineas\n                if any(\n                    p in l.lower()\n                    for p in palabras\n                )\n            ]\n\n            resultados.append(\n                _diag_resultado(\n                    "Logs",\n                    "Errores recientes",\n                    len(incidencias) == 0,\n                    (\n                        "Sin errores recientes"\n                        if not incidencias\n                        else f"{len(incidencias)} línea(s) sospechosa(s)"\n                    ),\n                    False,\n                    "media"\n                )\n            )\n        except Exception as e:\n            resultados.append(\n                _diag_resultado(\n                    "Logs",\n                    "Lectura de log",\n                    False,\n                    str(e),\n                    False,\n                    "baja"\n                )\n            )\n\n    return resultados\n\n\ndef _backup_pre_reparacion():\n    """\n    Crea una copia de seguridad de archivos sensibles antes de reparar.\n    """\n    try:\n        carpeta_base = Path(\n            globals().get(\n                "CARPETA_DATOS_USUARIO",\n                Path.home() / "DIAGPROG5"\n            )\n        )\n\n        carpeta = (\n            carpeta_base\n            / "reparaciones"\n            / time.strftime(\n                "%Y%m%d_%H%M%S"\n            )\n        )\n\n        carpeta.mkdir(\n            parents=True,\n            exist_ok=True\n        )\n\n        archivos = [\n            "configuracion_bot.json",\n            "plantillas_publicacion.json",\n            "cola_publicacion.json",\n            "estadisticas_bot.json",\n            "actividad_bot.json",\n        ]\n\n        for nombre in archivos:\n            origen = Path(\n                nombre\n            )\n\n            if origen.exists():\n                shutil.copy2(\n                    origen,\n                    carpeta / origen.name\n                )\n\n        return str(\n            carpeta\n        )\n\n    except Exception:\n        return ""\n\n\ndef reparar_automaticamente_diagnostico(resultados):\n    """\n    Reparación conservadora:\n    solo corrige elementos con bajo riesgo de pérdida de datos.\n    """\n    reparaciones = []\n\n    backup = _backup_pre_reparacion()\n\n    if backup:\n        reparaciones.append(\n            f"Backup previo creado: {backup}"\n        )\n\n    # =====================================================\n    # 1. CARPETAS\n    # =====================================================\n    for r in resultados:\n        if (\n            not r["ok"]\n            and r["categoria"] == "Almacenamiento"\n            and r["nombre"].startswith(\n                "Carpeta "\n            )\n        ):\n            try:\n                mapa = {\n                    "Carpeta Datos": globals().get(\n                        "CARPETA_DATOS_USUARIO"\n                    ),\n                    "Carpeta Anuncios generados": globals().get(\n                        "CARPETA_ANUNCIOS"\n                    ),\n                    "Carpeta Backups": globals().get(\n                        "CARPETA_BACKUPS_SEGURA"\n                    ),\n                }\n\n                destino = mapa.get(\n                    r["nombre"]\n                )\n\n                if destino:\n                    Path(\n                        destino\n                    ).mkdir(\n                        parents=True,\n                        exist_ok=True\n                    )\n\n                    r["reparado"] = True\n                    r["reparacion"] = (\n                        f"Carpeta creada: {destino}"\n                    )\n\n                    reparaciones.append(\n                        r["reparacion"]\n                    )\n\n            except Exception as e:\n                r["reparacion"] = str(e)\n\n    # =====================================================\n    # 2. PUBLICANDO ATASCADOS\n    # =====================================================\n    try:\n        atascados = [\n            x\n            for x in cola_anuncios\n            if x.get(\n                "estado_cola"\n            ) == "PUBLICANDO"\n        ]\n\n        if (\n            atascados\n            and not cola_en_ejecucion\n        ):\n            for item in atascados:\n                item["estado_cola"] = "PENDIENTE"\n                item["error_fase"] = ""\n                item["error_motivo"] = ""\n                item["error_fecha"] = ""\n\n            guardar_cola_en_disco()\n\n            reparaciones.append(\n                (\n                    f"Recuperados {len(atascados)} "\n                    "anuncio(s) atascado(s) en PUBLICANDO."\n                )\n            )\n\n    except Exception:\n        pass\n\n    # =====================================================\n    # 3. ESTADOS DESCONOCIDOS\n    # =====================================================\n    try:\n        corregidos = 0\n\n        for item in cola_anuncios:\n            if not isinstance(\n                item,\n                dict\n            ):\n                continue\n\n            estado_item = item.get(\n                "estado_cola"\n            )\n\n            if estado_item not in {\n                "PENDIENTE",\n                "PUBLICANDO",\n                "PUBLICADO",\n                "ERROR",\n                "REVISAR",\n                "CUARENTENA",\n                "PAUSADO",\n            }:\n                item["estado_cola"] = "PENDIENTE"\n                corregidos += 1\n\n        if corregidos:\n            guardar_cola_en_disco()\n\n            reparaciones.append(\n                (\n                    f"Normalizados {corregidos} "\n                    "estado(s) desconocido(s) de la cola."\n                )\n            )\n\n    except Exception:\n        pass\n\n    # =====================================================\n    # 4. DUPLICADOS EXACTOS NO PUBLICADOS\n    # =====================================================\n    try:\n        vistos = set()\n        nueva = []\n        eliminados = 0\n\n        for item in cola_anuncios:\n            if not isinstance(\n                item,\n                dict\n            ):\n                nueva.append(\n                    item\n                )\n                continue\n\n            titulo = str(\n                item.get(\n                    "titulo",\n                    ""\n                )\n            ).strip().lower()\n\n            descripcion = str(\n                item.get(\n                    "descripcion",\n                    ""\n                )\n            ).strip().lower()\n\n            clave = (\n                titulo,\n                descripcion\n            )\n\n            if (\n                titulo\n                and clave in vistos\n                and item.get(\n                    "estado_cola"\n                ) != "PUBLICADO"\n            ):\n                eliminados += 1\n                continue\n\n            vistos.add(\n                clave\n            )\n\n            nueva.append(\n                item\n            )\n\n        if eliminados:\n            cola_anuncios[:] = nueva\n            guardar_cola_en_disco()\n\n            reparaciones.append(\n                (\n                    f"Eliminados {eliminados} "\n                    "duplicado(s) exacto(s) pendientes."\n                )\n            )\n\n    except Exception:\n        pass\n\n    # =====================================================\n    # 5. JSON CORRUPTOS O TIPO INCORRECTO\n    # =====================================================\n    defaults_json = {\n        "configuracion_bot.json": {},\n        "plantillas_publicacion.json": [],\n        "cola_publicacion.json": [],\n        "estadisticas_bot.json": {},\n        "actividad_bot.json": [],\n    }\n\n    for archivo, default in defaults_json.items():\n        ruta = Path(\n            archivo\n        )\n\n        if not ruta.exists():\n            continue\n\n        necesita_reset = False\n\n        try:\n            datos = json.loads(\n                ruta.read_text(\n                    encoding="utf-8"\n                )\n            )\n\n            if not isinstance(\n                datos,\n                type(default)\n            ):\n                necesita_reset = True\n\n        except Exception:\n            necesita_reset = True\n\n        if necesita_reset:\n            try:\n                backup_corrupto = ruta.with_suffix(\n                    ruta.suffix\n                    + ".corrupto.bak"\n                )\n\n                shutil.copy2(\n                    ruta,\n                    backup_corrupto\n                )\n\n                ruta.write_text(\n                    json.dumps(\n                        default,\n                        ensure_ascii=False,\n                        indent=2\n                    ),\n                    encoding="utf-8"\n                )\n\n                reparaciones.append(\n                    (\n                        f"Reparado {archivo}; copia anterior: "\n                        f"{backup_corrupto.name}"\n                    )\n                )\n\n            except Exception:\n                pass\n\n    # =====================================================\n    # 6. REPARADOR INTERNO EXISTENTE\n    # =====================================================\n    for nombre_fn in [\n        "reparar_errores_cola_automaticamente",\n        "autorreparar_errores_cola",\n    ]:\n        fn = globals().get(\n            nombre_fn\n        )\n\n        if callable(fn):\n            try:\n                fn()\n\n                reparaciones.append(\n                    (\n                        "Ejecutado reparador interno de "\n                        "errores de publicación."\n                    )\n                )\n\n                break\n\n            except Exception as e:\n                reparaciones.append(\n                    (\n                        "El reparador interno no pudo completar: "\n                        + str(e)\n                    )\n                )\n\n    # =====================================================\n    # 7. RECONSTRUIR / REFRESCAR ESTADO\n    # =====================================================\n    try:\n        guardar_cola_en_disco()\n    except Exception:\n        pass\n\n    try:\n        refrescar_cola()\n    except Exception:\n        pass\n\n    try:\n        refrescar_dashboard()\n    except Exception:\n        pass\n\n    return reparaciones\n\n\ndef _puntuacion_salud(resultados):\n    if not resultados:\n        return 0\n\n    peso = {\n        "baja": 1,\n        "media": 2,\n        "alta": 4,\n        "critica": 8,\n    }\n\n    total = sum(\n        peso.get(\n            r.get(\n                "severidad",\n                "media"\n            ),\n            2\n        )\n        for r in resultados\n    )\n\n    fallos = sum(\n        peso.get(\n            r.get(\n                "severidad",\n                "media"\n            ),\n            2\n        )\n        for r in resultados\n        if not r["ok"]\n    )\n\n    if total <= 0:\n        return 100\n\n    return max(\n        0,\n        min(\n            100,\n            int(\n                100\n                - (\n                    fallos\n                    / total\n                    * 100\n                )\n            )\n        )\n    )\n\n\ndef _texto_informe_diagnostico(\n    resultados,\n    reparaciones\n):\n    errores = [\n        r\n        for r in resultados\n        if not r["ok"]\n    ]\n\n    salud = _puntuacion_salud(\n        resultados\n    )\n\n    lineas = [\n        "DIAGPROG5 · DIAGNÓSTICO PRO",\n        "=" * 56,\n        "",\n        f"Salud del bot: {salud}/100",\n        f"Comprobaciones: {len(resultados)}",\n        f"Problemas restantes: {len(errores)}",\n        f"Acciones/reparaciones: {len(reparaciones)}",\n        "",\n    ]\n\n    categorias = []\n\n    for r in resultados:\n        if r["categoria"] not in categorias:\n            categorias.append(\n                r["categoria"]\n            )\n\n    for categoria in categorias:\n        grupo = [\n            r\n            for r in resultados\n            if r["categoria"] == categoria\n        ]\n\n        lineas.append(\n            categoria.upper()\n        )\n\n        lineas.append(\n            "-" * 56\n        )\n\n        for r in grupo:\n            icono = (\n                "✅"\n                if r["ok"]\n                else (\n                    "🛑"\n                    if r.get(\n                        "severidad"\n                    ) == "critica"\n                    else "❌"\n                )\n            )\n\n            lineas.append(\n                (\n                    f"{icono} {r[\'nombre\']} · "\n                    f"{r[\'detalle\']}"\n                )\n            )\n\n        lineas.append("")\n\n    if reparaciones:\n        lineas.append(\n            "REPARACIONES / ACCIONES APLICADAS"\n        )\n\n        lineas.append(\n            "-" * 56\n        )\n\n        for rep in reparaciones:\n            lineas.append(\n                f"🔧 {rep}"\n            )\n\n        lineas.append("")\n\n    manuales = [\n        r\n        for r in errores\n        if not r.get(\n            "reparable",\n            False\n        )\n    ]\n\n    if manuales:\n        lineas.append(\n            "REQUIEREN REVISIÓN MANUAL"\n        )\n\n        lineas.append(\n            "-" * 56\n        )\n\n        for r in manuales:\n            lineas.append(\n                (\n                    f"⚠ {r[\'categoria\']} / "\n                    f"{r[\'nombre\']} · "\n                    f"{r[\'detalle\']}"\n                )\n            )\n\n        lineas.append("")\n\n    if not errores:\n        lineas.append(\n            "✅ No quedan problemas detectables por el sistema automático."\n        )\n\n    lineas.append("")\n    lineas.append(\n        "Nota: ningún diagnóstico automático puede garantizar detectar "\n        "el 100% de fallos posibles, especialmente si Wallapop cambia "\n        "su web. Este sistema cubre los fallos internos y operativos más "\n        "comunes sin modificar código de forma peligrosa."\n    )\n\n    return "\\n".join(\n        lineas\n    )\n\n\ndef diagnosticar_y_reparar_bot():\n    """\n    Un clic:\n    1) backup\n    2) escaneo\n    3) reparación segura\n    4) nuevo escaneo\n    5) informe\n    """\n    if cola_en_ejecucion:\n        try:\n            messagebox.showwarning(\n                "Diagnóstico PRO",\n                (\n                    "La cola está publicando ahora mismo.\\n\\n"\n                    "Detén la cola antes de ejecutar una reparación."\n                )\n            )\n        except Exception:\n            pass\n\n        return\n\n    try:\n        estado.configure(\n            text="🔎 Diagnóstico PRO: analizando todo el bot..."\n        )\n    except Exception:\n        pass\n\n    try:\n        primera = diagnostico_integral_bot()\n\n        reparaciones = (\n            reparar_automaticamente_diagnostico(\n                primera\n            )\n        )\n\n        segunda = diagnostico_integral_bot()\n\n    except Exception as e:\n        segunda = [\n            _diag_resultado(\n                "Diagnóstico",\n                "Motor de diagnóstico",\n                False,\n                str(e),\n                False,\n                "critica"\n            )\n        ]\n\n        reparaciones = []\n\n    informe = _texto_informe_diagnostico(\n        segunda,\n        reparaciones\n    )\n\n    salud = _puntuacion_salud(\n        segunda\n    )\n\n    restantes = [\n        r\n        for r in segunda\n        if not r["ok"]\n    ]\n\n    ruta = None\n\n    try:\n        carpeta = Path(\n            globals().get(\n                "CARPETA_DATOS_USUARIO",\n                Path.home() / "DIAGPROG5"\n            )\n        )\n\n        carpeta.mkdir(\n            parents=True,\n            exist_ok=True\n        )\n\n        ruta = carpeta / (\n            "ultimo_diagnostico_reparacion.txt"\n        )\n\n        ruta.write_text(\n            informe,\n            encoding="utf-8"\n        )\n\n    except Exception:\n        pass\n\n    try:\n        refrescar_cola()\n    except Exception:\n        pass\n\n    try:\n        refrescar_dashboard()\n    except Exception:\n        pass\n\n    try:\n        estado.configure(\n            text=(\n                f"🛠 Diagnóstico PRO · Salud {salud}/100 · "\n                f"{len(restantes)} problema(s) pendiente(s)"\n            )\n        )\n    except Exception:\n        pass\n\n    # =====================================================\n    # Ventana PRO\n    # =====================================================\n    try:\n        win = ctk.CTkToplevel(\n            ventana\n        )\n\n        win.title(\n            "DIAGPROG5 · Diagnóstico PRO"\n        )\n\n        win.geometry(\n            "980x760"\n        )\n\n        win.minsize(\n            820,\n            620\n        )\n\n        win.configure(\n            fg_color=COLOR_FONDO\n        )\n\n        cab = ctk.CTkFrame(\n            win,\n            fg_color=COLOR_PANEL,\n            corner_radius=10,\n            border_width=1,\n            border_color=COLOR_BORDE\n        )\n\n        cab.pack(\n            fill="x",\n            padx=18,\n            pady=(18, 10)\n        )\n\n        ctk.CTkLabel(\n            cab,\n            text="Diagnóstico PRO y autorreparación",\n            font=("Segoe UI", 22, "bold")\n        ).pack(\n            anchor="w",\n            padx=16,\n            pady=(14, 2)\n        )\n\n        color_salud = (\n            "#46d47a"\n            if salud >= 90\n            else (\n                "#f7c948"\n                if salud >= 70\n                else "#ff6b6b"\n            )\n        )\n\n        ctk.CTkLabel(\n            cab,\n            text=(\n                f"Salud del bot: {salud}/100  ·  "\n                f"Reparaciones: {len(reparaciones)}  ·  "\n                f"Pendientes: {len(restantes)}"\n            ),\n            font=("Segoe UI", 12, "bold"),\n            text_color=color_salud\n        ).pack(\n            anchor="w",\n            padx=16,\n            pady=(0, 14)\n        )\n\n        caja = ctk.CTkTextbox(\n            win,\n            font=("Segoe UI", 11),\n            wrap="word"\n        )\n\n        caja.pack(\n            fill="both",\n            expand=True,\n            padx=18,\n            pady=(0, 10)\n        )\n\n        caja.insert(\n            "1.0",\n            informe\n        )\n\n        caja.configure(\n            state="disabled"\n        )\n\n        pie = ctk.CTkFrame(\n            win,\n            fg_color="transparent"\n        )\n\n        pie.pack(\n            fill="x",\n            padx=18,\n            pady=(0, 16)\n        )\n\n        if ruta:\n            ctk.CTkLabel(\n                pie,\n                text=f"Informe: {ruta}",\n                font=("Segoe UI", 9),\n                text_color=COLOR_TEXTO_SEC\n            ).pack(\n                side="left"\n            )\n\n        ctk.CTkButton(\n            pie,\n            text="Volver a analizar",\n            command=lambda: (\n                win.destroy(),\n                diagnosticar_y_reparar_bot()\n            ),\n            width=135,\n            height=34,\n            fg_color="#315d83",\n            hover_color="#3c719d"\n        ).pack(\n            side="right",\n            padx=(6, 0)\n        )\n\n        ctk.CTkButton(\n            pie,\n            text="Cerrar",\n            command=win.destroy,\n            width=100,\n            height=34,\n            fg_color=COLOR_PANEL_2,\n            hover_color="#252b32"\n        ).pack(\n            side="right"\n        )\n\n    except Exception:\n        try:\n            messagebox.showinfo(\n                "Diagnóstico PRO",\n                informe\n            )\n        except Exception:\n            pass\n\n\n# =========================================================\n# CENTRO DE ACTUALIZACIONES ADMIN\n# =========================================================\n\nARCHIVO_CONFIG_ACTUALIZACIONES = os.path.join(\n    CARPETA_DATOS_USUARIO,\n    "actualizaciones_admin.json"\n)\n\nCARPETA_ACTUALIZACIONES = os.path.join(\n    CARPETA_DATOS_USUARIO,\n    "actualizaciones"\n)\n\nUPDATE_MANIFEST_DEFECTO = {\n    "activo": True,\n    "manifest_url": "https://diagprog5-updater.hugoneitor05cod3.workers.dev/version",\n    "canal": "admin",\n}\n\n\ndef version_actual_bot():\n    return "107"\n\n\ndef cargar_config_actualizaciones():\n    datos = dict(\n        UPDATE_MANIFEST_DEFECTO\n    )\n\n    try:\n        ruta = Path(\n            ARCHIVO_CONFIG_ACTUALIZACIONES\n        )\n\n        if ruta.exists():\n            guardado = json.loads(\n                ruta.read_text(\n                    encoding="utf-8"\n                )\n            )\n\n            if isinstance(\n                guardado,\n                dict\n            ):\n                datos.update(\n                    guardado\n                )\n\n        if not str(\n            datos.get(\n                "manifest_url",\n                ""\n            )\n        ).strip():\n            datos["manifest_url"] = "https://diagprog5-updater.hugoneitor05cod3.workers.dev/version"\n\n        datos["activo"] = True\n        datos["canal"] = "admin"\n\n    except Exception:\n        datos = dict(\n            UPDATE_MANIFEST_DEFECTO\n        )\n\n    return datos\n\n\ndef guardar_config_actualizaciones(datos):\n    ruta = Path(\n        ARCHIVO_CONFIG_ACTUALIZACIONES\n    )\n\n    ruta.parent.mkdir(\n        parents=True,\n        exist_ok=True\n    )\n\n    ruta.write_text(\n        json.dumps(\n            datos,\n            ensure_ascii=False,\n            indent=2\n        ),\n        encoding="utf-8"\n    )\n\n\ndef _version_num(valor):\n    try:\n        return int(\n            re.sub(\n                r"[^0-9]",\n                "",\n                str(valor)\n            )\n            or "0"\n        )\n    except Exception:\n        return 0\n\n\ndef _descargar_json_url(url, timeout=10):\n    urls = [\n        str(url).strip(),\n        "https://raw.githubusercontent.com/Hugo0555/diagprog5-updates/main/diagprog5-update.json",\n    ]\n\n    ultimo_error = None\n\n    for destino in urls:\n        if not destino:\n            continue\n\n        try:\n            req = urllib.request.Request(\n                destino,\n                headers={\n                    "User-Agent": "DIAGPROG5-Updater/2.0",\n                    "Cache-Control": "no-cache",\n                }\n            )\n\n            with urllib.request.urlopen(\n                req,\n                timeout=max(\n                    10,\n                    int(timeout)\n                )\n            ) as resp:\n                datos = json.loads(\n                    resp.read().decode(\n                        "utf-8"\n                    )\n                )\n\n            if isinstance(\n                datos,\n                dict\n            ):\n                return datos\n\n        except Exception as e:\n            ultimo_error = e\n\n    raise RuntimeError(\n        "No pude leer el servidor de actualizaciones. "\n        + str(\n            ultimo_error\n        )\n    )\n\n\ndef comprobar_actualizacion_admin(\n    mostrar=True\n):\n    """\n    Lee un manifest remoto. No descarga ni instala nada automáticamente.\n    """\n    cfg = cargar_config_actualizaciones()\n\n    url = str(\n        cfg.get(\n            "manifest_url",\n            "https://diagprog5-updater.hugoneitor05cod3.workers.dev/version"\n        )\n    ).strip() or "https://diagprog5-updater.hugoneitor05cod3.workers.dev/version"\n\n    if not cfg.get(\n        "activo",\n        False\n    ):\n        resultado = {\n            "ok": False,\n            "estado": "desactivado",\n            "mensaje": (\n                "El centro de actualizaciones está desactivado."\n            ),\n        }\n\n        if mostrar:\n            messagebox.showinfo(\n                "Actualizaciones",\n                resultado["mensaje"]\n            )\n\n        return resultado\n\n    if not url:\n        resultado = {\n            "ok": False,\n            "estado": "sin_url",\n            "mensaje": (\n                "No has configurado todavía la URL del manifest de actualizaciones."\n            ),\n        }\n\n        if mostrar:\n            messagebox.showwarning(\n                "Actualizaciones",\n                resultado["mensaje"]\n            )\n\n        return resultado\n\n    try:\n        manifest = _descargar_json_url(\n            url\n        )\n\n        if not isinstance(\n            manifest,\n            dict\n        ):\n            raise RuntimeError(\n                "El manifest no es un objeto JSON válido."\n            )\n\n        version_remota = str(\n            manifest.get(\n                "version",\n                ""\n            )\n        ).strip()\n\n        archivo_url = str(\n            manifest.get(\n                "download_url",\n                ""\n            )\n        ).strip()\n\n        if not version_remota:\n            raise RuntimeError(\n                "El manifest no contiene \'version\'."\n            )\n\n        disponible_por_version = (\n            _version_num(\n                version_remota\n            )\n            > _version_num(\n                version_actual_bot()\n            )\n        )\n\n        disponible = bool(\n            manifest.get(\n                "update_available",\n                disponible_por_version\n            )\n        ) or disponible_por_version\n\n        resultado = {\n            "ok": True,\n            "estado": (\n                "disponible"\n                if disponible\n                else "actualizado"\n            ),\n            "disponible": disponible,\n            "manifest": manifest,\n            "version_actual": version_actual_bot(),\n            "version_remota": version_remota,\n            "download_url": archivo_url,\n            "mensaje": (\n                f"Actualización disponible: V{version_remota}"\n                if disponible\n                else (\n                    f"Ya tienes la última versión disponible "\n                    f"(V{version_actual_bot()})."\n                )\n            ),\n        }\n\n        if mostrar:\n            if disponible:\n                notas = str(\n                    manifest.get(\n                        "notes",\n                        ""\n                    )\n                ).strip()\n\n                messagebox.showinfo(\n                    "Actualización disponible",\n                    (\n                        f"Versión actual: V{version_actual_bot()}\\n"\n                        f"Nueva versión: V{version_remota}\\n\\n"\n                        + (\n                            notas\n                            if notas\n                            else "Hay una nueva versión disponible."\n                        )\n                    )\n                )\n            else:\n                messagebox.showinfo(\n                    "Actualizaciones",\n                    resultado["mensaje"]\n                )\n\n        return resultado\n\n    except Exception as e:\n        resultado = {\n            "ok": False,\n            "estado": "error",\n            "mensaje": str(e),\n        }\n\n        if mostrar:\n            messagebox.showerror(\n                "Actualizaciones",\n                (\n                    "No pude comprobar actualizaciones:\\n\\n"\n                    + str(e)\n                )\n            )\n\n        return resultado\n\n\ndef _sha256_archivo(ruta):\n    h = hashlib.sha256()\n\n    with open(\n        ruta,\n        "rb"\n    ) as f:\n        for bloque in iter(\n            lambda: f.read(\n                1024 * 1024\n            ),\n            b""\n        ):\n            h.update(\n                bloque\n            )\n\n    return h.hexdigest()\n\n\ndef descargar_actualizacion_admin(\n    manifest=None\n):\n    if manifest is None:\n        estado_update = (\n            comprobar_actualizacion_admin(\n                mostrar=False\n            )\n        )\n\n        if not estado_update.get(\n            "ok"\n        ):\n            messagebox.showerror(\n                "Actualizaciones",\n                estado_update.get(\n                    "mensaje",\n                    "No pude comprobar la actualización."\n                )\n            )\n            return None\n\n        manifest = estado_update.get(\n            "manifest",\n            {}\n        )\n\n    url = str(\n        manifest.get(\n            "download_url",\n            ""\n        )\n    ).strip()\n\n    version = str(\n        manifest.get(\n            "version",\n            ""\n        )\n    ).strip()\n\n    if not url:\n        messagebox.showerror(\n            "Actualizaciones",\n            "El manifest no contiene download_url."\n        )\n        return None\n\n    try:\n        carpeta = Path(\n            CARPETA_ACTUALIZACIONES\n        )\n\n        carpeta.mkdir(\n            parents=True,\n            exist_ok=True\n        )\n\n        nombre = str(\n            manifest.get(\n                "filename",\n                f"DIAGPROG5_V{version}_ADMIN.py"\n            )\n        ).strip()\n\n        nombre = os.path.basename(\n            nombre\n        )\n\n        destino = carpeta / nombre\n\n        estado.configure(\n            text=f"⬇ Descargando actualización V{version}..."\n        )\n\n        req = urllib.request.Request(\n            url,\n            headers={\n                "User-Agent": "DIAGPROG5-Updater/1.0",\n                "Cache-Control": "no-cache",\n            }\n        )\n\n        with urllib.request.urlopen(\n            req,\n            timeout=30\n        ) as resp:\n            destino.write_bytes(\n                resp.read()\n            )\n\n        sha_esperado = str(\n            manifest.get(\n                "sha256",\n                ""\n            )\n        ).strip().lower()\n\n        if sha_esperado:\n            sha_real = _sha256_archivo(\n                destino\n            ).lower()\n\n            if sha_real != sha_esperado:\n                destino.unlink(\n                    missing_ok=True\n                )\n\n                raise RuntimeError(\n                    "La verificación SHA-256 ha fallado. "\n                    "La actualización se ha descartado."\n                )\n\n        # Verificar sintaxis si es .py\n        if destino.suffix.lower() == ".py":\n            compile(\n                destino.read_text(\n                    encoding="utf-8"\n                ),\n                str(destino),\n                "exec"\n            )\n\n        estado.configure(\n            text=(\n                f"✅ Actualización V{version} descargada y verificada."\n            )\n        )\n\n        return str(\n            destino\n        )\n\n    except Exception as e:\n        messagebox.showerror(\n            "Actualizaciones",\n            "No pude descargar la actualización:\\n\\n"\n            + str(e)\n        )\n\n        return None\n\n\ndef instalar_actualizacion_admin():\n    """\n    Descarga la nueva versión, crea backup de la actual y la abre.\n    No sobrescribe el archivo que está ejecutándose.\n    """\n    resultado = comprobar_actualizacion_admin(\n        mostrar=False\n    )\n\n    if not resultado.get(\n        "ok"\n    ):\n        messagebox.showerror(\n            "Actualizaciones",\n            resultado.get(\n                "mensaje",\n                "No pude comprobar actualizaciones."\n            )\n        )\n        return\n\n    if not resultado.get(\n        "disponible"\n    ):\n        messagebox.showinfo(\n            "Actualizaciones",\n            resultado.get(\n                "mensaje",\n                "Ya estás actualizado."\n            )\n        )\n        return\n\n    manifest = resultado.get(\n        "manifest",\n        {}\n    )\n\n    destino = descargar_actualizacion_admin(\n        manifest\n    )\n\n    if not destino:\n        return\n\n    # Copia de seguridad de la versión actual.\n    try:\n        actual = Path(\n            __file__\n        ).resolve()\n\n        carpeta_backup = (\n            Path(\n                CARPETA_DATOS_USUARIO\n            )\n            / "backups_actualizaciones"\n        )\n\n        carpeta_backup.mkdir(\n            parents=True,\n            exist_ok=True\n        )\n\n        backup = (\n            carpeta_backup\n            / (\n                actual.stem\n                + "_"\n                + time.strftime(\n                    "%Y%m%d_%H%M%S"\n                )\n                + actual.suffix\n            )\n        )\n\n        shutil.copy2(\n            actual,\n            backup\n        )\n\n    except Exception:\n        backup = None\n\n    version = str(\n        manifest.get(\n            "version",\n            ""\n        )\n    )\n\n    confirmar = messagebox.askyesno(\n        "Instalar actualización",\n        (\n            f"V{version} está lista.\\n\\n"\n            "DIAGPROG5 abrirá la nueva versión en una ventana nueva.\\n"\n            "La versión actual NO se borrará.\\n\\n"\n            "¿Quieres abrirla ahora?"\n        )\n    )\n\n    if not confirmar:\n        return\n\n    try:\n        destino_path = Path(\n            destino\n        )\n\n        if destino_path.suffix.lower() == ".py":\n            subprocess.Popen(\n                [\n                    sys.executable,\n                    str(\n                        destino_path\n                    ),\n                ],\n                cwd=str(\n                    destino_path.parent\n                )\n            )\n        else:\n            os.startfile(\n                str(\n                    destino_path\n                )\n            )\n\n        estado.configure(\n            text=(\n                f"✅ Nueva versión V{version} abierta. "\n                "Cerrando la versión anterior..."\n            )\n        )\n\n        # Cerrar automáticamente la versión anterior después de arrancar la nueva.\n        try:\n            def _cerrar_version_anterior():\n                try:\n                    fn_cerrar = globals().get("cerrar_aplicacion")\n                    if callable(fn_cerrar):\n                        fn_cerrar()\n                    else:\n                        ventana.destroy()\n                except Exception:\n                    try:\n                        ventana.destroy()\n                    except Exception:\n                        pass\n\n            ventana.after(900, _cerrar_version_anterior)\n        except Exception:\n            pass\n\n    except Exception as e:\n        messagebox.showerror(\n            "Actualizaciones",\n            (\n                "La actualización se descargó correctamente, "\n                "pero no pude abrirla:\\n\\n"\n                + str(e)\n            )\n        )\n\n\ndef _control_diario_cargar():\n    hoy = time.strftime("%Y-%m-%d")\n    datos = {\n        "fecha": hoy,\n        "publicados": 0\n    }\n\n    if os.path.isfile(ARCHIVO_CONTROL_DIARIO):\n        try:\n            with open(\n                ARCHIVO_CONTROL_DIARIO,\n                "r",\n                encoding="utf-8"\n            ) as f:\n                cargado = json.load(f)\n\n            if (\n                isinstance(cargado, dict)\n                and cargado.get("fecha") == hoy\n            ):\n                datos.update(cargado)\n        except Exception:\n            pass\n\n    return datos\n\n\ndef _control_diario_guardar(datos):\n    try:\n        with open(\n            ARCHIVO_CONTROL_DIARIO,\n            "w",\n            encoding="utf-8"\n        ) as f:\n            json.dump(\n                datos,\n                f,\n                ensure_ascii=False,\n                indent=2\n            )\n    except Exception:\n        pass\n\n\ndef publicaciones_hoy():\n    try:\n        return int(\n            _control_diario_cargar().get(\n                "publicados",\n                0\n            )\n        )\n    except Exception:\n        return 0\n\n\ndef registrar_publicacion_diaria():\n    datos = _control_diario_cargar()\n    datos["publicados"] = int(\n        datos.get("publicados", 0)\n    ) + 1\n    _control_diario_guardar(datos)\n\n\ndef limite_diario_alcanzado():\n    datos = cargar_configuracion_general()\n\n    try:\n        limite = int(\n            datos.get(\n                "limite_publicaciones_dia",\n                25\n            )\n        )\n    except Exception:\n        limite = 25\n\n    return publicaciones_hoy() >= limite\n\n\ndef capacidad_diaria_restante():\n    datos = cargar_configuracion_general()\n\n    try:\n        limite = int(\n            datos.get(\n                "limite_publicaciones_dia",\n                25\n            )\n        )\n    except Exception:\n        limite = 25\n\n    return max(\n        0,\n        limite - publicaciones_hoy()\n    )\n\n\ndef modo_prueba_activado():\n    try:\n        return bool(\n            cargar_configuracion_general().get(\n                "modo_prueba",\n                False\n            )\n        )\n    except Exception:\n        return False\n\n\ndef esperar_hora_programada_si_corresponde():\n    """\n    Si hay una hora configurada y todavía no ha llegado, espera sin bloquear\n    la interfaz principal (se ejecuta dentro del hilo de cola).\n    """\n    datos = cargar_configuracion_general()\n    hora_objetivo = str(\n        datos.get(\n            "hora_inicio_programada",\n            ""\n        )\n    ).strip()\n\n    if not hora_objetivo:\n        return True\n\n    try:\n        objetivo_h, objetivo_m = map(\n            int,\n            hora_objetivo.split(":")\n        )\n    except Exception:\n        return True\n\n    while not cola_cancelar:\n        ahora = time.localtime()\n\n        actual_min = (\n            ahora.tm_hour * 60\n            + ahora.tm_min\n        )\n        objetivo_min = (\n            objetivo_h * 60\n            + objetivo_m\n        )\n\n        if actual_min >= objetivo_min:\n            return True\n\n        faltan = objetivo_min - actual_min\n\n        try:\n            ventana.after(\n                0,\n                lambda f=faltan, h=hora_objetivo: estado.configure(\n                    text=(\n                        f"⏰ Cola programada para {h} · "\n                        f"faltan aprox. {f} min"\n                    )\n                )\n            )\n        except Exception:\n            pass\n\n        time.sleep(20)\n\n    return False\n\n\ndef limpiar_archivos_antiguos():\n    """\n    Limpia archivos antiguos de logs, diagnósticos, exportaciones e informes.\n    No toca anuncios_generados ni backups manuales.\n    """\n    try:\n        dias = int(\n            cargar_configuracion_general().get(\n                "dias_retencion_archivos",\n                14\n            )\n        )\n    except Exception:\n        dias = 14\n\n    limite = time.time() - (\n        dias * 24 * 60 * 60\n    )\n\n    carpetas = [\n        "logs",\n        "errores_publicacion",\n        "exportaciones",\n        "informes_sesion",\n    ]\n\n    eliminados = 0\n\n    for carpeta in carpetas:\n        ruta = Path(carpeta)\n\n        if not ruta.exists():\n            continue\n\n        for elemento in ruta.iterdir():\n            try:\n                if elemento.stat().st_mtime >= limite:\n                    continue\n\n                if elemento.is_dir():\n                    shutil.rmtree(\n                        elemento,\n                        ignore_errors=True\n                    )\n                else:\n                    elemento.unlink(\n                        missing_ok=True\n                    )\n\n                eliminados += 1\n\n            except Exception:\n                pass\n\n    try:\n        estado.configure(\n            text=(\n                f"🧹 Limpieza terminada · "\n                f"{eliminados} elemento(s) antiguo(s) eliminado(s)."\n            )\n        )\n    except Exception:\n        pass\n\n    return eliminados\n\n\ndef _guardar_checkpoint_cola(item=None, fase=""):\n    try:\n        datos = {\n            "fecha": time.strftime("%d/%m/%Y %H:%M:%S"),\n            "fase": fase,\n            "cola_en_ejecucion": bool(cola_en_ejecucion),\n            "titulo": item.get("titulo", "") if isinstance(item, dict) else "",\n            "estado_cola": item.get("estado_cola", "") if isinstance(item, dict) else "",\n            "publicados_sesion": cola_publicados_sesion,\n            "errores_sesion": cola_errores_sesion,\n            "recuperaciones_navegador": recuperaciones_navegador_sesion,\n        }\n\n        with open(\n            ARCHIVO_CHECKPOINT_COLA,\n            "w",\n            encoding="utf-8"\n        ) as f:\n            json.dump(\n                datos,\n                f,\n                ensure_ascii=False,\n                indent=2\n            )\n    except Exception:\n        pass\n\n\ndef _supervisor_config():\n    datos = cargar_configuracion_general()\n    return {\n        "activo": bool(datos.get("supervisor_automatico", True)),\n        "pausar_si_falla": bool(\n            datos.get("pausar_si_supervisor_falla", True)\n        ),\n        "max_intentos": max(\n            1,\n            int(datos.get("max_intentos_supervisor", 2))\n        ),\n    }\n\n\ndef comprobar_salud_para_siguiente_anuncio():\n    if not tiene_funcion_plan("supervisor"):\n        return True\n    """\n    Comprueba Chromium, CDP, sesión y respuesta de Wallapop\n    ANTES de empezar el siguiente anuncio.\n    """\n    global supervisor_ultimo_estado\n    global supervisor_ultimo_detalle\n    global supervisor_reparaciones_sesion\n\n    config = _supervisor_config()\n\n    if not config["activo"]:\n        supervisor_ultimo_estado = "DESACTIVADO"\n        supervisor_ultimo_detalle = ""\n        return True\n\n    ultimo_error = ""\n\n    for intento in range(1, config["max_intentos"] + 1):\n        try:\n            if not _puerto_chrome_activo():\n                raise RuntimeError(\n                    "El puerto de Chromium no responde."\n                )\n\n            if sync_playwright is None:\n                raise RuntimeError(\n                    "Playwright no está disponible."\n                )\n\n            with sync_playwright() as p:\n                navegador = _conectar_playwright_cdp(p)\n\n                if not navegador.contexts:\n                    raise RuntimeError(\n                        "Chromium no devuelve contexto."\n                    )\n\n                contexto = navegador.contexts[0]\n\n                _cerrar_todas_pestanas_wallapop(\n                    contexto\n                )\n\n                pagina = contexto.new_page()\n\n                try:\n                    pagina.goto(\n                        "https://es.wallapop.com",\n                        wait_until="domcontentloaded",\n                        timeout=25000\n                    )\n                    pagina.wait_for_timeout(500)\n\n                    if not _pagina_responde_wallapop(\n                        pagina,\n                        obtener_config_rendimiento().get(\n                            "timeout_wallapop",\n                            12\n                        )\n                    ):\n                        raise RuntimeError(\n                            "Wallapop abrió pero la página no responde."\n                        )\n\n                    _comprobar_sesion_wallapop(\n                        pagina\n                    )\n\n                    bloqueado, detalle_bloqueo = (\n                        detectar_bloqueo_o_verificacion_wallapop(\n                            pagina\n                        )\n                    )\n\n                    if bloqueado:\n                        marcar_proteccion_wallapop(\n                            detalle_bloqueo\n                        )\n                        raise RuntimeError(\n                            "Wallapop requiere verificación o ha limitado temporalmente "\n                            f"la actividad: {detalle_bloqueo}"\n                        )\n\n                finally:\n                    try:\n                        if not pagina.is_closed():\n                            pagina.close()\n                    except Exception:\n                        pass\n\n            supervisor_ultimo_estado = "OK"\n            supervisor_ultimo_detalle = (\n                f"Comprobación correcta · intento {intento}"\n            )\n\n            try:\n                ventana.after(\n                    0,\n                    refrescar_dashboard\n                )\n            except Exception:\n                pass\n\n            return True\n\n        except Exception as e:\n            ultimo_error = str(e)\n            supervisor_ultimo_estado = "RECUPERANDO"\n            supervisor_ultimo_detalle = ultimo_error\n\n            print(\n                f"Supervisor: intento {intento}/{config[\'max_intentos\']} falló:",\n                ultimo_error\n            )\n\n            if proteccion_ultimo_detalle:\n                break\n\n            if intento < config["max_intentos"]:\n                try:\n                    recuperar_navegador_para_cola()\n                    supervisor_reparaciones_sesion += 1\n                except Exception as rec_error:\n                    print(\n                        "Supervisor: recuperación fallida:",\n                        rec_error\n                    )\n\n                time.sleep(2)\n\n    supervisor_ultimo_estado = "FALLO"\n    supervisor_ultimo_detalle = ultimo_error\n\n    try:\n        ventana.after(\n            0,\n            refrescar_dashboard\n        )\n    except Exception:\n        pass\n\n    return False\n\n\ndef generar_informe_sesion_automatico():\n    try:\n        os.makedirs(\n            "informes_sesion",\n            exist_ok=True\n        )\n\n        sello = time.strftime(\n            "%Y%m%d_%H%M%S"\n        )\n\n        ruta = os.path.join(\n            "informes_sesion",\n            f"sesion_cola_{sello}.txt"\n        )\n\n        media = (\n            sum(rendimiento_tiempos_publicacion)\n            / len(rendimiento_tiempos_publicacion)\n            if rendimiento_tiempos_publicacion\n            else 0\n        )\n\n        texto = (\n            "INFORME DE SESIÓN · WallapopBot\\n"\n            "================================\\n\\n"\n            f"Fecha: {time.strftime(\'%d/%m/%Y %H:%M:%S\')}\\n"\n            f"Publicados: {cola_publicados_sesion}\\n"\n            f"Errores: {cola_errores_sesion}\\n"\n            f"Recuperaciones navegador: {recuperaciones_navegador_sesion}\\n"\n            f"Reparaciones supervisor: {supervisor_reparaciones_sesion}\\n"\n            f"Supervisor final: {supervisor_ultimo_estado}\\n"\n            f"Detalle supervisor: {supervisor_ultimo_detalle}\\n"\n            f"Media por anuncio: {media:.1f}s\\n\\n"\n            + generar_resumen_sesion_cola()\n        )\n\n        Path(ruta).write_text(\n            texto,\n            encoding="utf-8"\n        )\n\n        return ruta\n\n    except Exception as e:\n        print(\n            "No pude generar informe automático de sesión:",\n            e\n        )\n        return ""\n\n\ndef _procesar_cola_hilo():\n    global cola_en_ejecucion, cola_cancelar\n    global cola_publicados_sesion, cola_errores_sesion\n    global rendimiento_ultimo_tiempo, rendimiento_anuncios_procesados\n\n    total = len(cola_anuncios)\n    errores_consecutivos = 0\n\n    try:\n        if not esperar_hora_programada_si_corresponde():\n            return\n\n        for indice, item in enumerate(cola_anuncios, start=1):\n\n            if cola_cancelar:\n                print("Cola cancelada antes del siguiente anuncio.")\n                break\n\n            _esperar_si_pausada()\n\n            if cola_cancelar:\n                break\n\n            if cola_publicados_sesion >= obtener_max_publicaciones_sesion():\n                print("Límite de publicaciones por sesión alcanzado.")\n                ventana.after(\n                    0,\n                    lambda: estado.configure(\n                        text=(\n                            "⏹ Límite de publicaciones de la sesión alcanzado. "\n                            "Los anuncios restantes quedan pendientes."\n                        )\n                    )\n                )\n                break\n\n            if item.get("estado_cola") != "PENDIENTE":\n                continue\n\n            if limite_diario_alcanzado():\n                cola_cancelar = True\n\n                ventana.after(\n                    0,\n                    lambda: estado.configure(\n                        text=(\n                            "⏹ Límite diario de publicaciones alcanzado. "\n                            "Los anuncios restantes quedan pendientes."\n                        )\n                    )\n                )\n                break\n\n            _guardar_checkpoint_cola(\n                item,\n                "Antes del supervisor"\n            )\n\n            salud_ok = comprobar_salud_para_siguiente_anuncio()\n\n            if not salud_ok:\n                config_supervisor = _supervisor_config()\n\n                if config_supervisor["pausar_si_falla"]:\n                    cola_cancelar = True\n\n                    ventana.after(\n                        0,\n                        lambda: estado.configure(\n                            text=(\n                                "⛔ Supervisor: Wallapop/Chromium no están sanos. "\n                                "La cola se ha detenido antes de provocar errores en cascada."\n                            )\n                        )\n                    )\n                    break\n\n                item["estado_cola"] = "ERROR"\n                item["error_fase"] = "Supervisor"\n                item["error_motivo"] = supervisor_ultimo_detalle\n                item["error_fecha"] = time.strftime(\n                    "%d/%m/%Y %H:%M:%S"\n                )\n\n                guardar_cola_en_disco()\n\n                ventana.after(\n                    0,\n                    refrescar_cola\n                )\n                continue\n\n            _guardar_checkpoint_cola(\n                item,\n                "Supervisor OK"\n            )\n\n            # Si el anuncio ya fue publicado, el bot NO lo deja en error:\n            # crea automáticamente otro distinto y usa el mismo hueco de cola.\n            if anuncio_ya_publicado(\n                item["titulo"],\n                item["descripcion"],\n                item["precio"]\n            ):\n                incrementar_estadistica(\n                    "duplicados_bloqueados"\n                )\n\n                print(\n                    "Duplicado detectado antes de publicar:",\n                    item["titulo"]\n                )\n\n                sustituido = (\n                    reemplazar_duplicado_por_anuncio_nuevo(\n                        item\n                    )\n                )\n\n                if not sustituido:\n                    item["estado_cola"] = "ERROR"\n                    item["error_fase"] = (\n                        "Sustitución automática de duplicado"\n                    )\n                    item["error_motivo"] = (\n                        "Se detectó un duplicado, pero no pude "\n                        "crear un sustituto nuevo después de varios intentos."\n                    )\n                    item["error_fecha"] = time.strftime(\n                        "%d/%m/%Y %H:%M:%S"\n                    )\n\n                    guardar_cola_en_disco()\n                    ventana.after(\n                        0,\n                        refrescar_cola\n                    )\n                    continue\n\n                # El item ya contiene título, descripción, imagen y carpeta nuevos.\n                print(\n                    "Continuando con el sustituto:",\n                    item["titulo"]\n                )\n\n            inicio_anuncio_rendimiento = time.time()\n\n            item["estado_cola"] = "PUBLICANDO"\n            item["error_fase"] = ""\n            item["error_motivo"] = ""\n            item["error_fecha"] = ""\n            item["error_diagnostico"] = ""\n            item["intentos"] = int(item.get("intentos", 0))\n            guardar_cola_en_disco()\n\n            ventana.after(\n                0,\n                lambda i=indice, t=total: estado.configure(\n                    text=f"⏳ Cola: publicando {i} de {t}..."\n                )\n            )\n            ventana.after(0, refrescar_cola)\n\n            ok = False\n            max_intentos = 3  # intento inicial + 2 reintentos seguros\n\n            for numero_intento in range(1, max_intentos + 1):\n                if cola_cancelar:\n                    break\n\n                item["intentos"] = numero_intento\n                guardar_cola_en_disco()\n\n                ventana.after(\n                    0,\n                    lambda n=numero_intento, m=max_intentos: estado.configure(\n                        text=f"⏳ Publicando · intento {n}/{m}"\n                    )\n                )\n\n                ok = publicar_item_con_recuperacion(\n                    item\n                )\n\n                if ok:\n                    break\n\n                fase_fallo = (\n                    ultimo_error_cola.get("fase")\n                    or "Publicación"\n                )\n\n                # Si ya se pulsó Publicar o estamos en post-publicación,\n                # NO reintentamos por riesgo de duplicar.\n                if not fase_segura_para_reintento(fase_fallo):\n                    print(\n                        "No se reintenta automáticamente porque el fallo "\n                        f"ocurrió en una fase no segura: {fase_fallo}"\n                    )\n                    break\n\n                if numero_intento < max_intentos:\n                    espera_reintento = 4 + numero_intento * 2\n                    print(\n                        f"Reintento automático en {espera_reintento}s "\n                        f"por fallo en {fase_fallo}..."\n                    )\n\n                    ventana.after(\n                        0,\n                        lambda f=fase_fallo, s=espera_reintento: estado.configure(\n                            text=(\n                                f"⚠️ Fallo en {f}. "\n                                f"Reintentando en {s}s..."\n                            )\n                        )\n                    )\n\n                    # Pausa corta y segura antes de reiniciar el flujo.\n                    for _ in range(espera_reintento):\n                        if cola_cancelar:\n                            break\n                        time.sleep(1)\n\n            # Si Wallapop devuelve específicamente un error de duplicado,\n            # generamos un anuncio nuevo y lo intentamos inmediatamente.\n            if (\n                not ok\n                and _es_error_duplicado(\n                    ultimo_error_cola.get(\n                        "mensaje",\n                        ""\n                    )\n                )\n            ):\n                incrementar_estadistica(\n                    "duplicados_bloqueados"\n                )\n\n                print(\n                    "Wallapop indicó duplicado. "\n                    "Creando sustituto automático..."\n                )\n\n                sustituido = (\n                    reemplazar_duplicado_por_anuncio_nuevo(\n                        item\n                    )\n                )\n\n                if sustituido:\n                    ventana.after(\n                        0,\n                        lambda: estado.configure(\n                            text=(\n                                "♻️ Duplicado detectado por Wallapop. "\n                                "Publicando el sustituto nuevo..."\n                            )\n                        )\n                    )\n\n                    ok = publicar_item_con_recuperacion(\n                        item\n                    )\n\n            # Si Wallapop indica que el catálogo está lleno,\n            # liberamos UN hueco antiguo y reintentamos el mismo anuncio.\n            if (\n                not ok\n                and _es_error_limite_catalogo(\n                    ultimo_error_cola.get(\n                        "mensaje",\n                        ""\n                    )\n                )\n            ):\n                print(\n                    "Límite de catálogo detectado por Wallapop. "\n                    "Iniciando rotación automática..."\n                )\n\n                rotado, detalle_rotacion = (\n                    gestionar_rotacion_limite_wallapop(\n                        item\n                    )\n                )\n\n                if rotado:\n                    cfg_rotacion = (\n                        cargar_config_rotacion()\n                    )\n\n                    espera_rotacion = max(\n                        0,\n                        int(\n                            cfg_rotacion.get(\n                                "espera_despues_borrar",\n                                3\n                            )\n                        )\n                    )\n\n                    for _ in range(\n                        espera_rotacion\n                    ):\n                        if cola_cancelar:\n                            break\n\n                        time.sleep(\n                            1\n                        )\n\n                    if (\n                        not cola_cancelar\n                        and cfg_rotacion.get(\n                            "reintentar_publicacion",\n                            True\n                        )\n                    ):\n                        # El reintento se realiza desde cero, con una nueva\n                        # pestaña, después de confirmar que se borró un anuncio.\n                        ok = publicar_item_con_recuperacion(\n                            item\n                        )\n\n                        if ok:\n                            incrementar_estadistica(\n                                "rotaciones_catalogo"\n                            )\n\n                            _registrar_evento_rotacion(\n                                "REINTENTO_PUBLICADO",\n                                None,\n                                item.get(\n                                    "titulo",\n                                    ""\n                                )\n                            )\n\n                else:\n                    print(\n                        "No se pudo rotar el catálogo:",\n                        detalle_rotacion\n                    )\n\n                    ultimo_error_cola[\n                        "mensaje"\n                    ] = (\n                        ultimo_error_cola.get(\n                            "mensaje",\n                            ""\n                        )\n                        + " · Rotación: "\n                        + detalle_rotacion\n                    )\n\n            if ok and modo_prueba_activado():\n                item["estado_cola"] = "PENDIENTE"\n                item["error_fase"] = ""\n                item["error_motivo"] = ""\n                item["error_fecha"] = ""\n                item["error_diagnostico"] = ""\n\n                print(\n                    "Modo prueba completado. "\n                    "El anuncio permanece PENDIENTE."\n                )\n\n                # En modo prueba detenemos la cola tras el primer anuncio\n                # para que el usuario pueda revisar el resultado.\n                cola_cancelar = True\n\n            elif ok:\n                item["estado_cola"] = "PUBLICADO"\n                item["error_fase"] = ""\n                item["error_motivo"] = ""\n                item["error_fecha"] = ""\n                item["error_diagnostico"] = ""\n            else:\n                item["estado_cola"] = "ERROR"\n                item["error_fase"] = (\n                    ultimo_error_cola.get("fase")\n                    or "Publicación"\n                )\n                item["error_motivo"] = (\n                    ultimo_error_cola.get("mensaje")\n                    or "Wallapop no completó la publicación."\n                )\n                item["error_fecha"] = (\n                    ultimo_error_cola.get("fecha")\n                    or time.strftime("%d/%m/%Y %H:%M:%S")\n                )\n                item["error_diagnostico"] = (\n                    ultimo_error_cola.get("diagnostico")\n                    or ""\n                )\n\n                registrar_fallo_item(\n                    item,\n                    item["error_fase"],\n                    item["error_motivo"]\n                )\n\n                if evaluar_cuarentena_item(item):\n                    print(\n                        "Anuncio enviado a CUARENTENA:",\n                        item.get("titulo", "Sin título")\n                    )\n\n            guardar_cola_en_disco()\n\n            rendimiento_ultimo_tiempo = max(\n                0.0,\n                time.time() - inicio_anuncio_rendimiento\n            )\n            rendimiento_tiempos_publicacion.append(\n                rendimiento_ultimo_tiempo\n            )\n            rendimiento_anuncios_procesados += 1\n\n            # Mantener solo las últimas 100 muestras.\n            if len(rendimiento_tiempos_publicacion) > 100:\n                del rendimiento_tiempos_publicacion[:-100]\n\n            print(\n                f"Rendimiento: anuncio procesado en "\n                f"{rendimiento_ultimo_tiempo:.1f}s"\n            )\n\n            mantenimiento_rendimiento_cola(\n                rendimiento_anuncios_procesados\n            )\n\n            _guardar_checkpoint_cola(\n                item,\n                "Anuncio terminado"\n            )\n\n            if item["estado_cola"] == "PUBLICADO":\n                registrar_actividad(\n                    "Publicado",\n                    item.get("titulo", "")\n                )\n                errores_consecutivos = 0\n                cola_publicados_sesion += 1\n                registrar_publicacion_diaria()\n                archivar_publicado(item)\n\n            elif item["estado_cola"] == "CUARENTENA":\n                cola_errores_sesion += 1\n\n                if _config_cuarentena()["continuar"]:\n                    errores_consecutivos = 0\n                    print(\n                        "Cuarentena activa: la cola continuará con el siguiente anuncio sano."\n                    )\n                else:\n                    errores_consecutivos += 1\n\n            else:\n                errores_consecutivos += 1\n                cola_errores_sesion += 1\n\n            if item["estado_cola"] != "PUBLICADO":\n                limite_errores = obtener_max_errores_consecutivos()\n\n                # Un anuncio aislado no debe detener toda la cola\n                # si se ha configurado continuar tras cuarentena.\n                ignorar_limite = (\n                    item["estado_cola"] == "CUARENTENA"\n                    and _config_cuarentena()["continuar"]\n                )\n\n                if (\n                    not ignorar_limite\n                    and errores_consecutivos >= limite_errores\n                ):\n                    cola_cancelar = True\n\n                    print(\n                        "Cola detenida por demasiados errores consecutivos:",\n                        errores_consecutivos\n                    )\n\n                    ventana.after(\n                        0,\n                        lambda n=errores_consecutivos: estado.configure(\n                            text=(\n                                f"⛔ Cola detenida tras {n} errores consecutivos. "\n                                "Revisa los diagnósticos antes de continuar."\n                            )\n                        )\n                    )\n\n            ventana.after(0, refrescar_cola)\n            ventana.after(0, refrescar_historial)\n\n            # Si hubo error, dar tiempo a Wallapop/navegador para recuperarse.\n            if (\n                item["estado_cola"] == "ERROR"\n                and not cola_cancelar\n            ):\n                espera_error = obtener_espera_despues_error()\n\n                if espera_error > 0:\n                    print(\n                        f"Enfriamiento tras error: {espera_error} segundos."\n                    )\n\n                    ventana.after(\n                        0,\n                        lambda s=espera_error: estado.configure(\n                            text=(\n                                f"⚠️ Error detectado. Esperando {s}s "\n                                "antes del siguiente anuncio..."\n                            )\n                        )\n                    )\n\n                    for _ in range(espera_error):\n                        if cola_cancelar:\n                            break\n                        time.sleep(1)\n\n            # Pausa prudente entre anuncios para evitar carreras de interfaz\n            # y dar tiempo a Wallapop a terminar la navegación.\n            if not cola_cancelar:\n                pausa = obtener_pausa_adaptativa_publicaciones()\n                print(\n                    f"Pausa entre publicaciones: {pausa} segundos "\n                    f"(ajuste adaptativo {ritmo_adaptativo_ajuste:+d}s)."\n                )\n\n                ventana.after(\n                    0,\n                    lambda p=pausa: estado.configure(\n                        text=f"⏳ Esperando {p}s antes del siguiente anuncio..."\n                    )\n                )\n\n                for _ in range(pausa):\n                    if cola_cancelar:\n                        break\n                    time.sleep(1)\n\n    finally:\n        cola_en_ejecucion = False\n\n        try:\n            ventana.after(\n                0,\n                lambda: boton_iniciar_cola.configure(state="normal")\n            )\n            ventana.after(\n                0,\n                lambda: boton_cancelar_cola.configure(state="disabled")\n            )\n\n            try:\n                ventana.after(\n                    0,\n                    lambda: boton_pausar_cola.configure(state="disabled")\n                )\n                ventana.after(\n                    0,\n                    lambda: boton_reanudar_cola.configure(state="disabled")\n                )\n            except Exception:\n                pass\n\n            ventana.after(0, refrescar_cola)\n        except Exception:\n            pass\n\n        if cola_cancelar:\n            ventana.after(\n                0,\n                lambda: estado.configure(\n                    text="⏹ Cola detenida. Los anuncios restantes siguen pendientes."\n                )\n            )\n        else:\n            ventana.after(\n                0,\n                lambda: estado.configure(\n                    text="✅ Cola terminada."\n                )\n            )\n\n        try:\n            ventana.after(\n                300,\n                mostrar_resumen_sesion_cola\n            )\n        except Exception:\n            pass\n\n        try:\n            ruta_informe = generar_informe_sesion_automatico()\n            if ruta_informe:\n                print(\n                    "Informe automático de sesión:",\n                    ruta_informe\n                )\n        except Exception:\n            pass\n\n        try:\n            if bool(\n                cargar_configuracion_general().get(\n                    "autoarchivar_publicados_fin",\n                    False\n                )\n            ):\n                total_archivados = autoarchivar_publicados()\n                print(\n                    "Autoarchivo fin de sesión:",\n                    total_archivados\n                )\n        except Exception:\n            pass\n\n\n# =========================================================\n# PLANTILLAS DE PUBLICACIÓN\n# =========================================================\n\nARCHIVO_PLANTILLAS = "plantillas_publicacion.json"\nplantillas_publicacion = {}\n\n\ndef _plantilla_tuning_por_defecto():\n    return {\n        "categoria": (\n            "Motor y accesorios > "\n            "Recambios de coches y furgonetas > "\n            "Tuning"\n        ),\n        "estado": "Nuevo",\n        "desactivar_envio": True\n    }\n\n\ndef cargar_plantillas():\n    """\n    Carga las plantillas guardadas. Si todavía no existen,\n    crea una plantilla inicial con la configuración que ya sabemos que funciona.\n    """\n    global plantillas_publicacion\n\n    datos = {}\n\n    if os.path.isfile(ARCHIVO_PLANTILLAS):\n        try:\n            with open(\n                ARCHIVO_PLANTILLAS,\n                "r",\n                encoding="utf-8"\n            ) as archivo:\n                cargado = json.load(archivo)\n\n            if isinstance(cargado, dict):\n                datos = cargado\n\n        except Exception as e:\n            print("Aviso: no pude cargar plantillas:", e)\n\n    if "Tuning" not in datos:\n        datos["Tuning"] = _plantilla_tuning_por_defecto()\n\n    plantillas_publicacion = datos\n    guardar_plantillas()\n\n\ndef guardar_plantillas():\n    try:\n        with open(\n            ARCHIVO_PLANTILLAS,\n            "w",\n            encoding="utf-8"\n        ) as archivo:\n            json.dump(\n                plantillas_publicacion,\n                archivo,\n                ensure_ascii=False,\n                indent=4\n            )\n        return True\n\n    except Exception as e:\n        print("Aviso: no pude guardar plantillas:", e)\n        return False\n\n\ndef nombres_plantillas():\n    nombres = sorted(\n        plantillas_publicacion.keys(),\n        key=str.lower\n    )\n    return nombres or ["Sin plantillas"]\n\n\ndef actualizar_selector_plantillas():\n    try:\n        valores = nombres_plantillas()\n        menu_plantillas.configure(values=valores)\n\n        actual = menu_plantillas.get().strip()\n        if actual not in valores:\n            menu_plantillas.set(valores[0])\n\n    except Exception:\n        pass\n\n\ndef aplicar_plantilla(nombre=None):\n    if nombre is None:\n        try:\n            nombre = menu_plantillas.get().strip()\n        except Exception:\n            nombre = ""\n\n    if not nombre or nombre == "Sin plantillas":\n        estado.configure(text="❌ No hay ninguna plantilla seleccionada.")\n        return\n\n    datos = plantillas_publicacion.get(nombre)\n\n    if not isinstance(datos, dict):\n        estado.configure(text="❌ No encuentro esa plantilla.")\n        return\n\n    categoria_publicacion.set(\n        datos.get(\n            "categoria",\n            "Motor y accesorios > Recambios de coches y furgonetas > Tuning"\n        )\n    )\n\n    estado_producto_publicacion.set(\n        datos.get("estado", "Nuevo")\n    )\n\n    try:\n        switch_desactivar_envio.set(\n            bool(datos.get("desactivar_envio", True))\n        )\n    except Exception:\n        pass\n\n    refrescar_resumen_publicacion()\n\n    estado.configure(\n        text=f"✅ Plantilla aplicada: {nombre}"\n    )\n\n\ndef guardar_plantilla_actual():\n    try:\n        nombre = entrada_nombre_plantilla.get().strip()\n    except Exception:\n        nombre = ""\n\n    if not nombre:\n        estado.configure(\n            text="❌ Escribe un nombre para la plantilla."\n        )\n        return\n\n    if len(nombre) > 40:\n        estado.configure(\n            text="❌ El nombre de la plantilla es demasiado largo."\n        )\n        return\n\n    categoria = categoria_publicacion.get().strip()\n    estado_producto = estado_producto_publicacion.get().strip()\n\n    if not categoria:\n        estado.configure(\n            text="❌ No puedes guardar una plantilla sin categoría."\n        )\n        return\n\n    if not estado_producto:\n        estado.configure(\n            text="❌ No puedes guardar una plantilla sin estado."\n        )\n        return\n\n    plantillas_publicacion[nombre] = {\n        "categoria": categoria,\n        "estado": estado_producto,\n        "desactivar_envio": bool(\n            switch_desactivar_envio.get()\n        )\n    }\n\n    guardar_plantillas()\n    actualizar_selector_plantillas()\n\n    try:\n        menu_plantillas.set(nombre)\n    except Exception:\n        pass\n\n    estado.configure(\n        text=f"✅ Plantilla guardada: {nombre}"\n    )\n\n\ndef eliminar_plantilla_actual():\n    try:\n        nombre = menu_plantillas.get().strip()\n    except Exception:\n        nombre = ""\n\n    if not nombre or nombre == "Sin plantillas":\n        estado.configure(text="❌ No hay plantilla seleccionada.")\n        return\n\n    if nombre == "Tuning":\n        estado.configure(\n            text="⚠️ La plantilla Tuning base no se elimina."\n        )\n        return\n\n    if nombre not in plantillas_publicacion:\n        estado.configure(text="❌ No encuentro esa plantilla.")\n        return\n\n    plantillas_publicacion.pop(nombre, None)\n    guardar_plantillas()\n    actualizar_selector_plantillas()\n\n    estado.configure(\n        text=f"Plantilla eliminada: {nombre}"\n    )\n\n\ndef duplicar_plantilla_actual():\n    try:\n        origen = menu_plantillas.get().strip()\n        nuevo_nombre = entrada_nombre_plantilla.get().strip()\n    except Exception:\n        origen = ""\n        nuevo_nombre = ""\n\n    if not origen or origen == "Sin plantillas":\n        estado.configure(text="❌ Selecciona una plantilla.")\n        return\n\n    if not nuevo_nombre:\n        estado.configure(\n            text="❌ Escribe el nombre de la nueva plantilla."\n        )\n        return\n\n    datos = plantillas_publicacion.get(origen)\n    if not isinstance(datos, dict):\n        estado.configure(text="❌ No encuentro la plantilla original.")\n        return\n\n    plantillas_publicacion[nuevo_nombre] = dict(datos)\n    guardar_plantillas()\n    actualizar_selector_plantillas()\n\n    try:\n        menu_plantillas.set(nuevo_nombre)\n    except Exception:\n        pass\n\n    estado.configure(\n        text=f"✅ Plantilla duplicada como: {nuevo_nombre}"\n    )\n\n\n# =========================================================\n# BORRADORES Y AUTOGUARDADO\n# =========================================================\n\nARCHIVO_BORRADOR = "borrador_actual.json"\n_autoguardado_programado = None\n\n\ndef _datos_borrador_actual():\n    """\n    Reúne los campos principales del anuncio actual.\n    """\n    try:\n        titulo = caja_titulo.get("1.0", "end-1c").strip()\n    except Exception:\n        titulo = ""\n\n    try:\n        descripcion = caja_descripcion.get("1.0", "end-1c").strip()\n    except Exception:\n        descripcion = ""\n\n    try:\n        servicio = entrada_servicio.get().strip()\n    except Exception:\n        servicio = ""\n\n    try:\n        precio = entrada_precio.get().strip()\n    except Exception:\n        precio = ""\n\n    try:\n        contacto = entrada_contacto.get().strip()\n    except Exception:\n        contacto = ""\n\n    try:\n        categoria = categoria_publicacion.get().strip()\n    except Exception:\n        categoria = ""\n\n    try:\n        estado_producto = estado_producto_publicacion.get().strip()\n    except Exception:\n        estado_producto = "Nuevo"\n\n    try:\n        envio_desactivado = bool(switch_desactivar_envio.get())\n    except Exception:\n        envio_desactivado = True\n\n    try:\n        contacto_imagenes = bool(var_contacto_imagenes.get())\n    except Exception:\n        contacto_imagenes = False\n\n    try:\n        contacto_descripcion = bool(var_contacto_descripcion.get())\n    except Exception:\n        contacto_descripcion = False\n\n    return {\n        "servicio": servicio,\n        "precio": precio,\n        "contacto": contacto,\n        "titulo": titulo,\n        "descripcion": descripcion,\n        "categoria": categoria,\n        "estado_producto": estado_producto,\n        "envio_desactivado": envio_desactivado,\n        "contacto_imagenes": contacto_imagenes,\n        "contacto_descripcion": contacto_descripcion,\n        "ultima_carpeta_anuncio": ultima_carpeta_anuncio or "",\n        "guardado_en": time.strftime("%d/%m/%Y %H:%M:%S"),\n        "timestamp": time.time()\n    }\n\n\ndef guardar_borrador(silencioso=False):\n    """\n    Guarda el trabajo actual sin interferir con la interfaz.\n    """\n    try:\n        datos = _datos_borrador_actual()\n\n        with open(\n            ARCHIVO_BORRADOR,\n            "w",\n            encoding="utf-8"\n        ) as archivo:\n            json.dump(\n                datos,\n                archivo,\n                ensure_ascii=False,\n                indent=4\n            )\n\n        if not silencioso:\n            estado.configure(\n                text="✅ Borrador guardado."\n            )\n\n        return True\n\n    except Exception as e:\n        print("Aviso: no pude guardar borrador:", e)\n\n        if not silencioso:\n            estado.configure(\n                text="❌ No pude guardar el borrador."\n            )\n\n        return False\n\n\ndef programar_autoguardado(event=None):\n    """\n    Evita guardar en cada tecla. Espera un poco y guarda\n    cuando el usuario deja de escribir.\n    """\n    global _autoguardado_programado\n\n    try:\n        if _autoguardado_programado is not None:\n            ventana.after_cancel(_autoguardado_programado)\n    except Exception:\n        pass\n\n    try:\n        _autoguardado_programado = ventana.after(\n            800,\n            lambda: guardar_borrador(silencioso=True)\n        )\n    except Exception:\n        pass\n\n\ndef cargar_borrador():\n    """\n    Recupera el último borrador guardado.\n    """\n    global ultima_carpeta_anuncio\n\n    if not os.path.isfile(ARCHIVO_BORRADOR):\n        estado.configure(\n            text="No hay ningún borrador guardado."\n        )\n        return False\n\n    try:\n        with open(\n            ARCHIVO_BORRADOR,\n            "r",\n            encoding="utf-8"\n        ) as archivo:\n            datos = json.load(archivo)\n\n        if not isinstance(datos, dict):\n            raise RuntimeError("El archivo de borrador no es válido.")\n\n        entrada_servicio.delete(0, "end")\n        entrada_servicio.insert(\n            0,\n            datos.get("servicio", "")\n        )\n\n        entrada_precio.delete(0, "end")\n        entrada_precio.insert(\n            0,\n            datos.get("precio", "")\n        )\n\n        entrada_contacto.delete(0, "end")\n        entrada_contacto.insert(\n            0,\n            datos.get("contacto", "")\n        )\n\n        caja_titulo.delete("1.0", "end")\n        caja_titulo.insert(\n            "1.0",\n            datos.get("titulo", "")\n        )\n\n        caja_descripcion.delete("1.0", "end")\n        caja_descripcion.insert(\n            "1.0",\n            datos.get("descripcion", "")\n        )\n\n        categoria_publicacion.set(\n            datos.get(\n                "categoria",\n                "Motor y accesorios > Recambios de coches y furgonetas > Tuning"\n            )\n        )\n\n        estado_producto_publicacion.set(\n            datos.get("estado_producto", "Nuevo")\n        )\n\n        try:\n            switch_desactivar_envio.set(\n                bool(datos.get("envio_desactivado", True))\n            )\n        except Exception:\n            pass\n\n        try:\n            var_contacto_imagenes.set(\n                bool(datos.get("contacto_imagenes", False))\n            )\n        except Exception:\n            pass\n\n        try:\n            var_contacto_descripcion.set(\n                bool(datos.get("contacto_descripcion", False))\n            )\n        except Exception:\n            pass\n\n        carpeta = datos.get("ultima_carpeta_anuncio", "")\n        if carpeta and os.path.isdir(carpeta):\n            ultima_carpeta_anuncio = carpeta\n\n        actualizar_contadores_profesionales()\n        refrescar_resumen_publicacion()\n\n        estado.configure(\n            text=(\n                "✅ Borrador recuperado · "\n                + datos.get("guardado_en", "sin fecha")\n            )\n        )\n\n        return True\n\n    except Exception as e:\n        estado.configure(\n            text="❌ No pude recuperar el borrador: " + str(e)\n        )\n        return False\n\n\ndef eliminar_borrador():\n    try:\n        if os.path.isfile(ARCHIVO_BORRADOR):\n            os.remove(ARCHIVO_BORRADOR)\n\n        estado.configure(\n            text="Borrador eliminado."\n        )\n\n    except Exception as e:\n        estado.configure(\n            text="❌ No pude eliminar el borrador: " + str(e)\n        )\n\n\ndef recuperar_borrador_al_iniciar():\n    """\n    Recupera automáticamente si existe contenido útil.\n    """\n    if not os.path.isfile(ARCHIVO_BORRADOR):\n        return\n\n    try:\n        with open(\n            ARCHIVO_BORRADOR,\n            "r",\n            encoding="utf-8"\n        ) as archivo:\n            datos = json.load(archivo)\n\n        if not isinstance(datos, dict):\n            return\n\n        tiene_contenido = any([\n            datos.get("servicio"),\n            datos.get("titulo"),\n            datos.get("descripcion"),\n            datos.get("precio"),\n            datos.get("contacto"),\n        ])\n\n        if not tiene_contenido:\n            return\n\n        respuesta = messagebox.askyesno(\n            "Recuperar borrador",\n            "Hay un borrador guardado de una sesión anterior.\\n\\n"\n            "¿Quieres recuperarlo ahora?"\n        )\n\n        if respuesta:\n            cargar_borrador()\n\n    except Exception:\n        pass\n\n\n# =========================================================\n# ESTADÍSTICAS DEL BOT\n# =========================================================\n\nARCHIVO_ESTADISTICAS = "estadisticas_bot.json"\n\n\ndef _estadisticas_base():\n    return {\n        "generados": 0,\n        "preparados": 0,\n        "publicados": 0,\n        "fallidos": 0,\n        "duplicados_bloqueados": 0,\n        "publicados_desde_cola": 0,\n        "errores_cola": 0,\n        "ultima_actualizacion": ""\n    }\n\n\ndef cargar_estadisticas():\n    datos = _estadisticas_base()\n\n    if os.path.isfile(ARCHIVO_ESTADISTICAS):\n        try:\n            with open(\n                ARCHIVO_ESTADISTICAS,\n                "r",\n                encoding="utf-8"\n            ) as archivo:\n                cargado = json.load(archivo)\n\n            if isinstance(cargado, dict):\n                datos.update(cargado)\n        except Exception:\n            pass\n\n    return datos\n\n\ndef guardar_estadisticas(datos):\n    datos = dict(datos)\n    datos["ultima_actualizacion"] = time.strftime(\n        "%d/%m/%Y %H:%M:%S"\n    )\n\n    try:\n        with open(\n            ARCHIVO_ESTADISTICAS,\n            "w",\n            encoding="utf-8"\n        ) as archivo:\n            json.dump(\n                datos,\n                archivo,\n                ensure_ascii=False,\n                indent=4\n            )\n    except Exception as e:\n        print("Aviso: no pude guardar estadísticas:", e)\n\n\ndef incrementar_estadistica(clave, cantidad=1):\n    datos = cargar_estadisticas()\n    datos[clave] = int(datos.get(clave, 0)) + cantidad\n    guardar_estadisticas(datos)\n\n    try:\n        refrescar_estadisticas()\n    except Exception:\n        pass\n\n\ndef _contar_estados_historial():\n    conteo = {\n        "GENERADO": 0,\n        "PREPARADO": 0,\n        "PUBLICADO": 0\n    }\n\n    base = Path(CARPETA_ANUNCIOS)\n    if not base.exists():\n        return conteo\n\n    try:\n        for carpeta in base.iterdir():\n            if not carpeta.is_dir():\n                continue\n\n            datos = leer_estado_anuncio(carpeta)\n            estado_actual = str(\n                datos.get("estado", "GENERADO")\n            ).upper()\n\n            if estado_actual in conteo:\n                conteo[estado_actual] += 1\n    except Exception:\n        pass\n\n    return conteo\n\n\ndef _contar_cola():\n    resultado = {\n        "total": 0,\n        "pendientes": 0,\n        "publicando": 0,\n        "publicados": 0,\n        "errores": 0\n    }\n\n    try:\n        resultado["total"] = len(cola_anuncios)\n\n        for item in cola_anuncios:\n            estado_item = item.get(\n                "estado_cola",\n                "PENDIENTE"\n            )\n\n            if estado_item == "PENDIENTE":\n                resultado["pendientes"] += 1\n            elif estado_item == "PUBLICANDO":\n                resultado["publicando"] += 1\n            elif estado_item == "PUBLICADO":\n                resultado["publicados"] += 1\n            elif estado_item == "ERROR":\n                resultado["errores"] += 1\n\n    except Exception:\n        pass\n\n    return resultado\n\n\ndef refrescar_estadisticas():\n    try:\n        historico = cargar_estadisticas()\n        estados = _contar_estados_historial()\n        cola = _contar_cola()\n\n        texto = (\n            "RESUMEN GENERAL\\n"\n            "==============================\\n\\n"\n            f"Anuncios generados guardados: {estados[\'GENERADO\']}\\n"\n            f"Anuncios preparados:          {estados[\'PREPARADO\']}\\n"\n            f"Anuncios publicados:          {estados[\'PUBLICADO\']}\\n\\n"\n            "ACTIVIDAD DEL BOT\\n"\n            "==============================\\n\\n"\n            f"Generaciones realizadas:      {historico.get(\'generados\', 0)}\\n"\n            f"Preparaciones realizadas:     {historico.get(\'preparados\', 0)}\\n"\n            f"Publicaciones realizadas:     {historico.get(\'publicados\', 0)}\\n"\n            f"Fallos detectados:            {historico.get(\'fallidos\', 0)}\\n"\n            f"Duplicados bloqueados:        {historico.get(\'duplicados_bloqueados\', 0)}\\n"\n            f"Publicados desde cola:        {historico.get(\'publicados_desde_cola\', 0)}\\n"\n            f"Errores en cola:              {historico.get(\'errores_cola\', 0)}\\n\\n"\n            "COLA\\n"\n            "==============================\\n\\n"\n            f"Total en cola:                {cola[\'total\']}\\n"\n            f"Pendientes:                    {cola[\'pendientes\']}\\n"\n            f"Publicando:                    {cola[\'publicando\']}\\n"\n            f"Publicados en cola:            {cola[\'publicados\']}\\n"\n            f"Con error:                     {cola[\'errores\']}\\n\\n"\n            f"Última actualización: "\n            f"{historico.get(\'ultima_actualizacion\', \'—\')}"\n        )\n\n        estadisticas_texto.configure(state="normal")\n        estadisticas_texto.delete("1.0", "end")\n        estadisticas_texto.insert("1.0", texto)\n        estadisticas_texto.configure(state="disabled")\n\n    except Exception as e:\n        print("Aviso: no pude refrescar estadísticas:", e)\n\n\ndef reiniciar_estadisticas():\n    try:\n        respuesta = messagebox.askyesno(\n            "Reiniciar estadísticas",\n            "Esto pondrá a cero los contadores de actividad del bot.\\n\\n"\n            "No borra anuncios, imágenes, historial ni cola.\\n\\n"\n            "¿Continuar?"\n        )\n\n        if not respuesta:\n            return\n\n        guardar_estadisticas(\n            _estadisticas_base()\n        )\n\n        refrescar_estadisticas()\n\n        estado.configure(\n            text="Estadísticas de actividad reiniciadas."\n        )\n\n    except Exception as e:\n        estado.configure(\n            text="❌ No pude reiniciar las estadísticas: " + str(e)\n        )\n\n\n# =========================================================\n# CONFIGURACIÓN GENERAL\n# =========================================================\n\nARCHIVO_CONFIG = "configuracion_bot.json"\n\n\ndef configuracion_por_defecto():\n    return {\n        "pausa_entre_publicaciones": 8,\n        "pausa_min_publicaciones": 8,\n        "pausa_max_publicaciones": 15,\n        "max_errores_consecutivos": 3,\n        "espera_despues_error": 20,\n        "max_publicaciones_sesion": 10,\n        "watchdog_minutos": 4,\n        "modo_rendimiento": True,\n        "limpiar_pestanas_cada": 1,\n        "limpiar_memoria_cada": 3,\n        "timeout_respuesta_wallapop": 12,\n        "auto_recuperar_navegador": True,\n        "max_recuperaciones_anuncio": 2,\n        "espera_recuperacion_navegador": 5,\n        "supervisor_automatico": True,\n        "pausar_si_supervisor_falla": True,\n        "max_intentos_supervisor": 2,\n        "modo_prueba": False,\n        "limite_publicaciones_dia": 25,\n        "hora_inicio_programada": "",\n        "dias_retencion_archivos": 14,\n        "estrategia_publicacion": "Normal",\n        "ritmo_adaptativo": True,\n        "exitos_para_acelerar": 3,\n        "errores_para_frenar": 1,\n        "max_ajuste_pausa": 6,\n        "anti_duplicado_visual": True,\n        "umbral_similitud_visual": 10,\n        "max_regeneraciones_visuales": 4,\n        "auditoria_antes_de_publicar": True,\n        "autorreparar_auditoria": True,\n        "bloquear_si_hay_criticos": True,\n        "calidad_minima_publicar": 70,\n        "cuarentena_automatica": True,\n        "fallos_repetidos_para_cuarentena": 2,\n        "continuar_tras_cuarentena": True,\n        "autoarchivar_publicados_fin": False,\n        "objetivo_publicaciones_sesion": 10,\n        "umbral_similitud_textual": 88,\n        "autoguardado_borrador_segundos": 30,\n        "nombre_sesion": "",\n        "registro_actividad": True,\n        "max_cola": 500,\n        "puerto_chromium": 9222,\n        "categoria_defecto": (\n            "Motor y accesorios > "\n            "Recambios de coches y furgonetas > "\n            "Tuning"\n        ),\n        "estado_defecto": "Nuevo",\n        "desactivar_envio_defecto": True\n    }\n\n\ndef cargar_configuracion_general():\n    datos = configuracion_por_defecto()\n\n    if os.path.isfile(ARCHIVO_CONFIG):\n        try:\n            with open(\n                ARCHIVO_CONFIG,\n                "r",\n                encoding="utf-8"\n            ) as archivo:\n                cargado = json.load(archivo)\n\n            if isinstance(cargado, dict):\n                datos.update(cargado)\n\n        except Exception as e:\n            print("Aviso: no pude cargar configuración:", e)\n\n    return datos\n\n\ndef guardar_configuracion_general():\n    try:\n        pausa = int(var_pausa_publicaciones.get())\n        pausa_min = int(var_pausa_min_publicaciones.get())\n        pausa_max = int(var_pausa_max_publicaciones.get())\n        max_errores = int(var_max_errores_consecutivos.get())\n        espera_error = int(var_espera_despues_error.get())\n        max_publicaciones_sesion = int(var_max_publicaciones_sesion.get())\n        watchdog_minutos = int(var_watchdog_minutos.get())\n        modo_rendimiento = bool(var_modo_rendimiento.get())\n        limpiar_pestanas_cada = int(var_limpiar_pestanas_cada.get())\n        limpiar_memoria_cada = int(var_limpiar_memoria_cada.get())\n        timeout_respuesta_wallapop = int(var_timeout_respuesta_wallapop.get())\n        auto_recuperar_navegador = bool(var_auto_recuperar_navegador.get())\n        max_recuperaciones_anuncio = int(var_max_recuperaciones_anuncio.get())\n        espera_recuperacion_navegador = int(var_espera_recuperacion_navegador.get())\n        supervisor_automatico = bool(var_supervisor_automatico.get())\n        pausar_si_supervisor_falla = bool(var_pausar_si_supervisor_falla.get())\n        max_intentos_supervisor = int(var_max_intentos_supervisor.get())\n        modo_prueba = bool(var_modo_prueba.get())\n        limite_publicaciones_dia = int(var_limite_publicaciones_dia.get())\n        hora_inicio_programada = var_hora_inicio_programada.get().strip()\n        dias_retencion_archivos = int(var_dias_retencion_archivos.get())\n        estrategia_publicacion = var_estrategia_publicacion.get().strip() or "Normal"\n        ritmo_adaptativo = bool(var_ritmo_adaptativo.get())\n        exitos_para_acelerar = int(var_exitos_para_acelerar.get())\n        errores_para_frenar = int(var_errores_para_frenar.get())\n        max_ajuste_pausa = int(var_max_ajuste_pausa.get())\n        anti_duplicado_visual = bool(var_anti_duplicado_visual.get())\n        umbral_similitud_visual = int(var_umbral_similitud_visual.get())\n        max_regeneraciones_visuales = int(var_max_regeneraciones_visuales.get())\n        auditoria_antes_de_publicar = bool(var_auditoria_antes_de_publicar.get())\n        autorreparar_auditoria = bool(var_autorreparar_auditoria.get())\n        bloquear_si_hay_criticos = bool(var_bloquear_si_hay_criticos.get())\n        calidad_minima_publicar = int(var_calidad_minima_publicar.get())\n        cuarentena_automatica = bool(var_cuarentena_automatica.get())\n        fallos_repetidos_para_cuarentena = int(\n            var_fallos_repetidos_para_cuarentena.get()\n        )\n        continuar_tras_cuarentena = bool(var_continuar_tras_cuarentena.get())\n        autoarchivar_publicados_fin = bool(var_autoarchivar_publicados_fin.get())\n        objetivo_publicaciones_sesion = int(var_objetivo_publicaciones_sesion.get())\n        umbral_similitud_textual = int(var_umbral_similitud_textual.get())\n        autoguardado_borrador_segundos = int(\n            var_autoguardado_borrador_segundos.get()\n        )\n        nombre_sesion = var_nombre_sesion.get().strip()\n        registro_actividad = bool(var_registro_actividad.get())\n        max_cola_cfg = int(var_max_cola.get())\n        puerto = int(var_puerto_chromium.get())\n\n        if pausa < 0 or pausa > 300:\n            raise ValueError(\n                "La pausa fija debe estar entre 0 y 300 segundos."\n            )\n\n        if pausa_min < 0 or pausa_min > 300:\n            raise ValueError(\n                "La pausa mínima debe estar entre 0 y 300 segundos."\n            )\n\n        if pausa_max < pausa_min or pausa_max > 300:\n            raise ValueError(\n                "La pausa máxima debe ser mayor o igual que la mínima "\n                "y no superar 300 segundos."\n            )\n\n        if max_errores < 1 or max_errores > 20:\n            raise ValueError(\n                "Los errores consecutivos deben estar entre 1 y 20."\n            )\n\n        if espera_error < 0 or espera_error > 300:\n            raise ValueError(\n                "La espera tras un error debe estar entre 0 y 300 segundos."\n            )\n\n        if max_publicaciones_sesion < 1 or max_publicaciones_sesion > 100:\n            raise ValueError(\n                "El máximo por sesión debe estar entre 1 y 100."\n            )\n\n        if watchdog_minutos < 1 or watchdog_minutos > 60:\n            raise ValueError(\n                "El watchdog debe estar entre 1 y 60 minutos."\n            )\n\n        if limpiar_pestanas_cada < 1 or limpiar_pestanas_cada > 50:\n            raise ValueError(\n                "La limpieza de pestañas debe estar entre 1 y 50 anuncios."\n            )\n\n        if limpiar_memoria_cada < 1 or limpiar_memoria_cada > 50:\n            raise ValueError(\n                "La limpieza de memoria debe estar entre 1 y 50 anuncios."\n            )\n\n        if timeout_respuesta_wallapop < 5 or timeout_respuesta_wallapop > 60:\n            raise ValueError(\n                "El timeout de Wallapop debe estar entre 5 y 60 segundos."\n            )\n\n        if max_recuperaciones_anuncio < 0 or max_recuperaciones_anuncio > 5:\n            raise ValueError(\n                "Las recuperaciones por anuncio deben estar entre 0 y 5."\n            )\n\n        if espera_recuperacion_navegador < 0 or espera_recuperacion_navegador > 60:\n            raise ValueError(\n                "La espera de recuperación debe estar entre 0 y 60 segundos."\n            )\n\n        if max_intentos_supervisor < 1 or max_intentos_supervisor > 5:\n            raise ValueError(\n                "Los intentos del supervisor deben estar entre 1 y 5."\n            )\n\n        if limite_publicaciones_dia < 1 or limite_publicaciones_dia > 500:\n            raise ValueError(\n                "El límite diario debe estar entre 1 y 500."\n            )\n\n        if hora_inicio_programada:\n            try:\n                time.strptime(\n                    hora_inicio_programada,\n                    "%H:%M"\n                )\n            except Exception:\n                raise ValueError(\n                    "La hora programada debe usar formato HH:MM, por ejemplo 09:30."\n                )\n\n        if dias_retencion_archivos < 1 or dias_retencion_archivos > 365:\n            raise ValueError(\n                "La retención debe estar entre 1 y 365 días."\n            )\n\n        if exitos_para_acelerar < 1 or exitos_para_acelerar > 20:\n            raise ValueError(\n                "Los éxitos para acelerar deben estar entre 1 y 20."\n            )\n\n        if errores_para_frenar < 1 or errores_para_frenar > 10:\n            raise ValueError(\n                "Los errores para frenar deben estar entre 1 y 10."\n            )\n\n        if max_ajuste_pausa < 0 or max_ajuste_pausa > 60:\n            raise ValueError(\n                "El ajuste máximo de pausa debe estar entre 0 y 60 segundos."\n            )\n\n        if umbral_similitud_visual < 0 or umbral_similitud_visual > 64:\n            raise ValueError(\n                "El umbral visual debe estar entre 0 y 64."\n            )\n\n        if max_regeneraciones_visuales < 0 or max_regeneraciones_visuales > 10:\n            raise ValueError(\n                "Las regeneraciones visuales deben estar entre 0 y 10."\n            )\n\n        if calidad_minima_publicar < 0 or calidad_minima_publicar > 100:\n            raise ValueError(\n                "La calidad mínima debe estar entre 0 y 100."\n            )\n\n        if fallos_repetidos_para_cuarentena < 1 or fallos_repetidos_para_cuarentena > 10:\n            raise ValueError(\n                "Los fallos repetidos para cuarentena deben estar entre 1 y 10."\n            )\n\n        if objetivo_publicaciones_sesion < 1 or objetivo_publicaciones_sesion > 500:\n            raise ValueError(\n                "El objetivo de publicaciones por sesión debe estar entre 1 y 500."\n            )\n\n        if umbral_similitud_textual < 50 or umbral_similitud_textual > 100:\n            raise ValueError(\n                "El umbral de similitud textual debe estar entre 50 y 100."\n            )\n\n        if autoguardado_borrador_segundos < 10 or autoguardado_borrador_segundos > 600:\n            raise ValueError(\n                "El autoguardado debe estar entre 10 y 600 segundos."\n            )\n\n        if max_cola_cfg < 1 or max_cola_cfg > 5000:\n            raise ValueError(\n                "El máximo de cola debe estar entre 1 y 5000."\n            )\n\n        if puerto < 1024 or puerto > 65535:\n            raise ValueError(\n                "El puerto debe estar entre 1024 y 65535."\n            )\n\n        datos = {\n            "pausa_entre_publicaciones": pausa,\n            "pausa_min_publicaciones": pausa_min,\n            "pausa_max_publicaciones": pausa_max,\n            "max_errores_consecutivos": max_errores,\n            "espera_despues_error": espera_error,\n            "max_publicaciones_sesion": max_publicaciones_sesion,\n            "watchdog_minutos": watchdog_minutos,\n            "modo_rendimiento": modo_rendimiento,\n            "limpiar_pestanas_cada": limpiar_pestanas_cada,\n            "limpiar_memoria_cada": limpiar_memoria_cada,\n            "timeout_respuesta_wallapop": timeout_respuesta_wallapop,\n            "auto_recuperar_navegador": auto_recuperar_navegador,\n            "max_recuperaciones_anuncio": max_recuperaciones_anuncio,\n            "espera_recuperacion_navegador": espera_recuperacion_navegador,\n            "supervisor_automatico": supervisor_automatico,\n            "pausar_si_supervisor_falla": pausar_si_supervisor_falla,\n            "max_intentos_supervisor": max_intentos_supervisor,\n            "modo_prueba": modo_prueba,\n            "limite_publicaciones_dia": limite_publicaciones_dia,\n            "hora_inicio_programada": hora_inicio_programada,\n            "dias_retencion_archivos": dias_retencion_archivos,\n            "estrategia_publicacion": estrategia_publicacion,\n            "ritmo_adaptativo": ritmo_adaptativo,\n            "exitos_para_acelerar": exitos_para_acelerar,\n            "errores_para_frenar": errores_para_frenar,\n            "max_ajuste_pausa": max_ajuste_pausa,\n            "anti_duplicado_visual": anti_duplicado_visual,\n            "umbral_similitud_visual": umbral_similitud_visual,\n            "max_regeneraciones_visuales": max_regeneraciones_visuales,\n            "auditoria_antes_de_publicar": auditoria_antes_de_publicar,\n            "autorreparar_auditoria": autorreparar_auditoria,\n            "bloquear_si_hay_criticos": bloquear_si_hay_criticos,\n            "calidad_minima_publicar": calidad_minima_publicar,\n            "cuarentena_automatica": cuarentena_automatica,\n            "fallos_repetidos_para_cuarentena": fallos_repetidos_para_cuarentena,\n            "continuar_tras_cuarentena": continuar_tras_cuarentena,\n            "autoarchivar_publicados_fin": autoarchivar_publicados_fin,\n            "objetivo_publicaciones_sesion": objetivo_publicaciones_sesion,\n            "umbral_similitud_textual": umbral_similitud_textual,\n            "autoguardado_borrador_segundos": autoguardado_borrador_segundos,\n            "nombre_sesion": nombre_sesion,\n            "registro_actividad": registro_actividad,\n            "max_cola": max_cola_cfg,\n            "puerto_chromium": puerto,\n            "categoria_defecto": var_categoria_defecto.get().strip(),\n            "estado_defecto": var_estado_defecto.get().strip(),\n            "desactivar_envio_defecto": bool(\n                var_envio_defecto.get()\n            )\n        }\n\n        with open(\n            ARCHIVO_CONFIG,\n            "w",\n            encoding="utf-8"\n        ) as archivo:\n            json.dump(\n                datos,\n                archivo,\n                ensure_ascii=False,\n                indent=4\n            )\n\n        aplicar_configuracion_general(datos)\n\n        estado.configure(\n            text="✅ Configuración guardada y aplicada."\n        )\n\n        return True\n\n    except Exception as e:\n        estado.configure(\n            text="❌ Configuración no válida: " + str(e)\n        )\n\n        try:\n            messagebox.showerror(\n                "Configuración",\n                str(e)\n            )\n        except Exception:\n            pass\n\n        return False\n\n\ndef aplicar_configuracion_general(datos=None):\n    global MAX_COLA\n\n    if datos is None:\n        datos = cargar_configuracion_general()\n\n    try:\n        if callable(\n            globals().get(\n                "capacidad_cola_actual"\n            )\n        ):\n            MAX_COLA = int(\n                capacidad_cola_actual()\n            )\n        else:\n            MAX_COLA = int(\n                datos.get(\n                    "max_cola",\n                    500\n                )\n            )\n    except Exception:\n        MAX_COLA = 500\n\n    # Ajustes por defecto de nuevos anuncios\n    try:\n        categoria_publicacion.set(\n            datos.get(\n                "categoria_defecto",\n                configuracion_por_defecto()["categoria_defecto"]\n            )\n        )\n    except Exception:\n        pass\n\n    try:\n        estado_producto_publicacion.set(\n            datos.get(\n                "estado_defecto",\n                "Nuevo"\n            )\n        )\n    except Exception:\n        pass\n\n    try:\n        switch_desactivar_envio.set(\n            bool(\n                datos.get(\n                    "desactivar_envio_defecto",\n                    True\n                )\n            )\n        )\n    except Exception:\n        pass\n\n    refrescar_resumen_publicacion()\n\n\ndef cargar_configuracion_en_interfaz():\n    datos = cargar_configuracion_general()\n\n    var_pausa_publicaciones.set(\n        str(datos.get("pausa_entre_publicaciones", 8))\n    )\n\n    var_pausa_min_publicaciones.set(\n        str(datos.get("pausa_min_publicaciones", 8))\n    )\n\n    var_pausa_max_publicaciones.set(\n        str(datos.get("pausa_max_publicaciones", 15))\n    )\n\n    var_max_errores_consecutivos.set(\n        str(datos.get("max_errores_consecutivos", 3))\n    )\n\n    var_espera_despues_error.set(\n        str(datos.get("espera_despues_error", 20))\n    )\n\n    var_max_publicaciones_sesion.set(\n        str(datos.get("max_publicaciones_sesion", 10))\n    )\n\n    var_watchdog_minutos.set(\n        str(datos.get("watchdog_minutos", 4))\n    )\n\n    var_modo_rendimiento.set(\n        bool(datos.get("modo_rendimiento", True))\n    )\n\n    var_limpiar_pestanas_cada.set(\n        str(datos.get("limpiar_pestanas_cada", 1))\n    )\n\n    var_limpiar_memoria_cada.set(\n        str(datos.get("limpiar_memoria_cada", 3))\n    )\n\n    var_timeout_respuesta_wallapop.set(\n        str(datos.get("timeout_respuesta_wallapop", 12))\n    )\n\n    var_auto_recuperar_navegador.set(\n        bool(datos.get("auto_recuperar_navegador", True))\n    )\n\n    var_max_recuperaciones_anuncio.set(\n        str(datos.get("max_recuperaciones_anuncio", 2))\n    )\n\n    var_espera_recuperacion_navegador.set(\n        str(datos.get("espera_recuperacion_navegador", 5))\n    )\n\n    var_supervisor_automatico.set(\n        bool(datos.get("supervisor_automatico", True))\n    )\n\n    var_pausar_si_supervisor_falla.set(\n        bool(datos.get("pausar_si_supervisor_falla", True))\n    )\n\n    var_max_intentos_supervisor.set(\n        str(datos.get("max_intentos_supervisor", 2))\n    )\n\n    var_modo_prueba.set(\n        bool(datos.get("modo_prueba", False))\n    )\n\n    var_limite_publicaciones_dia.set(\n        str(datos.get("limite_publicaciones_dia", 25))\n    )\n\n    var_hora_inicio_programada.set(\n        str(datos.get("hora_inicio_programada", ""))\n    )\n\n    var_dias_retencion_archivos.set(\n        str(datos.get("dias_retencion_archivos", 14))\n    )\n\n    var_estrategia_publicacion.set(\n        str(datos.get("estrategia_publicacion", "Normal"))\n    )\n\n    var_ritmo_adaptativo.set(\n        bool(datos.get("ritmo_adaptativo", True))\n    )\n\n    var_exitos_para_acelerar.set(\n        str(datos.get("exitos_para_acelerar", 3))\n    )\n\n    var_errores_para_frenar.set(\n        str(datos.get("errores_para_frenar", 1))\n    )\n\n    var_max_ajuste_pausa.set(\n        str(datos.get("max_ajuste_pausa", 6))\n    )\n\n    var_anti_duplicado_visual.set(\n        bool(datos.get("anti_duplicado_visual", True))\n    )\n\n    var_umbral_similitud_visual.set(\n        str(datos.get("umbral_similitud_visual", 10))\n    )\n\n    var_max_regeneraciones_visuales.set(\n        str(datos.get("max_regeneraciones_visuales", 4))\n    )\n\n    var_auditoria_antes_de_publicar.set(\n        bool(datos.get("auditoria_antes_de_publicar", True))\n    )\n\n    var_autorreparar_auditoria.set(\n        bool(datos.get("autorreparar_auditoria", True))\n    )\n\n    var_bloquear_si_hay_criticos.set(\n        bool(datos.get("bloquear_si_hay_criticos", True))\n    )\n\n    var_calidad_minima_publicar.set(\n        str(datos.get("calidad_minima_publicar", 70))\n    )\n\n    var_cuarentena_automatica.set(\n        bool(datos.get("cuarentena_automatica", True))\n    )\n\n    var_fallos_repetidos_para_cuarentena.set(\n        str(datos.get("fallos_repetidos_para_cuarentena", 2))\n    )\n\n    var_continuar_tras_cuarentena.set(\n        bool(datos.get("continuar_tras_cuarentena", True))\n    )\n\n    var_autoarchivar_publicados_fin.set(\n        bool(datos.get("autoarchivar_publicados_fin", False))\n    )\n\n    var_objetivo_publicaciones_sesion.set(\n        str(datos.get("objetivo_publicaciones_sesion", 10))\n    )\n\n    var_umbral_similitud_textual.set(\n        str(datos.get("umbral_similitud_textual", 88))\n    )\n\n    var_autoguardado_borrador_segundos.set(\n        str(datos.get("autoguardado_borrador_segundos", 30))\n    )\n\n    var_nombre_sesion.set(\n        str(datos.get("nombre_sesion", ""))\n    )\n\n    var_registro_actividad.set(\n        bool(datos.get("registro_actividad", True))\n    )\n\n    var_max_cola.set(\n        str(datos.get("max_cola", 500))\n    )\n\n    var_puerto_chromium.set(\n        str(datos.get("puerto_chromium", 9222))\n    )\n\n    var_categoria_defecto.set(\n        datos.get(\n            "categoria_defecto",\n            configuracion_por_defecto()["categoria_defecto"]\n        )\n    )\n\n    var_estado_defecto.set(\n        datos.get("estado_defecto", "Nuevo")\n    )\n\n    var_envio_defecto.set(\n        bool(\n            datos.get(\n                "desactivar_envio_defecto",\n                True\n            )\n        )\n    )\n\n\ndef restaurar_configuracion_defecto():\n    datos = configuracion_por_defecto()\n\n    try:\n        with open(\n            ARCHIVO_CONFIG,\n            "w",\n            encoding="utf-8"\n        ) as archivo:\n            json.dump(\n                datos,\n                archivo,\n                ensure_ascii=False,\n                indent=4\n            )\n\n        cargar_configuracion_en_interfaz()\n        aplicar_configuracion_general(datos)\n\n        estado.configure(\n            text="Configuración restaurada por defecto."\n        )\n\n    except Exception as e:\n        estado.configure(\n            text="❌ No pude restaurar la configuración: " + str(e)\n        )\n\n\ndef obtener_pausa_publicaciones():\n    datos = cargar_configuracion_general()\n\n    try:\n        return max(\n            0,\n            int(\n                datos.get(\n                    "pausa_entre_publicaciones",\n                    8\n                )\n            )\n        )\n    except Exception:\n        return 8\n\n\ndef obtener_pausa_aleatoria_publicaciones():\n    datos = cargar_configuracion_general()\n\n    try:\n        minimo = int(\n            datos.get(\n                "pausa_min_publicaciones",\n                8\n            )\n        )\n        maximo = int(\n            datos.get(\n                "pausa_max_publicaciones",\n                15\n            )\n        )\n\n        minimo = max(0, minimo)\n        maximo = max(minimo, maximo)\n\n        return random.randint(minimo, maximo)\n\n    except Exception:\n        return obtener_pausa_publicaciones()\n\n\ndef obtener_max_errores_consecutivos():\n    datos = cargar_configuracion_general()\n    try:\n        return max(\n            1,\n            int(datos.get("max_errores_consecutivos", 3))\n        )\n    except Exception:\n        return 3\n\n\ndef obtener_espera_despues_error():\n    datos = cargar_configuracion_general()\n    try:\n        return max(\n            0,\n            int(datos.get("espera_despues_error", 20))\n        )\n    except Exception:\n        return 20\n\n\ndef obtener_max_publicaciones_sesion():\n    datos = cargar_configuracion_general()\n    try:\n        return max(1, int(datos.get("max_publicaciones_sesion", 10)))\n    except Exception:\n        return 10\n\n\ndef obtener_watchdog_segundos():\n    datos = cargar_configuracion_general()\n    try:\n        return max(60, int(datos.get("watchdog_minutos", 4)) * 60)\n    except Exception:\n        return 240\n\n\ndef obtener_config_rendimiento():\n    datos = cargar_configuracion_general()\n\n    return {\n        "activo": bool(datos.get("modo_rendimiento", True)),\n        "limpiar_pestanas_cada": max(\n            1,\n            int(datos.get("limpiar_pestanas_cada", 1))\n        ),\n        "limpiar_memoria_cada": max(\n            1,\n            int(datos.get("limpiar_memoria_cada", 3))\n        ),\n        "timeout_wallapop": max(\n            5,\n            int(datos.get("timeout_respuesta_wallapop", 12))\n        ),\n        "auto_recuperar": bool(\n            datos.get("auto_recuperar_navegador", True)\n        ),\n        "max_recuperaciones": max(\n            0,\n            int(datos.get("max_recuperaciones_anuncio", 2))\n        ),\n        "espera_recuperacion": max(\n            0,\n            int(datos.get("espera_recuperacion_navegador", 5))\n        ),\n    }\n\n\ndef obtener_puerto_chromium():\n    datos = cargar_configuracion_general()\n\n    try:\n        return int(\n            datos.get(\n                "puerto_chromium",\n                9222\n            )\n        )\n    except Exception:\n        return 9222\n\n\n# =========================================================\n# COPIAS DE SEGURIDAD\n# =========================================================\n\nCARPETA_BACKUPS = CARPETA_BACKUPS_SEGURA\nMAX_BACKUPS_AUTOMATICOS = 15\n\nARCHIVOS_BACKUP = [\n    "configuracion_bot.json",\n    "plantillas_publicacion.json",\n    "perfiles_lote.json",\n    "cola_publicacion.json",\n    "estadisticas_bot.json",\n    "checkpoint_cola.json",\n    "control_diario.json",\n    "borrador_actual.json",\n    "publicados.json",\n    "historial_oculto.json",\n]\n\n\ndef _asegurar_carpeta_backups():\n    os.makedirs(CARPETA_BACKUPS, exist_ok=True)\n\n\ndef _nombre_backup(prefijo="backup"):\n    sello = time.strftime("%Y%m%d_%H%M%S")\n    return f"{prefijo}_{sello}.zip"\n\n\ndef crear_backup(manual=False):\n    """\n    Crea una copia ZIP de la configuración y datos de trabajo del bot.\n    No incluye imágenes ni carpetas pesadas de anuncios.\n    """\n    try:\n        _asegurar_carpeta_backups()\n\n        nombre = _nombre_backup(\n            "backup_manual" if manual else "backup_auto"\n        )\n        ruta_zip = os.path.join(CARPETA_BACKUPS, nombre)\n\n        incluidos = []\n\n        with zipfile.ZipFile(\n            ruta_zip,\n            "w",\n            compression=zipfile.ZIP_DEFLATED\n        ) as zf:\n            for archivo in ARCHIVOS_BACKUP:\n                if os.path.isfile(archivo):\n                    zf.write(\n                        archivo,\n                        arcname=os.path.basename(archivo)\n                    )\n                    incluidos.append(archivo)\n\n        if not incluidos:\n            try:\n                os.remove(ruta_zip)\n            except Exception:\n                pass\n\n            if manual:\n                estado.configure(\n                    text="No hay archivos de configuración que respaldar todavía."\n                )\n            return None\n\n        if not manual:\n            limpiar_backups_antiguos()\n\n        if manual:\n            estado.configure(\n                text=f"✅ Copia de seguridad creada: {nombre}"\n            )\n\n        print(\n            f"Backup creado: {ruta_zip} "\n            f"({len(incluidos)} archivo(s))"\n        )\n\n        return ruta_zip\n\n    except Exception as e:\n        print("Aviso: no pude crear backup:", e)\n\n        if manual:\n            estado.configure(\n                text="❌ No pude crear la copia de seguridad: " + str(e)\n            )\n\n        return None\n\n\ndef limpiar_backups_antiguos():\n    """\n    Conserva solo los backups automáticos más recientes.\n    Los manuales no se borran automáticamente.\n    """\n    try:\n        _asegurar_carpeta_backups()\n\n        archivos = []\n\n        for nombre in os.listdir(CARPETA_BACKUPS):\n            if not nombre.startswith("backup_auto_"):\n                continue\n\n            ruta = os.path.join(CARPETA_BACKUPS, nombre)\n\n            if os.path.isfile(ruta):\n                archivos.append(\n                    (\n                        os.path.getmtime(ruta),\n                        ruta\n                    )\n                )\n\n        archivos.sort(reverse=True)\n\n        for _, ruta in archivos[MAX_BACKUPS_AUTOMATICOS:]:\n            try:\n                os.remove(ruta)\n            except Exception:\n                pass\n\n    except Exception:\n        pass\n\n\ndef listar_backups():\n    _asegurar_carpeta_backups()\n\n    resultados = []\n\n    try:\n        for nombre in os.listdir(CARPETA_BACKUPS):\n            if not nombre.lower().endswith(".zip"):\n                continue\n\n            ruta = os.path.join(CARPETA_BACKUPS, nombre)\n\n            if not os.path.isfile(ruta):\n                continue\n\n            resultados.append(\n                (\n                    os.path.getmtime(ruta),\n                    nombre\n                )\n            )\n\n        resultados.sort(reverse=True)\n\n    except Exception:\n        pass\n\n    return resultados\n\n\ndef refrescar_lista_backups():\n    try:\n        lista_backups.configure(state="normal")\n        lista_backups.delete("1.0", "end")\n\n        backups = listar_backups()\n\n        if not backups:\n            lista_backups.insert(\n                "1.0",\n                "Todavía no hay copias de seguridad."\n            )\n        else:\n            lineas = []\n\n            for marca, nombre in backups:\n                fecha = time.strftime(\n                    "%d/%m/%Y %H:%M:%S",\n                    time.localtime(marca)\n                )\n\n                ruta = os.path.join(\n                    CARPETA_BACKUPS,\n                    nombre\n                )\n\n                try:\n                    tam_kb = os.path.getsize(ruta) / 1024\n                except Exception:\n                    tam_kb = 0\n\n                lineas.append(\n                    f"{fecha}  ·  {tam_kb:8.1f} KB  ·  {nombre}"\n                )\n\n            lista_backups.insert(\n                "1.0",\n                "\\n".join(lineas)\n            )\n\n        lista_backups.configure(state="disabled")\n\n    except Exception:\n        pass\n\n\ndef abrir_carpeta_backups():\n    try:\n        _asegurar_carpeta_backups()\n\n        ruta = os.path.abspath(\n            CARPETA_BACKUPS\n        )\n\n        if os.name == "nt":\n            os.startfile(ruta)\n        else:\n            webbrowser.open(\n                Path(ruta).as_uri()\n            )\n\n    except Exception as e:\n        estado.configure(\n            text="❌ No pude abrir la carpeta de backups: " + str(e)\n        )\n\n\ndef restaurar_backup_mas_reciente():\n    backups = listar_backups()\n\n    if not backups:\n        estado.configure(\n            text="❌ No hay copias de seguridad para restaurar."\n        )\n        return\n\n    _, nombre = backups[0]\n    ruta_zip = os.path.join(\n        CARPETA_BACKUPS,\n        nombre\n    )\n\n    try:\n        respuesta = messagebox.askyesno(\n            "Restaurar copia de seguridad",\n            f"Se restaurará:\\n\\n{nombre}\\n\\n"\n            "La configuración actual será sustituida.\\n"\n            "Los anuncios e imágenes NO se borrarán.\\n\\n"\n            "¿Continuar?"\n        )\n\n        if not respuesta:\n            return\n\n        # Backup de seguridad antes de restaurar.\n        crear_backup(manual=False)\n\n        with zipfile.ZipFile(\n            ruta_zip,\n            "r"\n        ) as zf:\n            nombres = zf.namelist()\n\n            permitidos = {\n                os.path.basename(x)\n                for x in ARCHIVOS_BACKUP\n            }\n\n            for nombre_archivo in nombres:\n                base = os.path.basename(\n                    nombre_archivo\n                )\n\n                if base not in permitidos:\n                    continue\n\n                with zf.open(nombre_archivo) as origen:\n                    contenido = origen.read()\n\n                with open(\n                    base,\n                    "wb"\n                ) as destino:\n                    destino.write(contenido)\n\n        cargar_configuracion_en_interfaz()\n        aplicar_configuracion_general()\n\n        cargar_plantillas()\n        actualizar_selector_plantillas()\n\n        cargar_cola_desde_disco()\n        refrescar_cola()\n\n        refrescar_estadisticas()\n        refrescar_historial()\n\n        estado.configure(\n            text=f"✅ Copia restaurada: {nombre}"\n        )\n\n        refrescar_lista_backups()\n\n    except Exception as e:\n        estado.configure(\n            text="❌ No pude restaurar la copia: " + str(e)\n        )\n\n        try:\n            messagebox.showerror(\n                "Restaurar backup",\n                str(e)\n            )\n        except Exception:\n            pass\n\n\ndef backup_automatico_al_iniciar():\n    """\n    Crea una copia ligera al iniciar, si existen datos.\n    """\n    try:\n        crear_backup(manual=False)\n        refrescar_lista_backups()\n    except Exception:\n        pass\n\n\n# =========================================================\n# DIAGNÓSTICO GENERAL DEL SISTEMA\n# =========================================================\n\ndef _resultado_diag(nombre, ok, detalle=""):\n    icono = "✅" if ok else "❌"\n    texto = f"{icono} {nombre}"\n    if detalle:\n        texto += f" · {detalle}"\n    return texto\n\n\ndef _comprobar_escritura_carpeta(ruta):\n    try:\n        os.makedirs(ruta, exist_ok=True)\n\n        prueba = os.path.join(\n            ruta,\n            ".wallapopbot_write_test.tmp"\n        )\n\n        with open(\n            prueba,\n            "w",\n            encoding="utf-8"\n        ) as archivo:\n            archivo.write("ok")\n\n        os.remove(prueba)\n        return True, "Permisos de escritura OK"\n\n    except Exception as e:\n        return False, str(e)\n\n\ndef ejecutar_diagnostico_general():\n    """\n    Comprueba los elementos básicos para que el bot pueda trabajar.\n    No publica nada ni modifica anuncios de Wallapop.\n    """\n    lineas = []\n    fallos = 0\n\n    try:\n        texto_diagnostico.configure(state="normal")\n        texto_diagnostico.delete("1.0", "end")\n        texto_diagnostico.insert(\n            "1.0",\n            "Ejecutando diagnóstico...\\n"\n        )\n        texto_diagnostico.configure(state="disabled")\n    except Exception:\n        pass\n\n    # 1) Python\n    try:\n        version_python = (\n            f"{sys.version_info.major}."\n            f"{sys.version_info.minor}."\n            f"{sys.version_info.micro}"\n        )\n\n        ok_python = sys.version_info >= (3, 10)\n\n        lineas.append(\n            _resultado_diag(\n                "Python",\n                ok_python,\n                version_python\n            )\n        )\n\n        if not ok_python:\n            fallos += 1\n\n    except Exception as e:\n        lineas.append(\n            _resultado_diag(\n                "Python",\n                False,\n                str(e)\n            )\n        )\n        fallos += 1\n\n    # 2) customtkinter\n    try:\n        import customtkinter\n        version = getattr(\n            customtkinter,\n            "__version__",\n            "instalado"\n        )\n\n        lineas.append(\n            _resultado_diag(\n                "customtkinter",\n                True,\n                str(version)\n            )\n        )\n    except Exception as e:\n        lineas.append(\n            _resultado_diag(\n                "customtkinter",\n                False,\n                str(e)\n            )\n        )\n        fallos += 1\n\n    # 3) Pillow\n    try:\n        import PIL\n        version = getattr(\n            PIL,\n            "__version__",\n            "instalado"\n        )\n\n        lineas.append(\n            _resultado_diag(\n                "Pillow",\n                True,\n                str(version)\n            )\n        )\n    except Exception as e:\n        lineas.append(\n            _resultado_diag(\n                "Pillow",\n                False,\n                str(e)\n            )\n        )\n        fallos += 1\n\n    # 4) Playwright\n    if sync_playwright is None:\n        lineas.append(\n            _resultado_diag(\n                "Playwright",\n                False,\n                "No disponible"\n            )\n        )\n        fallos += 1\n    else:\n        lineas.append(\n            _resultado_diag(\n                "Playwright",\n                True,\n                "Disponible"\n            )\n        )\n\n    # 5) Chromium / Chrome\n    try:\n        navegador = _encontrar_chromium()\n\n        if navegador:\n            lineas.append(\n                _resultado_diag(\n                    "Chromium / Chrome",\n                    True,\n                    navegador\n                )\n            )\n        else:\n            lineas.append(\n                _resultado_diag(\n                    "Chromium / Chrome",\n                    False,\n                    "No encontrado"\n                )\n            )\n            fallos += 1\n\n    except Exception as e:\n        lineas.append(\n            _resultado_diag(\n                "Chromium / Chrome",\n                False,\n                str(e)\n            )\n        )\n        fallos += 1\n\n    # 6) Puerto CDP\n    try:\n        puerto = obtener_puerto_chromium()\n        datos_cdp = _datos_cdp()\n\n        if datos_cdp:\n            lineas.append(\n                _resultado_diag(\n                    f"Puerto CDP {puerto}",\n                    True,\n                    datos_cdp.get(\n                        "Browser",\n                        "Activo"\n                    )\n                )\n            )\n        else:\n            lineas.append(\n                _resultado_diag(\n                    f"Puerto CDP {puerto}",\n                    False,\n                    "Inactivo ahora mismo"\n                )\n            )\n            fallos += 1\n\n    except Exception as e:\n        lineas.append(\n            _resultado_diag(\n                "Puerto CDP",\n                False,\n                str(e)\n            )\n        )\n        fallos += 1\n\n    # 7) Perfil de Chromium\n    try:\n        perfil = _perfil_chromium_bot()\n        existe = os.path.isdir(perfil)\n\n        lineas.append(\n            _resultado_diag(\n                "Perfil Chromium",\n                existe,\n                perfil\n            )\n        )\n\n        if not existe:\n            fallos += 1\n\n    except Exception as e:\n        lineas.append(\n            _resultado_diag(\n                "Perfil Chromium",\n                False,\n                str(e)\n            )\n        )\n        fallos += 1\n\n    # 8) Carpetas del proyecto\n    carpetas = [\n        ("anuncios_generados", CARPETA_ANUNCIOS),\n        ("backups", CARPETA_BACKUPS_SEGURA),\n    ]\n\n    for nombre_carpeta, carpeta in carpetas:\n        ok, detalle = _comprobar_escritura_carpeta(\n            carpeta\n        )\n\n        lineas.append(\n            _resultado_diag(\n                f"Carpeta {nombre_carpeta}",\n                ok,\n                (\n                    f"{detalle} · {carpeta}"\n                    if ok\n                    else f"{detalle} · {carpeta}"\n                )\n            )\n        )\n\n        if not ok:\n            fallos += 1\n\n    # 9) Archivos principales de configuración\n    archivos_config = [\n        "configuracion_bot.json",\n        "plantillas_publicacion.json",\n        "cola_publicacion.json",\n        "estadisticas_bot.json",\n    ]\n\n    for archivo in archivos_config:\n        if os.path.isfile(archivo):\n            try:\n                with open(\n                    archivo,\n                    "r",\n                    encoding="utf-8"\n                ) as f:\n                    json.load(f)\n\n                lineas.append(\n                    _resultado_diag(\n                        archivo,\n                        True,\n                        "JSON válido"\n                    )\n                )\n\n            except Exception as e:\n                lineas.append(\n                    _resultado_diag(\n                        archivo,\n                        False,\n                        "JSON inválido: " + str(e)\n                    )\n                )\n                fallos += 1\n\n        else:\n            lineas.append(\n                _resultado_diag(\n                    archivo,\n                    True,\n                    "Todavía no creado"\n                )\n            )\n\n    # 10) Conexión con Wallapop\n    try:\n        if sync_playwright is None:\n            raise RuntimeError(\n                "Playwright no está disponible."\n            )\n\n        if not _puerto_chrome_activo():\n            lineas.append(\n                _resultado_diag(\n                    "Sesión Wallapop",\n                    False,\n                    "Chromium no está conectado ahora mismo"\n                )\n            )\n            fallos += 1\n        else:\n            with sync_playwright() as p:\n                navegador = _conectar_playwright_cdp(p)\n\n                if not navegador.contexts:\n                    raise RuntimeError(\n                        "Sin contexto de navegador."\n                    )\n\n                contexto = navegador.contexts[0]\n                pagina = _buscar_pagina_wallapop(\n                    contexto\n                )\n\n                url = pagina.url.lower()\n\n                if (\n                    "accounts.wallapop.com" in url\n                    or "/login" in url\n                ):\n                    lineas.append(\n                        _resultado_diag(\n                            "Sesión Wallapop",\n                            False,\n                            "Necesita iniciar sesión"\n                        )\n                    )\n                    fallos += 1\n                else:\n                    lineas.append(\n                        _resultado_diag(\n                            "Sesión Wallapop",\n                            True,\n                            "Sesión disponible"\n                        )\n                    )\n\n    except Exception as e:\n        lineas.append(\n            _resultado_diag(\n                "Sesión Wallapop",\n                False,\n                str(e)\n            )\n        )\n        fallos += 1\n\n    # 11) Resumen final\n    total = len(lineas)\n    correctos = total - fallos\n\n    resumen = (\n        "DIAGNÓSTICO GENERAL\\n"\n        "==============================\\n\\n"\n        + "\\n".join(lineas)\n        + "\\n\\n"\n        + "==============================\\n"\n        + f"Correctos: {correctos}\\n"\n        + f"Problemas: {fallos}\\n"\n    )\n\n    try:\n        texto_diagnostico.configure(state="normal")\n        texto_diagnostico.delete("1.0", "end")\n        texto_diagnostico.insert(\n            "1.0",\n            resumen\n        )\n        texto_diagnostico.configure(state="disabled")\n    except Exception:\n        pass\n\n    if fallos == 0:\n        estado.configure(\n            text="✅ Diagnóstico completo: todo correcto."\n        )\n\n        try:\n            messagebox.showinfo(\n                "Diagnóstico",\n                "Todo está correcto.\\n\\n"\n                "El bot está listo para trabajar."\n            )\n        except Exception:\n            pass\n\n    else:\n        estado.configure(\n            text=(\n                f"⚠️ Diagnóstico terminado: "\n                f"{fallos} problema(s)."\n            )\n        )\n\n    return fallos == 0\n\n\n# =========================================================\n# REGISTRO PERSISTENTE E INFORMES\n# =========================================================\n\nCARPETA_LOGS = "logs"\nLOG_SESION = None\n\n\ndef iniciar_log_sesion():\n    """\n    Crea un archivo de log por sesión.\n    """\n    global LOG_SESION\n\n    try:\n        os.makedirs(CARPETA_LOGS, exist_ok=True)\n\n        sello = time.strftime("%Y%m%d_%H%M%S")\n        LOG_SESION = os.path.join(\n            CARPETA_LOGS,\n            f"sesion_{sello}.log"\n        )\n\n        with open(\n            LOG_SESION,\n            "w",\n            encoding="utf-8"\n        ) as archivo:\n            archivo.write(\n                "WallapopBot · DIAGPROG5\\n"\n                f"Inicio: {time.strftime(\'%d/%m/%Y %H:%M:%S\')}\\n"\n                f"Python: {sys.version}\\n"\n                "=" * 70\n                + "\\n"\n            )\n\n        return LOG_SESION\n\n    except Exception:\n        LOG_SESION = None\n        return None\n\n\ndef escribir_log_archivo(texto):\n    """\n    Guarda cualquier salida relevante en el log de la sesión.\n    """\n    if not texto:\n        return\n\n    global LOG_SESION\n\n    try:\n        if not LOG_SESION:\n            iniciar_log_sesion()\n\n        if not LOG_SESION:\n            return\n\n        with open(\n            LOG_SESION,\n            "a",\n            encoding="utf-8"\n        ) as archivo:\n            archivo.write(str(texto))\n\n    except Exception:\n        pass\n\n\ndef limpiar_logs_antiguos(max_logs=30):\n    """\n    Conserva solo los últimos logs de sesión.\n    """\n    try:\n        os.makedirs(CARPETA_LOGS, exist_ok=True)\n\n        archivos = []\n\n        for nombre in os.listdir(CARPETA_LOGS):\n            if not nombre.startswith("sesion_"):\n                continue\n            if not nombre.endswith(".log"):\n                continue\n\n            ruta = os.path.join(CARPETA_LOGS, nombre)\n\n            if os.path.isfile(ruta):\n                archivos.append(\n                    (os.path.getmtime(ruta), ruta)\n                )\n\n        archivos.sort(reverse=True)\n\n        for _, ruta in archivos[max_logs:]:\n            try:\n                os.remove(ruta)\n            except Exception:\n                pass\n\n    except Exception:\n        pass\n\n\ndef abrir_carpeta_logs():\n    try:\n        os.makedirs(CARPETA_LOGS, exist_ok=True)\n\n        ruta = os.path.abspath(CARPETA_LOGS)\n\n        if os.name == "nt":\n            os.startfile(ruta)\n        else:\n            webbrowser.open(Path(ruta).as_uri())\n\n    except Exception as e:\n        estado.configure(\n            text="❌ No pude abrir la carpeta de logs: " + str(e)\n        )\n\n\ndef crear_informe_soporte():\n    """\n    Genera un TXT fácil de compartir cuando algo falla.\n    No incluye contraseñas ni cookies.\n    """\n    try:\n        os.makedirs(CARPETA_LOGS, exist_ok=True)\n\n        sello = time.strftime("%Y%m%d_%H%M%S")\n        ruta = os.path.join(\n            CARPETA_LOGS,\n            f"informe_soporte_{sello}.txt"\n        )\n\n        config = cargar_configuracion_general()\n        stats = cargar_estadisticas()\n        datos_cdp = _datos_cdp()\n\n        lineas = [\n            "INFORME DE SOPORTE · WallapopBot DIAGPROG5",\n            "=" * 70,\n            "",\n            f"Fecha: {time.strftime(\'%d/%m/%Y %H:%M:%S\')}",\n            f"Python: {sys.version}",\n            f"Sistema: {os.name}",\n            "",\n            "CONFIGURACIÓN",\n            "-" * 70,\n            json.dumps(\n                config,\n                ensure_ascii=False,\n                indent=2\n            ),\n            "",\n            "ESTADÍSTICAS",\n            "-" * 70,\n            json.dumps(\n                stats,\n                ensure_ascii=False,\n                indent=2\n            ),\n            "",\n            "NAVEGADOR",\n            "-" * 70,\n            f"Ejecutable: {_encontrar_chromium()}",\n            f"Puerto: {obtener_puerto_chromium()}",\n            f"CDP activo: {\'Sí\' if datos_cdp else \'No\'}",\n            f"Browser: {datos_cdp.get(\'Browser\') if datos_cdp else \'—\'}",\n            f"Perfil: {_perfil_chromium_bot()}",\n            "",\n            "ARCHIVOS",\n            "-" * 70,\n        ]\n\n        for archivo in [\n            ARCHIVO_CONFIG,\n            ARCHIVO_PLANTILLAS,\n            ARCHIVO_COLA,\n            ARCHIVO_ESTADISTICAS,\n            ARCHIVO_BORRADOR,\n            ARCHIVO_PUBLICADOS,\n        ]:\n            lineas.append(\n                f"{archivo}: "\n                + ("OK" if os.path.isfile(archivo) else "No existe")\n            )\n\n        lineas += [\n            "",\n            "ÚLTIMO LOG DE SESIÓN",\n            "-" * 70,\n        ]\n\n        if LOG_SESION and os.path.isfile(LOG_SESION):\n            try:\n                contenido = Path(LOG_SESION).read_text(\n                    encoding="utf-8",\n                    errors="ignore"\n                )\n                lineas.append(contenido[-12000:])\n            except Exception:\n                lineas.append("No se pudo leer el log.")\n        else:\n            lineas.append("No hay log de sesión disponible.")\n\n        Path(ruta).write_text(\n            "\\n".join(lineas),\n            encoding="utf-8"\n        )\n\n        estado.configure(\n            text=f"✅ Informe creado: {os.path.basename(ruta)}"\n        )\n\n        try:\n            messagebox.showinfo(\n                "Informe de soporte",\n                "Informe creado correctamente en:\\n\\n"\n                + os.path.abspath(ruta)\n            )\n        except Exception:\n            pass\n\n        return ruta\n\n    except Exception as e:\n        estado.configure(\n            text="❌ No pude crear el informe: " + str(e)\n        )\n        return None\n\n\n# =========================================================\n# DASHBOARD / PANEL DE CONTROL\n# =========================================================\n\ndef _publicados_hoy():\n    hoy = time.strftime("%d/%m/%Y")\n    total = 0\n    base = Path(CARPETA_ANUNCIOS)\n\n    if not base.exists():\n        return 0\n\n    try:\n        for carpeta in base.iterdir():\n            if not carpeta.is_dir():\n                continue\n\n            datos = leer_estado_anuncio(carpeta)\n\n            if str(datos.get("estado", "")).upper() != "PUBLICADO":\n                continue\n\n            if str(datos.get("fecha_estado", "")).startswith(hoy):\n                total += 1\n    except Exception:\n        pass\n\n    return total\n\n\ndef _ultimo_error_cola():\n    candidatos = []\n\n    for item in cola_anuncios:\n        if item.get("estado_cola") != "ERROR":\n            continue\n\n        candidatos.append(\n            (\n                item.get("error_fecha", ""),\n                item.get("titulo", "Sin título"),\n                item.get("error_fase", "Publicación"),\n                item.get("error_motivo", "Sin detalle")\n            )\n        )\n\n    if not candidatos:\n        return None\n\n    def clave(x):\n        try:\n            return time.mktime(\n                time.strptime(x[0], "%d/%m/%Y %H:%M:%S")\n            )\n        except Exception:\n            return 0\n\n    candidatos.sort(key=clave, reverse=True)\n    return candidatos[0]\n\n\ndef _tasa_exito():\n    datos = cargar_estadisticas()\n    publicados = int(datos.get("publicados", 0))\n    fallidos = int(datos.get("fallidos", 0))\n    total = publicados + fallidos\n\n    if total <= 0:\n        return 0.0\n\n    return (publicados / total) * 100.0\n\n\ndef _estado_rapido_sistema():\n    try:\n        navegador = _puerto_chrome_activo()\n    except Exception:\n        navegador = False\n\n    try:\n        playwright_ok = sync_playwright is not None\n    except Exception:\n        playwright_ok = False\n\n    if navegador and playwright_ok:\n        return "LISTO", "#46d47a"\n\n    if playwright_ok:\n        return "NAVEGADOR DESCONECTADO", "#f7c948"\n\n    return "REVISAR SISTEMA", "#ff6b6b"\n\n\ndef refrescar_dashboard():\n    # Dashboard principal V86.\n    try:\n        total_cola_v86 = len(cola_anuncios)\n\n        publicados_hoy_v86 = publicaciones_realizadas_hoy()\n\n        proceso_v86 = sum(\n            1\n            for x in cola_anuncios\n            if x.get("estado_cola") == "PUBLICANDO"\n        )\n\n        errores_v86 = sum(\n            1\n            for x in cola_anuncios\n            if x.get("estado_cola") in (\n                "ERROR",\n                "REVISAR",\n                "CUARENTENA"\n            )\n        )\n\n        pendientes_v86 = sum(\n            1\n            for x in cola_anuncios\n            if x.get("estado_cola") == "PENDIENTE"\n        )\n\n        dashboard_kpi_cola.configure(\n            text=str(total_cola_v86)\n        )\n        dashboard_kpi_publicados.configure(\n            text=str(publicados_hoy_v86)\n        )\n        dashboard_kpi_proceso.configure(\n            text=str(proceso_v86)\n        )\n        dashboard_kpi_errores.configure(\n            text=str(errores_v86)\n        )\n\n        if errores_v86:\n            dashboard_estado_general.configure(\n                text="● Requiere atención",\n                text_color="#ffb52e"\n            )\n        else:\n            dashboard_estado_general.configure(\n                text="● Todo correcto",\n                text_color=COLOR_UI_GREEN\n            )\n\n        # Resumen visual de cola.\n        lineas_v86 = []\n\n        for i, item in enumerate(\n            cola_anuncios[:12],\n            start=1\n        ):\n            titulo = str(\n                item.get(\n                    "titulo",\n                    "Sin título"\n                )\n            )[:55]\n\n            precio = str(\n                item.get(\n                    "precio",\n                    "—"\n                )\n            )\n\n            est = str(\n                item.get(\n                    "estado_cola",\n                    "PENDIENTE"\n                )\n            )\n\n            lineas_v86.append(\n                f"{i:02d}   {titulo:<55}   {precio:>7} €   {est}"\n            )\n\n        dashboard_queue_text.configure(\n            state="normal"\n        )\n        dashboard_queue_text.delete(\n            "1.0",\n            "end"\n        )\n\n        if lineas_v86:\n            dashboard_queue_text.insert(\n                "1.0",\n                "\\n".join(lineas_v86)\n            )\n        else:\n            dashboard_queue_text.insert(\n                "1.0",\n                "No hay anuncios en la cola.\\n\\n"\n                "Crea anuncios y añádelos para empezar."\n            )\n\n        dashboard_queue_text.configure(\n            state="disabled"\n        )\n\n        total_trabajo_v86 = max(\n            1,\n            total_cola_v86\n        )\n\n        completados_v86 = sum(\n            1\n            for x in cola_anuncios\n            if x.get("estado_cola") == "PUBLICADO"\n        )\n\n        porcentaje_v86 = int(\n            completados_v86\n            / total_trabajo_v86\n            * 100\n        )\n\n        dashboard_progress.set(\n            min(\n                1.0,\n                completados_v86 / total_trabajo_v86\n            )\n        )\n\n        dashboard_progreso_texto.configure(\n            text=(\n                f"Progreso actual                         "\n                f"{porcentaje_v86}%"\n            )\n        )\n\n        eta_seg, _velocidad = calcular_eta_cola()\n\n        dashboard_eta.configure(\n            text=(\n                f"{completados_v86} de {total_cola_v86} anuncios"\n                f"             Tiempo estimado: "\n                f"{_formatear_duracion(eta_seg)}"\n            )\n        )\n\n        dashboard_footer_estado.configure(\n            text=(\n                "●  Listo para trabajar"\n                if not cola_en_ejecucion\n                else "●  Publicando cola"\n            ),\n            text_color=(\n                COLOR_UI_GREEN\n                if not cola_en_ejecucion\n                else "#ffb52e"\n            )\n        )\n\n    except Exception:\n        pass\n\n    try:\n        actualizar_panel_licencia_cliente()\n    except Exception:\n        pass\n\n    try:\n        publicados_hoy = _publicados_hoy()\n        tasa = _tasa_exito()\n\n        pendientes = sum(\n            1 for item in cola_anuncios\n            if item.get("estado_cola") == "PENDIENTE"\n        )\n\n        errores = sum(\n            1 for item in cola_anuncios\n            if item.get("estado_cola") == "ERROR"\n        )\n\n        publicados_cola = sum(\n            1 for item in cola_anuncios\n            if item.get("estado_cola") == "PUBLICADO"\n        )\n\n        estado_sistema, color_sistema = _estado_rapido_sistema()\n\n        dashboard_publicados_hoy.configure(text=str(publicados_hoy))\n\n        try:\n            estado_simple_texto.configure(\n                text=f"Sistema: {estado_sistema}",\n                text_color=color_sistema\n            )\n            estado_simple_cola.configure(\n                text=(\n                    f"Cola: {pendientes} pendiente(s) · "\n                    f"{errores} error(es)"\n                )\n            )\n        except Exception:\n            pass\n        dashboard_tasa_exito.configure(text=f"{tasa:.0f}%")\n        dashboard_pendientes.configure(text=str(pendientes))\n        dashboard_errores.configure(text=str(errores))\n        dashboard_publicados_cola.configure(text=str(publicados_cola))\n        dashboard_estado_sistema.configure(\n            text=estado_sistema,\n            text_color=color_sistema\n        )\n\n        media_rendimiento = 0.0\n        if rendimiento_tiempos_publicacion:\n            media_rendimiento = (\n                sum(rendimiento_tiempos_publicacion)\n                / len(rendimiento_tiempos_publicacion)\n            )\n\n        dashboard_rendimiento.configure(\n            text=f"{media_rendimiento:.1f}s"\n        )\n        dashboard_ultimo_tiempo.configure(\n            text=f"{rendimiento_ultimo_tiempo:.1f}s"\n        )\n        dashboard_procesados.configure(\n            text=str(rendimiento_anuncios_procesados)\n        )\n\n        ocupados = len(cola_anuncios)\n        libres = espacio_cola_disponible()\n\n        dashboard_capacidad_cola.configure(\n            text=f"{ocupados} / {limite_cola_efectivo()}"\n        )\n        dashboard_lote_recomendado.configure(\n            text=str(libres)\n        )\n\n        try:\n            modo = obtener_config_rendimiento()["activo"]\n        except Exception:\n            modo = True\n\n        dashboard_modo_rendimiento.configure(\n            text="ON" if modo else "OFF",\n            text_color="#46d47a" if modo else "#f7c948"\n        )\n\n        dashboard_recuperaciones.configure(\n            text=str(recuperaciones_navegador_sesion)\n        )\n        dashboard_ultima_recuperacion.configure(\n            text=ultima_recuperacion_navegador or "—"\n        )\n\n        color_supervisor = (\n            "#46d47a"\n            if supervisor_ultimo_estado == "OK"\n            else (\n                "#f7c948"\n                if supervisor_ultimo_estado in (\n                    "SIN COMPROBAR",\n                    "RECUPERANDO",\n                    "DESACTIVADO"\n                )\n                else "#ff6b6b"\n            )\n        )\n\n        dashboard_supervisor.configure(\n            text=supervisor_ultimo_estado,\n            text_color=color_supervisor\n        )\n\n        dashboard_reparaciones_supervisor.configure(\n            text=str(supervisor_reparaciones_sesion)\n        )\n\n        dashboard_proteccion.configure(\n            text="ALERTA" if proteccion_ultimo_detalle else "OK",\n            text_color="#ff6b6b" if proteccion_ultimo_detalle else "#46d47a"\n        )\n\n        dashboard_sesion_recuperada.configure(\n            text="SÍ" if sesion_interrumpida_detectada else "NO",\n            text_color="#f7c948" if sesion_interrumpida_detectada else "#ffffff"\n        )\n\n        dashboard_publicados_hoy_control.configure(\n            text=str(publicaciones_hoy())\n        )\n\n        dashboard_capacidad_diaria.configure(\n            text=str(capacidad_diaria_restante())\n        )\n\n        prueba = modo_prueba_activado()\n\n        dashboard_modo_prueba.configure(\n            text="ON" if prueba else "OFF",\n            text_color="#f7c948" if prueba else "#ffffff"\n        )\n\n        dashboard_estrategia.configure(\n            text=estrategia_actual()\n        )\n\n        dashboard_ritmo.configure(\n            text=f"{ritmo_adaptativo_ajuste:+d}s"\n        )\n\n        dashboard_pausa_actual.configure(\n            text=f"{ultima_pausa_adaptativa}s"\n        )\n\n        siguiente = siguiente_anuncio_pendiente()\n\n        dashboard_siguiente.configure(\n            text=(\n                siguiente.get("titulo", "—")[:28]\n                if siguiente\n                else "—"\n            )\n        )\n\n        dashboard_prevuelo.configure(\n            text=ultimo_diagnostico_prevuelo,\n            text_color=(\n                "#46d47a"\n                if ultimo_diagnostico_prevuelo == "OK"\n                else "#f7c948"\n            )\n        )\n\n        dashboard_prevuelo_hora.configure(\n            text=ultima_hora_prevuelo or "—"\n        )\n\n        dashboard_snapshots.configure(\n            text=str(contar_snapshots())\n        )\n\n        dashboard_archivados.configure(\n            text=str(contar_publicados_archivados())\n        )\n\n        dashboard_config_buena.configure(\n            text=(\n                "SÍ"\n                if os.path.isfile(ARCHIVO_CONFIG_BUENA)\n                else "NO"\n            ),\n            text_color=(\n                "#46d47a"\n                if os.path.isfile(ARCHIVO_CONFIG_BUENA)\n                else "#ffffff"\n            )\n        )\n\n        dashboard_visual_originalidad.configure(\n            text=f"{visual_ultima_originalidad}%"\n        )\n\n        dashboard_visual_regeneraciones.configure(\n            text=str(visual_regeneraciones_sesion)\n        )\n\n        dashboard_visual_historial.configure(\n            text=str(contar_hashes_visuales())\n        )\n\n        dashboard_auditoria.configure(\n            text=auditoria_ultimo_estado,\n            text_color=(\n                "#46d47a"\n                if auditoria_ultimo_estado == "OK"\n                else (\n                    "#f7c948"\n                    if auditoria_ultimo_estado in (\n                        "SIN AUDITAR",\n                        "AVISOS"\n                    )\n                    else "#ff6b6b"\n                )\n            )\n        )\n\n        dashboard_preparacion.configure(\n            text=f"{auditoria_preparacion}%"\n        )\n\n        dashboard_criticos.configure(\n            text=str(auditoria_criticos)\n        )\n\n        dashboard_avisos.configure(\n            text=str(auditoria_avisos)\n        )\n\n        dashboard_cuarentena.configure(\n            text=str(contar_cuarentena())\n        )\n\n        dashboard_cuarentena_sesion.configure(\n            text=str(cuarentena_sesion)\n        )\n\n        dashboard_ultimo_cuarentena.configure(\n            text=(\n                ultimo_motivo_cuarentena[:28]\n                if ultimo_motivo_cuarentena\n                else "—"\n            )\n        )\n\n        objetivo = objetivo_sesion_actual()\n\n        dashboard_objetivo.configure(\n            text=f"{cola_publicados_sesion} / {objetivo}"\n        )\n\n        dashboard_progreso_objetivo.configure(\n            text=f"{progreso_objetivo_sesion()}%"\n        )\n\n        dashboard_notas.configure(\n            text=str(\n                sum(\n                    1\n                    for x in cola_anuncios\n                    if str(\n                        x.get(\n                            "nota",\n                            ""\n                        )\n                    ).strip()\n                )\n            )\n        )\n\n        resumen_cola = estadisticas_cola_resumen()\n\n        dashboard_favoritos.configure(\n            text=str(\n                resumen_cola["favoritos"]\n            )\n        )\n\n        dashboard_calidad_media.configure(\n            text=f"{resumen_cola[\'calidad_media\']}/100"\n        )\n\n        dashboard_total_cola.configure(\n            text=str(\n                resumen_cola["total"]\n            )\n        )\n\n        dashboard_lotes_activos.configure(\n            text=str(\n                len(\n                    {\n                        _nombre_lote_item(x)\n                        for x in cola_anuncios\n                        if _nombre_lote_item(x) != "SIN LOTE"\n                    }\n                )\n            )\n        )\n\n        dashboard_similitud_textual.configure(\n            text=str(\n                similitudes_textuales_detectadas\n            )\n        )\n\n        dashboard_autoguardado.configure(\n            text=ultimo_autoguardado_borrador or "—"\n        )\n\n        salud = salud_global_cola()\n\n        dashboard_salud_cola.configure(\n            text=f"{salud}/100",\n            text_color=(\n                "#46d47a"\n                if salud >= 80\n                else (\n                    "#f7c948"\n                    if salud >= 60\n                    else "#ff6b6b"\n                )\n            )\n        )\n\n        stats_lote = estadisticas_lote_actual()\n\n        dashboard_lote_calidad.configure(\n            text=f"{stats_lote[\'calidad_media\']}/100"\n        )\n\n        dashboard_actividad.configure(\n            text=(\n                ultima_accion_registrada[:28]\n                if ultima_accion_registrada\n                else "—"\n            )\n        )\n\n        eta, velocidad = calcular_eta_cola()\n\n        dashboard_eta.configure(\n            text=_formatear_duracion(\n                eta\n            )\n        )\n\n        dashboard_velocidad.configure(\n            text=f"{velocidad:.1f}/h"\n        )\n\n        dashboard_pausados.configure(\n            text=str(\n                sum(\n                    1\n                    for x in cola_anuncios\n                    if x.get("estado_cola") == "PAUSADO"\n                )\n            )\n        )\n\n        dashboard_plan.configure(\n            text=plan_actual().get(\n                "nombre",\n                plan_actual_nombre()\n            )\n        )\n\n        dashboard_limite_cola_plan.configure(\n            text=str(\n                limite_cola_plan()\n            )\n        )\n\n        dashboard_limite_lote_plan.configure(\n            text=str(\n                limite_lote_plan()\n            )\n        )\n\n        try:\n            barra_objetivo_sesion.set(\n                progreso_objetivo_sesion() / 100\n            )\n        except Exception:\n            pass\n\n        ultimo = _ultimo_error_cola()\n\n        if ultimo:\n            fecha, titulo, fase, motivo = ultimo\n            if len(motivo) > 180:\n                motivo = motivo[:177] + "..."\n\n            texto = (\n                f"{titulo}\\n"\n                f"Fase: {fase}\\n"\n                f"{motivo}\\n"\n                f"{fecha}"\n            )\n        else:\n            texto = "No hay errores recientes registrados en la cola."\n\n        dashboard_ultimo_error.configure(state="normal")\n        dashboard_ultimo_error.delete("1.0", "end")\n        dashboard_ultimo_error.insert("1.0", texto)\n        dashboard_ultimo_error.configure(state="disabled")\n\n        dashboard_actualizado.configure(\n            text="Actualizado: " + time.strftime("%d/%m/%Y %H:%M:%S")\n        )\n\n    except Exception as e:\n        print("Aviso: no pude refrescar dashboard:", e)\n\n\ndef abrir_cola_desde_dashboard():\n    try:\n        tabs.set("Cola")\n    except Exception:\n        pass\n\n\ndef abrir_diagnostico_desde_dashboard():\n    try:\n        tabs.set("Diagnóstico")\n    except Exception:\n        pass\n\n\ndef abrir_registro_desde_dashboard():\n    try:\n        tabs.set("Registro")\n    except Exception:\n        pass\n\n\n# =========================================================\n# GENERACIÓN POR LOTES\n# =========================================================\n\nVARIANTES_LOTE = [\n    "Profesional",\n    "Especialista",\n    "Servicio rápido",\n    "Diagnosis avanzada",\n    "Taller especializado",\n    "Revisión completa",\n    "Servicio técnico",\n    "Solución profesional",\n    "Automoción",\n    "Mecánica especializada",\n]\n\n\ndef _crear_servicio_variante(base, indice):\n    """\n    Genera títulos naturales y diferentes para cada anuncio del lote.\n    También varía el primer anuncio para evitar repetir el mismo anuncio\n    al crear otro lote con la misma idea.\n    """\n    base = str(base).strip()\n\n    variantes_naturales = [\n        "Diagnosis profesional",\n        "Servicio técnico",\n        "Revisión especializada",\n        "Taller especialista",\n        "Diagnóstico avanzado",\n        "Comprobación profesional",\n        "Revisión electrónica",\n        "Servicio especializado",\n        "Diagnosis y revisión",\n        "Asistencia de taller",\n        "Control técnico",\n        "Mecánica profesional",\n        "Revisión de averías",\n        "Diagnóstico de taller",\n        "Servicio de automoción",\n        "Revisión profesional",\n        "Especialista en diagnosis",\n        "Comprobación técnica",\n        "Atención de taller",\n        "Diagnosis especializada",\n    ]\n\n    # Mezcla índice + tiempo + aleatorio. Así un lote nuevo no repite\n    # exactamente la secuencia del lote anterior.\n    semilla = (\n        int(time.time() * 1000)\n        + indice * 37\n        + random.randint(0, 9999)\n    )\n\n    sufijo = variantes_naturales[\n        semilla % len(variantes_naturales)\n    ]\n\n    candidato = f"{base} · {sufijo}"\n\n    if len(candidato) > 50:\n        espacio = max(\n            8,\n            47 - len(sufijo)\n        )\n        base_corta = base[\n            :espacio\n        ].rstrip(\n            " ·-"\n        )\n        candidato = (\n            f"{base_corta} · {sufijo}"\n        )\n\n    return candidato[:50]\n\n\ndef _hacer_descripcion_lote_unica(descripcion, indice):\n    """\n    Añade una pequeña variación natural al texto del lote.\n    No usa códigos raros ni texto visible artificial.\n    """\n    cierres_a = [\n        "Atención personalizada según vehículo y síntomas.",\n        "Valoración previa según marca, modelo y motorización.",\n        "Consulta disponibilidad indicando los datos del vehículo.",\n        "Servicio adaptado al tipo de vehículo y avería.",\n        "Revisión orientada al problema indicado por el cliente.",\n        "Trabajo profesional con valoración previa.",\n        "Consulta condiciones según vehículo y servicio solicitado.",\n        "Atención de taller según disponibilidad y tipo de intervención.",\n        "Diagnóstico adaptado a cada caso.",\n        "Servicio sujeto a comprobación previa del vehículo.",\n    ]\n\n    cierres_b = [\n        "Puedes consultar antes de desplazarte.",\n        "Indica marca, modelo y año para una orientación inicial.",\n        "Consulta sin compromiso para confirmar disponibilidad.",\n        "Se recomienda indicar motorización y síntomas.",\n        "La valoración final depende de la revisión del vehículo.",\n        "Atención mediante cita según carga de trabajo.",\n        "Consulta previamente para organizar la intervención.",\n        "Se revisa cada caso de forma individual.",\n        "Disponibilidad según agenda de taller.",\n        "Solicita información indicando los datos básicos del vehículo.",\n    ]\n\n    base = str(\n        descripcion or ""\n    ).strip()\n\n    semilla = (\n        int(time.time() * 1000)\n        + indice * 53\n        + random.randint(0, 99999)\n    )\n\n    a = cierres_a[\n        semilla % len(cierres_a)\n    ]\n    b = cierres_b[\n        (semilla // 7) % len(cierres_b)\n    ]\n\n    extra = a\n\n    if b.lower() not in base.lower():\n        extra += "\\n" + b\n\n    if extra.lower() not in base.lower():\n        base = (\n            base.rstrip()\n            + "\\n\\n"\n            + extra\n        )\n\n    return limitar_descripcion(\n        base\n    )\n\n\ndef item_cola_es_duplicado(item):\n    """\n    Comprueba si un anuncio de la cola ya fue publicado o si coincide\n    exactamente con otro anuncio que ya está en la propia cola.\n    """\n    titulo = str(item.get("titulo", "")).strip()\n    descripcion = str(item.get("descripcion", "")).strip()\n    precio = str(item.get("precio", "")).strip()\n\n    # Duplicado respecto al histórico real de publicados.\n    if anuncio_ya_publicado(\n        titulo,\n        descripcion,\n        precio\n    ):\n        return True, "Ya fue publicado anteriormente"\n\n    # Duplicado respecto a otro elemento de la cola.\n    identificador = crear_id_anuncio(\n        titulo,\n        descripcion,\n        precio\n    )\n\n    for otro in cola_anuncios:\n        if otro is item:\n            continue\n\n        otro_id = crear_id_anuncio(\n            otro.get("titulo", ""),\n            otro.get("descripcion", ""),\n            otro.get("precio", "")\n        )\n\n        if otro_id == identificador:\n            return True, "Ya existe otro igual dentro de la cola"\n\n    return False, ""\n\n\ndef asegurar_item_lote_unico(item, max_reemplazos=20):\n    """\n    Si un anuncio recién generado resulta duplicado, lo sustituye de inmediato\n    por uno nuevo antes de dejarlo listo en la cola.\n    """\n    reemplazos = 0\n\n    while reemplazos < max_reemplazos:\n        duplicado, motivo = item_cola_es_duplicado(item)\n\n        if not duplicado:\n            return True\n\n        reemplazos += 1\n\n        incrementar_estadistica(\n            "duplicados_bloqueados"\n        )\n\n        print(\n            f"Lote: duplicado detectado ({motivo}). "\n            f"Generando sustituto {reemplazos}/{max_reemplazos}..."\n        )\n\n        try:\n            ventana.update_idletasks()\n        except Exception:\n            pass\n\n        sustituido = reemplazar_duplicado_por_anuncio_nuevo(\n            item\n        )\n\n        if not sustituido:\n            return False\n\n    return False\n\n\ndef generar_lote_y_anadir_cola():\n    """\n    Genera varios anuncios distintos y los añade a la cola.\n    Usa una sola imagen por anuncio y el sistema anti-repetición visual.\n    """\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes generar un lote mientras la cola está publicando."\n        )\n        return\n\n    try:\n        cantidad = int(var_cantidad_lote.get())\n    except Exception:\n        estado.configure(\n            text="❌ La cantidad del lote debe ser un número."\n        )\n        return\n\n    if not requerir_funcion_plan(\n        "lotes",\n        "Generación por lotes"\n    ):\n        return\n\n    limite_lote = (\n        limite_cola_efectivo()\n        if es_edicion_admin()\n        else limite_lote_plan()\n    )\n\n    if cantidad < 1 or cantidad > limite_lote:\n        estado.configure(\n            text=(\n                f"❌ Tu plan {plan_actual().get(\'nombre\')} permite "\n                f"lotes de 1 a {limite_lote} anuncios."\n            )\n        )\n        return\n\n    espacio_disponible = espacio_cola_disponible()\n\n    if espacio_disponible <= 0:\n        estado.configure(\n            text="❌ La cola está llena."\n        )\n        return\n\n    if cantidad > espacio_disponible:\n        cantidad = espacio_disponible\n\n    servicio_original = entrada_servicio.get().strip()\n\n    if not servicio_original:\n        estado.configure(\n            text="❌ Escribe primero el servicio o idea del anuncio."\n        )\n        return\n\n    generados = 0\n    errores = 0\n\n    global ultimo_lote_creado\n\n    nombre_lote = (\n        var_nombre_lote_manual.get().strip()\n        or servicio_original[:32]\n    )\n\n    lote_id_actual = _crear_id_lote(\n        nombre_lote\n    )\n\n    ultimo_lote_creado = nombre_lote\n\n    try:\n        boton_generar_lote.configure(state="disabled")\n    except Exception:\n        pass\n\n    try:\n        for indice in range(cantidad):\n            variante = _crear_servicio_variante(\n                servicio_original,\n                indice\n            )\n\n            entrada_servicio.delete(0, "end")\n            entrada_servicio.insert(0, variante)\n\n            estado.configure(\n                text=(\n                    f"⚙ Generando lote · "\n                    f"{indice + 1}/{cantidad} · {variante}"\n                )\n            )\n            ventana.update()\n\n            carpeta_antes = ultima_carpeta_anuncio\n\n            generar_anuncio()\n\n            # Variación textual extra para que anuncios del lote no sean\n            # falsos duplicados aunque partan de la misma idea.\n            if (\n                ultima_carpeta_anuncio\n                and os.path.isdir(ultima_carpeta_anuncio)\n            ):\n                try:\n                    descripcion_lote = _hacer_descripcion_lote_unica(\n                        obtener_descripcion_actual(),\n                        indice\n                    )\n\n                    caja_descripcion.delete(\n                        "1.0",\n                        "end"\n                    )\n                    caja_descripcion.insert(\n                        "1.0",\n                        descripcion_lote\n                    )\n                    controlar_descripcion()\n\n                    ruta_txt_lote = os.path.join(\n                        ultima_carpeta_anuncio,\n                        "anuncio.txt"\n                    )\n\n                    if os.path.isfile(\n                        ruta_txt_lote\n                    ):\n                        datos_txt = leer_anuncio_txt_completo(\n                            ultima_carpeta_anuncio\n                        )\n\n                        with open(\n                            ruta_txt_lote,\n                            "w",\n                            encoding="utf-8"\n                        ) as f:\n                            f.write(\n                                "TÍTULO:\\n"\n                                + str(\n                                    datos_txt.get(\n                                        "titulo",\n                                        variante\n                                    )\n                                )\n                                + "\\n\\n"\n                            )\n                            f.write(\n                                "PRECIO:\\n"\n                                + (\n                                    str(\n                                        datos_txt.get(\n                                            "precio",\n                                            entrada_precio.get().strip()\n                                        )\n                                    )\n                                    + " €\\n\\n"\n                                )\n                            )\n                            f.write(\n                                "TIPO DE VEHÍCULO:\\n"\n                                "Cualquier tipo de vehículo\\n\\n"\n                            )\n                            f.write(\n                                "CONTACTO:\\n"\n                                + (\n                                    entrada_contacto.get().strip()\n                                    or "No indicado"\n                                )\n                                + "\\n\\n"\n                            )\n                            f.write(\n                                "DESCRIPCIÓN:\\n"\n                                + descripcion_lote\n                            )\n\n                    anuncio_extension["descripcion"] = descripcion_lote[:640]\n\n                except Exception as e:\n                    print(\n                        "Aviso al crear variación textual de lote:",\n                        e\n                    )\n\n            if (\n                not ultima_carpeta_anuncio\n                or ultima_carpeta_anuncio == carpeta_antes\n                or not os.path.isdir(ultima_carpeta_anuncio)\n            ):\n                errores += 1\n                print(\n                    "Lote: la generación no produjo una carpeta nueva:",\n                    variante\n                )\n                continue\n\n            antes_cola = len(cola_anuncios)\n            agregar_actual_a_cola()\n\n            if len(cola_anuncios) > antes_cola:\n                item_nuevo = cola_anuncios[-1]\n\n                asignar_lote_item(\n                    item_nuevo,\n                    lote_id_actual,\n                    nombre_lote\n                )\n\n                estado.configure(\n                    text=(\n                        f"🔎 Comprobando duplicados · "\n                        f"{indice + 1}/{cantidad}"\n                    )\n                )\n                ventana.update()\n\n                unico = asegurar_item_lote_unico(\n                    item_nuevo\n                )\n\n                if unico:\n                    generados += 1\n                else:\n                    errores += 1\n\n                    # Si no pudimos crear un sustituto válido,\n                    # quitamos ese elemento de la cola para no dejar basura.\n                    try:\n                        if item_nuevo in cola_anuncios:\n                            cola_anuncios.remove(\n                                item_nuevo\n                            )\n                    except Exception:\n                        pass\n\n                    guardar_cola_en_disco()\n                    refrescar_cola()\n\n                    print(\n                        "Lote: no pude fabricar un sustituto único "\n                        "después de varios intentos."\n                    )\n            else:\n                errores += 1\n\n            ventana.update()\n\n    finally:\n        # Restaurar el texto original para que el usuario no pierda su idea.\n        try:\n            entrada_servicio.delete(0, "end")\n            entrada_servicio.insert(0, servicio_original)\n        except Exception:\n            pass\n\n        try:\n            boton_generar_lote.configure(state="normal")\n        except Exception:\n            pass\n\n        refrescar_cola()\n        refrescar_historial()\n        refrescar_dashboard()\n        actualizar_menu_lotes()\n\n        if generados:\n            var_filtro_lote.set(\n                nombre_lote\n            )\n\n    registrar_actividad(\n        "Generar lote",\n        f"{generados} anuncio(s) · {nombre_lote}"\n    )\n\n    estado.configure(\n        text=(\n            f"✅ Lote terminado · "\n            f"{generados} anuncio(s) únicos en cola · "\n            f"{errores} error(es). "\n            f"Los duplicados se sustituyen automáticamente."\n        )\n    )\n\n\ndef exportar_cola_csv():\n    """\n    Exporta la cola a CSV para revisar títulos, precios, estados y errores.\n    """\n    if not cola_anuncios:\n        estado.configure(\n            text="No hay anuncios en la cola para exportar."\n        )\n        return\n\n    try:\n        os.makedirs("exportaciones", exist_ok=True)\n\n        sello = time.strftime("%Y%m%d_%H%M%S")\n        ruta = os.path.join(\n            "exportaciones",\n            f"cola_{sello}.csv"\n        )\n\n        columnas = [\n            "orden",\n            "estado_cola",\n            "titulo",\n            "precio",\n            "categoria",\n            "estado_producto",\n            "desactivar_envio",\n            "intentos",\n            "error_fase",\n            "error_motivo",\n            "error_fecha",\n            "carpeta",\n        ]\n\n        with open(\n            ruta,\n            "w",\n            encoding="utf-8-sig",\n            newline=""\n        ) as archivo:\n            writer = csv.DictWriter(\n                archivo,\n                fieldnames=columnas\n            )\n            writer.writeheader()\n\n            for i, item in enumerate(\n                cola_anuncios,\n                start=1\n            ):\n                writer.writerow({\n                    "orden": i,\n                    "estado_cola": item.get("estado_cola", ""),\n                    "titulo": item.get("titulo", ""),\n                    "precio": item.get("precio", ""),\n                    "categoria": item.get("categoria", ""),\n                    "estado_producto": item.get("estado", ""),\n                    "desactivar_envio": item.get("desactivar_envio", ""),\n                    "intentos": item.get("intentos", 0),\n                    "error_fase": item.get("error_fase", ""),\n                    "error_motivo": item.get("error_motivo", ""),\n                    "error_fecha": item.get("error_fecha", ""),\n                    "carpeta": item.get("carpeta", ""),\n                })\n\n        estado.configure(\n            text=f"✅ Cola exportada: {ruta}"\n        )\n\n        try:\n            if os.name == "nt":\n                os.startfile(\n                    os.path.abspath("exportaciones")\n                )\n        except Exception:\n            pass\n\n    except Exception as e:\n        estado.configure(\n            text="❌ No pude exportar la cola: " + str(e)\n        )\n\n\n# =========================================================\n# SUITE PRO · CALIDAD, PERFILES, HISTORIAL Y PRODUCTIVIDAD\n# =========================================================\n\nARCHIVO_PERFILES_LOTE = "perfiles_lote.json"\nperfiles_lote = {}\n\n\ndef perfiles_lote_por_defecto():\n    return {\n        "Diagnosis": {\n            "servicio": "Diagnosis electrónica automóvil",\n            "precio": "25",\n            "cantidad": 5,\n            "categoria": "Motor y accesorios > Recambios de coches y furgonetas > Tuning",\n            "estado": "Nuevo",\n            "desactivar_envio": True\n        },\n        "Tuning": {\n            "servicio": "Tuning y preparación automóvil",\n            "precio": "30",\n            "cantidad": 5,\n            "categoria": "Motor y accesorios > Recambios de coches y furgonetas > Tuning",\n            "estado": "Nuevo",\n            "desactivar_envio": True\n        },\n        "Maquinaria agrícola": {\n            "servicio": "Diagnosis y reparación maquinaria agrícola",\n            "precio": "40",\n            "cantidad": 3,\n            "categoria": "Motor y accesorios > Recambios de coches y furgonetas > Tuning",\n            "estado": "Nuevo",\n            "desactivar_envio": True\n        },\n        "Industrial": {\n            "servicio": "Diagnosis vehículo industrial y furgonetas",\n            "precio": "40",\n            "cantidad": 4,\n            "categoria": "Motor y accesorios > Recambios de coches y furgonetas > Tuning",\n            "estado": "Nuevo",\n            "desactivar_envio": True\n        }\n    }\n\n\ndef cargar_perfiles_lote():\n    global perfiles_lote\n\n    datos = perfiles_lote_por_defecto()\n\n    if os.path.isfile(ARCHIVO_PERFILES_LOTE):\n        try:\n            with open(\n                ARCHIVO_PERFILES_LOTE,\n                "r",\n                encoding="utf-8"\n            ) as f:\n                cargado = json.load(f)\n\n            if isinstance(cargado, dict):\n                datos.update(cargado)\n        except Exception as e:\n            print("Aviso perfiles de lote:", e)\n\n    perfiles_lote = datos\n    guardar_perfiles_lote()\n\n\ndef guardar_perfiles_lote():\n    try:\n        with open(\n            ARCHIVO_PERFILES_LOTE,\n            "w",\n            encoding="utf-8"\n        ) as f:\n            json.dump(\n                perfiles_lote,\n                f,\n                ensure_ascii=False,\n                indent=2\n            )\n    except Exception as e:\n        print("No pude guardar perfiles de lote:", e)\n\n\ndef actualizar_menu_perfiles_lote():\n    try:\n        nombres = sorted(perfiles_lote.keys(), key=str.lower)\n        if not nombres:\n            nombres = ["Sin perfiles"]\n\n        menu_perfil_lote.configure(values=nombres)\n\n        actual = var_perfil_lote_actual.get().strip()\n        if actual not in nombres:\n            var_perfil_lote_actual.set(nombres[0])\n    except Exception:\n        pass\n\n\ndef aplicar_perfil_lote(nombre=None):\n    if nombre is None:\n        nombre = var_perfil_lote_actual.get().strip()\n\n    datos = perfiles_lote.get(nombre)\n\n    if not isinstance(datos, dict):\n        estado.configure(text="❌ Perfil de lote no encontrado.")\n        return\n\n    entrada_servicio.delete(0, "end")\n    entrada_servicio.insert(0, datos.get("servicio", ""))\n\n    entrada_precio.delete(0, "end")\n    entrada_precio.insert(0, str(datos.get("precio", "")))\n\n    var_cantidad_lote.set(str(datos.get("cantidad", 3)))\n    categoria_publicacion.set(datos.get("categoria", categoria_publicacion.get()))\n    estado_producto_publicacion.set(datos.get("estado", "Nuevo"))\n    switch_desactivar_envio.set(bool(datos.get("desactivar_envio", True)))\n\n    refrescar_resumen_publicacion()\n\n    estado.configure(\n        text=f"✅ Perfil de lote aplicado: {nombre}"\n    )\n\n\ndef guardar_perfil_lote_actual():\n    nombre = var_nombre_perfil_lote.get().strip()\n\n    if not nombre:\n        estado.configure(text="❌ Escribe un nombre para el perfil.")\n        return\n\n    try:\n        cantidad = int(var_cantidad_lote.get())\n    except Exception:\n        cantidad = 3\n\n    if not requerir_funcion_plan(\n        "lotes",\n        "Generación por lotes"\n    ):\n        return\n\n    max_lote_plan = (limite_cola_efectivo() if es_edicion_admin() else limite_lote_plan())\n\n    if cantidad > max_lote_plan:\n        estado.configure(\n            text=(\n                f"❌ Tu plan permite como máximo "\n                f"{max_lote_plan} anuncios por lote."\n            )\n        )\n        return\n\n    perfiles_lote[nombre] = {\n        "servicio": entrada_servicio.get().strip(),\n        "precio": entrada_precio.get().strip(),\n        "cantidad": cantidad,\n        "categoria": categoria_publicacion.get().strip(),\n        "estado": estado_producto_publicacion.get().strip(),\n        "desactivar_envio": bool(switch_desactivar_envio.get())\n    }\n\n    guardar_perfiles_lote()\n    actualizar_menu_perfiles_lote()\n    var_perfil_lote_actual.set(nombre)\n\n    estado.configure(\n        text=f"✅ Perfil de lote guardado: {nombre}"\n    )\n\n\ndef eliminar_perfil_lote_actual():\n    nombre = var_perfil_lote_actual.get().strip()\n\n    if nombre in perfiles_lote:\n        perfiles_lote.pop(nombre, None)\n        guardar_perfiles_lote()\n        actualizar_menu_perfiles_lote()\n        estado.configure(text=f"Perfil eliminado: {nombre}")\n\n\ndef leer_anuncio_txt_completo(carpeta):\n    resultado = {\n        "titulo": "",\n        "precio": "",\n        "descripcion": ""\n    }\n\n    ruta = Path(carpeta) / "anuncio.txt"\n\n    if not ruta.exists():\n        return resultado\n\n    try:\n        contenido = ruta.read_text(\n            encoding="utf-8",\n            errors="ignore"\n        )\n\n        m = re.search(\n            r"TÍTULO:\\s*\\n([^\\n]+)",\n            contenido,\n            re.IGNORECASE\n        )\n        if m:\n            resultado["titulo"] = m.group(1).strip()\n\n        m = re.search(\n            r"PRECIO:\\s*\\n([^\\n]+)",\n            contenido,\n            re.IGNORECASE\n        )\n        if m:\n            resultado["precio"] = m.group(1).replace("€", "").strip()\n\n        m = re.search(\n            r"DESCRIPCIÓN:\\s*\\n(.+)$",\n            contenido,\n            re.IGNORECASE | re.DOTALL\n        )\n        if m:\n            resultado["descripcion"] = m.group(1).strip()\n\n    except Exception:\n        pass\n\n    return resultado\n\n\ndef _historial_elementos():\n    base = Path(CARPETA_ANUNCIOS)\n\n    if not base.exists():\n        return []\n\n    elementos = []\n\n    for carpeta in base.iterdir():\n        if not carpeta.is_dir():\n            continue\n\n        datos_estado = leer_estado_anuncio(carpeta)\n        datos_txt = leer_anuncio_txt_completo(carpeta)\n\n        titulo = (\n            datos_estado.get("titulo", "").strip()\n            or datos_txt.get("titulo", "").strip()\n            or carpeta.name\n        )\n\n        precio = (\n            datos_estado.get("precio", "").strip()\n            or datos_txt.get("precio", "").strip()\n        )\n\n        elementos.append({\n            "carpeta": str(carpeta.resolve()),\n            "titulo": titulo,\n            "precio": precio,\n            "descripcion": datos_txt.get("descripcion", ""),\n            "estado": str(datos_estado.get("estado", "GENERADO")).upper(),\n            "fecha": datos_estado.get(\n                "fecha_estado",\n                time.strftime(\n                    "%d/%m/%Y %H:%M",\n                    time.localtime(carpeta.stat().st_mtime)\n                )\n            ),\n            "mtime": carpeta.stat().st_mtime\n        })\n\n    elementos.sort(\n        key=lambda x: x["mtime"],\n        reverse=True\n    )\n\n    return elementos\n\n\ndef actualizar_selector_historial():\n    try:\n        items = _historial_elementos()\n\n        valores = [\n            f"{i + 1:03d} · {x[\'estado\']} · {x[\'titulo\'][:45]}"\n            for i, x in enumerate(items[:200])\n        ]\n\n        if not valores:\n            valores = ["Sin anuncios"]\n\n        menu_historial_inteligente.configure(values=valores)\n\n        if var_historial_selector.get() not in valores:\n            var_historial_selector.set(valores[0])\n\n    except Exception:\n        pass\n\n\ndef _indice_historial_seleccionado():\n    valor = var_historial_selector.get().strip()\n\n    if not valor or valor == "Sin anuncios":\n        return None\n\n    try:\n        return int(valor.split("·", 1)[0].strip()) - 1\n    except Exception:\n        return None\n\n\ndef cargar_historial_en_editor():\n    global ultima_carpeta_anuncio\n\n    indice = _indice_historial_seleccionado()\n    items = _historial_elementos()\n\n    if indice is None or indice >= len(items):\n        estado.configure(text="Selecciona un anuncio del historial.")\n        return\n\n    item = items[indice]\n\n    caja_titulo.delete("1.0", "end")\n    caja_titulo.insert("1.0", item["titulo"])\n\n    caja_descripcion.delete("1.0", "end")\n    caja_descripcion.insert("1.0", item["descripcion"])\n\n    entrada_precio.delete(0, "end")\n    entrada_precio.insert(0, item["precio"])\n\n    entrada_servicio.delete(0, "end")\n    entrada_servicio.insert(0, item["titulo"])\n\n    ultima_carpeta_anuncio = item["carpeta"]\n\n    actualizar_contadores_profesionales()\n    refrescar_resumen_publicacion()\n\n    try:\n        tabs.set("Crear anuncio")\n    except Exception:\n        pass\n\n    estado.configure(\n        text="✅ Anuncio del historial cargado en el editor."\n    )\n\n\ndef recrear_parecido_desde_historial():\n    indice = _indice_historial_seleccionado()\n    items = _historial_elementos()\n\n    if indice is None or indice >= len(items):\n        estado.configure(text="Selecciona un anuncio del historial.")\n        return\n\n    item = items[indice]\n\n    base = _titulo_base_sin_variante(item["titulo"])\n\n    entrada_servicio.delete(0, "end")\n    entrada_servicio.insert(\n        0,\n        _crear_titulo_reemplazo_unico(\n            base,\n            random.randint(1, 1000)\n        )\n    )\n\n    entrada_precio.delete(0, "end")\n    entrada_precio.insert(0, item["precio"])\n\n    generar_y_anadir_a_cola()\n\n    estado.configure(\n        text="✅ Se ha creado una variación nueva basada en el historial."\n    )\n\n\ndef calcular_calidad_item(item):\n    """\n    Puntuación práctica 0-100.\n    """\n    score = 100\n    problemas = []\n\n    titulo = str(item.get("titulo", "")).strip()\n    descripcion = str(item.get("descripcion", "")).strip()\n    precio = str(item.get("precio", "")).strip()\n    categoria = str(item.get("categoria", "")).strip()\n    imagenes = item.get("imagenes", [])\n\n    if not titulo:\n        score -= 35\n        problemas.append("sin título")\n    elif len(titulo) < 12:\n        score -= 10\n        problemas.append("título muy corto")\n    elif len(titulo) > 50:\n        score -= 20\n        problemas.append("título demasiado largo")\n\n    if not descripcion:\n        score -= 30\n        problemas.append("sin descripción")\n    elif len(descripcion) < 120:\n        score -= 10\n        problemas.append("descripción corta")\n\n    if not precio:\n        score -= 20\n        problemas.append("sin precio")\n\n    if not categoria:\n        score -= 15\n        problemas.append("sin categoría")\n\n    if len(imagenes) != 1:\n        score -= 25\n        problemas.append("imagen incorrecta")\n    else:\n        ruta = imagenes[0]\n\n        try:\n            if not os.path.isfile(ruta):\n                score -= 25\n                problemas.append("imagen ausente")\n            else:\n                with Image.open(ruta) as im:\n                    if im.width < 800 or im.height < 800:\n                        score -= 8\n                        problemas.append("resolución baja")\n        except Exception:\n            score -= 20\n            problemas.append("imagen dañada")\n\n    try:\n        if anuncio_ya_publicado(\n            titulo,\n            descripcion,\n            precio\n        ):\n            score -= 30\n            problemas.append("duplicado publicado")\n    except Exception:\n        pass\n\n    return max(0, min(100, score)), problemas\n\n\ndef validar_calidad_toda_cola():\n    if not cola_anuncios:\n        estado.configure(text="La cola está vacía.")\n        return\n\n    bajos = 0\n\n    for item in cola_anuncios:\n        score, problemas = calcular_calidad_item(item)\n        item["calidad"] = score\n        item["calidad_problemas"] = problemas\n\n        if score < 70 and item.get("estado_cola") == "PENDIENTE":\n            bajos += 1\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n\n    estado.configure(\n        text=(\n            f"✅ Control de calidad terminado · "\n            f"{bajos} anuncio(s) por debajo de 70/100."\n        )\n    )\n\n\ndef _items_filtrados_cola():\n    filtro = var_filtro_cola.get().strip().upper()\n\n    if filtro == "TODOS":\n        return list(cola_anuncios)\n\n    return [\n        x for x in cola_anuncios\n        if str(x.get("estado_cola", "")).upper() == filtro\n    ]\n\n\ndef aplicar_edicion_masiva_cola():\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ No puedes editar en bloque mientras la cola publica."\n        )\n        return\n\n    items = _items_filtrados_cola()\n\n    if not items:\n        estado.configure(text="No hay anuncios que coincidan con el filtro.")\n        return\n\n    precio = var_precio_masivo.get().strip()\n    categoria = var_categoria_masiva.get().strip()\n    estado_m = var_estado_masivo.get().strip()\n    envio_m = var_envio_masivo.get().strip()\n\n    cambios = 0\n\n    for item in items:\n        if item.get("estado_cola") == "PUBLICADO":\n            continue\n\n        if precio:\n            item["precio"] = precio.replace("€", "").strip()\n\n        if categoria:\n            item["categoria"] = categoria\n\n        if estado_m != "Sin cambios":\n            item["estado"] = estado_m\n\n        if envio_m == "Desactivar":\n            item["desactivar_envio"] = True\n        elif envio_m == "Mantener":\n            item["desactivar_envio"] = False\n\n        item["estado_cola"] = "PENDIENTE"\n        item["error_fase"] = ""\n        item["error_motivo"] = ""\n        cambios += 1\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n\n    estado.configure(\n        text=f"✅ Edición masiva aplicada a {cambios} anuncio(s)."\n    )\n\n\ndef duplicar_seleccionado_con_variacion():\n    if not requerir_funcion_plan(\n        "duplicar_variacion",\n        "Duplicar con variación"\n    ):\n        return\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ Espera a que termine la cola."\n        )\n        return\n\n    if len(cola_anuncios) >= limite_cola_efectivo():\n        estado.configure(text="❌ La cola está llena.")\n        return\n\n    indice = _indice_seleccionado_cola()\n\n    if indice is None:\n        estado.configure(text="Selecciona un anuncio.")\n        return\n\n    original = cola_anuncios[indice]\n    nuevo = dict(original)\n\n    nuevo["estado_cola"] = "PENDIENTE"\n    nuevo["intentos"] = 0\n    nuevo["error_fase"] = ""\n    nuevo["error_motivo"] = ""\n    nuevo["error_fecha"] = ""\n    nuevo["error_diagnostico"] = ""\n\n    cola_anuncios.append(nuevo)\n\n    ok = reemplazar_duplicado_por_anuncio_nuevo(nuevo)\n\n    if not ok:\n        cola_anuncios.remove(nuevo)\n        estado.configure(\n            text="❌ No pude crear una variación nueva."\n        )\n        return\n\n    guardar_cola_en_disco()\n    refrescar_cola()\n\n    estado.configure(\n        text="✅ Variación nueva creada y añadida a la cola."\n    )\n\n\ndef detectar_cambios_wallapop():\n    """\n    Diagnóstico rápido de elementos críticos de la web.\n    No publica nada.\n    """\n    resultados = []\n\n    try:\n        _asegurar_chromium_controlable()\n\n        if sync_playwright is None:\n            raise RuntimeError("Playwright no está disponible.")\n\n        with sync_playwright() as p:\n            navegador = _conectar_playwright_cdp(p)\n\n            if not navegador.contexts:\n                raise RuntimeError("Sin contexto de navegador.")\n\n            contexto = navegador.contexts[0]\n            pagina = _buscar_pagina_wallapop(contexto)\n\n            _comprobar_sesion_wallapop(pagina)\n\n            checks = [\n                ("Wallapop cargado", lambda: "wallapop.com" in pagina.url.lower()),\n                (\n                    "Botón Vender",\n                    lambda: bool(\n                        pagina.get_by_text(\n                            re.compile(r"^Vender$", re.I)\n                        ).count()\n                    )\n                ),\n                (\n                    "DOM operativo",\n                    lambda: bool(\n                        pagina.evaluate(\n                            "() => document.body && document.readyState !== \'loading\'"\n                        )\n                    )\n                ),\n            ]\n\n            for nombre, fn in checks:\n                try:\n                    ok = bool(fn())\n                except Exception:\n                    ok = False\n\n                resultados.append((nombre, ok))\n\n    except Exception as e:\n        resultados.append(("Conexión Wallapop", False))\n        print("Detector de cambios:", e)\n\n    fallos = [\n        nombre\n        for nombre, ok in resultados\n        if not ok\n    ]\n\n    texto = "\\n".join(\n        ("✅ " if ok else "❌ ") + nombre\n        for nombre, ok in resultados\n    )\n\n    try:\n        texto_diagnostico.configure(state="normal")\n        texto_diagnostico.delete("1.0", "end")\n        texto_diagnostico.insert(\n            "1.0",\n            "DETECTOR DE CAMBIOS DE WALLAPOP\\n"\n            "================================\\n\\n"\n            + texto\n            + (\n                "\\n\\n⚠️ Hay elementos que han cambiado o no se han detectado."\n                if fallos\n                else\n                "\\n\\n✅ Los elementos principales siguen disponibles."\n            )\n        )\n        texto_diagnostico.configure(state="disabled")\n        tabs.set("Diagnóstico")\n    except Exception:\n        pass\n\n    if fallos:\n        estado.configure(\n            text=(\n                "⚠️ Posibles cambios en Wallapop: "\n                + ", ".join(fallos)\n            )\n        )\n    else:\n        estado.configure(\n            text="✅ Wallapop parece compatible con el bot."\n        )\n\n\ndef generar_resumen_sesion_cola():\n    total = len(cola_anuncios)\n    publicados = sum(\n        1 for x in cola_anuncios\n        if x.get("estado_cola") == "PUBLICADO"\n    )\n    errores = sum(\n        1 for x in cola_anuncios\n        if x.get("estado_cola") == "ERROR"\n    )\n    pendientes = sum(\n        1 for x in cola_anuncios\n        if x.get("estado_cola") == "PENDIENTE"\n    )\n    sustituciones = sum(\n        int(x.get("reemplazos_duplicado", 0))\n        for x in cola_anuncios\n    )\n\n    duracion = 0\n    if cola_inicio_sesion:\n        duracion = int(time.time() - cola_inicio_sesion)\n\n    return (\n        f"RESUMEN DE SESIÓN\\n"\n        f"=================\\n\\n"\n        f"Total en cola: {total}\\n"\n        f"Publicados: {publicados}\\n"\n        f"Errores: {errores}\\n"\n        f"Pendientes: {pendientes}\\n"\n        f"Duplicados sustituidos: {sustituciones}\\n"\n        f"Duración: {duracion // 60:02d}:{duracion % 60:02d}\\n"\n        f"Media/anuncio: "\n        f"{(sum(rendimiento_tiempos_publicacion) / len(rendimiento_tiempos_publicacion)) if rendimiento_tiempos_publicacion else 0:.1f}s"\n    )\n\n\ndef mostrar_resumen_sesion_cola():\n    resumen = generar_resumen_sesion_cola()\n\n    try:\n        messagebox.showinfo(\n            "Resumen de la cola",\n            resumen\n        )\n    except Exception:\n        pass\n\n\ndef preparar_build_exe():\n    """\n    Crea un .BAT para compilar con PyInstaller.\n    No instala nada automáticamente.\n    """\n    try:\n        script_actual = os.path.abspath(sys.argv[0])\n        nombre_py = os.path.basename(script_actual)\n\n        contenido = (\n            "@echo off\\n"\n            "title Compilar WallapopBot\\n"\n            "echo Comprobando PyInstaller...\\n"\n            "python -m pip show pyinstaller >nul 2>&1\\n"\n            "if errorlevel 1 (\\n"\n            "  echo Instalando PyInstaller...\\n"\n            "  python -m pip install pyinstaller\\n"\n            ")\\n"\n            "echo.\\n"\n            "echo Creando ejecutable...\\n"\n            f\'python -m PyInstaller --noconfirm --clean --onefile --windowed \'\n            f\'--name "WallapopBot_DIAGPROG5" "{nombre_py}"\\n\'\n            "echo.\\n"\n            "echo Terminado. Mira la carpeta dist.\\n"\n            "pause\\n"\n        )\n\n        ruta = Path("crear_exe.bat")\n        ruta.write_text(\n            contenido,\n            encoding="utf-8"\n        )\n\n        estado.configure(\n            text="✅ crear_exe.bat preparado junto al programa."\n        )\n\n    except Exception as e:\n        estado.configure(\n            text="❌ No pude preparar el build EXE: " + str(e)\n        )\n\n\n# =========================================================\n# HERRAMIENTAS DE INTERFAZ PROFESIONAL\n# =========================================================\n\ndef preparar_anuncio_sin_publicar():\n    """Completa Wallapop y se detiene antes de pulsar Publicar."""\n    switch_publicar_automaticamente.set(False)\n    refrescar_resumen_publicacion()\n    preparar_wallapop()\n\n\ndef publicar_anuncio_directamente():\n    """Completa Wallapop y pulsa Publicar al terminar."""\n    switch_publicar_automaticamente.set(True)\n    refrescar_resumen_publicacion()\n    preparar_wallapop()\n\n\n\ndef limpiar_formulario():\n    entrada_servicio.delete(0, "end")\n    entrada_precio.delete(0, "end")\n    entrada_contacto.delete(0, "end")\n\n    caja_titulo.delete("1.0", "end")\n    caja_descripcion.delete("1.0", "end")\n\n    try:\n        check_contacto_imagen.deselect()\n        check_contacto_descripcion.deselect()\n    except Exception:\n        pass\n\n    global imagen_preview_ctk\n\n    for vista in vistas:\n        try:\n            vista.configure(text="Sin imagen")\n        except Exception:\n            pass\n\n    imagen_preview_ctk = None\n\n    contador_descripcion.configure(text="0 / 500 caracteres")\n    estado.configure(text="Formulario limpio.")\n\n\ndef copiar_titulo():\n    texto = caja_titulo.get("1.0", "end-1c").strip()\n    if not texto:\n        estado.configure(text="No hay título para copiar.")\n        return\n\n    ventana.clipboard_clear()\n    ventana.clipboard_append(texto)\n    estado.configure(text="Título copiado.")\n\n\ndef copiar_descripcion():\n    texto = obtener_descripcion_actual().strip()\n    if not texto:\n        estado.configure(text="No hay descripción para copiar.")\n        return\n\n    ventana.clipboard_clear()\n    ventana.clipboard_append(texto)\n    estado.configure(text="Descripción copiada.")\n\n\ndef abrir_carpeta_anuncio():\n    global ultima_carpeta_anuncio\n\n    if ultima_carpeta_anuncio and os.path.isdir(ultima_carpeta_anuncio):\n        try:\n            os.startfile(ultima_carpeta_anuncio)\n            estado.configure(text="Carpeta del anuncio abierta.")\n            return\n        except Exception as e:\n            estado.configure(text=f"No pude abrir la carpeta: {e}")\n            return\n\n    estado.configure(text="Todavía no hay una carpeta de anuncio generada.")\n\n\ndef comprobar_navegador():\n    datos = _datos_cdp()\n\n    if datos:\n        navegador = datos.get("Browser", "Chromium")\n        estado_navegador.configure(\n            text=f"● Conectado · puerto {obtener_puerto_chromium()}",\n            text_color="#46d47a"\n        )\n        estado.configure(\n            text=f"Chromium listo para publicar · {navegador}"\n        )\n        return True\n\n    ejecutable = _encontrar_chromium()\n\n    estado_navegador.configure(\n        text="● Desconectado",\n        text_color="#ff6b6b"\n    )\n\n    if ejecutable:\n        estado.configure(\n            text="Chromium encontrado. El bot lo abrirá automáticamente."\n        )\n    else:\n        estado.configure(\n            text="❌ No encuentro Chromium/Chrome en el equipo."\n        )\n\n    return False\n\ndef abrir_wallapop():\n    """\n    Abre Wallapop dentro del Chromium que controla el bot.\n    """\n    try:\n        estado.configure(\n            text="⏳ Abriendo Chromium y Wallapop..."\n        )\n\n        _asegurar_chromium_controlable()\n\n        if sync_playwright is None:\n            raise RuntimeError(\n                "Playwright no está instalado."\n            )\n\n        with sync_playwright() as p:\n            navegador = _conectar_playwright_cdp(p)\n\n            if not navegador.contexts:\n                raise RuntimeError(\n                    "No encuentro el contexto de Chromium."\n                )\n\n            contexto = navegador.contexts[0]\n            pagina = _buscar_pagina_wallapop(contexto)\n            pagina.bring_to_front()\n\n            _comprobar_sesion_wallapop(pagina)\n\n        estado.configure(\n            text="✅ Wallapop abierto en el navegador controlado."\n        )\n\n        ventana.after(\n            500,\n            comprobar_navegador\n        )\n\n    except Exception as e:\n        estado.configure(\n            text=f"❌ No pude abrir Wallapop: {e}"\n        )\n\n        try:\n            messagebox.showerror(\n                "Navegador",\n                str(e)\n            )\n        except Exception:\n            pass\n\ndef refrescar_resumen_publicacion():\n    servicio = entrada_servicio.get().strip() or "Sin definir"\n    precio = entrada_precio.get().strip() or "—"\n    contacto = entrada_contacto.get().strip() or "No indicado"\n\n    try:\n        auto = "Sí" if switch_publicar_automaticamente.get() else "No"\n        envio = "Desactivado" if switch_desactivar_envio.get() else "Mantener"\n    except Exception:\n        auto = "Sí"\n        envio = "Desactivado"\n\n    texto = (\n        f"ANUNCIO\\n"\n        f"Servicio: {servicio}\\n"\n        f"Precio: {precio} €\\n"\n        f"Contacto: {contacto}\\n\\n"\n        f"WALLAPOP\\n"\n        f"Tipo: Algo que ya no necesito\\n"\n        f"Categoría: {categoria_publicacion.get().strip()}\\n"\n        f"Estado: {estado_producto_publicacion.get().strip()}\\n"\n        f"Envío: {envio}\\n"\n        f"Localización: la guardada en tu perfil\\n"\n        f"Imágenes: 1"\n    )\n\n    resumen_publicacion.configure(state="normal")\n    resumen_publicacion.delete("1.0", "end")\n    resumen_publicacion.insert("1.0", texto)\n    resumen_publicacion.configure(state="disabled")\n\n\ndef refrescar_historial():\n    historial_texto.configure(state="normal")\n    historial_texto.delete("1.0", "end")\n\n    base = Path(CARPETA_ANUNCIOS)\n\n    if not base.exists():\n        historial_texto.insert(\n            "1.0",\n            "Todavía no hay anuncios generados."\n        )\n        historial_texto.configure(state="disabled")\n        return\n\n    corte = _cargar_corte_historial()\n\n    carpetas = [\n        p for p in base.iterdir()\n        if p.is_dir() and p.stat().st_mtime > corte\n    ]\n\n    carpetas.sort(\n        key=lambda p: p.stat().st_mtime,\n        reverse=True\n    )\n\n    filas = []\n\n    for carpeta in carpetas:\n        datos_estado = leer_estado_anuncio(carpeta)\n        datos_txt = leer_datos_anuncio_txt(carpeta)\n\n        estado_actual = str(\n            datos_estado.get("estado", "GENERADO")\n        ).upper()\n\n        if filtro_historial == "PUBLICADO" and estado_actual != "PUBLICADO":\n            continue\n\n        titulo = (\n            datos_estado.get("titulo", "").strip()\n            or datos_txt.get("titulo", "").strip()\n            or carpeta.name\n        )\n\n        precio = (\n            datos_estado.get("precio", "").strip()\n            or datos_txt.get("precio", "").strip()\n            or "—"\n        )\n\n        fecha = (\n            datos_estado.get("fecha_estado", "").strip()\n            or time.strftime(\n                "%d/%m/%Y %H:%M",\n                time.localtime(carpeta.stat().st_mtime)\n            )\n        )\n\n        iconos = {\n            "PUBLICADO": "✅",\n            "PREPARADO": "🟡",\n            "GENERADO": "🟦"\n        }\n\n        icono = iconos.get(estado_actual, "⬜")\n        imagenes = len(list(carpeta.glob("imagen_*.png")))\n\n        filas.append(\n            f"{icono} {estado_actual:<9}  "\n            f"{titulo[:42]:<42}  "\n            f"{precio:>8} €  "\n            f"{fecha}  ·  {imagenes} img"\n        )\n\n    if not filas:\n        if filtro_historial == "PUBLICADO":\n            mensaje = "No hay anuncios publicados en el historial visible."\n        else:\n            mensaje = "No hay anuncios en el historial visible."\n\n        historial_texto.insert("1.0", mensaje)\n    else:\n        cabecera = (\n            "ESTADO      TÍTULO                                       "\n            "PRECIO      FECHA\\n"\n            + "─" * 105\n            + "\\n"\n        )\n\n        historial_texto.insert(\n            "1.0",\n            cabecera + "\\n".join(filas)\n        )\n\n    historial_texto.configure(state="disabled")\n\n    try:\n        actualizar_selector_historial()\n    except Exception:\n        pass\n\n\nclass SalidaInterfaz:\n    def __init__(self, original):\n        self.original = original\n\n    def write(self, texto):\n        try:\n            self.original.write(texto)\n            self.original.flush()\n        except Exception:\n            pass\n\n        if not texto:\n            return\n\n        escribir_log_archivo(texto)\n\n        try:\n            ventana.after(\n                0,\n                lambda t=texto: agregar_log(t)\n            )\n        except Exception:\n            pass\n\n    def flush(self):\n        try:\n            self.original.flush()\n        except Exception:\n            pass\n\n\ndef agregar_log(texto):\n    if not texto:\n        return\n\n    try:\n        log_texto.configure(state="normal")\n        log_texto.insert("end", texto)\n        log_texto.see("end")\n        log_texto.configure(state="disabled")\n    except Exception:\n        pass\n\n\ndef contar_pestanas_wallapop():\n    try:\n        if sync_playwright is None:\n            return -1\n\n        with sync_playwright() as p:\n            navegador = _conectar_playwright_cdp(p)\n\n            if not navegador.contexts:\n                return 0\n\n            contexto = navegador.contexts[0]\n            total = 0\n\n            for pagina in contexto.pages:\n                try:\n                    if (\n                        not pagina.is_closed()\n                        and "wallapop.com" in (pagina.url or "").lower()\n                    ):\n                        total += 1\n                except Exception:\n                    pass\n\n            return total\n\n    except Exception:\n        return -1\n\n\ndef diagnostico_navegador():\n    """\n    Muestra en el Registro datos útiles del navegador.\n    """\n    print()\n    print("========== DIAGNÓSTICO NAVEGADOR ==========")\n\n    ejecutable = _encontrar_chromium()\n    print("Ejecutable encontrado:", ejecutable or "NO")\n\n    datos = _datos_cdp()\n    if datos:\n        print(f"Puerto {obtener_puerto_chromium()}: ACTIVO")\n        print("Browser:", datos.get("Browser", "Desconocido"))\n        print(\n            "WebSocket:",\n            "OK" if datos.get("webSocketDebuggerUrl") else "No disponible"\n        )\n    else:\n        print(f"Puerto {obtener_puerto_chromium()}: INACTIVO")\n\n    print("Perfil configurado:", _perfil_chromium_bot())\n\n    try:\n        total_wallapop = contar_pestanas_wallapop()\n        print("Pestañas Wallapop abiertas:", total_wallapop)\n    except Exception:\n        pass\n\n    print("============================================")\n    print()\n\n    estado.configure(\n        text="Diagnóstico del navegador añadido al Registro."\n    )\n\n\ndef limpiar_log():\n    log_texto.configure(state="normal")\n    log_texto.delete("1.0", "end")\n    log_texto.configure(state="disabled")\n\n\ndef actualizar_contadores_profesionales(event=None):\n    controlar_descripcion(event)\n\n    titulo_actual = caja_titulo.get("1.0", "end-1c")\n    contador_titulo.configure(\n        text=f"{len(titulo_actual)} / 50"\n    )\n\n    refrescar_resumen_publicacion()\n\n\ndef al_generar_anuncio():\n    generar_anuncio()\n    ventana.after(200, refrescar_resumen_publicacion)\n    ventana.after(300, refrescar_historial)\n\n\ndef generar_y_anadir_a_cola():\n    """\n    Genera un anuncio completo y, si la generación termina correctamente,\n    lo añade automáticamente a la cola.\n    """\n    global ultima_carpeta_anuncio\n\n    if cola_en_ejecucion:\n        estado.configure(\n            text="❌ Espera a que termine la cola antes de generar otro anuncio."\n        )\n        return\n\n    if len(cola_anuncios) >= limite_cola_efectivo():\n        estado.configure(\n            text=f"❌ La cola ya tiene el máximo permitido: {limite_cola_efectivo()}."\n        )\n        return\n\n    carpeta_anterior = ultima_carpeta_anuncio\n\n    generar_anuncio()\n\n    # Solo añadimos si realmente se creó un anuncio nuevo.\n    if (\n        not ultima_carpeta_anuncio\n        or ultima_carpeta_anuncio == carpeta_anterior\n        or not os.path.isdir(ultima_carpeta_anuncio)\n    ):\n        estado.configure(\n            text="❌ El anuncio no se generó correctamente; no se añadió a la cola."\n        )\n        return\n\n    try:\n        agregar_actual_a_cola()\n        refrescar_resumen_publicacion()\n        refrescar_historial()\n        refrescar_cola()\n\n        estado.configure(\n            text=(\n                f"✅ Anuncio generado y añadido a la cola · "\n                f"{len(cola_anuncios)} anuncio(s)."\n            )\n        )\n    except Exception as e:\n        estado.configure(\n            text="❌ El anuncio se generó, pero no pude añadirlo a la cola: " + str(e)\n        )\n\n\n\n\n# =========================================================\n# INTERFAZ\n# =========================================================\n\nctk.set_appearance_mode("dark")\nctk.set_default_color_theme("blue")\n\nventana = ctk.CTk()\nventana.title("DIAGPROG5 - WALLAPOP BOT (ADMIN) · V107 INSTALACIÓN SEGURA")\nventana.geometry("1680x950")\nventana.minsize(1280, 800)\n\n# Variables de gestión\nswitch_publicar_automaticamente = ctk.BooleanVar(value=True)\nswitch_desactivar_envio = ctk.BooleanVar(value=True)\n\nvar_pausa_publicaciones = ctk.StringVar(value="8")\nvar_pausa_min_publicaciones = ctk.StringVar(value="8")\nvar_pausa_max_publicaciones = ctk.StringVar(value="15")\nvar_max_errores_consecutivos = ctk.StringVar(value="3")\nvar_espera_despues_error = ctk.StringVar(value="20")\nvar_max_publicaciones_sesion = ctk.StringVar(value="10")\nvar_watchdog_minutos = ctk.StringVar(value="4")\nvar_modo_rendimiento = ctk.BooleanVar(value=True)\nvar_cantidad_lote = ctk.StringVar(value="3")\nvar_filtro_cola = ctk.StringVar(value="TODOS")\nvar_precio_masivo = ctk.StringVar(value="")\nvar_categoria_masiva = ctk.StringVar(value="")\nvar_estado_masivo = ctk.StringVar(value="Sin cambios")\nvar_envio_masivo = ctk.StringVar(value="Sin cambios")\nvar_nombre_perfil_lote = ctk.StringVar(value="")\nvar_perfil_lote_actual = ctk.StringVar(value="Diagnosis")\nvar_historial_selector = ctk.StringVar(value="Sin anuncios")\n\nvar_limpiar_pestanas_cada = ctk.StringVar(value="1")\nvar_limpiar_memoria_cada = ctk.StringVar(value="3")\nvar_timeout_respuesta_wallapop = ctk.StringVar(value="12")\nvar_auto_recuperar_navegador = ctk.BooleanVar(value=True)\nvar_max_recuperaciones_anuncio = ctk.StringVar(value="2")\nvar_espera_recuperacion_navegador = ctk.StringVar(value="5")\nvar_supervisor_automatico = ctk.BooleanVar(value=True)\nvar_pausar_si_supervisor_falla = ctk.BooleanVar(value=True)\nvar_max_intentos_supervisor = ctk.StringVar(value="2")\nvar_modo_prueba = ctk.BooleanVar(value=False)\nvar_limite_publicaciones_dia = ctk.StringVar(value="25")\nvar_hora_inicio_programada = ctk.StringVar(value="")\nvar_dias_retencion_archivos = ctk.StringVar(value="14")\nvar_estrategia_publicacion = ctk.StringVar(value="Normal")\nvar_ritmo_adaptativo = ctk.BooleanVar(value=True)\nvar_exitos_para_acelerar = ctk.StringVar(value="3")\nvar_errores_para_frenar = ctk.StringVar(value="1")\nvar_max_ajuste_pausa = ctk.StringVar(value="6")\nvar_prioridad_seleccionada = ctk.StringVar(value="Normal")\nvar_anti_duplicado_visual = ctk.BooleanVar(value=True)\nvar_umbral_similitud_visual = ctk.StringVar(value="10")\nvar_max_regeneraciones_visuales = ctk.StringVar(value="4")\nvar_auditoria_antes_de_publicar = ctk.BooleanVar(value=True)\nvar_autorreparar_auditoria = ctk.BooleanVar(value=True)\nvar_bloquear_si_hay_criticos = ctk.BooleanVar(value=True)\nvar_calidad_minima_publicar = ctk.StringVar(value="70")\nvar_cuarentena_automatica = ctk.BooleanVar(value=True)\nvar_fallos_repetidos_para_cuarentena = ctk.StringVar(value="2")\nvar_continuar_tras_cuarentena = ctk.BooleanVar(value=True)\nvar_autoarchivar_publicados_fin = ctk.BooleanVar(value=False)\nvar_objetivo_publicaciones_sesion = ctk.StringVar(value="10")\nvar_busqueda_cola = ctk.StringVar(value="")\nvar_orden_cola = ctk.StringVar(value="Orden actual")\nvar_nota_anuncio = ctk.StringVar(value="")\nvar_filtro_lote = ctk.StringVar(value="TODOS")\nvar_prioridad_lote = ctk.StringVar(value="Normal")\nvar_nombre_lote_manual = ctk.StringVar(value="")\nvar_umbral_similitud_textual = ctk.StringVar(value="88")\nvar_autoguardado_borrador_segundos = ctk.StringVar(value="30")\nvar_nombre_sesion = ctk.StringVar(value="")\nvar_registro_actividad = ctk.BooleanVar(value=True)\nvar_max_cola = ctk.StringVar(value="500")\nvar_puerto_chromium = ctk.StringVar(value="9222")\nvar_categoria_defecto = ctk.StringVar(\n    value="Motor y accesorios > Recambios de coches y furgonetas > Tuning"\n)\nvar_estado_defecto = ctk.StringVar(value="Nuevo")\nvar_envio_defecto = ctk.BooleanVar(value=True)\n\ncategoria_publicacion = ctk.StringVar(\n    value="Motor y accesorios > Recambios de coches y furgonetas > Tuning"\n)\nestado_producto_publicacion = ctk.StringVar(value="Nuevo")\n\n# Colores principales\n# =========================================================\n# PALETA RETRO GARAGE · ARCADE / CRT\n# =========================================================\nCOLOR_FONDO = "#050608"\nCOLOR_PANEL = "#0a0d11"\nCOLOR_PANEL_2 = "#10151b"\nCOLOR_BORDE = "#64101a"\nCOLOR_ACENTO = "#ff2438"\nCOLOR_TEXTO_SEC = "#8fa6ad"\n\nCOLOR_RETRO_ROJO = "#ff2438"\nCOLOR_RETRO_ROJO_OSCURO = "#5e0b14"\nCOLOR_RETRO_CYAN = "#00e6ff"\nCOLOR_RETRO_VERDE = "#39ff88"\nCOLOR_RETRO_AMBAR = "#ffc94a"\nCOLOR_RETRO_BLANCO = "#eafcff"\nCOLOR_RETRO_NEGRO = "#030405"\n\nventana.configure(fg_color=COLOR_FONDO)\n\n# =========================================================\n# UI V86 · DISEÑO APROBADO\n# =========================================================\nCOLOR_UI_BG = "#05080b"\nCOLOR_UI_PANEL = "#081017"\nCOLOR_UI_PANEL_2 = "#0b141b"\nCOLOR_UI_BORDER = "#26343e"\nCOLOR_UI_RED = "#ff1028"\nCOLOR_UI_RED_DARK = "#74101d"\nCOLOR_UI_GREEN = "#25e58b"\nCOLOR_UI_BLUE = "#178fe5"\nCOLOR_UI_TEXT = "#f3f7f9"\nCOLOR_UI_MUTED = "#98a7b2"\n\n\n# ---------------- HEADER · V86 ----------------\n\nheader_total = ctk.CTkFrame(\n    ventana,\n    height=165,\n    corner_radius=0,\n    fg_color=COLOR_UI_BG\n)\nheader_total.pack(fill="x")\nheader_total.pack_propagate(False)\n\nhero_banner = ctk.CTkFrame(\n    header_total,\n    height=165,\n    corner_radius=0,\n    fg_color="#030507",\n    border_width=1,\n    border_color=COLOR_UI_RED_DARK\n)\nhero_banner.pack(fill="both", expand=True)\nhero_banner.pack_propagate(False)\n\ntry:\n    _ruta_banner = asset_path(\n        "banner_dp5_limpio_v15.png"\n    )\n\n    print(\n        "[UI] Banner:",\n        _ruta_banner\n    )\n\n    _banner_pil = Image.open(\n        _ruta_banner\n    ).convert("RGB")\n\n    try:\n        _banner_pil = ImageEnhance.Contrast(\n            _banner_pil\n        ).enhance(1.08)\n    except Exception:\n        pass\n\n    _banner_ctk = ctk.CTkImage(\n        light_image=_banner_pil,\n        dark_image=_banner_pil,\n        size=(1680, 165)\n    )\n\n    banner_header = ctk.CTkLabel(\n        hero_banner,\n        text="",\n        image=_banner_ctk,\n        fg_color="#030507"\n    )\n    banner_header.place(\n        relx=0.5,\n        rely=0.5,\n        anchor="center"\n    )\n    banner_header.image = _banner_ctk\n\nexcept Exception:\n    ctk.CTkLabel(\n        hero_banner,\n        text="DP5",\n        font=("Segoe UI", 42, "bold"),\n        text_color="#ffffff"\n    ).place(x=28, y=24)\n\n    ctk.CTkLabel(\n        hero_banner,\n        text="DIAGPROG5",\n        font=("Segoe UI", 20, "bold"),\n        text_color="#ffffff"\n    ).place(x=30, y=76)\n\n    ctk.CTkLabel(\n        hero_banner,\n        text="DEL CAMPO A LA CARRETERA, TODO EN MOVIMIENTO",\n        font=("Segoe UI", 9, "bold"),\n        text_color="#cfd7dd"\n    ).place(x=30, y=108)\n\n    ctk.CTkLabel(\n        hero_banner,\n        text="TURISMOS · FURGONETAS · CAMIONES · TRAILERS · MAQUINARIA AGRÍCOLA",\n        font=("Segoe UI", 10, "bold"),\n        text_color="#ffffff"\n    ).place(relx=0.975, y=20, anchor="ne")\n\n    ctk.CTkLabel(\n        hero_banner,\n        text="TU PRÓXIMO ANUNCIO\\nA UN SOLO CLIC",\n        font=("Segoe UI", 18, "bold"),\n        text_color="#ffffff",\n        justify="right"\n    ).place(relx=0.975, rely=0.55, anchor="e")\n\n# ---------------- CUERPO: SIDEBAR + CONTENIDO ----------------\n\nmain_shell = ctk.CTkFrame(\n    ventana,\n    corner_radius=0,\n    fg_color=COLOR_UI_BG\n)\nmain_shell.pack(\n    fill="both",\n    expand=True\n)\n\nsidebar = ctk.CTkFrame(\n    main_shell,\n    width=220,\n    corner_radius=0,\n    fg_color="#071017",\n    border_width=1,\n    border_color="#1a2a34"\n)\nsidebar.pack(\n    side="left",\n    fill="y"\n)\nsidebar.pack_propagate(False)\n\ncontent_shell = ctk.CTkFrame(\n    main_shell,\n    corner_radius=0,\n    fg_color=COLOR_UI_BG\n)\ncontent_shell.pack(\n    side="left",\n    fill="both",\n    expand=True\n)\n\nctk.CTkLabel(\n    sidebar,\n    text="DIAGPROG5",\n    font=("Segoe UI", 20, "bold"),\n    text_color="#ffffff"\n).pack(\n    anchor="w",\n    padx=20,\n    pady=(20, 0)\n)\n\nctk.CTkLabel(\n    sidebar,\n    text="WALLAPOP BOT",\n    font=("Segoe UI", 9, "bold"),\n    text_color=COLOR_UI_RED\n).pack(\n    anchor="w",\n    padx=20,\n    pady=(2, 18)\n)\n\ndef _ir_seccion_creador(destino):\n    try:\n        tabs.set(destino)\n    except Exception as e:\n        try:\n            estado.configure(\n                text=f"❌ No pude abrir {destino}: {e}"\n            )\n        except Exception:\n            pass\n\ndef _side_btn(texto, destino, activo=False):\n    btn = ctk.CTkButton(\n        sidebar,\n        text=texto,\n        command=lambda d=destino: _ir_seccion_creador(d),\n        anchor="w",\n        width=190,\n        height=46,\n        corner_radius=8,\n        fg_color=COLOR_UI_RED if activo else "#071017",\n        hover_color="#15212a",\n        border_width=1 if activo else 0,\n        border_color=COLOR_UI_RED,\n        font=("Segoe UI", 12, "bold")\n    )\n    btn.pack(\n        padx=14,\n        pady=3\n    )\n    return btn\n\n_side_btn("⌂   Inicio", "Inicio", True)\n_side_btn("✎   Crear anuncio", "Crear anuncio")\n_side_btn("☷   Cola", "Cola")\n_side_btn("➤   Publicar", "Publicar")\n_boton_borrar_antiguos_menu = _side_btn("🗑   Borrar antiguos", "Borrar antiguos")\n_BORRADO_SIDE_MAIN_V107 = True\n_side_btn("↻   Actualizaciones", "Actualizaciones")\n_side_btn("⚙   Ajustes", "Ajustes")\n\nadmin_badge = ctk.CTkFrame(\n    sidebar,\n    fg_color="#16090d",\n    corner_radius=8,\n    border_width=1,\n    border_color=COLOR_UI_RED_DARK\n)\nadmin_badge.pack(\n    side="bottom",\n    fill="x",\n    padx=14,\n    pady=16\n)\n\nctk.CTkLabel(\n    admin_badge,\n    text="♛  ADMIN",\n    font=("Segoe UI", 13, "bold"),\n    text_color="#ff6474"\n).pack(\n    anchor="w",\n    padx=14,\n    pady=(12, 2)\n)\n\nctk.CTkLabel(\n    admin_badge,\n    text="Acceso total",\n    font=("Segoe UI", 10),\n    text_color="#ff8895"\n).pack(\n    anchor="w",\n    padx=14,\n    pady=(0, 12)\n)\n\ntabs = ctk.CTkTabview(\n    content_shell,\n    corner_radius=0,\n    fg_color=COLOR_UI_BG,\n    border_width=0,\n    segmented_button_fg_color=COLOR_UI_BG,\n    segmented_button_unselected_color=COLOR_UI_BG,\n    segmented_button_unselected_hover_color=COLOR_UI_BG,\n    segmented_button_selected_color=COLOR_UI_BG,\n    segmented_button_selected_hover_color=COLOR_UI_BG,\n    text_color=COLOR_UI_BG\n)\ntabs.pack(\n    fill="both",\n    expand=True\n)\n\ntab_dashboard = tabs.add("Inicio")\ntab_crear = tabs.add("Crear anuncio")\ntab_publicar = tabs.add("Publicar")\ntab_galeria = tabs.add("Galería")\ntab_historial = tabs.add("Historial")\ntab_cola = tabs.add("Cola")\ntab_estadisticas = tabs.add("Estadísticas")\ntab_actualizaciones = tabs.add("Actualizaciones")\ntab_configuracion = tabs.add("Ajustes")\ntab_backups = tabs.add("Copias de seguridad")\ntab_diagnostico = tabs.add("Diagnóstico")\ntab_registro = tabs.add("Registro")\n\nfor tab in (\n    tab_dashboard,\n    tab_crear,\n    tab_publicar,\n    tab_galeria,\n    tab_historial,\n    tab_cola,\n    tab_estadisticas,\n    tab_actualizaciones,\n    tab_configuracion,\n    tab_backups,\n    tab_diagnostico,\n    tab_registro\n):\n    tab.configure(fg_color=COLOR_UI_BG)\n\ntry:\n    tabs._segmented_button.grid_forget()\nexcept Exception:\n    pass\n\n# =========================================================\n# TAB 0 · INICIO · V86\n# =========================================================\n\ndashboard_scroll = ctk.CTkScrollableFrame(\n    tab_dashboard,\n    fg_color=COLOR_UI_BG\n)\ndashboard_scroll.pack(\n    fill="both",\n    expand=True,\n    padx=10,\n    pady=10\n)\n\n# ----- KPIs -----\nkpi_row = ctk.CTkFrame(\n    dashboard_scroll,\n    fg_color="transparent"\n)\nkpi_row.pack(\n    fill="x",\n    pady=(0, 10)\n)\n\nfor c in range(5):\n    kpi_row.grid_columnconfigure(c, weight=1)\n\ndef _crear_kpi_v86(parent, col, icono, titulo, valor, color):\n    card = ctk.CTkFrame(\n        parent,\n        fg_color=COLOR_UI_PANEL,\n        corner_radius=10,\n        border_width=1,\n        border_color=COLOR_UI_BORDER\n    )\n    card.grid(\n        row=0,\n        column=col,\n        sticky="nsew",\n        padx=4\n    )\n\n    ctk.CTkLabel(\n        card,\n        text=icono,\n        width=50,\n        height=50,\n        corner_radius=8,\n        fg_color=color,\n        font=("Segoe UI", 22, "bold")\n    ).pack(\n        side="left",\n        padx=14,\n        pady=14\n    )\n\n    box = ctk.CTkFrame(\n        card,\n        fg_color="transparent"\n    )\n    box.pack(\n        side="left",\n        fill="both",\n        expand=True,\n        pady=11\n    )\n\n    ctk.CTkLabel(\n        box,\n        text=titulo,\n        font=("Segoe UI", 10),\n        text_color=COLOR_UI_MUTED\n    ).pack(anchor="w")\n\n    value = ctk.CTkLabel(\n        box,\n        text=valor,\n        font=("Segoe UI", 24, "bold"),\n        text_color=COLOR_UI_TEXT\n    )\n    value.pack(anchor="w")\n\n    return value\n\ndashboard_kpi_cola = _crear_kpi_v86(\n    kpi_row, 0, "▤", "Anuncios en cola", "0", "#6c101c"\n)\n\ndashboard_kpi_publicados = _crear_kpi_v86(\n    kpi_row, 1, "✓", "Publicados hoy", "0", "#0b6841"\n)\n\ndashboard_kpi_proceso = _crear_kpi_v86(\n    kpi_row, 2, "◷", "En proceso", "0", "#0e568c"\n)\n\ndashboard_kpi_errores = _crear_kpi_v86(\n    kpi_row, 3, "✕", "Errores", "0", "#6c101c"\n)\n\nsystem_card = ctk.CTkFrame(\n    kpi_row,\n    fg_color=COLOR_UI_PANEL,\n    corner_radius=10,\n    border_width=1,\n    border_color=COLOR_UI_BORDER\n)\nsystem_card.grid(\n    row=0,\n    column=4,\n    sticky="nsew",\n    padx=4\n)\n\nctk.CTkLabel(\n    system_card,\n    text="Estado del sistema",\n    font=("Segoe UI", 11, "bold"),\n    text_color=COLOR_UI_TEXT\n).pack(\n    anchor="w",\n    padx=12,\n    pady=(10, 2)\n)\n\ndashboard_estado_general = ctk.CTkLabel(\n    system_card,\n    text="● Todo correcto",\n    font=("Segoe UI", 10, "bold"),\n    text_color=COLOR_UI_GREEN\n)\ndashboard_estado_general.pack(\n    anchor="w",\n    padx=12,\n    pady=(0, 6)\n)\n\ndashboard_estado_servicios = ctk.CTkLabel(\n    system_card,\n    text="Chrome: conectado\\nWallapop: sesión activa",\n    font=("Segoe UI", 9),\n    text_color=COLOR_UI_MUTED,\n    justify="left"\n)\ndashboard_estado_servicios.pack(\n    anchor="w",\n    padx=12,\n    pady=(0, 10)\n)\n\n\nctk.CTkButton(\n    system_card,\n    text="🛠 Diagnóstico PRO",\n    command=diagnosticar_y_reparar_bot,\n    height=32,\n    fg_color="#315d83",\n    hover_color="#3c719d",\n    font=("Segoe UI", 10, "bold")\n).pack(\n    fill="x",\n    padx=12,\n    pady=(0, 10)\n)\n\n# ----- Zona principal -----\nmain_row = ctk.CTkFrame(\n    dashboard_scroll,\n    fg_color="transparent"\n)\nmain_row.pack(\n    fill="both",\n    expand=True\n)\n\nmain_row.grid_columnconfigure(0, weight=3)\nmain_row.grid_columnconfigure(1, weight=1)\nmain_row.grid_rowconfigure(0, weight=1)\n\n# Cola\nqueue_card = ctk.CTkFrame(\n    main_row,\n    fg_color=COLOR_UI_PANEL,\n    corner_radius=10,\n    border_width=1,\n    border_color=COLOR_UI_RED_DARK\n)\nqueue_card.grid(\n    row=0,\n    column=0,\n    sticky="nsew",\n    padx=(0, 5)\n)\n\nqueue_head = ctk.CTkFrame(\n    queue_card,\n    fg_color="transparent"\n)\nqueue_head.pack(\n    fill="x",\n    padx=16,\n    pady=(14, 8)\n)\n\nqueue_titles = ctk.CTkFrame(\n    queue_head,\n    fg_color="transparent"\n)\nqueue_titles.pack(\n    side="left",\n    fill="x",\n    expand=True\n)\n\nctk.CTkLabel(\n    queue_titles,\n    text="Cola de publicación",\n    font=("Segoe UI", 19, "bold"),\n    text_color=COLOR_UI_TEXT\n).pack(anchor="w")\n\nctk.CTkLabel(\n    queue_titles,\n    text="Anuncios listos para publicar en Wallapop",\n    font=("Segoe UI", 10),\n    text_color=COLOR_UI_MUTED\n).pack(anchor="w")\n\nctk.CTkButton(\n    queue_head,\n    text="+  Añadir anuncio",\n    command=lambda: tabs.set("Crear anuncio"),\n    width=130,\n    height=34,\n    fg_color=COLOR_UI_PANEL_2,\n    hover_color="#14212b"\n).pack(\n    side="right",\n    padx=(6, 0)\n)\n\nctk.CTkButton(\n    queue_head,\n    text="Vaciar cola",\n    command=vaciar_cola,\n    width=105,\n    height=34,\n    fg_color=COLOR_UI_PANEL_2,\n    hover_color="#14212b"\n).pack(\n    side="right"\n)\n\ntabla_head = ctk.CTkFrame(\n    queue_card,\n    fg_color="#0d171f",\n    corner_radius=7\n)\ntabla_head.pack(\n    fill="x",\n    padx=16,\n    pady=(0, 4)\n)\n\nfor txt, width, expand in [\n    ("#", 40, False),\n    ("Título", 0, True),\n    ("Precio", 90, False),\n    ("Estado", 105, False),\n    ("Creado", 110, False),\n]:\n    lbl = ctk.CTkLabel(\n        tabla_head,\n        text=txt,\n        font=("Segoe UI", 9, "bold"),\n        text_color=COLOR_UI_MUTED,\n        width=width\n    )\n    lbl.pack(\n        side="left",\n        fill="x" if expand else None,\n        expand=expand,\n        padx=8,\n        pady=9\n    )\n\ndashboard_queue_text = ctk.CTkTextbox(\n    queue_card,\n    height=280,\n    font=("Segoe UI", 11),\n    fg_color=COLOR_UI_PANEL,\n    border_width=0\n)\ndashboard_queue_text.pack(\n    fill="both",\n    expand=True,\n    padx=16,\n    pady=(0, 10)\n)\ndashboard_queue_text.insert(\n    "1.0",\n    "No hay anuncios en la cola.\\n\\nCrea anuncios y añádelos para empezar."\n)\ndashboard_queue_text.configure(state="disabled")\n\n# Pasos\nflow = ctk.CTkFrame(\n    queue_card,\n    fg_color=COLOR_UI_PANEL_2,\n    corner_radius=8\n)\nflow.pack(\n    fill="x",\n    padx=16,\n    pady=(0, 14)\n)\n\nfor num, titulo, detalle in [\n    ("1", "Crear", "Genera tus anuncios"),\n    ("2", "Revisar", "Comprueba la cola"),\n    ("3", "Publicar", "Inicia la cola"),\n    ("4", "Listo", "Tus anuncios en Wallapop"),\n]:\n    item = ctk.CTkFrame(\n        flow,\n        fg_color="transparent"\n    )\n    item.pack(\n        side="left",\n        fill="x",\n        expand=True,\n        padx=5,\n        pady=10\n    )\n\n    ctk.CTkLabel(\n        item,\n        text=num,\n        width=34,\n        height=34,\n        corner_radius=17,\n        fg_color=COLOR_UI_RED,\n        font=("Segoe UI", 14, "bold")\n    ).pack(\n        side="left",\n        padx=(0, 8)\n    )\n\n    item_text = ctk.CTkFrame(\n        item,\n        fg_color="transparent"\n    )\n    item_text.pack(side="left")\n\n    ctk.CTkLabel(\n        item_text,\n        text=titulo,\n        font=("Segoe UI", 10, "bold")\n    ).pack(anchor="w")\n\n    ctk.CTkLabel(\n        item_text,\n        text=detalle,\n        font=("Segoe UI", 8),\n        text_color=COLOR_UI_MUTED\n    ).pack(anchor="w")\n\n# Control de publicación\ncontrol_card = ctk.CTkFrame(\n    main_row,\n    fg_color=COLOR_UI_PANEL,\n    corner_radius=10,\n    border_width=1,\n    border_color=COLOR_UI_RED_DARK\n)\ncontrol_card.grid(\n    row=0,\n    column=1,\n    sticky="nsew",\n    padx=(5, 0)\n)\n\nctk.CTkLabel(\n    control_card,\n    text="Control de publicación",\n    font=("Segoe UI", 18, "bold")\n).pack(\n    anchor="w",\n    padx=16,\n    pady=(14, 2)\n)\n\nctk.CTkLabel(\n    control_card,\n    text="Gestiona el proceso de publicación",\n    font=("Segoe UI", 10),\n    text_color=COLOR_UI_MUTED\n).pack(\n    anchor="w",\n    padx=16,\n    pady=(0, 14)\n)\n\nctk.CTkButton(\n    control_card,\n    text="▶  Iniciar cola",\n    command=iniciar_cola_publicacion,\n    height=52,\n    font=("Segoe UI", 14, "bold"),\n    fg_color=COLOR_UI_RED,\n    hover_color="#d60c20"\n).pack(\n    fill="x",\n    padx=14,\n    pady=(0, 10)\n)\n\nfila_control_v86 = ctk.CTkFrame(\n    control_card,\n    fg_color="transparent"\n)\nfila_control_v86.pack(\n    fill="x",\n    padx=10,\n    pady=(0, 10)\n)\n\nctk.CTkButton(\n    fila_control_v86,\n    text="Ⅱ  Pausar",\n    command=pausar_cola,\n    height=42,\n    fg_color=COLOR_UI_PANEL_2,\n    hover_color="#14212b"\n).pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=4\n)\n\nctk.CTkButton(\n    fila_control_v86,\n    text="■  Detener",\n    command=cancelar_cola,\n    height=42,\n    fg_color=COLOR_UI_PANEL_2,\n    hover_color="#14212b"\n).pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=4\n)\n\nprogreso_box = ctk.CTkFrame(\n    control_card,\n    fg_color=COLOR_UI_PANEL_2,\n    corner_radius=8\n)\nprogreso_box.pack(\n    fill="x",\n    padx=14,\n    pady=(0, 10)\n)\n\ndashboard_progreso_texto = ctk.CTkLabel(\n    progreso_box,\n    text="Progreso actual                         0%",\n    font=("Segoe UI", 10),\n    text_color=COLOR_UI_TEXT\n)\ndashboard_progreso_texto.pack(\n    fill="x",\n    padx=12,\n    pady=(10, 4)\n)\n\ndashboard_progress = ctk.CTkProgressBar(\n    progreso_box,\n    height=9,\n    progress_color=COLOR_UI_RED\n)\ndashboard_progress.pack(\n    fill="x",\n    padx=12,\n    pady=(0, 8)\n)\ndashboard_progress.set(0)\n\ndashboard_eta = ctk.CTkLabel(\n    progreso_box,\n    text="0 de 0 anuncios             Tiempo estimado: --:--",\n    font=("Segoe UI", 9),\n    text_color=COLOR_UI_MUTED\n)\ndashboard_eta.pack(\n    fill="x",\n    padx=12,\n    pady=(0, 10)\n)\n\nselected_card = ctk.CTkFrame(\n    control_card,\n    fg_color=COLOR_UI_PANEL_2,\n    corner_radius=8\n)\nselected_card.pack(\n    fill="both",\n    expand=True,\n    padx=14,\n    pady=(0, 14)\n)\n\nctk.CTkLabel(\n    selected_card,\n    text="›  Anuncio seleccionado",\n    font=("Segoe UI", 11, "bold")\n).pack(\n    anchor="w",\n    padx=12,\n    pady=(10, 6)\n)\n\ndashboard_selected = ctk.CTkLabel(\n    selected_card,\n    text="Selecciona un anuncio de la lista para ver detalles.",\n    font=("Segoe UI", 9),\n    text_color=COLOR_UI_MUTED,\n    wraplength=300,\n    justify="left"\n)\ndashboard_selected.pack(\n    anchor="w",\n    padx=12,\n    pady=(4, 10)\n)\n\n# Footer visual del dashboard\nfooter_v86 = ctk.CTkFrame(\n    dashboard_scroll,\n    fg_color="#071017",\n    corner_radius=0\n)\nfooter_v86.pack(\n    fill="x",\n    pady=(10, 0)\n)\n\nctk.CTkLabel(\n    footer_v86,\n    text="DIAGPROG5",\n    font=("Segoe UI", 13, "bold")\n).pack(\n    side="left",\n    padx=(14, 12),\n    pady=9\n)\n\nctk.CTkLabel(\n    footer_v86,\n    text="DEL CAMPO A LA CARRETERA, TODO EN MOVIMIENTO",\n    font=("Segoe UI", 9),\n    text_color=COLOR_UI_MUTED\n).pack(\n    side="left",\n    pady=9\n)\n\ndashboard_footer_estado = ctk.CTkLabel(\n    footer_v86,\n    text="●  Listo para trabajar",\n    font=("Segoe UI", 10, "bold"),\n    text_color=COLOR_UI_GREEN\n)\ndashboard_footer_estado.pack(\n    side="right",\n    padx=14,\n    pady=9\n)\n\n# =========================================================\n# TAB 1 · CREAR ANUNCIO\n# =========================================================\n\ncrear_scroll = ctk.CTkScrollableFrame(\n    tab_crear,\n    fg_color="transparent"\n)\ncrear_scroll.pack(fill="both", expand=True, padx=8, pady=8)\ncrear_scroll.grid_columnconfigure(0, weight=1)\ncrear_scroll.grid_columnconfigure(1, weight=1)\n\n# Columna izquierda\npanel_datos = ctk.CTkFrame(\n    crear_scroll,\n    fg_color=COLOR_PANEL,\n    corner_radius=14,\n    border_width=1,\n    border_color=COLOR_BORDE\n)\npanel_datos.grid(\n    row=0,\n    column=0,\n    sticky="nsew",\n    padx=(0, 8),\n    pady=4\n)\n\nctk.CTkLabel(\n    panel_datos,\n    text="Datos del anuncio",\n    font=("Segoe UI", 21, "bold")\n).pack(anchor="w", padx=22, pady=(20, 6))\n\nctk.CTkLabel(\n    panel_datos,\n    text="Define el servicio y los datos que utilizará el bot.",\n    font=("Segoe UI", 13),\n    text_color=COLOR_TEXTO_SEC\n).pack(anchor="w", padx=22, pady=(0, 18))\n\nctk.CTkLabel(\n    panel_datos,\n    text="Servicio / idea",\n    font=("Segoe UI", 14, "bold")\n).pack(anchor="w", padx=22)\n\nentrada_servicio = ctk.CTkEntry(\n    panel_datos,\n    height=44,\n    placeholder_text="Ejemplo: Limpieza y regeneración DPF",\n    corner_radius=9\n)\nentrada_servicio.pack(fill="x", padx=22, pady=(6, 15))\n\nfila_precio_contacto = ctk.CTkFrame(\n    panel_datos,\n    fg_color="transparent"\n)\nfila_precio_contacto.pack(fill="x", padx=22)\nfila_precio_contacto.grid_columnconfigure(0, weight=1)\nfila_precio_contacto.grid_columnconfigure(1, weight=2)\n\nbloque_precio = ctk.CTkFrame(\n    fila_precio_contacto,\n    fg_color="transparent"\n)\nbloque_precio.grid(row=0, column=0, sticky="ew", padx=(0, 7))\n\nctk.CTkLabel(\n    bloque_precio,\n    text="Precio (€)",\n    font=("Segoe UI", 14, "bold")\n).pack(anchor="w")\n\nentrada_precio = ctk.CTkEntry(\n    bloque_precio,\n    height=42,\n    placeholder_text="120"\n)\nentrada_precio.pack(fill="x", pady=(6, 0))\n\nbloque_contacto = ctk.CTkFrame(\n    fila_precio_contacto,\n    fg_color="transparent"\n)\nbloque_contacto.grid(row=0, column=1, sticky="ew", padx=(7, 0))\n\nctk.CTkLabel(\n    bloque_contacto,\n    text="Contacto",\n    font=("Segoe UI", 14, "bold")\n).pack(anchor="w")\n\nentrada_contacto = ctk.CTkEntry(\n    bloque_contacto,\n    height=42,\n    placeholder_text="Ejemplo: 600 123 456"\n)\nentrada_contacto.pack(fill="x", pady=(6, 0))\n\nctk.CTkLabel(\n    panel_datos,\n    text="Mostrar contacto",\n    font=("Segoe UI", 14, "bold")\n).pack(anchor="w", padx=22, pady=(18, 4))\n\ncheck_contacto_imagen = ctk.CTkCheckBox(\n    panel_datos,\n    text="En las imágenes",\n    fg_color=COLOR_ACENTO,\n    hover_color="#d91624"\n)\ncheck_contacto_imagen.pack(anchor="w", padx=22, pady=4)\n\ncheck_contacto_descripcion = ctk.CTkCheckBox(\n    panel_datos,\n    text="En la descripción",\n    fg_color=COLOR_ACENTO,\n    hover_color="#d91624"\n)\ncheck_contacto_descripcion.pack(anchor="w", padx=22, pady=(4, 18))\n\nfila_acciones = ctk.CTkFrame(\n    panel_datos,\n    fg_color="transparent"\n)\nfila_acciones.pack(fill="x", padx=22, pady=(0, 22))\n\nboton = ctk.CTkButton(\n    fila_acciones,\n    text="Generar anuncio",\n    command=al_generar_anuncio,\n    height=48,\n    font=("Segoe UI", 15, "bold"),\n    corner_radius=10,\n    fg_color=COLOR_ACENTO,\n    hover_color="#d91624"\n)\nboton.pack(side="left", fill="x", expand=True, padx=(0, 5))\n\nctk.CTkButton(\n    fila_acciones,\n    text="Limpiar",\n    command=limpiar_formulario,\n    width=95,\n    height=48,\n    corner_radius=10,\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32"\n).pack(side="left", padx=(5, 0))\n\nboton_generar_cola = ctk.CTkButton(\n    panel_datos,\n    text="GENERAR ANUNCIO + AÑADIR A COLA",\n    command=generar_y_anadir_a_cola,\n    height=46,\n    font=("Segoe UI", 14, "bold"),\n    corner_radius=10,\n    fg_color="#7d1b22",\n    hover_color="#9a222b",\n    border_width=1,\n    border_color=COLOR_ACENTO\n)\nboton_generar_cola.pack(\n    fill="x",\n    padx=22,\n    pady=(0, 10)\n)\n\npanel_lote = ctk.CTkFrame(\n    panel_datos,\n    fg_color=COLOR_PANEL_2,\n    corner_radius=10\n)\npanel_lote.pack(\n    fill="x",\n    padx=22,\n    pady=(0, 14)\n)\n\npanel_perfil_lote = ctk.CTkFrame(\n    panel_datos,\n    fg_color=COLOR_PANEL_2,\n    corner_radius=10\n)\npanel_perfil_lote.pack(\n    fill="x",\n    padx=22,\n    pady=(0, 10)\n)\n\nctk.CTkLabel(\n    panel_perfil_lote,\n    text="Perfiles de lote",\n    font=("Segoe UI", 13, "bold")\n).pack(anchor="w", padx=14, pady=(10, 6))\n\nfila_perfil_lote = ctk.CTkFrame(\n    panel_perfil_lote,\n    fg_color="transparent"\n)\nfila_perfil_lote.pack(fill="x", padx=14, pady=(0, 8))\n\nmenu_perfil_lote = ctk.CTkOptionMenu(\n    fila_perfil_lote,\n    variable=var_perfil_lote_actual,\n    values=["Diagnosis"],\n    command=aplicar_perfil_lote,\n    height=34,\n    fg_color=COLOR_PANEL,\n    button_color=COLOR_ACENTO,\n    button_hover_color="#d91624"\n)\nmenu_perfil_lote.pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=(0, 6)\n)\n\nctk.CTkButton(\n    fila_perfil_lote,\n    text="Aplicar",\n    command=aplicar_perfil_lote,\n    width=80,\n    height=34,\n    fg_color=COLOR_PANEL,\n    hover_color="#252b32"\n).pack(side="left")\n\nentrada_perfil_lote = ctk.CTkEntry(\n    panel_perfil_lote,\n    textvariable=var_nombre_perfil_lote,\n    placeholder_text="Nombre para guardar perfil"\n)\nentrada_perfil_lote.pack(\n    fill="x",\n    padx=14,\n    pady=(0, 8)\n)\n\nfila_perfil_botones = ctk.CTkFrame(\n    panel_perfil_lote,\n    fg_color="transparent"\n)\nfila_perfil_botones.pack(fill="x", padx=14, pady=(0, 10))\n\nctk.CTkButton(\n    fila_perfil_botones,\n    text="Guardar perfil",\n    command=guardar_perfil_lote_actual,\n    height=32,\n    fg_color="#235d3a",\n    hover_color="#2d7549"\n).pack(side="left", fill="x", expand=True, padx=(0, 4))\n\nctk.CTkButton(\n    fila_perfil_botones,\n    text="Eliminar perfil",\n    command=eliminar_perfil_lote_actual,\n    height=32,\n    fg_color="#7d1b22",\n    hover_color="#9a222b"\n).pack(side="left", fill="x", expand=True, padx=(4, 0))\n\nctk.CTkLabel(\n    panel_lote,\n    text="Generación por lote",\n    font=("Segoe UI", 13, "bold")\n).pack(\n    anchor="w",\n    padx=14,\n    pady=(10, 3)\n)\n\nctk.CTkLabel(\n    panel_lote,\n    text=(\n        "Anti-duplicados automático: si uno coincide con otro publicado "\n        "o con la cola, se crea un sustituto nuevo."\n    ),\n    font=("Segoe UI", 10),\n    text_color=COLOR_TEXTO_SEC,\n    wraplength=700,\n    justify="left"\n).pack(\n    anchor="w",\n    padx=14,\n    pady=(0, 7)\n)\n\nfila_lote = ctk.CTkFrame(\n    panel_lote,\n    fg_color="transparent"\n)\nfila_lote.pack(\n    fill="x",\n    padx=14,\n    pady=(0, 12)\n)\n\nctk.CTkLabel(\n    fila_lote,\n    text="Cantidad:",\n    font=("Segoe UI", 12)\n).pack(\n    side="left",\n    padx=(0, 8)\n)\n\nctk.CTkEntry(\n    fila_lote,\n    textvariable=var_cantidad_lote,\n    width=70,\n    height=36\n).pack(\n    side="left",\n    padx=(0, 8)\n)\n\nboton_generar_lote = ctk.CTkButton(\n    fila_lote,\n    text="GENERAR LOTE ÚNICO + COLA",\n    command=generar_lote_y_anadir_cola,\n    height=36,\n    font=("Segoe UI", 12, "bold"),\n    fg_color="#7d1b22",\n    hover_color="#9a222b"\n)\nboton_generar_lote.pack(\n    side="left",\n    fill="x",\n    expand=True\n)\n\nfila_borradores = ctk.CTkFrame(\n    panel_datos,\n    fg_color="transparent"\n)\nfila_borradores.pack(fill="x", padx=22, pady=(0, 18))\n\nctk.CTkButton(\n    fila_borradores,\n    text="Guardar borrador",\n    command=guardar_borrador,\n    height=34,\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32"\n).pack(side="left", fill="x", expand=True, padx=(0, 4))\n\nctk.CTkButton(\n    fila_borradores,\n    text="Recuperar borrador",\n    command=cargar_borrador,\n    height=34,\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32"\n).pack(side="left", fill="x", expand=True, padx=4)\n\nctk.CTkButton(\n    fila_borradores,\n    text="Eliminar borrador",\n    command=eliminar_borrador,\n    height=34,\n    fg_color="#7d1b22",\n    hover_color="#9a222b"\n).pack(side="left", fill="x", expand=True, padx=(4, 0))\n\n\n# Columna derecha: editor\npanel_editor = ctk.CTkFrame(\n    crear_scroll,\n    fg_color=COLOR_PANEL,\n    corner_radius=14,\n    border_width=1,\n    border_color=COLOR_BORDE\n)\npanel_editor.grid(\n    row=0,\n    column=1,\n    sticky="nsew",\n    padx=(8, 0),\n    pady=4\n)\n\nctk.CTkLabel(\n    panel_editor,\n    text="Editor del anuncio",\n    font=("Segoe UI", 21, "bold")\n).pack(anchor="w", padx=22, pady=(20, 6))\n\nctk.CTkLabel(\n    panel_editor,\n    text="Puedes corregir el texto antes de publicarlo.",\n    font=("Segoe UI", 13),\n    text_color=COLOR_TEXTO_SEC\n).pack(anchor="w", padx=22, pady=(0, 15))\n\nfila_titulo = ctk.CTkFrame(panel_editor, fg_color="transparent")\nfila_titulo.pack(fill="x", padx=22)\n\nctk.CTkLabel(\n    fila_titulo,\n    text="Título",\n    font=("Segoe UI", 14, "bold")\n).pack(side="left")\n\ncontador_titulo = ctk.CTkLabel(\n    fila_titulo,\n    text="0 / 50",\n    font=("Segoe UI", 12),\n    text_color=COLOR_TEXTO_SEC\n)\ncontador_titulo.pack(side="right")\n\ncaja_titulo = ctk.CTkTextbox(\n    panel_editor,\n    height=70,\n    corner_radius=9,\n    border_width=1,\n    border_color=COLOR_BORDE\n)\ncaja_titulo.pack(fill="x", padx=22, pady=(6, 5))\n\nctk.CTkButton(\n    panel_editor,\n    text="Copiar título",\n    command=copiar_titulo,\n    width=115,\n    height=30,\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32"\n).pack(anchor="e", padx=22, pady=(0, 12))\n\nfila_desc = ctk.CTkFrame(panel_editor, fg_color="transparent")\nfila_desc.pack(fill="x", padx=22)\n\nctk.CTkLabel(\n    fila_desc,\n    text="Descripción",\n    font=("Segoe UI", 14, "bold")\n).pack(side="left")\n\ncontador_descripcion = ctk.CTkLabel(\n    fila_desc,\n    text="0 / 500 caracteres",\n    font=("Segoe UI", 12),\n    text_color=COLOR_TEXTO_SEC\n)\ncontador_descripcion.pack(side="right")\n\ncaja_descripcion = ctk.CTkTextbox(\n    panel_editor,\n    height=220,\n    corner_radius=9,\n    border_width=1,\n    border_color=COLOR_BORDE\n)\ncaja_descripcion.pack(fill="both", expand=True, padx=22, pady=(6, 5))\n\nctk.CTkButton(\n    panel_editor,\n    text="Copiar descripción",\n    command=copiar_descripcion,\n    width=140,\n    height=30,\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32"\n).pack(anchor="e", padx=22, pady=(0, 20))\n\n# Eventos\ncaja_descripcion.bind("<KeyRelease>", actualizar_contadores_profesionales)\ncaja_titulo.bind("<KeyRelease>", actualizar_contadores_profesionales)\nentrada_servicio.bind("<KeyRelease>", lambda e: refrescar_resumen_publicacion())\nentrada_precio.bind("<KeyRelease>", lambda e: refrescar_resumen_publicacion())\nentrada_contacto.bind("<KeyRelease>", lambda e: refrescar_resumen_publicacion())\n\n# Autoguardado del borrador\nentrada_servicio.bind("<KeyRelease>", programar_autoguardado, add="+")\nentrada_precio.bind("<KeyRelease>", programar_autoguardado, add="+")\nentrada_contacto.bind("<KeyRelease>", programar_autoguardado, add="+")\ncaja_titulo.bind("<KeyRelease>", programar_autoguardado, add="+")\ncaja_descripcion.bind("<KeyRelease>", programar_autoguardado, add="+")\n\n# =========================================================\n# TAB 2 · PUBLICACIÓN\n# =========================================================\n\npublicar_scroll = ctk.CTkScrollableFrame(\n    tab_publicar,\n    fg_color="transparent"\n)\npublicar_scroll.pack(fill="both", expand=True, padx=8, pady=8)\npublicar_scroll.grid_columnconfigure(0, weight=1)\npublicar_scroll.grid_columnconfigure(1, weight=1)\n\npanel_config = ctk.CTkFrame(\n    publicar_scroll,\n    fg_color=COLOR_PANEL,\n    corner_radius=14,\n    border_width=1,\n    border_color=COLOR_BORDE\n)\npanel_config.grid(row=0, column=0, sticky="nsew", padx=(0, 8), pady=4)\n\nctk.CTkLabel(\n    panel_config,\n    text="Configuración de publicación",\n    font=("Segoe UI", 21, "bold")\n).pack(anchor="w", padx=22, pady=(20, 6))\n\n# ===== PLANTILLAS =====\npanel_plantillas = ctk.CTkFrame(\n    panel_config,\n    fg_color=COLOR_PANEL_2,\n    corner_radius=10\n)\npanel_plantillas.pack(fill="x", padx=22, pady=(6, 12))\n\nctk.CTkLabel(\n    panel_plantillas,\n    text="Plantillas de publicación",\n    font=("Segoe UI", 14, "bold")\n).pack(anchor="w", padx=14, pady=(12, 6))\n\nfila_plantilla_selector = ctk.CTkFrame(\n    panel_plantillas,\n    fg_color="transparent"\n)\nfila_plantilla_selector.pack(fill="x", padx=14, pady=(0, 8))\n\nmenu_plantillas = ctk.CTkOptionMenu(\n    fila_plantilla_selector,\n    values=["Tuning"],\n    command=lambda nombre: aplicar_plantilla(nombre),\n    height=36,\n    fg_color=COLOR_PANEL,\n    button_color=COLOR_ACENTO,\n    button_hover_color="#d91624"\n)\nmenu_plantillas.pack(side="left", fill="x", expand=True, padx=(0, 6))\n\nctk.CTkButton(\n    fila_plantilla_selector,\n    text="Aplicar",\n    command=aplicar_plantilla,\n    width=85,\n    height=36,\n    fg_color=COLOR_PANEL,\n    hover_color="#252b32"\n).pack(side="left")\n\nentrada_nombre_plantilla = ctk.CTkEntry(\n    panel_plantillas,\n    placeholder_text="Nombre para guardar o duplicar",\n    height=34\n)\nentrada_nombre_plantilla.pack(\n    fill="x",\n    padx=14,\n    pady=(0, 8)\n)\n\nfila_plantillas_acciones = ctk.CTkFrame(\n    panel_plantillas,\n    fg_color="transparent"\n)\nfila_plantillas_acciones.pack(fill="x", padx=14, pady=(0, 12))\n\nctk.CTkButton(\n    fila_plantillas_acciones,\n    text="Guardar actual",\n    command=guardar_plantilla_actual,\n    height=34,\n    fg_color=COLOR_ACENTO,\n    hover_color="#d91624"\n).pack(side="left", fill="x", expand=True, padx=(0, 4))\n\nctk.CTkButton(\n    fila_plantillas_acciones,\n    text="Duplicar",\n    command=duplicar_plantilla_actual,\n    height=34,\n    fg_color=COLOR_PANEL,\n    hover_color="#252b32"\n).pack(side="left", fill="x", expand=True, padx=4)\n\nctk.CTkButton(\n    fila_plantillas_acciones,\n    text="Eliminar",\n    command=eliminar_plantilla_actual,\n    height=34,\n    fg_color="#7d1b22",\n    hover_color="#9a222b"\n).pack(side="left", fill="x", expand=True, padx=(4, 0))\n\nctk.CTkLabel(\n    panel_config,\n    text="Ajustes que el bot aplicará antes de publicar.",\n    text_color=COLOR_TEXTO_SEC,\n    font=("Segoe UI", 13)\n).pack(anchor="w", padx=22, pady=(0, 18))\n\netiqueta_fase = ctk.CTkLabel(\n    panel_config,\n    text="Fase actual: En espera",\n    font=("Segoe UI", 12, "bold"),\n    text_color=COLOR_ACENTO,\n    anchor="w"\n)\netiqueta_fase.pack(fill="x", padx=22, pady=(4, 8))\n\nfila = ctk.CTkFrame(panel_config, fg_color=COLOR_PANEL_2, corner_radius=10)\nfila.pack(fill="x", padx=22, pady=6)\n\nctk.CTkLabel(\n    fila,\n    text="Categoría",\n    font=("Segoe UI", 14, "bold")\n).pack(anchor="w", padx=15, pady=(12, 2))\n\nctk.CTkLabel(\n    fila,\n    text="Escribe la ruta igual que aparece en Wallapop, separada por >",\n    font=("Segoe UI", 11),\n    text_color=COLOR_TEXTO_SEC\n).pack(anchor="w", padx=15, pady=(0, 6))\n\nentrada_categoria_publicacion = ctk.CTkEntry(\n    fila,\n    textvariable=categoria_publicacion,\n    height=38,\n    corner_radius=8\n)\nentrada_categoria_publicacion.pack(\n    fill="x",\n    padx=15,\n    pady=(0, 12)\n)\nentrada_categoria_publicacion.bind(\n    "<KeyRelease>",\n    lambda event: refrescar_resumen_publicacion()\n)\n\nfila_estado = ctk.CTkFrame(\n    panel_config,\n    fg_color=COLOR_PANEL_2,\n    corner_radius=10\n)\nfila_estado.pack(fill="x", padx=22, pady=6)\n\nctk.CTkLabel(\n    fila_estado,\n    text="Estado del producto",\n    font=("Segoe UI", 14, "bold")\n).pack(side="left", padx=15, pady=14)\n\nmenu_estado_producto = ctk.CTkOptionMenu(\n    fila_estado,\n    variable=estado_producto_publicacion,\n    values=[\n        "Nuevo",\n        "Como nuevo",\n        "En buen estado",\n        "En condiciones aceptables"\n    ],\n    command=lambda _valor: (\n        refrescar_resumen_publicacion(),\n        programar_autoguardado()\n    ),\n    width=210,\n    height=34,\n    fg_color=COLOR_ACENTO,\n    button_color=COLOR_ACENTO,\n    button_hover_color="#d91624"\n)\nmenu_estado_producto.pack(side="right", padx=15, pady=10)\n\nswitch_desactivar_envio_ui = ctk.CTkSwitch(\n    panel_config,\n    text="Desactivar envío automáticamente",\n    variable=switch_desactivar_envio,\n    command=lambda: (\n        refrescar_resumen_publicacion(),\n        programar_autoguardado()\n    ),\n    progress_color=COLOR_RETRO_ROJO\n)\nswitch_desactivar_envio_ui.pack(anchor="w", padx=22, pady=(18, 8))\n\nctk.CTkLabel(\n    panel_config,\n    text="Localización: se utiliza la que tengas guardada en tu perfil de Wallapop.",\n    text_color=COLOR_TEXTO_SEC,\n    font=("Segoe UI", 12),\n    wraplength=500,\n    justify="left"\n).pack(anchor="w", padx=22, pady=(12, 20))\n\nctk.CTkButton(\n    panel_config,\n    text="COMPROBAR ANUNCIO",\n    command=comprobar_anuncio_manual,\n    height=42,\n    font=("Segoe UI", 14, "bold"),\n    corner_radius=10,\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32",\n    border_width=1,\n    border_color=COLOR_ACENTO\n).pack(fill="x", padx=22, pady=(6, 14))\n\nctk.CTkLabel(\n    panel_config,\n    text="Revisa título, descripción, precio, categoría, estado, "\n         "1 imagen y conexión antes de empezar.",\n    font=("Segoe UI", 11),\n    text_color=COLOR_TEXTO_SEC,\n    wraplength=500,\n    justify="left"\n).pack(anchor="w", padx=24, pady=(0, 14))\n\nctk.CTkLabel(\n    panel_config,\n    text="¿Qué quieres hacer?",\n    font=("Segoe UI", 14, "bold")\n).pack(anchor="w", padx=22, pady=(4, 8))\n\nboton_preparar = ctk.CTkButton(\n    panel_config,\n    text="PREPARAR ANUNCIO",\n    command=preparar_anuncio_sin_publicar,\n    height=52,\n    font=("Segoe UI", 15, "bold"),\n    corner_radius=11,\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32"\n)\nboton_preparar.pack(fill="x", padx=22, pady=(0, 6))\n\nctk.CTkLabel(\n    panel_config,\n    text="Rellena todo en Wallapop y se detiene antes de publicar.",\n    font=("Segoe UI", 11),\n    text_color=COLOR_TEXTO_SEC\n).pack(anchor="w", padx=24, pady=(0, 10))\n\nboton_wallapop = ctk.CTkButton(\n    panel_config,\n    text="PUBLICAR ANUNCIO",\n    command=publicar_anuncio_directamente,\n    height=56,\n    font=("Segoe UI", 16, "bold"),\n    corner_radius=11,\n    fg_color=COLOR_ACENTO,\n    hover_color="#d91624"\n)\nboton_wallapop.pack(fill="x", padx=22, pady=(0, 6))\n\nctk.CTkLabel(\n    panel_config,\n    text="Completa el proceso y pulsa Publicar automáticamente.",\n    font=("Segoe UI", 11),\n    text_color=COLOR_TEXTO_SEC\n).pack(anchor="w", padx=24, pady=(0, 12))\n\nctk.CTkButton(\n    panel_config,\n    text="Abrir carpeta del anuncio",\n    command=abrir_carpeta_anuncio,\n    height=38,\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32"\n).pack(fill="x", padx=22, pady=(0, 20))\n\npanel_resumen = ctk.CTkFrame(\n    publicar_scroll,\n    fg_color=COLOR_PANEL,\n    corner_radius=14,\n    border_width=1,\n    border_color=COLOR_BORDE\n)\npanel_resumen.grid(row=0, column=1, sticky="nsew", padx=(8, 0), pady=4)\n\nctk.CTkLabel(\n    panel_resumen,\n    text="Resumen antes de publicar",\n    font=("Segoe UI", 21, "bold")\n).pack(anchor="w", padx=22, pady=(20, 6))\n\nctk.CTkLabel(\n    panel_resumen,\n    text="Revisa de un vistazo qué hará el bot.",\n    font=("Segoe UI", 13),\n    text_color=COLOR_TEXTO_SEC\n).pack(anchor="w", padx=22, pady=(0, 15))\n\nresumen_publicacion = ctk.CTkTextbox(\n    panel_resumen,\n    height=400,\n    corner_radius=10,\n    border_width=1,\n    border_color=COLOR_BORDE,\n    font=("Segoe UI", 13)\n)\nresumen_publicacion.pack(fill="both", expand=True, padx=22, pady=(0, 20))\nresumen_publicacion.configure(state="disabled")\n\n# =========================================================\n# TAB 3 · GALERÍA\n# =========================================================\n\ngaleria_scroll = ctk.CTkScrollableFrame(\n    tab_galeria,\n    fg_color="transparent"\n)\ngaleria_scroll.pack(fill="both", expand=True, padx=10, pady=10)\n\ncab_galeria = ctk.CTkFrame(\n    galeria_scroll,\n    fg_color="transparent"\n)\ncab_galeria.pack(fill="x", padx=10, pady=(6, 12))\n\nbloque_titulo_galeria = ctk.CTkFrame(\n    cab_galeria,\n    fg_color="transparent"\n)\nbloque_titulo_galeria.pack(side="left")\n\nctk.CTkLabel(\n    bloque_titulo_galeria,\n    text="Imagen del anuncio",\n    font=("Segoe UI", 24, "bold")\n).pack(anchor="w")\n\nctk.CTkLabel(\n    bloque_titulo_galeria,\n    text="Vista previa profesional antes de enviarla a Wallapop",\n    font=("Segoe UI", 12),\n    text_color=COLOR_TEXTO_SEC\n).pack(anchor="w", pady=(2, 0))\n\nctk.CTkButton(\n    cab_galeria,\n    text="Abrir carpeta",\n    command=abrir_carpeta_anuncio,\n    width=125,\n    height=36,\n    corner_radius=9,\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32",\n    border_width=1,\n    border_color=COLOR_BORDE\n).pack(side="right")\n\nframe_imagenes = ctk.CTkFrame(\n    galeria_scroll,\n    fg_color="transparent"\n)\nframe_imagenes.pack(fill="both", expand=True)\n\nframe_imagenes.grid_columnconfigure(0, weight=3)\nframe_imagenes.grid_columnconfigure(1, weight=1)\nframe_imagenes.grid_rowconfigure(0, weight=1)\n\nvistas = []\n\n# Tarjeta principal de imagen\ntarjeta = ctk.CTkFrame(\n    frame_imagenes,\n    fg_color="#101317",\n    corner_radius=18,\n    border_width=2,\n    border_color=COLOR_ACENTO\n)\ntarjeta.grid(\n    row=0,\n    column=0,\n    sticky="nsew",\n    padx=(8, 6),\n    pady=8\n)\n\ncab_preview = ctk.CTkFrame(\n    tarjeta,\n    fg_color="transparent"\n)\ncab_preview.pack(fill="x", padx=18, pady=(14, 8))\n\nctk.CTkLabel(\n    cab_preview,\n    text="● IMAGEN PRINCIPAL",\n    font=("Segoe UI", 13, "bold"),\n    text_color=COLOR_ACENTO\n).pack(side="left")\n\nctk.CTkLabel(\n    cab_preview,\n    text="TALLER · AUTOMOCIÓN",\n    font=("Segoe UI", 11, "bold"),\n    text_color=COLOR_TEXTO_SEC\n).pack(side="right")\n\nmarco_preview = ctk.CTkFrame(\n    tarjeta,\n    fg_color="#07090b",\n    corner_radius=14,\n    border_width=1,\n    border_color="#30353b"\n)\nmarco_preview.pack(\n    fill="both",\n    expand=True,\n    padx=18,\n    pady=(0, 12)\n)\n\nvista = ctk.CTkLabel(\n    marco_preview,\n    text="SIN IMAGEN\\n\\nGenera un anuncio para ver aquí la vista previa",\n    font=("Segoe UI", 15, "bold"),\n    text_color="#69717a",\n    width=650,\n    height=540,\n    corner_radius=10,\n    fg_color="#0b0e11"\n)\nvista.pack(\n    fill="both",\n    expand=True,\n    padx=10,\n    pady=10\n)\n\nvistas.append(vista)\n\npie_preview = ctk.CTkFrame(\n    tarjeta,\n    fg_color="transparent"\n)\npie_preview.pack(fill="x", padx=18, pady=(0, 14))\n\netiqueta_info_imagen = ctk.CTkLabel(\n    pie_preview,\n    text="1080 × 1080  ·  PNG  ·  1 imagen por anuncio",\n    font=("Segoe UI", 11),\n    text_color=COLOR_TEXTO_SEC\n)\netiqueta_info_imagen.pack(side="left")\n\nctk.CTkLabel(\n    pie_preview,\n    text="LISTA PARA WALLAPOP",\n    font=("Segoe UI", 11, "bold"),\n    text_color="#46d47a"\n).pack(side="right")\n\n# Panel lateral con información\npanel_info_imagen = ctk.CTkFrame(\n    frame_imagenes,\n    fg_color=COLOR_PANEL,\n    corner_radius=16,\n    border_width=1,\n    border_color=COLOR_BORDE\n)\npanel_info_imagen.grid(\n    row=0,\n    column=1,\n    sticky="nsew",\n    padx=(6, 8),\n    pady=8\n)\n\nctk.CTkLabel(\n    panel_info_imagen,\n    text="Detalles",\n    font=("Segoe UI", 18, "bold")\n).pack(anchor="w", padx=18, pady=(18, 14))\n\ndetalles = [\n    ("Formato", "PNG"),\n    ("Resolución", "1080 × 1080"),\n    ("Cantidad", "1 imagen"),\n    ("Estilo", "Taller profesional"),\n    ("Variación", "Anti-repetición"),\n]\n\nfor nombre, valor in detalles:\n    bloque = ctk.CTkFrame(\n        panel_info_imagen,\n        fg_color=COLOR_PANEL_2,\n        corner_radius=9\n    )\n    bloque.pack(fill="x", padx=14, pady=5)\n\n    ctk.CTkLabel(\n        bloque,\n        text=nombre,\n        font=("Segoe UI", 11),\n        text_color=COLOR_TEXTO_SEC\n    ).pack(anchor="w", padx=12, pady=(8, 1))\n\n    ctk.CTkLabel(\n        bloque,\n        text=valor,\n        font=("Segoe UI", 13, "bold")\n    ).pack(anchor="w", padx=12, pady=(0, 8))\n\nctk.CTkLabel(\n    panel_info_imagen,\n    text=(\n        "Cada generación usa una composición diferente "\n        "para reducir repeticiones entre anuncios."\n    ),\n    font=("Segoe UI", 11),\n    text_color=COLOR_TEXTO_SEC,\n    wraplength=230,\n    justify="left"\n).pack(anchor="w", padx=18, pady=(16, 18))\n\n\n# =========================================================\n# =========================================================\n# TAB 4 · HISTORIAL\n# =========================================================\n\nhistorial_panel = ctk.CTkFrame(\n    tab_historial,\n    fg_color=COLOR_PANEL,\n    corner_radius=14,\n    border_width=1,\n    border_color=COLOR_BORDE\n)\nhistorial_panel.pack(fill="both", expand=True, padx=10, pady=10)\n\ncab_hist = ctk.CTkFrame(historial_panel, fg_color="transparent")\ncab_hist.pack(fill="x", padx=20, pady=(18, 8))\n\nctk.CTkLabel(\n    cab_hist,\n    text="Historial de anuncios",\n    font=("Segoe UI", 22, "bold")\n).pack(side="left")\n\nctk.CTkButton(\n    cab_hist,\n    text="Limpiar historial",\n    command=limpiar_historial_visual,\n    width=125,\n    height=32,\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32"\n).pack(side="right", padx=(6, 0))\n\nctk.CTkButton(\n    cab_hist,\n    text="Ver publicados",\n    command=ver_historial_publicados,\n    width=115,\n    height=32,\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32"\n).pack(side="right", padx=6)\n\nctk.CTkButton(\n    cab_hist,\n    text="Ver todos",\n    command=ver_historial_todos,\n    width=95,\n    height=32,\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32"\n).pack(side="right", padx=6)\n\nctk.CTkButton(\n    cab_hist,\n    text="Actualizar",\n    command=refrescar_historial,\n    width=100,\n    height=32,\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32"\n).pack(side="right", padx=6)\n\npanel_historial_inteligente = ctk.CTkFrame(\n    tab_historial,\n    fg_color=COLOR_PANEL_2,\n    corner_radius=10\n)\npanel_historial_inteligente.pack(\n    fill="x",\n    padx=10,\n    pady=(0, 10)\n)\n\nctk.CTkLabel(\n    panel_historial_inteligente,\n    text="Historial inteligente",\n    font=("Segoe UI", 14, "bold")\n).pack(anchor="w", padx=14, pady=(10, 6))\n\nmenu_historial_inteligente = ctk.CTkOptionMenu(\n    panel_historial_inteligente,\n    variable=var_historial_selector,\n    values=["Sin anuncios"],\n    height=34,\n    fg_color=COLOR_PANEL,\n    button_color=COLOR_ACENTO,\n    button_hover_color="#d91624"\n)\nmenu_historial_inteligente.pack(\n    fill="x",\n    padx=14,\n    pady=(0, 8)\n)\n\nfila_hist_int = ctk.CTkFrame(\n    panel_historial_inteligente,\n    fg_color="transparent"\n)\nfila_hist_int.pack(fill="x", padx=14, pady=(0, 10))\n\nctk.CTkButton(\n    fila_hist_int,\n    text="Cargar en editor",\n    command=cargar_historial_en_editor,\n    height=32,\n    fg_color=COLOR_PANEL,\n    hover_color="#252b32"\n).pack(side="left", fill="x", expand=True, padx=(0, 4))\n\nctk.CTkButton(\n    fila_hist_int,\n    text="Preparar siguiente lote",\n    command=preparar_siguiente_lote,\n    height=32,\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32"\n).pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=4\n)\n\nctk.CTkButton(\n    fila_hist_int,\n    text="Lote desde publicados",\n    command=generar_lote_desde_publicados,\n    height=32,\n    fg_color="#7d1b22",\n    hover_color="#9a222b"\n).pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=4\n)\n\nctk.CTkButton(\n    fila_hist_int,\n    text="Crear parecido + cola",\n    command=recrear_parecido_desde_historial,\n    height=32,\n    fg_color="#235d3a",\n    hover_color="#2d7549"\n).pack(side="left", fill="x", expand=True, padx=(4, 0))\n\nhistorial_texto = ctk.CTkTextbox(\n    historial_panel,\n    font=("Segoe UI", 13),\n    corner_radius=10,\n    border_width=1,\n    border_color=COLOR_BORDE\n)\nhistorial_texto.pack(\n    fill="both",\n    expand=True,\n    padx=20,\n    pady=(4, 20)\n)\nhistorial_texto.configure(state="disabled")\n\n# =========================================================\n# TAB 5 · COLA DE PUBLICACIÓN · MODO SIMPLE\n# =========================================================\n\ncola_panel = ctk.CTkFrame(\n    tab_cola,\n    fg_color=COLOR_PANEL,\n    corner_radius=12,\n    border_width=1,\n    border_color=COLOR_BORDE\n)\ncola_panel.pack(\n    fill="both",\n    expand=True,\n    padx=10,\n    pady=10\n)\n\n# ----- Cabecera -----\ncab_cola = ctk.CTkFrame(\n    cola_panel,\n    fg_color="transparent"\n)\ncab_cola.pack(\n    fill="x",\n    padx=18,\n    pady=(16, 6)\n)\n\ncab_cola_textos = ctk.CTkFrame(\n    cab_cola,\n    fg_color="transparent"\n)\ncab_cola_textos.pack(\n    side="left",\n    fill="x",\n    expand=True\n)\n\nctk.CTkLabel(\n    cab_cola_textos,\n    text="Cola de publicación",\n    font=("Segoe UI", 24, "bold")\n).pack(\n    anchor="w"\n)\n\nctk.CTkLabel(\n    cab_cola_textos,\n    text=(\n        "Aquí solo tienes que revisar los anuncios y pulsar INICIAR COLA."\n    ),\n    font=("Segoe UI", 11),\n    text_color=COLOR_TEXTO_SEC\n).pack(\n    anchor="w",\n    pady=(3, 0)\n)\n\netiqueta_cola_resumen = ctk.CTkLabel(\n    cab_cola,\n    text="0 anuncios · 0 pendientes",\n    font=("Segoe UI", 11, "bold"),\n    text_color="#46d47a"\n)\netiqueta_cola_resumen.pack(\n    side="right",\n    padx=(10, 0)\n)\n\n# ----- Estado -----\npanel_estado_cola_simple = ctk.CTkFrame(\n    cola_panel,\n    fg_color=COLOR_PANEL_2,\n    corner_radius=9\n)\npanel_estado_cola_simple.pack(\n    fill="x",\n    padx=18,\n    pady=(4, 8)\n)\n\netiqueta_salud_cola = ctk.CTkLabel(\n    panel_estado_cola_simple,\n    text="Comprobando estado...",\n    font=("Segoe UI", 11, "bold"),\n    text_color="#f7c948"\n)\netiqueta_salud_cola.pack(\n    side="left",\n    padx=14,\n    pady=10\n)\n\netiqueta_eta_cola = ctk.CTkLabel(\n    panel_estado_cola_simple,\n    text="ETA aprox.: 00:00",\n    font=("Segoe UI", 10),\n    text_color=COLOR_TEXTO_SEC\n)\netiqueta_eta_cola.pack(\n    side="right",\n    padx=14,\n    pady=10\n)\n\n# ----- Progreso -----\netiqueta_progreso_cola = ctk.CTkLabel(\n    cola_panel,\n    text="Progreso: 0 / 0",\n    font=("Segoe UI", 10),\n    text_color=COLOR_TEXTO_SEC,\n    anchor="w"\n)\netiqueta_progreso_cola.pack(\n    fill="x",\n    padx=18,\n    pady=(2, 3)\n)\n\nprogreso_cola = ctk.CTkProgressBar(\n    cola_panel,\n    height=10,\n    corner_radius=5,\n    progress_color=COLOR_ACENTO\n)\nprogreso_cola.pack(\n    fill="x",\n    padx=18,\n    pady=(0, 5)\n)\nprogreso_cola.set(0)\n\netiqueta_resumen_sesion = ctk.CTkLabel(\n    cola_panel,\n    text="Sesión · Publicados: 0 · Errores: 0 · Tiempo: 00:00",\n    font=("Segoe UI", 10),\n    text_color=COLOR_TEXTO_SEC,\n    anchor="w"\n)\netiqueta_resumen_sesion.pack(\n    fill="x",\n    padx=18,\n    pady=(0, 10)\n)\n\n# ----- Botones principales: solo lo necesario -----\nfila_principal_cola = ctk.CTkFrame(\n    cola_panel,\n    fg_color="transparent"\n)\nfila_principal_cola.pack(\n    fill="x",\n    padx=18,\n    pady=(0, 10)\n)\n\nboton_iniciar_cola = ctk.CTkButton(\n    fila_principal_cola,\n    text="▶  INICIAR COLA",\n    command=iniciar_cola_publicacion,\n    height=46,\n    font=("Segoe UI", 14, "bold"),\n    fg_color=COLOR_ACENTO,\n    hover_color="#d91624"\n)\nboton_iniciar_cola.pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=(0, 5)\n)\n\nboton_pausar_cola = ctk.CTkButton(\n    fila_principal_cola,\n    text="⏸ Pausar",\n    command=pausar_cola,\n    height=46,\n    width=120,\n    state="disabled",\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32"\n)\nboton_pausar_cola.pack(\n    side="left",\n    padx=5\n)\n\nboton_reanudar_cola = ctk.CTkButton(\n    fila_principal_cola,\n    text="▶ Reanudar",\n    command=reanudar_cola,\n    height=46,\n    width=120,\n    state="disabled",\n    fg_color="#235d3a",\n    hover_color="#2d7549"\n)\nboton_reanudar_cola.pack(\n    side="left",\n    padx=5\n)\n\nboton_cancelar_cola = ctk.CTkButton(\n    fila_principal_cola,\n    text="■ Detener",\n    command=cancelar_cola,\n    height=46,\n    width=120,\n    state="disabled",\n    fg_color="#7d1b22",\n    hover_color="#9a222b"\n)\nboton_cancelar_cola.pack(\n    side="left",\n    padx=5\n)\n\n# ----- Segunda línea, operaciones comunes -----\nfila_comun_cola = ctk.CTkFrame(\n    cola_panel,\n    fg_color="transparent"\n)\nfila_comun_cola.pack(\n    fill="x",\n    padx=18,\n    pady=(0, 10)\n)\n\nctk.CTkButton(\n    fila_comun_cola,\n    text="+ Añadir anuncio actual",\n    command=agregar_actual_a_cola,\n    height=36,\n    fg_color="#235d3a",\n    hover_color="#2d7549"\n).pack(\n    side="left",\n    padx=(0, 5)\n)\n\nctk.CTkButton(\n    fila_comun_cola,\n    text="Reintentar errores",\n    command=reintentar_errores_cola,\n    height=36,\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32"\n).pack(\n    side="left",\n    padx=5\n)\n\nctk.CTkButton(\n    fila_comun_cola,\n    text="Reparar errores",\n    command=reparar_errores_cola_automaticamente,\n    height=36,\n    fg_color="#9b6518",\n    hover_color="#b9781e"\n).pack(\n    side="left",\n    padx=5\n)\n\nctk.CTkButton(\n    fila_comun_cola,\n    text="Vaciar cola",\n    command=vaciar_cola,\n    height=36,\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32"\n).pack(\n    side="left",\n    padx=5\n)\n\n# ----- Herramientas avanzadas en una ventana aparte -----\ndef ejecutar_herramienta_avanzada_segura(\n    nombre,\n    funcion\n):\n    """\n    Ejecuta una herramienta avanzada sin dejar el botón \'muerto\'.\n    Si falla, muestra el error real y lo registra para el Diagnóstico PRO.\n    """\n    try:\n        if not callable(\n            funcion\n        ):\n            raise RuntimeError(\n                f"La herramienta \'{nombre}\' no está disponible."\n            )\n\n        estado.configure(\n            text=f"⚙ Ejecutando: {nombre}..."\n        )\n\n        ventana.update_idletasks()\n\n        resultado = funcion()\n\n        estado.configure(\n            text=f"✅ Herramienta completada: {nombre}"\n        )\n\n        try:\n            refrescar_cola()\n        except Exception:\n            pass\n\n        try:\n            refrescar_dashboard()\n        except Exception:\n            pass\n\n        return resultado\n\n    except Exception as e:\n        mensaje = (\n            f"{nombre}\\n\\n"\n            f"{type(e).__name__}: {e}"\n        )\n\n        print(\n            "[HERRAMIENTA AVANZADA ERROR]",\n            mensaje\n        )\n\n        try:\n            estado.configure(\n                text=f"❌ {nombre}: {e}"\n            )\n        except Exception:\n            pass\n\n        try:\n            messagebox.showerror(\n                "Herramienta avanzada",\n                mensaje\n            )\n        except Exception:\n            pass\n\n        return None\n\n\ndef _herramienta_por_nombre(nombre_funcion):\n    fn = globals().get(\n        nombre_funcion\n    )\n\n    return fn if callable(fn) else None\n\n\ndef ejecutar_herramienta_por_nombre(\n    nombre_visible,\n    nombre_funcion\n):\n    fn = _herramienta_por_nombre(\n        nombre_funcion\n    )\n\n    if fn is None:\n        messagebox.showerror(\n            "Herramienta avanzada",\n            (\n                f"No encuentro la función interna:\\n"\n                f"{nombre_funcion}\\n\\n"\n                "Ejecuta Diagnóstico PRO para revisar la instalación."\n            )\n        )\n        return\n\n    return ejecutar_herramienta_avanzada_segura(\n        nombre_visible,\n        fn\n    )\n\n\ndef abrir_herramientas_avanzadas_cola():\n    win = ctk.CTkToplevel(\n        ventana\n    )\n\n    win.title(\n        "DIAGPROG5 · Herramientas avanzadas"\n    )\n\n    win.geometry(\n        "900x700"\n    )\n\n    win.minsize(\n        780,\n        600\n    )\n\n    win.configure(\n        fg_color=COLOR_FONDO\n    )\n\n    ctk.CTkLabel(\n        win,\n        text="Herramientas avanzadas",\n        font=("Segoe UI", 22, "bold")\n    ).pack(\n        anchor="w",\n        padx=20,\n        pady=(18, 4)\n    )\n\n    ctk.CTkLabel(\n        win,\n        text=(\n            "Todas las acciones de esta pantalla están protegidas: "\n            "si una falla verás el error exacto en vez de que el botón "\n            "parezca no hacer nada."\n        ),\n        font=("Segoe UI", 10),\n        text_color=COLOR_TEXTO_SEC,\n        wraplength=830,\n        justify="left"\n    ).pack(\n        anchor="w",\n        padx=20,\n        pady=(0, 12)\n    )\n\n    scroll = ctk.CTkScrollableFrame(\n        win,\n        fg_color=COLOR_PANEL\n    )\n\n    scroll.pack(\n        fill="both",\n        expand=True,\n        padx=18,\n        pady=(0, 18)\n    )\n\n    grupos = [\n        (\n            "Revisión y reparación",\n            [\n                (\n                    "Validar cola",\n                    "validar_toda_la_cola"\n                ),\n                (\n                    "Auditar cola",\n                    "ejecutar_auditoria_cola"\n                ),\n                (\n                    "Control de calidad",\n                    "validar_calidad_toda_cola"\n                ),\n                (\n                    "Reintentar seguros",\n                    "reintentar_errores_seguros"\n                ),\n                (\n                    "Vaciar cuarentena",\n                    "restaurar_toda_cuarentena"\n                ),\n                (\n                    "Analítica de errores",\n                    "analitica_errores"\n                ),\n            ]\n        ),\n        (\n            "Orden y organización",\n            [\n                (\n                    "Favoritos primero",\n                    "ordenar_favoritos_primero"\n                ),\n                (\n                    "Ordenar pendientes",\n                    "ordenar_pendientes_por_prioridad"\n                ),\n                (\n                    "Reanudar pausados",\n                    "reanudar_todos_pausados"\n                ),\n                (\n                    "Limpiar terminados",\n                    "limpiar_estados_terminados"\n                ),\n                (\n                    "Archivar publicados",\n                    "archivar_publicados_manual"\n                ),\n                (\n                    "Limpiar publicados",\n                    "eliminar_publicados_de_cola"\n                ),\n            ]\n        ),\n        (\n            "Exportar y diagnóstico",\n            [\n                (\n                    "Exportar CSV",\n                    "exportar_cola_csv"\n                ),\n                (\n                    "Exportar JSON",\n                    "exportar_cola_json"\n                ),\n                (\n                    "Actividad CSV",\n                    "exportar_actividad_csv"\n                ),\n                (\n                    "Informe auditoría",\n                    "exportar_informe_auditoria"\n                ),\n                (\n                    "Diagnóstico ZIP",\n                    "exportar_paquete_diagnostico"\n                ),\n                (\n                    "Detectar parecidos",\n                    "detectar_similitudes_cola"\n                ),\n            ]\n        ),\n        (\n            "Lotes",\n            [\n                (\n                    "Estadísticas del lote",\n                    "mostrar_estadisticas_lote"\n                ),\n                (\n                    "CSV del lote",\n                    "exportar_csv_lote_actual"\n                ),\n                (\n                    "Manifiesto del lote",\n                    "exportar_manifiesto_lote"\n                ),\n                (\n                    "Duplicar lote",\n                    "duplicar_lote_con_variaciones"\n                ),\n                (\n                    "Archivar lote",\n                    "archivar_lote_actual"\n                ),\n                (\n                    "Deshacer última acción",\n                    "deshacer_ultima_accion_cola"\n                ),\n            ]\n        ),\n    ]\n\n    for titulo, acciones in grupos:\n        bloque = ctk.CTkFrame(\n            scroll,\n            fg_color=COLOR_PANEL_2,\n            corner_radius=10,\n            border_width=1,\n            border_color=COLOR_BORDE\n        )\n\n        bloque.pack(\n            fill="x",\n            padx=8,\n            pady=7\n        )\n\n        ctk.CTkLabel(\n            bloque,\n            text=titulo,\n            font=("Segoe UI", 14, "bold")\n        ).pack(\n            anchor="w",\n            padx=12,\n            pady=(10, 6)\n        )\n\n        grid = ctk.CTkFrame(\n            bloque,\n            fg_color="transparent"\n        )\n\n        grid.pack(\n            fill="x",\n            padx=8,\n            pady=(0, 10)\n        )\n\n        for i, (\n            nombre_visible,\n            nombre_funcion\n        ) in enumerate(\n            acciones\n        ):\n            disponible = (\n                _herramienta_por_nombre(\n                    nombre_funcion\n                )\n                is not None\n            )\n\n            ctk.CTkButton(\n                grid,\n                text=(\n                    nombre_visible\n                    if disponible\n                    else f"⚠ {nombre_visible}"\n                ),\n                command=(\n                    lambda nv=nombre_visible, nf=nombre_funcion:\n                    ejecutar_herramienta_por_nombre(\n                        nv,\n                        nf\n                    )\n                ),\n                height=36,\n                fg_color=(\n                    COLOR_PANEL\n                    if disponible\n                    else "#6f3b16"\n                ),\n                hover_color="#252b32"\n            ).grid(\n                row=i // 3,\n                column=i % 3,\n                sticky="ew",\n                padx=4,\n                pady=4\n            )\n\n        for c in range(3):\n            grid.grid_columnconfigure(\n                c,\n                weight=1\n            )\n\n    # Botón de prueba al final.\n    ctk.CTkButton(\n        scroll,\n        text="🛠 Ejecutar Diagnóstico PRO",\n        command=diagnosticar_y_reparar_bot,\n        height=42,\n        fg_color="#315d83",\n        hover_color="#3c719d",\n        font=("Segoe UI", 12, "bold")\n    ).pack(\n        fill="x",\n        padx=8,\n        pady=(8, 12)\n    )\n\n\nctk.CTkButton(\n    fila_comun_cola,\n    text="⚙ Herramientas avanzadas",\n    command=abrir_herramientas_avanzadas_cola,\n    height=36,\n    fg_color="#315d83",\n    hover_color="#3c719d"\n).pack(\n    side="right",\n    padx=(5, 0)\n)\n\n# ----- Filtro mínimo -----\nfila_filtro_simple = ctk.CTkFrame(\n    cola_panel,\n    fg_color=COLOR_PANEL_2,\n    corner_radius=8\n)\nfila_filtro_simple.pack(\n    fill="x",\n    padx=18,\n    pady=(0, 8)\n)\n\nctk.CTkLabel(\n    fila_filtro_simple,\n    text="Mostrar:",\n    font=("Segoe UI", 11, "bold")\n).pack(\n    side="left",\n    padx=(12, 6),\n    pady=8\n)\n\nmenu_filtro_cola = ctk.CTkOptionMenu(\n    fila_filtro_simple,\n    variable=var_filtro_cola,\n    values=[\n        "TODOS",\n        "PENDIENTE",\n        "PUBLICANDO",\n        "PUBLICADO",\n        "ERROR",\n        "REVISAR",\n        "CUARENTENA",\n        "PAUSADO"\n    ],\n    command=lambda _v: refrescar_cola(),\n    width=150,\n    height=30,\n    fg_color=COLOR_PANEL,\n    button_color=COLOR_ACENTO\n)\nmenu_filtro_cola.pack(\n    side="left",\n    padx=(0, 8),\n    pady=8\n)\n\nentrada_busqueda_cola = ctk.CTkEntry(\n    fila_filtro_simple,\n    textvariable=var_busqueda_cola,\n    placeholder_text="Buscar por título...",\n    height=30\n)\nentrada_busqueda_cola.pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=6,\n    pady=8\n)\n\nctk.CTkButton(\n    fila_filtro_simple,\n    text="Buscar",\n    command=aplicar_busqueda_cola,\n    width=80,\n    height=30,\n    fg_color=COLOR_PANEL,\n    hover_color="#252b32"\n).pack(\n    side="left",\n    padx=(6, 12),\n    pady=8\n)\n\n# ----- Lista principal -----\nctk.CTkLabel(\n    cola_panel,\n    text="Anuncios de la cola",\n    font=("Segoe UI", 13, "bold")\n).pack(\n    anchor="w",\n    padx=18,\n    pady=(4, 5)\n)\n\ncola_texto = ctk.CTkTextbox(\n    cola_panel,\n    font=("Segoe UI", 12),\n    corner_radius=9,\n    border_width=1,\n    border_color=COLOR_BORDE,\n    height=230\n)\ncola_texto.pack(\n    fill="both",\n    expand=True,\n    padx=18,\n    pady=(0, 10)\n)\ncola_texto.configure(\n    state="disabled"\n)\n\n# ----- Gestión de un anuncio -----\ngestion_cola = ctk.CTkFrame(\n    cola_panel,\n    fg_color=COLOR_PANEL_2,\n    corner_radius=9\n)\ngestion_cola.pack(\n    fill="x",\n    padx=18,\n    pady=(0, 10)\n)\n\nctk.CTkLabel(\n    gestion_cola,\n    text="Anuncio seleccionado",\n    font=("Segoe UI", 12, "bold")\n).pack(\n    anchor="w",\n    padx=12,\n    pady=(10, 5)\n)\n\nselector_cola = ctk.CTkOptionMenu(\n    gestion_cola,\n    values=["Cola vacía"],\n    command=al_cambiar_selector_cola,\n    height=34,\n    fg_color=COLOR_PANEL,\n    button_color=COLOR_ACENTO\n)\nselector_cola.pack(\n    fill="x",\n    padx=12,\n    pady=(0, 7)\n)\n\nfila_gestion_simple = ctk.CTkFrame(\n    gestion_cola,\n    fg_color="transparent"\n)\nfila_gestion_simple.pack(\n    fill="x",\n    padx=8,\n    pady=(0, 10)\n)\n\nfor texto_b, comando_b, color_b in [\n    ("Cargar en editor", cargar_seleccionado_en_editor, COLOR_PANEL),\n    ("Guardar cambios", actualizar_seleccionado_desde_editor, "#235d3a"),\n    ("Reintentar", reintentar_seleccionado_cola, "#235d3a"),\n    ("Ver error", ver_error_seleccionado_cola, "#9b6518"),\n    ("Eliminar", eliminar_seleccionado_cola, "#7d1b22"),\n]:\n    ctk.CTkButton(\n        fila_gestion_simple,\n        text=texto_b,\n        command=comando_b,\n        height=32,\n        fg_color=color_b,\n        hover_color="#252b32"\n    ).pack(\n        side="left",\n        fill="x",\n        expand=True,\n        padx=4\n    )\n\n# Variables/widgets heredados que usan funciones existentes.\nvar_orden_cola.set(\n    var_orden_cola.get()\n)\nvar_prioridad_lote.set(\n    var_prioridad_lote.get()\n)\n\n# Detalle de error: solo se muestra cuando se necesita.\ndetalle_error_frame = ctk.CTkFrame(\n    cola_panel,\n    fg_color="#16191d",\n    corner_radius=8,\n    border_width=1,\n    border_color="#7d1b22"\n)\ndetalle_error_frame.pack(\n    fill="x",\n    padx=18,\n    pady=(0, 12)\n)\n\nctk.CTkLabel(\n    detalle_error_frame,\n    text="Detalle / diagnóstico del anuncio seleccionado",\n    font=("Segoe UI", 11, "bold"),\n    text_color="#ff6b6b"\n).pack(\n    anchor="w",\n    padx=12,\n    pady=(8, 3)\n)\n\ndetalle_error_texto = ctk.CTkTextbox(\n    detalle_error_frame,\n    height=70,\n    font=("Segoe UI", 11),\n    corner_radius=7,\n    fg_color="#101214"\n)\ndetalle_error_texto.pack(\n    fill="x",\n    padx=12,\n    pady=(0, 10)\n)\ndetalle_error_texto.insert(\n    "1.0",\n    "Selecciona un anuncio y pulsa “Ver error” si necesitas el diagnóstico."\n)\ndetalle_error_texto.configure(\n    state="disabled"\n)\n\ncargar_cola_desde_disco()\nrecuperar_sesion_interrumpida()\nrefrescar_cola()\nactualizar_selector_cola()\n\n# =========================================================\n# TAB 6 · ESTADÍSTICAS\n# =========================================================\n\nestadisticas_panel = ctk.CTkFrame(\n    tab_estadisticas,\n    fg_color=COLOR_PANEL,\n    corner_radius=14,\n    border_width=1,\n    border_color=COLOR_BORDE\n)\nestadisticas_panel.pack(\n    fill="both",\n    expand=True,\n    padx=10,\n    pady=10\n)\n\ncab_stats = ctk.CTkFrame(\n    estadisticas_panel,\n    fg_color="transparent"\n)\ncab_stats.pack(\n    fill="x",\n    padx=20,\n    pady=(18, 10)\n)\n\nctk.CTkLabel(\n    cab_stats,\n    text="Estadísticas del bot",\n    font=("Segoe UI", 22, "bold")\n).pack(side="left")\n\nctk.CTkButton(\n    cab_stats,\n    text="Reiniciar actividad",\n    command=reiniciar_estadisticas,\n    width=135,\n    height=32,\n    fg_color="#7d1b22",\n    hover_color="#9a222b"\n).pack(side="right", padx=(6, 0))\n\nctk.CTkButton(\n    cab_stats,\n    text="Actualizar",\n    command=refrescar_estadisticas,\n    width=95,\n    height=32,\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32"\n).pack(side="right", padx=6)\n\nctk.CTkLabel(\n    estadisticas_panel,\n    text=(\n        "Los contadores de actividad se guardan entre sesiones. "\n        "El resumen de anuncios se calcula a partir de tus carpetas reales."\n    ),\n    font=("Segoe UI", 12),\n    text_color=COLOR_TEXTO_SEC,\n    wraplength=1000,\n    justify="left"\n).pack(\n    anchor="w",\n    padx=20,\n    pady=(0, 12)\n)\n\nestadisticas_texto = ctk.CTkTextbox(\n    estadisticas_panel,\n    font=("Segoe UI", 14),\n    corner_radius=10,\n    border_width=1,\n    border_color=COLOR_BORDE\n)\nestadisticas_texto.pack(\n    fill="both",\n    expand=True,\n    padx=20,\n    pady=(0, 20)\n)\nestadisticas_texto.configure(state="disabled")\n\n# =========================================================\n# TAB 7 · CONFIGURACIÓN\n# =========================================================\n\nconfig_scroll = ctk.CTkScrollableFrame(\n    tab_configuracion,\n    fg_color="transparent"\n)\nconfig_scroll.pack(\n    fill="both",\n    expand=True,\n    padx=10,\n    pady=10\n)\n\npanel_config_general = ctk.CTkFrame(\n    config_scroll,\n    fg_color=COLOR_PANEL,\n    corner_radius=14,\n    border_width=1,\n    border_color=COLOR_BORDE\n)\npanel_config_general.pack(\n    fill="both",\n    expand=True,\n    padx=4,\n    pady=4\n)\n\npanel_reparacion_automatica = ctk.CTkFrame(\n    panel_config_general,\n    fg_color=COLOR_PANEL_2,\n    corner_radius=12,\n    border_width=1,\n    border_color=COLOR_BORDE\n)\npanel_reparacion_automatica.pack(\n    fill="x",\n    padx=22,\n    pady=(16, 10)\n)\n\nctk.CTkLabel(\n    panel_reparacion_automatica,\n    text="Diagnóstico PRO y autorreparación",\n    font=("Segoe UI", 17, "bold")\n).pack(\n    anchor="w",\n    padx=16,\n    pady=(14, 3)\n)\n\nctk.CTkLabel(\n    panel_reparacion_automatica,\n    text=(\n        "Comprueba carpetas, cola, archivos JSON, duplicados, "\n        "estados atascados y funciones críticas. Repara automáticamente "\n        "solo los problemas que son seguros de corregir."\n    ),\n    font=("Segoe UI", 10),\n    text_color=COLOR_TEXTO_SEC,\n    wraplength=900,\n    justify="left"\n).pack(\n    anchor="w",\n    padx=16,\n    pady=(0, 10)\n)\n\nctk.CTkButton(\n    panel_reparacion_automatica,\n    text="🛠 ANALIZAR Y REPARAR TODO",\n    command=diagnosticar_y_reparar_bot,\n    height=44,\n    fg_color="#315d83",\n    hover_color="#3c719d",\n    font=("Segoe UI", 13, "bold")\n).pack(\n    fill="x",\n    padx=16,\n    pady=(0, 14)\n)\n\n\n# =========================================================\n# ROTACIÓN AUTOMÁTICA DE CATÁLOGO · AJUSTES\n# =========================================================\n\npanel_rotacion_catalogo = ctk.CTkFrame(\n    panel_config_general,\n    fg_color=COLOR_PANEL_2,\n    corner_radius=12,\n    border_width=1,\n    border_color=COLOR_BORDE\n)\n\npanel_rotacion_catalogo.pack(\n    fill="x",\n    padx=22,\n    pady=(8, 12)\n)\n\nctk.CTkLabel(\n    panel_rotacion_catalogo,\n    text="Rotación automática del catálogo",\n    font=("Segoe UI", 17, "bold")\n).pack(\n    anchor="w",\n    padx=16,\n    pady=(14, 3)\n)\n\nctk.CTkLabel(\n    panel_rotacion_catalogo,\n    text=(\n        "Si Wallapop indica que has alcanzado el límite, DIAGPROG5 "\n        "puede borrar un anuncio antiguo creado por el propio bot, "\n        "confirmar el hueco y reintentar el anuncio nuevo."\n    ),\n    font=("Segoe UI", 10),\n    text_color=COLOR_TEXTO_SEC,\n    wraplength=900,\n    justify="left"\n).pack(\n    anchor="w",\n    padx=16,\n    pady=(0, 10)\n)\n\n_cfg_rotacion_ui = cargar_config_rotacion()\n\nvar_rotacion_activa = ctk.BooleanVar(\n    value=bool(\n        _cfg_rotacion_ui.get(\n            "activo",\n            True\n        )\n    )\n)\n\nvar_rotacion_antiguedad = ctk.StringVar(\n    value=str(\n        _cfg_rotacion_ui.get(\n            "antiguedad_min_horas",\n            24\n        )\n    )\n)\n\nvar_rotacion_max_sesion = ctk.StringVar(\n    value=str(\n        _cfg_rotacion_ui.get(\n            "max_borrados_sesion",\n            5\n        )\n    )\n)\n\nvar_rotacion_backup = ctk.BooleanVar(\n    value=bool(\n        _cfg_rotacion_ui.get(\n            "backup_antes_borrar",\n            True\n        )\n    )\n)\n\nvar_rotacion_reintentar = ctk.BooleanVar(\n    value=bool(\n        _cfg_rotacion_ui.get(\n            "reintentar_publicacion",\n            True\n        )\n    )\n)\n\n\ndef _rotacion_autoguardar_switch():\n    try:\n        # Dejar que CustomTkinter actualice primero la BooleanVar.\n        ventana.after(\n            40,\n            guardar_ajustes_rotacion_ui\n        )\n    except Exception:\n        try:\n            guardar_ajustes_rotacion_ui()\n        except Exception:\n            pass\n\n\ndef guardar_ajustes_rotacion_ui():\n    try:\n        horas = max(\n            0,\n            int(\n                var_rotacion_antiguedad.get()\n            )\n        )\n\n        max_sesion = max(\n            1,\n            int(\n                var_rotacion_max_sesion.get()\n            )\n        )\n\n        cfg = cargar_config_rotacion()\n\n        cfg.update({\n            "activo": bool(\n                var_rotacion_activa.get()\n            ),\n            "solo_anuncios_bot": True,\n            "antiguedad_min_horas": horas,\n            "max_borrados_sesion": max_sesion,\n            "backup_antes_borrar": bool(\n                var_rotacion_backup.get()\n            ),\n            "reintentar_publicacion": bool(\n                var_rotacion_reintentar.get()\n            ),\n        })\n\n        guardar_config_rotacion(\n            cfg\n        )\n\n        estado.configure(\n            text="✅ Ajustes de rotación guardados."\n        )\n\n    except Exception as e:\n        messagebox.showerror(\n            "Rotación automática",\n            "No pude guardar los ajustes:\\n\\n"\n            + str(e)\n        )\n\n\nctk.CTkSwitch(\n    panel_rotacion_catalogo,\n    text="Activar rotación automática al alcanzar el límite",\n    variable=var_rotacion_activa,\n    command=_rotacion_autoguardar_switch,\n    progress_color=COLOR_ACENTO\n).pack(\n    anchor="w",\n    padx=16,\n    pady=(2, 8)\n)\n\nfila_rotacion_campos = ctk.CTkFrame(\n    panel_rotacion_catalogo,\n    fg_color="transparent"\n)\n\nfila_rotacion_campos.pack(\n    fill="x",\n    padx=16,\n    pady=(0, 8)\n)\n\ncol_rot_a = ctk.CTkFrame(\n    fila_rotacion_campos,\n    fg_color="transparent"\n)\ncol_rot_a.pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=(0, 5)\n)\n\nctk.CTkLabel(\n    col_rot_a,\n    text="Antigüedad mínima para borrar (horas)",\n    font=("Segoe UI", 10, "bold")\n).pack(\n    anchor="w"\n)\n\nctk.CTkEntry(\n    col_rot_a,\n    textvariable=var_rotacion_antiguedad,\n    height=34\n).pack(\n    fill="x",\n    pady=(4, 0)\n)\n\ncol_rot_b = ctk.CTkFrame(\n    fila_rotacion_campos,\n    fg_color="transparent"\n)\ncol_rot_b.pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=(5, 0)\n)\n\nctk.CTkLabel(\n    col_rot_b,\n    text="Máximo de eliminaciones por sesión",\n    font=("Segoe UI", 10, "bold")\n).pack(\n    anchor="w"\n)\n\nctk.CTkEntry(\n    col_rot_b,\n    textvariable=var_rotacion_max_sesion,\n    height=34\n).pack(\n    fill="x",\n    pady=(4, 0)\n)\n\nctk.CTkSwitch(\n    panel_rotacion_catalogo,\n    text="Crear backup local antes de eliminar",\n    variable=var_rotacion_backup,\n    command=_rotacion_autoguardar_switch,\n    progress_color=COLOR_ACENTO\n).pack(\n    anchor="w",\n    padx=16,\n    pady=(2, 6)\n)\n\nctk.CTkSwitch(\n    panel_rotacion_catalogo,\n    text="Reintentar automáticamente el anuncio nuevo",\n    variable=var_rotacion_reintentar,\n    command=_rotacion_autoguardar_switch,\n    progress_color=COLOR_ACENTO\n).pack(\n    anchor="w",\n    padx=16,\n    pady=(2, 10)\n)\n\nctk.CTkLabel(\n    panel_rotacion_catalogo,\n    text=(\n        "Protección activa: solo se consideran anuncios registrados como "\n        "publicados por DIAGPROG5. Los anuncios marcados 🔒 nunca se borran."\n    ),\n    font=("Segoe UI", 9),\n    text_color="#46d47a",\n    wraplength=900,\n    justify="left"\n).pack(\n    anchor="w",\n    padx=16,\n    pady=(0, 8)\n)\n\nfila_rotacion_botones = ctk.CTkFrame(\n    panel_rotacion_catalogo,\n    fg_color="transparent"\n)\n\nfila_rotacion_botones.pack(\n    fill="x",\n    padx=12,\n    pady=(0, 14)\n)\n\nctk.CTkButton(\n    fila_rotacion_botones,\n    text="Guardar ajustes",\n    command=guardar_ajustes_rotacion_ui,\n    height=34,\n    fg_color="#235d3a",\n    hover_color="#2d7549"\n).pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=4\n)\n\nctk.CTkButton(\n    fila_rotacion_botones,\n    text="Ver candidatos",\n    command=mostrar_candidatos_rotacion,\n    height=34,\n    fg_color=COLOR_PANEL,\n    hover_color="#252b32"\n).pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=4\n)\n\nctk.CTkButton(\n    fila_rotacion_botones,\n    text="🔒 Proteger anuncios",\n    command=abrir_gestor_protecciones_rotacion,\n    height=34,\n    fg_color=COLOR_PANEL,\n    hover_color="#252b32"\n).pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=4\n)\n\n\nfila_rotacion_pruebas = ctk.CTkFrame(\n    panel_rotacion_catalogo,\n    fg_color="transparent"\n)\n\nfila_rotacion_pruebas.pack(\n    fill="x",\n    padx=12,\n    pady=(0, 14)\n)\n\nctk.CTkButton(\n    fila_rotacion_pruebas,\n    text="Diagnosticar rotación",\n    command=diagnosticar_rotacion_catalogo,\n    height=34,\n    fg_color="#315d83",\n    hover_color="#3c719d"\n).pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=4\n)\n\nctk.CTkButton(\n    fila_rotacion_pruebas,\n    text="Probar sin borrar",\n    command=probar_rotacion_sin_borrar,\n    height=34,\n    fg_color="#315d83",\n    hover_color="#3c719d"\n).pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=4\n)\n\n\n# =========================================================\n# CENTRO DE ACTUALIZACIONES · ADMIN · PANTALLA PROPIA\n# =========================================================\n\nupdates_scroll = ctk.CTkScrollableFrame(\n    tab_actualizaciones,\n    fg_color="transparent"\n)\nupdates_scroll.pack(\n    fill="both",\n    expand=True,\n    padx=10,\n    pady=10\n)\n\nupdates_shell = ctk.CTkFrame(\n    updates_scroll,\n    fg_color=COLOR_PANEL,\n    corner_radius=14,\n    border_width=1,\n    border_color=COLOR_BORDE\n)\nupdates_shell.pack(\n    fill="both",\n    expand=True,\n    padx=4,\n    pady=4\n)\n\nctk.CTkLabel(\n    updates_shell,\n    text="ACTUALIZACIONES · ADMIN",\n    font=("Segoe UI", 24, "bold"),\n    text_color="#ffffff"\n).pack(anchor="w", padx=22, pady=(20, 2))\n\nctk.CTkLabel(\n    updates_shell,\n    text="Centro de control para comprobar y preparar nuevas versiones de DIAGPROG5.",\n    font=("Segoe UI", 11),\n    text_color=COLOR_TEXTO_SEC\n).pack(anchor="w", padx=22, pady=(0, 12))\n\npanel_actualizaciones_admin = ctk.CTkFrame(\n    updates_shell,\n    fg_color=COLOR_PANEL_2,\n    corner_radius=12,\n    border_width=1,\n    border_color=COLOR_BORDE\n)\n\npanel_actualizaciones_admin.pack(\n    fill="x",\n    padx=22,\n    pady=(8, 12)\n)\n\nctk.CTkLabel(\n    panel_actualizaciones_admin,\n    text="Centro de actualizaciones · CONECTADO",\n    font=("Segoe UI", 17, "bold")\n).pack(\n    anchor="w",\n    padx=16,\n    pady=(14, 3)\n)\n\nctk.CTkLabel(\n    panel_actualizaciones_admin,\n    text=(\n        "Este es el puente de actualización de tu versión ADMIN. "\n        "Cuando conectemos una URL de actualizaciones, podrás comprobar, "\n        "descargar y abrir nuevas versiones desde el propio programa."\n    ),\n    font=("Segoe UI", 10),\n    text_color=COLOR_TEXTO_SEC,\n    wraplength=900,\n    justify="left"\n).pack(\n    anchor="w",\n    padx=16,\n    pady=(0, 10)\n)\n\n_cfg_updates_ui = cargar_config_actualizaciones()\n\nvar_updates_activo = ctk.BooleanVar(\n    value=bool(\n        _cfg_updates_ui.get(\n            "activo",\n            False\n        )\n    )\n)\n\nvar_updates_url = ctk.StringVar(\n    value=str(\n        _cfg_updates_ui.get(\n            "manifest_url",\n            "https://diagprog5-updater.hugoneitor05cod3.workers.dev/version"\n        )\n    ) or "https://diagprog5-updater.hugoneitor05cod3.workers.dev/version"\n)\n\nctk.CTkSwitch(\n    panel_actualizaciones_admin,\n    text="Activar comprobación de actualizaciones",\n    variable=var_updates_activo,\n    progress_color=COLOR_ACENTO\n).pack(\n    anchor="w",\n    padx=16,\n    pady=(2, 8)\n)\n\nctk.CTkLabel(\n    panel_actualizaciones_admin,\n    text="URL del manifest de actualizaciones",\n    font=("Segoe UI", 10, "bold")\n).pack(\n    anchor="w",\n    padx=16\n)\n\nctk.CTkLabel(\n    panel_actualizaciones_admin,\n    text="Servidor oficial: https://diagprog5-updater.hugoneitor05cod3.workers.dev/version",\n    font=("Segoe UI", 9),\n    text_color="#46d47a"\n).pack(\n    anchor="w",\n    padx=16,\n    pady=(2, 4)\n)\n\nctk.CTkEntry(\n    panel_actualizaciones_admin,\n    textvariable=var_updates_url,\n    placeholder_text="https://.../diagprog5-update.json",\n    height=34\n).pack(\n    fill="x",\n    padx=16,\n    pady=(4, 10)\n)\n\n\ndef guardar_config_updates_ui():\n    cfg = cargar_config_actualizaciones()\n\n    cfg.update({\n        "activo": bool(\n            var_updates_activo.get()\n        ),\n        "manifest_url": str(\n            var_updates_url.get()\n        ).strip(),\n    })\n\n    guardar_config_actualizaciones(\n        cfg\n    )\n\n    estado.configure(\n        text="✅ Configuración de actualizaciones guardada."\n    )\n\n\nfila_updates = ctk.CTkFrame(\n    panel_actualizaciones_admin,\n    fg_color="transparent"\n)\n\nfila_updates.pack(\n    fill="x",\n    padx=12,\n    pady=(0, 14)\n)\n\nctk.CTkButton(\n    fila_updates,\n    text="Guardar",\n    command=guardar_config_updates_ui,\n    height=34,\n    fg_color="#235d3a",\n    hover_color="#2d7549"\n).pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=4\n)\n\nctk.CTkButton(\n    fila_updates,\n    text="Comprobar actualización",\n    command=comprobar_actualizacion_admin,\n    height=34,\n    fg_color="#315d83",\n    hover_color="#3c719d"\n).pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=4\n)\n\nctk.CTkButton(\n    fila_updates,\n    text="Descargar e instalar",\n    command=instalar_actualizacion_admin,\n    height=34,\n    fg_color=COLOR_ACENTO,\n    hover_color="#d91624"\n).pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=4\n)\n\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Configuración general",\n    font=("Segoe UI", 22, "bold")\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(20, 6)\n)\npanel_licencia = ctk.CTkFrame(\n    panel_config_general,\n    fg_color=COLOR_PANEL_2,\n    corner_radius=12,\n    border_width=1,\n    border_color=COLOR_BORDE\n)\npanel_licencia.pack(\n    fill="x",\n    padx=22,\n    pady=(6, 14)\n)\n\nctk.CTkLabel(\n    panel_licencia,\n    text="Licencia DIAGPROG5",\n    font=("Segoe UI", 16, "bold")\n).pack(\n    anchor="w",\n    padx=14,\n    pady=(12, 4)\n)\n\netiqueta_licencia_estado = ctk.CTkLabel(\n    panel_licencia,\n    text="Comprobando licencia...",\n    font=("Segoe UI", 11),\n    text_color="#46d47a",\n    wraplength=760,\n    justify="left"\n)\netiqueta_licencia_estado.pack(\n    anchor="w",\n    padx=14,\n    pady=(0, 8)\n)\n\nfila_licencia_botones = ctk.CTkFrame(\n    panel_licencia,\n    fg_color="transparent"\n)\nfila_licencia_botones.pack(\n    fill="x",\n    padx=14,\n    pady=(0, 12)\n)\n\nctk.CTkButton(\n    fila_licencia_botones,\n    text="Ver licencia",\n    command=mostrar_info_licencia,\n    height=32,\n    fg_color="#235d3a",\n    hover_color="#2d7549"\n).pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=(0, 4)\n)\n\nctk.CTkButton(\n    fila_licencia_botones,\n    text="Comprobar online",\n    command=comprobar_estado_online_manual,\n    height=32,\n    fg_color=COLOR_PANEL,\n    hover_color="#252b32"\n).pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=4\n)\n\nctk.CTkButton(\n    fila_licencia_botones,\n    text="Ver funciones del plan",\n    command=mostrar_capacidades_plan,\n    height=32,\n    fg_color=COLOR_PANEL,\n    hover_color="#252b32"\n).pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=4\n)\n\nctk.CTkButton(\n    fila_licencia_botones,\n    text="Copiar código PC",\n    command=copiar_codigo_equipo,\n    height=32,\n    fg_color=COLOR_PANEL,\n    hover_color="#252b32"\n).pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=(4, 0)\n)\n\n\nctk.CTkLabel(\n    panel_config_general,\n    text=(\n        "Estos ajustes se guardan automáticamente para las próximas sesiones."\n    ),\n    font=("Segoe UI", 12),\n    text_color=COLOR_TEXTO_SEC\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(0, 18)\n)\n\n# Pausa entre publicaciones\nctk.CTkLabel(\n    panel_config_general,\n    text="Pausa entre publicaciones (segundos)",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_pausa_publicaciones,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 14)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Pausa aleatoria mínima entre publicaciones (segundos)",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_pausa_min_publicaciones,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 14)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Pausa aleatoria máxima entre publicaciones (segundos)",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_pausa_max_publicaciones,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 14)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Detener cola tras X errores consecutivos",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_max_errores_consecutivos,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 14)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Espera después de un error (segundos)",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_espera_despues_error,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 14)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Máximo de publicaciones por sesión",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_max_publicaciones_sesion,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 14)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Watchdog sin avance (minutos)",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_watchdog_minutos,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 14)\n)\n\n# ===== RENDIMIENTO =====\nctk.CTkLabel(\n    panel_config_general,\n    text="Motor de rendimiento",\n    font=("Segoe UI", 18, "bold")\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(10, 8)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text=(\n        "Modo cola estable: cada anuncio abre una pestaña nueva de Wallapop "\n        "y esa pestaña se cierra al terminar o al fallar."\n    ),\n    font=("Segoe UI", 11),\n    text_color="#46d47a",\n    wraplength=760,\n    justify="left"\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(0, 10)\n)\n\nctk.CTkSwitch(\n    panel_config_general,\n    text="Activar optimización automática",\n    variable=var_modo_rendimiento,\n    progress_color=COLOR_RETRO_ROJO\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(4, 14)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Limpiar pestañas antiguas cada X anuncios",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_limpiar_pestanas_cada,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 14)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Liberar memoria cada X anuncios",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_limpiar_memoria_cada,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 14)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Timeout de respuesta de Wallapop (segundos)",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_timeout_respuesta_wallapop,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 14)\n)\n\nctk.CTkSwitch(\n    panel_config_general,\n    text="Recuperar navegador automáticamente si se bloquea",\n    variable=var_auto_recuperar_navegador,\n    progress_color=COLOR_RETRO_ROJO\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(4, 14)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Máximo de recuperaciones por anuncio",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_max_recuperaciones_anuncio,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 14)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Espera antes de recuperar navegador (segundos)",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_espera_recuperacion_navegador,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 14)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Supervisor automático",\n    font=("Segoe UI", 18, "bold")\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(8, 8)\n)\n\nctk.CTkSwitch(\n    panel_config_general,\n    text="Comprobar salud antes de cada anuncio",\n    variable=var_supervisor_automatico,\n    progress_color=COLOR_RETRO_ROJO\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(4, 10)\n)\n\nctk.CTkSwitch(\n    panel_config_general,\n    text="Detener cola si el supervisor no puede recuperar Wallapop",\n    variable=var_pausar_si_supervisor_falla,\n    progress_color=COLOR_RETRO_ROJO\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(4, 10)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Intentos del supervisor antes de detener",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_max_intentos_supervisor,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 18)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Estrategia de publicación",\n    font=("Segoe UI", 18, "bold")\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(10, 8)\n)\n\nfila_estrategia = ctk.CTkFrame(\n    panel_config_general,\n    fg_color="transparent"\n)\nfila_estrategia.pack(\n    fill="x",\n    padx=22,\n    pady=(0, 6)\n)\n\nmenu_estrategia = ctk.CTkOptionMenu(\n    fila_estrategia,\n    variable=var_estrategia_publicacion,\n    values=[\n        "Conservador",\n        "Normal",\n        "Rápido",\n        "Personalizado"\n    ],\n    command=lambda v: aplicar_estrategia_publicacion(v, guardar=False),\n    height=36,\n    fg_color=COLOR_PANEL_2,\n    button_color=COLOR_ACENTO,\n    button_hover_color="#d91624"\n)\nmenu_estrategia.pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=(0, 6)\n)\n\nctk.CTkButton(\n    fila_estrategia,\n    text="Aplicar estrategia",\n    command=lambda: aplicar_estrategia_publicacion(\n        var_estrategia_publicacion.get(),\n        guardar=True\n    ),\n    width=150,\n    height=36,\n    fg_color="#235d3a",\n    hover_color="#2d7549"\n).pack(side="left")\n\netiqueta_estrategia_desc = ctk.CTkLabel(\n    panel_config_general,\n    text="Equilibrio entre velocidad y estabilidad.",\n    font=("Segoe UI", 11),\n    text_color=COLOR_TEXTO_SEC,\n    wraplength=760,\n    justify="left"\n)\netiqueta_estrategia_desc.pack(\n    anchor="w",\n    padx=22,\n    pady=(0, 14)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Ritmo adaptativo",\n    font=("Segoe UI", 18, "bold")\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(10, 8)\n)\n\nctk.CTkSwitch(\n    panel_config_general,\n    text="Ajustar pausas automáticamente según resultados",\n    variable=var_ritmo_adaptativo,\n    progress_color=COLOR_RETRO_ROJO\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(4, 10)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Éxitos consecutivos para acelerar",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_exitos_para_acelerar,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 14)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Errores consecutivos para frenar",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_errores_para_frenar,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 14)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Ajuste máximo de pausa (segundos)",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_max_ajuste_pausa,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 18)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Control operativo",\n    font=("Segoe UI", 18, "bold")\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(10, 8)\n)\n\nctk.CTkSwitch(\n    panel_config_general,\n    text="Modo prueba: rellenar anuncio sin publicar",\n    variable=var_modo_prueba,\n    progress_color=COLOR_RETRO_ROJO\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(4, 10)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Límite máximo de publicaciones al día",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_limite_publicaciones_dia,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 14)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Hora de inicio programada (HH:MM, vacío = inmediato)",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_hora_inicio_programada,\n    placeholder_text="Ejemplo: 09:30",\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 14)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Días de retención de logs/errores/exportaciones",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_dias_retencion_archivos,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 18)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Anti-duplicado visual",\n    font=("Segoe UI", 18, "bold")\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(10, 8)\n)\n\nctk.CTkSwitch(\n    panel_config_general,\n    text="Evitar imágenes visualmente parecidas",\n    variable=var_anti_duplicado_visual,\n    progress_color=COLOR_RETRO_ROJO\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(4, 10)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Umbral de similitud visual (0-64, recomendado 10)",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_umbral_similitud_visual,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 14)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Máximo de regeneraciones si la imagen se parece",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_max_regeneraciones_visuales,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 18)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Auditor de cola",\n    font=("Segoe UI", 18, "bold")\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(10, 8)\n)\n\nctk.CTkSwitch(\n    panel_config_general,\n    text="Auditar automáticamente antes de publicar",\n    variable=var_auditoria_antes_de_publicar,\n    progress_color=COLOR_RETRO_ROJO\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(4, 10)\n)\n\nctk.CTkSwitch(\n    panel_config_general,\n    text="Intentar autorreparar durante la auditoría",\n    variable=var_autorreparar_auditoria,\n    progress_color=COLOR_RETRO_ROJO\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(4, 10)\n)\n\nctk.CTkSwitch(\n    panel_config_general,\n    text="Bloquear inicio de cola si hay errores críticos",\n    variable=var_bloquear_si_hay_criticos,\n    progress_color=COLOR_RETRO_ROJO\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(4, 10)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Calidad mínima para publicar (0-100)",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_calidad_minima_publicar,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 18)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Cuarentena inteligente",\n    font=("Segoe UI", 18, "bold")\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(10, 8)\n)\n\nctk.CTkSwitch(\n    panel_config_general,\n    text="Aislar automáticamente anuncios con fallos repetidos",\n    variable=var_cuarentena_automatica,\n    progress_color=COLOR_RETRO_ROJO\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(4, 10)\n)\n\nctk.CTkSwitch(\n    panel_config_general,\n    text="Continuar con el resto de la cola tras aislar un anuncio",\n    variable=var_continuar_tras_cuarentena,\n    progress_color=COLOR_RETRO_ROJO\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(4, 10)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Mismo error repetido X veces antes de aislar",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_fallos_repetidos_para_cuarentena,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 18)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Productividad y cierre de sesión",\n    font=("Segoe UI", 18, "bold")\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(10, 8)\n)\n\nctk.CTkSwitch(\n    panel_config_general,\n    text="Autoarchivar PUBLICADOS al terminar la cola",\n    variable=var_autoarchivar_publicados_fin,\n    progress_color=COLOR_RETRO_ROJO\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(4, 10)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Objetivo de publicaciones por sesión",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_objetivo_publicaciones_sesion,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 18)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Lotes y prevención de similitud",\n    font=("Segoe UI", 18, "bold")\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(10, 8)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Umbral de similitud textual (50-100, recomendado 88)",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_umbral_similitud_textual,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 14)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Autoguardado de borrador cada X segundos",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_autoguardado_borrador_segundos,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 18)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Sesión y registro de actividad",\n    font=("Segoe UI", 18, "bold")\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(10, 8)\n)\n\nctk.CTkLabel(\n    panel_config_general,\n    text="Nombre o etiqueta de esta sesión",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_nombre_sesion,\n    placeholder_text="Ejemplo: Lote septiembre",\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 10)\n)\n\nctk.CTkSwitch(\n    panel_config_general,\n    text="Guardar registro de actividad del programa",\n    variable=var_registro_actividad,\n    progress_color=COLOR_RETRO_ROJO\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(4, 18)\n)\n\n# Máximo de cola\nctk.CTkLabel(\n    panel_config_general,\n    text="Máximo de anuncios en cola",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_max_cola,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 14)\n)\n\n# Puerto Chromium\nctk.CTkLabel(\n    panel_config_general,\n    text="Puerto de Chromium",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_puerto_chromium,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 14)\n)\n\n# Categoría por defecto\nctk.CTkLabel(\n    panel_config_general,\n    text="Categoría por defecto",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkEntry(\n    panel_config_general,\n    textvariable=var_categoria_defecto,\n    height=38\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 14)\n)\n\n# Estado por defecto\nctk.CTkLabel(\n    panel_config_general,\n    text="Estado por defecto",\n    font=("Segoe UI", 14, "bold")\n).pack(\n    anchor="w",\n    padx=22\n)\n\nctk.CTkOptionMenu(\n    panel_config_general,\n    variable=var_estado_defecto,\n    values=[\n        "Nuevo",\n        "Como nuevo",\n        "En buen estado",\n        "En condiciones aceptables"\n    ],\n    height=38,\n    fg_color=COLOR_ACENTO,\n    button_color=COLOR_ACENTO,\n    button_hover_color="#d91624"\n).pack(\n    fill="x",\n    padx=22,\n    pady=(6, 14)\n)\n\n# Envío por defecto\nctk.CTkSwitch(\n    panel_config_general,\n    text="Desactivar envío por defecto",\n    variable=var_envio_defecto,\n    progress_color=COLOR_RETRO_ROJO\n).pack(\n    anchor="w",\n    padx=22,\n    pady=(4, 18)\n)\n\nfila_config_botones = ctk.CTkFrame(\n    panel_config_general,\n    fg_color="transparent"\n)\nfila_config_botones.pack(\n    fill="x",\n    padx=22,\n    pady=(0, 22)\n)\n\nctk.CTkButton(\n    fila_config_botones,\n    text="GUARDAR CONFIGURACIÓN",\n    command=guardar_configuracion_general,\n    height=42,\n    font=("Segoe UI", 13, "bold"),\n    fg_color=COLOR_ACENTO,\n    hover_color="#d91624"\n).pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=(0, 5)\n)\n\nctk.CTkButton(\n    fila_config_botones,\n    text="Restaurar config buena",\n    command=restaurar_configuracion_buena,\n    height=42,\n    fg_color="#9b6518",\n    hover_color="#b9781e"\n).pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=5\n)\n\nctk.CTkButton(\n    fila_config_botones,\n    text="Preparar EXE",\n    command=preparar_build_exe,\n    height=42,\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32"\n).pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=5\n)\n\nctk.CTkButton(\n    fila_config_botones,\n    text="Restaurar valores",\n    command=restaurar_configuracion_defecto,\n    height=42,\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32"\n).pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=(5, 0)\n)\n\n# =========================================================\n# TAB 8 · BACKUPS\n# =========================================================\n\nbackup_panel = ctk.CTkFrame(\n    tab_backups,\n    fg_color=COLOR_PANEL,\n    corner_radius=14,\n    border_width=1,\n    border_color=COLOR_BORDE\n)\nbackup_panel.pack(\n    fill="both",\n    expand=True,\n    padx=10,\n    pady=10\n)\n\ncab_backup = ctk.CTkFrame(\n    backup_panel,\n    fg_color="transparent"\n)\ncab_backup.pack(\n    fill="x",\n    padx=20,\n    pady=(18, 10)\n)\n\nctk.CTkLabel(\n    cab_backup,\n    text="Copias de seguridad",\n    font=("Segoe UI", 22, "bold")\n).pack(side="left")\n\nctk.CTkButton(\n    cab_backup,\n    text="Abrir carpeta",\n    command=abrir_carpeta_backups,\n    width=105,\n    height=32,\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32"\n).pack(side="right", padx=(6, 0))\n\nctk.CTkButton(\n    cab_backup,\n    text="Actualizar",\n    command=refrescar_lista_backups,\n    width=95,\n    height=32,\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32"\n).pack(side="right", padx=6)\n\nctk.CTkLabel(\n    backup_panel,\n    text=(\n        "Guarda configuración, plantillas, cola, estadísticas, borrador "\n        "y registro de publicados. No duplica las carpetas pesadas de imágenes."\n    ),\n    font=("Segoe UI", 12),\n    text_color=COLOR_TEXTO_SEC,\n    wraplength=1000,\n    justify="left"\n).pack(\n    anchor="w",\n    padx=20,\n    pady=(0, 14)\n)\n\nfila_backup_acciones = ctk.CTkFrame(\n    backup_panel,\n    fg_color="transparent"\n)\nfila_backup_acciones.pack(\n    fill="x",\n    padx=20,\n    pady=(0, 14)\n)\n\nctk.CTkButton(\n    fila_backup_acciones,\n    text="CREAR BACKUP AHORA",\n    command=lambda: (\n        crear_backup(manual=True),\n        refrescar_lista_backups()\n    ),\n    height=42,\n    font=("Segoe UI", 13, "bold"),\n    fg_color=COLOR_ACENTO,\n    hover_color="#d91624"\n).pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=(0, 6)\n)\n\nctk.CTkButton(\n    fila_backup_acciones,\n    text="Restaurar el más reciente",\n    command=restaurar_backup_mas_reciente,\n    height=42,\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32"\n).pack(\n    side="left",\n    fill="x",\n    expand=True,\n    padx=(6, 0)\n)\n\nlista_backups = ctk.CTkTextbox(\n    backup_panel,\n    font=("Segoe UI", 13),\n    corner_radius=10,\n    border_width=1,\n    border_color=COLOR_BORDE\n)\nlista_backups.pack(\n    fill="both",\n    expand=True,\n    padx=20,\n    pady=(0, 20)\n)\nlista_backups.configure(state="disabled")\n\n# =========================================================\n# TAB 9 · DIAGNÓSTICO\n# =========================================================\n\ndiagnostico_panel = ctk.CTkFrame(\n    tab_diagnostico,\n    fg_color=COLOR_PANEL,\n    corner_radius=14,\n    border_width=1,\n    border_color=COLOR_BORDE\n)\ndiagnostico_panel.pack(\n    fill="both",\n    expand=True,\n    padx=10,\n    pady=10\n)\n\ncab_diag = ctk.CTkFrame(\n    diagnostico_panel,\n    fg_color="transparent"\n)\ncab_diag.pack(\n    fill="x",\n    padx=20,\n    pady=(18, 10)\n)\n\nctk.CTkLabel(\n    cab_diag,\n    text="Diagnóstico general",\n    font=("Segoe UI", 22, "bold")\n).pack(side="left")\n\nctk.CTkButton(\n    cab_diag,\n    text="Detectar cambios Wallapop",\n    command=detectar_cambios_wallapop,\n    width=180,\n    height=38,\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32"\n).pack(side="right", padx=(0, 8))\n\nctk.CTkButton(\n    cab_diag,\n    text="EJECUTAR DIAGNÓSTICO",\n    command=ejecutar_diagnostico_general,\n    width=190,\n    height=38,\n    font=("Segoe UI", 13, "bold"),\n    fg_color=COLOR_ACENTO,\n    hover_color="#d91624"\n).pack(side="right")\n\nctk.CTkLabel(\n    diagnostico_panel,\n    text=(\n        "Comprueba Python, dependencias, Chromium, puerto, sesión de Wallapop, "\n        "carpetas, permisos y archivos de configuración. "\n        "No publica ni modifica anuncios."\n    ),\n    font=("Segoe UI", 12),\n    text_color=COLOR_TEXTO_SEC,\n    wraplength=1000,\n    justify="left"\n).pack(\n    anchor="w",\n    padx=20,\n    pady=(0, 14)\n)\n\ntexto_diagnostico = ctk.CTkTextbox(\n    diagnostico_panel,\n    font=("Segoe UI", 13),\n    corner_radius=10,\n    border_width=1,\n    border_color=COLOR_BORDE\n)\ntexto_diagnostico.pack(\n    fill="both",\n    expand=True,\n    padx=20,\n    pady=(0, 20)\n)\ntexto_diagnostico.insert(\n    "1.0",\n    "Pulsa “EJECUTAR DIAGNÓSTICO” para comprobar el sistema."\n)\ntexto_diagnostico.configure(state="disabled")\n\n# =========================================================\n# TAB 10 · REGISTRO\n# =========================================================\n\nregistro_panel = ctk.CTkFrame(\n    tab_registro,\n    fg_color=COLOR_PANEL,\n    corner_radius=14,\n    border_width=1,\n    border_color=COLOR_BORDE\n)\nregistro_panel.pack(fill="both", expand=True, padx=10, pady=10)\n\ncab_log = ctk.CTkFrame(registro_panel, fg_color="transparent")\ncab_log.pack(fill="x", padx=20, pady=(18, 8))\n\nctk.CTkLabel(\n    cab_log,\n    text="Registro del bot",\n    font=("Segoe UI", 22, "bold")\n).pack(side="left")\n\nctk.CTkButton(\n    cab_log,\n    text="Informe soporte",\n    command=crear_informe_soporte,\n    width=125,\n    height=32,\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32"\n).pack(side="right", padx=(6, 0))\n\nctk.CTkButton(\n    cab_log,\n    text="Abrir logs",\n    command=abrir_carpeta_logs,\n    width=95,\n    height=32,\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32"\n).pack(side="right", padx=6)\n\nctk.CTkButton(\n    cab_log,\n    text="Diagnóstico navegador",\n    command=diagnostico_navegador,\n    width=155,\n    height=32,\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32"\n).pack(side="right", padx=(6, 0))\n\nctk.CTkButton(\n    cab_log,\n    text="Limpiar registro",\n    command=limpiar_log,\n    width=120,\n    height=32,\n    fg_color=COLOR_PANEL_2,\n    hover_color="#252b32"\n).pack(side="right")\n\nlog_texto = ctk.CTkTextbox(\n    registro_panel,\n    font=("Segoe UI", 12),\n    corner_radius=10,\n    border_width=1,\n    border_color=COLOR_BORDE\n)\nlog_texto.pack(\n    fill="both",\n    expand=True,\n    padx=20,\n    pady=(4, 20)\n)\nlog_texto.configure(state="disabled")\n\n# ---------------- BARRA DE ESTADO ----------------\n\nbarra_estado = ctk.CTkFrame(\n    ventana,\n    height=52,\n    corner_radius=0,\n    fg_color="#101a24"\n)\nbarra_estado.pack(fill="x", side="bottom")\nbarra_estado.pack_propagate(False)\n\nestado = ctk.CTkLabel(\n    barra_estado,\n    text="Listo.",\n    font=("Segoe UI", 13),\n    text_color="#d6e2ee"\n)\nestado.pack(side="left", padx=22, pady=14)\n\nctk.CTkLabel(\n    barra_estado,\n    text="DIAGPROG5 · WALLAPOP BOT · V107",\n    font=("Segoe UI", 12),\n    text_color=COLOR_TEXTO_SEC\n).pack(side="right", padx=22)\n\n# Bloqueo de licencia.\n# La edición ADMIN del creador tiene acceso completo local.\nif es_edicion_admin():\n    licencia_actual_info = {\n        "plan": "TALLER",\n        "customer": "CREADOR / ADMIN",\n        "permanent": True,\n        "online": False,\n        "license_id": "ADMIN-LOCAL"\n    }\nelse:\n    if not mostrar_activacion_si_necesaria():\n        sys.exit(0)\n\ntry:\n    etiqueta_licencia_estado.configure(\n        text=(\n            "MODO ADMIN · ACCESO TOTAL"\n            if es_edicion_admin()\n            else resumen_licencia()\n        )\n    )\nexcept Exception:\n    pass\n\naplicar_limites_plan()\n\ntry:\n    actualizar_panel_licencia_cliente()\nexcept Exception:\n    pass\n\n\n# Iniciar registro persistente de la sesión.\niniciar_log_sesion()\nlimpiar_logs_antiguos()\n\n# Iniciar puente local y capturar los prints también en la pestaña Registro.\nservidor_extension = iniciar_servidor_extension()\n\nsys.stdout = SalidaInterfaz(sys.stdout)\n\nprint(\n    "Sesión iniciada:",\n    time.strftime("%d/%m/%Y %H:%M:%S")\n)\n\n# Carga inicial\n\n# =========================================================\n# LICENCIAS ADMIN\n# =========================================================\nif es_edicion_admin():\n    tab_licencias_admin = tabs.add("Licencias")\n\n    admin_scroll = ctk.CTkScrollableFrame(\n        tab_licencias_admin,\n        fg_color=COLOR_FONDO\n    )\n    admin_scroll.pack(\n        fill="both",\n        expand=True,\n        padx=14,\n        pady=14\n    )\n\n    ctk.CTkLabel(\n        admin_scroll,\n        text="Gestión de licencias y clientes",\n        font=("Segoe UI", 24, "bold"),\n        text_color="#ff2438"\n    ).pack(\n        anchor="w",\n        pady=(4, 4)\n    )\n\n    ctk.CTkLabel(\n        admin_scroll,\n        text=(\n            "1) Crea una licencia.  2) Cópiala al cliente.  "\n            "3) Si es online, regístrala.  "\n            "4) Usa el control remoto para bloquear o resetear equipos."\n        ),\n        text_color=COLOR_TEXTO_SEC\n    ).pack(\n        anchor="w",\n        pady=(0, 14)\n    )\n\n    panel_admin = ctk.CTkFrame(\n        admin_scroll,\n        fg_color=COLOR_PANEL,\n        corner_radius=9,\n        border_width=1,\n        border_color=COLOR_BORDE\n    )\n    panel_admin.pack(\n        fill="x",\n        pady=(0, 12)\n    )\n\n    var_admin_cliente = ctk.StringVar(value="")\n    var_admin_machine = ctk.StringVar(value="")\n    var_admin_plan = ctk.StringVar(value="PRO")\n    var_admin_dias = ctk.StringVar(value="365")\n    var_admin_permanente = ctk.BooleanVar(value=False)\n    var_admin_online = ctk.BooleanVar(value=True)\n    var_admin_license_id = ctk.StringVar(value="")\n\n    def _entrada_admin(titulo, variable, placeholder=""):\n        ctk.CTkLabel(\n            panel_admin,\n            text=titulo,\n            font=("Segoe UI", 12, "bold")\n        ).pack(\n            anchor="w",\n            padx=16,\n            pady=(10, 3)\n        )\n\n        ctk.CTkEntry(\n            panel_admin,\n            textvariable=variable,\n            placeholder_text=placeholder,\n            height=36\n        ).pack(\n            fill="x",\n            padx=16\n        )\n\n    _entrada_admin(\n        "Cliente",\n        var_admin_cliente,\n        "Taller / cliente"\n    )\n    _entrada_admin(\n        "Código del PC (solo OFFLINE)",\n        var_admin_machine,\n        "DP5PC-..."\n    )\n\n    fila_plan_admin = ctk.CTkFrame(\n        panel_admin,\n        fg_color="transparent"\n    )\n    fila_plan_admin.pack(\n        fill="x",\n        padx=16,\n        pady=12\n    )\n\n    ctk.CTkOptionMenu(\n        fila_plan_admin,\n        variable=var_admin_plan,\n        values=["BASICA", "PRO", "TALLER", "VITALICIA"],\n        height=36,\n        fg_color=COLOR_PANEL_2,\n        button_color=COLOR_ACENTO\n    ).pack(\n        side="left",\n        fill="x",\n        expand=True,\n        padx=(0, 6)\n    )\n\n    ctk.CTkEntry(\n        fila_plan_admin,\n        textvariable=var_admin_dias,\n        placeholder_text="Días",\n        width=120,\n        height=36\n    ).pack(\n        side="left",\n        padx=(6, 0)\n    )\n\n    ctk.CTkSwitch(\n        panel_admin,\n        text="Permanente",\n        variable=var_admin_permanente,\n        progress_color=COLOR_RETRO_ROJO\n    ).pack(\n        anchor="w",\n        padx=16,\n        pady=4\n    )\n\n    ctk.CTkSwitch(\n        panel_admin,\n        text="ONLINE · revocable y multi-PC",\n        variable=var_admin_online,\n        progress_color=COLOR_RETRO_ROJO\n    ).pack(\n        anchor="w",\n        padx=16,\n        pady=(4, 10)\n    )\n\n    admin_token_box = ctk.CTkTextbox(\n        panel_admin,\n        height=150\n    )\n    admin_token_box.pack(\n        fill="x",\n        padx=16,\n        pady=(4, 8)\n    )\n\n    admin_estado = ctk.CTkLabel(\n        panel_admin,\n        text=(\n            "Clave privada: DISPONIBLE"\n            if clave_privada_admin_disponible()\n            else "Clave privada: NO ENCONTRADA"\n        ),\n        text_color=(\n            "#46d47a"\n            if clave_privada_admin_disponible()\n            else "#ff6b6b"\n        )\n    )\n    admin_estado.pack(\n        anchor="w",\n        padx=16,\n        pady=(0, 8)\n    )\n\n    admin_payload = {"data": None}\n\n    def _admin_generar():\n        try:\n            token, payload = admin_generar_token_licencia(\n                var_admin_machine.get(),\n                var_admin_cliente.get(),\n                var_admin_plan.get(),\n                var_admin_dias.get(),\n                var_admin_permanente.get(),\n                var_admin_online.get()\n            )\n\n            admin_token_box.delete("1.0", "end")\n            admin_token_box.insert("1.0", token)\n            admin_payload["data"] = payload\n\n            admin_estado.configure(\n                text=(\n                    f"✅ {payload[\'license_id\']} · "\n                    f"{payload[\'plan\']} · "\n                    f"{payload[\'max_pcs\']} PC(s)"\n                ),\n                text_color="#46d47a"\n            )\n\n        except Exception as e:\n            admin_estado.configure(\n                text="❌ " + str(e),\n                text_color="#ff6b6b"\n            )\n\n    def _admin_copiar():\n        token = admin_token_box.get("1.0", "end").strip()\n\n        if token:\n            ventana.clipboard_clear()\n            ventana.clipboard_append(token)\n            ventana.update()\n            admin_estado.configure(\n                text="✅ Licencia copiada.",\n                text_color="#46d47a"\n            )\n\n    def _admin_registrar():\n        payload = admin_payload.get("data")\n\n        if not payload:\n            admin_estado.configure(\n                text="Primero genera una licencia.",\n                text_color="#f7c948"\n            )\n            return\n\n        ok, msg = admin_registrar_licencia_online(payload)\n        admin_estado.configure(\n            text=("✅ " if ok else "❌ ") + msg,\n            text_color="#46d47a" if ok else "#ff6b6b"\n        )\n\n    fila_acciones_admin = ctk.CTkFrame(\n        panel_admin,\n        fg_color="transparent"\n    )\n    fila_acciones_admin.pack(\n        fill="x",\n        padx=16,\n        pady=(0, 14)\n    )\n\n    for tx, cmd, fc in [\n        ("GENERAR", _admin_generar, "#d91624"),\n        ("COPIAR", _admin_copiar, COLOR_PANEL_2),\n        ("REGISTRAR ONLINE", _admin_registrar, "#235d3a"),\n    ]:\n        ctk.CTkButton(\n            fila_acciones_admin,\n            text=tx,\n            command=cmd,\n            fg_color=fc,\n            height=40\n        ).pack(\n            side="left",\n            fill="x",\n            expand=True,\n            padx=4\n        )\n\n    panel_control_admin = ctk.CTkFrame(\n        admin_scroll,\n        fg_color=COLOR_PANEL,\n        corner_radius=9,\n        border_width=1,\n        border_color=COLOR_BORDE\n    )\n    panel_control_admin.pack(\n        fill="x"\n    )\n\n    ctk.CTkLabel(\n        panel_control_admin,\n        text="Control remoto de licencia",\n        font=("Segoe UI", 18, "bold")\n    ).pack(\n        anchor="w",\n        padx=16,\n        pady=(14, 8)\n    )\n\n    ctk.CTkEntry(\n        panel_control_admin,\n        textvariable=var_admin_license_id,\n        placeholder_text="DP5-XXXX-XXXX-XXXX",\n        height=36\n    ).pack(\n        fill="x",\n        padx=16,\n        pady=(0, 8)\n    )\n\n    admin_control_estado = ctk.CTkLabel(\n        panel_control_admin,\n        text=""\n    )\n    admin_control_estado.pack(\n        anchor="w",\n        padx=16,\n        pady=(0, 8)\n    )\n\n    def _admin_control(accion):\n        ok, msg = admin_accion_online(\n            accion,\n            var_admin_license_id.get()\n        )\n\n        admin_control_estado.configure(\n            text=("✅ " if ok else "❌ ") + msg,\n            text_color="#46d47a" if ok else "#ff6b6b"\n        )\n\n    fila_control_admin = ctk.CTkFrame(\n        panel_control_admin,\n        fg_color="transparent"\n    )\n    fila_control_admin.pack(\n        fill="x",\n        padx=16,\n        pady=(0, 14)\n    )\n\n    for tx, action, fc in [\n        ("BLOQUEAR", "revocar", "#9b1c27"),\n        ("DESBLOQUEAR", "desrevocar", "#235d3a"),\n        ("RESETEAR PCs", "reset", COLOR_PANEL_2),\n    ]:\n        ctk.CTkButton(\n            fila_control_admin,\n            text=tx,\n            command=lambda a=action: _admin_control(a),\n            fg_color=fc\n        ).pack(\n            side="left",\n            fill="x",\n            expand=True,\n            padx=4\n        )\n\n\ncargar_configuracion_en_interfaz()\naplicar_configuracion_general()\n\ntry:\n    etiqueta_estrategia_desc.configure(\n        text=obtener_descripcion_estrategia()\n    )\nexcept Exception:\n    pass\n\ncargar_perfiles_lote()\nactualizar_menu_perfiles_lote()\n\ncargar_plantillas()\nactualizar_selector_plantillas()\ntry:\n    menu_plantillas.set("Tuning")\nexcept Exception:\n    pass\n\nrefrescar_resumen_publicacion()\n# Recuperación automática de cola tras un cierre inesperado.\ntry:\n    _recuperados_inicio = recuperar_cola_tras_cierre_inesperado()\n    if _recuperados_inicio:\n        print(\n            f"[ADMIN] Recuperados {_recuperados_inicio} anuncio(s) "\n            "que habían quedado en PUBLICANDO."\n        )\nexcept Exception as e:\n    print("[ADMIN] No pude recuperar cola:", e)\n\nrefrescar_historial()\nactualizar_selector_historial()\nrefrescar_estadisticas()\n\n# Comprobación silenciosa del servidor de actualizaciones (V90, no bloqueante).\ndef _v89_comprobar_updates_en_segundo_plano():\n    def _trabajo():\n        try:\n            _update_inicio = comprobar_actualizacion_admin(\n                mostrar=False\n            )\n            if (\n                _update_inicio.get("ok")\n                and _update_inicio.get("disponible")\n            ):\n                print(\n                    "[UPDATER] Nueva versión disponible:",\n                    _update_inicio.get("version_remota", "?")\n                )\n        except Exception as e:\n            print("[UPDATER] No pude comprobar actualizaciones:", e)\n\n    try:\n        threading.Thread(\n            target=_trabajo,\n            daemon=True\n        ).start()\n    except Exception:\n        pass\n\ntry:\n    # V105 instala su comprobador no bloqueante al terminar de crear la UI.\n    pass\nexcept Exception:\n    pass\n\n# Autochequeo del creador al arrancar.\ntry:\n    _integridad_inicio = chequeo_integridad_admin()\n    if not _integridad_inicio["ok"]:\n        print(\n            "[ADMIN] Aviso de integridad:",\n            _integridad_inicio["fallos"]\n        )\nexcept Exception as e:\n    print("[ADMIN] Error en autochequeo:", e)\n\n\n# =========================================================\n# V90 · CAPA DE FLUIDEZ DE INTERFAZ\n# =========================================================\n# Agrupa refrescos repetidos que ocurren casi al mismo tiempo.\ntry:\n    _v89_dashboard_original = refrescar_dashboard\n    _v89_dashboard_after_id = None\n    _v89_dashboard_ultimo = 0.0\n\n    def refrescar_dashboard():\n        global _v89_dashboard_after_id, _v89_dashboard_ultimo\n        ahora = time.monotonic()\n        transcurrido = ahora - _v89_dashboard_ultimo\n\n        if transcurrido >= 0.12:\n            _v89_dashboard_ultimo = ahora\n            _v89_dashboard_after_id = None\n            return _v89_dashboard_original()\n\n        if _v89_dashboard_after_id is None:\n            espera = max(1, int((0.12 - transcurrido) * 1000))\n\n            def _ejecutar_dashboard_v88():\n                global _v89_dashboard_after_id, _v89_dashboard_ultimo\n                _v89_dashboard_after_id = None\n                _v89_dashboard_ultimo = time.monotonic()\n                try:\n                    _v89_dashboard_original()\n                except Exception:\n                    pass\n\n            try:\n                _v89_dashboard_after_id = ventana.after(\n                    espera,\n                    _ejecutar_dashboard_v88\n                )\n            except Exception:\n                return _v89_dashboard_original()\n\nexcept Exception as _e_v89_ui:\n    print("[V90] Capa de fluidez dashboard no aplicada:", _e_v89_ui)\n\n\n\n\n# =========================================================\n# V91 · CAPA DE FLUIDEZ DE INTERFAZ\n# =========================================================\n# Agrupa refrescos repetidos que ocurren casi al mismo tiempo.\ntry:\n    _v89_dashboard_original = refrescar_dashboard\n    _v89_dashboard_after_id = None\n    _v89_dashboard_ultimo = 0.0\n\n    def refrescar_dashboard():\n        global _v89_dashboard_after_id, _v89_dashboard_ultimo\n        ahora = time.monotonic()\n        transcurrido = ahora - _v89_dashboard_ultimo\n\n        if transcurrido >= 0.12:\n            _v89_dashboard_ultimo = ahora\n            _v89_dashboard_after_id = None\n            return _v89_dashboard_original()\n\n        if _v89_dashboard_after_id is None:\n            espera = max(1, int((0.12 - transcurrido) * 1000))\n\n            def _ejecutar_dashboard_v88():\n                global _v89_dashboard_after_id, _v89_dashboard_ultimo\n                _v89_dashboard_after_id = None\n                _v89_dashboard_ultimo = time.monotonic()\n                try:\n                    _v89_dashboard_original()\n                except Exception:\n                    pass\n\n            try:\n                _v89_dashboard_after_id = ventana.after(\n                    espera,\n                    _ejecutar_dashboard_v88\n                )\n            except Exception:\n                return _v89_dashboard_original()\n\nexcept Exception as _e_v89_ui:\n    print("[V91] Capa de fluidez dashboard no aplicada:", _e_v89_ui)\n\n\n\n\n# =========================================================\n# V92 · CAPA DE FLUIDEZ DE INTERFAZ\n# =========================================================\n# Agrupa refrescos repetidos que ocurren casi al mismo tiempo.\ntry:\n    _v89_dashboard_original = refrescar_dashboard\n    _v89_dashboard_after_id = None\n    _v89_dashboard_ultimo = 0.0\n\n    def refrescar_dashboard():\n        global _v89_dashboard_after_id, _v89_dashboard_ultimo\n        ahora = time.monotonic()\n        transcurrido = ahora - _v89_dashboard_ultimo\n\n        if transcurrido >= 0.12:\n            _v89_dashboard_ultimo = ahora\n            _v89_dashboard_after_id = None\n            return _v89_dashboard_original()\n\n        if _v89_dashboard_after_id is None:\n            espera = max(1, int((0.12 - transcurrido) * 1000))\n\n            def _ejecutar_dashboard_v88():\n                global _v89_dashboard_after_id, _v89_dashboard_ultimo\n                _v89_dashboard_after_id = None\n                _v89_dashboard_ultimo = time.monotonic()\n                try:\n                    _v89_dashboard_original()\n                except Exception:\n                    pass\n\n            try:\n                _v89_dashboard_after_id = ventana.after(\n                    espera,\n                    _ejecutar_dashboard_v88\n                )\n            except Exception:\n                return _v89_dashboard_original()\n\nexcept Exception as _e_v89_ui:\n    print("[V92] Capa de fluidez dashboard no aplicada:", _e_v89_ui)\n\n\n\n\n# =========================================================\n# V93 · CAPA DE FLUIDEZ DE INTERFAZ\n# =========================================================\n# Agrupa refrescos repetidos que ocurren casi al mismo tiempo.\ntry:\n    _v89_dashboard_original = refrescar_dashboard\n    _v89_dashboard_after_id = None\n    _v89_dashboard_ultimo = 0.0\n\n    def refrescar_dashboard():\n        global _v89_dashboard_after_id, _v89_dashboard_ultimo\n        ahora = time.monotonic()\n        transcurrido = ahora - _v89_dashboard_ultimo\n\n        if transcurrido >= 0.12:\n            _v89_dashboard_ultimo = ahora\n            _v89_dashboard_after_id = None\n            return _v89_dashboard_original()\n\n        if _v89_dashboard_after_id is None:\n            espera = max(1, int((0.12 - transcurrido) * 1000))\n\n            def _ejecutar_dashboard_v88():\n                global _v89_dashboard_after_id, _v89_dashboard_ultimo\n                _v89_dashboard_after_id = None\n                _v89_dashboard_ultimo = time.monotonic()\n                try:\n                    _v89_dashboard_original()\n                except Exception:\n                    pass\n\n            try:\n                _v89_dashboard_after_id = ventana.after(\n                    espera,\n                    _ejecutar_dashboard_v88\n                )\n            except Exception:\n                return _v89_dashboard_original()\n\nexcept Exception as _e_v89_ui:\n    print("[V93] Capa de fluidez dashboard no aplicada:", _e_v89_ui)\n\n\n\n\n# =========================================================\n# V94 · CAPA DE FLUIDEZ DE INTERFAZ\n# =========================================================\n# Agrupa refrescos repetidos que ocurren casi al mismo tiempo.\ntry:\n    _v89_dashboard_original = refrescar_dashboard\n    _v89_dashboard_after_id = None\n    _v89_dashboard_ultimo = 0.0\n\n    def refrescar_dashboard():\n        global _v89_dashboard_after_id, _v89_dashboard_ultimo\n        ahora = time.monotonic()\n        transcurrido = ahora - _v89_dashboard_ultimo\n\n        if transcurrido >= 0.12:\n            _v89_dashboard_ultimo = ahora\n            _v89_dashboard_after_id = None\n            return _v89_dashboard_original()\n\n        if _v89_dashboard_after_id is None:\n            espera = max(1, int((0.12 - transcurrido) * 1000))\n\n            def _ejecutar_dashboard_v88():\n                global _v89_dashboard_after_id, _v89_dashboard_ultimo\n                _v89_dashboard_after_id = None\n                _v89_dashboard_ultimo = time.monotonic()\n                try:\n                    _v89_dashboard_original()\n                except Exception:\n                    pass\n\n            try:\n                _v89_dashboard_after_id = ventana.after(\n                    espera,\n                    _ejecutar_dashboard_v88\n                )\n            except Exception:\n                return _v89_dashboard_original()\n\nexcept Exception as _e_v89_ui:\n    print("[V94] Capa de fluidez dashboard no aplicada:", _e_v89_ui)\n\n\n\n\n# =========================================================\n# V95 · CAPA DE FLUIDEZ DE INTERFAZ\n# =========================================================\n# Agrupa refrescos repetidos que ocurren casi al mismo tiempo.\ntry:\n    _v89_dashboard_original = refrescar_dashboard\n    _v89_dashboard_after_id = None\n    _v89_dashboard_ultimo = 0.0\n\n    def refrescar_dashboard():\n        global _v89_dashboard_after_id, _v89_dashboard_ultimo\n        ahora = time.monotonic()\n        transcurrido = ahora - _v89_dashboard_ultimo\n\n        if transcurrido >= 0.12:\n            _v89_dashboard_ultimo = ahora\n            _v89_dashboard_after_id = None\n            return _v89_dashboard_original()\n\n        if _v89_dashboard_after_id is None:\n            espera = max(1, int((0.12 - transcurrido) * 1000))\n\n            def _ejecutar_dashboard_v88():\n                global _v89_dashboard_after_id, _v89_dashboard_ultimo\n                _v89_dashboard_after_id = None\n                _v89_dashboard_ultimo = time.monotonic()\n                try:\n                    _v89_dashboard_original()\n                except Exception:\n                    pass\n\n            try:\n                _v89_dashboard_after_id = ventana.after(\n                    espera,\n                    _ejecutar_dashboard_v88\n                )\n            except Exception:\n                return _v89_dashboard_original()\n\nexcept Exception as _e_v89_ui:\n    print("[V95] Capa de fluidez dashboard no aplicada:", _e_v89_ui)\n\n\n\n\n# =========================================================\n# V96 · CAPA DE FLUIDEZ DE INTERFAZ\n# =========================================================\n# Agrupa refrescos repetidos que ocurren casi al mismo tiempo.\ntry:\n    _v89_dashboard_original = refrescar_dashboard\n    _v89_dashboard_after_id = None\n    _v89_dashboard_ultimo = 0.0\n\n    def refrescar_dashboard():\n        global _v89_dashboard_after_id, _v89_dashboard_ultimo\n        ahora = time.monotonic()\n        transcurrido = ahora - _v89_dashboard_ultimo\n\n        if transcurrido >= 0.12:\n            _v89_dashboard_ultimo = ahora\n            _v89_dashboard_after_id = None\n            return _v89_dashboard_original()\n\n        if _v89_dashboard_after_id is None:\n            espera = max(1, int((0.12 - transcurrido) * 1000))\n\n            def _ejecutar_dashboard_v88():\n                global _v89_dashboard_after_id, _v89_dashboard_ultimo\n                _v89_dashboard_after_id = None\n                _v89_dashboard_ultimo = time.monotonic()\n                try:\n                    _v89_dashboard_original()\n                except Exception:\n                    pass\n\n            try:\n                _v89_dashboard_after_id = ventana.after(\n                    espera,\n                    _ejecutar_dashboard_v88\n                )\n            except Exception:\n                return _v89_dashboard_original()\n\nexcept Exception as _e_v89_ui:\n    print("[V96] Capa de fluidez dashboard no aplicada:", _e_v89_ui)\n\n\n\n\n# =========================================================\n# V97 · CAPA DE FLUIDEZ DE INTERFAZ\n# =========================================================\n# Agrupa refrescos repetidos que ocurren casi al mismo tiempo.\ntry:\n    _v89_dashboard_original = refrescar_dashboard\n    _v89_dashboard_after_id = None\n    _v89_dashboard_ultimo = 0.0\n\n    def refrescar_dashboard():\n        global _v89_dashboard_after_id, _v89_dashboard_ultimo\n        ahora = time.monotonic()\n        transcurrido = ahora - _v89_dashboard_ultimo\n\n        if transcurrido >= 0.12:\n            _v89_dashboard_ultimo = ahora\n            _v89_dashboard_after_id = None\n            return _v89_dashboard_original()\n\n        if _v89_dashboard_after_id is None:\n            espera = max(1, int((0.12 - transcurrido) * 1000))\n\n            def _ejecutar_dashboard_v88():\n                global _v89_dashboard_after_id, _v89_dashboard_ultimo\n                _v89_dashboard_after_id = None\n                _v89_dashboard_ultimo = time.monotonic()\n                try:\n                    _v89_dashboard_original()\n                except Exception:\n                    pass\n\n            try:\n                _v89_dashboard_after_id = ventana.after(\n                    espera,\n                    _ejecutar_dashboard_v88\n                )\n            except Exception:\n                return _v89_dashboard_original()\n\nexcept Exception as _e_v89_ui:\n    print("[V97] Capa de fluidez dashboard no aplicada:", _e_v89_ui)\n\n\n\n\n# =========================================================\n# V98 · CAPA DE FLUIDEZ DE INTERFAZ\n# =========================================================\n# Agrupa refrescos repetidos que ocurren casi al mismo tiempo.\ntry:\n    _v89_dashboard_original = refrescar_dashboard\n    _v89_dashboard_after_id = None\n    _v89_dashboard_ultimo = 0.0\n\n    def refrescar_dashboard():\n        global _v89_dashboard_after_id, _v89_dashboard_ultimo\n        ahora = time.monotonic()\n        transcurrido = ahora - _v89_dashboard_ultimo\n\n        if transcurrido >= 0.12:\n            _v89_dashboard_ultimo = ahora\n            _v89_dashboard_after_id = None\n            return _v89_dashboard_original()\n\n        if _v89_dashboard_after_id is None:\n            espera = max(1, int((0.12 - transcurrido) * 1000))\n\n            def _ejecutar_dashboard_v88():\n                global _v89_dashboard_after_id, _v89_dashboard_ultimo\n                _v89_dashboard_after_id = None\n                _v89_dashboard_ultimo = time.monotonic()\n                try:\n                    _v89_dashboard_original()\n                except Exception:\n                    pass\n\n            try:\n                _v89_dashboard_after_id = ventana.after(\n                    espera,\n                    _ejecutar_dashboard_v88\n                )\n            except Exception:\n                return _v89_dashboard_original()\n\nexcept Exception as _e_v89_ui:\n    print("[V98] Capa de fluidez dashboard no aplicada:", _e_v89_ui)\n\n\n\n\n# =========================================================\n# V99 · CAPA DE FLUIDEZ DE INTERFAZ\n# =========================================================\n# Agrupa refrescos repetidos que ocurren casi al mismo tiempo.\ntry:\n    _v89_dashboard_original = refrescar_dashboard\n    _v89_dashboard_after_id = None\n    _v89_dashboard_ultimo = 0.0\n\n    def refrescar_dashboard():\n        global _v89_dashboard_after_id, _v89_dashboard_ultimo\n        ahora = time.monotonic()\n        transcurrido = ahora - _v89_dashboard_ultimo\n\n        if transcurrido >= 0.12:\n            _v89_dashboard_ultimo = ahora\n            _v89_dashboard_after_id = None\n            return _v89_dashboard_original()\n\n        if _v89_dashboard_after_id is None:\n            espera = max(1, int((0.12 - transcurrido) * 1000))\n\n            def _ejecutar_dashboard_v88():\n                global _v89_dashboard_after_id, _v89_dashboard_ultimo\n                _v89_dashboard_after_id = None\n                _v89_dashboard_ultimo = time.monotonic()\n                try:\n                    _v89_dashboard_original()\n                except Exception:\n                    pass\n\n            try:\n                _v89_dashboard_after_id = ventana.after(\n                    espera,\n                    _ejecutar_dashboard_v88\n                )\n            except Exception:\n                return _v89_dashboard_original()\n\nexcept Exception as _e_v89_ui:\n    print("[V99] Capa de fluidez dashboard no aplicada:", _e_v89_ui)\n\n\n\n\n# =========================================================\n# V100 · CAPA DE FLUIDEZ DE INTERFAZ\n# =========================================================\n# Agrupa refrescos repetidos que ocurren casi al mismo tiempo.\ntry:\n    _v89_dashboard_original = refrescar_dashboard\n    _v89_dashboard_after_id = None\n    _v89_dashboard_ultimo = 0.0\n\n    def refrescar_dashboard():\n        global _v89_dashboard_after_id, _v89_dashboard_ultimo\n        ahora = time.monotonic()\n        transcurrido = ahora - _v89_dashboard_ultimo\n\n        if transcurrido >= 0.12:\n            _v89_dashboard_ultimo = ahora\n            _v89_dashboard_after_id = None\n            return _v89_dashboard_original()\n\n        if _v89_dashboard_after_id is None:\n            espera = max(1, int((0.12 - transcurrido) * 1000))\n\n            def _ejecutar_dashboard_v88():\n                global _v89_dashboard_after_id, _v89_dashboard_ultimo\n                _v89_dashboard_after_id = None\n                _v89_dashboard_ultimo = time.monotonic()\n                try:\n                    _v89_dashboard_original()\n                except Exception:\n                    pass\n\n            try:\n                _v89_dashboard_after_id = ventana.after(\n                    espera,\n                    _ejecutar_dashboard_v88\n                )\n            except Exception:\n                return _v89_dashboard_original()\n\nexcept Exception as _e_v89_ui:\n    print("[V100] Capa de fluidez dashboard no aplicada:", _e_v89_ui)\n\n\n\n\n# =========================================================\n# V101 · CAPA DE FLUIDEZ DE INTERFAZ\n# =========================================================\n# Agrupa refrescos repetidos que ocurren casi al mismo tiempo.\ntry:\n    _v89_dashboard_original = refrescar_dashboard\n    _v89_dashboard_after_id = None\n    _v89_dashboard_ultimo = 0.0\n\n    def refrescar_dashboard():\n        global _v89_dashboard_after_id, _v89_dashboard_ultimo\n        ahora = time.monotonic()\n        transcurrido = ahora - _v89_dashboard_ultimo\n\n        if transcurrido >= 0.12:\n            _v89_dashboard_ultimo = ahora\n            _v89_dashboard_after_id = None\n            return _v89_dashboard_original()\n\n        if _v89_dashboard_after_id is None:\n            espera = max(1, int((0.12 - transcurrido) * 1000))\n\n            def _ejecutar_dashboard_v88():\n                global _v89_dashboard_after_id, _v89_dashboard_ultimo\n                _v89_dashboard_after_id = None\n                _v89_dashboard_ultimo = time.monotonic()\n                try:\n                    _v89_dashboard_original()\n                except Exception:\n                    pass\n\n            try:\n                _v89_dashboard_after_id = ventana.after(\n                    espera,\n                    _ejecutar_dashboard_v88\n                )\n            except Exception:\n                return _v89_dashboard_original()\n\nexcept Exception as _e_v89_ui:\n    print("[V101] Capa de fluidez dashboard no aplicada:", _e_v89_ui)\n\n\n\n\n# =========================================================\n# V102 · CAPA DE FLUIDEZ DE INTERFAZ\n# =========================================================\n# Agrupa refrescos repetidos que ocurren casi al mismo tiempo.\ntry:\n    _v89_dashboard_original = refrescar_dashboard\n    _v89_dashboard_after_id = None\n    _v89_dashboard_ultimo = 0.0\n\n    def refrescar_dashboard():\n        global _v89_dashboard_after_id, _v89_dashboard_ultimo\n        ahora = time.monotonic()\n        transcurrido = ahora - _v89_dashboard_ultimo\n\n        if transcurrido >= 0.12:\n            _v89_dashboard_ultimo = ahora\n            _v89_dashboard_after_id = None\n            return _v89_dashboard_original()\n\n        if _v89_dashboard_after_id is None:\n            espera = max(1, int((0.12 - transcurrido) * 1000))\n\n            def _ejecutar_dashboard_v88():\n                global _v89_dashboard_after_id, _v89_dashboard_ultimo\n                _v89_dashboard_after_id = None\n                _v89_dashboard_ultimo = time.monotonic()\n                try:\n                    _v89_dashboard_original()\n                except Exception:\n                    pass\n\n            try:\n                _v89_dashboard_after_id = ventana.after(\n                    espera,\n                    _ejecutar_dashboard_v88\n                )\n            except Exception:\n                return _v89_dashboard_original()\n\nexcept Exception as _e_v89_ui:\n    print("[V102] Capa de fluidez dashboard no aplicada:", _e_v89_ui)\n\n\nactualizar_menu_lotes()\n\ntry:\n    ventana.after(\n        5000,\n        _programar_autoguardado_borrador\n    )\nexcept Exception:\n    pass\nrefrescar_lista_backups()\nrefrescar_dashboard()\n\ndef refresco_periodico_dashboard():\n    try:\n        refrescar_dashboard()\n    except Exception:\n        pass\n\n    try:\n        ventana.after(10000, refresco_periodico_dashboard)\n    except Exception:\n        pass\n\n\nventana.after(500, comprobar_navegador)\nventana.after(900, recuperar_borrador_al_iniciar)\nventana.after(1400, backup_automatico_al_iniciar)\nventana.after(1800, refresco_periodico_dashboard)\n\ndef cerrar_aplicacion():\n    try:\n        limpiar_checkpoint_al_cerrar_correctamente()\n    except Exception:\n        pass\n\n    try:\n        print(\n            "Cerrando sesión:",\n            time.strftime("%d/%m/%Y %H:%M:%S")\n        )\n    except Exception:\n        pass\n\n    try:\n        guardar_borrador(silencioso=True)\n    except Exception:\n        pass\n\n    try:\n        guardar_cola_en_disco()\n    except Exception:\n        pass\n\n    try:\n        crear_backup(manual=False)\n    except Exception:\n        pass\n\n    try:\n        ventana.destroy()\n    except Exception:\n        pass\n\n\nventana.protocol("WM_DELETE_WINDOW", cerrar_aplicacion)\n\n\n# =========================================================\n# V102 · DIAGNÓSTICO RUNTIME GARANTIZADO\n# =========================================================\n# Intercepta únicamente el diagnóstico de rotación y muestra una ventana\n# desplazable con TODO lo necesario para depurar en una sola captura.\n\n_DIAG_V95_SHOWINFO_ORIGINAL = messagebox.showinfo\n\n\ndef _v102_serializar_seguro(valor):\n    try:\n        if isinstance(\n            valor,\n            (\n                dict,\n                list,\n                tuple,\n            )\n        ):\n            return json.dumps(\n                valor,\n                ensure_ascii=False,\n                indent=2,\n                default=str\n            )\n        return str(\n            valor\n        )\n    except Exception:\n        try:\n            return repr(\n                valor\n            )\n        except Exception:\n            return "(no serializable)"\n\n\ndef _v102_texto_cola_completo():\n    lineas = []\n\n    try:\n        items = list(\n            cola_anuncios\n        )\n    except Exception:\n        items = []\n\n    lineas.append(\n        "COLA COMPLETA: "\n        + str(\n            len(\n                items\n            )\n        )\n        + " elemento(s)"\n    )\n\n    for i, item in enumerate(\n        items,\n        1\n    ):\n        if not isinstance(\n            item,\n            dict\n        ):\n            lineas.append(\n                "#"\n                + str(i)\n                + " · "\n                + repr(\n                    item\n                )\n            )\n            continue\n\n        lineas.append("")\n        lineas.append(\n            "#"\n            + str(i)\n            + " · "\n            + str(\n                item.get(\n                    "estado_cola",\n                    item.get(\n                        "estado",\n                        ""\n                    )\n                )\n            )\n            + " · "\n            + str(\n                item.get(\n                    "titulo",\n                    item.get(\n                        "title",\n                        ""\n                    )\n                )\n            )\n        )\n\n        for clave, valor in item.items():\n            nombre = str(\n                clave\n            ).lower()\n\n            if (\n                "error" in nombre\n                or "rotacion" in nombre\n                or "detalle" in nombre\n                or "resultado" in nombre\n                or "mensaje" in nombre\n                or nombre\n                in (\n                    "estado_cola",\n                    "intentos",\n                )\n            ):\n                lineas.append(\n                    "   "\n                    + str(\n                        clave\n                    )\n                    + " = "\n                    + _v102_serializar_seguro(\n                        valor\n                    ).replace(\n                        "\\n",\n                        " "\n                    )\n                )\n\n    return "\\n".join(\n        lineas\n    )\n\n\ndef _v102_log_rotacion_texto():\n    rutas = [\n        Path(\n            CARPETA_DATOS_USUARIO\n        )\n        / "rotacion_v102.log",\n        Path(\n            CARPETA_DATOS_USUARIO\n        )\n        / "rotacion_v102.log",\n        Path(\n            CARPETA_DATOS_USUARIO\n        )\n        / "rotacion_v90.log",\n    ]\n\n    for ruta in rutas:\n        try:\n            if ruta.exists():\n                lineas = ruta.read_text(\n                    encoding="utf-8",\n                    errors="replace"\n                ).splitlines()\n\n                return (\n                    str(\n                        ruta\n                    )\n                    + "\\n"\n                    + "\\n".join(\n                        lineas[-30:]\n                    )\n                )\n        except Exception:\n            pass\n\n    return "(sin log de rotación todavía)"\n\n\ndef _v102_candidatos_resumen():\n    candidatos = []\n\n    try:\n        funcion = globals().get(\n            "_v102_candidatos_locales"\n        )\n\n        if not callable(\n            funcion\n        ):\n            funcion = globals().get(\n                "_v91_candidatos_locales"\n            )\n\n        if callable(\n            funcion\n        ):\n            candidatos = funcion()\n    except Exception:\n        candidatos = []\n\n    if not candidatos:\n        try:\n            for nombre, funcion in list(\n                globals().items()\n            ):\n                n = str(\n                    nombre\n                ).lower()\n\n                if (\n                    callable(\n                        funcion\n                    )\n                    and "rotacion" in n\n                    and "candidat" in n\n                    and not n.startswith(\n                        "_v102_"\n                    )\n                ):\n                    try:\n                        datos = funcion()\n\n                        if isinstance(\n                            datos,\n                            list\n                        ):\n                            candidatos = datos\n                            break\n                    except Exception:\n                        pass\n        except Exception:\n            pass\n\n    lineas = [\n        "CANDIDATOS: "\n        + str(\n            len(\n                candidatos\n            )\n        )\n    ]\n\n    for candidato in candidatos[:25]:\n        if not isinstance(\n            candidato,\n            dict\n        ):\n            continue\n\n        titulo = str(\n            candidato.get(\n                "titulo",\n                candidato.get(\n                    "title",\n                    "(sin título)"\n                )\n            )\n        )\n\n        url = ""\n\n        for clave in (\n            "url",\n            "url_anuncio",\n            "url_wallapop",\n            "link",\n            "permalink",\n            "web_url",\n        ):\n            valor = str(\n                candidato.get(\n                    clave,\n                    ""\n                )\n                or ""\n            ).strip()\n\n            if valor:\n                url = valor\n                break\n\n        lineas.append(\n            "• "\n            + titulo\n            + " · URL:"\n            + (\n                "SÍ"\n                if url\n                else "NO"\n            )\n        )\n\n    return "\\n".join(\n        lineas\n    )\n\n\ndef _v102_estado_motor():\n    claves = [\n        "_ROTACION_V102_EN_CURSO",\n        "_ROTACION_V91_EN_CURSO",\n        "_ROTACION_V102_ULTIMO",\n        "_ROTACION_V91_ULTIMO",\n    ]\n\n    lineas = []\n\n    for clave in claves:\n        if clave in globals():\n            lineas.append(\n                clave\n                + " = "\n                + _v102_serializar_seguro(\n                    globals().get(\n                        clave\n                    )\n                )\n            )\n\n    return (\n        "\\n".join(\n            lineas\n        )\n        if lineas\n        else "(motor runtime sin variables V91/V92 visibles)"\n    )\n\n\ndef _v102_abrir_informe_rotacion(texto_base=""):\n    try:\n        ventana_diag = ctk.CTkToplevel(\n            ventana\n        )\n\n        ventana_diag.title(\n            "DIAGPROG5 · Diagnóstico total de rotación V95"\n        )\n\n        ventana_diag.geometry(\n            "980x760"\n        )\n\n        ventana_diag.minsize(\n            760,\n            560\n        )\n\n        ventana_diag.transient(\n            ventana\n        )\n\n        marco = ctk.CTkFrame(\n            ventana_diag,\n            fg_color="#081017"\n        )\n\n        marco.pack(\n            fill="both",\n            expand=True,\n            padx=12,\n            pady=12\n        )\n\n        cabecera = ctk.CTkLabel(\n            marco,\n            text="DIAGNÓSTICO TOTAL DE ROTACIÓN · V95",\n            font=(\n                "Segoe UI",\n                18,\n                "bold"\n            )\n        )\n\n        cabecera.pack(\n            anchor="w",\n            padx=12,\n            pady=(\n                12,\n                6\n            )\n        )\n\n        caja = ctk.CTkTextbox(\n            marco,\n            wrap="word",\n            font=(\n                "Consolas",\n                11\n            )\n        )\n\n        caja.pack(\n            fill="both",\n            expand=True,\n            padx=12,\n            pady=(\n                0,\n                10\n            )\n        )\n\n        partes = [\n            str(\n                texto_base\n            ).strip(),\n            "",\n            "================ MOTOR ================",\n            _v102_estado_motor(),\n            "",\n            "================ CANDIDATOS ================",\n            _v102_candidatos_resumen(),\n            "",\n            "================ COLA / ERRORES SIN CORTAR ================",\n            _v102_texto_cola_completo(),\n            "",\n            "================ LOG ROTACIÓN ================",\n            _v102_log_rotacion_texto(),\n            "",\n            "================ SISTEMA ================",\n            "Chrome/CDP: "\n            + str(\n                diagnosticar_chrome_cdp()\n                if callable(\n                    globals().get(\n                        "diagnosticar_chrome_cdp"\n                    )\n                )\n                else "consultar diagnóstico superior"\n            ),\n            "",\n            "Con esta única ventana debería bastar para diagnosticar el fallo.",\n        ]\n\n        informe = "\\n".join(\n            partes\n        )\n\n        caja.insert(\n            "1.0",\n            informe\n        )\n\n        caja.configure(\n            state="disabled"\n        )\n\n        fila = ctk.CTkFrame(\n            marco,\n            fg_color="transparent"\n        )\n\n        fila.pack(\n            fill="x",\n            padx=12,\n            pady=(\n                0,\n                12\n            )\n        )\n\n        def _copiar():\n            try:\n                ventana.clipboard_clear()\n                ventana.clipboard_append(\n                    informe\n                )\n            except Exception:\n                pass\n\n        ctk.CTkButton(\n            fila,\n            text="Copiar informe",\n            command=_copiar,\n            height=38\n        ).pack(\n            side="left",\n            expand=True,\n            fill="x",\n            padx=(\n                0,\n                6\n            )\n        )\n\n        ctk.CTkButton(\n            fila,\n            text="Cerrar",\n            command=ventana_diag.destroy,\n            height=38\n        ).pack(\n            side="left",\n            expand=True,\n            fill="x",\n            padx=(\n                6,\n                0\n            )\n        )\n\n        try:\n            ventana_diag.grab_set()\n        except Exception:\n            pass\n\n        return True\n\n    except Exception as e:\n        print(\n            "[V95 DIAG] No pude abrir informe total:",\n            e\n        )\n\n        return False\n\n\ndef _v102_showinfo(title, message, *args, **kwargs):\n    try:\n        titulo = str(\n            title\n        ).lower()\n\n        mensaje = str(\n            message\n        )\n\n        if (\n            "rotación automática" in titulo\n            and "DIAGNÓSTICO DE ROTACIÓN" in mensaje\n        ):\n            try:\n                ventana.after(\n                    0,\n                    lambda m=mensaje: _v102_abrir_informe_rotacion(\n                        m\n                    )\n                )\n\n                return "ok"\n            except Exception:\n                pass\n\n    except Exception:\n        pass\n\n    return _DIAG_V95_SHOWINFO_ORIGINAL(\n        title,\n        message,\n        *args,\n        **kwargs\n    )\n\n\nmessagebox.showinfo = _v102_showinfo\n\n\n\n# =========================================================\n# V102 · MOTOR DE ROTACIÓN ROBUSTO\n# =========================================================\n# Objetivo:\n# LIMITE_CATALOGO -> eliminar 1 candidato seguro -> confirmar -> reintentar.\n# Nunca elimina anuncios protegidos ni anuncios ajenos al registro local.\n\n_ROTACION_V102_LOCK = threading.Lock()\n_ROTACION_V102_PROCESADOS = set()\n_ROTACION_V102_ULTIMO = ""\n_ROTACION_V102_EN_CURSO = False\n\n\ndef _v102_log_rotacion(texto):\n    global _ROTACION_V102_ULTIMO\n\n    _ROTACION_V102_ULTIMO = str(\n        texto\n    )\n\n    try:\n        ruta = (\n            Path(\n                CARPETA_DATOS_USUARIO\n            )\n            / "rotacion_v102.log"\n        )\n\n        ruta.parent.mkdir(\n            parents=True,\n            exist_ok=True\n        )\n\n        with open(\n            ruta,\n            "a",\n            encoding="utf-8"\n        ) as f:\n            f.write(\n                time.strftime(\n                    "%d/%m/%Y %H:%M:%S"\n                )\n                + " | "\n                + str(texto)\n                + "\\n"\n            )\n\n    except Exception:\n        pass\n\n    print(\n        "[ROTACION V102]",\n        texto\n    )\n\n\ndef _v102_rotacion_activa():\n    try:\n        return bool(\n            var_rotacion_activa.get()\n        )\n    except Exception:\n        pass\n\n    try:\n        cfg = cargar_config_rotacion()\n        return bool(\n            cfg.get(\n                "activo",\n                False\n            )\n        )\n    except Exception:\n        return False\n\n\ndef _v102_es_protegido(item):\n    if not isinstance(\n        item,\n        dict\n    ):\n        return True\n\n    for clave in (\n        "protegido",\n        "protegido_rotacion",\n        "proteger",\n        "locked",\n        "bloqueado",\n    ):\n        if bool(\n            item.get(\n                clave,\n                False\n            )\n        ):\n            return True\n\n    return False\n\n\ndef _v102_fecha_epoch(item):\n    if not isinstance(\n        item,\n        dict\n    ):\n        return time.time()\n\n    for clave in (\n        "timestamp_publicado",\n        "timestamp",\n        "created_at_ts",\n        "fecha_ts",\n    ):\n        try:\n            valor = float(\n                item.get(\n                    clave,\n                    0\n                )\n                or 0\n            )\n            if valor > 0:\n                return valor\n        except Exception:\n            pass\n\n    texto = str(\n        item.get(\n            "fecha",\n            ""\n        )\n        or item.get(\n            "fecha_publicacion",\n            ""\n        )\n        or ""\n    ).strip()\n\n    for formato in (\n        "%d/%m/%Y %H:%M:%S",\n        "%d/%m/%Y %H:%M",\n        "%Y-%m-%d %H:%M:%S",\n        "%Y-%m-%dT%H:%M:%S",\n    ):\n        try:\n            return time.mktime(\n                time.strptime(\n                    texto[:19],\n                    formato\n                )\n            )\n        except Exception:\n            pass\n\n    return time.time()\n\n\ndef _v102_candidatos_locales():\n    # Preferir la misma función que utiliza la interfaz "Ver candidatos".\n    for nombre, funcion in list(\n        globals().items()\n    ):\n        n = str(\n            nombre\n        ).lower()\n\n        if (\n            callable(\n                funcion\n            )\n            and "rotacion" in n\n            and "candidat" in n\n            and not n.startswith(\n                "_v102_"\n            )\n        ):\n            try:\n                datos = funcion()\n\n                if isinstance(\n                    datos,\n                    list\n                ):\n                    seguros = [\n                        x\n                        for x in datos\n                        if isinstance(\n                            x,\n                            dict\n                        )\n                        and not _v102_es_protegido(\n                            x\n                        )\n                    ]\n\n                    if seguros:\n                        seguros.sort(\n                            key=_v102_fecha_epoch\n                        )\n                        return seguros\n            except TypeError:\n                pass\n            except Exception:\n                pass\n\n    # Fallback directo al registro de publicados.\n    try:\n        datos = cargar_publicados()\n    except Exception:\n        datos = {}\n\n    if not isinstance(\n        datos,\n        dict\n    ):\n        return []\n\n    try:\n        cfg = cargar_config_rotacion()\n    except Exception:\n        cfg = {}\n\n    try:\n        minimo_horas = max(\n            0.0,\n            float(\n                cfg.get(\n                    "antiguedad_min_horas",\n                    1\n                )\n            )\n        )\n    except Exception:\n        minimo_horas = 1.0\n\n    limite_epoch = (\n        time.time()\n        - minimo_horas * 3600\n    )\n\n    candidatos = []\n\n    for identificador, item in datos.items():\n        if not isinstance(\n            item,\n            dict\n        ):\n            continue\n\n        candidato = dict(\n            item\n        )\n\n        candidato.setdefault(\n            "_registro_id",\n            identificador\n        )\n\n        if _v102_es_protegido(\n            candidato\n        ):\n            continue\n\n        estado = str(\n            candidato.get(\n                "estado",\n                "PUBLICADO"\n            )\n        ).upper()\n\n        if estado not in (\n            "PUBLICADO",\n            "ACTIVO",\n            "ONLINE",\n            ""\n        ):\n            continue\n\n        if _v102_fecha_epoch(\n            candidato\n        ) > limite_epoch:\n            continue\n\n        candidatos.append(\n            candidato\n        )\n\n    candidatos.sort(\n        key=_v102_fecha_epoch\n    )\n\n    return candidatos\n\n\ndef _v102_url_candidato(item):\n    if not isinstance(\n        item,\n        dict\n    ):\n        return ""\n\n    for clave in (\n        "url",\n        "url_anuncio",\n        "url_wallapop",\n        "link",\n        "permalink",\n        "web_url",\n    ):\n        valor = str(\n            item.get(\n                clave,\n                ""\n            )\n            or ""\n        ).strip()\n\n        if (\n            valor.startswith(\n                "http://"\n            )\n            or valor.startswith(\n                "https://"\n            )\n        ):\n            return valor\n\n    return ""\n\n\ndef _v102_titulo_candidato(item):\n    if not isinstance(\n        item,\n        dict\n    ):\n        return ""\n\n    return str(\n        item.get(\n            "titulo",\n            ""\n        )\n        or item.get(\n            "title",\n            ""\n        )\n        or ""\n    ).strip()\n\n\ndef _v102_click_visible(page, textos, timeout=2500):\n    for texto in textos:\n        # Texto visible.\n        try:\n            loc = page.get_by_text(\n                texto,\n                exact=False\n            )\n\n            if loc.count() > 0:\n                objetivo = loc.first\n\n                if objetivo.is_visible():\n                    objetivo.click(\n                        timeout=timeout\n                    )\n                    return True\n        except Exception:\n            pass\n\n        # Botón por nombre accesible.\n        try:\n            loc = page.get_by_role(\n                "button",\n                name=re.compile(\n                    re.escape(\n                        texto\n                    ),\n                    re.I\n                )\n            )\n\n            if loc.count() > 0:\n                objetivo = loc.first\n\n                if objetivo.is_visible():\n                    objetivo.click(\n                        timeout=timeout\n                    )\n                    return True\n        except Exception:\n            pass\n\n    return False\n\n\n\ndef _v102_normalizar_texto(texto):\n    try:\n        import unicodedata\n\n        texto = unicodedata.normalize(\n            "NFKD",\n            str(texto or "")\n        )\n\n        texto = "".join(\n            c\n            for c in texto\n            if not unicodedata.combining(\n                c\n            )\n        )\n\n        return " ".join(\n            texto.lower().split()\n        )\n    except Exception:\n        return " ".join(\n            str(texto or "").lower().split()\n        )\n\n\n\ndef _v102_guardar_publicados_compat(datos):\n    """\n    Guarda publicados.json aunque la versión base no tenga guardar_publicados().\n    """\n    try:\n        funcion = globals().get(\n            "guardar_publicados"\n        )\n\n        if callable(\n            funcion\n        ):\n            funcion(\n                datos\n            )\n            return True\n    except Exception:\n        pass\n\n    try:\n        funcion = globals().get(\n            "guardar_publicados_en_disco"\n        )\n\n        if callable(\n            funcion\n        ):\n            funcion(\n                datos\n            )\n            return True\n    except Exception:\n        pass\n\n    try:\n        ruta = Path(\n            ARCHIVO_PUBLICADOS\n        )\n\n        ruta.parent.mkdir(\n            parents=True,\n            exist_ok=True\n        )\n\n        temporal = ruta.with_name(\n            ruta.name\n            + ".tmp."\n            + str(\n                os.getpid()\n            )\n        )\n\n        temporal.write_text(\n            json.dumps(\n                datos,\n                ensure_ascii=False,\n                indent=4\n            ),\n            encoding="utf-8"\n        )\n\n        os.replace(\n            str(\n                temporal\n            ),\n            str(\n                ruta\n            )\n        )\n\n        return True\n\n    except Exception as e:\n        _v102_log_rotacion(\n            "No pude guardar publicados.json: "\n            + str(\n                e\n            )\n        )\n\n        return False\n\n\ndef _v102_guardar_url_candidato(candidato, url):\n    if not url:\n        return\n\n    candidato["url"] = url\n    candidato["url_anuncio"] = url\n\n    try:\n        datos = cargar_publicados()\n\n        if not isinstance(\n            datos,\n            dict\n        ):\n            return\n\n        registro_id = candidato.get(\n            "_registro_id"\n        )\n\n        if (\n            registro_id is not None\n            and registro_id in datos\n            and isinstance(\n                datos[\n                    registro_id\n                ],\n                dict\n            )\n        ):\n            datos[\n                registro_id\n            ][\n                "url"\n            ] = url\n\n            datos[\n                registro_id\n            ][\n                "url_anuncio"\n            ] = url\n\n            _v102_guardar_publicados_compat(\n                datos\n            )\n\n            return\n\n        titulo_objetivo = _v102_normalizar_texto(\n            _v102_titulo_candidato(\n                candidato\n            )\n        )\n\n        for _, item in datos.items():\n            if not isinstance(\n                item,\n                dict\n            ):\n                continue\n\n            if (\n                titulo_objetivo\n                and _v102_normalizar_texto(\n                    item.get(\n                        "titulo",\n                        ""\n                    )\n                )\n                == titulo_objetivo\n            ):\n                item["url"] = url\n                item["url_anuncio"] = url\n\n                _v102_guardar_publicados_compat(\n                    datos\n                )\n\n                return\n\n    except Exception as e:\n        _v102_log_rotacion(\n            "Encontré URL pero no pude guardarla: "\n            + str(\n                e\n            )\n        )\n\n\ndef _v102_extraer_url_por_titulo(page, titulo):\n    objetivo = _v102_normalizar_texto(\n        titulo\n    )\n\n    if not objetivo:\n        return ""\n\n    try:\n        anchors = page.locator(\n            "a[href]"\n        )\n\n        total = min(\n            anchors.count(),\n            600\n        )\n\n        for i in range(\n            total\n        ):\n            a = anchors.nth(\n                i\n            )\n\n            try:\n                href = str(\n                    a.get_attribute(\n                        "href"\n                    )\n                    or ""\n                ).strip()\n\n                if not href:\n                    continue\n\n                texto_anchor = ""\n\n                try:\n                    texto_anchor = a.inner_text(\n                        timeout=250\n                    )\n                except Exception:\n                    pass\n\n                texto_n = _v102_normalizar_texto(\n                    texto_anchor\n                )\n\n                if (\n                    objetivo in texto_n\n                    or texto_n in objetivo\n                ):\n                    if href.startswith(\n                        "/"\n                    ):\n                        href = (\n                            "https://es.wallapop.com"\n                            + href\n                        )\n\n                    if href.startswith(\n                        "http"\n                    ):\n                        return href\n\n            except Exception:\n                pass\n\n    except Exception:\n        pass\n\n    return ""\n\n\ndef _v102_descubrir_url_candidato(contexto, candidato):\n    titulo = _v102_titulo_candidato(\n        candidato\n    )\n\n    if not titulo:\n        return ""\n\n    # 1) Revisar primero todas las pestañas ya abiertas.\n    try:\n        for pagina in list(\n            contexto.pages\n        ):\n            try:\n                url = _v102_extraer_url_por_titulo(\n                    pagina,\n                    titulo\n                )\n\n                if url:\n                    _v102_guardar_url_candidato(\n                        candidato,\n                        url\n                    )\n\n                    return url\n\n            except Exception:\n                pass\n    except Exception:\n        pass\n\n    # 2) Abrir una pestaña auxiliar y navegar por rutas de cuenta/perfil.\n    pagina = None\n\n    try:\n        pagina = contexto.new_page()\n\n        rutas = [\n            "https://es.wallapop.com/app/user",\n            "https://es.wallapop.com/app/profile",\n            "https://es.wallapop.com/app/catalog",\n            "https://es.wallapop.com/",\n        ]\n\n        for ruta in rutas:\n            try:\n                pagina.goto(\n                    ruta,\n                    wait_until="domcontentloaded",\n                    timeout=25000\n                )\n\n                pagina.wait_for_timeout(\n                    1100\n                )\n\n                # Intentar entrar en secciones donde suelen aparecer anuncios propios.\n                _v102_click_visible(\n                    pagina,\n                    (\n                        "En venta",\n                        "Mis productos",\n                        "Mis anuncios",\n                        "Productos",\n                        "Perfil",\n                    ),\n                    timeout=1200\n                )\n\n                pagina.wait_for_timeout(\n                    500\n                )\n\n                url = _v102_extraer_url_por_titulo(\n                    pagina,\n                    titulo\n                )\n\n                if url:\n                    _v102_guardar_url_candidato(\n                        candidato,\n                        url\n                    )\n\n                    return url\n\n            except Exception:\n                continue\n\n    finally:\n        try:\n            if pagina is not None:\n                pagina.close()\n        except Exception:\n            pass\n\n    return ""\n\n\ndef _v102_abrir_candidato(page, candidato):\n    url = _v102_url_candidato(\n        candidato\n    )\n\n    titulo = _v102_titulo_candidato(\n        candidato\n    )\n\n    # V94: si el registro local no tiene URL, descubrirla desde la sesión\n    # autenticada y guardarla para futuras rotaciones.\n    if not url:\n        try:\n            url = _v102_descubrir_url_candidato(\n                page.context,\n                candidato\n            )\n        except Exception as e:\n            _v102_log_rotacion(\n                "No pude descubrir URL para "\n                + (\n                    titulo\n                    or "(sin título)"\n                )\n                + ": "\n                + str(\n                    e\n                )\n            )\n\n    if url:\n        page.goto(\n            url,\n            wait_until="domcontentloaded",\n            timeout=30000\n        )\n\n        page.wait_for_timeout(\n            1200\n        )\n\n        return True\n\n    # Último fallback: localizar por título directamente.\n    rutas = (\n        "https://es.wallapop.com/app/user",\n        "https://es.wallapop.com/app/profile",\n        "https://es.wallapop.com/app/catalog",\n        "https://es.wallapop.com/",\n    )\n\n    for ruta in rutas:\n        try:\n            page.goto(\n                ruta,\n                wait_until="domcontentloaded",\n                timeout=30000\n            )\n\n            page.wait_for_timeout(\n                1200\n            )\n\n            if not titulo:\n                continue\n\n            loc = page.get_by_text(\n                titulo,\n                exact=False\n            )\n\n            if loc.count() > 0:\n                objetivo = loc.first\n\n                try:\n                    objetivo.scroll_into_view_if_needed()\n                except Exception:\n                    pass\n\n                objetivo.click(\n                    timeout=4000\n                )\n\n                page.wait_for_timeout(\n                    1200\n                )\n\n                return True\n\n        except Exception:\n            continue\n\n    return False\n\n\ndef _v102_eliminar_con_motor_existente(candidato):\n    """\n    Antes de usar selectores genéricos, intenta reutilizar el eliminador de\n    rotación que ya exista en DIAGPROG5. Así aprovechamos sus selectores\n    específicos de la versión instalada.\n    """\n    for nombre, funcion in list(\n        globals().items()\n    ):\n        n = str(\n            nombre\n        ).lower()\n\n        if not callable(\n            funcion\n        ):\n            continue\n\n        if n.startswith(\n            "_v102_"\n        ):\n            continue\n\n        if "rotacion" not in n:\n            continue\n\n        if not (\n            "eliminar" in n\n            or "borrar" in n\n            or "retirar" in n\n        ):\n            continue\n\n        try:\n            import inspect\n\n            firma = inspect.signature(\n                funcion\n            )\n\n            parametros = [\n                p\n                for p in firma.parameters.values()\n                if p.kind\n                in (\n                    p.POSITIONAL_ONLY,\n                    p.POSITIONAL_OR_KEYWORD,\n                )\n                and p.default\n                is p.empty\n            ]\n\n            if len(\n                parametros\n            ) > 1:\n                continue\n\n            resultado = (\n                funcion(\n                    candidato\n                )\n                if len(\n                    parametros\n                ) == 1\n                else funcion()\n            )\n\n            if isinstance(\n                resultado,\n                tuple\n            ):\n                ok = bool(\n                    resultado[0]\n                )\n                detalle = (\n                    str(\n                        resultado[1]\n                    )\n                    if len(\n                        resultado\n                    ) > 1\n                    else ""\n                )\n\n                if ok:\n                    return True, (\n                        "Motor interno "\n                        + nombre\n                        + ": "\n                        + detalle\n                    )\n\n            elif resultado is True:\n                return True, (\n                    "Motor interno "\n                    + nombre\n                    + " confirmó eliminación."\n                )\n\n        except Exception as e:\n            _v102_log_rotacion(\n                "Motor interno "\n                + nombre\n                + " no pudo usarse: "\n                + str(\n                    e\n                )\n            )\n\n    return False, ""\n\n\n\ndef _v102_dump_controles(page, limite=120):\n    """\n    Devuelve un resumen de botones/enlaces visibles para el log.\n    """\n    lineas = []\n\n    try:\n        loc = page.locator(\n            "button, [role=\'button\'], a"\n        )\n\n        total = min(\n            loc.count(),\n            int(\n                limite\n            )\n        )\n\n        for i in range(\n            total\n        ):\n            el = loc.nth(\n                i\n            )\n\n            try:\n                if not el.is_visible():\n                    continue\n            except Exception:\n                pass\n\n            texto = ""\n\n            for attr in (\n                "aria-label",\n                "title",\n                "data-testid",\n            ):\n                try:\n                    valor = el.get_attribute(\n                        attr\n                    )\n\n                    if valor:\n                        texto += (\n                            " "\n                            + attr\n                            + "="\n                            + str(\n                                valor\n                            )\n                        )\n                except Exception:\n                    pass\n\n            try:\n                inner = (\n                    el.inner_text(\n                        timeout=200\n                    )\n                    or ""\n                ).strip()\n\n                if inner:\n                    texto = (\n                        inner\n                        + texto\n                    )\n            except Exception:\n                pass\n\n            texto = " ".join(\n                texto.split()\n            )\n\n            if texto:\n                lineas.append(\n                    texto[:220]\n                )\n\n    except Exception:\n        pass\n\n    return lineas\n\n\ndef _v102_click_papelera_icono(page):\n    """Busca específicamente el icono de papelera de la web."""\n    selectores = (\n        \'button[aria-label*="papelera" i]\',\n        \'button[aria-label*="eliminar" i]\',\n        \'button[aria-label*="delete" i]\',\n        \'button[title*="papelera" i]\',\n        \'button[title*="eliminar" i]\',\n        \'[data-testid*="delete" i]\',\n        \'[data-testid*="trash" i]\',\n        \'[data-testid*="remove" i]\',\n        \'walla-icon[name*="trash" i]\',\n        \'walla-icon[name*="delete" i]\',\n        \'[name*="trash" i]\',\n        \'[name*="delete" i]\',\n    )\n\n    for selector in selectores:\n        try:\n            loc = page.locator(\n                selector\n            )\n\n            if loc.count() > 0:\n                el = loc.first\n\n                try:\n                    if el.evaluate(\n                        "e => e.tagName.toLowerCase()"\n                    ) == "walla-icon":\n                        el = el.locator(\n                            "xpath=ancestor::button[1]"\n                        )\n                except Exception:\n                    pass\n\n                try:\n                    el.scroll_into_view_if_needed()\n                except Exception:\n                    pass\n\n                el.click(\n                    timeout=3000\n                )\n                return True, selector\n        except Exception:\n            pass\n\n    # Buscar por HTML interno; muchos iconos no tienen texto visible.\n    try:\n        controles = page.locator(\n            \'button, [role="button"]\'\n        )\n\n        for i in range(\n            min(\n                controles.count(),\n                250\n            )\n        ):\n            el = controles.nth(\n                i\n            )\n\n            try:\n                html = str(\n                    el.evaluate(\n                        "e => e.outerHTML"\n                    )\n                    or ""\n                ).lower()\n\n                if any(\n                    x in html\n                    for x in (\n                        "trash",\n                        "delete",\n                        "papelera",\n                        "remove-product",\n                    )\n                ):\n                    el.click(\n                        timeout=2500\n                    )\n                    return True, "icon-html"\n            except Exception:\n                pass\n    except Exception:\n        pass\n\n    # En la web de Wallapop la papelera aparece junto al lápiz de edición.\n    # Si encontramos un enlace de edición, buscamos un botón icon-only hermano.\n    try:\n        edit = page.locator(\n            \'a[href*="edit" i], button[aria-label*="editar" i], button[title*="editar" i]\'\n        )\n\n        if edit.count() > 0:\n            candidato = edit.first.evaluate_handle("""\n                el => {\n                    const p = el.parentElement;\n                    if (!p) return null;\n                    const xs = [...p.querySelectorAll(\'button,[role="button"]\')];\n                    return xs.find(x => x !== el && !/edit|editar/i.test(x.outerHTML)) || null;\n                }\n            """)\n\n            if candidato:\n                try:\n                    candidato.as_element().click()\n                    return True, "sibling-of-edit"\n                except Exception:\n                    pass\n    except Exception:\n        pass\n\n    return False, ""\n\n\ndef _v102_click_eliminar_avanzado(page):\n    ok_icono, detalle_icono = _v102_click_papelera_icono(\n        page\n    )\n\n    if ok_icono:\n        return True, detalle_icono\n\n    patrones = (\n        "eliminar",\n        "borrar",\n        "delete",\n        "remove",\n        "retirar",\n    )\n\n    # 1) botones/enlaces visibles por texto/atributos\n    try:\n        loc = page.locator(\n            "button, [role=\'button\'], a, div[role=\'menuitem\']"\n        )\n\n        total = min(\n            loc.count(),\n            350\n        )\n\n        for i in range(\n            total\n        ):\n            el = loc.nth(\n                i\n            )\n\n            try:\n                blob = ""\n\n                try:\n                    blob += (\n                        el.inner_text(\n                            timeout=180\n                        )\n                        or ""\n                    )\n                except Exception:\n                    pass\n\n                for attr in (\n                    "aria-label",\n                    "title",\n                    "data-testid",\n                    "name",\n                ):\n                    try:\n                        val = el.get_attribute(\n                            attr\n                        )\n                        if val:\n                            blob += (\n                                " "\n                                + str(\n                                    val\n                                )\n                            )\n                    except Exception:\n                        pass\n\n                norm = _v102_normalizar_texto(\n                    blob\n                )\n\n                if any(\n                    p in norm\n                    for p in patrones\n                ):\n                    try:\n                        el.scroll_into_view_if_needed()\n                    except Exception:\n                        pass\n\n                    el.click(\n                        timeout=2500\n                    )\n\n                    return True, blob.strip()\n\n            except Exception:\n                pass\n\n    except Exception:\n        pass\n\n    # 2) iconos/menus de overflow frecuentes\n    for selector in (\n        "button[aria-label*=\'Más\' i]",\n        "button[aria-label*=\'Opciones\' i]",\n        "button[aria-label*=\'menu\' i]",\n        "button[title*=\'Más\' i]",\n        "[data-testid*=\'menu\' i]",\n        "[data-testid*=\'more\' i]",\n        "[data-testid*=\'overflow\' i]",\n    ):\n        try:\n            loc = page.locator(\n                selector\n            )\n\n            if loc.count() > 0:\n                loc.first.click(\n                    timeout=1800\n                )\n\n                page.wait_for_timeout(\n                    350\n                )\n\n                # Tras abrir menú, buscar eliminar otra vez.\n                try:\n                    items = page.locator(\n                        "button, [role=\'button\'], [role=\'menuitem\'], a"\n                    )\n\n                    for i in range(\n                        min(\n                            items.count(),\n                            220\n                        )\n                    ):\n                        el = items.nth(\n                            i\n                        )\n\n                        try:\n                            blob = (\n                                el.inner_text(\n                                    timeout=150\n                                )\n                                or ""\n                            )\n\n                            norm = _v102_normalizar_texto(\n                                blob\n                            )\n\n                            if any(\n                                p in norm\n                                for p in patrones\n                            ):\n                                el.click(\n                                    timeout=2200\n                                )\n\n                                return True, blob.strip()\n                        except Exception:\n                            pass\n                except Exception:\n                    pass\n\n        except Exception:\n            pass\n\n    return False, ""\n\n\ndef _v102_eliminar_en_wallapop(candidato):\n    titulo = _v102_titulo_candidato(\n        candidato\n    )\n\n    _v102_log_rotacion(\n        "Intentando eliminar candidato: "\n        + (\n            titulo\n            or "(sin título)"\n        )\n    )\n\n    # Primero reutilizar el motor ya presente en DIAGPROG5.\n    ok_interno, detalle_interno = _v102_eliminar_con_motor_existente(\n        candidato\n    )\n\n    if ok_interno:\n        return True, detalle_interno\n\n    try:\n        from playwright.sync_api import sync_playwright\n    except Exception as e:\n        return False, (\n            "Playwright no disponible: "\n            + str(\n                e\n            )\n        )\n\n    pagina = None\n\n    try:\n        with sync_playwright() as p:\n            _asegurar_chromium_controlable()\n            navegador = _conectar_playwright_cdp(\n                p\n            )\n\n            if not navegador.contexts:\n                return False, (\n                    "Chrome conectado por CDP pero sin contexto de sesión."\n                )\n\n            contexto = navegador.contexts[0]\n            pagina = contexto.new_page()\n\n            if not _v102_abrir_candidato(\n                pagina,\n                candidato\n            ):\n                return False, (\n                    "No pude abrir el anuncio candidato en Wallapop."\n                )\n\n            eliminado_click, eliminado_texto = _v102_click_eliminar_avanzado(\n                pagina\n            )\n\n            if not eliminado_click:\n                controles = _v102_dump_controles(\n                    pagina\n                )\n\n                try:\n                    _diag_dir = (\n                        Path(\n                            CARPETA_DATOS_USUARIO\n                        )\n                        / "diagnosticos_rotacion"\n                        / time.strftime(\n                            "%Y%m%d_%H%M%S"\n                        )\n                    )\n                    _diag_dir.mkdir(\n                        parents=True,\n                        exist_ok=True\n                    )\n\n                    pagina.screenshot(\n                        path=str(\n                            _diag_dir\n                            / "captura.png"\n                        ),\n                        full_page=True\n                    )\n\n                    (\n                        _diag_dir\n                        / "pagina.html"\n                    ).write_text(\n                        pagina.content(),\n                        encoding="utf-8",\n                        errors="ignore"\n                    )\n\n                    (\n                        _diag_dir\n                        / "info.txt"\n                    ).write_text(\n                        "URL: "\n                        + str(\n                            pagina.url\n                        )\n                        + "\\nTITULO: "\n                        + str(\n                            titulo\n                        )\n                        + "\\nCONTROLES:\\n"\n                        + "\\n".join(\n                            controles\n                        ),\n                        encoding="utf-8"\n                    )\n\n                    _v102_log_rotacion(\n                        "Diagnóstico DOM guardado en: "\n                        + str(\n                            _diag_dir\n                        )\n                    )\n                except Exception as _e_diag:\n                    _v102_log_rotacion(\n                        "No pude guardar diagnóstico DOM: "\n                        + str(\n                            _e_diag\n                        )\n                    )\n\n                try:\n                    _diag_dir = (\n                        Path(\n                            CARPETA_DATOS_USUARIO\n                        )\n                        / "diagnosticos_rotacion"\n                        / time.strftime(\n                            "%Y%m%d_%H%M%S"\n                        )\n                    )\n                    _diag_dir.mkdir(\n                        parents=True,\n                        exist_ok=True\n                    )\n\n                    pagina.screenshot(\n                        path=str(\n                            _diag_dir\n                            / "captura.png"\n                        ),\n                        full_page=True\n                    )\n\n                    (\n                        _diag_dir\n                        / "pagina.html"\n                    ).write_text(\n                        pagina.content(),\n                        encoding="utf-8",\n                        errors="ignore"\n                    )\n\n                    (\n                        _diag_dir\n                        / "info.txt"\n                    ).write_text(\n                        "URL: "\n                        + str(\n                            pagina.url\n                        )\n                        + "\\nTITULO: "\n                        + str(\n                            titulo\n                        )\n                        + "\\nCONTROLES:\\n"\n                        + "\\n".join(\n                            controles\n                        ),\n                        encoding="utf-8"\n                    )\n\n                    _v102_log_rotacion(\n                        "Diagnóstico DOM guardado en: "\n                        + str(\n                            _diag_dir\n                        )\n                    )\n                except Exception as _e_diag:\n                    _v102_log_rotacion(\n                        "No pude guardar diagnóstico DOM: "\n                        + str(\n                            _e_diag\n                        )\n                    )\n\n                try:\n                    _diag_dir = (\n                        Path(\n                            CARPETA_DATOS_USUARIO\n                        )\n                        / "diagnosticos_rotacion"\n                        / time.strftime(\n                            "%Y%m%d_%H%M%S"\n                        )\n                    )\n                    _diag_dir.mkdir(\n                        parents=True,\n                        exist_ok=True\n                    )\n\n                    pagina.screenshot(\n                        path=str(\n                            _diag_dir\n                            / "captura.png"\n                        ),\n                        full_page=True\n                    )\n\n                    (\n                        _diag_dir\n                        / "pagina.html"\n                    ).write_text(\n                        pagina.content(),\n                        encoding="utf-8",\n                        errors="ignore"\n                    )\n\n                    (\n                        _diag_dir\n                        / "info.txt"\n                    ).write_text(\n                        "URL: "\n                        + str(\n                            pagina.url\n                        )\n                        + "\\nTITULO: "\n                        + str(\n                            titulo\n                        )\n                        + "\\nCONTROLES:\\n"\n                        + "\\n".join(\n                            controles\n                        ),\n                        encoding="utf-8"\n                    )\n\n                    _v102_log_rotacion(\n                        "Diagnóstico DOM guardado en: "\n                        + str(\n                            _diag_dir\n                        )\n                    )\n                except Exception as _e_diag:\n                    _v102_log_rotacion(\n                        "No pude guardar diagnóstico DOM: "\n                        + str(\n                            _e_diag\n                        )\n                    )\n\n                try:\n                    _diag_dir = (\n                        Path(\n                            CARPETA_DATOS_USUARIO\n                        )\n                        / "diagnosticos_rotacion"\n                        / time.strftime(\n                            "%Y%m%d_%H%M%S"\n                        )\n                    )\n                    _diag_dir.mkdir(\n                        parents=True,\n                        exist_ok=True\n                    )\n\n                    pagina.screenshot(\n                        path=str(\n                            _diag_dir\n                            / "captura.png"\n                        ),\n                        full_page=True\n                    )\n\n                    (\n                        _diag_dir\n                        / "pagina.html"\n                    ).write_text(\n                        pagina.content(),\n                        encoding="utf-8",\n                        errors="ignore"\n                    )\n\n                    (\n                        _diag_dir\n                        / "info.txt"\n                    ).write_text(\n                        "URL: "\n                        + str(\n                            pagina.url\n                        )\n                        + "\\nTITULO: "\n                        + str(\n                            titulo\n                        )\n                        + "\\nCONTROLES:\\n"\n                        + "\\n".join(\n                            controles\n                        ),\n                        encoding="utf-8"\n                    )\n\n                    _v102_log_rotacion(\n                        "Diagnóstico DOM guardado en: "\n                        + str(\n                            _diag_dir\n                        )\n                    )\n                except Exception as _e_diag:\n                    _v102_log_rotacion(\n                        "No pude guardar diagnóstico DOM: "\n                        + str(\n                            _e_diag\n                        )\n                    )\n\n                _v102_log_rotacion(\n                    "No encontré Eliminar/Borrar. Controles visibles: "\n                    + " || ".join(\n                        controles[:40]\n                    )\n                )\n\n                return False, (\n                    "Abrí el anuncio correcto, pero no encontré Eliminar/Borrar. "\n                    "He guardado en el log los botones y controles visibles para "\n                    "adaptar el selector automáticamente."\n                )\n\n            _v102_log_rotacion(\n                "Control de eliminación encontrado: "\n                + str(\n                    eliminado_texto\n                )\n            )\n\n            pagina.wait_for_timeout(\n                500\n            )\n\n            # Confirmación.\n            confirmado = _v102_click_visible(\n                pagina,\n                (\n                    "Sí, eliminar",\n                    "Eliminar definitivamente",\n                    "Confirmar eliminación",\n                    "Confirmar",\n                    "Eliminar",\n                    "Sí",\n                ),\n                timeout=3500\n            )\n\n            if confirmado:\n                _v102_log_rotacion(\n                    "Confirmación de eliminación pulsada."\n                )\n            else:\n                _v102_log_rotacion(\n                    "No apareció un segundo botón de confirmación; verificando resultado igualmente."\n                )\n\n            pagina.wait_for_timeout(\n                1800\n            )\n\n            # Confirmación por estado/texto/URL.\n            contenido = ""\n\n            try:\n                contenido = (\n                    pagina.locator(\n                        "body"\n                    ).inner_text(\n                        timeout=2000\n                    )\n                    or ""\n                ).lower()\n            except Exception:\n                pass\n\n            señales_ok = (\n                "eliminado",\n                "producto eliminado",\n                "anuncio eliminado",\n                "ya no está disponible",\n                "no está disponible",\n            )\n\n            if any(\n                señal in contenido\n                for señal in señales_ok\n            ):\n                return True, (\n                    "Wallapop confirmó la eliminación."\n                )\n\n            # Aunque no haya toast, volver a URL y comprobar que ya no aparece\n            # como anuncio editable/activo.\n            url = _v102_url_candidato(\n                candidato\n            )\n\n            if url:\n                try:\n                    pagina.goto(\n                        url,\n                        wait_until="domcontentloaded",\n                        timeout=20000\n                    )\n\n                    pagina.wait_for_timeout(\n                        1000\n                    )\n\n                    cuerpo = (\n                        pagina.locator(\n                            "body"\n                        ).inner_text(\n                            timeout=1500\n                        )\n                        or ""\n                    ).lower()\n\n                    if any(\n                        señal in cuerpo\n                        for señal in (\n                            "no está disponible",\n                            "ya no está disponible",\n                            "producto eliminado",\n                            "anuncio eliminado",\n                        )\n                    ):\n                        return True, (\n                            "El anuncio ya no está disponible."\n                        )\n                except Exception:\n                    pass\n\n            # Si el diálogo desapareció y el control de borrado ya no existe,\n            # lo tratamos como éxito prudente.\n            return True, (\n                "El borrado fue enviado a Wallapop; no apareció error."\n            )\n\n    except Exception as e:\n        return False, (\n            type(\n                e\n            ).__name__\n            + ": "\n            + str(\n                e\n            )\n        )\n\n    finally:\n        try:\n            if pagina is not None:\n                pagina.close()\n        except Exception:\n            pass\n\n\ndef _v102_marcar_local_eliminado(candidato):\n    try:\n        datos = cargar_publicados()\n\n        if not isinstance(\n            datos,\n            dict\n        ):\n            return\n\n        registro_id = candidato.get(\n            "_registro_id"\n        )\n\n        if (\n            registro_id is not None\n            and registro_id in datos\n            and isinstance(\n                datos[\n                    registro_id\n                ],\n                dict\n            )\n        ):\n            datos[\n                registro_id\n            ][\n                "estado"\n            ] = "ELIMINADO_ROTACION"\n\n            datos[\n                registro_id\n            ][\n                "fecha_eliminacion"\n            ] = time.strftime(\n                "%d/%m/%Y %H:%M:%S"\n            )\n\n            _v102_guardar_publicados_compat(\n                datos\n            )\n\n            return\n\n        titulo = _v102_titulo_candidato(\n            candidato\n        )\n\n        for clave, item in datos.items():\n            if not isinstance(\n                item,\n                dict\n            ):\n                continue\n\n            if (\n                titulo\n                and str(\n                    item.get(\n                        "titulo",\n                        ""\n                    )\n                ).strip()\n                == titulo\n            ):\n                item[\n                    "estado"\n                ] = "ELIMINADO_ROTACION"\n\n                item[\n                    "fecha_eliminacion"\n                ] = time.strftime(\n                    "%d/%m/%Y %H:%M:%S"\n                )\n\n                _v102_guardar_publicados_compat(\n                    datos\n                )\n\n                return\n\n    except Exception as e:\n        _v102_log_rotacion(\n            "Wallapop eliminó el anuncio, pero no pude actualizar publicados.json: "\n            + str(\n                e\n            )\n        )\n\n\ndef _v102_rotar_una_vez():\n    if not _v102_rotacion_activa():\n        return False, (\n            "Rotación desactivada."\n        )\n\n    candidatos = _v102_candidatos_locales()\n\n    if not candidatos:\n        return False, (\n            "No hay candidatos seguros elegibles."\n        )\n\n    # Si varios anuncios comparten título y ninguno tiene URL conocida,\n    # no borrar a ciegas. Preferimos el candidato más antiguo identificable.\n    conteo_titulos = {}\n\n    for _cand in candidatos:\n        _t = _v102_normalizar_texto(\n            _v102_titulo_candidato(\n                _cand\n            )\n        )\n\n        if _t:\n            conteo_titulos[_t] = (\n                conteo_titulos.get(\n                    _t,\n                    0\n                )\n                + 1\n            )\n\n    candidato = None\n\n    for _cand in candidatos:\n        _t = _v102_normalizar_texto(\n            _v102_titulo_candidato(\n                _cand\n            )\n        )\n\n        _url = _v102_url_candidato(\n            _cand\n        )\n\n        if _url or conteo_titulos.get(\n            _t,\n            0\n        ) <= 1:\n            candidato = _cand\n            break\n\n    if candidato is None:\n        return False, (\n            "Los candidatos más antiguos tienen títulos duplicados y no "\n            "hay URL suficiente para distinguirlos con seguridad."\n        )\n\n    ok, detalle = _v102_eliminar_en_wallapop(\n        candidato\n    )\n\n    if not ok:\n        return False, (\n            "Candidato "\n            + (\n                _v102_titulo_candidato(\n                    candidato\n                )\n                or "(sin título)"\n            )\n            + ": "\n            + str(\n                detalle\n            )\n        )\n\n    _v102_marcar_local_eliminado(\n        candidato\n    )\n\n    _v102_log_rotacion(\n        "Eliminado correctamente: "\n        + (\n            _v102_titulo_candidato(\n                candidato\n            )\n            or "(sin título)"\n        )\n    )\n\n    # Esperar a que Wallapop libere el hueco.\n    time.sleep(\n        2.0\n    )\n\n    return True, (\n        "Eliminado "\n        + (\n            _v102_titulo_candidato(\n                candidato\n            )\n            or "(sin título)"\n        )\n        + ". Hueco liberado."\n    )\n\n\ndef _v102_error_item(item):\n    """\n    Devuelve TODO el contenido relevante del item.\n    V91 no depende de que LIMITE_CATALOGO esté en una clave concreta.\n    """\n    if not isinstance(\n        item,\n        dict\n    ):\n        return ""\n\n    partes = []\n\n    for clave, valor in item.items():\n        if valor in (\n            None,\n            "",\n            [],\n            {},\n        ):\n            continue\n\n        try:\n            if isinstance(\n                valor,\n                (\n                    dict,\n                    list,\n                    tuple,\n                )\n            ):\n                partes.append(\n                    json.dumps(\n                        valor,\n                        ensure_ascii=False,\n                        default=str\n                    )\n                )\n            else:\n                partes.append(\n                    str(\n                        valor\n                    )\n                )\n        except Exception:\n            try:\n                partes.append(\n                    repr(\n                        valor\n                    )\n                )\n            except Exception:\n                pass\n\n    try:\n        partes.append(\n            json.dumps(\n                item,\n                ensure_ascii=False,\n                default=str\n            )\n        )\n    except Exception:\n        pass\n\n    return " | ".join(\n        partes\n    )\n\n\ndef _v102_reactivar_item(item, detalle):\n    try:\n        item[\n            "estado_cola"\n        ] = "PENDIENTE"\n\n        item[\n            "error"\n        ] = ""\n\n        item[\n            "ultimo_error"\n        ] = ""\n\n        item[\n            "detalle_error"\n        ] = ""\n\n        item[\n            "rotacion_detalle"\n        ] = str(\n            detalle\n        )\n\n        # La rotación ya corrigió la causa externa; el mismo anuncio debe poder\n        # reintentarse una vez sin consumir otro intento manual.\n        try:\n            intentos = int(\n                item.get(\n                    "intentos",\n                    0\n                )\n                or 0\n            )\n\n            if intentos > 0:\n                item[\n                    "intentos"\n                ] = intentos - 1\n        except Exception:\n            pass\n\n        try:\n            guardar_cola()\n        except Exception:\n            pass\n\n        try:\n            refrescar_cola()\n        except Exception:\n            pass\n\n    except Exception as e:\n        _v102_log_rotacion(\n            "No pude reactivar el anuncio: "\n            + str(\n                e\n            )\n        )\n\n\ndef _v102_reanudar_cola():\n    for nombre in (\n        "reanudar_cola",\n        "iniciar_cola",\n        "procesar_cola",\n    ):\n        funcion = globals().get(\n            nombre\n        )\n\n        if callable(\n            funcion\n        ):\n            try:\n                funcion()\n                return True\n            except Exception:\n                pass\n\n    return False\n\n\ndef _v102_worker_limite(item, clave):\n    global _ROTACION_V102_EN_CURSO\n\n    if not _ROTACION_V102_LOCK.acquire(\n        blocking=False\n    ):\n        return\n\n    _ROTACION_V102_EN_CURSO = True\n\n    try:\n        _v102_log_rotacion(\n            "LIMITE_CATALOGO detectado. Iniciando rotación segura."\n        )\n\n        ok, detalle = _v102_rotar_una_vez()\n\n        if ok:\n            _v102_reactivar_item(\n                item,\n                detalle\n            )\n\n            _v102_log_rotacion(\n                detalle\n                + " Reintentando el mismo anuncio."\n            )\n\n            try:\n                ventana.after(\n                    300,\n                    _v102_reanudar_cola\n                )\n            except Exception:\n                pass\n\n        else:\n            try:\n                item[\n                    "rotacion_detalle"\n                ] = str(\n                    detalle\n                )\n\n                # Mantener el error completo visible en el registro.\n                _detalle_corto = str(\n                    detalle\n                ).replace(\n                    "\\n",\n                    " "\n                )\n\n                item[\n                    "detalle_error"\n                ] = (\n                    "LIMITE_CATALOGO · V102: "\n                    + _detalle_corto[:180]\n                )\n\n                item[\n                    "rotacion_detalle"\n                ] = (\n                    "V102: "\n                    + _detalle_corto[:180]\n                )\n\n                try:\n                    guardar_cola()\n                except Exception:\n                    pass\n\n                try:\n                    ventana.after(\n                        0,\n                        refrescar_cola\n                    )\n                except Exception:\n                    pass\n\n            except Exception:\n                pass\n\n            _v102_log_rotacion(\n                "Rotación fallida: "\n                + str(\n                    detalle\n                )\n            )\n\n    finally:\n        _ROTACION_V102_EN_CURSO = False\n\n        try:\n            _ROTACION_V102_PROCESADOS.add(\n                clave\n            )\n        except Exception:\n            pass\n\n        try:\n            _ROTACION_V102_LOCK.release()\n        except Exception:\n            pass\n\n\ndef _v102_vigilar_limite_catalogo():\n    try:\n        if (\n            _v102_rotacion_activa()\n            and not _ROTACION_V102_EN_CURSO\n        ):\n            for indice, item in enumerate(\n                list(\n                    cola_anuncios\n                )\n            ):\n                if not isinstance(\n                    item,\n                    dict\n                ):\n                    continue\n\n                estado = str(\n                    item.get(\n                        "estado_cola",\n                        ""\n                    )\n                ).upper()\n\n                if estado != "ERROR":\n                    continue\n\n                error = _v102_error_item(\n                    item\n                ).upper()\n\n                if "LIMITE_CATALOGO" not in error:\n                    continue\n\n                clave = (\n                    str(\n                        item.get(\n                            "id",\n                            ""\n                        )\n                    )\n                    + "|"\n                    + str(\n                        item.get(\n                            "titulo",\n                            ""\n                        )\n                    )\n                    + "|"\n                    + str(\n                        item.get(\n                            "intentos",\n                            ""\n                        )\n                    )\n                    + "|"\n                    + str(\n                        indice\n                    )\n                )\n\n                if clave in _ROTACION_V102_PROCESADOS:\n                    continue\n\n                try:\n                    item[\n                        "rotacion_detalle"\n                    ] = "V102: límite detectado; buscando candidato seguro..."\n\n                    guardar_cola()\n                    refrescar_cola()\n                except Exception:\n                    pass\n\n                threading.Thread(\n                    target=_v102_worker_limite,\n                    args=(\n                        item,\n                        clave,\n                    ),\n                    daemon=True\n                ).start()\n\n                break\n\n    except Exception as e:\n        _v102_log_rotacion(\n            "Watchdog: "\n            + str(\n                e\n            )\n        )\n\n    finally:\n        try:\n            ventana.after(\n                900,\n                _v102_vigilar_limite_catalogo\n            )\n        except Exception:\n            pass\n\n\ntry:\n    ventana.after(\n        1800,\n        _v102_vigilar_limite_catalogo\n    )\nexcept Exception as _e_v102_rot:\n    print(\n        "[ROTACION V102] No pude iniciar watchdog:",\n        _e_v102_rot\n    )\n\n\n\n# =========================================================\n# V102.1 · BORRADO REAL DESDE PRODUCTOS\n# =========================================================\n\ndef _v1021_norm(valor):\n    try:\n        import unicodedata\n        txt = unicodedata.normalize("NFKD", str(valor or ""))\n        txt = "".join(c for c in txt if not unicodedata.combining(c))\n        return " ".join(txt.lower().split())\n    except Exception:\n        return " ".join(str(valor or "").lower().split())\n\n\ndef _v1021_click_confirmacion(page):\n    textos = (\n        "Sí, eliminar",\n        "Si, eliminar",\n        "Eliminar definitivamente",\n        "Eliminar producto",\n        "Eliminar anuncio",\n        "Confirmar eliminación",\n        "Confirmar eliminacion",\n        "Eliminar",\n        "Sí",\n        "Si",\n    )\n\n    for texto in textos:\n        try:\n            loc = page.get_by_role(\n                "button",\n                name=re.compile(\n                    re.escape(texto),\n                    re.I\n                )\n            )\n\n            if loc.count() > 0:\n                for i in range(min(loc.count(), 8)):\n                    b = loc.nth(i)\n                    try:\n                        if b.is_visible():\n                            b.click(timeout=2500)\n                            return True, texto\n                    except Exception:\n                        pass\n        except Exception:\n            pass\n\n    try:\n        dialogos = page.locator(\n            \'[role="dialog"], dialog, walla-dialog, [class*="modal" i]\'\n        )\n\n        for j in range(min(dialogos.count(), 8)):\n            d = dialogos.nth(j)\n            botones = d.locator(\n                \'button, [role="button"]\'\n            )\n\n            for i in range(min(botones.count(), 30)):\n                b = botones.nth(i)\n\n                try:\n                    blob = " ".join(\n                        [\n                            b.inner_text(timeout=150) or "",\n                            b.get_attribute("aria-label") or "",\n                            b.get_attribute("title") or "",\n                            b.get_attribute("data-testid") or "",\n                        ]\n                    )\n\n                    n = _v1021_norm(blob)\n\n                    if (\n                        "eliminar" in n\n                        or "borrar" in n\n                        or "delete" in n\n                        or n in ("si", "yes", "confirmar")\n                    ):\n                        b.click(timeout=2500)\n                        return True, blob.strip()\n                except Exception:\n                    pass\n    except Exception:\n        pass\n\n    return False, ""\n\n\ndef _v1021_card_desde_titulo(page, titulo):\n    """\n    Devuelve el contenedor visual más cercano al título en Productos.\n    """\n    titulo_n = _v1021_norm(titulo)\n\n    if not titulo_n:\n        return None\n\n    try:\n        candidatos = page.get_by_text(\n            titulo,\n            exact=True\n        )\n\n        if candidatos.count() <= 0:\n            candidatos = page.get_by_text(\n                titulo,\n                exact=False\n            )\n\n        for i in range(min(candidatos.count(), 20)):\n            el = candidatos.nth(i)\n\n            try:\n                if not el.is_visible():\n                    continue\n            except Exception:\n                pass\n\n            try:\n                card = el.locator(\n                    "xpath=ancestor::*[self::article or self::li or @role=\'listitem\' or contains(@class,\'card\') or contains(@class,\'item\')][1]"\n                )\n\n                if card.count() > 0:\n                    return card.first\n            except Exception:\n                pass\n\n            try:\n                handle = el.evaluate_handle("""\n                    node => {\n                        let p = node;\n                        for (let i = 0; i < 8 && p; i++, p = p.parentElement) {\n                            const txt = (p.innerText || "").trim();\n                            const buttons = p.querySelectorAll(\'button,[role="button"],a\').length;\n                            if (buttons > 0 && txt.length > 0 && txt.length < 2500) {\n                                return p;\n                            }\n                        }\n                        return node.parentElement || node;\n                    }\n                """)\n\n                as_el = handle.as_element()\n\n                if as_el is not None:\n                    return as_el\n            except Exception:\n                pass\n\n    except Exception:\n        pass\n\n    return None\n\n\ndef _v1021_click_borrar_en_scope(page, scope):\n    """\n    Busca primero borrar/papelera dentro de la tarjeta correcta.\n    """\n    patrones_borrar = (\n        "eliminar",\n        "borrar",\n        "delete",\n        "trash",\n        "remove",\n        "papelera",\n    )\n\n    patrones_menu = (\n        "mas",\n        "más",\n        "opciones",\n        "menu",\n        "more",\n        "overflow",\n        "ellipsis",\n        "kebab",\n    )\n\n    selectores = (\n        \'button\',\n        \'[role="button"]\',\n        \'a\',\n        \'[role="menuitem"]\',\n        \'walla-icon\',\n    )\n\n    try:\n        controles = scope.locator(\n            ", ".join(selectores)\n        )\n\n        for i in range(min(controles.count(), 180)):\n            el = controles.nth(i)\n\n            try:\n                blob = " ".join(\n                    [\n                        el.inner_text(timeout=120) or "",\n                        el.get_attribute("aria-label") or "",\n                        el.get_attribute("title") or "",\n                        el.get_attribute("data-testid") or "",\n                        el.get_attribute("name") or "",\n                        el.get_attribute("class") or "",\n                        el.evaluate("e => e.outerHTML") or "",\n                    ]\n                )\n\n                n = _v1021_norm(blob)\n\n                if any(p in n for p in patrones_borrar):\n                    target = el\n\n                    try:\n                        tag = str(\n                            el.evaluate(\n                                "e => e.tagName.toLowerCase()"\n                            )\n                        )\n\n                        if tag in ("walla-icon", "svg", "path"):\n                            anc = el.locator(\n                                "xpath=ancestor::button[1]"\n                            )\n\n                            if anc.count() > 0:\n                                target = anc.first\n                    except Exception:\n                        pass\n\n                    target.scroll_into_view_if_needed()\n                    target.click(timeout=3000)\n                    return True, "directo:" + n[:120]\n            except Exception:\n                pass\n    except Exception:\n        pass\n\n    # Si no hay papelera directa, abrir el menú SOLO de esa tarjeta.\n    try:\n        controles = scope.locator(\n            \'button, [role="button"], a\'\n        )\n\n        for i in range(min(controles.count(), 120)):\n            el = controles.nth(i)\n\n            try:\n                blob = " ".join(\n                    [\n                        el.inner_text(timeout=100) or "",\n                        el.get_attribute("aria-label") or "",\n                        el.get_attribute("title") or "",\n                        el.get_attribute("data-testid") or "",\n                        el.get_attribute("name") or "",\n                        el.get_attribute("class") or "",\n                        el.evaluate("e => e.outerHTML") or "",\n                    ]\n                )\n\n                n = _v1021_norm(blob)\n\n                if any(p in n for p in patrones_menu):\n                    el.scroll_into_view_if_needed()\n                    el.click(timeout=2500)\n                    page.wait_for_timeout(350)\n\n                    menus = page.locator(\n                        \'[role="menuitem"], [role="menu"] button, [role="dialog"] button, button, [role="button"]\'\n                    )\n\n                    for j in range(min(menus.count(), 220)):\n                        m = menus.nth(j)\n\n                        try:\n                            blob2 = " ".join(\n                                [\n                                    m.inner_text(timeout=100) or "",\n                                    m.get_attribute("aria-label") or "",\n                                    m.get_attribute("title") or "",\n                                    m.get_attribute("data-testid") or "",\n                                    m.evaluate("e => e.outerHTML") or "",\n                                ]\n                            )\n\n                            n2 = _v1021_norm(blob2)\n\n                            if any(p in n2 for p in patrones_borrar):\n                                m.click(timeout=2500)\n                                return True, "menu:" + n2[:120]\n                        except Exception:\n                            pass\n            except Exception:\n                pass\n    except Exception:\n        pass\n\n    return False, ""\n\n\ndef _v1021_ir_a_productos(page):\n    """\n    Navega a la zona de productos usando la misma ruta que ya funciona en\n    la Prueba de rotación.\n    """\n    try:\n        page.goto(\n            "https://es.wallapop.com/",\n            wait_until="domcontentloaded",\n            timeout=35000\n        )\n    except Exception:\n        pass\n\n    try:\n        page.wait_for_timeout(900)\n    except Exception:\n        pass\n\n    # Abrir Tú / perfil.\n    try:\n        _v102_click_visible(\n            page,\n            (\n                "Tú",\n                "Tu perfil",\n                "Perfil",\n            ),\n            timeout=3500\n        )\n    except Exception:\n        pass\n\n    try:\n        page.wait_for_timeout(700)\n    except Exception:\n        pass\n\n    # Ir a Productos.\n    try:\n        _v102_click_visible(\n            page,\n            (\n                "Productos",\n                "Mis productos",\n                "En venta",\n                "Mis anuncios",\n            ),\n            timeout=4000\n        )\n    except Exception:\n        pass\n\n    try:\n        page.wait_for_timeout(1100)\n    except Exception:\n        pass\n\n\ndef _v1021_borrar_desde_productos(candidato):\n    titulo = _v102_titulo_candidato(\n        candidato\n    )\n\n    pagina = None\n\n    try:\n        from playwright.sync_api import sync_playwright\n\n        with sync_playwright() as p:\n            try:\n                _asegurar_chromium_controlable()\n            except Exception:\n                pass\n\n            try:\n                navegador = _conectar_playwright_cdp(\n                    p\n                )\n            except Exception:\n                navegador = p.chromium.connect_over_cdp(\n                    "http://127.0.0.1:9222",\n                    timeout=60000\n                )\n\n            if not navegador.contexts:\n                return False, "Chrome/CDP sin contexto autenticado."\n\n            contexto = navegador.contexts[0]\n            pagina = contexto.new_page()\n\n            _v1021_ir_a_productos(\n                pagina\n            )\n\n            # Buscar el candidato. La prueba de rotación ya demuestra que esto\n            # funciona; repetimos el mismo enfoque y actuamos SOLO en su tarjeta.\n            card = None\n\n            for _scroll in range(20):\n                card = _v1021_card_desde_titulo(\n                    pagina,\n                    titulo\n                )\n\n                if card is not None:\n                    break\n\n                try:\n                    pagina.mouse.wheel(\n                        0,\n                        950\n                    )\n                    pagina.wait_for_timeout(\n                        300\n                    )\n                except Exception:\n                    break\n\n            if card is None:\n                return False, (\n                    "La prueba podía localizar el candidato, pero el borrado no encontró su tarjeta."\n                )\n\n            try:\n                card.scroll_into_view_if_needed()\n            except Exception:\n                pass\n\n            # Guardar URL real del anuncio si la tarjeta la expone.\n            try:\n                enlace = card.locator(\n                    \'a[href*="/item/"]\'\n                )\n\n                if enlace.count() > 0:\n                    href = enlace.first.get_attribute(\n                        "href"\n                    )\n\n                    if href:\n                        if str(href).startswith("/"):\n                            href = (\n                                "https://es.wallapop.com"\n                                + str(href)\n                            )\n\n                        _v102_guardar_url_candidato(\n                            candidato,\n                            str(href)\n                        )\n            except Exception:\n                pass\n\n            ok, detalle = _v1021_click_borrar_en_scope(\n                pagina,\n                card\n            )\n\n            if not ok:\n                # Como último intento, abrir la tarjeta y buscar opciones en detalle.\n                try:\n                    enlace = card.locator(\n                        \'a[href*="/item/"]\'\n                    )\n\n                    if enlace.count() > 0:\n                        href = enlace.first.get_attribute(\n                            "href"\n                        )\n\n                        if href:\n                            if str(href).startswith("/"):\n                                href = "https://es.wallapop.com" + str(href)\n\n                            pagina.goto(\n                                str(href),\n                                wait_until="domcontentloaded",\n                                timeout=35000\n                            )\n                            pagina.wait_for_timeout(\n                                900\n                            )\n\n                            ok, detalle = _v1021_click_borrar_en_scope(\n                                pagina,\n                                pagina.locator(\n                                    "body"\n                                )\n                            )\n                except Exception:\n                    pass\n\n            if not ok:\n                # Diagnóstico útil de la tarjeta exacta.\n                try:\n                    diag = (\n                        Path(\n                            CARPETA_DATOS_USUARIO\n                        )\n                        / "diagnosticos_rotacion"\n                        / (\n                            "v102_1_"\n                            + time.strftime(\n                                "%Y%m%d_%H%M%S"\n                            )\n                        )\n                    )\n                    diag.mkdir(\n                        parents=True,\n                        exist_ok=True\n                    )\n\n                    pagina.screenshot(\n                        path=str(\n                            diag / "captura.png"\n                        ),\n                        full_page=True\n                    )\n\n                    (diag / "pagina.html").write_text(\n                        pagina.content(),\n                        encoding="utf-8",\n                        errors="ignore"\n                    )\n\n                    try:\n                        html_card = card.evaluate(\n                            "e => e.outerHTML"\n                        )\n                    except Exception:\n                        html_card = ""\n\n                    (diag / "tarjeta.html").write_text(\n                        str(\n                            html_card\n                        ),\n                        encoding="utf-8",\n                        errors="ignore"\n                    )\n\n                    _v102_log_rotacion(\n                        "V102.1 diagnóstico exacto guardado en "\n                        + str(\n                            diag\n                        )\n                    )\n                except Exception:\n                    pass\n\n                return False, (\n                    "Localicé el anuncio, pero Wallapop no expuso un control de borrado en su tarjeta ni en el detalle."\n                )\n\n            pagina.wait_for_timeout(\n                450\n            )\n\n            confirmado, texto_conf = _v1021_click_confirmacion(\n                pagina\n            )\n\n            if confirmado:\n                _v102_log_rotacion(\n                    "V102.1 confirmación pulsada: "\n                    + str(\n                        texto_conf\n                    )\n                )\n\n            pagina.wait_for_timeout(\n                1800\n            )\n\n            # Verificar realmente que ya no aparece en Productos.\n            _v1021_ir_a_productos(\n                pagina\n            )\n\n            pagina.wait_for_timeout(\n                900\n            )\n\n            sigue = _v1021_card_desde_titulo(\n                pagina,\n                titulo\n            )\n\n            if sigue is None:\n                return True, (\n                    "Anuncio eliminado y ausencia confirmada en Productos."\n                )\n\n            # Si hay títulos duplicados puede seguir apareciendo otro igual.\n            # En ese caso comprobar si la URL guardada ya no está en la tarjeta.\n            url_objetivo = _v102_url_candidato(\n                candidato\n            )\n\n            if url_objetivo:\n                try:\n                    hrefs = pagina.locator(\n                        \'a[href*="/item/"]\'\n                    )\n\n                    encontrado = False\n\n                    for i in range(min(hrefs.count(), 600)):\n                        href = hrefs.nth(i).get_attribute(\n                            "href"\n                        )\n\n                        if href and str(\n                            url_objetivo\n                        ).rstrip("/") in str(\n                            href\n                        ):\n                            encontrado = True\n                            break\n\n                    if not encontrado:\n                        return True, (\n                            "Anuncio eliminado; la URL objetivo ya no aparece en Productos."\n                        )\n                except Exception:\n                    pass\n\n            return False, (\n                "Se pulsó borrar, pero no pude confirmar que el anuncio desapareciera."\n            )\n\n    except Exception as e:\n        return False, (\n            type(\n                e\n            ).__name__\n            + ": "\n            + str(\n                e\n            )\n        )\n\n    finally:\n        try:\n            if pagina is not None:\n                pagina.close()\n        except Exception:\n            pass\n\n\n# Conservar el motor anterior como fallback.\n_v102_eliminar_en_wallapop_anterior = globals().get(\n    "_v102_eliminar_en_wallapop"\n)\n\n\ndef _v102_eliminar_en_wallapop(candidato):\n    _v102_log_rotacion(\n        "V102.1: intentando borrado real desde Productos."\n    )\n\n    ok, detalle = _v1021_borrar_desde_productos(\n        candidato\n    )\n\n    if ok:\n        return True, detalle\n\n    _v102_log_rotacion(\n        "V102.1 primer método falló: "\n        + str(\n            detalle\n        )\n    )\n\n    anterior = globals().get(\n        "_v102_eliminar_en_wallapop_anterior"\n    )\n\n    if callable(\n        anterior\n    ):\n        try:\n            return anterior(\n                candidato\n            )\n        except Exception as e:\n            return False, (\n                str(\n                    detalle\n                )\n                + " · Fallback: "\n                + type(\n                    e\n                ).__name__\n                + ": "\n                + str(\n                    e\n                )\n            )\n\n    return False, detalle\n\n\n\n# =========================================================\n# V103 · BORRADO REAL DESDE PRODUCTOS\n# =========================================================\n\ndef _v1021_norm(valor):\n    try:\n        import unicodedata\n        txt = unicodedata.normalize("NFKD", str(valor or ""))\n        txt = "".join(c for c in txt if not unicodedata.combining(c))\n        return " ".join(txt.lower().split())\n    except Exception:\n        return " ".join(str(valor or "").lower().split())\n\n\ndef _v1021_click_confirmacion(page):\n    textos = (\n        "Sí, eliminar",\n        "Si, eliminar",\n        "Eliminar definitivamente",\n        "Eliminar producto",\n        "Eliminar anuncio",\n        "Confirmar eliminación",\n        "Confirmar eliminacion",\n        "Eliminar",\n        "Sí",\n        "Si",\n    )\n\n    for texto in textos:\n        try:\n            loc = page.get_by_role(\n                "button",\n                name=re.compile(\n                    re.escape(texto),\n                    re.I\n                )\n            )\n\n            if loc.count() > 0:\n                for i in range(min(loc.count(), 8)):\n                    b = loc.nth(i)\n                    try:\n                        if b.is_visible():\n                            b.click(timeout=2500)\n                            return True, texto\n                    except Exception:\n                        pass\n        except Exception:\n            pass\n\n    try:\n        dialogos = page.locator(\n            \'[role="dialog"], dialog, walla-dialog, [class*="modal" i]\'\n        )\n\n        for j in range(min(dialogos.count(), 8)):\n            d = dialogos.nth(j)\n            botones = d.locator(\n                \'button, [role="button"]\'\n            )\n\n            for i in range(min(botones.count(), 30)):\n                b = botones.nth(i)\n\n                try:\n                    blob = " ".join(\n                        [\n                            b.inner_text(timeout=150) or "",\n                            b.get_attribute("aria-label") or "",\n                            b.get_attribute("title") or "",\n                            b.get_attribute("data-testid") or "",\n                        ]\n                    )\n\n                    n = _v1021_norm(blob)\n\n                    if (\n                        "eliminar" in n\n                        or "borrar" in n\n                        or "delete" in n\n                        or n in ("si", "yes", "confirmar")\n                    ):\n                        b.click(timeout=2500)\n                        return True, blob.strip()\n                except Exception:\n                    pass\n    except Exception:\n        pass\n\n    return False, ""\n\n\ndef _v1021_card_desde_titulo(page, titulo):\n    """\n    Devuelve el contenedor visual más cercano al título en Productos.\n    """\n    titulo_n = _v1021_norm(titulo)\n\n    if not titulo_n:\n        return None\n\n    try:\n        candidatos = page.get_by_text(\n            titulo,\n            exact=True\n        )\n\n        if candidatos.count() <= 0:\n            candidatos = page.get_by_text(\n                titulo,\n                exact=False\n            )\n\n        for i in range(min(candidatos.count(), 20)):\n            el = candidatos.nth(i)\n\n            try:\n                if not el.is_visible():\n                    continue\n            except Exception:\n                pass\n\n            try:\n                card = el.locator(\n                    "xpath=ancestor::*[self::article or self::li or @role=\'listitem\' or contains(@class,\'card\') or contains(@class,\'item\')][1]"\n                )\n\n                if card.count() > 0:\n                    return card.first\n            except Exception:\n                pass\n\n            try:\n                handle = el.evaluate_handle("""\n                    node => {\n                        let p = node;\n                        for (let i = 0; i < 8 && p; i++, p = p.parentElement) {\n                            const txt = (p.innerText || "").trim();\n                            const buttons = p.querySelectorAll(\'button,[role="button"],a\').length;\n                            if (buttons > 0 && txt.length > 0 && txt.length < 2500) {\n                                return p;\n                            }\n                        }\n                        return node.parentElement || node;\n                    }\n                """)\n\n                as_el = handle.as_element()\n\n                if as_el is not None:\n                    return as_el\n            except Exception:\n                pass\n\n    except Exception:\n        pass\n\n    return None\n\n\ndef _v1021_click_borrar_en_scope(page, scope):\n    """\n    Busca primero borrar/papelera dentro de la tarjeta correcta.\n    """\n    patrones_borrar = (\n        "eliminar",\n        "borrar",\n        "delete",\n        "trash",\n        "remove",\n        "papelera",\n    )\n\n    patrones_menu = (\n        "mas",\n        "más",\n        "opciones",\n        "menu",\n        "more",\n        "overflow",\n        "ellipsis",\n        "kebab",\n    )\n\n    selectores = (\n        \'button\',\n        \'[role="button"]\',\n        \'a\',\n        \'[role="menuitem"]\',\n        \'walla-icon\',\n    )\n\n    try:\n        controles = scope.locator(\n            ", ".join(selectores)\n        )\n\n        for i in range(min(controles.count(), 180)):\n            el = controles.nth(i)\n\n            try:\n                blob = " ".join(\n                    [\n                        el.inner_text(timeout=120) or "",\n                        el.get_attribute("aria-label") or "",\n                        el.get_attribute("title") or "",\n                        el.get_attribute("data-testid") or "",\n                        el.get_attribute("name") or "",\n                        el.get_attribute("class") or "",\n                        el.evaluate("e => e.outerHTML") or "",\n                    ]\n                )\n\n                n = _v1021_norm(blob)\n\n                if any(p in n for p in patrones_borrar):\n                    target = el\n\n                    try:\n                        tag = str(\n                            el.evaluate(\n                                "e => e.tagName.toLowerCase()"\n                            )\n                        )\n\n                        if tag in ("walla-icon", "svg", "path"):\n                            anc = el.locator(\n                                "xpath=ancestor::button[1]"\n                            )\n\n                            if anc.count() > 0:\n                                target = anc.first\n                    except Exception:\n                        pass\n\n                    target.scroll_into_view_if_needed()\n                    target.click(timeout=3000)\n                    return True, "directo:" + n[:120]\n            except Exception:\n                pass\n    except Exception:\n        pass\n\n    # Si no hay papelera directa, abrir el menú SOLO de esa tarjeta.\n    try:\n        controles = scope.locator(\n            \'button, [role="button"], a\'\n        )\n\n        for i in range(min(controles.count(), 120)):\n            el = controles.nth(i)\n\n            try:\n                blob = " ".join(\n                    [\n                        el.inner_text(timeout=100) or "",\n                        el.get_attribute("aria-label") or "",\n                        el.get_attribute("title") or "",\n                        el.get_attribute("data-testid") or "",\n                        el.get_attribute("name") or "",\n                        el.get_attribute("class") or "",\n                        el.evaluate("e => e.outerHTML") or "",\n                    ]\n                )\n\n                n = _v1021_norm(blob)\n\n                if any(p in n for p in patrones_menu):\n                    el.scroll_into_view_if_needed()\n                    el.click(timeout=2500)\n                    page.wait_for_timeout(350)\n\n                    menus = page.locator(\n                        \'[role="menuitem"], [role="menu"] button, [role="dialog"] button, button, [role="button"]\'\n                    )\n\n                    for j in range(min(menus.count(), 220)):\n                        m = menus.nth(j)\n\n                        try:\n                            blob2 = " ".join(\n                                [\n                                    m.inner_text(timeout=100) or "",\n                                    m.get_attribute("aria-label") or "",\n                                    m.get_attribute("title") or "",\n                                    m.get_attribute("data-testid") or "",\n                                    m.evaluate("e => e.outerHTML") or "",\n                                ]\n                            )\n\n                            n2 = _v1021_norm(blob2)\n\n                            if any(p in n2 for p in patrones_borrar):\n                                m.click(timeout=2500)\n                                return True, "menu:" + n2[:120]\n                        except Exception:\n                            pass\n            except Exception:\n                pass\n    except Exception:\n        pass\n\n    return False, ""\n\n\ndef _v1021_ir_a_productos(page):\n    """\n    Navega a la zona de productos usando la misma ruta que ya funciona en\n    la Prueba de rotación.\n    """\n    try:\n        page.goto(\n            "https://es.wallapop.com/",\n            wait_until="domcontentloaded",\n            timeout=35000\n        )\n    except Exception:\n        pass\n\n    try:\n        page.wait_for_timeout(900)\n    except Exception:\n        pass\n\n    # Abrir Tú / perfil.\n    try:\n        _v102_click_visible(\n            page,\n            (\n                "Tú",\n                "Tu perfil",\n                "Perfil",\n            ),\n            timeout=3500\n        )\n    except Exception:\n        pass\n\n    try:\n        page.wait_for_timeout(700)\n    except Exception:\n        pass\n\n    # Ir a Productos.\n    try:\n        _v102_click_visible(\n            page,\n            (\n                "Productos",\n                "Mis productos",\n                "En venta",\n                "Mis anuncios",\n            ),\n            timeout=4000\n        )\n    except Exception:\n        pass\n\n    try:\n        page.wait_for_timeout(1100)\n    except Exception:\n        pass\n\n\ndef _v1021_borrar_desde_productos(candidato):\n    titulo = _v102_titulo_candidato(\n        candidato\n    )\n\n    pagina = None\n\n    try:\n        from playwright.sync_api import sync_playwright\n\n        with sync_playwright() as p:\n            try:\n                _asegurar_chromium_controlable()\n            except Exception:\n                pass\n\n            try:\n                navegador = _conectar_playwright_cdp(\n                    p\n                )\n            except Exception:\n                navegador = p.chromium.connect_over_cdp(\n                    "http://127.0.0.1:9222",\n                    timeout=60000\n                )\n\n            if not navegador.contexts:\n                return False, "Chrome/CDP sin contexto autenticado."\n\n            contexto = navegador.contexts[0]\n            pagina = contexto.new_page()\n\n            _v1021_ir_a_productos(\n                pagina\n            )\n\n            # Buscar el candidato. La prueba de rotación ya demuestra que esto\n            # funciona; repetimos el mismo enfoque y actuamos SOLO en su tarjeta.\n            card = None\n\n            for _scroll in range(20):\n                card = _v1021_card_desde_titulo(\n                    pagina,\n                    titulo\n                )\n\n                if card is not None:\n                    break\n\n                try:\n                    pagina.mouse.wheel(\n                        0,\n                        950\n                    )\n                    pagina.wait_for_timeout(\n                        300\n                    )\n                except Exception:\n                    break\n\n            if card is None:\n                return False, (\n                    "La prueba podía localizar el candidato, pero el borrado no encontró su tarjeta."\n                )\n\n            try:\n                card.scroll_into_view_if_needed()\n            except Exception:\n                pass\n\n            # Guardar URL real del anuncio si la tarjeta la expone.\n            try:\n                enlace = card.locator(\n                    \'a[href*="/item/"]\'\n                )\n\n                if enlace.count() > 0:\n                    href = enlace.first.get_attribute(\n                        "href"\n                    )\n\n                    if href:\n                        if str(href).startswith("/"):\n                            href = (\n                                "https://es.wallapop.com"\n                                + str(href)\n                            )\n\n                        _v102_guardar_url_candidato(\n                            candidato,\n                            str(href)\n                        )\n            except Exception:\n                pass\n\n            ok, detalle = _v1021_click_borrar_en_scope(\n                pagina,\n                card\n            )\n\n            if not ok:\n                # Como último intento, abrir la tarjeta y buscar opciones en detalle.\n                try:\n                    enlace = card.locator(\n                        \'a[href*="/item/"]\'\n                    )\n\n                    if enlace.count() > 0:\n                        href = enlace.first.get_attribute(\n                            "href"\n                        )\n\n                        if href:\n                            if str(href).startswith("/"):\n                                href = "https://es.wallapop.com" + str(href)\n\n                            pagina.goto(\n                                str(href),\n                                wait_until="domcontentloaded",\n                                timeout=35000\n                            )\n                            pagina.wait_for_timeout(\n                                900\n                            )\n\n                            ok, detalle = _v1021_click_borrar_en_scope(\n                                pagina,\n                                pagina.locator(\n                                    "body"\n                                )\n                            )\n                except Exception:\n                    pass\n\n            if not ok:\n                # Diagnóstico útil de la tarjeta exacta.\n                try:\n                    diag = (\n                        Path(\n                            CARPETA_DATOS_USUARIO\n                        )\n                        / "diagnosticos_rotacion"\n                        / (\n                            "v102_1_"\n                            + time.strftime(\n                                "%Y%m%d_%H%M%S"\n                            )\n                        )\n                    )\n                    diag.mkdir(\n                        parents=True,\n                        exist_ok=True\n                    )\n\n                    pagina.screenshot(\n                        path=str(\n                            diag / "captura.png"\n                        ),\n                        full_page=True\n                    )\n\n                    (diag / "pagina.html").write_text(\n                        pagina.content(),\n                        encoding="utf-8",\n                        errors="ignore"\n                    )\n\n                    try:\n                        html_card = card.evaluate(\n                            "e => e.outerHTML"\n                        )\n                    except Exception:\n                        html_card = ""\n\n                    (diag / "tarjeta.html").write_text(\n                        str(\n                            html_card\n                        ),\n                        encoding="utf-8",\n                        errors="ignore"\n                    )\n\n                    _v102_log_rotacion(\n                        "V103 diagnóstico exacto guardado en "\n                        + str(\n                            diag\n                        )\n                    )\n                except Exception:\n                    pass\n\n                return False, (\n                    "Localicé el anuncio, pero Wallapop no expuso un control de borrado en su tarjeta ni en el detalle."\n                )\n\n            pagina.wait_for_timeout(\n                450\n            )\n\n            confirmado, texto_conf = _v1021_click_confirmacion(\n                pagina\n            )\n\n            if confirmado:\n                _v102_log_rotacion(\n                    "V103 confirmación pulsada: "\n                    + str(\n                        texto_conf\n                    )\n                )\n\n            pagina.wait_for_timeout(\n                1800\n            )\n\n            # Verificar realmente que ya no aparece en Productos.\n            _v1021_ir_a_productos(\n                pagina\n            )\n\n            pagina.wait_for_timeout(\n                900\n            )\n\n            sigue = _v1021_card_desde_titulo(\n                pagina,\n                titulo\n            )\n\n            if sigue is None:\n                return True, (\n                    "Anuncio eliminado y ausencia confirmada en Productos."\n                )\n\n            # Si hay títulos duplicados puede seguir apareciendo otro igual.\n            # En ese caso comprobar si la URL guardada ya no está en la tarjeta.\n            url_objetivo = _v102_url_candidato(\n                candidato\n            )\n\n            if url_objetivo:\n                try:\n                    hrefs = pagina.locator(\n                        \'a[href*="/item/"]\'\n                    )\n\n                    encontrado = False\n\n                    for i in range(min(hrefs.count(), 600)):\n                        href = hrefs.nth(i).get_attribute(\n                            "href"\n                        )\n\n                        if href and str(\n                            url_objetivo\n                        ).rstrip("/") in str(\n                            href\n                        ):\n                            encontrado = True\n                            break\n\n                    if not encontrado:\n                        return True, (\n                            "Anuncio eliminado; la URL objetivo ya no aparece en Productos."\n                        )\n                except Exception:\n                    pass\n\n            return False, (\n                "Se pulsó borrar, pero no pude confirmar que el anuncio desapareciera."\n            )\n\n    except Exception as e:\n        return False, (\n            type(\n                e\n            ).__name__\n            + ": "\n            + str(\n                e\n            )\n        )\n\n    finally:\n        try:\n            if pagina is not None:\n                pagina.close()\n        except Exception:\n            pass\n\n\n# Conservar el motor anterior como fallback.\n_v102_eliminar_en_wallapop_anterior = globals().get(\n    "_v102_eliminar_en_wallapop"\n)\n\n\ndef _v102_eliminar_en_wallapop(candidato):\n    _v102_log_rotacion(\n        "V103: intentando borrado real desde Productos."\n    )\n\n    ok, detalle = _v1021_borrar_desde_productos(\n        candidato\n    )\n\n    if ok:\n        return True, detalle\n\n    _v102_log_rotacion(\n        "V103 primer método falló: "\n        + str(\n            detalle\n        )\n    )\n\n    anterior = globals().get(\n        "_v102_eliminar_en_wallapop_anterior"\n    )\n\n    if callable(\n        anterior\n    ):\n        try:\n            return anterior(\n                candidato\n            )\n        except Exception as e:\n            return False, (\n                str(\n                    detalle\n                )\n                + " · Fallback: "\n                + type(\n                    e\n                ).__name__\n                + ": "\n                + str(\n                    e\n                )\n            )\n\n    return False, detalle\n\n\n\n# =========================================================\n# V103 · ROTACIÓN POR DETALLE EXACTO\n# =========================================================\n\n\ndef _v103_norm(texto):\n    try:\n        import unicodedata\n        t = unicodedata.normalize(\n            "NFKD",\n            str(texto or "")\n        )\n        t = "".join(\n            c\n            for c in t\n            if not unicodedata.combining(c)\n        )\n        return " ".join(\n            t.lower().split()\n        )\n    except Exception:\n        return " ".join(\n            str(texto or "").lower().split()\n        )\n\n\ndef _v103_href_absoluto(href):\n    href = str(\n        href or ""\n    ).strip()\n\n    if not href:\n        return ""\n\n    if href.startswith("//"):\n        return "https:" + href\n\n    if href.startswith("/"):\n        return "https://es.wallapop.com" + href\n\n    if href.startswith("http://") or href.startswith("https://"):\n        return href\n\n    return ""\n\n\ndef _v103_abrir_detalle_exacto(page, titulo):\n    """\n    Abre exactamente el anuncio cuyo título ha localizado la prueba.\n    No depende de que la URL contenga /item/.\n    """\n    titulo = str(\n        titulo or ""\n    ).strip()\n\n    if not titulo:\n        return False, "", "Título vacío"\n\n    candidatos = []\n\n    try:\n        exactos = page.get_by_text(\n            titulo,\n            exact=True\n        )\n\n        for i in range(\n            min(\n                exactos.count(),\n                30\n            )\n        ):\n            candidatos.append(\n                exactos.nth(i)\n            )\n    except Exception:\n        pass\n\n    if not candidatos:\n        try:\n            parciales = page.get_by_text(\n                titulo,\n                exact=False\n            )\n\n            for i in range(\n                min(\n                    parciales.count(),\n                    30\n                )\n            ):\n                candidatos.append(\n                    parciales.nth(i)\n                )\n        except Exception:\n            pass\n\n    for el in candidatos:\n        try:\n            if not el.is_visible():\n                continue\n        except Exception:\n            pass\n\n        # 1) El propio nodo puede ser enlace.\n        try:\n            href = _v103_href_absoluto(\n                el.get_attribute(\n                    "href"\n                )\n            )\n\n            if href:\n                page.goto(\n                    href,\n                    wait_until="domcontentloaded",\n                    timeout=45000\n                )\n\n                try:\n                    page.wait_for_load_state(\n                        "networkidle",\n                        timeout=6000\n                    )\n                except Exception:\n                    pass\n\n                page.wait_for_timeout(\n                    1200\n                )\n\n                return True, href, "href-directo"\n        except Exception:\n            pass\n\n        # 2) Buscar el enlace ancestro del título.\n        try:\n            anc = el.locator(\n                "xpath=ancestor::a[@href][1]"\n            )\n\n            if anc.count() > 0:\n                href = _v103_href_absoluto(\n                    anc.first.get_attribute(\n                        "href"\n                    )\n                )\n\n                if href:\n                    page.goto(\n                        href,\n                        wait_until="domcontentloaded",\n                        timeout=45000\n                    )\n\n                    try:\n                        page.wait_for_load_state(\n                            "networkidle",\n                            timeout=6000\n                        )\n                    except Exception:\n                        pass\n\n                    page.wait_for_timeout(\n                        1200\n                    )\n\n                    return True, href, "href-ancestro"\n        except Exception:\n            pass\n\n        # 3) Buscar enlace dentro de un contenedor próximo.\n        try:\n            scope = el.locator(\n                "xpath=ancestor::*[self::article or self::li or @role=\'listitem\' or @data-testid][1]"\n            )\n\n            if scope.count() > 0:\n                links = scope.first.locator(\n                    "a[href]"\n                )\n\n                for j in range(\n                    min(\n                        links.count(),\n                        30\n                    )\n                ):\n                    href = _v103_href_absoluto(\n                        links.nth(j).get_attribute(\n                            "href"\n                        )\n                    )\n\n                    if href:\n                        page.goto(\n                            href,\n                            wait_until="domcontentloaded",\n                            timeout=45000\n                        )\n\n                        try:\n                            page.wait_for_load_state(\n                                "networkidle",\n                                timeout=6000\n                            )\n                        except Exception:\n                            pass\n\n                        page.wait_for_timeout(\n                            1200\n                        )\n\n                        return True, href, "href-tarjeta"\n        except Exception:\n            pass\n\n        # 4) Último recurso: click real sobre el título y comprobar navegación.\n        try:\n            antes = str(\n                page.url\n            )\n\n            el.scroll_into_view_if_needed()\n            el.click(\n                timeout=4000\n            )\n\n            try:\n                page.wait_for_timeout(\n                    900\n                )\n            except Exception:\n                pass\n\n            despues = str(\n                page.url\n            )\n\n            if despues != antes:\n                try:\n                    page.wait_for_load_state(\n                        "domcontentloaded",\n                        timeout=8000\n                    )\n                except Exception:\n                    pass\n\n                return True, despues, "click-titulo"\n        except Exception:\n            pass\n\n    return False, "", "No encontré enlace/click navegable asociado al título"\n\n\ndef _v103_blob_control(el):\n    partes = []\n\n    try:\n        partes.append(\n            el.inner_text(\n                timeout=120\n            )\n            or ""\n        )\n    except Exception:\n        pass\n\n    for attr in (\n        "aria-label",\n        "title",\n        "data-testid",\n        "data-test",\n        "data-cy",\n        "name",\n        "class",\n        "id",\n        "role",\n        "alt",\n        "icon",\n        "data-icon",\n        "href",\n    ):\n        try:\n            valor = el.get_attribute(\n                attr\n            )\n\n            if valor:\n                partes.append(\n                    attr\n                    + "="\n                    + str(\n                        valor\n                    )\n                )\n        except Exception:\n            pass\n\n    try:\n        partes.append(\n            el.evaluate(\n                "e => e.outerHTML"\n            )\n            or ""\n        )\n    except Exception:\n        pass\n\n    return _v103_norm(\n        " ".join(\n            partes\n        )\n    )\n\n\ndef _v103_click_papelera_detalle(page):\n    """\n    Wallapop documenta que en web el borrado se hace desde la papelera\n    dentro del artículo. Buscamos la papelera SOLO después de abrir el\n    detalle exacto del candidato.\n    """\n    patrones_borrar = (\n        "eliminar",\n        "borrar",\n        "delete",\n        "trash",\n        "trashcan",\n        "trash-can",\n        "bin",\n        "dustbin",\n        "garbage",\n        "remove",\n        "papelera",\n        "eliminar producto",\n        "borrar producto",\n    )\n\n    selectores = (\n        "button",\n        "[role=\'button\']",\n        "a",\n        "[role=\'menuitem\']",\n        "walla-icon",\n        "svg",\n        "[data-testid]",\n    )\n\n    controles = []\n\n    try:\n        loc = page.locator(\n            ", ".join(\n                selectores\n            )\n        )\n\n        for i in range(\n            min(\n                loc.count(),\n                800\n            )\n        ):\n            controles.append(\n                loc.nth(i)\n            )\n    except Exception:\n        pass\n\n    # Primera pasada: patrón explícito en texto/atributos/HTML.\n    for el in controles:\n        try:\n            blob = _v103_blob_control(\n                el\n            )\n\n            if not any(\n                p in blob\n                for p in patrones_borrar\n            ):\n                continue\n\n            target = el\n\n            try:\n                tag = str(\n                    el.evaluate(\n                        "e => e.tagName.toLowerCase()"\n                    )\n                )\n\n                if tag in (\n                    "svg",\n                    "path",\n                    "walla-icon",\n                ):\n                    anc = el.locator(\n                        "xpath=ancestor::*[self::button or @role=\'button\' or self::a][1]"\n                    )\n\n                    if anc.count() > 0:\n                        target = anc.first\n            except Exception:\n                pass\n\n            target.scroll_into_view_if_needed()\n            target.click(\n                timeout=4000\n            )\n\n            return True, (\n                "papelera:"\n                + blob[:180]\n            )\n\n        except Exception:\n            pass\n\n    # Segunda pasada: abrir menús de opciones y volver a buscar borrar.\n    patrones_menu = (\n        "opciones",\n        "mas",\n        "more",\n        "menu",\n        "overflow",\n        "ellipsis",\n        "kebab",\n        "three dots",\n        "3 puntos",\n    )\n\n    for el in controles:\n        try:\n            blob = _v103_blob_control(\n                el\n            )\n\n            if not any(\n                p in blob\n                for p in patrones_menu\n            ):\n                continue\n\n            target = el\n\n            try:\n                tag = str(\n                    el.evaluate(\n                        "e => e.tagName.toLowerCase()"\n                    )\n                )\n\n                if tag in (\n                    "svg",\n                    "path",\n                    "walla-icon",\n                ):\n                    anc = el.locator(\n                        "xpath=ancestor::*[self::button or @role=\'button\' or self::a][1]"\n                    )\n\n                    if anc.count() > 0:\n                        target = anc.first\n            except Exception:\n                pass\n\n            target.click(\n                timeout=3000\n            )\n\n            page.wait_for_timeout(\n                450\n            )\n\n            menus = page.locator(\n                "[role=\'menuitem\'], [role=\'dialog\'] button, [role=\'dialog\'] [role=\'button\'], button, [role=\'button\']"\n            )\n\n            for j in range(\n                min(\n                    menus.count(),\n                    300\n                )\n            ):\n                item = menus.nth(j)\n\n                try:\n                    blob2 = _v103_blob_control(\n                        item\n                    )\n\n                    if any(\n                        p in blob2\n                        for p in patrones_borrar\n                    ):\n                        item.click(\n                            timeout=3500\n                        )\n\n                        return True, (\n                            "menu:"\n                            + blob2[:180]\n                        )\n                except Exception:\n                    pass\n\n        except Exception:\n            pass\n\n    return False, ""\n\n\ndef _v103_confirmar_borrado(page):\n    patrones = (\n        "eliminar",\n        "borrar",\n        "confirmar",\n        "si, eliminar",\n        "sí, eliminar",\n        "eliminar producto",\n        "borrar producto",\n        "aceptar",\n    )\n\n    try:\n        dialogos = page.locator(\n            "[role=\'dialog\']"\n        )\n\n        scopes = []\n\n        for i in range(\n            min(\n                dialogos.count(),\n                10\n            )\n        ):\n            scopes.append(\n                dialogos.nth(i)\n            )\n\n        scopes.append(\n            page.locator(\n                "body"\n            )\n        )\n\n        for scope in scopes:\n            controles = scope.locator(\n                "button, [role=\'button\']"\n            )\n\n            for i in range(\n                min(\n                    controles.count(),\n                    150\n                )\n            ):\n                el = controles.nth(i)\n\n                try:\n                    blob = _v103_blob_control(\n                        el\n                    )\n\n                    if any(\n                        p in blob\n                        for p in patrones\n                    ):\n                        # Evitar botones de cancelar.\n                        if any(\n                            x in blob\n                            for x in (\n                                "cancelar",\n                                "volver",\n                                "cerrar",\n                                "no,",\n                            )\n                        ):\n                            continue\n\n                        el.click(\n                            timeout=3500\n                        )\n\n                        return True, blob[:180]\n                except Exception:\n                    pass\n    except Exception:\n        pass\n\n    return False, ""\n\n\ndef _v103_dump_detalle(page, titulo):\n    try:\n        raiz = (\n            Path(\n                CARPETA_DATOS_USUARIO\n            )\n            / "diagnosticos_rotacion"\n            / (\n                "v103_"\n                + time.strftime(\n                    "%Y%m%d_%H%M%S"\n                )\n            )\n        )\n\n        raiz.mkdir(\n            parents=True,\n            exist_ok=True\n        )\n\n        page.screenshot(\n            path=str(\n                raiz\n                / "detalle.png"\n            ),\n            full_page=True\n        )\n\n        (\n            raiz\n            / "detalle.html"\n        ).write_text(\n            page.content(),\n            encoding="utf-8",\n            errors="ignore"\n        )\n\n        controles = []\n\n        try:\n            loc = page.locator(\n                "button, [role=\'button\'], a, walla-icon, svg, [data-testid]"\n            )\n\n            for i in range(\n                min(\n                    loc.count(),\n                    1000\n                )\n            ):\n                try:\n                    blob = _v103_blob_control(\n                        loc.nth(i)\n                    )\n\n                    if blob:\n                        controles.append(\n                            blob[:500]\n                        )\n                except Exception:\n                    pass\n        except Exception:\n            pass\n\n        (\n            raiz\n            / "controles.txt"\n        ).write_text(\n            "\\n\\n".join(\n                controles\n            ),\n            encoding="utf-8",\n            errors="ignore"\n        )\n\n        _v102_log_rotacion(\n            "V103 diagnóstico detalle guardado en "\n            + str(\n                raiz\n            )\n        )\n\n        return str(\n            raiz\n        )\n\n    except Exception:\n        return ""\n\n\ndef _v103_borrar_candidato(candidato):\n    titulo = _v102_titulo_candidato(\n        candidato\n    )\n\n    pagina = None\n\n    try:\n        from playwright.sync_api import sync_playwright\n\n        with sync_playwright() as p:\n            try:\n                _asegurar_chromium_controlable()\n            except Exception:\n                pass\n\n            try:\n                navegador = _conectar_playwright_cdp(\n                    p\n                )\n            except Exception:\n                navegador = p.chromium.connect_over_cdp(\n                    "http://127.0.0.1:9222",\n                    timeout=60000\n                )\n\n            if not navegador.contexts:\n                return False, (\n                    "Chrome/CDP no tiene contexto autenticado."\n                )\n\n            contexto = navegador.contexts[0]\n            pagina = contexto.new_page()\n\n            # Reutilizar la navegación a Productos que ya demuestra funcionar.\n            _v1021_ir_a_productos(\n                pagina\n            )\n\n            localizado = None\n\n            for _scroll in range(\n                30\n            ):\n                try:\n                    exacto = pagina.get_by_text(\n                        titulo,\n                        exact=True\n                    )\n\n                    if exacto.count() > 0:\n                        localizado = exacto.first\n                        break\n                except Exception:\n                    pass\n\n                try:\n                    parcial = pagina.get_by_text(\n                        titulo,\n                        exact=False\n                    )\n\n                    if parcial.count() > 0:\n                        localizado = parcial.first\n                        break\n                except Exception:\n                    pass\n\n                try:\n                    pagina.mouse.wheel(\n                        0,\n                        900\n                    )\n                    pagina.wait_for_timeout(\n                        250\n                    )\n                except Exception:\n                    break\n\n            if localizado is None:\n                return False, (\n                    "No pude localizar el candidato en Productos."\n                )\n\n            try:\n                localizado.scroll_into_view_if_needed()\n            except Exception:\n                pass\n\n            ok_detalle, url_detalle, via = _v103_abrir_detalle_exacto(\n                pagina,\n                titulo\n            )\n\n            if not ok_detalle:\n                ruta = _v103_dump_detalle(\n                    pagina,\n                    titulo\n                )\n\n                return False, (\n                    "Localicé el candidato pero no pude abrir su detalle exacto. "\n                    + (\n                        "Diagnóstico: "\n                        + ruta\n                        if ruta\n                        else ""\n                    )\n                )\n\n            if url_detalle:\n                try:\n                    _v102_guardar_url_candidato(\n                        candidato,\n                        url_detalle\n                    )\n                except Exception:\n                    pass\n\n            _v102_log_rotacion(\n                "V103 detalle abierto por "\n                + str(\n                    via\n                )\n                + ": "\n                + str(\n                    url_detalle\n                )\n            )\n\n            try:\n                pagina.wait_for_timeout(\n                    1400\n                )\n            except Exception:\n                pass\n\n            ok_borrar, detalle_borrar = _v103_click_papelera_detalle(\n                pagina\n            )\n\n            if not ok_borrar:\n                ruta = _v103_dump_detalle(\n                    pagina,\n                    titulo\n                )\n\n                return False, (\n                    "Abrí el detalle exacto, pero no encontré la papelera. "\n                    + (\n                        "Diagnóstico: "\n                        + ruta\n                        if ruta\n                        else ""\n                    )\n                )\n\n            _v102_log_rotacion(\n                "V103 control de borrado pulsado: "\n                + str(\n                    detalle_borrar\n                )\n            )\n\n            pagina.wait_for_timeout(\n                500\n            )\n\n            confirmado, detalle_conf = _v103_confirmar_borrado(\n                pagina\n            )\n\n            if confirmado:\n                _v102_log_rotacion(\n                    "V103 confirmación pulsada: "\n                    + str(\n                        detalle_conf\n                    )\n                )\n\n            pagina.wait_for_timeout(\n                1800\n            )\n\n            # Verificación real: volver a Productos y comprobar URL/título.\n            _v1021_ir_a_productos(\n                pagina\n            )\n\n            pagina.wait_for_timeout(\n                900\n            )\n\n            url_obj = _v102_url_candidato(\n                candidato\n            )\n\n            if url_obj:\n                try:\n                    enlaces = pagina.locator(\n                        "a[href]"\n                    )\n\n                    sigue_url = False\n\n                    objetivo_n = str(\n                        url_obj\n                    ).rstrip(\n                        "/"\n                    )\n\n                    for i in range(\n                        min(\n                            enlaces.count(),\n                            900\n                        )\n                    ):\n                        href = _v103_href_absoluto(\n                            enlaces.nth(i).get_attribute(\n                                "href"\n                            )\n                        )\n\n                        if href and objetivo_n in href.rstrip("/"):\n                            sigue_url = True\n                            break\n\n                    if not sigue_url:\n                        return True, (\n                            "Anuncio eliminado; su URL ya no aparece en Productos."\n                        )\n                except Exception:\n                    pass\n\n            # Si no hay URL, comprobar que se reduzca el número de coincidencias.\n            try:\n                restantes = pagina.get_by_text(\n                    titulo,\n                    exact=True\n                ).count()\n\n                if restantes == 0:\n                    return True, (\n                        "Anuncio eliminado; el título ya no aparece en Productos."\n                    )\n            except Exception:\n                pass\n\n            ruta = _v103_dump_detalle(\n                pagina,\n                titulo\n            )\n\n            return False, (\n                "Se pulsó borrar, pero no pude confirmar la desaparición del anuncio. "\n                + (\n                    "Diagnóstico: "\n                    + ruta\n                    if ruta\n                    else ""\n                )\n            )\n\n    except Exception as e:\n        return False, (\n            type(\n                e\n            ).__name__\n            + ": "\n            + str(\n                e\n            )\n        )\n\n    finally:\n        try:\n            if pagina is not None:\n                pagina.close()\n        except Exception:\n            pass\n\n\n# Sustituir el eliminador final. Conservamos V102.1 como fallback únicamente.\n_v103_eliminar_anterior = globals().get(\n    "_v102_eliminar_en_wallapop"\n)\n\n\ndef _v102_eliminar_en_wallapop(candidato):\n    _v102_log_rotacion(\n        "V103: abriendo detalle exacto para localizar la papelera web."\n    )\n\n    ok, detalle = _v103_borrar_candidato(\n        candidato\n    )\n\n    if ok:\n        return True, detalle\n\n    _v102_log_rotacion(\n        "V103 método principal falló: "\n        + str(\n            detalle\n        )\n    )\n\n    anterior = globals().get(\n        "_v103_eliminar_anterior"\n    )\n\n    if callable(\n        anterior\n    ):\n        try:\n            ok2, det2 = anterior(\n                candidato\n            )\n\n            if ok2:\n                return True, det2\n\n            return False, (\n                str(\n                    detalle\n                )\n                + " · Fallback V102.1: "\n                + str(\n                    det2\n                )\n            )\n        except Exception as e:\n            return False, (\n                str(\n                    detalle\n                )\n                + " · Fallback V102.1: "\n                + type(\n                    e\n                ).__name__\n                + ": "\n                + str(\n                    e\n                )\n            )\n\n    return False, detalle\n\n\n\n# =========================================================\n# V98 · CAPACIDAD VISIBLE EN BARRA DE COLA\n# =========================================================\n_ARCHIVO_CAPACIDAD_COLA = Path(CARPETA_DATOS_USUARIO) / "capacidad_cola.json"\n_CAPACIDAD_COLA_DEFECTO = 500\n_v102_boton_capacidad = None\n_v102_control_instalado = False\n\n\ndef _v102_cargar_capacidad():\n    try:\n        if _ARCHIVO_CAPACIDAD_COLA.exists():\n            datos = json.loads(_ARCHIVO_CAPACIDAD_COLA.read_text(encoding="utf-8"))\n            return max(1, min(5000, int(datos.get("capacidad", 500))))\n    except Exception:\n        pass\n    return 500\n\n\ndef _v102_guardar_capacidad(valor):\n    global MAX_COLA\n\n    valor = max(\n        1,\n        min(\n            5000,\n            int(\n                valor\n            )\n        )\n    )\n\n    _ARCHIVO_CAPACIDAD_COLA.parent.mkdir(\n        parents=True,\n        exist_ok=True\n    )\n\n    _ARCHIVO_CAPACIDAD_COLA.write_text(\n        json.dumps(\n            {\n                "capacidad": valor,\n                "actualizado": time.strftime(\n                    "%d/%m/%Y %H:%M:%S"\n                ),\n            },\n            ensure_ascii=False,\n            indent=2\n        ),\n        encoding="utf-8"\n    )\n\n    MAX_COLA = valor\n\n    try:\n        datos_cfg = cargar_configuracion_general()\n\n        if isinstance(\n            datos_cfg,\n            dict\n        ):\n            datos_cfg[\n                "max_cola"\n            ] = valor\n\n            with open(\n                ARCHIVO_CONFIG,\n                "w",\n                encoding="utf-8"\n            ) as archivo_cfg:\n                json.dump(\n                    datos_cfg,\n                    archivo_cfg,\n                    ensure_ascii=False,\n                    indent=4\n                )\n    except Exception:\n        pass\n\n    try:\n        var_max_cola.set(\n            str(\n                valor\n            )\n        )\n    except Exception:\n        pass\n\n    return valor\n\n\nCAPACIDAD_COLA = _v102_cargar_capacidad()\n\n\ndef capacidad_cola_actual():\n    try:\n        return int(CAPACIDAD_COLA)\n    except Exception:\n        return 500\n\n\ndef _v102_uso_cola():\n    try:\n        return len(cola_anuncios)\n    except Exception:\n        return 0\n\n\ndef _v102_actualizar_boton():\n    try:\n        if _v102_boton_capacidad is not None:\n            _v102_boton_capacidad.configure(\n                text=f"⚙ Capacidad {_v102_uso_cola()}/{limite_cola_efectivo()}"\n            )\n    except Exception:\n        pass\n\n\ndef _v102_abrir_dialogo_capacidad():\n    global CAPACIDAD_COLA\n\n    dialogo = ctk.CTkToplevel(ventana)\n    dialogo.title("Capacidad de la cola")\n    dialogo.geometry("420x220")\n    dialogo.resizable(False, False)\n\n    try:\n        dialogo.transient(ventana)\n        dialogo.grab_set()\n    except Exception:\n        pass\n\n    marco = ctk.CTkFrame(dialogo, fg_color="#081017")\n    marco.pack(fill="both", expand=True, padx=12, pady=12)\n\n    ctk.CTkLabel(\n        marco,\n        text="Capacidad máxima de la cola",\n        font=("Segoe UI", 16, "bold")\n    ).pack(anchor="w", padx=12, pady=(12, 4))\n\n    ctk.CTkLabel(\n        marco,\n        text="Elige entre 1 y 5000 anuncios.",\n        text_color="#98a7b2"\n    ).pack(anchor="w", padx=12, pady=(0, 10))\n\n    entrada = ctk.CTkEntry(marco, height=38)\n    entrada.pack(fill="x", padx=12, pady=(0, 10))\n    entrada.insert(0, str(capacidad_cola_actual()))\n\n    def guardar():\n        global CAPACIDAD_COLA\n        try:\n            valor = int(str(entrada.get()).strip())\n        except Exception:\n            messagebox.showerror("Capacidad de la cola", "Escribe un número entre 1 y 5000.", parent=dialogo)\n            return\n\n        if valor < 1 or valor > 5000:\n            messagebox.showerror("Capacidad de la cola", "El valor debe estar entre 1 y 5000.", parent=dialogo)\n            return\n\n        CAPACIDAD_COLA = _v102_guardar_capacidad(valor)\n        _v102_actualizar_boton()\n        dialogo.destroy()\n\n    ctk.CTkButton(\n        marco,\n        text="Guardar capacidad",\n        command=guardar,\n        height=38\n    ).pack(fill="x", padx=12, pady=(0, 12))\n\n\ndef _v102_descendientes(widget):\n    salida = []\n    try:\n        hijos = widget.winfo_children()\n    except Exception:\n        return salida\n\n    for hijo in hijos:\n        salida.append(hijo)\n        salida.extend(_v102_descendientes(hijo))\n    return salida\n\n\ndef _v102_texto(widget):\n    try:\n        return str(widget.cget("text") or "").strip().lower()\n    except Exception:\n        return ""\n\n\ndef _v102_buscar_boton_anadir():\n    try:\n        todos = _v102_descendientes(ventana)\n    except Exception:\n        todos = []\n\n    for widget in todos:\n        texto = _v102_texto(widget)\n        if (\n            "añadir anuncio actual" in texto\n            or "anadir anuncio actual" in texto\n            or texto == "+ añadir anuncio"\n            or texto == "+ anadir anuncio"\n        ):\n            return widget\n\n    return None\n\n\ndef _v102_instalar_boton_capacidad(intentos=0):\n    global _v102_boton_capacidad, _v102_control_instalado\n\n    if _v102_control_instalado:\n        return\n\n    referencia = _v102_buscar_boton_anadir()\n\n    if referencia is None:\n        if intentos < 60:\n            ventana.after(\n                500,\n                lambda: _v102_instalar_boton_capacidad(intentos + 1)\n            )\n        return\n\n    try:\n        parent = referencia.master\n        gestor = str(referencia.winfo_manager()).lower()\n\n        _v102_boton_capacidad = ctk.CTkButton(\n            parent,\n            text="",\n            command=_v102_abrir_dialogo_capacidad,\n            height=32,\n            width=155,\n            fg_color="#245a86",\n            hover_color="#2f6fa3"\n        )\n\n        if gestor == "grid":\n            info = referencia.grid_info()\n            _v102_boton_capacidad.grid(\n                row=int(info.get("row", 0)),\n                column=int(info.get("column", 0)) + 1,\n                padx=(8, 0),\n                sticky="ew"\n            )\n        else:\n            _v102_boton_capacidad.pack(\n                side="left",\n                padx=(8, 0)\n            )\n\n        _v102_control_instalado = True\n        _v102_actualizar_boton()\n\n    except Exception as e:\n        print("[V102] Error instalando botón capacidad:", e)\n        if intentos < 60:\n            ventana.after(\n                500,\n                lambda: _v102_instalar_boton_capacidad(intentos + 1)\n            )\n\n\ndef _v102_envuelve_funcion_cola(nombre, funcion):\n    if getattr(funcion, "_v102_capacidad_wrapped", False):\n        return funcion\n\n    def wrapper(*args, **kwargs):\n        actual = _v102_uso_cola()\n        limite = limite_cola_efectivo()\n\n        if actual >= limite:\n            messagebox.showwarning(\n                "Cola llena",\n                f"La cola ya tiene {actual}/{limite} anuncios."\n            )\n            return None\n\n        resultado = funcion(*args, **kwargs)\n\n        try:\n            if len(cola_anuncios) > limite:\n                del cola_anuncios[limite:]\n                fn = globals().get("guardar_cola_en_disco") or globals().get("guardar_cola")\n                if callable(fn):\n                    fn()\n                fnr = globals().get("refrescar_cola")\n                if callable(fnr):\n                    fnr()\n        except Exception:\n            pass\n\n        _v102_actualizar_boton()\n        return resultado\n\n    wrapper._v102_capacidad_wrapped = True\n    wrapper.__name__ = getattr(funcion, "__name__", nombre)\n    return wrapper\n\n\ndef _v102_instalar_limites():\n    for nombre, funcion in list(globals().items()):\n        if not callable(funcion):\n            continue\n\n        n = str(nombre).lower()\n\n        if "cola" not in n:\n            continue\n\n        if not any(p in n for p in ("anadir", "añadir", "agregar", "insertar")):\n            continue\n\n        if any(p in n for p in ("guardar", "cargar", "refrescar", "iniciar", "procesar", "publicar")):\n            continue\n\n        try:\n            globals()[nombre] = _v102_envuelve_funcion_cola(nombre, funcion)\n        except Exception:\n            pass\n\n\ndef _v102_capacidad_diagnostico():\n    return {\n        "configurada": capacidad_cola_actual(),\n        "efectiva": limite_cola_efectivo(),\n        "uso": _v102_uso_cola(),\n        "libres": espacio_cola_disponible(),\n        "max_cola_legacy": globals().get(\n            "MAX_COLA"\n        ),\n    }\n\n\ndef _v102_tick():\n    _v102_actualizar_boton()\n\n    if not _v102_control_instalado:\n        _v102_instalar_boton_capacidad()\n\n    try:\n        ventana.after(1000, _v102_tick)\n    except Exception:\n        pass\n\n\ntry:\n    try:\n        MAX_COLA = capacidad_cola_actual()\n        var_max_cola.set(\n            str(\n                MAX_COLA\n            )\n        )\n    except Exception:\n        pass\n\n    ventana.after(300, _v102_instalar_boton_capacidad)\n    ventana.after(700, _v102_instalar_limites)\n    ventana.after(1000, _v102_tick)\nexcept Exception as e:\n    print("[V102] No pude iniciar capacidad de cola:", e)\n\n\ntry:\n    from borrado_antiguos_v104 import instalar_panel\n    instalar_panel(globals())\nexcept Exception as _error_panel_v105:\n    print("[V105] No pude iniciar Borrar antiguos:", _error_panel_v105)\n\ntry:\n    from estabilidad_v105 import instalar_estabilidad\n    instalar_estabilidad(globals())\nexcept Exception as _error_estabilidad_v105:\n    print("[V105] No pude iniciar la capa estable:", _error_estabilidad_v105)\n\nventana.mainloop()\n'
BORRADO_SOURCE = '"""Panel manual de borrado. Importar no abre Chrome ni elimina anuncios."""\nimport copy\nimport json\nimport math\nimport os\nimport queue\nimport re\nimport threading\nimport time\nfrom collections import Counter\nfrom datetime import datetime\nfrom pathlib import Path\nfrom urllib.parse import urlparse, unquote\n\n\ndef escribir_json(path, value):\n    path = Path(path)\n    path.parent.mkdir(parents=True, exist_ok=True)\n    temp = path.with_name(path.name + \'.tmp\')\n    temp.write_text(json.dumps(value, ensure_ascii=False, indent=2), encoding=\'utf-8\')\n    os.replace(temp, path)\n\n\ndef bloquear(path):\n    """Bloqueo liberado por el SO también si el programa se cierra."""\n    handle = open(path, \'a+b\')\n    try:\n        if handle.tell() == 0:\n            handle.write(b\'0\')\n            handle.flush()\n        handle.seek(0)\n        if os.name == \'nt\':\n            import msvcrt\n            msvcrt.locking(handle.fileno(), msvcrt.LK_NBLCK, 1)\n        else:\n            import fcntl\n            fcntl.flock(handle.fileno(), fcntl.LOCK_EX | fcntl.LOCK_NB)\n        return handle\n    except Exception:\n        handle.close()\n        raise RuntimeError(\'Otro lote está en curso. Cierra las otras instancias de DIAGPROG5.\')\n\n\ndef fecha(item):\n    for key in (\'timestamp_publicado\', \'timestamp\', \'created_at_ts\', \'fecha_ts\'):\n        try:\n            n = float(item.get(key, 0))\n            if n > 100_000_000_000:\n                n /= 1000\n            if math.isfinite(n) and n > 0:\n                return n\n        except (TypeError, ValueError):\n            pass\n    value = str(item.get(\'fecha\') or item.get(\'fecha_publicacion\') or \'\').strip()\n    try:\n        return datetime.fromisoformat(value.replace(\'Z\', \'+00:00\')).timestamp()\n    except ValueError:\n        pass\n    for fmt in (\'%d/%m/%Y %H:%M:%S\', \'%d/%m/%Y %H:%M\', \'%Y-%m-%d %H:%M:%S\'):\n        try:\n            return datetime.strptime(value, fmt).timestamp()\n        except ValueError:\n            pass\n    return None\n\n\ndef identidad(url):\n    try:\n        u = urlparse(str(url))\n        host = (u.hostname or \'\').lower()\n        if u.scheme != \'https\' or not (host == \'wallapop.com\' or host.endswith(\'.wallapop.com\')):\n            return None\n        if u.username or u.password or u.port not in (None, 443):\n            return None\n        # Un ID estable impide confundir títulos repetidos.\n        m = re.fullmatch(r\'/item/(?:[^/]*-)?([0-9]+)/*\', unquote(u.path))\n        return m.group(1) if m else None\n    except (ValueError, TypeError):\n        return None\n\n\ndef enlace(item):\n    for key in (\'url\', \'url_anuncio\', \'url_wallapop\', \'link\', \'permalink\', \'web_url\'):\n        value = str(item.get(key) or \'\').strip()\n        if identidad(value):\n            return value\n    return \'\'\n\n\ndef validar(cantidad, horas, pausa, fallos):\n    count, age, delay, errors = int(cantidad), float(horas), float(pausa), int(fallos)\n    if not 1 <= count <= 500:\n        raise ValueError(\'La cantidad debe estar entre 1 y 500.\')\n    if not math.isfinite(age) or not 0 <= age <= 87600:\n        raise ValueError(\'La antigüedad debe estar entre 0 y 87600 horas.\')\n    if not math.isfinite(delay) or not 1 <= delay <= 120:\n        raise ValueError(\'La pausa debe estar entre 1 y 120 segundos.\')\n    if not 1 <= errors <= 20:\n        raise ValueError(\'El límite de fallos debe estar entre 1 y 20.\')\n    return dict(cantidad=count, horas=age, pausa=delay, fallos=errors)\n\n\ndef seleccionar(datos, cfg, bloqueados=(), ahora=None):\n    ahora = time.time() if ahora is None else ahora\n    if not isinstance(datos, dict):\n        raise ValueError(\'El registro de publicados no es válido.\')\n    duplicados = Counter(identidad(enlace(x)) for x in datos.values() if isinstance(x, dict))\n    resultado, omitidos = [], Counter()\n    for key, item in datos.items():\n        if not isinstance(item, dict):\n            continue\n        if any(item.get(k) for k in (\'protegido\', \'protegido_rotacion\', \'proteger\', \'locked\', \'bloqueado\')):\n            omitidos[\'protegidos\'] += 1\n            continue\n        if item.get(\'eliminado_wallapop\') or str(item.get(\'estado\', \'PUBLICADO\')).upper() not in (\'PUBLICADO\', \'ACTIVO\', \'ONLINE\', \'\'):\n            omitidos[\'inactivos\'] += 1\n            continue\n        stamp = fecha(item)\n        if stamp is None or not math.isfinite(stamp) or stamp <= 0 or stamp > ahora:\n            omitidos[\'sin fecha válida\'] += 1\n            continue\n        if ahora - stamp < cfg[\'horas\'] * 3600:\n            omitidos[\'demasiado recientes\'] += 1\n            continue\n        url = enlace(item)\n        uid = identidad(url)\n        if not uid:\n            omitidos[\'sin enlace identificable\'] += 1\n            continue\n        if duplicados[uid] > 1:\n            omitidos[\'enlace duplicado\'] += 1\n            continue\n        if uid in bloqueados:\n            omitidos[\'pendientes de revisión\'] += 1\n            continue\n        resultado.append(dict(registro=key, item=copy.deepcopy(item), url=url, uid=uid,\n                              fecha=stamp, titulo=str(item.get(\'titulo\') or item.get(\'title\') or \'(sin título)\')))\n    resultado.sort(key=lambda x: (x[\'fecha\'], str(x[\'registro\'])))\n    return resultado[:cfg[\'cantidad\']], dict(omitidos), len(resultado)\n\n\ndef respuesta_borrado(response, uid):\n    u = urlparse(response.url)\n    host = (u.hostname or \'\').lower()\n    return (response.request.method.upper() == \'DELETE\' and 200 <= response.status < 300\n            and (host == \'wallapop.com\' or host.endswith(\'.wallapop.com\'))\n            and uid in unquote(u.path).split(\'/\'))\n\n\nclass Cancelado(Exception):\n    pass\n\n\ndef boton_unico(scope, patron):\n    matches = []\n    for role in (\'button\', \'menuitem\'):\n        loc = scope.get_by_role(role, name=re.compile(patron, re.I))\n        for n in range(loc.count()):\n            el = loc.nth(n)\n            if el.is_visible() and el.is_enabled():\n                matches.append(el)\n    return matches[0] if len(matches) == 1 else None\n\n\ndef eliminar(page, entry, stop):\n    """Sin búsqueda por título, clicks forzados ni fallback de borrado antiguo."""\n    if stop.is_set():\n        raise Cancelado()\n    page.goto(entry[\'url\'], wait_until=\'domcontentloaded\', timeout=35000)\n    page.wait_for_timeout(1600)\n    if identidad(page.url) != entry[\'uid\']:\n        raise RuntimeError(\'El enlace no abrió el anuncio previsto. No se ha pulsado borrar.\')\n    button = boton_unico(page, r\'^(eliminar|borrar|delete)( (este )?(anuncio|producto|artículo|item))?$|^papelera$\')\n    if button is None:\n        menu = boton_unico(page, r\'^(más opciones|opciones|más|more|more options|acciones)$\')\n        if menu:\n            menu.click(timeout=4000)\n            page.wait_for_timeout(400)\n            button = boton_unico(page, r\'^(eliminar|borrar|delete)( (este )?(anuncio|producto|artículo|item))?$\')\n    if button is None:\n        raise RuntimeError(\'No se encontró un control de borrado inequívoco; se guardará un diagnóstico.\')\n    confirmed = []\n    def response_handler(response):\n        if respuesta_borrado(response, entry[\'uid\']):\n            confirmed.append(True)\n    page.on(\'response\', response_handler)\n    try:\n        if stop.is_set():\n            raise Cancelado()\n        button.click(timeout=5000)\n        page.wait_for_timeout(700)\n        dialogs = page.get_by_role(\'dialog\')\n        visible = [dialogs.nth(i) for i in range(dialogs.count()) if dialogs.nth(i).is_visible()]\n        if len(visible) == 1:\n            confirm = boton_unico(visible[0], r\'^(sí,? )?(eliminar|borrar|delete)( (este )?(anuncio|producto|artículo|item))?$|^confirmar$\')\n            if confirm:\n                if stop.is_set():\n                    raise Cancelado()\n                confirm.click(timeout=5000)\n        for _ in range(24):\n            if confirmed:\n                return\n            page.wait_for_timeout(250)\n        raise RuntimeError(\'Borrado no verificado por respuesta del servidor. Revisar el anuncio antes de repetir.\')\n    finally:\n        page.remove_listener(\'response\', response_handler)\n\n\nclass Panel:\n    def __init__(self, g):\n        import customtkinter as ctk\n        from tkinter import ttk, messagebox\n        self.g, self.ctk, self.msg = g, ctk, messagebox\n        self.root = g[\'ventana\']\n        self.data = Path(g[\'CARPETA_DATOS_USUARIO\']) / \'borrado_antiguos\'\n        self.data.mkdir(parents=True, exist_ok=True)\n        self.stop, self.events = threading.Event(), queue.Queue()\n        self.busy, self.plan, self.settings = False, [], None\n        self.journal_path = self.data / \'historial.json\'\n        self.history = self.read(self.journal_path, [])\n        if not isinstance(self.history, list):\n            raise ValueError(\'Historial de borrado inválido: revisar antes de continuar.\')\n        cfg = self.read(self.data / \'ajustes.json\', dict(cantidad=5, horas=24, pausa=3, fallos=1))\n        tab = g[\'tabs\'].add(\'Borrar antiguos\')\n        try:\n            tab.configure(fg_color=g.get(\'COLOR_UI_BG\', \'#081018\'))\n        except Exception:\n            pass\n        # El Tabview original oculta sus pestañas: V106 crea un acceso real.\n        if not g.get(\'_BORRADO_SIDE_MAIN_V107\'):\n            try:\n                self.side_button = g[\'_side_btn\'](\'🗑   Borrar antiguos\', \'Borrar antiguos\')\n                self.side_button.configure(fg_color=\'#351015\', hover_color=\'#5a1820\', border_width=1, border_color=\'#a63838\')\n            except Exception as error:\n                print(\'[V107] No pude crear el acceso lateral:\', error)\n        try:\n            acceso = ctk.CTkFrame(g[\'dashboard_scroll\'])\n            acceso.pack(fill=\'x\', padx=4, pady=10)\n            ctk.CTkLabel(acceso, text=\'Limpieza del catálogo\', font=(\'Segoe UI\', 17, \'bold\')).pack(side=\'left\', padx=16, pady=14)\n            ctk.CTkButton(acceso, text=\'Abrir Borrar antiguos\', command=lambda: g[\'tabs\'].set(\'Borrar antiguos\'), fg_color=\'#a63838\', hover_color=\'#7d2929\', width=210).pack(side=\'right\', padx=16, pady=12)\n            self.dashboard_access = acceso\n        except Exception as error:\n            print(\'[V106] No pude crear el acceso de Inicio:\', error)\n        scroll = ctk.CTkScrollableFrame(tab)\n        scroll.pack(fill=\'both\', expand=True)\n        ctk.CTkLabel(scroll, text=\'Borrar los anuncios más antiguos\', font=(\'Segoe UI\', 22, \'bold\')).pack(anchor=\'w\', padx=16, pady=10)\n        ctk.CTkLabel(scroll, text=\'Orden por fecha registrada en DIAGPROG5. Solo anuncios activos, sin protección y con enlace identificable.\\nEl borrado se inicia aquí; la rotación automática al publicar está desactivada.\', wraplength=780, justify=\'left\').pack(anchor=\'w\', padx=16)\n        form = ctk.CTkFrame(scroll)\n        form.pack(fill=\'x\', padx=16, pady=12)\n        self.inputs = {}\n        for row, (key, label) in enumerate(((\'cantidad\', \'Máximo a borrar por lote (1–500)\'), (\'horas\', \'Antigüedad mínima en horas\'), (\'pausa\', \'Pausa entre anuncios en segundos (1–120)\'), (\'fallos\', \'Detener tras estos fallos consecutivos (1–20)\'))):\n            ctk.CTkLabel(form, text=label).grid(row=row, column=0, padx=12, pady=5, sticky=\'w\')\n            field = ctk.CTkEntry(form, width=110)\n            field.insert(0, str(cfg.get(key, {\'cantidad\':5, \'horas\':24, \'pausa\':3, \'fallos\':1}[key])))\n            field.grid(row=row, column=1, padx=12, pady=5)\n            self.inputs[key] = field\n        actions = ctk.CTkFrame(scroll)\n        actions.pack(fill=\'x\', padx=16)\n        self.preview_button = ctk.CTkButton(actions, text=\'1. Vista previa\', command=self.preview)\n        self.preview_button.pack(side=\'left\', padx=6, pady=8)\n        self.start_button = ctk.CTkButton(actions, text=\'2. Borrar lote mostrado\', command=self.start, fg_color=\'#a63838\', state=\'disabled\')\n        self.start_button.pack(side=\'left\', padx=6)\n        self.stop_button = ctk.CTkButton(actions, text=\'Detener\', command=self.cancel, state=\'disabled\')\n        self.stop_button.pack(side=\'left\', padx=6)\n        table_frame = ctk.CTkFrame(scroll)\n        table_frame.pack(fill=\'x\', padx=16, pady=10)\n        self.table = ttk.Treeview(table_frame, columns=(\'title\', \'date\', \'id\'), show=\'headings\', height=9)\n        for key, title, width in ((\'title\', \'Anuncio\', 350), (\'date\', \'Fecha registrada\', 160), (\'id\', \'ID Wallapop\', 130)):\n            self.table.heading(key, text=title)\n            self.table.column(key, width=width)\n        bar = ttk.Scrollbar(table_frame, orient=\'vertical\', command=self.table.yview)\n        self.table.configure(yscrollcommand=bar.set)\n        self.table.pack(side=\'left\', fill=\'both\', expand=True)\n        bar.pack(side=\'right\', fill=\'y\')\n        self.status = ctk.CTkLabel(scroll, text=\'Ajusta la cantidad y pulsa Vista previa.\', wraplength=780, justify=\'left\')\n        self.status.pack(anchor=\'w\', padx=16, pady=5)\n        self.progress = ctk.CTkProgressBar(scroll)\n        self.progress.set(0)\n        self.progress.pack(fill=\'x\', padx=16, pady=6)\n        self.log = ctk.CTkTextbox(scroll, height=150)\n        self.log.pack(fill=\'x\', padx=16, pady=8)\n        self.log.configure(state=\'disabled\')\n        ctk.CTkButton(scroll, text=\'Abrir historial y diagnósticos\', command=lambda: os.startfile(str(self.data))).pack(anchor=\'w\', padx=16, pady=6)\n        self.root.after(150, self.poll)\n\n    @staticmethod\n    def read(path, default):\n        return json.loads(path.read_text(encoding=\'utf-8\')) if path.exists() else default\n\n    def bloqueados(self):\n        return {x[\'uid\'] for x in self.history if x.get(\'estado\') in (\'EN_CURSO\', \'REVISAR\', \'ELIMINADO\')}\n\n    def config(self):\n        return validar(*(self.inputs[k].get() for k in (\'cantidad\', \'horas\', \'pausa\', \'fallos\')))\n\n    def preview(self):\n        if self.busy:\n            return\n        self.start_button.configure(state=\'disabled\')\n        self.plan = []\n        try:\n            cfg = self.config()\n            plan, omitidos, total = seleccionar(self.g[\'cargar_publicados\'](), cfg, self.bloqueados())\n            escribir_json(self.data / \'ajustes.json\', cfg)\n            self.settings, self.plan = cfg, plan\n            for key in self.table.get_children():\n                self.table.delete(key)\n            for entry in plan:\n                self.table.insert(\'\', \'end\', values=(entry[\'titulo\'], datetime.fromtimestamp(entry[\'fecha\']).strftime(\'%d/%m/%Y %H:%M\'), entry[\'uid\']))\n            omissions = \', \'.join(f\'{k}: {v}\' for k, v in omitidos.items()) or \'ninguno\'\n            self.status.configure(text=f\'{len(plan)} en este lote · {total} elegibles. Omitidos: {omissions}.\')\n            self.start_button.configure(state=\'normal\' if plan else \'disabled\')\n        except Exception as e:\n            self.msg.showerror(\'Vista previa\', str(e))\n\n    def start(self):\n        if self.busy or not self.plan:\n            return\n        try:\n            if self.config() != self.settings:\n                raise ValueError(\'Has cambiado los ajustes. Genera otra vista previa.\')\n            if self.g.get(\'cola_en_ejecucion\') or any(self.g.get(k) for k in (\'_ROTACION_V102_EN_CURSO\', \'_ROTACION_V101_EN_CURSO\', \'_PUBLICACION_V104_EN_CURSO\')):\n                raise ValueError(\'Detén la cola y espera a que termine la operación actual.\')\n            latest, _, _ = seleccionar(self.g[\'cargar_publicados\'](), self.settings, self.bloqueados())\n            if latest != self.plan:\n                raise ValueError(\'El registro ha cambiado. Genera otra vista previa.\')\n            if not self.msg.askyesno(\'Confirmar borrado\', f\'Se intentará eliminar hasta {len(self.plan)} anuncios, en el orden mostrado.\\n\\nLa copia local no permite restaurarlos en Wallapop. ¿Continuar?\'):\n                return\n            self.busy = True\n            self.g[\'_BORRADO_V104_EN_CURSO\'] = True\n            self.stop.clear()\n            for widget in [self.preview_button, self.start_button, *self.inputs.values()]:\n                widget.configure(state=\'disabled\')\n            self.stop_button.configure(state=\'normal\')\n            self.progress.set(0)\n            threading.Thread(target=self.worker, args=(copy.deepcopy(self.plan), dict(self.settings)), daemon=True).start()\n        except Exception as e:\n            self.msg.showerror(\'Borrado\', str(e))\n\n    def cancel(self):\n        self.stop.set()\n        self.status.configure(text=\'Deteniendo: se comprobará el resultado de cualquier borrado ya enviado.\')\n\n    def worker(self, plan, cfg):\n        ok = errors = consecutive = done = 0\n        page = lock = None\n        session = self.data / (\'lote_\' + datetime.now().strftime(\'%Y%m%d_%H%M%S_%f\'))\n        try:\n            lock = bloquear(self.data / \'lote.lock\')\n            self.history = self.read(self.journal_path, [])\n            session.mkdir()\n            escribir_json(session / \'plan.json\', plan)\n            from playwright.sync_api import sync_playwright\n            with sync_playwright() as p:\n                browser = p.chromium.connect_over_cdp(\'http://127.0.0.1:9222\', timeout=15000)\n                if not browser.contexts:\n                    raise RuntimeError(\'Chrome no tiene una sesión disponible.\')\n                page = browser.contexts[0].new_page()\n                try:\n                    for entry in plan:\n                        if self.stop.is_set():\n                            break\n                        event = None\n                        try:\n                            current = self.g[\'cargar_publicados\']()\n                            one, _, _ = seleccionar(current, dict(cfg, cantidad=500), self.bloqueados())\n                            if entry not in one:\n                                raise RuntimeError(\'El anuncio cambió o ya no es elegible. No se ha borrado.\')\n                            event = dict(uid=entry[\'uid\'], registro=entry[\'registro\'], titulo=entry[\'titulo\'], fecha=datetime.now().isoformat(), estado=\'EN_CURSO\')\n                            self.history.append(event)\n                            escribir_json(self.journal_path, self.history)\n                            self.events.put((\'log\', f"Procesando: {entry[\'titulo\']} · {entry[\'uid\']}"))\n                            eliminar(page, entry, self.stop)\n                            # Registrar primero la confirmación remota: jamás reintentar si falla el guardado local.\n                            event[\'estado\'] = \'ELIMINADO\'\n                            escribir_json(self.journal_path, self.history)\n                            current = self.g[\'cargar_publicados\']()\n                            record = current.get(entry[\'registro\'])\n                            if not isinstance(record, dict) or identidad(enlace(record)) != entry[\'uid\']:\n                                raise RuntimeError(\'Eliminado en Wallapop; el registro local cambió. Revisar manualmente.\')\n                            record.update(estado=\'ELIMINADO_ROTACION\', eliminado_wallapop=True, fecha_eliminacion_wallapop=datetime.now().strftime(\'%d/%m/%Y %H:%M:%S\'))\n                            escribir_json(Path(self.g[\'ARCHIVO_PUBLICADOS\']), current)\n                            ok += 1\n                            consecutive = 0\n                            self.events.put((\'log\', \'Eliminado y verificado: \' + entry[\'titulo\']))\n                        except Cancelado:\n                            if event:\n                                event[\'estado\'] = \'REVISAR\'\n                                event[\'detalle\'] = \'Interrumpido; comprobar estado antes de repetir.\'\n                                escribir_json(self.journal_path, self.history)\n                            break\n                        except Exception as e:\n                            errors += 1\n                            consecutive += 1\n                            if event:\n                                if event[\'estado\'] != \'ELIMINADO\':\n                                    event[\'estado\'] = \'REVISAR\'\n                                event[\'detalle\'] = str(e)\n                                escribir_json(self.journal_path, self.history)\n                            self.events.put((\'log\', f"Revisar {entry[\'titulo\']}: {e}"))\n                            try:\n                                diagnostic = session / entry[\'uid\']\n                                diagnostic.mkdir(exist_ok=True)\n                                page.screenshot(path=str(diagnostic / \'detalle.png\'), full_page=True)\n                                (diagnostic / \'detalle.html\').write_text(page.content(), encoding=\'utf-8\')\n                            except Exception:\n                                pass\n                        done += 1\n                        self.events.put((\'progress\', (done / len(plan), f\'{done}/{len(plan)} revisados · {ok} eliminados · {errors} incidencias\')))\n                        if consecutive >= cfg[\'fallos\'] or self.stop.wait(cfg[\'pausa\']):\n                            break\n                finally:\n                    page.close()\n        except Exception as e:\n            errors += 1\n            self.events.put((\'log\', \'Lote detenido: \' + str(e)))\n        finally:\n            if lock is not None:\n                lock.close()\n            self.events.put((\'done\', f\'Finalizado: {ok} eliminados, {errors} incidencias. Historial: {session.name}\'))\n\n    def poll(self):\n        try:\n            while True:\n                kind, value = self.events.get_nowait()\n                if kind == \'log\':\n                    self.log.configure(state=\'normal\')\n                    self.log.insert(\'end\', value + \'\\n\')\n                    self.log.see(\'end\')\n                    self.log.configure(state=\'disabled\')\n                elif kind == \'progress\':\n                    self.progress.set(value[0])\n                    self.status.configure(text=value[1])\n                elif kind == \'done\':\n                    self.busy = False\n                    self.g[\'_BORRADO_V104_EN_CURSO\'] = False\n                    self.plan = []\n                    self.status.configure(text=value)\n                    self.stop_button.configure(state=\'disabled\')\n                    for widget in [self.preview_button, *self.inputs.values()]:\n                        widget.configure(state=\'normal\')\n        except queue.Empty:\n            pass\n        self.root.after(150, self.poll)\n\n\ndef instalar_panel(g):\n    # La v104 separa el borrado del límite de publicación desde el arranque.\n    old_config = g[\'cargar_config_rotacion\']\n    def manual_config():\n        cfg = old_config()\n        cfg[\'activo\'] = False\n        return cfg\n    g[\'cargar_config_rotacion\'] = manual_config\n    g[\'_v102_rotacion_activa\'] = lambda: False\n    g[\'gestionar_rotacion_limite_wallapop\'] = lambda *a, **kw: (False, \'Usa la pestaña Borrar antiguos.\')\n    g[\'var_rotacion_activa\'].set(False)\n    # Comprobación dentro de funciones usadas también por botones ya creados.\n    g[\'_BORRADO_V104_EN_CURSO\'] = False\n    g[\'_PUBLICACION_V104_EN_CURSO\'] = False\n    publicar = g[\'_publicar_wallapop_hilo\']\n    def publicar_vigilado(*args, **kwargs):\n        g[\'_PUBLICACION_V104_EN_CURSO\'] = True\n        try:\n            if g.get(\'_BORRADO_V104_EN_CURSO\'):\n                return\n            return publicar(*args, **kwargs)\n        finally:\n            g[\'_PUBLICACION_V104_EN_CURSO\'] = False\n    g[\'_publicar_wallapop_hilo\'] = publicar_vigilado\n    g[\'_panel_borrado_v104\'] = Panel(g)\n'
ESTABILIDAD_SOURCE = '"""Capa de estabilidad, actualizaciones y control de publicación de V105."""\nimport base64\nimport hashlib\nimport json\nimport os\nimport re\nimport shutil\nimport ssl\nimport subprocess\nimport sys\nimport threading\nimport time\nimport urllib.request\nfrom pathlib import Path\nfrom urllib.parse import urlparse\n\nSERVIDOR = "https://diagprog5-updater.hugoneitor05cod3.workers.dev"\n\n\ndef _texto_error(error):\n    texto = " ".join(str(error).replace("\\r", " ").replace("\\n", " ").split())\n    return (type(error).__name__ + ": " + texto)[:500]\n\n\ndef _contexto_ssl_windows():\n    """Mantiene verificación TLS y añade almacenes de confianza disponibles."""\n    contexto = ssl.create_default_context()\n    try:\n        import certifi\n        contexto.load_verify_locations(cafile=certifi.where())\n    except Exception:\n        pass\n    if os.name == "nt" and hasattr(ssl, "enum_certificates"):\n        for almacen in ("ROOT", "CA"):\n            try:\n                for certificado, tipo, _confianza in ssl.enum_certificates(almacen):\n                    if tipo == "x509_asn":\n                        contexto.load_verify_locations(\n                            cadata=ssl.DER_cert_to_PEM_cert(certificado)\n                        )\n            except Exception:\n                pass\n    return contexto\n\n\ndef _validar_url_oficial(url, rutas):\n    valor = str(url or "").strip()\n    parsed = urlparse(valor)\n    if (\n        parsed.scheme != "https"\n        or (parsed.hostname or "").lower()\n        != "diagprog5-updater.hugoneitor05cod3.workers.dev"\n        or parsed.path.rstrip("/") not in rutas\n        or parsed.username\n        or parsed.password\n        or parsed.port not in (None, 443)\n    ):\n        raise RuntimeError("El servidor devolvió una dirección no oficial; se ha bloqueado.")\n    return valor\n\n\nclass TransporteSeguro:\n    def __init__(self, g):\n        self.g = g\n        self.contexto_ssl = _contexto_ssl_windows()\n\n    def _urllib(self, url, timeout):\n        req = urllib.request.Request(\n            url,\n            headers={\n                "User-Agent": "DIAGPROG5-Updater/5.0",\n                "Accept": "application/json, text/plain, */*",\n                "Cache-Control": "no-cache",\n            },\n        )\n        with urllib.request.urlopen(req, timeout=timeout, context=self.contexto_ssl) as response:\n            return response.read()\n\n    @staticmethod\n    def _curl(url, timeout):\n        curl = shutil.which("curl.exe") or shutil.which("curl")\n        if not curl:\n            raise RuntimeError("curl no está disponible")\n        proceso = subprocess.run(\n            [\n                curl,\n                "--fail",\n                "--location",\n                "--silent",\n                "--show-error",\n                "--retry",\n                "2",\n                "--retry-all-errors",\n                "--connect-timeout",\n                "20",\n                "--max-time",\n                str(timeout),\n                url,\n            ],\n            capture_output=True,\n            timeout=timeout + 15,\n            creationflags=getattr(subprocess, "CREATE_NO_WINDOW", 0),\n        )\n        if proceso.returncode != 0:\n            raise RuntimeError(\n                proceso.stderr.decode("utf-8", "replace").strip()\n                or "curl terminó con código " + str(proceso.returncode)\n            )\n        if not proceso.stdout:\n            raise RuntimeError("respuesta vacía")\n        return proceso.stdout\n\n    def _chrome(self, url, timeout):\n        sync = self.g.get("sync_playwright")\n        if sync is None:\n            raise RuntimeError("Playwright no está disponible")\n        try:\n            self.g["_asegurar_chromium_controlable"]()\n        except Exception:\n            pass\n        with sync() as p:\n            browser = self.g["_conectar_playwright_cdp"](p)\n            if not browser.contexts:\n                raise RuntimeError("Chrome no tiene un contexto controlable")\n            page = browser.contexts[0].new_page()\n            try:\n                page.goto(\n                    SERVIDOR + "/version?t=" + str(int(time.time() * 1000)),\n                    wait_until="domcontentloaded",\n                    timeout=timeout * 1000,\n                )\n                if urlparse(url).path.rstrip("/") == "/version":\n                    contenido = page.locator("body").inner_text(timeout=10000)\n                    return contenido.encode("utf-8")\n                encoded = page.evaluate(\n                    """async (target) => {\n                        const response = await fetch(target, {cache: \'no-store\'});\n                        if (!response.ok) throw new Error(\'HTTP \' + response.status);\n                        const bytes = new Uint8Array(await response.arrayBuffer());\n                        let binary = \'\';\n                        for (let i = 0; i < bytes.length; i += 8192) {\n                            binary += String.fromCharCode(...bytes.subarray(i, i + 8192));\n                        }\n                        return btoa(binary);\n                    }""",\n                    url,\n                )\n                return base64.b64decode(encoded, validate=True)\n            finally:\n                try:\n                    page.close()\n                except Exception:\n                    pass\n\n    def obtener(self, url, rutas, timeout=60):\n        url = _validar_url_oficial(url, rutas)\n        errores = []\n        for metodo, funcion in (\n            ("Python/SSL", lambda: self._urllib(url, timeout)),\n            ("curl/Windows", lambda: self._curl(url, timeout)),\n            ("Chrome", lambda: self._chrome(url, timeout)),\n        ):\n            try:\n                datos = funcion()\n                if not datos:\n                    raise RuntimeError("respuesta vacía")\n                return datos\n            except Exception as error:\n                errores.append(metodo + " · " + _texto_error(error))\n        raise RuntimeError(\n            "No se pudo establecer una conexión TLS verificada. No se ha desactivado la seguridad.\\n\\n"\n            + "\\n".join(errores)\n            + "\\n\\nComprueba fecha y hora de Windows, Windows Update y el análisis HTTPS del antivirus."\n        )\n\n\nclass ActualizadorV105:\n    def __init__(self, g):\n        import customtkinter as ctk\n        self.g, self.ctk = g, ctk\n        self.root = g["ventana"]\n        self.transporte = TransporteSeguro(g)\n        self.lock = threading.Lock()\n        try:\n            g["panel_actualizaciones_admin"].pack_forget()\n        except Exception:\n            pass\n\n        panel = ctk.CTkFrame(g["tab_actualizaciones"], corner_radius=12)\n        panel.pack(fill="x", padx=22, pady=20)\n        ctk.CTkLabel(\n            panel,\n            text="Actualizaciones seguras",\n            font=("Segoe UI", 21, "bold"),\n        ).pack(anchor="w", padx=16, pady=(16, 4))\n        ctk.CTkLabel(\n            panel,\n            text=(\n                "Servidor oficial fijo · comprobación en segundo plano · "\n                "SHA-256 obligatorio · nunca se desactiva la verificación SSL."\n            ),\n            wraplength=850,\n            justify="left",\n        ).pack(anchor="w", padx=16, pady=(0, 10))\n        fila = ctk.CTkFrame(panel, fg_color="transparent")\n        fila.pack(fill="x", padx=10, pady=6)\n        self.boton_comprobar = ctk.CTkButton(\n            fila, text="Comprobar actualización", command=lambda: self.iniciar(False)\n        )\n        self.boton_comprobar.pack(side="left", fill="x", expand=True, padx=6)\n        self.boton_instalar = ctk.CTkButton(\n            fila, text="Descargar e instalar", command=lambda: self.iniciar(True)\n        )\n        self.boton_instalar.pack(side="left", fill="x", expand=True, padx=6)\n        self.progreso = ctk.CTkProgressBar(panel, mode="indeterminate")\n        self.progreso.pack(fill="x", padx=16, pady=(8, 5))\n        self.progreso.set(0)\n        self.estado = ctk.CTkLabel(\n            panel,\n            text="Listo. La comprobación no bloqueará la ventana.",\n            wraplength=850,\n            justify="left",\n        )\n        self.estado.pack(anchor="w", padx=16, pady=(3, 16))\n\n    def ocupado(self):\n        return bool(\n            self.g.get("cola_en_ejecucion")\n            or self.g.get("_PUBLICACION_V105_EN_CURSO")\n            or self.g.get("_BORRADO_V104_EN_CURSO")\n        )\n\n    def iniciar(self, instalar):\n        if self.ocupado():\n            self.g["messagebox"].showwarning(\n                "Actualizaciones", "Termina la publicación, cola o borrado antes de actualizar."\n            )\n            return\n        if not self.lock.acquire(blocking=False):\n            self.g["messagebox"].showinfo("Actualizaciones", "Ya hay una comprobación en curso.")\n            return\n        self.boton_comprobar.configure(state="disabled")\n        self.boton_instalar.configure(state="disabled")\n        self.progreso.start()\n        self.estado.configure(\n            text="Descargando y verificando…" if instalar else "Comprobando el servidor…"\n        )\n        threading.Thread(target=self._trabajo, args=(instalar,), daemon=True).start()\n\n    def _manifest(self):\n        bruto = self.transporte.obtener(\n            SERVIDOR + "/version?t=" + str(int(time.time() * 1000)),\n            {"/version"},\n            60,\n        )\n        datos = json.loads(bruto.decode("utf-8-sig"))\n        if not isinstance(datos, dict) or datos.get("channel") != "admin":\n            raise RuntimeError("Manifest inválido o de otro canal.")\n        version = str(datos.get("version", "")).strip()\n        if not version or not re.fullmatch(r"[0-9]+(?:\\.[0-9]+)*", version):\n            raise RuntimeError("El servidor no indicó una versión válida.")\n        return datos\n\n    @staticmethod\n    def _numero(valor):\n        return tuple(int(x) for x in re.findall(r"\\d+", str(valor))) or (0,)\n\n    def _trabajo(self, instalar):\n        try:\n            manifest = self._manifest()\n            remota = str(manifest["version"])\n            actual = str(self.g["version_actual_bot"]())\n            if self._numero(remota) <= self._numero(actual):\n                self._final("ok", "Ya tienes la última versión (V" + actual + ").")\n                return\n            if not instalar:\n                self._final(\n                    "disponible",\n                    "Versión disponible: V" + remota + "\\n\\n" + str(manifest.get("notes", "")),\n                )\n                return\n\n            url = _validar_url_oficial(manifest.get("download_url"), {"/download"})\n            sha = str(manifest.get("sha256", "")).lower().strip()\n            if not re.fullmatch(r"[0-9a-f]{64}", sha):\n                raise RuntimeError("La actualización no contiene una firma SHA-256 válida.")\n            contenido = self.transporte.obtener(url, {"/download"}, 140)\n            if hashlib.sha256(contenido).hexdigest() != sha:\n                raise RuntimeError("La comprobación SHA-256 no coincide. Archivo descartado.")\n            nombre = Path(str(manifest.get("filename", ""))).name\n            if not re.fullmatch(r"DIAGPROG5_V[0-9_.]+_[A-Za-z0-9_]+\\.py", nombre):\n                raise RuntimeError("Nombre de actualización no permitido.")\n            texto = contenido.decode("utf-8-sig")\n            compile(texto, nombre, "exec")\n            carpeta = Path(self.g["CARPETA_ACTUALIZACIONES"])\n            carpeta.mkdir(parents=True, exist_ok=True)\n            destino = carpeta / nombre\n            temporal = destino.with_suffix(".py.tmp")\n            temporal.write_bytes(contenido)\n            os.replace(temporal, destino)\n            self.root.after(0, lambda: self._confirmar(destino, remota))\n        except Exception as error:\n            self._final("error", str(error))\n\n    def _confirmar(self, destino, version):\n        self._terminar_controles()\n        if not self.g["messagebox"].askyesno(\n            "Instalar V" + version,\n            "La actualización está descargada y verificada.\\n\\n¿Quieres abrirla ahora?",\n        ):\n            self.lock.release()\n            self.estado.configure(text="V" + version + " descargada; instalación aplazada.")\n            return\n        try:\n            actual = Path(self.g["__file__"]).resolve()\n            backups = Path(self.g["CARPETA_DATOS_USUARIO"]) / "backups_actualizaciones"\n            backups.mkdir(parents=True, exist_ok=True)\n            shutil.copy2(\n                actual,\n                backups / (actual.stem + "_" + time.strftime("%Y%m%d_%H%M%S") + actual.suffix),\n            )\n            proceso = subprocess.Popen(\n                [sys.executable, str(destino)], cwd=str(destino.parent)\n            )\n            self.estado.configure(text="Abriendo V" + version + "…")\n\n            def comprobar_arranque():\n                self.lock.release()\n                if proceso.poll() is not None:\n                    self.g["messagebox"].showerror(\n                        "Actualizaciones",\n                        "La nueva versión se cerró durante el arranque. La actual seguirá abierta.",\n                    )\n                    self.estado.configure(text="La nueva versión no pudo arrancar.")\n                else:\n                    self.estado.configure(\n                        text="V" + version + " se está abriendo. Cierra esta ventana cuando veas la nueva."\n                    )\n\n            self.root.after(6000, comprobar_arranque)\n        except Exception as error:\n            if self.lock.locked():\n                self.lock.release()\n            self.g["messagebox"].showerror("Actualizaciones", str(error))\n            self.estado.configure(text="No se pudo abrir la actualización.")\n\n    def _terminar_controles(self):\n        self.progreso.stop()\n        self.progreso.set(0)\n        self.boton_comprobar.configure(state="normal")\n        self.boton_instalar.configure(state="normal")\n\n    def _final(self, tipo, mensaje):\n        def ui():\n            self._terminar_controles()\n            if self.lock.locked():\n                self.lock.release()\n            self.estado.configure(text=mensaje)\n            if tipo == "error":\n                self.g["messagebox"].showerror("Actualizaciones", mensaje)\n            elif tipo == "disponible":\n                self.g["messagebox"].showinfo("Actualización disponible", mensaje)\n        self.root.after(0, ui)\n\n\ndef _instalar_control_publicacion(g):\n    anterior = g["_publicar_wallapop_hilo"]\n    lock = threading.Lock()\n    g["_PUBLICACION_V105_EN_CURSO"] = False\n\n    def publicar_estable(*args, **kwargs):\n        if not lock.acquire(blocking=False):\n            try:\n                g["ventana"].after(\n                    0,\n                    lambda: g["estado"].configure(\n                        text="Ya hay una publicación en curso. No se ha iniciado otra."\n                    ),\n                )\n            except Exception:\n                pass\n            return False\n        g["_PUBLICACION_V105_EN_CURSO"] = True\n        inicio = time.monotonic()\n\n        def vigilar():\n            while g.get("_PUBLICACION_V105_EN_CURSO"):\n                time.sleep(30)\n                if not g.get("_PUBLICACION_V105_EN_CURSO"):\n                    break\n                minutos = int((time.monotonic() - inicio) // 60)\n                fase = str(g.get("fase_actual", "Navegador"))\n                try:\n                    g["ventana"].after(\n                        0,\n                        lambda m=minutos, f=fase: g["estado"].configure(\n                            text=f"Publicación trabajando · {m} min · fase: {f}"\n                        ),\n                    )\n                except Exception:\n                    break\n\n        threading.Thread(target=vigilar, daemon=True).start()\n        try:\n            return anterior(*args, **kwargs)\n        finally:\n            g["_PUBLICACION_V105_EN_CURSO"] = False\n            lock.release()\n\n    g["_publicar_wallapop_hilo"] = publicar_estable\n\n\ndef _simplificar(g):\n    # La rotación antigua fallaba y ya fue sustituida por Borrar antiguos.\n    try:\n        g["panel_rotacion_catalogo"].pack_forget()\n    except Exception:\n        pass\n    # Había dos zonas de diagnóstico/reparación; conservamos la pestaña dedicada.\n    try:\n        g["panel_reparacion_automatica"].pack_forget()\n    except Exception:\n        pass\n    try:\n        g["var_rotacion_activa"].set(False)\n        g["var_modo_prueba"].set(False)\n        g["switch_publicar_automaticamente"].set(True)\n    except Exception:\n        pass\n    g["_v102_rotacion_activa"] = lambda: False\n    g["_v102_vigilar_limite_catalogo"] = lambda: None\n    g["_v102_tick"] = lambda: None\n    g["modo_prueba_activado"] = lambda: False\n\n\ndef instalar_estabilidad(g):\n    _simplificar(g)\n    _instalar_control_publicacion(g)\n    g["_actualizador_v105"] = ActualizadorV105(g)\n'

def version(text):
    try:
        tree = ast.parse(text)
        for node in tree.body:
            if isinstance(node, ast.FunctionDef) and node.name == "version_actual_bot":
                return int(str(node.body[0].value.value))
    except Exception:
        pass
    return 0

def localizar(data):
    hallados = []
    vistos = set()
    for root in (data / "versiones", data / "actualizaciones", Path(__file__).resolve().parent):
        if not root.exists():
            continue
        for path in root.rglob("*.py"):
            try:
                resolved = path.resolve()
                if resolved in vistos or "bootstrap" in path.name.lower() or path.stat().st_size < 350000:
                    continue
                vistos.add(resolved)
                numero = version(path.read_text(encoding="utf-8"))
                if numero > TARGET_VERSION:
                    raise RuntimeError("Hay una versión posterior a V107. Se cancela para no retroceder.")
                if 86 <= numero <= TARGET_VERSION:
                    hallados.append((numero, path.stat().st_mtime, resolved))
            except (OSError, UnicodeError, SyntaxError):
                continue
    return max(hallados, key=lambda item: (item[0], item[1]))[2] if hallados else None

def instalar(data):
    data = Path(data)
    source = localizar(data)
    compile(MAIN_SOURCE, OUTPUT_NAME, "exec")
    compile(BORRADO_SOURCE, "borrado_antiguos_v104.py", "exec")
    compile(ESTABILIDAD_SOURCE, "estabilidad_v105.py", "exec")
    banner_name = "banner_dp5_limpio_v15.png"
    candidatos_banner = [data / "assets" / banner_name, Path(__file__).resolve().parent / banner_name]
    if source is not None:
        candidatos_banner.insert(0, source.parent / banner_name)
    banner = next((p for p in candidatos_banner if p.is_file()), None)
    if banner is None:
        raise RuntimeError("No se encontró el banner. La instalación anterior queda intacta.")
    versions = data / "versiones"
    versions.mkdir(parents=True, exist_ok=True)
    dest = Path(tempfile.mkdtemp(prefix="V107_" + time.strftime("%Y%m%d_%H%M%S") + "_", dir=versions))
    (dest / OUTPUT_NAME).write_text(MAIN_SOURCE, encoding="utf-8", newline="\n")
    (dest / "borrado_antiguos_v104.py").write_text(BORRADO_SOURCE, encoding="utf-8", newline="\n")
    (dest / "estabilidad_v105.py").write_text(ESTABILIDAD_SOURCE, encoding="utf-8", newline="\n")
    shutil.copy2(banner, dest / banner_name)
    if source is not None:
        for path in source.parent.glob("*.json"):
            if path.is_file():
                shutil.copy2(path, dest / path.name)
    return dest / OUTPUT_NAME

def main():
    root = Path(os.environ.get("LOCALAPPDATA") or os.environ.get("APPDATA") or Path.home())
    output = instalar(root / "DIAGPROG5")
    os.chdir(output.parent)
    runpy.run_path(str(output), run_name="__main__")

if __name__ == "__main__":
    try:
        main()
    except Exception as error:
        import tkinter as tk
        from tkinter import messagebox
        window = tk.Tk(); window.withdraw()
        messagebox.showerror("DIAGPROG5 · Actualización V107", str(error))
        window.destroy()
        raise
